"""定时触发调度器（docs/68，打包 N）。

`cron`＝最小 5 字段 cron 子集求值（纯函数、零新依赖，ADR T30）；
`engine`＝一个 tick 的派发判定（可全注入、零 IO）；`models`＝两档共用的记录与投影。

线程与存储不在此包内：装配在 `api/main.py` 的 lifespan，落库在两档 store。
**不解锁多副本**——槽位一次性认领只保证"同一槽至多派发一次"，审批 broker、事件等待、
灰度态与急停句柄仍是进程内（docs/62 L2 的同一口径）。
"""
