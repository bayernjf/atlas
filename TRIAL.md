# Atlas 试用操作指南（Runbook）

欢迎试用 Atlas——用自然语言/拖拽搭一条「AI 自动处理退款」流程。本指南约 15 分钟，不需要任何编程基础。

## 0. 你会看到什么

- **流程编辑器**（http://localhost:8000）：拖拽节点、自然语言生成流程、一键运行，实时看每个节点在干什么。
- **模拟商家后台**（http://localhost:8000/demo/shop，账号 demo / 密码 demo）：一个假的电商售后后台，里面有 5 笔待处理退款单，用来观察 AI 的处理结果。

试用过程中任何不对劲的地方，点编辑器右上角的 **「反馈」** 按钮告诉我们。

## 1. 启动（约 3 分钟）

前置条件：电脑上装好 [Docker Desktop](https://www.docker.com/products/docker-desktop/) 并保持运行。

在本项目目录执行：

```bash
docker compose up --build
```

看到 `Uvicorn running on http://0.0.0.0:8000` 后，浏览器打开 http://localhost:8000。

> 试用结束后回到终端按 `Ctrl+C`，再执行 `docker compose down`。
>
> 数据说明：本 Demo 的数据存在内存里，**重启容器即恢复初始状态**（这是试用版的有意设计，不是故障）。想手动恢复初始数据：
>
> ```bash
> curl -X POST http://localhost:8000/api/demo/reset
> ```

## 2. 三个场景

### 场景 A：AI 自动退款（约 3 分钟）

1. 打开 http://localhost:8000，编辑器里已有一条「退款审核」示例流程（触发 → AI 决策 → 执行退款）。
2. 顶部运行区选择退款单 **12345**（原因：商品破损，金额 ¥299），点「编译并运行」。
3. 观察画布：节点依次亮起，调试台显示 AI 的判断理由。
4. 打开模拟商家后台（demo/demo），刷新——**12345 已从待处理列表消失**，因为「质量问题且金额在限额内」，AI 直接退了款。

想验证的问题：**这个判断你认同吗？换成你的真实订单，规则合理吗？**

### 场景 B：敏感情况转人工（约 3 分钟）

1. reset 一下（上面的 curl 命令），或换一单 **12346**（原因：不想要了，金额 ¥5000）。
2. 运行流程。
3. AI 判断为「主观原因 / 金额超限额」，不会自动退款，而是**转人工审核**（订单状态变为 human_review，仍留在后台）。

想验证的问题：**AI 不越权、拿不准就找人，这个边界让你放心吗？**

### 场景 C：自然语言生成流程（约 5 分钟）

1. 点编辑器顶部「自然语言生成」，用大白话描述，例如：

   > 收到退款申请后，让 AI 判断退款原因和金额，合理就自动退款，不合理就转人工。

2. 生成的草稿会自动载入画布。你可以：拖动节点、点节点改配置、在 AI 节点的提示词里插入 `{{变量}}`。
3. 改完点「编译并运行」，选一单试试。

想验证的问题：**生成的草稿离你想要的有多远？你能自己改明白吗？**

## 3. 试用后反馈（2 分钟）

请把你的答案直接点编辑器右上角「反馈」发给我们，或当面告诉陪同人员：

1. 从启动到跑通场景 A，你花了多久？中间卡在哪？
2. AI 对 12345 / 12346 的判断，哪些认同、哪些不认同？
3. 场景 C 生成的草稿，需要改哪些地方才能用？
4. 画布上的运行进度和日志，你看得懂吗？
5. 如果这个产品能接你的真实店铺后台，你愿意继续试用吗？还缺什么？

谢谢！你的反馈会直接决定我们下一步做什么。
