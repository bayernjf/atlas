# Handoff 归档 — 2026-09-25

> 主文件「Recently shipped」5 条上限滚动下来的条目，保留**滚出前的原编号**。同批把主文件中 `Recently shipped N` 数字引用统一改为按批次命名，避免再因滚动漂移。

## 滚出自 Recently shipped 的条目

5. ✅ **打包 E 收口（docs/58，2026-09-24，dev 已 push；后端、零迁移）**：E-1 webhook 出站 HMAC-SHA256 签名（X-Atlas-Timestamp/X-Atlas-Signature，基串 {ts}\n{raw}）；E-2 IM markdown 富文本与 @人（钉钉/企微/飞书三家官方报文，mentions 归一化）；E-3 webhook/IM 多 URL 群发（1-20、per-URL 记录、EGRESS fail-fast、全成才写 _messages）。门：后端 1713/69/0、im smoke 27/27。

### 2026-09-25 治理批（#69）滚出的 5 条

2. ✅ **第三次项目级上线复审落成 docs/63（2026-09-25，纯 docs 原子）**：判定＝**技术验证 MVP 达到、生产 MVP 未达到（PROD 不 GO）**；新增 8 条阻断 S1–S8、3 条缓做 D38/D39/D40、docs/08 A 组 3 批候选。**本次是仓库第一次成体系复核安全与运维面**（docs/29/34 审"能力有没有落"，从未审"不安全会怎样"）。详见 Active work #64。
2. ✅ **打包 H 五项落码收口（docs/61，2026-09-25；push 状态见本文件顶部收口块；零新依赖/迁移 027·028/无 ADR/零新 REST 端点·错误码；D11/D20/D26 部分取回不解除；门：后端 1799/105/0、PG 直连 89、前端 722/2/53 文件、oxlint 0/6、build ✓（仅 +4.4 kB，RangePicker 已回退为原生 datetime-local）**：H1 前端接线后端编译 422 的稀疏 `locations` 侧车（`bbb9a2c`/`eea76ad`，**后端零改动**；`CompileIssue`/`buildCompileIssues`/`CompileValidationError`，`message` 逐字保持既有 join 串；快照存 `validationStore` 并以 `computedRevision` 门控陈旧；Problems 面板三元组去重、本地优先、「后端编译」标记、复用 `[data-pointer]` 闪烁；给 `t()` 加可选 `lng` 使语言成为真依赖）；**顺带修掉自 M4 起从未生效的点击定位 bug**（`.side-card` 被三面板共用，`adb9b7c`）；H2 已决审批历史 PG 化（`collaboration/history.py` 两档＋迁移 027，`33a5998`/`528f6b1`/`e380f0b`；四决策来源经两方法共享 `_record_history`、写失败只 warning 绝不阻断放行；投影 `createdAt` float→UTC ISO 破坏性变更已执行、未触发退化预案）；H3 审计 actor/时间过滤＋游标分页（`8a9f7df`/`36a3cbb`/`a07794c`；`AuditEvent.seq` 两档共用游标、时间边界按时刻比而非 TEXT 字典序、PG 走 `at::timestamptz`＋CAST；零迁移零新端点）；H4 影子运行 PG 化（`pg_shadow.py` 与 `ShadowStore` 一比一、迁移 028、四子模型 JSONB、`inputs` 改可空、REST 与前端零改动，`2bb6d3c`/`3eac4af`）；H5 三处「已 PG 化但注释写不 PG 化」勘误（`55c1553`）。冒烟 h1 12/12、h3 22/22（8 截图 `docs/assets/h1-*.png`·`h3-*.png`）；七处落码偏差带文件:行号见 docs/61 顶部注记；D11/D20/D26 不解除，合 main 须用户授权。**收口后文档口径同步（紧随其后的 docs 原子）**：docs/03·04·06·09·11·12·14·30＋README＋CONTRIBUTING 清掉已失效正文——「影子运行/报告/审批历史不 PG 化」「decided `createdAt` 为 float epoch」「审计页零后端改动」「静默/值班不 PG 化」（并写明告警/静默/值班同库的类是 `PgMonitoringStore`，无独立 `PgAlertStore`），H3 时间边界全库统一为**按时刻比**（docs/61 注记 ⑥，非原契约 TEXT 字典序），09 树内补 `pg_shadow.py`/`pg_reports.py`/`history.py` 三行，docs/30 §6 追加「重启后什么在」修正注（021/022/024/025/026/027/028 已在库），docs/04 §5.6 新增**「日期控件口径」**记录 RangePicker 回退的三条硬事实与重开条件，README/CONTRIBUTING 补 M11 之后的 docs/27–61 批链并声明模块清单以 docs/09 为准。
3. ✅ **打包 F 收口（docs/59，2026-09-24，dev 已随 c1db210 push；迁移 024）**：F-1 告警规则模板市场 v1（4 内置只读模板＋两只读 GET＋一键应用复用 PUT rules，无新写端点）；F-2 静默/值班/新建告警 assignee PG 化（仅 PG 档，迁移 024 两表、移除进程内 OpsStore 兜底、内存档不动）。门：后端 1734/77/0、PG 44、前端 675/2/50、build 过、PG live smoke 20/20。
4. ✅ **i18n 第二批：Graph DSL 422 码化（docs/17 遗留①，2026-09-24，dev 已 push）**：graph 422 全量码化（图级/节点/边/变量/条件/循环/等待），前端按码映射中/英＋定位（节点 id/字段/边/变量），旧中文 message 兜底保留；i18n.test 静态守护扩至该键表。〔2026-09-25 更正〕本条原写「graphErrors.ts」与「守护扩 graphErrors」均不确——该文件名在全 git 历史中不存在，实际承载处是 `frontend/src/lib/apiClient.ts:53` 的 `t('dsl.<CODE>', { ns: 'validation' })`（`validationItemMessage`，逐条与整表共用）；且 `validation` 这个 namespace **至今不在 `PARITY_PAIRS` 里**，详情与收口路径见 docs/14 D12 的 2026-09-25 注记。门：后端 1728/69/0、前端 673/2/50。
5. ✅ **i18n 第一批：认证/L1 英文态（docs/17，2026-09-24，dev 已 push）**：登录/403/会话过期/认证错误码 zh/en；前端 L1 校验错误码化（required/range/type/pattern/enum 等），FormRenderer/CodeEditor/各 schema 面板按码展示；i18n.test 静态守护（缺失键/零硬编码中文/后端中文 detail 豁免）。

### 2026-09-25 打包 L 收口滚出的 1 条

3. ✅ **打包 I 全批收口＝L1 单副本护栏＋L2 挂起帧一次性认领（docs/62，2026-09-25；立项与 L1 `57a2477`/`4877677`＋docs 声明，L2 同日第二批 `af38176`/`593bed0`/`df5a18c`＋docs 收口；新增迁移 029）**：实测出的缺陷级阻断项（多副本＝重复扣款）落成两道防线——部署侧三道闸让第二进程起不来，数据侧一条 `UPDATE ... WHERE resumed_at IS NULL` 让"谁可以越过挂起点继续执行下游"只有一个答案。**端到端硬门翻绿**：`scripts/dev/multi_instance_resume_recon.py` 退出码 1→0（一次审批下游恰落一次；改造前通过×1／拒绝×1 双写）。**不解除 D19/D20/D27/D31/D32、单副本约束与三道闸一条都不撤**（只拆最贵那颗雷）；新增缓做 D37（前端未认 `superseded` 终帧）；D36（认领后崩溃＝永久 suspended、不自动重放）是本次有意引入的收紧。详见 Active work #63 与 Quality gate 2026-09-25 打包 I 条。

### 2026-09-25 打包 M 收口滚出的 1 条

3. ✅ **打包 J 全批收口＝P0-1 安全闸门＋P0-2 核心完整性＋P1-1 门禁（docs/64，2026-09-25；用户拍板「推进任务」立项，11 个功能原子＋2 个修正原子＋docs 收口原子）**：P0-1 `4ab6edc`（ATLAS_ENV fail-closed，prod 缺两密钥拒绝启动）/`0400be8`（邮件决策 token 绑收件人）/`f0e50ed`（prod 不播种＋种子口令 403）/`ef3451a`（登录确定性＋用户名全局唯一）；P0-2 `f1c54aa`（启动打印决策器与降级状态，不再静默）/`405161e`（跨 human_approval 真实退款全链 e2e，docs/08 criterion 4「或」字口径已补强）/`b220ba3`（waits/tasks 操作台 UI，D40 取回）；P1-1 `9b4ec9a`（CI postgres service＋integration job＋audit `seq` 红修）/`c6a1afe`（三处 litellm timeout+max_tokens）/`de023ab`（webhook 线程池＋1MB body 上限＋事件循环解耦）/`4b0794a`（demo 模拟面 prod 默认 404）。**J-3b limit `le=` 落码勘误**：本仓全部 limit 端点已有 clamp/reject 门禁，`le=` 统一覆盖会把 clamp 破坏成 reject（三测断言失败），故还原裸默认不引入（docs/64 §10）。**三道门**：后端 **1834/108/0**、PG 直连 **1941/1/0**（原 1 红消除）、前端 vitest **728/2/54 文件**、oxlint 0/6 既有、build ✓。D38/D39 与 D36 不解除；合 main 须用户授权。
3. ✅ **打包 K 全批收口＝P1-2 运维批（docs/65，2026-09-25；用户「继续推进任务」拍板立项，契约 docs-only `c965d22`＋4 个功能原子＋docs 收口原子）**：K-A retention 8 表按保留期淘汰＋过期会话全量清扫（`5aa51ca`：`PgBackend.prune_expired`，`runs` 仅终止态/`interruptions` 仅已认领，挂起态与未认领帧永不删；`scripts/ops/run_retention.py` 带 `--dry-run`；lifespan 启动钩子失败只 warning）；K-B 备份轮转/异地（`2d3ec70`：`ATLAS_BACKUP_KEEP` 默认 7 份轮转、`ATLAS_BACKUP_REMOTE_DIR` 拷贝异地，BSD head 兼容计数法，fake-docker 冒烟 6 份删 3 留 3 通过）；K-C `/metrics` 收口（`0082b1e`：prod fail-closed，未配置 `ATLAS_METRICS_TOKEN`→404、配置后须 Bearer 否则 401，非 prod 行为不变）；K-D 日志 formatter＋request-id（`d171639`：`observability/logging.py` UTC ISO formatter＋`X-Request-Id` 响应头/日志字段，异常路径收窄为日志关联〔docs/65 §5 落码偏差〕）。**三道门**：后端 **1849/114/0**、PG 直连 **1962/1/0**（含 K-A 6）、前端零改动沿用 **728/2/54**、oxlint 0/6、build ✓；测试 U840–U859 转正式。D38/D39 取回不解除（D11 OTel 仍缓做；定时调度由部署侧 cron 自建）；D36 卡住帧 reconcile 仍具备开工条件、未立项；合 main 须用户授权。
