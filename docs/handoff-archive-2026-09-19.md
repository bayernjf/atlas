# Handoff 归档补遗 — 2026-09-19

本文件保存 2026-09-19 自 handoff「Recently shipped」按 5 条上限整块滚出的历史变更，原条目编号保留；主文件里「Recently shipped N」的交叉引用按本原号定位。

---

5. **feat(routing/release/monitoring/frontend): M9 落码收口——入站 Router 三段分桶 + 灰度发布 + 指标门控自动回滚（题二最后里程碑，2026-09-18；立项 `ce57355`、批 1 `c863f86`/`6e3f74a`/`60d885b`/`57b9966`、批 2 `1dc6314`/`1380cff`、批 3 `dcd40d1`/`7f2b503`、批 4 `d6223ed`/`d53de4b`/`d7f0c77`、收口 `7bdbe72`/`67f82fa`/`e014cdf`/`97c4afb`；08 M9 立项条+落码收口条、ADR T22、U55–U59 转正式，契约 03 rollout_config/route_decision/release_gate/business_metrics、04 §5.11/§5.13/§5.16、06 §6.11/§6.17、09 routing/ 包位、12 §3.12+REST、13 U55–U59、14 D26/D32、20 §4.4/§5/§8）**——**题二 M1–M10 主线至此全闭合**，零新依赖、后端纯 stdlib。四块：① 新包 `routing/`（models RolloutConfig 判别联合规则固定序 internal→lowValueBucket→canary→full + Router 纯函数三段分桶〔租户 allowlist→payload.amount<=200 低金额桶→订单 id 哈希稳定百分比，同订单不跳版本，桶键缺失 fail-safe 落 stable〕+ RoutingStore per-tenant 状态机 idle/canary/full/rolled_back 与 traffic 计数 + gate.evaluate_after_run）；② 发布流批量回放门禁（recording/gate.run_release_gate 复用 replay，POST release-gate 只跑不发布、publish 可选 {gate:true} blocked→409 携 GateReport 不产版本；GateReport case 行无 diff_keys、差异并入 note）；③ monitoring 业务三率（business.py extract_business 以 shop 终态 refunded/human_review 为准、expected_amount 取 trigger/event payload.amount，demo shop 不回金额则全额回退、沙盘金额差异率恒 0）+ Alert.action 与 rollout_gate 规则，门控仅 canary+candidate run+autoRollback 且样本≥minSamples 才求值，任一越阈一次 rollback(actor=auto)+critical 告警带 action{rollback,from,to,reason}，**只自动回滚、放量全手动（promote 仅手动，全仓无自动 promote）**，回滚不改外部已发生事实、无补偿；④ 前端 ReleaseModal（批量回放逐例报告+门禁通过才发布）+ RolloutModal（状态/三动作/五段流量计数/四段规则/门控表单/模拟入站事件看分流落点）+ Editor 草稿/发布版选择与发布灰度按钮 + Monitoring 业务三率卡/per_version 表/告警 action 徽标。**pin-to-version 复用 M6 不可变快照+M5b 中断帧冻结，无新机制**（U56 实测 canary 中挂审批→回滚→resume 续跑仍 candidate、新事件落 stable）。**计划外补件** `PUT /api/graphs/{id}` update_draft（覆盖同图 latest 草稿不新建 id、不动发布版，发布流对同一 graph 发 v2 的闭环前提；d6223ed）。event 路由生命周期实测：仅 publish 未 PUT/start 入站事件 409，start 需≥2 发布版（stable=versions[-2]/candidate=versions[-1]），rolled_back 为终态不能再 start（重配规则需新图），重复回滚幂等 200。收口：m9_smoke.py **32 断言全过**、BENCHMARK Router 10k resolve **p50 0.0041ms/p99 0.0450ms（约 242000 resolves/s）**、真实浏览器全链路（start canary→事件落 v2→manual 100% 越阈自动回滚→新事件回落 v1）**23 条 console 0 error/0 warning**、4 截图入库；e014cdf 修 Editor draftGraphId 集成缺陷（录制用例 graph_id 与门禁筛选错位致门禁恒 skipped）。三道门：后端 **602 passed/13 skipped**、前端 oxlint 0 error（2 既有 warning）/ **vitest 396 passed/2 skipped/34 文件** / build 过。**不解除 D32**（真实生产发布/多实例，随 D5/D6/D10b/S1）、**D26 仅取批量回放+跨版本 diff**（用例集报告/影子/Mock/持久化仍缓做）。**PG 路径本环境未验证**（无 DATABASE_URL：PgGraphStore.update_draft、RoutingStore PG 装配、business JSONB/迁移 002·004·005 仅列对齐审查，在 13 skipped 内；验证 ATLAS_RUN_INTEGRATION=1+DATABASE_URL）。已同步 origin/dev。



---

## 第二次滚出（2026-09-19 C 包收口）：原主文件 #5（D26 用例集报告 v1）

5. **feat(recording/api/frontend): D26 用例集报告 v1 落码收口——批量回放报告沉淀 + 通过率历史趋势（2026-09-18；立项 `71153a8`、后端 `9dc9690`/`5c812c5`、前端 `f5c789c`；08 D26 立项条+落码收口条、U60 转正式，契约 03 `release_report`、04 §5.11 末、09 reports.py、12 §3.7/§5、13 U60+§9、14 D26、20 §4.4）**——M1–M10 主线全闭合后首个缓做项提前取回，零新依赖、不新增 ADR、gate.py 保持纯函数。四块：① 新模块 `recording/reports.py`（ReleaseReport/ReportCaseRow pydantic + ReportStore 进程内 ring 100/租户，record/list_summary/get/reset，rr-N 租户级单调计数且 reset 归零，pass_rate=round(passed/total,4)、total=0 为 null，不进 Repository 抽象/不 PG 化照 RoutingStore 先例）；② 两触发沉淀（release-gate 端点 trigger=manual、publish `{gate:true}` trigger=publish-gate，通过/blocked 均沉淀、409 报告体带 id、skipped 也沉淀，GateReport 响应纯超集加 id，TenantServices memory/PG 两档挂 report_store、reset 清报告而录制用例保留）；③ 两 GET（release-reports 倒序摘要剥 cases、release-reports/{rid} 详情含逐例 cases，read 角色，图不存在/跨图/跨租户 404）；④ 前端 ReleaseModal ghost Collapse「历史报告（通过率趋势）· N 条」（时间 MM-DD HH:mm、蓝手动门禁/紫发布门禁 Tag、通过率 Progress 成功绿/blocked exception 红、null 显「未覆盖」灰 Tag、结论 Tag，展开行懒加载详情嵌套复用 GateCaseRow 逐例 ✓/✗/note 表、未加载显 Spin），apiClient ReleaseReport(+Summary) 与 list/get、release.ts GATE_CONCLUSION_META/REPORT_TRIGGER_META/reportConclusion/reportTimeLabel 纯函数（loadGate 与 doPublish finally 各刷新历史、loadHistory catch 静默置空）。三道门：后端 **615 passed/13 skipped**（M9 基线 602，+13＝6 存储+7 API）、前端 **404 passed/2 skipped（34 文件）**（基线 396，+8）、oxlint 0 error（2 warning 经 stash 对比为既有）/tsc+vite build 过。API 全链路沙盘 `scripts/dev/d26_smoke.py` **24 断言全过**（两触发/倒序/摘要剥 cases/详情/跨图与图不存在 404/reset 清图清报告留用例；reset 需 administer 先登录、脚本 DELETE 遗留用例自隔离），`scripts/dev/d26_ui_seed.py` 供浏览器对已存图造完整历史（inputs.approvals 传 dict）；真实 :8000+:5174 浏览器冒烟 graph-3 六条历史（manual/publish-gate × 未覆盖/通过/未通过）+ 展开 rr-9 懒加载逐例差异 note，2 截图 `docs/assets/d26-report-{trend,detail}.png`。**不解除 D26**：影子模式/Mock 工具响应/用例编辑参数化/subgraph 快照内联/PG 持久化多租户/跨图看板/定时 CI/导出仍缓做（14 注记，触发条件不变）。


---

## 第三次滚出（2026-09-19 A 包收口）：原主文件 #5（docs/25 行业趋势盘点）

5. **docs(plan): 新增 docs/25 行业趋势对照与能力现状盘点（2026-09-19；零代码零依赖）**——外部行业盘点表（MCP/A2A 生态协议、记忆/长期上下文、Agent eval、NL chat-first、Durable execution 五项）逐条对照 dev HEAD `a9a2414` 实际状态重核：① MCP/A2A 仍无（仅仓内 harness 契约 + M7 进程内 Envelope，无对外协议出入口）、② 记忆/长期上下文仍空洞（`memory/` 仅 PG 连接骨架+settings，pgvector 扩展已建但无记忆表/向量检索/长期上下文读写；六类实体中唯一未动者）、③ Agent eval 仍无（仅 L1/L2/L3 配置校验 + recording 确定性回放 + D26 通过率趋势，无 LLM 输出打分/离线评测集/业务质量指标）、④ NL chat-first 半推进（`llm/nl_generate.py` 单轮 NL→草稿已落，多轮对话式配置仍空）、⑤ Durable execution 已过时（M5b `storage/` PG 中断帧+recovery 扫描器+`run_graph(resume=)` 已落，单实例重启可恢复实测通过）。结论：三个真实空洞=记忆/eval/MCP-A2A；本盘点非契约非 ADR，不直接触发立项，仅作下一轮方向参考。00 索引已登记。
