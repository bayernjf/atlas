# 题述原题与方案作答（题一 / 题二）

> **文档性质**：工程推导文档（题卷 + 答卷合订；同 10–13、17–20，非规格唯一事实源，与 01-08 冲突时以 01-08 为准）。
> **内容与边界**：§1 **原题照录**（2026-09 两道设计题，原文不改一字）；§2/§3 是对应答卷，**只到方案级**——架构划分、数据流、Schema 形状、伪代码、机制与策略——不落具体实现代码。
> **与 19/20 的分工**：[docs/19](docs/19-Schema驱动配置内核与多智能体端到端方案.md) = 本答卷的完整推导版（每条主张附 Atlas 现状「已有/缺口」核对）；[docs/20](docs/20-题述方案工程化落地路线.md) = 答卷在本仓库分几期落码的工程路线（M0 已落、M1–M10 未立项）；**本文 = 题面逐条作答的阅读版**，三份文档答案一致，细节以 19 为准、排期以 20 为准。

---

## 1. 原题照录

> 题一：Schema 驱动编辑器的“动态表单 + 变量补全 + 图级校验”内核
>
> 说明：编辑后台要求所有节点、工具、技能、记忆、部署、交互模板都由 JSON Schema 描述，前端根据 Schema 自动生成配置表单。请设计一个 Schema 驱动的前端配置内核，要求支持：
>
> 1、嵌套对象、数组、枚举、条件显示；
> 2、自定义控件扩展；
> 3、变量自动补全 {{...}}；
> 4、跨节点依赖校验：变量引用上游节点输出，若不存在则高亮；
> 5、图级校验：不可达节点、循环依赖、无条件分支缺失；
> 6、实时校验、错误定位、性能优化。
>
> 请给出：
> 核心模块划分；
> 数据流；
> 关键 Schema 示例；
> 变量作用域索引设计；
> 校验分层与伪代码；
> 性能优化方案。
>
> 题二：多智能体协同 + 部署 + 交互模板的端到端设计
>
> 说明：以“电商退款处理运营体”为例，设计从自然语言需求到发布灰度的端到端方案。要求包含：
> 1、自然语言生成流程、技能推荐、自动组合；
> 2、多智能体协同：主协调、客服 Bot、物流 Bot；
> 3、通信协议、任务分发、超时、幂等、冲突解决；
> 4、人机审批与交互模板：审批卡片、变量绑定、渠道适配；
> 5、部署：Webhook、IM、网页嵌入、灰度发布、自动回滚；
> 6、可观测：链路追踪、指标、告警、回放。
>
> 请给出：
> 总体架构；
> 关键流程；
> 核心 Schema；
> 冲突解决策略；
> 灰度与回滚机制。

---

## 2. 题一作答：Schema 驱动配置内核

**一句话方案**：异构实体（节点/工具/技能/记忆/部署/交互模板）共用一套「**Schema 注册表 → 递归表单 → 拓扑作用域补全 → 三层校验**」机制。其中 `{{}}` 模板引用是**横切在 JSON Schema 之外的一层**（标准 JSON Schema 只看得到 string，看不到字符串里的引用），必须由独立的模板分析器 + 作用域索引承担。

题面六项要求在答卷中的位置：

| 题面要求 | 答卷机制 | 详见 |
|---|---|---|
| 1 嵌套对象/数组/枚举/条件显示 | Data Schema 的 properties 递归 / items / enum / if-then + UISchema 条件 | §2.1、§2.3 |
| 2 自定义控件扩展 | WidgetRegistry（name→component 唯一扩展点） | §2.1、§2.3 |
| 3 变量自动补全 `{{...}}` | ScopeIndex.visibleAt + variable-input 控件 + TemplateAnalyzer | §2.4 |
| 4 引用上游输出不存在则高亮 | L2 模板引用校验四码 + token 级 Diagnostic | §2.5 |
| 5 不可达/循环/分支缺失 | L3 图级校验（BFS + 环检测 + branchCompleteness） | §2.5 |
| 6 实时校验/错误定位/性能 | 前后端分层 + Diagnostic 定位 + 增量调度七项手段 | §2.5、§2.6 |

### 2.1 核心模块划分

```
┌─────────────────────────────────────────────────────────┐
│                    Editor Runtime                       │
├──────────────┬──────────────┬───────────────────────────┤
│ Schema 层    │ 渲染层       │ 校验/智能层               │
├──────────────┼──────────────┼───────────────────────────┤
│ SchemaRegistry│ FormRenderer│ ValidationEngine          │
│ (实体×来源)   │ (递归下降)   │  ├ field: JSON Schema     │
│ MetaSchema    │ UISchema     │  ├ template: {{}} 扫描    │
│ 自定义keyword │ WidgetRegistry│ └ graph: 拓扑分析        │
│               │ (控件注册表) │ ScopeIndex (作用域图)     │
│               │ Visibility   │ Diagnostics (诊断模型)    │
│               │ CondResolver │ IncrementalScheduler      │
└──────────────┴──────────────┴───────────────────────────┘
```

| 模块 | 职责 | 关键设计 |
|---|---|---|
| SchemaRegistry | 按实体类型（节点/工具/技能/记忆/部署/卡片）装载 schema | 多来源：内置节点随包发布；工具/技能参数 schema 来自适配器能力声明；部署/卡片来自渠道插件。注册表只持有引用+版本，不复制 |
| MetaSchema | 约束「schema 本身合法」 | 规定允许的 JSON Schema 子集 + Atlas 扩展 keyword（一律 `x-` 前缀） |
| FormRenderer | data + uiSchema → 控件树 | object 递归分组、array 增删行、enum→Select、oneOf/if-then→条件区块；不可变更新产出下一份 config |
| WidgetRegistry | 控件名→组件的唯一扩展点 | 内置 text/number/select/textarea/json/code/expression/variable-input 八件；业务侧 register 自定义控件 |
| UISchema / CondResolver | 布局与条件显示 | `ui:order`、`ui:widget`、`ui:hidden: "expr"`；可见性表达式声明依赖、响应式求值，不做命令式 show/hide |
| TemplateAnalyzer | 横切层：扫描所有字符串值中的 `{{...}}` | 与 JSON Schema 正交——Schema 只看到 string，引用由这层提取 |
| ScopeIndex | 变量作用域索引 | 图拓扑 + 各节点 output schema 的派生物（§2.4） |
| ValidationEngine | 三层校验 + 诊断聚合 | field / template / graph 三层（§2.5） |
| IncrementalScheduler | 脏标记传播与调度 | 编辑 → 失效范围计算 → 分层重算（§2.6） |

### 2.2 数据流

```
实体选择(节点类型/工具)
   │ 1. 取 schema        SchemaRegistry.get(kind, version)
   ▼
[Data Schema] [UI Schema] ──► FormRenderer 递归建控件树
   │                              │
   │                       用户编辑（受控值）
   ▼                              ▼
config draft (Immutable) ──► IncrementalScheduler
                                  │ 局部失效
            ┌─────────────────────┼──────────────────────┐
            ▼                     ▼                      ▼
     FieldValidator        TemplateAnalyzer         GraphAnalyzer
   (JSON Schema 数据约束)  extractRefs→ScopeIndex   (拓扑/可达/环/分支)
            └─────────────────────┼──────────────────────┘
                                  ▼
                          Diagnostics[] 统一诊断流
                                  │
              ┌───────────────────┼────────────────────┐
              ▼                   ▼                    ▼
        字段红字/边框        token 高亮(内嵌)      画布节点角标+边高亮
              └───────────────────┼────────────────────┘
                                  ▼
                    Problems 面板（点击定位 nodeId+字段+token）
保存/编译 ──► 后端同一规则集权威校验（422 聚合）  ← 不信任前端
```

两条要点：

1. **编辑流（体验）与提交流（权威）共用规则定义、分开执行**：前端负责实时提示，后端保存/编译期用同一规则集兜底，防绕过。
2. 规则以可序列化形式维护（TS/Python 各一份同构实现，或后端下发 schema），不把后端校验器整个搬进前端。

### 2.3 关键 Schema 示例

约定：标准 JSON Schema 做数据约束，Atlas 扩展一律 `x-` 前缀。

**(a) 节点 Data Schema（human_approval 目标形态）**——同时示范嵌套约束、枚举、条件必填、`x-` 扩展与输出声明：

```json
{
  "$id": "atlas/node/human_approval/v1",
  "type": "object",
  "required": ["summary", "approver", "timeoutSeconds"],
  "properties": {
    "summary":     { "type": "string", "minLength": 1, "x-variable": true },
    "approver":    { "type": "string", "x-widget": "approver-picker" },
    "timeoutSeconds": { "type": "integer", "minimum": 10, "maximum": 3600 },
    "onTimeout":   { "enum": ["approve", "reject"], "default": "reject" },
    "approvedTarget": { "type": "string", "x-ref": "nodeId" },
    "rejectedTarget": { "type": "string", "x-ref": "nodeId" }
  },
  "allOf": [
    { "if": { "properties": { "onTimeout": { "const": "approve" } } },
      "then": { "required": ["approvedTarget"] } }
  ],
  "x-outputSchema": {
    "type": "object",
    "properties": {
      "decision":  { "enum": ["approved", "rejected"] },
      "resolvedBy":{ "enum": ["human", "input", "timeout"] },
      "target":    { "type": "string" }
    }
  }
}
```

题面四类形态的落点：**嵌套对象** → `properties` 递归；**数组** → `items` + array widget；**枚举** → `enum` → Select；**条件显示** → 影响数据合法性的用 `if/then/allOf`，只影响观感的用 UI 条件（见 (b)）。分工原则：**影响数据合法性的用 if/then，只影响观感的用 UI 条件**。

**(b) UI Schema（与数据解耦）**：

```json
{
  "ui:order": ["summary", "approver", "timeoutSeconds", "onTimeout",
               "approvedTarget", "rejectedTarget"],
  "summary":       { "ui:widget": "tpl-textarea", "ui:rows": 3 },
  "approver":      { "ui:widget": "approver-picker" },
  "timeoutSeconds":{ "ui:group": "timeout" },
  "onTimeout":     { "ui:widget": "radio", "ui:group": "timeout" }
}
```

**(c) 工具能力声明（让工具参数也驱动表单）**——inputSchema 驱动表单、outputSchema 驱动下游作用域：

```json
{
  "adapterId": "database", "tool": "database/query",
  "permission": "read", "idempotent": true,
  "inputSchema": {
    "type": "object",
    "required": ["sql"],
    "properties": {
      "sql":   { "type": "string", "x-widget": "sql-editor" },
      "params":{ "type": "object", "x-variable": true,
                 "description": "绑定参数 :name → {{路径}}/字面量" },
      "limit": { "type": "integer", "minimum": 1, "maximum": 1000, "default": 500 }
    }
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "columns":   { "type": "array", "items": { "type": "string" } },
      "rows":      { "type": "array", "items": { "type": "object" } },
      "row_count": { "type": "integer" },
      "truncated": { "type": "boolean" }
    }
  }
}
```

**(d) 自定义控件契约（TypeScript 形状，仅声明不实现）**：

```ts
interface WidgetProps<T = unknown> {
  value: T
  onChange: (next: T) => void
  schema: JSONSchema          // 该字段的 schema 片段
  uiSchema: UISchemaNode
  scope: ScopeQuery           // 控件可查当前作用域（variable-input 用）
  diagnostics: Diagnostic[]   // 命中本字段的诊断（含 token 区间）
  renderMarkers?: (ranges: TokenRange[]) => ReactNode  // {{}} 高亮
}
registerWidget('approver-picker', ApproverPicker)
registerWidget('tpl-textarea', TemplateTextArea)       // 内含补全弹层
```

### 2.4 变量作用域索引设计

**两层结构**：

```
ScopeIndex {
  symbols: Map<path, SymbolInfo>   // "http-1.result.status" → {type, source, nodeId}
  visibleAt(nodeId): Set<path>     // 该配置位置可见的符号
  reverseDeps: BidirectionalGraph  // path ↔ 引用它的字段/节点（失效传播+数据环检测）
}
SymbolInfo = {
  source: 'trigger' | 'global' | 'node' | 'subgraph' | 'secret' | 'env'
  type:   JSON type 片段（来自 outputSchema）
  nodeId?: string
}
```

**可见性规则（沿拓扑推导，不是全局平铺）**——对编辑位置节点 `N`：

1. `trigger.context.*`、`global.*` 恒可见；
2. **控制流上游**：从 N 沿入边反向 BFS 可达节点的 outputs——引用下游/并行另一支不合法；
3. 结构投影：
   - `condition`：`branch / target / evaluation`；
   - `loop`：体内额外可见 `{{loop-x.index}}`，体外不可见；
   - `parallel`：`status` 静态可见；`result.<分支入口节点id>.*` 仅汇聚点之后可见；
   - `subgraph`：`outputs.*`（深层路径依赖子图 output schema 展开）；
   - `human_approval`：`decision / resolvedBy`；
4. 受限来源单列：`secret.*`、`env.*` 只对标记 `x-secret-allowed` 的字段可见，不回显明文。

**构建算法**：拓扑序一遍扫描，每个节点可见集 = 全部前驱可见集的并（分支汇聚点取并）∪ 自身 output，再叠加结构规则，O(V+E)。作用域来源不限于图节点——工具/技能/记忆/部署/卡片实体的配置表单，候选源同样含 trigger payload、全局变量、上游 outputs、子图 outputs、密钥/环境变量。

**引用解析与诊断**（`{{a.b[0].c}}` 分级）：

- 节点不存在 → `REF_NODE_NOT_FOUND`；
- 节点存在但不在可见集 → `REF_NOT_IN_SCOPE`；
- 路径在 outputSchema 中不存在 → `REF_PATH_NOT_FOUND`；
- 类型与使用点不符 → `REF_TYPE_MISMATCH`（warning 级；outputSchema 缺省时降级为仅存在性检查——这是题面第 4 条的硬要求下限）。

另有**数据依赖环**（A 引用 B 输出、B 又引用 A）与控制流拓扑环分开建图、分开检测。

### 2.5 校验分层与伪代码

**结构化诊断模型**（错误定位的载体）：

```ts
Diagnostic = {
  severity: 'error' | 'warning',
  layer: 'field' | 'template' | 'graph',
  code: string,                       // REF_NODE_NOT_FOUND ...
  message: string,                    // 中文，i18n key 同源
  loc: { nodeId?: string,
         pointer?: string,           // RFC6902 JSON Pointer，定位到字段
         token?: { start: number, end: number, raw: string } },
  quickFix?: Fix[]                   // 改名/删除悬空引用/补 default
}
```

**三层校验伪代码**：

```
validate(graph, changed):
  diags = []
  # L1 字段级：数据约束（仅重算变更实体）
  for node in changed.nodes:
    schema = SchemaRegistry.get(node.kind)
    diags += validateJsonSchema(schema, node.config).map(toDiag(node.id))

  # L2 模板引用级（横切）
  scope = ScopeIndex.of(graph)                    # 增量重建
  for (nodeId, pointer, value) in allStringFields(graph, changed):
    for {raw, range} in extractRefsWithRange(value):
      sym = scope.resolve(raw, visibleAt=nodeId)
      if !sym: diags += {layer:'template', loc:{nodeId,pointer,token:range}, ...}
      else if typeMismatch(sym, schemaAt(pointer)): diags += warning(...)

  # L3 图级
  g = adjacency(graph)
  diags += BFS(trigger, g).unreached.map(UNREACHABLE)            # 不可达节点
  diags += detectCycles(removeLegalLoopBackEdges(g))             # 控制流环（豁免 loop 回边）
  diags += detectCycles(reverseDeps.dataGraph)                   # 变量数据依赖环
  diags += branchCompleteness(graph)                             # 无条件分支缺失
  return rank(diags)   # error 优先，按拓扑序
```

`branchCompleteness` 覆盖题面「无条件分支缺失」全部形态：

- **condition**：无 branches / 有空 expression 分支 / **缺 defaultTarget** / 出边未被任一分支覆盖（有边无分支）/ 分支 target 不存在；
- **parallel**：分支数越界、label 空/重、target 重复、有分支不可达 joinTarget；
- **loop**：body/exit 目标缺失或指向自身/END；
- **human_approval**：双出口未配对、target 不存在；
- **普通节点**：悬空边、自环、直连 END 违规。

**前后端职责**：

| 层 | 前端（实时体验） | 后端（保存/编译权威） |
|---|---|---|
| L1 | 即时红字 | 同规则 422 |
| L2 | 输入时高亮、补全只列可见符号 | 编译期复查（防绕过） |
| L3 | 画布角标/边高亮（可延迟到空闲） | **唯一权威**，聚合报错拒绝保存 |

### 2.6 性能优化方案

| 手段 | 做法 | 解决什么 |
|---|---|---|
| 增量校验 | config 变更只失效该实体 L1；增删边/改 target 失效受影响子图 L3；改 output schema 经 reverseDeps 只重算引用方 L2 | 避免每次按键全图重算 |
| 分层调度 | L1 同步（按键）→ L2 50–100ms 防抖 → L3 requestIdleCallback / Web Worker | 拖拽/输入不掉帧 |
| 索引缓存 | ScopeIndex 拓扑序记忆化，结构版本号（nodes/edges hash）未变直接复用；visibleAt 按节点缓存 | 补全弹层 O(1) |
| 虚拟化 | 大数组表单虚拟滚动；Problems 列表虚拟化 | 大图 DOM 爆炸 |
| 不可变更新 + 结构化 memo | 字段级订阅，控件仅在自身值/诊断变化时重渲染 | 表单联动重渲染风暴 |
| Schema 编译缓存 | schema 按 $id+version 编译一次，validate 复用 | JSON Schema 校验主要开销 |
| 补全检索 | 符号按段建 trie/前缀索引 | 千级符号仍即时 |
| 降级策略 | 节点数超阈值（如 500）：L3 降为保存时 + 手动「检查图」，编辑中只给 L1/L2 | 超大图保底 |

---

## 3. 题二作答：多智能体协同 + 部署 + 交互模板端到端

**一句话方案**：自然语言生成的图草稿经人审发布为**不可变版本**，由**确定性 Graph（而非自由 Agent）当主协调者**，通过任务信封调度客服/物流 Bot，经双向审批卡片完成人机协同，入站 Router 按租户/业务桶/百分比灰度，业务指标门控自动回滚，trace/指标/告警/回放贯穿全程。

题面六项要求在答卷中的位置：

| 题面要求 | 答卷机制 | 详见 |
|---|---|---|
| 1 NL 生成/技能推荐/自动组合 | NL 六步（parse→retrieve/rank→plan→fill→validate→人确认） | §3.2 |
| 2 主协调/客服 Bot/物流 Bot | 确定性 Graph 主干 + dispatch/join/escalate | §3.1、§3.2 |
| 3 协议/分发/超时/幂等/冲突 | Task Envelope 状态机 + 超时三级 + 冲突三层 | §3.3、§3.4 |
| 4 审批卡片/变量绑定/渠道适配 | 双向卡片 Schema，渠道=渲染降级 | §3.2、§3.3 |
| 5 Webhook/IM/嵌入/灰度/回滚 | TriggerEvent 归一化 + 版本不可变 + Router 分桶 + pin-to-version | §3.3、§3.5 |
| 6 追踪/指标/告警/回放 | traceId/spanId 贯穿 + 系统/业务指标分列 + 门控告警 + 批量回放 | §3.3、§3.5 |

### 3.1 总体架构

```
┌─────────────────────────────── 入站渠道层 ───────────────────────────────┐
│ Webhook 网关        IM 适配(钉钉/企微/飞书)      网页嵌入(SDK/iframe)      │
│              统一归一化为 TriggerEvent{channel, tenant, payload}          │
└──────────────────────────────────┬───────────────────────────────────────┘
                                    ▼
┌──────────────────────── 设计态（题一内核的产物） ────────────────────────┐
│ NL 生成 → 技能/模板检索 → 图规划 → 人确认 → 编译校验 → 版本化发布          │
└──────────────────────────────────┬───────────────────────────────────────┘
                                    ▼ 不可变 Graph 版本 vN
┌──────────────────────────── 运行时（控制平面） ──────────────────────────┐
│ Router(灰度规则: 租户/业务桶/百分比 → 版本)                               │
│ Graph Engine（确定性主干: 路由/分支/并行/等待/子图/审批挂起）              │
│   Coordinator(主协调)                                                    │
│     ├─ Task Envelope Bus ──► CustomerService Bot (技能+适配器)           │
│     ├─                    ──► Logistics Bot                             │
│     └─ Approval Service ◄──── 人工决策（卡片回调 / 超时 / 预置）          │
│ Harness 适配器层: shop / http / database / message(+真实渠道)            │
└──────────────────────────────────┬───────────────────────────────────────┘
                                    ▼
┌──────────────────────────── 可观测与发布 ────────────────────────────────┐
│ Trace(事件流/span) │ Metrics(系统+业务) │ Alerts(门控) │ Recording 回放  │
│ 灰度切流 ─ 指标门控 ─ 自动回滚（在途实例钉版本）                          │
└──────────────────────────────────────────────────────────────────────────┘
```

**设计立场（显式声明）**：主协调者是**确定性 Graph，不是自由 Agent**——可静态校验（题一全部图级规则适用）、可回放、灰度可按版本切流、金融动作可审计。柔性判断（路由拿不准、客诉情绪升级）放在节点内嵌、受护栏的 Coordinator Agent：只输出「下一步意图 + 置信度」，低置信走规则或升级人工，**LLM 不掌握控制流写入权**。退款这类金融+合规场景不采用多 Agent 自治 mesh。

### 3.2 关键流程

**(a) NL → 可运行图（六步；生成与推荐/组合是三个独立考点）**：

```
1 解析    NL → 意图(退款流程) + 实体(订单号/金额/渠道/时限)
2 检索    意图+实体 → 技能库/模板库候选原子（retrieve + rank：标签/历史成功率）
3 规划    planner 出图骨架: trigger→决策→分支→工具/审批→通知
           （LLM 只产出节点+边声明，工具 id 必须取自注册表，禁捏造）
4 填充    参数绑定 {{路径}}，引用必须在 ScopeIndex 可见集内（题一 L2 卡点）
5 校验    完整过 L1/L2/L3（NL 草稿与手画同等待遇，无豁免）
6 确认    草稿进编辑器，人审改后保存为新版本 —— NL 永远不到「发布」
```

步骤 2 是**技能推荐**（retrieve/rank 选原子），步骤 3 是**自动组合**（plan/connect 出控制流），步骤 4 复用题一作用域索引保证填充合法。

**(b) 退款协同（dispatch / join / escalate 三模式）**：

```
webhook 退款单
  → condition: 金额/类型分流
     ├─ 仅退款·低额·信用好 → 客服 Bot 核验 → shop.execute_refund（financial 权限）
     ├─ 退货退款           → 并行 dispatch:
     │                        客服 Bot: 核验订单/沟通
     │                        物流 Bot: 确认签收入库
     │                      join(两者 completed) → human_approval(>阈值) → 放款
     └─ 争议/高情绪/低置信  → escalate: 人工客服主管（审批卡片）
```

- **dispatch**：协调者发任务信封给单 Bot，状态机 `pending→accepted→running→done/failed/timeout`；
- **join**：parallel 网关等两 Bot 任务都 completed，物流签收入库结果经 `{{parallel-x.result...}}` 进放款条件——「退货退款必须签收后放款」是跨 Bot 时序/数据依赖，复用题一作用域；
- **escalate**：升级是带审批卡片的特殊 human_approval，不是自由 @人。

**(c) 人机审批与交互模板（双向；渠道 = 渲染降级）**：

```
Web 嵌入  : 完整表单(订单明细/金额/历史对话/意见框/批准·拒绝按钮)
IM 卡片   : 受限交互组件(文本+两按钮，详情跳链接)
邮件      : 只读摘要 + 带 token 的批准/拒绝链接
回调      : POST /approvals/{token}/decision（首决生效，重复 409）
```

卡片是**双向**的：input bindings 把变量渲染进展示；actions 的 output schema 把按钮/表单回写成结构化决策流回流程。

**(d) 发布流**：

```
保存 → 编译校验全绿 → 录制用例批量回放(门禁) → 新版本 vN(不可变)
     → 灰度(内部/低金额桶/百分比) → 指标门控观察期 → 全量
                                            └─ 越阈 → 自动回滚 vN-1
```

### 3.3 核心 Schema

**(a) 多 Bot 任务信封（通信协议）**：

```json
{
  "taskId": "task-uuid",
  "runId": "run-123",
  "idempotencyKey": "refund-12345|approve|v3",
  "traceId": "run-123",
  "graphVersion": "refund-flow@7",
  "type": "refund.verify_order | logistics.check_receipt",
  "assignee": "bot.customer | bot.logistics",
  "payload": { "orderId": "12345", "amount": 128,
               "refs": { "receipt": "{{logistics-1.result.signed}}" } },
  "deadlineMs": 30000,
  "state": "pending|accepted|running|done|failed|timeout",
  "result": {},
  "attempt": 1
}
```

**(b) 审批卡片 / 交互模板（双向）**：

```json
{
  "$id": "card/refund_approval/v1",
  "channels": ["web", "im", "email"],
  "sections": [
    { "type": "fields",
      "bindings": [
        { "label": "订单号", "value": "{{trigger.context.payload.order_id}}" },
        { "label": "退款金额", "value": "{{tool-1.result.amount}}" },
        { "label": "物流签收", "value": "{{logistics-1.result.signed}}" }
      ]},
    { "type": "textarea", "name": "comment", "required": false }
  ],
  "actions": [
    { "id": "approve", "label": "批准", "style": "primary",
      "output": { "decision": "approved", "comment": "{{form.comment}}" },
      "channels": { "email": { "render": "link" } } },
    { "id": "reject", "label": "拒绝", "style": "danger",
      "output": { "decision": "rejected", "comment": "{{form.comment}}" } }
  ],
  "fallback": { "im": { "detailUrl": "/a/{token}" },
                "email": { "timeoutHint": true } }
}
```

`bindings` 里的 `{{}}` 受题一 ScopeIndex 同一套规则校验——**卡片模板就是题一内核的第六类实体**；actions.output 回写结果成为节点产出。

**(c) 部署与灰度描述**：

```json
{
  "graphId": "refund-flow", "version": 7,
  "ingress": [
    { "channel": "webhook", "path": "/ingress/refund", "secretRef": "secret.webhook" },
    { "channel": "im", "adapters": ["dingtalk", "wecom"] },
    { "channel": "embed", "allowedOrigins": ["https://*.example.com"] }
  ],
  "rollout": {
    "strategy": "progressive",
    "rules": [
      { "to": "internal", "when": "tenant in allowlist" },
      { "to": "lowValueBucket", "when": "payload.amount <= 200", "percent": 100 },
      { "to": "canary", "percent": 5 },
      { "to": "full" }
    ],
    "gate": {
      "observeMinutes": 60,
      "autoRollback": true,
      "metrics": [
        { "id": "run_error_rate",          "threshold": 0.02 },
        { "id": "manual_escalation_rate",  "threshold": 0.10, "compareWith": "v6" },
        { "id": "refund_amount_diff_rate", "threshold": 0.005 }
      ]
    },
    "inFlightPolicy": "pin-to-version"
  }
}
```

**(d) Trace 事件（现有节点事件流的超集）**：

```json
{
  "traceId": "run-123", "spanId": "task-2", "parentSpanId": "parallel-1",
  "graphVersion": "refund-flow@7", "kind": "task_dispatch|task_done|approval|tool",
  "actor": "bot.logistics", "ts": "...", "status": "done",
  "attrs": { "orderId": "12345" } }
```

### 3.4 冲突解决策略（三层分开）

| 层 | 场景 | 策略 |
|---|---|---|
| L1 投递冲突 | 超时重试致同一任务执行两次 | 幂等键 `资源|动作|版本`；Bot 侧 idempotencyKey 去重，重复请求返回**首结果**；至少一次投递 + 幂等消费 |
| L2 写入冲突 | 两 Bot 并发改同一退款单 | 资源带版本号，**乐观锁 CAS**（`UPDATE ... WHERE version=n`）；金融写只走 shop 单一通道且需 financial 权限；失败方重读发现已终态则放弃 |
| L3 决策冲突 | 客服 Bot 说可退、物流 Bot 说未签收 | 硬约束写死图里（「未签收→禁止放款」condition）；规则未覆盖**不做 Agent 投票，升级 human_approval**；双方结论进卡片供人判断 |

**超时三级**：任务级 deadline（信封 `deadlineMs`）+ 节点级 retry.timeout + 审批级 timeoutSeconds（默认 reject）。超时动作链：**重试（幂等键不变）→ 升级分支 → fail-safe**（run 仍 completed，下游 condition 判 status）。

**任务分发**：dispatch（单信封派单）/ join（parallel 全 completed 汇聚）/ escalate（带卡片升级人工）三模式；信封经任务总线流转，状态机即 (a) 的 `state` 枚举。

### 3.5 灰度与回滚机制

1. **版本不可变**：发布产物 `graphId@version`，Graph + 绑定的模板/技能引用一起冻结（subgraph 递归钉版）；编辑产生新版本，老版本永不原地改。
2. **灰度三维度**：租户 allowlist（内部先行）→ **业务桶**（低金额自动退款先灰，高金额/退货退款强制人工且不参与早期灰度）→ 百分比（订单 id 哈希稳定分桶，同一单不跨版本跳动）。
3. **Router 与在途钉版**：TriggerEvent 入口查路由表定版本；**pin-to-version**——回滚只影响新流量，跑到一半的流程用启动时版本跑完（中断恢复后仍读老版本）。
4. **门控自动回滚**：观察窗内 gate 指标任一越阈 → Router 切回 vN-1 → 告警 → 新版本用录制回放定位（冻结快照在新版本重放，做跨版本对比）。只自动回滚，放量保持手动。
5. **回滚安全前提（金融硬条款）**：回滚不改外部世界已发生的事实（已退款不可逆），故 gate **必须含业务结果指标**（退款金额差异率），把问题挡在放量前；这是金融场景必须按业务桶而非纯百分比灰度的原因。

**可观测四件套**：

| 能力 | 方案 |
|---|---|
| 链路追踪 | traceId 从入站贯穿到 Bot 任务（spanId/parentSpanId 父子），parallel/subgraph 内部建 span、可选不外泄 |
| 指标 | 系统指标（时延/失败率/超时率）与业务指标（自动退款率/升级率/金额差异率）分列 |
| 告警 | 规则触发 → 展示 → 挂回滚门控驱动动作 |
| 回放 | 发布前批量回放门禁 + 事故后在新版本跨版本重放 |

---

## 4. 两份答卷的接缝与落地状态

**接缝（为什么是两道题而不是两个系统）**：适配器/工具能力声明上的 `inputSchema/outputSchema` 同时撑住两边——设计态靠它生成工具参数表单、推导 `{{}}` 作用域与类型；运行态靠它让 NL planner 合法组合、让卡片模板绑定变量、让回放入参形状稳定。编辑期静态解析的 `{{}}` 与运行期插值是同一语义的两个时期；设计态图级校验是发布态灰度的准入，运行态录制回放是静态校验的运行期延伸。

**落地状态（2026-09-16，以 docs/20 为准）**：

- 答卷**方案层**完整落在 docs/19（含每条主张对仓库现状的「已有/缺口」核对）；
- 已落码的只有 **M0 前置项**：Capability input/outputSchema 子集声明 + 拓扑作用域索引 + L2 三码校验 + 编译期复查 + NL 参数填充非阻塞警告（dev 本地 8606aa3→34d5cb5，未推送）；
- 题一六项能力的完整闭合在 **M1–M4**，题二六项跨 **M5–M10**，均**未立项**；缓做部分（多 Bot 总线/版本化灰度/卡片渠道/span 追踪）登记在 docs/14 D29–D34，触发条件未解除；
- 本文不含新契约、不触发选型决策；任何一期转落码时，按 docs/20 §5 同步矩阵走「10 ADR → 08 立项 → 03/04/12 契约同步」。

---

*本文为题卷与方案作答的合订阅读版；答案细节以 docs/19 为完整推导源，工程分期与验收口径以 docs/20 为准。*
