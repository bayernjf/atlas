# Atlas 运行时与质量保障

> **来源**：《技术实现设计.md》"技术方案（Atlas）· 运行时与质量保障摘编"整章（运行时引擎 / 测试与质量保障 / 安全设计清单）。
> **切分原则**：只重组、不改写，以下内容为原文章节原样搬运。
> **AI 使用提示**：本文件是引擎层规格——Loop 主循环、决策节点、记忆系统、Harness Gateway、Web 适配器（含代码示例）、测试层次、评估 Harness、安全清单。实现引擎与写测试时以本文件为准。

# 技术方案（Atlas）· 运行时与质量保障摘编

> 摘编说明：以下为《技术方案.md》原文第六、九、十部分（运行时引擎设计 / 测试与质量保障 / 安全设计清单）。

## 第六部分：运行时引擎设计
### 6.1 Loop 主循环实现
基于 LangGraph StateGraph 构建持久运行的状态机（代码示例）：
class LoopState(TypedDict):
goal: str
messages: list          # 对话/执行历史
current_node: str
observations: list      # 感知结果
variables: dict         # 会话变量
status: str             # running/paused/completed/error
memory_id: str
# 构建主循环
graph = StateGraph(LoopState)
graph.add_node("observe", observe_node)      # 调用Harness感知当前状态
graph.add_node("orient", orient_node)        # 分析数据，定位进展
graph.add_node("decide", decide_node)        # LLM决策下一步
graph.add_node("act", act_node)              # 执行工具/操作
graph.add_node("reflect", reflect_node)      # 阶段性反思
graph.add_edge("observe", "orient")
graph.add_conditional_edges("orient", should_continue)
graph.add_conditional_edges("decide", should_act_or_wait)
graph.add_conditional_edges("act", should_reflect_or_continue)
```

> **Graph condition 节点执行语义（Phase 2 首版，2026-09-14；契约权威见 04 §5.2）**：编辑后台 Graph 中的 condition 节点编译为 LangGraph conditional edges——同一 condition 节点的出边**全部**走 `add_conditional_edges`（不得与普通边混用，否则双路激活）。执行器按 config.branches 数组顺序短路求值（第一个为真者胜出），全假或求值异常时走 defaultTarget；求值只发生一次并写入 `outputs[condition_id]`，路由函数仅读取其中的 `target`。含变量的运行时类型错误/缺失变量 fail-safe 走 defaultTarget，错误进 `expression_errors`；trace（messages）追加一行 `condition-x: branch=<label|__default__> → target=<node_id>`。表达式首版白名单（比较/逻辑/括号/字面量/`{{路径}}`，无算术与函数、禁止 eval）见 04 §5.1。

> **Graph loop 节点执行语义（Phase 2 第二项，2026-09-14；契约权威见 04 §5.3）**：v1 仅条件循环（while）。loop 节点的两条出边（bodyTarget/exitTarget）全部走 `add_conditional_edges`，router 只读 `outputs[loop_id]["target"]`；循环体末端以**普通回边**连回 loop 节点，形成 LangGraph 原生环（回边白名单由 dsl 静态校验保证，非法环保存时拒绝，见 13 U7）。执行器可重入：每次进入读取自身上次产出的 `iterations`，达 `maxIterations` 或表达式求值异常时 **fail-safe 路由 exitTarget**（exitReason 分别为 `max_iterations`/`expression_error`，运行整体仍 completed），表达式为真则计数加一路由 bodyTarget，为假路由 exitTarget（`condition_false`）。循环体节点经 `{{loop-x.index}}` 引用当前 1-based 轮次。LangGraph 默认 recursion_limit=25 会中断循环，`run_graph` 按 `2*节点总数 + 2*maxIterations*(循环体节点数+1) + 10` 派生并经 `invoke(..., config={"recursion_limit": n})` 传入。trace 行：`loop-x: continue (i/max) → body` / `loop-x: exit (reason) after N → exit`。遍历循环与 break/continue 缓做（14 登记表 D16/D17）。

> **Graph parallel 节点执行语义（Phase 2 第三项，2026-09-14；契约权威见 04 §5.4）**：v1 静态扇出/扇入，合并策略仅 `all_success`/`all_completed`（any_success 真 OR-join 需取消其他分支，缓做 14 D18；动态 map-reduce 不做）。parallel 节点的 N 条出边全部走 `add_conditional_edges`，router 返回**分支目标列表**（`list[str]`）实现同超步静态扇出；分支末端全部汇聚到 config.joinTarget——LangGraph 静态多入边节点天然是 barrier，所有前驱完成后 joinTarget **只执行一次**（不等长分支亦成立，依赖 checkpoint pending-writes；编译期 spike 实测背书，若上游行为变化的备选为注入合成节点 `__join__:<parallel-id>`）。支撑并发写的横切变更：`GraphState.outputs` 由无 reducer 的 LastValue 通道改为 `Annotated[dict, merge_dicts]` **按键合并**通道，各执行器只返回自己的分片 `{"outputs": {node_id: output}}`（同 key 后者覆盖）；`messages` 仍为 `operator.add`，`status` 同值 LastValue。扇出不增加递归深度，recursion_limit 公式不变。分支成败判定：该分支路径上任一节点产出出现 `result.status == "FAILED"` 即失败（工具失败只写 FAILED 不抛错）。**fail-safe 汇聚**：all_success 下即便有分支 FAILED，joinTarget 照常执行、parallel 产出 `status="failed"` 与逐分支错误、运行整体仍 completed；下游可用 condition 判 `{{parallel-x.status}}`。聚合产出（写入 `outputs[parallel_id]`，入口先写 running 占位、汇聚时覆盖为终态）形状与 trace（`parallel-x: fork N branches → a, b` / `parallel-x: joined (all_success) success` / `… failed: <label>（<error>）`）见 04 §5.4、12 文档。
### 6.2 决策节点实现（代码示例）
def decide_node(state: LoopState):
# 1. 从记忆中检索相关上下文
relevant_memory = memory_retriever.query(state.goal, state.messages[-5:])
# 2. 构建提示词（含思考缓冲区）
prompt = f"""
【目标】{state.goal}
【当前状态】{state.messages[-10:]}
【相关记忆】{relevant_memory}

请先进行内部推理（思考：
1. 当前进展如何？
2. 下一步应该做什么？
3. 有什么风险？
然后输出结构化决策。
# 3. 调用LLM，强制结构化输出
decision = llm.with_structured_output(DecisionSchema).invoke(prompt)

# 4. 置信度检查
if decision.confidence < 0.6:
return {"status": "paused", "request_human": True, ...}

return {"current_node": decision.next_action, ...}
### 6.3 记忆系统

| 层次 | 存储 | 内容 | 更新策略 |
| --- | --- | --- | --- |
| 短期工作记忆 | Redis / LangGraph Checkpointer | 最近N步对话/操作 | 实时写入 |
| 中期摘要记忆 | PostgreSQL | 每超阈值自动摘要 | 步数/Token阈值触发 |
| 长期事实记忆 | pgvector | 业务事实、用户偏好 | 会话结束提取 |
| 案例记忆 | 结构化索引 | 成功/失败轨迹 | 定期批量写入 |

### 6.4 Harness Gateway（代码示例）
type HarnessAdapter interface {
ListCapabilities(ctx context.Context) ([]Capability, error)
Execute(ctx context.Context, req ActionRequest) (ActionResult, error)
Observe(ctx context.Context, req ObserveRequest) (Observation, error)
}
type ActionRequest struct {
CapabilityName string
Parameters     map[string]interface{}
Context        map[string]interface{}
Timeout        time.Duration
}
type ActionResult struct {
Status     string          // SUCCESS / PARTIAL / FAILED
Output     interface{}
Screenshots [][]byte
Error      *StructuredError
}
### 6.5 Web 操作适配器（Demo 核心）
三层定位冗余：（代码示例）
class WebHarnessAdapter:
def click(self, element_desc: str, selector: str = None):
# 第一层：精确选择器
if selector:
try:
self.page.click(selector, timeout=3000)
return success()
except:
pass

# 第二层：视觉语义定位
screenshot = self.page.screenshot()
elements = omni_parser.parse(screenshot)  # 返回元素列表+描述
target = llm_match(element_desc, elements)
if target.confidence > 0.7:
self.page.mouse.click(target.x, target.y)
self.update_selector_cache(element_desc, target.selector)
return success()

# 第三层：LLM全图推理
result = vision_llm.analyze(screenshot, f"找到{element_desc}并返回坐标")
if result.coordinates:
self.page.mouse.click(*result.coordinates)
return success()

return failed("元素未找到，三层定位均失败")


## 第九部分：测试与质量保障
### 9.1 测试层次

| 层次 | 工具 | 内容 |
| --- | --- | --- |
| 单元测试 | Pytest | 节点逻辑、变量系统、表达式引擎 |
| 集成测试 | Playwright | Harness操作正确性 |
| 回放测试 | 录制回放框架 | 运营体行为一致性 |
| 端到端测试 | 沙盒环境 | 完整业务场景 |
| AI评估 | 评估Harness | 决策质量、任务成功率 |

### 9.2 评估 Harness 设计（借鉴 lm-evaluation-harness）代码示例
evaluation_task:
task_id: "refund_processing"
description: "处理退款申请"
test_cases:
- input:
order_id: "12345"
reason: "商品破损"
amount: 299
expected:
action: "approve_refund"
verify: "refund_status == 'completed'"
- input:
order_id: "12346"
reason: "不想要了"
amount: 5000
expected:
action: "request_human_approval"
verify: "approval_request_created"
metrics:
- task_success_rate
- average_steps
- decision_accuracy
### 9.3 上线前强制测试
每个运营体发布前必须通过：
1、Schema 完整性校验
2、黄金测试集回归（至少5个测试用例）
3、沙盒环境模拟运行（成功执行3次）
4、安全审查（敏感操作确认机制有效）


## 第十部分：安全设计清单

| 安全维度 | 实现方式 |
| --- | --- |
| LLM输出过滤 | Guardrails-AI + 规则引擎 |
| 沙盒执行 | Docker gVisor隔离，危险操作白名单 |
| 提示注入防御 | 上下文隔离、输入过滤、检测模型 |
| 数据脱敏 | 日志自动PII模糊（Presidio） |
| 密钥管理 | Vault加密存储，运行时注入 |
| 审计追踪 | 不可篡改日志，全链路追踪 |
| 权限控制 | RBAC + 工具级权限 |
| 高风险保护 | 金额阈值、二次确认、熔断机制 |



---
