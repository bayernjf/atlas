# Handoff 归档补遗 — 2026-09-24

> 本文件归档 2026-09-24 收口（docs/55 打包 B、docs/56 打包 C）时自主 handoff.md 滚出的历史：
> ① 主 handoff 顶部「更新时间」流水（docs/34 尾批 ~ docs/54，2026-09-21 ~ 2026-09-23）；
> ② 当时 Recently shipped 最近 5 条（docs/47/44/43/38/37）。原编号保留，按时间倒序。
> 归档惯例同 docs/handoff-archive-2026-09-18.md、docs/handoff-archive-2026-09-19.md：主 handoff 只留当前状态＋活跃任务＋最近 5 条 Recently shipped，完成项详情滚本文件。

## 一、顶部「更新时间」历史流水（归档自旧主 handoff）

更新时间：2026-09-23（**✅ wait/告警 v1 批落码收口（docs/54，打包 A，Active work 55；原子①-⑥全完成；AI 承接「你口气搞了」总授权）**：①docs-only 立项 `67650ed`；②duration/event 上限放开＋duration jitter `76fa61c`（U510–U519）；③多事件 OR 竞速＋跨重启 restore `dcf4626`（broker request_any/eventKeys、payload.matchedEventKey、DSL/loader/api，U520–U530，broker19/graph19/dsl116/api9/loader81/resume4 全过）；④健康运行自动恢复 open 内置告警＋lifecycle 限流退避 `2d53654`——record_run 本次 healthy 即把该图 open 且 rule_id≠rollout_gate 的内置告警置 resolved（复用 last_seen 不加列、acknowledged/rollout_gate 不自动恢复、一次健康即恢复不做 flapping），锁外发 transition=recovery；LIFECYCLE_TITLES 增「告警已自动恢复」；AlertNotifier 增 lifecycle_min_interval_seconds=60/time_func（monotonic），同 (alert,transition) 窗口内跳过返空 delivery（不记 lastNotifiedAt），投递成败都计时，new 首条 notify 不限流、进程内不持久化；monitoring 全套 **96/2** 零回归、notify 文件 **32**（新 U531–U536）。⑤前端 wait 面板（jitter/多键）`2e86862` 已落——l1 常量 duration 600→3600/event 3600→86400、新增 MAX_JITTER_SECONDS=300/MAX_EVENT_KEYS=8；eventKeys 多键 L1 校验（与 eventKey 互斥、1-8、逐键静态白名单≤128、空/重复按 /eventKeys/{idx} 定位、二选一必填）、duration static jitterSeconds RANGE(0-300) 校验；wait.schema duration max3600+jitterSeconds、event eventKeys array(1-8)+timeout max86400、x-outputSchema 加 plannedDurationSeconds/jitterSeconds/eventKeys/matchedEventKey；WaitConfig 面板 static 抖动 InputNumber（实际=时长+0~上限随机）＋event 单/多竞速 Radio＋eventKeys 动态列表（n/8、增删、逐键标红）；新增 7 个 L1 用例（jitter 合法/越界、多键合法/二选一必填/超量空集/非法空重复/互斥），前端全量 **643 passed/2 skipped/48 文件**（净增 7）、oxlint **0 error/6 既有 warning**、tsc+vite build 过（仅既有 chunk>500kB 提示）。⑥docs 收口完成——后端全量内存门 **1630 passed/61 skipped/0 failed**（349s；skipped 60→61 系新增 PG-only 多键测试在无 DATABASE_URL 内存门跳过）、PG 直连集成 test_storage_pg_integration.py **14 passed**（含新增非首键多事件 restore 测试）、前端 **643 passed/2 skipped/48 文件**＋oxlint 0 error（6 既有 warning）＋tsc+vite build 过；HTTP smoke 对 live :8000 全绿：event_wait **19**、alert_notify **21**、dynamic_wait **21**。收口另修一处 docs/47 起既有健壮性缺陷（fix(api)）：运行期 WaitNodeFailure（timeout fail/非法 eventKey/duration/absolute）现经全局 `@app.exception_handler` 返回结构化 500 JSON（detail.code/message/nodeId），原先逃逸 ASGI 顶层被 re-raise、uvicorn 关闭 keep-alive，失败运行后紧邻的列表/详情 GET 复用连接被 RST；同步 smoke 的 docs/53 suspended 与 B1 lifecycle 语义（event⑥ running/suspended、① matchedEventKey、alert②/⑤ merged lifecycle＋新增 60s 限流〔注入时钟〕/recovery/rollout_gate 不恢复断言、dynamic over-max 601→3601），`.smoke/event_wait_smoke.py` 自 docs/47 漏 `git add -f` 本次补入库，新增 `scripts/dev/d54_smoke_run.sh` 一键复跑三 smoke。03/04/12/13/14/00/08/CHANGELOG 已回填。打包 B（docs/55）、打包 C（docs/56）未开工。以下为上批记录。）
更新时间：2026-09-23（**📋 wait 时长放开/jitter/多事件竞速＋告警 recovery/lifecycle 限流 v1 批 docs-only 立项（docs/54，Active work 55；AI 承接「你口气搞了」总授权，打包 A/B/C 三包一口气推、本包为打包 A）**：部分取回 docs/14 D19 wait 余部与 D28 告警 lifecycle 余部，进程内 v1、零新依赖/零迁移/无 ADR/零新 REST，**不解除缓做条目**。五小项：①duration 上限 600→3600、event 超时上限 3600→86400（24h）；②duration 可选 `jitterSeconds`（0-300，actual=planned+randint，rng 可注入，event 超时不加抖动）；③event 可选 `eventKeys`（1-8，与 eventKey 互斥，broker request_any 多键挂载、任一信号首决命中 matchedEventKey、唤醒/超时/取消清理余键、跨重启帧 eventKeys+restore 多键）；④健康运行自动 resolve 该图 open 内置告警（acknowledged/rollout_gate 不自动恢复）并发 transition=recovery 通知；⑤AlertNotifier lifecycle 同(alert,transition) 默认 60s 限流退避（new 首条不限）。测试候选 U510 起。形状权威 [docs/54](54-wait时长放开-jitter-竞速-告警恢复限流v1批契约设计.md)；立项基线后端 **1591/60**、前端 **636/2**。多实例跨进程路由/停摆期信号排队/AND 竞速/PG 档 lifecycle/OTel/flapping 抑制仍缓做。以下为上批记录。）
更新时间：2026-09-23（**✅ event wait timeoutSeconds 表达式化 B5 落码收口（D19 子集；AI 承接「不用管git，你推任务」；零新依赖/零迁移/无新 ADR·REST，D19 部分取回、不解除）**：event wait config 增 `timeoutMode`（static 默认 / expression）与可选 `timeoutExpression`（≤200）；loader 抽出共用 `_resolve_wait_expression(node_id,expr,context,lo,hi)`——duration（1-600）/event（1-3600）统一经条件引擎求值，非 bool/非有限/越界 run failed WAIT_DURATION_INVALID；旧图缺省 static 零回归，跨重启帧存已求值秒数、resume 不重求值。前端 wait.schema event oneOf 加 radio/字段、L1 按 mode 校验（static 缺 timeoutSeconds 报 1-3600、expression 缺表达式报必填）。净增图级 3 测 + dsl 6 测；收口全量内存门 **1591 passed / 60 skipped**（净增 9、零失败）、前端 636/2（零新增前端测）。**B4（审批/告警/消息 {{}} 模板插值）勘察判空集、不落码**：插值接缝已由 `graph/interpolation.interpolate` 全覆盖（tool params 统一插值、approval summary/recipients、cards、eventKey、prompt 均接线，告警正文为系统字段无用户模板）。以下为上批记录。）
更新时间：2026-09-23（**✅ 跨版本配置结构 diff B3 落码收口（D26 子集；AI 承接「不用管git，你推任务」；零新依赖/零迁移/无新 ADR，新增 1 read 端点，D26 部分取回、不解除）**：新纯函数 `graph/diff.py`——`diff_graph(base,candidate)` 输出 nodes/edges/variables 的 added/removed/changed，节点 changed 细分 type/name 与 config 键级、position 等画布布局不参与，附 `diff_summary`/`has_changes`，零依赖；`GET /api/graphs/{id}/diff`（read、纯只读不产版本；toVersion 缺省＝latest 草稿、fromVersion 缺省＝最近发布版，该图从未发布则基线取空图，目标/基线快照缺失 404、跨租户不泄漏）；前端 ReleaseModal「与 vN/空图 的配置差异」折叠区（与门禁并行、失败 fail-safe）。tests/test_graph_diff.py 净增 6、tests/test_api_graph_diff.py 净增 4；收口全量内存门 **1582 passed / 60 skipped**（净增 10、零失败，以收口实跑为准）、前端 636/2。以下为上批记录。）
更新时间：2026-09-23（**✅ OpenAPI HTTP Digest 认证 B2 落码收口（docs/46 §8；AI 承接「不用管git，你推任务」；零新依赖/零迁移/无新 REST·ADR/前端零改动，D22 部分取回、不解除）**：parser 收录 `type:http,scheme:digest`（kind=digest/param=Authorization/无 prefix），凭证值与 basic 同形（{username,password} JSON 信封）；适配器不静态拼 header，构造 `httpx.DigestAuth` 经 HttpApiClient.request(auth=) 透传，httpx 单次调用内完成 RFC2617 挑战-响应（先无 Authorization 探测、收 401 WWW-Authenticate、按 realm/nonce/qop 算 HA1/HA2/response 重发）。models kind 加 digest、adapter _resolve_credentials 返三元组(headers,query,auth)、service request 加 auth 参数。tests/test_openapi_adapter.py 净增 2（挑战流重算 MD5 验证、坏信封 SECRET_DECRYPT_ERROR 不发请求）、test_openapi_parser.py digest 移入支持集；收口全量内存门 **1572 passed / 60 skipped**（净增 2、零失败）。**cookie 判为缓做**（OpenAPI 3.x 无标准 cookie scheme、apiKey in 不支持 cookie，本质登录端点+有状态会话，随连接/会话管理批次，参 docs/34 T4）。以下为上批记录。）
更新时间：2026-09-23（**✅ 告警 lifecycle 通知 B1 落码收口（docs/52 §7，991cd19；AI 承接「不用管git，你推任务」；零新依赖/零迁移/无新 REST·ADR/前端零改动，D28 部分取回、不解除）**：AlertNotifier 除新建外，对合并归并（再次发生已归并）、未确认升级（warning→critical，未确认已升级）、解决（告警已解决）三类状态变化经既有 MessageService 投递，过滤/失败口径同新建（enabled/to/minSeverity、fail-safe）；acknowledge 不通知。notify.py 增 LIFECYCLE_TITLES/build_lifecycle_subject/body/notify_lifecycle，records.py 六处接线（内存档；PG 档 lifecycle 随告警 PG 化缓做）。tests/test_monitoring_notify.py 净增 4；收口全量内存门 **1570 passed / 60 skipped**。以下为上批记录。）
更新时间：2026-09-23（**✅ wait 事件跨重启持久化 v1 批落码收口（docs/53，Active work 49；53a5cb4→4450430→a40873a〔＋断环719b3c9〕→b88fc41〔＋seed fix 6ad1a9f〕→smoke ba2028e＋docs 本步；AI 承接「不用管git，你推任务」；零新依赖/零迁移/无 ADR/无新 REST/前端零改动，D19 部分取回、不解除）**：event 挂起落 kind=wait 帧（帧内 wait:{waitType:event,eventKey,onTimeout,timeoutSeconds}，零 DDL）、runs suspended；启动 EventWaitBroker.restore 同 token/event_key、按绝对 deadline 扣剩余（幂等），/api/waits list/广播/直投跨重启可用，缺 wait→WAIT_EVENT_FRAME_INVALID。顺带修两个 PG 整栈阻断：storage.pg↔iam 循环导入、PG 首启不播种管理员无法登录。U400–U402 转正式；HTTP 重启冒烟 scripts/dev/d53_event_wait_restart_smoke.py **ALL SCENARIOS PASSED**（直投 completed/payload 透传、广播 released=1、fail 重启后 WAIT_TIMEOUT_FAILED）。收口全量内存门 **1566 passed / 60 skipped**。以下为上批记录。）
更新时间：2026-09-23（**✅ 监控告警外部通知 v1 批落码收口（docs/52，Active work 48；92b4621→70b4889→07baa89→6a7d846→b352638＋smoke 8ae7d76＋docs 本步；AI 判断承接「不用管git，你推任务」；零新依赖/无 ADR/新增 2 端点/迁移 020，缓做 D28/D24 部分取回、不解除）**：租户单例 AlertChannel（dingtalk/wecom/feishu/webhook/email＋minSeverity）＋AlertNotifier——仅新建告警（内存/PG/rollout_gate 三处）经 MessageService 旁路投固定纯文本，severity 过滤，异常全 fail-safe，最近投递状态 GET 可查。收口门后端 **1548/59**（净增 22）、前端 **636/2**（净增 9）；`.smoke/alert_notify_smoke.py` 两层 **ALL PASS: 15 checks**，浏览器配置卡/最近投递/中文校验截图 `.smoke/alert-channel-admin.png`（零 JS 错误）。以下为上批收口记录。）

更新时间：2026-09-23（**✅ IM 群机器人消息投递 v1 批落码收口（docs/51，Active work 47；274d784→d4823d5→80b2436＋docs 本步；AI 判断承接「不用管git，你推任务」；零新依赖/零迁移/无新 REST/无 ADR，缓做 D24 部分取回、不解除）**：message/send channel 增 dingtalk/wecom/feishu，to 为群机器人 Webhook 单 URL（过 EgressGuard、10s、不重定向），新增可选 secret（≤200，仅钉钉 URL 加签/飞书 body 加签）；平台 errcode/code 非 0/非 2xx/超时 → IM_SEND_FAILED 不写记录，EGRESS_DENIED 透传，成功 delivered 标渠道名，未注入投递器回退进程内。收口门后端 **1526/59**（+36）、前端 **627/2/48 文件**（+3）；`.smoke/im_robot_smoke.py` **20/20**（假机器人逐字节签名向量＋真实护栏 loopback 拦截/参数校验/in_process 零回归），浏览器 secret 字段截图 docs/smoke-shots/im-robot-1（零 JS 错误；浏览器内运行未做——合成事件无法触发 React Flow 连线）。短信/富文本/多 URL/OAuth 入站/模板/限流退避仍缓做。以下为上批收口记录。）

更新时间：2026-09-23（**✅ wait 到点时刻等待 v1 批落码收口（docs/50，Active work 46；e6c9789→12de72d→4f82a1c＋docs 本步；AI 判断承接「不用管git，你推任务」；零新依赖/零迁移/无新 REST/无 ADR，缓做 D19 部分取回、不解除）**：wait duration config 的 durationMode 增枚举 `absolute`，config 增 `absoluteTime`（≤64；ISO 8601 或 epoch 秒，支持 `{{路径}}` 插值，naive 按 UTC）；loader absolute 先插值再解析目标时刻，按注入时钟取差值 delta，须有限且 1 ≤ round(delta) ≤ 600，取 int(round) 秒等待并作为帧 timeout_seconds；插值/解析异常、目标已过点或差值 >600 **run failed WAIT_ABSOLUTE_TIME_INVALID，不 sleep**。absolute 产出另含 durationMode/absoluteTime（渲染后 ISO，epoch 回写统一 ISO），static/dynamic 旧图零回归。收口门后端 **1490/59**（+18）、前端 **624/2/47 文件**（+3）、lint/build 干净；`.smoke/absolute_time_smoke.py` HTTP **21/21**（带时区 2.09s/naive 3.07s/Z 2.16s/epoch 4.09s/插值 1.07s、四坏用例 ≤0.09s、static 深等、三 422），浏览器 4 截图 docs/smoke-shots/absolute-wait-*（三模式切换/L1 同步/静态还原，零 JS 错误；浏览器内运行未做——合成事件无法触发 React Flow 连线）。>600 秒长时刻/时区日历控件/周期时刻/event timeout 时刻化仍缓做。以下为上批收口记录。）

更新时间：2026-09-23（**✅ wait 定时等待动态时长 v1 批落码收口（docs/49，Active work 45；257ba38→8580fcb→4eaaddd＋docs 本步；AI 判断承接「不用管git，你推任务」；零新依赖/零迁移/无新 REST/无 ADR，缓做 D19 部分取回、不解除）**：wait duration config 增 `durationMode: static|dynamic`（缺省 static，旧图零回归）与 `durationExpression`（≤200，dynamic 必填）；loader duration 分支 dynamic 时经 D15 条件引擎求值（`{{路径}}`＋算术/白名单函数），结果须为非 bool 有限数值、1 ≤ round ≤ 600，取 int(round) 秒等待并作为帧 timeout_seconds；表达式无法求值或结果非法（非数值/bool/非有限/0/601/除零）**run failed WAIT_DURATION_INVALID，不 sleep 错误时长**。static 产出形状不变，dynamic 产出另含 durationMode/durationExpression。收口门后端 **1472/59**（+15）、前端 **621/2/46 文件**（+5）、lint/build 干净；`.smoke/dynamic_wait_smoke.py` HTTP **21/21**（waitSecs=2 实等 2.09s、算术=5 实等 5.05s、六坏用例 <0.1s、static 深等、三 422），浏览器 2 截图 docs/smoke-shots/dynamic-wait-*（模式切换/L1 同步/静态还原，零 JS 错误；浏览器内运行未做——合成事件无法触发 React Flow 连线，运行路径以 HTTP 冒烟＋loader 单测为准）。到点时刻/event timeout 表达式化/jitter/多单位/跨重启中断仍缓做。以下为上批收口记录。）

更新时间：2026-09-23（**✅ condition LLM 语义判断分支 v1 批落码收口（docs/48，Active work 44；39f7029→7f8392d→de3bc83→285cfc0→5a955d2＋docs 本步；AI 判断承接「不用管git，你推任务」；零新依赖/零迁移/无新 REST·错误码/无 ADR，缓做 D14 部分取回、不解除）**：condition 节点新增 conditionMode=llm——分支用自然语言 description（≤300）替代 expression（禁带），节点可选 classifierPrompt（≤500）；执行时上下文 JSON 序列化截断 12000 字符，经 `atlas.llm.condition_classifier`（镜像 decision.py 三层：LiteLLM 单次调用 temperature=0 返 `{"branch":"<label>"}`、Offline 未配 LITELLM_MODEL 必抛错、工厂环境切换）取唯一标签。合法标签按标签路由；`__default__`/坏 JSON/未知标签/调用异常（含供应商错误）**全部 fail-safe 走 defaultTarget**，不使节点/run 失败，原因进 llm_errors 与 trace；分类器经 run_graph(condition_classifier=) 注入、子图透传。收口门：后端 **1457 passed / 59 skipped**（净 +22）、前端 **616/2**、lint/build 干净；`.smoke/llm_condition_smoke.py` HTTP **11/11**，浏览器冒烟 mode 双向切换、3 截图 docs/smoke-shots/llm-condition-*、零控制台错误。Demo 阶段不视为可上线。置信度/多候选/澄清重问、每分支独立 prompt 或调用、模型可选与按租户配置、字段级脱敏、structured outputs、LLM 路由录制回放断言仍缓做。以下为上批收口记录。）

更新时间：2026-09-23（**✅ wait 事件等待进程内 v1 批落码收口（docs/47，Active work 43；32abb9f→f647c96→aa82eed→1608002→ce8c4f0＋docs 本步；AI 判断承接「不用管git，你推任务」；零新依赖/零迁移/无 ADR，缓做 D19 部分取回、不解除）**：wait 节点 waitType=event——per-tenant EventWaitBroker 进程内挂起（event_key 索引、`wait-` token、monotonic 超时、0.2s 取消切片），`POST /api/waits/events`（按 key 广播，released 计数）/`POST /api/waits/{token}/signal`（直投，未知 404 WAIT_TOKEN_NOT_FOUND/重复 409 WAIT_ALREADY_SIGNALED）/`GET /api/waits` 列 pending，或 run inputs `waitEvents` 预置（resolvedBy input）；eventKey 支持 {{}} 插值、静态白名单 [A-Za-z0-9:_-] ≤128，运行时再校验 WAIT_EVENT_KEY_INVALID；timeoutSeconds 1-3600，onTimeout continue（透传 signaled:false/resolvedBy timeout）/fail（run failed WAIT_TIMEOUT_FAILED）；输出携带信号 payload（≤4096B/≤50 键）。事件等待不支持单步调试（422），影子运行预置空 payload 秒过。收口门：后端 **1435 passed / 59 skipped**（净 +37）、前端 **606/2**、lint/build 干净；HTTP 冒烟 19 检查全过（`.smoke/event_wait_smoke.py`），浏览器冒烟 2 截图 docs/smoke-shots/event-wait-*。中断帧/恢复扫描器/多实例、动态/表达式时长、多事件竞速取消订阅、公开免登录信号口仍缓做。以下为上批收口记录。）

更新时间：2026-09-23（**✅ OpenAPI HTTP Basic 凭证子集批落码收口（docs/46，Active work 42；0252ffd→2f3b12b→685c3a6→11b3cbc＋f24ee8a 冒烟修复＋docs 本步；零新依赖/无 ADR）**：parser 收录 `type:http,scheme:basic`（kind=basic/param=Authorization/prefix="Basic "）；PUT basic 值＝`{username,password}`（信封明文 JSON、密文形状不变），缺字段 422 OPENAPI_INVALID_CREDENTIAL，null/空对象删除；适配器渲染 `Authorization: Basic base64(u:p)`，信封损坏 SECRET_DECRYPT_ERROR；前端用户名/密码双输入（新纯逻辑 lib/openApiCredentials.ts）。冒烟取回既有缺口 f24ee8a `fix(graph)`：tool_call 对 `openapi:*` 此前不走 params JSON 解析，参数无法下发。收口门：后端 **1398 passed / 59 skipped**（净 +7）、前端 **602/2**、lint/build 干净；浏览器冒烟（4 截图 docs/smoke-shots/basic46-*，真实 httpbin.org）：无凭证 fail-closed OPENAPI_CREDENTIAL_MISSING → 配置后「已配置 1/1」→ 运行 SUCCESS HTTP 200 body `{"authenticated":true,"user":"alice"}`。**D22 部分取回不解除**：digest/cookie/oauth2/连接测试/凭证轮换仍缓做。以下为上批收口记录。）

更新时间：2026-09-23（**✅ 循环 foreach 批落码收口（docs/45，Active work 41；efe5de4→1e79121→01296d5＋docs 本步；零新依赖/无 ADR）**：loop 节点新增 mode=foreach——itemsExpression 首轮求值一次冻结（≤100）、串行逐项暴露 `{{loop-x.item}}/index/results`、collectTarget 回边按序聚合 results（break 网关补在途结果）、exitReason 增 completed/empty/expression_error/items_too_large；DSL 静态输出键按被引用 loop 的 mode 放宽。前端 mode Select 切换 schema oneOf/字段显隐，L1 foreach 手写规则（parseExpression 纯语法）。收口门：后端 **1391 passed / 59 skipped**（净增 16）、前端 **597/2**、oxlint **45/1（46 files）**、build 干净。浏览器冒烟（3 截图 docs/smoke-shots/foreach45-*）：3 元素 completed/results 长度 3（params item/index 按序渲染），空数组 empty/循环体不执行；画布「编译并运行」两档一致。D16 主体取回不解除：嵌套循环/并行 map-reduce/裸 item 短名/skip-current/>100 分批仍缓做。以下为上批收口记录。）

更新时间：2026-09-23（**📋 循环 foreach 批 docs-only 立项（docs/45，Active work 41；AI 判断承接「不用管 git，你推任务」）**：loop 节点新增 mode=foreach——itemsExpression 首轮求值一次冻结（≤100）、串行逐项暴露 `{{loop-x.item}}`、collectTarget 回边聚合 results，exitReason 增 completed/empty/expression_error/items_too_large，break 沿用 D17；前端 mode Select 切换字段。零新依赖/零迁移/零新 REST·错误码、无 ADR；D16 主体取回不解除（嵌套循环/并行 map-reduce/裸 item 短名/skip-current/>100 分批仍缓做）。立项基线后端 1375/59、前端 593/2/45。以下为上批收口记录。）

更新时间：2026-09-23（**✅ OpenAPI securitySchemes 静态密钥批落码收口（docs/44，Active work 40；5a7e580→ab08c12/2d534a2/43d8001＋docs 本步；零新依赖/无 ADR）**：docs/42/43 忽略全部安全声明，本批接通静态密钥——parser 收录 apiKey（header/query）与 HTTP Bearer、有效要求＝operation 覆盖全局的 OR-of-AND 组，OperationDescriptor 增 `security`，ParsedSpec/ImportedSpec 增 `security_schemes`＋`credential_envelopes`；密钥经既有 SecretProvider 信封（T26，AES-GCM/plain）加密，迁移 019 给 openapi_imports 加两 JSONB 列（内存/PG 两档 store 同形），适配器执行逐次解密注入、缺密钥 fail-closed `OPENAPI_CREDENTIAL_MISSING` 且不发请求；新增 `PUT /api/openapi/imports/{id}/credentials`（operate），前端 Password 输入/「已配置 n/m」状态进 openapi namespace，所有响应不回显明文。收口门：后端内存档 **1375 passed / 59 skipped**（净增 23 常跑）、PG 直连 **11 passed**（atlas-pg：往返/跨重启/旧行默认）、前端 **593/2/45**；浏览器冒烟（3 截图 docs/smoke-shots/openapi44-*）：无密钥运行得明确中文失败 → PUT 配密钥 → SUCCESS HTTP 200（body Rex），PG 档重启后信封仍在、重跑仍 200。收口后恢复普通内存档 uvicorn。**D22 部分取回不解除**：basic/cookie/oauth2/openIdConnect、密钥取回/轮换、真实外部 API 联调仍缓做。以下为上批收口记录。）

更新时间：2026-09-23（**✅ OpenAPI 导入规格 PG 持久化批落码收口（docs/43，Active work 39；f39d86b→7bf13f5＋docs 本步；零新依赖/无 ADR）**：导入规格两档存储落地——迁移 018 `openapi_imports`（(tenant_id,id) PK、seq 走 storage_id_seq、operations JSONB），`PgImportStore` 与 ImportStore 同形（add/list/get/delete、5 specs/200 ops、OPENAPI_LIMIT_EXCEEDED 422 不变、reset 不清、行级租户过滤），iam/registry 仅 PG 档换装配，字段类型放宽为 `ImportStore | PgImportStore`；发现按请求读 store.list() 动态构建适配器，**重启无需预热**。REST 五端点/前端/编辑器/执行链路零改动、无新错误码。收口门：后端内存档 **1352 passed / 56 skipped**（默认跑新增 8 PG 测 skip；立项 48 skip）、PG 直连 **8 passed**（atlas-pg 实跑），前端 **591/2/46** 零改动；PG 档浏览器冒烟（假 DNS＋MockTransport）：重启后「API 导入」列表 openapi-768 仍在、`/api/adapters` 适配器自动重建，编辑器切 `openapi:openapi-768/list_pets`、limit=10 编译运行 tool_call-1 **SUCCESS（HTTP 200，body Rex）**，控制台仅一次预期旧 token 401，2 截图 docs/smoke-shots/openapi-pg-smoke-*。收口后已恢复普通内存档 uvicorn。**D22 部分取回不解除**：YAML/securitySchemes/原始文档留存/导入去重/软删除/共享仍缓做。以下为上批收口记录。）

更新时间：2026-09-23（**📋 OpenAPI 导入规格 PG 持久化批 docs-only 立项（docs/43，Active work 39；AI 判断承接「不用管 git，你推任务」）**：docs/42 的导入规格 v1 仅进程内、重启即失；本批照 015-017 两档先例——迁移 018 `openapi_imports`（tenant+spec_id PK、seq 排序、operations JSONB），新 PgImportStore 与 ImportStore 同形（add/list/get/delete、上限/错误码不变、reset 不清），PG 档装配切换；发现接口本按请求读 store.list() 动态构建适配器，故 REST 形状、前端、编辑器零改动、无需启动预热。立项基线后端 **1352/48**、前端 **591/2/46**。零新依赖/无 ADR；**D22 部分取回不解除**（原始文档留存/导入去重/软删除/共享仍缓做）。以下为上批收口记录。）

更新时间：2026-09-23（**✅ Shopify 侧 Webhook 注册批落码收口（docs/41，Active work 37；93cd1a0→6b86a1d→cefc4fe→3eccc6d＋docs 本步；零新依赖/零迁移/无 ADR）**：ShopifyChannelClient 增 list/register/delete webhooks（422→CHANNEL_ALREADY_REGISTERED/409；base_url 测试缝），registry 三方法（ATLAS_PUBLIC_URL 地址钉版、非 https 明确 422；取消按 topic+address 双键、UNAUTHORIZED 落 binding error 态），REST 三端点（GET read 折 200+error/POST operate 201·409·422/DELETE 幂等）；订阅弹窗加店铺侧注册区（viewer readonly）。mock 缝浏览器冒烟全绿：注册→重复 409→取消闭环（3 截图 docs/screenshots/41-shot-*）。落账后端 **1293/48**（净增 17）、前端 **584/2/45**（净增 3）。**D22 不解除**：真实店铺/公网 HTTPS 联调、Amazon/通用渠道、自动双向同步仍缓做。以下为上批收口记录。）
更新时间：2026-09-23（**✅ 入站可靠性补强批落码收口（docs/40，Active work 36；8cfcf6f→3036acc＋docs 70422e9；零新依赖/无 ADR）**：投递去重 PG 化（webhook_deliveries，迁移 017，跨重启/多实例、duplicates 计数；ignored 不落表、reset 不清）、死信捕获＋一键重放（全订阅未触发才 dead、payload 仅 dead 行、重放按当前订阅）、投递指标实时聚合（byTopic/totals），REST 四端点；前端 Monitoring 页 WebhookReliabilityCard。后端 **1276 passed/48 skipped**、前端 **581/2**；真实 HTTP 签名死信→发布/rollout→一键重放冒烟全绿，3 截图 docs/smoke-shots/docs40-*。D24 部分取回不解除。以下为上批收口记录。）
更新时间：2026-09-23（**✅ 入站 Webhook 批收口（docs/39，Active work 35；ADR T29）**：docs/38 Shopify 出向渠道之上的首个公开免登录入站触发口全部落码——channels/webhooks.py HMAC-SHA256 验签（原始 body 先于解析）＋WebhookDeliverer 后台线程经 M9 Router 异步触发图，按绑定 WebhookSubscription（topic×graph，迁移 016 webhook_subscriptions、reset 不清），公开 hook 端 received/duplicate/ignored（401/400/404/503）＋订阅 GET/PUT，前端订阅 Modal；幂等环进程内 ring 200/1h，**零新依赖/零外部资源**，D22/D24 不解除。落账后端 1252/44、前端 579/2；真实 HMAC HTTP 冒烟三态＋触发图 completed，截图 docs/smoke-shots/webhooks-smoke-*。）
更新时间：2026-09-23（**✅ 真实渠道适配批全部落码收口（docs/38，Active work 34；ADR T28；四 feat＋2 test/docs commit 在 dev、未 push、合 main 待授权）**：`channels/` 包 Shopify Admin API 适配层全部落码——ShopifyChannelClient（list_orders/get_order/create_refund）、ChannelRegistry/ChannelStore、动态 Harness 适配器 `channel:shopify:{binding_id}`（shop/* 三工具），迁移 015_channel_bindings、REST /api/channels 五端点（审计 channel.bind/unbind，绑定 reset 不清），Connections 页「销售渠道绑定」卡；token 每次现解密不缓存不记日志，**零新依赖/零外部资源**。最终门：后端 **1220 passed/40 skipped**（1190 基线净增 30：内核 14/registry 10/API 6，4 PG 集成 skip）、前端 **577 passed/2 skipped/45 文件**（净增 6：apiClient 4＋i18n 2）；浏览器冒烟 3 截图存 docs/smoke-shots/（空态/绑定弹窗/未授权连接探活→error 态）。D22 不解除（真实店铺 OAuth 联调/入站 webhook/Amazon/订单同步仍缓做）。）
更新时间：2026-09-22（**📋 真实渠道适配批 docs-only 立项（docs/38，Active work 34；ADR T28，三处同步 10→02→09）**：T4 平台无关 OAuth2 连接框架之上新增 `channels/` 平台渠道适配层，v1 仅 Shopify Admin API（读订单/发起退款、动态 Harness 适配器 `channel:shopify:{binding_id}`、shop/* 三工具），迁移 015_channel_bindings、REST /api/channels 五端点，绑定 reset 不清；token 每次现解密不缓存不记日志，**零新依赖/零外部资源**，D22 不解除（真实店铺联调/入站 webhook/Amazon/订单同步仍缓做）。以下为上批收口记录。）
更新时间：2026-09-22（**✅ 审计可见与审批结果补强批落码收口（docs/37，ecfa325/bc3f116/033619a＋docs 收口；dev 三 commit 未 push）**：A admin 审计日志页（复用 audit events/export 端点，action 前缀/limit，Bearer blob 导出）、B 已决审批历史（broker list_decided＋GET /api/approvals/decided＋已处理 Tab）、C 决策结果邮件（notify_recipients＋notify_decided，全来源旁路 fail-safe、无 token/链接）。后端 1190 passed/36 skipped、前端 571 passed/2 skipped/45 files，浏览器冒烟过、控制台零错误，2 截图 docs/assets/audit-*.png；零新依赖/无 ADR，不解除 D11/D20/D24。详见 Active work 33 与 Recently shipped 1。以下为上批收口记录。）
更新时间：2026-09-22（**✅ 审批闭环批五原子全部落码收口（docs/36，e97ec37/bd6ec7a/8923473/e2771ba＋docs 收口；dev 四 commit 未 push）**：A 邮件一键决策（HMAC capability token、公开 email-view/email-decision、统一 404、actor=email-link 审计）、B 应用内审批队列页（10s 轮询、viewer 只读）＋免登录 /approvals/{token} 深链页。后端 1174 passed/36 skipped、前端 571 passed/2 skipped/45 files，浏览器六场景冒烟过、控制台零错误；ADR T27，三处同步 10→02→09 已完成；D20 不解除。另核：docs/35 T2–T6 已随 PR #54/#55 合 main（origin/main=`7e8cae6`），docs/34 过时措辞已加收口复核注记。详见 Active work 32 与 Recently shipped 1。）
更新时间：2026-09-22（**✅ T1 治理完成：delivery batch 已 push 并经 PR #53 合 main（merge `c1fe886`，CI 三道门全绿）；docs/35《真实接入交付批契约设计》已立项——T2 审批挂起邮件通知（D20 邮件子集）、T3 webhook 渠道（D24 子集）、T4 generic OAuth2 连接管理 v1（D22 子集，P0 #1 最大缺口）、T5 Grafana/Prometheus 样例（D11 子集）、T6 审计日志导出（P1 #8），五包均部分取回、不解除缓做；迁移 013 audit_events/014 oauth_connections；落码按 docs/35 §9 九步原子序推进——②T5、③T6、④T3、⑤⑥T2、⑦⑧T4 均已落码收口（T4 feat `a57164d`，后端 1141 passed/36 skipped、前端 557 passed、lint 0/0、build 过），**⑨总收口亦已完成（2026-09-22）：PG 直连集成 013+014 共 38 passed、d26/m11/d28 三内存档 smoke 全过、T4 HTTP 端到端冒烟 ALL_PASS，契约文档 03/09/12/13/14/00/08/35＋CHANGELOG 全回填（最终后端 1141 passed/36 skipped、前端 557 passed/2 skipped/44 文件、oxlint 0/0、build 过）。T2–T6 五包＋总收口全部完成；该批 12 commit（`ac6d9ff`→`ae95968`）**已 push origin/dev（dev==origin/dev=`ae95968`）、尚未合 main（dev 领先 main 12 commit，待开 PR；合 main 须用户授权）**；T1 delivery batch 已随 PR #53 合 main（merge `c1fe886`）、整栈 PG 修复 `b2ac85d`＋docs `66717d3` 已随 PR #52 合 main（merge `a799fa6`）。**详见 Active work 31 与 Recently shipped 1。**）
更新时间：2026-09-22（**✅ 2026-09-22 docs/34 对外交付层小批（A+B 九项）落码收口：MIT LICENSE、.pre-commit、/api/ready、/metrics（Prometheus 进程内）、SMTP email 真实投递、Caddy TLS 反代样例、前端 oxlint warning 清零（首次 0/0）；后端 1073 passed/34 skipped、前端 555 passed/2 skipped、build 过、compose 默认/edge 两档合法；D10b/D11/D24 部分取回不解除，双口径 MVP 结论不变；dev 待 push。详见 Active work 30 与 Recently shipped 1。**
**✅ 2026-09-22 整栈 PG 装配缺口修复（单代码原子 `b2ac85d`）：为 `PgUserStore` 补 `bind_session_store` 同构 no-op，解除 `ATLAS_STORAGE_BACKEND=pg` 整栈 uvicorn 装配期 AttributeError；真实 uvicorn `/api/health` 200、U269 转整栈 PG 正式、后端内存档 1051/34 不变、PG 直连 56 passed/1 skipped；docs/29 该具体装配缺口解除（不改变六项阻断总评）。详见 Active work 29 与 Recently shipped 1。**
**✅ 2026-09-21 docs/33「影子模式＋监控告警补强＋i18n 续批」打包 12 原子全部落码收口：后端 1051 passed/34 skipped、前端 555 passed/2 skipped/43 文件、build ✓、oxlint 0 error/2 既有 warning，内存档 d26/m11/d28 三 smoke 过、PG 直连 integration 43 passed、真实浏览器 4 截图；D12/D26/D28 仅部分取回、均不解除缓做；详见 Active work 28 与 Recently shipped。以下为立项时原文。**
**2026-09-21：影子模式＋监控告警补强＋i18n 续批打包 docs-only 立项**：用户拍板打包一+打包三共 6 项（docs/14 D12/D26/D28 工程内可闭环余部，进程内 v1、零新依赖、不解除缓做条目），契约设计见 **docs/33**（本包形状权威）。四批：①i18n 续批四原子（editor 框架/画布/左栏＋属性面板/调试台、monitoring、memory；不引 i18next、en-US 空占位、专名不译）②D26 影子模式（Capability.permission 判定 dry-run、旁路录制、4 端点、ring 不 PG 化）③D28 Trace 钻取（迁移 012 spans JSONB＋单运行 trace 端点＋前端瀑布）④D28 告警静默/未确认惰性升级/值班手动轮换；共 11 个新端点，仅 spans 走 PG。12 原子序、U240–U309、三道门基线（后端 1008p/31s、前端 509p/2s/40 文件、build 过、oxlint 0 error/2 既有 warning）固化于 docs/33 §8。**当前进度＝原子① docs-only 立项已完成（`9fcf40b`）、原子② i18n editor-a（框架＋画布＋左栏面板，含 monitoring/memory namespace 空骨架注册）已落码收口（`97d61dc`，前端 516 passed/2 skipped/40 文件、tsc＋build 过、oxlint 0 error/2 既有 warning，后端零改动 1008 不变）；原子② i18n editor-a（`97d61dc`）、原子③ i18n editor-b（属性面板＋调试台，`0c299ef`）均已落码收口（前端累计 518 passed/2 skipped/40 文件、tsc＋build 过、oxlint 0 error/2 既有 warning，后端零改动 1008 不变）；原子④ i18n monitoring 页已落码收口（`3445576`，监控告警全页外壳＋lib 标签映射改 i18n key 由 t() 解析，zh-CN/monitoring.json 约 100 键，i18n.test 净增 5 用例，前端累计 523 passed/2 skipped/40 文件、tsc＋build 过、oxlint 0 error/2 既有 warning，后端零改动 1008 不变）；原子⑤ i18n memory 页已落码收口（`bf9cdb6`，长期记忆全页＋lib MEMORY_KIND_LABELS 改 i18n key 由 t() 解析，zh-CN/memory.json 39 键，buildMemoryPayload/parseStringMapText 表单校验 message 照 conditions 校验先例原样不抽、JSON 示例 placeholder 保留，i18n.test 净增 3 用例，前端累计 526 passed/2 skipped/40 文件、tsc＋build 过、oxlint 0 error/2 既有 warning，后端零改动 1008 不变）；**批 1 i18n 四原子（②③④⑤）全部收口；原子⑥影子模式后端已落码收口（`cea7111`，U250–U269、后端 1029 passed/32 skipped，零 RunRecord/告警/门控/tool_metric/店铺副作用），原子⑦影子前端已落码收口（`c29ce15`，前端 536 passed/2 skipped/41 文件、build/lint 过、后端零改动 1029 不变），原子⑧trace 后端已落码收口（`4579843`，后端 1037 passed/33 skipped、U278 PG 集成过、迁移 012 已应用），原子⑨trace 前端瀑布已落码收口（`ce6cc23`，前端 548 passed/2 skipped/42 文件、build/lint 过、后端零改动），原子⑩告警静默/惰性升级/值班轮换后端已落码收口（`00bba8c`，后端 1051 passed/34 skipped、U299 PG 集成过），原子⑪告警静默/升级/值班前端已落码收口（`1804255`，前端 555 passed/2 skipped/43 文件、build/lint 过、后端零改动），**原子⑫总收口亦已完成（2026-09-21）：最终三道门后端 1051 passed/34 skipped、前端 555 passed/2 skipped/43 文件、tsc+vite build ✓、oxlint 0 error/2 既有 warning；内存档 d26/m11/d28 三 smoke 全过、PG 直连 integration 43 passed（整栈 PG uvicorn/U269 受既有 PgUserStore.bind_session_store 缺失阻断，非本包回归、已登记 docs/29）；真实浏览器冒烟过 i18n 中文/值班轮换/静默闭环/未确认升级/适配器埋点/Trace 三级瀑布/影子卡，4 截图 docs/assets/d28-*.png；契约 03/04/06/09/12/13/14/17/00/08/29/33＋CHANGELOG 全回填；另修静默联动缺陷 90b942a、d28_smoke 扩充 8b29396。至此 docs/33 打包 12 原子全部收口，D12/D26/D28 仅部分取回、均不解除缓做**（Active work 28 ✅）。
**2026-09-21：P0 批 3 真实接入安全准入已落码收口并随 **PR #48 合并 dev→main（merge `5a5976c`，CI 三道门全绿：Backend 2m4s/Frontend 37s/gitleaks 11s；dev 长期分支保留在 `8f8f0b0`）**（形状权威 docs/32、ADR T26 定稿 (a) 引 cryptography 落 AES-256-GCM、Active work 27 已 ✅）：五原子 egress `06b2473`/DB guard `a189dc1`/resilience `5376e79`/deps·env `a61680b`/secrets `01a2f97`，新增 7 错误码（EGRESS_DENIED/EGRESS_INVALID_URL、DB_SQL_NOT_READ_ONLY/DB_WRITE_FORBIDDEN、SECRET_UNAVAILABLE/SECRET_DECRYPT_ERROR、HTTP_CIRCUIT_OPEN）；后端 1008 passed/31 skipped（净增 186）、PG database 集成 3 passed、前端零改动 509 passed、离线冒烟四类 fail-closed 错误码零外呼＋公网带秘密 200 全过，U233–U239 转正式；docs/29 阻断项 #3 的纯逻辑安全准入子集解除，D22/D23/D24 不解除（真实 OAuth/SMTP/IM/webhook/数仓、多实例/KMS/轮换、DNS-pinning transport 仍随真实渠道/部署批）。** **2026-09-21（早些）：P0 批 3 真实接入安全准入已 docs-only 立项（形状权威 docs/32、ADR T26、Active work 27；零代码、未落码，待发令开工）：甲方案纯逻辑校验层——凭证信封＋运行时注入脱敏、SSRF egress 私网/云元数据拦截＋白名单、DB 只读＋SQL 静态审查、出向重试/按 host 熔断；不立 OAuth2/真实邮件/真实数仓假集成；U233–U239；唯一待落码拍板＝真 AES-GCM 是否引 cryptography（T26 候选 a/b）。** **2026-09-21：P0 批 2 生产认证已落码收口（dev 领先 origin/dev、未 push；形状权威 docs/31、ADR T25、Active work 26 已 ✅）：后端五原子 `cd1133a`（scrypt 哈希＋种子哈希账号）/`097c056`（iam_users 迁移 010＋UserStore 两档/幂等 seeder）/`e65e4df`（账号生命周期五端点）/`4224578`（会话绝对 TTL 迁移 011）/`36ab54e`（登录滑动窗口节流）＋前端 `222552d`（用户管理页/改密 UI）；后端内存档 822 passed/31 skipped、前端 509 passed/2 skipped/40 文件、oxlint 0 error/2 既有 warning、curl 23 项 HTTP 契约全绿、真实浏览器 9 项冒烟＋8 截图 docs/assets/p0b2-*.png，U227–U232 转正式，docs/29 阻断项 #2（生产鉴权）解除（单实例口径；SSO/MFA/邮箱找回/JWT/审计/多实例节流仍缓做 14）。下一步＝批 3 真实接入安全准入纯逻辑层（凭证加密存储/SSRF egress 白名单/DB 只读＋SQL 审查/重试熔断，不写假 OAuth/假邮件），先 docs-only 契约立项。** 2026-09-21 早些时候：**P0 批 1 持久化交付已落码收口（dev 领先 origin/dev，未 push；六原子 20b18f8→11f9014，docs/30 为形状权威，Active work 25 已 ✅）：compose 起 pgvector:pg16＋命名卷＋entrypoint 迁移、schema_migrations 运行器、PG 会话、备份/恢复；后端 792/PG 集成 26/前端 498 全绿，真实删卷演练通过（八项行数一致、旧 token 有效、挂起审批可续跑），docs/29 阻断项 #1 解除。批 2 生产认证、批 3 真实接入待立项。** 2026-09-20 历史：**2026-09-20 再续：M12 续批 dashboard namespace 首批生产接线已落码收口（feat＋docs 原子已 push、dev==origin/dev==`2f0351b`；U219–U221，前端 498/40 文件、后端零改动 782、截图 m12-dashboard；PR #43（merge `5a41f9e`）已把 M12 骨架 `5200fe0` 合入 main，dashboard 两 commit `d6dc855`/`2f0351b` 仍在 dev、待下一 PR 合 main）。** **2026-09-20 续：dev 全量已经 PR #42（merge `1956379`）合并 main，下方各批收口时的“未 push/待发令”措辞均为当时状态；PR #42（merge `1956379`）后又有 PR #43（merge `5a41f9e`）把 M12 骨架合入 main，现 origin/main=`5a41f9e`、origin/dev=`2f0351b`。** **M12 i18n 零依赖骨架已落码收口（docs 刷新 `7f65ce8`、立项 `3197e07`、骨架 `9f1963f`、接线 `a894fa7`＋docs 收口原子；U212–U218，前端 495/40 文件、后端零改动 782、浏览器两截图；M12 五 commit 已全部 push、dev==origin/dev==`5200fe0`）。** **docs/28 工程内可闭环缓做项 11 项打包四批全部落码收口、#36 总收口完成：批 4 D35·D21 三原子 `ec0fd81`/`ba97088`/`19f31b4` ＋ docs 回填原子（含两张 batch4-*.png，均未 push）；U192–U211 转正式；三道门最终后端 782 passed/24 skipped、PG integration 全量 24 passed、前端 482 passed/2 skipped/39 文件、build 过、lint 0 error/2 既有 warning；d26/m11/d28 三 smoke PG 档全过；真实浏览器两截图 docs/assets/batch4-{memory-edit,subgraph-upgrades}.png；零新依赖/不新增 ADR/不解除 D21·D26·D27·D28·D35。工程内可闭环缓做余部已清，剩 i18n（触发＝首个英文使用者，未到；设计 token 已落码）或进入 Phase 3 真实种子试用准备。** 同日早些时候历史：**工程内可闭环缓做项 11 项打包 docs-only 立项 `b0d8023` 已提交，**批 1 D26 四项已落码收口（四原子 `d1f455b`/`c7bf138`/`89e21fc`/`0e8ebe7` 未 push、U139–U148 转正式、后端 720 passed/20 skipped·PG integration 8 passed·前端 472/build 过/lint 0 error、d26＋m11 双 smoke 内存·PG 两档全过、PG 新端点 HTTP 冒烟 14 项、真实浏览器三场景 3 截图 docs/assets/batch1-*.png）；**批 2 D27 三项亦已落码收口（四原子 `038dcaa`/`9b378c2`/`f6f7d30`/`c13886c` 未 push、U150–U165 转正式、后端 736 passed/20 skipped·PG integration 8 passed〔批 2 无 DDL〕·前端 473/build 过/lint 0 error、d26＋m11 双 smoke PG 档全过、httpx 并发 SSE 端到端、真实浏览器三场景 3 截图 docs/assets/batch2-*.png）**；**批 3 D28 两项亦已落码收口（四原子 `a318d97`/`11b1ba7`/`4256326`/`dc46f20` 未 push、U166–U191 转正式、后端 764 passed/22 skipped·PG integration 10 passed〔批 3 无 DDL，沿用迁移 008〕·前端 475/build 过/lint 0 error、d26＋m11＋d28 三 smoke PG 档全过、真实浏览器两场景 2 截图 docs/assets/batch3-*.png；批 4 D35·D21 待开工，见 Active work 22）**；立项时规划：批 1 D26 四项＝迁移 008 修 PG recordings 富字段 500 缺口/Mock 工具回放/用例编辑 PUT＋入参覆写/跨图报告看板，批 2 D27 三项＝暂停帧变量历史/异常断点/子图内部断点，批 3 D28 两项＝适配器 tool_metric 埋点分位/自定义告警规则 DSL，批 4＝D35 记忆 POST·PUT 写入端点＋D21 子图升级体检 UI；零新依赖、不新增 ADR、不解除 D21/D26/D27/D28/D35，见 Active work 22）**。**C+A+B 打包之 B 包（协作式运行急停＋hitCount/logpoint 断点＋暂停 globals 改写续跑）已落码收口（feat `f9a1301`，U124–U138 转正式；后端 712 passed/前端 472、HTTP SSE＋真实浏览器三场景冒烟过、4 截图 docs/assets/b-*.png；docs 回填 27/03/04/06/09/12/13/14＋handoff/CHANGELOG，C+A+B 三包全部收口、总收口全绿（见 Active work 21，含一处 tsc build 回归修复 `b521f5a`）；本批 commit 默认未 push）**。**2026-09-19：M11 记忆/长期上下文已四批落码收口（四原子 `5441902`/`58d936c`/`73e53bd`/`75c4150`，U72–U99 转正式；契约 docs/26 为形状权威、ADR T23、08 M11 落码收口条、14 D35 非目标仍缓做，文档矩阵 26/03/08/09/12/13/14/00/CHANGELOG/README 已同步；后端 673/前端 449、两档 m11_smoke 全过、d26 零回归，见 Active work 19 与 Recently shipped 1；4 commit 默认未 push）**。**同日续：进程内运行体验补强 C+A+B 已完成 docs-only 立项（形状权威 docs/27；用户拍板 D 判空集不落码、A/B 取进程内 v1、C 直接做；零新依赖不新增 ADR 不解除 D15/D21/D27/D30，见 Active work 20；立项提交本身未落业务代码）**。**同日再续：C 包（D15 余部 可注入回放时钟 + today/now/datetime/hoursBetween + RecordingCase.recorded_at 冻结锚点）已落码收口（单原子 `3415377`，U100–U113 转正式；后端 687/前端 453、两档 m11 与内存档 d26 全过；两处落码细化＋PG recordings 富字段既有 500 缺口〔归属 D26〕见 docs/27 顶部 C 落码注记，见 Recently shipped 2 与 Active work 20；commit 默认未 push）。**同日再续：A 包（D21 进程内形态——子图内部 node_start/node_end 以可选 subgraphPath 上 SSE、吞子层 run_end、子图内 human_approval 经共享 broker＋现有审批 Modal/决策端点交互，无新端点）已落码收口（单原子 `e594a4b`，U114–U123 转正式；后端 695 passed/18 skipped、前端 457 passed/2 skipped/38 文件，真实 HTTP SSE 端到端＋真实浏览器子图审批冒烟过、2 截图 docs/assets/a-subgraph-*.png，零新依赖/不新增 ADR/不解除 D21，见 Recently shipped 1 与 Active work 20；commit 默认未 push），C+A+B 打包仅剩 B 包待续**同日此前：**A+B 打包六项已全部落码收口（立项 `646ca79` + 六原子 `ac8c718` D15 表达式函数库/`ee534ef` B1 parallel.result 汇聚可见性/`25db6df` B2 subgraph.outputs 深层/`8496b2d` B3 改 id quickFix 联动/`5f87654` D17 loop break·continue/`f45daa9` D18 parallel any_success + build 修复 `4b9396e`，U66–U71 转正式；后端 **645 passed/13 skipped**、前端 **436 passed/2 skipped/35 文件**、oxlint 0 error、tsc+vite build 过、d26_smoke **24 断言全过**零运行语义回归，零新依赖/不新增 ADR/不解除 D15·D17·D18·D30，见 Recently shipped 1 与 Active work 18；本批 8 commit 默认未 push）**。此前同日：**D26/D30 收尾小项打包已落码收口（立项 `d4cd42b` + 五项原子提交 `29bb3d9` 子图快照内联/`8a37b4e` 报告 CSV·JSON 导出/`cea70f4` 周一定时回放 CI/`9a357df` GRAPH_DATA_CYCLE 数据依赖环/`47ecf48` REF_TYPE_MISMATCH 标量类型 warning，U61–U65 转正式；后端 **628 passed/13 skipped**、前端 **418 passed/2 skipped/35 文件**、oxlint 0 error、d26_smoke 24 断言，零新依赖/不新增 ADR/不解除 D26·D30，见 Recently shipped 1 与 Active work 17；本批 6 commit 默认未 push）**；一处契约订正：subgraph 快照**仅单用例 replay 内联、发布门禁 gate 保持实时 resolver**（验当前 latest 草稿，坏子图应 block）。此前 2026-09-18：M9 入站 Router+灰度发布+指标门控自动回滚、M8 交互模板、M10 span 追踪全部收口（M10 详细过程已按 5 条上限滚入归档补遗），U49–U59 转正式；D26 用例集报告 v1 落码（U60，见 Recently shipped 3）；**题一 M1–M4、题二 M5a/M6/M5b/M7/M8/M9/M10 全部闭合——M1–M10 主线全闭合**）

## 二、旧 Recently shipped 最近 5 条（归档自旧主 handoff）

1. ✅ **feat：wait 事件等待进程内 v1 批落码收口（2026-09-23，docs/47；`32abb9f`→`f647c96`/`aa82eed`/`1608002`/`ce8c4f0`＋docs 在 dev、未 push；零新依赖/零迁移/无 ADR，D19 部分取回不解除）**——wait v1 仅 duration 盲等，本批接通事件等待进程内形态：per-tenant `EventWaitBroker`（event_key 索引、token=`wait-`+uuid4、广播/直投、monotonic deadline、0.2s 取消切片），DSL event 四字段（eventKey 支持 {{}} 插值、运行时渲染复检 WAIT_EVENT_KEY_INVALID、timeoutSeconds 1-3600、onTimeout continue/fail），三 REST 端点（POST /api/waits/events 广播、POST /api/waits/{token}/signal 直投〔404 WAIT_TOKEN_NOT_FOUND/409 WAIT_ALREADY_SIGNALED〕、GET /api/waits pending），run inputs waitEvents 预置（resolvedBy:"input"）；超时 fail → run failed WAIT_TIMEOUT_FAILED。前端 WaitConfig 定时/事件双模式、wait schema oneOf、L1 eventKey/timeout/onTimeout 字段诊断。门：后端 **1435 passed / 59 skipped**（净增 37，零回归）、前端 **606/2**、lint/build 干净；HTTP 冒烟 **19 检查全过**（`.smoke/event_wait_smoke.py`），浏览器冒烟 2 截图 docs/smoke-shots/event-wait-*。持久化中断帧/恢复扫描器/多实例、动态时长仍缓做。详见 Active work 43、docs/47。

2. ✅ **feat：OpenAPI securitySchemes 静态密钥批落码收口（2026-09-23，docs/44；`5a7e580`→`ab08c12`/`2d534a2`/`43d8001`＋docs 五 commit 在 dev、未 push；零新依赖/无 ADR，D22 部分取回不解除）**——docs/42/43 导入链路忽略全部安全声明，本批接通静态密钥：parser 收录 apiKey（header/query）与 HTTP Bearer、有效要求＝operation 覆盖全局的 OR-of-AND 组（unsupported 方案名剔除、空组＝匿名）；密钥只以 SecretProvider 信封（T26，AES-GCM/plain 两档）随 ImportedSpec 落内存/PG（迁移 019 加 security_schemes、credential_envelopes 两 JSONB 列；两端 store 加 put_credentials），适配器每次调用现解密注入（header/query；用户参数优先），缺密钥 fail-closed `OPENAPI_CREDENTIAL_MISSING` 不发请求；`PUT /api/openapi/imports/{id}/credentials`（operate，未知 scheme 422 OPENAPI_INVALID_CREDENTIAL，清空即删除），前端 Password 输入＋「已配置 n/m」状态进 openapi namespace，任何响应不回显明文。门：后端内存档 **1375 passed / 59 skipped**（净增 23 常跑：parser 10/adapter 7/API 6，零回归）、PG 直连 **11 passed**（往返/跨重启信封仍在/旧行默认 `{}`）、前端 **593/2/45**、build 过。浏览器冒烟 3 截图 docs/smoke-shots/openapi44-{1-preview-credentials,2-missing-credential,3-success}.png：无密钥中文失败 → PUT 配密钥 → SUCCESS HTTP 200（body Rex）；PG 档重启后仍 200。收口后恢复普通内存档 uvicorn。详见 Active work 40、docs/44。

3. ✅ **feat：OpenAPI 导入规格 PG 持久化批落码收口（2026-09-23，docs/43；`f39d86b`→`7bf13f5`＋docs 三 commit 在 dev、未 push；零新依赖/无 ADR，D22 部分取回不解除）**——docs/42 导入规格由「仅进程内、重启即失」升级为两档：迁移 `018_openapi_imports`（(tenant_id,id) PK、seq 走 storage_id_seq、operations JSONB）＋`PgImportStore`（与 ImportStore 同形 add/list/get/delete，5 specs/200 ops、OPENAPI_LIMIT_EXCEEDED 422 不变、行级 tenant 过滤、reset 不清），iam/registry 仅 PG 档换装配、TenantServices 字段类型放宽为 `ImportStore | PgImportStore`；发现按请求读 store.list() 动态重建适配器（main.py:345），**重启无需预热**。REST 五端点/前端/编辑器/执行链路零改动、无新错误码。门：后端内存档 **1352 passed / 56 skipped**（默认新增 8 PG 测 skip）、PG 直连 **8 passed**（atlas-pg 实跑：往返/skipped 丢弃/seq 排序/模拟重启/跨租户/删除幂等/双上限/reset 不清），前端 **591/2/46** 零改动；PG 档浏览器冒烟（假 DNS＋MockTransport，2 截图 docs/smoke-shots/openapi-pg-smoke-*）：重启后列表 openapi-768 仍在、适配器自动重建，编辑器 list_pets limit=10 运行 tool_call-1 **SUCCESS HTTP 200**，控制台仅一次预期旧 token 401。收口后恢复普通内存档 uvicorn。详见 Active work 39、docs/43。

4. ✅ **feat：真实渠道适配批全部落码收口（2026-09-23，docs/38；ADR T28；`d9592b5`→`3d6d28a` 六 commit 在 dev、未 push；零新依赖/零外部资源，D22 不解除）**——T4 connections 平台无关框架之上新增 `src/atlas/channels/`：ShopifyChannelClient（list_orders/get_order/create_refund，版本默认 2025-01，结构化错误码 CHANNEL_NOT_BOUND/UNAUTHORIZED/UPSTREAM_FAILED/INVALID_RESPONSE/INVALID_PARAMETER）、ChannelRegistry＋ChannelStore（绑定 reset 不清）、按绑定动态产出的 ShopifyHarnessAdapter（适配器 id `channel:shopify:{binding_id}`，工具 shop/list_orders·shop/get_order〔read〕·shop/create_refund〔financial〕），迁移 `015_channel_bindings`＋PgChannelStore，REST `/api/channels` 五端点（read/operate/administer，审计 channel.bind/unbind）；token 每次现解密、不缓存明文、不记日志；前端 Connections 页「销售渠道绑定」卡＋channels namespace（zh 填实/en-US 空 `{}`）。门：后端 **1220 passed/40 skipped**（1190 基线净增 30 零回归；4 PG 集成 skip 门控）、前端 **577 passed/2 skipped/45 文件**（净增 6）、oxlint/build 过；浏览器冒烟三场景（空态/绑定弹窗/未授权连接探活→绑定 error 态「绑定连接未完成授权或令牌不可用」）、控制台仅 antd deprecation＋一次预期 401，3 截图 docs/smoke-shots/。真实店铺 OAuth 联调/入站 webhook/Amazon/订单同步仍缓做 D22。详见 Active work 34、docs/38。

5. ✅ **feat：审计可见与审批结果补强批全部落码收口（2026-09-22，docs/37；ecfa325/bc3f116/033619a 三 commit 在 dev、未 push；D11/D20/D24 边界不变、均不解除）**——三处工程内可见性缺口一次补齐：**A 审计日志页**，`pages/AuditLog.tsx` admin 应用内页面（复用既有 events/export 端点零后端改动，action 前缀过滤、limit 50/100/200、Bearer blob 导出，audit namespace）；**B 已决审批历史**，broker `list_decided` 只读投影（resolved_at 倒序、clamp 1–200、不泄露 card_context/Event）＋`GET /api/approvals/decided`（read、全角色、租户分区）＋Approvals「已处理」Tab（切 Tab 加载不轮询，10s 轮询仅待处理 Tab 激活且可见时）；**C 决策结果邮件**，`_Pending.notify_recipients`＋`notify_decided` 纯文本结果邮件（无 token/链接、comment ≤200），各决策路径旁路 fail-safe 触发、异常不改决策响应不阻断图、重复决策不重发。门：后端 **1190 passed/36 skipped**（净增 16 零回归）、前端 **571 passed/2 skipped/45 files**（计数不变）、lint/build 过；浏览器冒烟过（审计页过滤/导出/空态、已处理 Tab）、控制台零错误，2 截图 docs/assets/audit-{log-page,decided-tab}.png。详见 Active work 33、docs/37。
