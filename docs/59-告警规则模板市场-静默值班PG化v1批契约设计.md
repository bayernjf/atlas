# 告警规则模板市场 / 静默·值班 PG 化 v1 批契约设计（打包 F）

> 立项：2026-09-24（AI 承接「一口气搞了」总授权，docs-only 立项原子）。
> 形状权威：本文件。缓做取回：**D28 部分取回、不解除**（规则模板市场＝D28 明列余部；静默/值班/升级持久化＝docs/33 §5 标注的 v1 进程内缺口）。
> 约束：**零新依赖**；F-2 新增**手写幂等迁移 024**（002 新装库同步）；**不新增 ADR**；内存档行为不变、PG 档为纯超集持久化；REST 形状对前端保持兼容。

## 0. 已核实的现状缺口（2026-09-24 对活代码）

- **规则模板市场（D28 余部，零进展）**：自定义规则 DSL 已落（docs/28 批 3，`RuleConfig.custom`、`custom:{cid}`、白名单上下文 `{status,durationMs,failedCount,hasError}`，前端 List 编辑器），但用户需手写表达式、无内置预设、无一键启用。流程模板有成熟先例 `template/catalog.py`（只读 Python 常量 + `GET /api/templates`/`GET /api/templates/{id}`，无 DB/CRUD、不受 reset 影响）与卡片目录 `cards/`（`GET /api/cards`）。
- **静默 / 值班状态仍进程内（docs/33 §5 自述缺口）**：`monitoring/silences.py` 的 `OpsStore`（单锁，`_silences` list cap 100、`_schedule` OnCallSchedule、`_assignees` alert_id→人 map）被**内存档 `MonitoringStore` 与 PG 档 `PgMonitoringStore` 共用**。
  - PG 档告警本体在库（`monitoring_alerts`，迁移 021 已补 `rule_name/escalated_at/assignee` 三列、002 同步），但：
    - **静默不落库**：`POST/GET/DELETE /api/monitoring/silences` 在 PG 档仍委托进程内 `OpsStore`，重启清空、多实例不共享；`suppressed_count` 只活在内存对象。
    - **值班不落库**：`GET/PUT /api/monitoring/on-call`、`POST .../rotate` 同样委托 `OpsStore._schedule`，重启回到空表/ index 0。
    - **assignee 取自进程内值班**：`PgMonitoringStore._raise_or_merge_locked` 新建告警时 `assignee = self._ops.current_assignee()`（值班在内存，重启恒为 None），再 `remember_assignee` 写进程内 map；虽然 `monitoring_alerts.assignee` 列存在，但其值来源（值班表）不持久，merge/decorate 还用 `self._ops.assignee_of(...)` 兜底。
  - **惰性升级 `escalated_at` 已 PG 化**（docs/55，`_decorate_alerts_locked` 读时评估并 `UPDATE severity/escalated_at`），**本批不重做**，仅随值班 PG 化确认 assignee 来源持久。
- 迁移现状：手写幂等 SQL **已到 023**（`023_openapi_imports_dedupe_softdelete.sql`），本批从 **024** 起；002_storage.sql 为新装库全量建表，加表须两处同步（021 注释确立的先例）。
- id 生成：PG 档 `_next_id(conn, "<prefix>")` 走租户序列，run/alt 等已用；静默 PG 化用前缀 `sil`。

## 1. 范围与非目标

### 1.1 本批范围（两项）

- **F-1 告警规则模板市场 v1（内置只读 + 一键启用）**
  - 新模块 `monitoring/rule_templates.py`：随代码发布的只读内置规则模板目录（无 DB / 无 CRUD / 不 reset / 全局共享，照 `template/catalog.py`）。
  - 两个只读端点：`GET /api/alert-rule-templates`（列表投影，**不含 config**）、`GET /api/alert-rule-templates/{id}`（含完整 config），未知 id 404，`read` 鉴权。
  - 一键启用＝前端取详情 config 后**复用既有 `PUT /api/monitoring/rules`（administer）全量替换**，后端不新增写端点（保证规则校验只有 `validate_rules` 一条通道）；前端二次确认「将全量替换当前规则配置」。
- **F-2 静默 / 值班 / 告警 assignee PG 化（仅 PG 档；迁移 024）**
  - 新表 `monitoring_silences`、`monitoring_oncall`（per-tenant）；002 新装库同步。
  - `PgMonitoringStore` 的静默 3 方法、值班 3 方法、`record_run` 内静默命中压下与计数、新建告警 assignee 全部改走 PG；**内存档 `MonitoringStore` + `OpsStore` 一行不动**。
  - PG 档移除对进程内 `OpsStore` 状态的依赖（Silence/OnCallSchedule/OnCallEmpty 与纯函数仍从 silences.py 复用）；`reset` 清两新表。
  - 语义对齐内存档：静默惰性过期、cap 100、命中只计数不建/不合/不升级；值班去重保序、set 复位 index、rotate 取模、空表 rotate 409；assignee 仅**新建**告警指派、合并/静默不指派。

### 1.2 非目标（继续缓做，触发条件不变）

- 规则模板的用户自建 / CRUD / 分享 / 市场上传 / PG 存储 / 多语言 / 按图绑定；模板与当前规则的「合并/增量」策略（v1 只做整包替换）。
- 静默的编辑（只能建/删）、静默命中通知、按日定时值班轮换 / 排班日历 / 时区 / 多值班组 / 值班 IM 通知、值班用户名有效性校验（沿用 v1 不校验）。
- 多实例分布式锁 / 跨实例实时广播（PG 化提供共享状态与单实例口径正确性，不引入 SELECT FOR UPDATE 之外的强一致调度；命中计数以单条 UPDATE 原子自增）。
- OTel/Prometheus/Grafana 正式栈（D11）、运行记录长保留 / 时序报表、外部告警出口富文本（D24）、规则图级绑定。

## 2. F-1 规则模板市场契约

### 2.1 内置目录（`monitoring/rule_templates.py`）

```python
class RuleTemplateMeta(BaseModel):
    id: str
    name: str
    description: str
    tags: list[str] = Field(default_factory=list)
    config: dict[str, Any]   # 完整 RuleConfig 形状，必须通过 validate_rules

RULE_TEMPLATES: tuple[RuleTemplateMeta, ...]
def list_rule_templates() -> tuple[RuleTemplateMeta, ...]
def get_rule_template(template_id: str) -> RuleTemplateMeta | None
```

内置 4 个（config 均为完整 RuleConfig，四段 `run_error/node_failed/consecutive_failures/failure_rate` 齐备，落测试断言 `validate_rules(config) == []`）：

| id | name | 要点 |
|---|---|---|
| `default-balanced` | 默认均衡 | 四内置全开；consecutive threshold=3；failure_rate window=20/min_samples=5/rate=0.5；无 custom；不升级；recovery streak=1、无冷却（＝当前 `RuleConfig()` 默认，供一键复位） |
| `strict-sre` | 严格 SRE | consecutive threshold=2；failure_rate window=10/min_samples=3/rate=0.3；`escalation_ack_minutes=15`；`recovery_healthy_streak=2`；`recovery_cooldown_minutes=30` |
| `demo-lenient` | 演示宽松 | consecutive threshold=5；failure_rate window=20/min_samples=10/rate=0.8；不升级；recovery streak=1 |
| `custom-quickstart` | 自定义规则上手 | 四段默认 + 两条 custom（固定 cid，表达式经 `validate_expression` 合法）：`tpl-node-failed`「任一节点失败即 critical」`{{failedCount}} > 0`；`tpl-slow-run`「运行超 5 秒 warning」`{{durationMs}} > 5000` |

模板为静态只读常量，cid 固定无碍：一键应用是 **PUT 整包替换**而非追加，重复应用得到同一 config，不产生重复 custom 段。

### 2.2 REST

| 方法 | 路径 | 鉴权 | 说明 |
|---|---|---|---|
| GET | `/api/alert-rule-templates` | read | `{items:[{id,name,description,tags}]}`，投影不含 config |
| GET | `/api/alert-rule-templates/{id}` | read | 完整 `RuleTemplateMeta.model_dump()`（含 config）；未知 404 `{"detail":"规则模板不存在：{id}"}` |

全局只读、不按租户分区、不受 `/api/demo/reset` 影响（与 `/api/templates`、`/api/cards` 同类基础设施）。一键应用复用 `PUT /api/monitoring/rules`，其 422 校验/中文聚合错误不变。

### 2.3 前端

- `apiClient` 增 `AlertRuleTemplateSummary`/`AlertRuleTemplate` 类型与 `getAlertRuleTemplates()`/`getAlertRuleTemplate(id)`。
- Monitoring 页规则 Card 增「规则模板」按钮（administer 可点，只读角色禁用），Modal 列出模板（name/description/tags），「应用此模板」→ 拉详情 → Popconfirm 二次确认（提示全量替换）→ `updateRules(config)` → 复用现有规则刷新与错误提示。
- 文案走 i18n（monitoring namespace 增补模板相关键，zh/en 对齐、en 零汉字；技术词 custom/webhook 等不译）。

## 3. F-2 静默 / 值班 PG 化契约

### 3.1 迁移 024（`024_monitoring_silences_oncall.sql`，手写幂等；002 同步）

```sql
CREATE TABLE IF NOT EXISTS monitoring_silences (
    id TEXT PRIMARY KEY,
    tenant_id TEXT NOT NULL,
    rule_id TEXT,
    graph_id TEXT,
    reason TEXT NOT NULL,
    created_by TEXT NOT NULL,
    created_at TEXT NOT NULL,
    expires_at TEXT NOT NULL,
    suppressed_count INTEGER NOT NULL DEFAULT 0
);
CREATE INDEX IF NOT EXISTS idx_monitoring_silences_tenant ON monitoring_silences (tenant_id);

CREATE TABLE IF NOT EXISTS monitoring_oncall (
    tenant_id TEXT PRIMARY KEY,
    members JSONB NOT NULL,           -- ["alice","bob"]
    rot_index INTEGER NOT NULL DEFAULT 0,
    updated_at TEXT,
    updated_by TEXT
);
```

（`index` 避用裸列名，取 `rot_index`。）002_storage.sql 新装库追加同样两表；ALTER/CREATE 均 IF NOT EXISTS 可重复执行。

### 3.2 PgMonitoringStore 方法演进（内存档不动）

- **静默**
  - `create_silence(...)`：开事务，先惰性 `DELETE ... WHERE tenant AND expires_at <= :now`；`_next_id(conn,"sil")`；Python 算 `expires_at`（created_at + duration_minutes，UTC ISO）；INSERT；cap 100：活跃行仍 >100 时删最旧（`ORDER BY created_at`  LIMIT 超额）。返回 `Silence`。
  - `list_silences(active=None)`：SELECT 全租户行（`ORDER BY created_at`）→ `Silence`；`active` 过滤复用纯函数 `is_silence_active`。
  - `delete_silence(id)`：`DELETE ... WHERE tenant AND id` RETURNING，命中 True 否则 False（API 404）。
  - record_run 内命中压下：新增 `_suppress_if_matched_locked(conn, rule_id, graph_id, now)`——SELECT 本租户**未过期**静默行，Python 端用单一事实源 `silence_matches(s, rule_id, graph_id, now)` 判定，命中首条即 `UPDATE ... SET suppressed_count = suppressed_count + 1 WHERE id` 并返回 True（不建/不合/不升级，对齐内存档）。
- **值班**
  - `get_oncall()`：SELECT 单行 → `OnCallSchedule(members=row.members,index=row.rot_index,updated_at,updated_by)`；无行返 `OnCallSchedule()`。
  - `set_oncall(members,updated_by)`：去重保序/丢空白（复用内存档同一规整逻辑），UPSERT（`rot_index=0`）。
  - `rotate_oncall(updated_by)`：SELECT 行，members 空则 raise `OnCallEmpty`（API 409）；否则 `rot_index=(rot_index+1)%len` UPSERT，返回新投影（current 由纯函数 `current_assignee` 算）。
  - 新建告警 assignee：新增 `_current_assignee_locked(conn)`——读 oncall 行后用纯函数 `current_assignee`；`_raise_or_merge_locked` INSERT 的 `assignee` 改用它（列 021 已存在）。
  - 删除 merge/decorate/rollout_gate 中对 `self._ops.assignee_of / remember_assignee` 的调用：PG 档 assignee 以列为唯一权威（`_alert_from_row` 本就读列）；rollout_gate 不指派 assignee（INSERT 不含该列，保持 NULL）。
- **reset**：事务内追加 `DELETE FROM monitoring_silences` / `DELETE FROM monitoring_oncall`（均按 tenant）。
- PG 档不再实例化/引用 `OpsStore`（`self._ops` 移除）；仍 import `Silence, OnCallSchedule, OnCallEmpty, silence_matches, is_silence_active, current_assignee`。
- 内存档 `MonitoringStore` 与 `OpsStore` 完全不改，既有进程内测试零改动。

### 3.3 持久化语义（相对 v1 的可观察变化）

- PG 档静默 / 值班 / 告警 assignee **跨重启与跨 store 实例保留**（集成测试用「新建第二个 PgMonitoringStore 实例」模拟重启/多实例）。
- 静默命中计数 `suppressed_count` 落库，GET 读回累计值。
- 时间均为 tz-aware UTC ISO 字符串，过期判定纯函数不变。

## 4. Schema / 接口契约同步

- `monitoring_silences`、`monitoring_oncall` 两表入 docs/03 PG 表清单与 12 §3 监控端点注记；规则模板形状（RuleTemplateMeta）入 03/12。
- 不新增 Graph/EdgeDSL 版本、不改 422/告警事件形状；REST 新增仅两个只读 GET。
- 规则模板 config 是 RuleConfig 的子集形状权威＝alerts.py `RuleConfig` 与 validate_rules。

## 5. 测试契约（候选 U646 起，docs/13 收口回填）

- U646–U651 F-1 后端（纯内存，非 integration）：
  - U646 目录 4 模板 id 稳定、list 投影、get 命中/未知 None。
  - U647 每个模板 config `validate_rules == []`；custom-quickstart 两表达式 `validate_expression` 合法、四内置段齐全。
  - U648 `GET /api/alert-rule-templates` 列表不含 config、含 tags。
  - U649 `GET /api/alert-rule-templates/{id}` 含 config；未知 404。
  - U650 鉴权：未登录 401；viewer 可读 200。
  - U651 一键应用链路：admin `PUT /api/monitoring/rules`（模板 config）往返、`GET rules` 一致；非法模板 config（人为破坏）PUT 422（证明写校验唯一通道）。
- U652–U659 F-2 PG 集成（`ATLAS_RUN_INTEGRATION=1` + `DATABASE_URL`，迁移 apply 到 024）：
  - U652 create/list 静默落库、active 过滤、suppressed_count 初值 0。
  - U653 record_run 命中静默：不建告警且该静默 suppressed_count+1 落库；未命中照常建告警。
  - U654 过期静默：SQL 插 `expires_at` 旧行，list active=false、不命中、create 惰性清理。
  - U655 delete 静默 True/重复 False（404）。
  - U656 cap 100：插 100 活跃后 create，最旧被裁。
  - U657 值班 set/get/rotate：current 取模正确、空表 rotate raise OnCallEmpty；set 复位 index。
  - U658 **跨实例持久**：第二个 PgMonitoringStore 实例读到静默/值班/计数；新建告警 assignee = PG 值班 current 且写入 monitoring_alerts.assignee 列、跨实例读回。
  - U659 reset 清两表（本租户）且不影响他租户。
- U660–U662 前端（vitest，无 jsdom 走纯函数/fetch mock 先例）：
  - U660 apiClient 两新方法 URL/投影/404 抛出。
  - U661 模板→应用：mock getAlertRuleTemplate 返回 config 后 updateRules 以该 config PUT 一次。
  - U662 i18n：monitoring 模板新增键 zh/en 对齐、en 零汉字静态守护。
- 组件渲染（Modal 列表、Popconfirm、禁用态）由真实浏览器冒烟覆盖（同 docs/49-51/57 先例）。

## 6. 门与冒烟

- 三道门（收口实跑回填，数字以实跑为准，禁止预估）：
  - 后端全量 `.venv/bin/pytest`（内存档不回归 + 新增 F-1 内存测试）。
  - PG 集成：迁移 apply 到 024 后 `ATLAS_RUN_INTEGRATION=1 DATABASE_URL=postgresql+psycopg://atlas:atlas@localhost:5432/atlas .venv/bin/pytest <monitoring 集成文件>`。
  - 前端 `pnpm vitest run`、`pnpm oxlint`（0 error）、`pnpm build`。
- HTTP smoke（PG 档 live）：模板列表/详情/404/401；admin 应用 strict-sre 后 GET rules 字段一致；建静默→GET 见 suppressed_count、造一次命中运行后计数 +1；set 值班→rotate→GET current 轮换；重启等价（新 store 实例集成测试覆盖）。
- 浏览器冒烟：Monitoring 页规则模板 Modal 列表→应用（二次确认）→规则表单刷新为模板值；静默管理器与值班栏 UI 不回归（中文态）；切英文态模板 Modal 无汉字。

## 7. 契约同步矩阵（立项原子内完成）

| 文档 | 立项改动 |
|---|---|
| docs/59（本文件） | 新建，形状权威 |
| docs/00 | 文档地图表加 59 一行 |
| docs/03 | 预告两新表 + RuleTemplateMeta 形状（落码回填列细节） |
| docs/12 | 预告两个只读端点 + 静默/值班/assignee PG 档持久化语义 |
| docs/08 | 2026-09-24 打包 F 立项条（范围/非目标/候选 U646–U662/原子序） |
| docs/14 | D28 加「2026-09-24 打包 F 立项取回规则模板市场＋静默/值班 PG 化，部分取回不解除」 |
| docs/13 | 落码收口回填 U646–U662 定稿 |
| handoff | 顶部立项流水 + Active work 条目 |
| CHANGELOG | **收口才加**（立项不加） |

## 8. 原子提交序（feat 与 docs/handoff 分开，英文 message，不 push）

1. **F-0 docs-only 立项**：docs/59 新建 + 00/03/12/08/14 + handoff（不含 CHANGELOG）。
2. **F-1a** `feat(monitoring): built-in alert rule template catalog and read endpoints`（rule_templates.py + main.py 两端点 + U646–U651 测试）。
3. **F-1b** `feat(frontend): alert rule template market with one-click apply`（apiClient + Monitoring Modal + i18n + U660–U662）。
4. **F-2** `feat(storage): persist alert silences and on-call schedule in postgres`（迁移 024 + 002 同步 + PgMonitoringStore 改造 + U652–U659 集成测试）。
5. **F-收口**：三道门 + PG 集成 + HTTP smoke + 浏览器冒烟；docs/59 落码注记、03/12/13/14/08 回填、handoff、CHANGELOG；单独 docs 原子。

## 9. 风险与回滚

- **PG 档静默判定与内存档漂移**：缓解＝命中判定只调 silences.py 纯函数 `silence_matches`，SQL 只做「租户 + 未过期」粗筛，不把规则复刻到 SQL。
- **assignee 改造遗漏路径**：merge/decorate/rollout_gate/reset 多处引用 `_ops`；缓解＝移除 PG 档 `self._ops` 后让静态检查/全量测试暴露所有未清引用，逐一改为列权威或纯函数。
- **模板 config 与 RuleConfig 漂移**：缓解＝U647 对每个模板跑 validate_rules 断言，杜绝发布非法模板。
- **迁移幂等/新装库不一致**：024 全 IF NOT EXISTS、002 同步，apply 可重复执行；集成测试前先 apply 到 024。
- 回滚：F-1 为纯新增只读端点/文件，删除即回滚；F-2 回滚为还原 PgMonitoringStore 委托 OpsStore（迁移表保留不影响，向下兼容）。
