# Atlas 技术架构设计

> **来源**：《产品与总体方案.md》产品概念第四节 + 技术方案第二、三部分；《技术实现设计.md》组件全景图、架构映射表。
> **切分原则**：只重组、不改写，以下内容为原文章节原样搬运。
> **AI 使用提示**：本文件回答"系统怎么分层、每层用什么、组件如何映射到底层模块"——技术选型与依赖关系以此为准，写代码前先对齐这里的约束。

四、三位一体架构设计：如何系统性融合
整体采用 “三层四环”架构：
三层：
1. 接入表示层（Harness）：实现多模态操作能力
2. 认知执行层（Graph + Loop Core）：大脑与剧本
3. 用户DIY层：配置与输出面
四环（Loop的核心，贯穿于Graph之上）：
`Observe（感知）→ Orient（定位）→ Decide（决策）→ Act（执行）`，并增加 `Reflect（反思）` 外环。
1. Harness层：原子化能力编织网
设计为插件化适配器工厂，每个平台对应一个适配器，向上提供统一接口：
- 界面操作适配器：基于视觉模型+可访问性树，输出“可点击/可输入元素”的统一空间。类似UI.Vision、Playwright的能力，但由模型驱动。
- API适配器：将OpenAPI/Swagger自动转换为工具描述，供Loop调用。
- 数据适配器：对接数据库/数据仓库，输出结构化上下文。
- 外设适配器：IoT、机器人等物理操作。
关键设计：能力注册发现机制。每个适配器启动时，会用自然语言+图模式声明自己的能力（如“我能点击，需要一个元素描述”），这成为Graph节点与Harness的粘合剂。
2. Graph层：从流程到因果图谱
这是框架的刚性骨架，保证运营不跑偏：

有向流程子图：定义正常业务路径（如商品上架的12个步骤）。

- 状态机子图：定义业务对象生命周期（如订单的待支付→已支付→已发货）。
- 决策事件图：当触发条件满足时，跳转到对应处理子图（如出现差评，进入挽回流程）。
- 知识图谱：存储产品关系、规则、FAQ，用于推理。
Graph用可扩展的DSL定义，并支持可视化编辑。例如一个“电商促销”图的片段：
```
start -> select_products(harn: product_api.search, params: {category:"美妆"})
select_products -> calc_discount_price(memory: profit_margin_rule)
calc_discount_price -> send_to_channel(harn: channel_publisher, params: {platform:"all"})
send_to_channel -> monitor_sales(loop: true, exit_condition: "time > 2 hours")
monitor_sales -- on: "stock <10" --> urgent_replenish
```
3. Loop层：智能体即持久运行的过程
Loop并不独立存在，它寄生在Graph和Harness之上：
- 动态导航：基于当前Graph节点、实时观察和历史记忆，用LLM决定走哪条边、调用Harness的哪个工具、传入什么参数。
- 异常兜底：当Graph未覆盖的情况发生，Loop将目标转为“调用大模型自由生成动作序列，同时请求专家标注，以自动生长Graph分支”。
- 反思进化：周期性将“动作序列+结果”喂给反思模型，总结策略优劣，调整Graph中的决策阈值或提示词模板。
核心技术：采用强化学习（RLHF）或DPO思想，当运营目标达成时（如GMV提升），整个Loop的决策轨迹被作为正样本微调小模型，让运营策略持续“长”在系统里。

## 第二部分：核心架构设计
### 2.1 三位一体架构模型
┌──────────────────────────────────────────────────────┐
│                      LOOP 循环层                      │
│  感知(Observe) → 定位(Orient) → 决策(Decide) → 行动(Act)  │
│                    ↑                    ↓            │
│                 反思(Reflect)  ←  执行结果           │
├──────────────────────────────────────────────────────┤
│                      GRAPH 图谱层                    │
│   有向流程子图 | 状态机子图 | 决策事件图 | 知识图谱    │
├──────────────────────────────────────────────────────┤
│                     HARNESS 适配层                   │
│   Web操作 | API调用 | 移动端 | 桌面端 | 数据库 | IoT   │
└──────────────────────────────────────────────────────┘
### 2.2 系统分层架构
┌───────────────────────────────────────────┐
│          用户DIY层（编辑器前端）             │
│  可视化画布 | 对话式配置 | 模板库 | 表单    │
├───────────────────────────────────────────┤
│        认知执行层（Loop+Graph引擎）         │
│  LangGraph StateGraph | 思考缓冲区 | 记忆系统 │
├───────────────────────────────────────────┤
│         能力接入层（Harness Gateway）        │
│  适配器注册 | 统一gRPC接口 | 服务发现        │
├───────────────────────────────────────────┤
│        评估与优化层（新增）                 │
│  评估Harness | 策略训练器 | 模型路由器      │
├───────────────────────────────────────────┤
│    基础设施层（消息队列/存储/模型服务）      │
│  K8s | NATS | PostgreSQL | vLLM | 监控    │
└───────────────────────────────────────────┘

## 第三部分：技术选型总览

| 层级 | 选型 |
| --- | --- |
| 后端主语言 | Python 3.11+（AI引擎）/ Go（Harness网关、高性能服务；Demo 阶段网关以 Python/FastAPI 实现同构接口，ADR-2026-09-13 T5，见 10 文档 §3） |
| 前端语言 | TypeScript + React 18+ |
| AI框架 | LangChain + LangGraph（核心）、LiteLLM（模型网关） |
| Web自动化 | Playwright + OmniParser（视觉定位） |
| 移动端自动化 | Appium + ADB |
| 数据库 | PostgreSQL（主库+Checkpointer）、pgvector（向量）、Redis（缓存）；Demo 数据适配器出站连接另走 `ATLAS_DATABASE_URL`（postgresql+psycopg/sqlite 白名单，与平台 `DATABASE_URL` 物理隔离，缺省内置 SQLite demo，ADR-2026-09-15 T10，见 10 文档 §4）。**2026-09-21 ADR T24（[docs/30](30-持久化交付契约设计.md)）：compose 缺省交付改为 `pgvector/pgvector:pg16`＋命名卷、atlas 走 PG 档；迁移运行器＝`schema_migrations` 登记（009）＋`scripts/ops/apply_migrations.py` 手写有序幂等 SQL（不引 Alembic）；会话 PG 接线；pg_dump 备份/恢复演练** |
| 消息队列 | NATS（Demo 阶段用进程内事件总线替代，ADR-2026-09-13 T4，见 10 文档 §3） |
| 容器编排 | Kubernetes |
| 模型部署 | 用户自定义（Demo 阶段：LiteLLM 对接主流商业 API，供应商经 `.env` 切换，ADR-2026-09-13 T1，见 10 文档 §3） |
| 可观测性 | OpenTelemetry + Prometheus + Grafana + Loki + Tempo（正式栈随部署，14 D11/D28）；Demo 基础监控告警 v1 为**进程内运行观测 + 站内告警中心**（`src/atlas/monitoring/`，ring 200 条运行记录、成功率/p50/p95/节点失败 Top、四内置规则阈值可调、不引 OTel/Prom 依赖，ADR-2026-09-15 T14，见 10 文档 §4、04 §5.13）；运行/告警/规则按租户分区（ADR-2026-09-16 T15，04 §5.14） |
| 认证与多租户 | 正式形态随真实账号体系（11 S1）；Demo v1 为**种子账号 + 进程内 sess-token 会话 + 三角色端点级 RBAC（viewer/operator/admin）+ 用户数据按租户全分区**（`src/atlas/iam/`，零新依赖，图/录制/反馈/监控/审批/调试/消息每租户独立 store，跨租户对象 404、角色不足 403、未登录 401，无平台超管；模板/适配器/demo 基础设施全局共享，ADR-2026-09-16 T15，见 10 文档 §4、04 §5.14、06 §6.12；密码哈希/SSO/JWT/节点级权限/审计/配额缓做 14 D7 边界/D20/D22、Phase 3） |
| 前端编辑器 | React Flow（图编辑）、Zustand（状态）、Ant Design/Shadcn UI；Demo 落码 `@xyflow/react` 12 + AntD 6 + Vite 8，前端单测 vitest 5（ADR-2026-09-13 T6，见 10 文档 §4） |
| 前端 Schema 驱动表单 | **自研最小 WidgetRegistry + 递归 FormRenderer**（新包 `lib/forms`，内置 text/number/select/radio/textarea/switch/json/expression/variable-input 九件〔M4 批 1 +radio〕，均由既有 AntD 承载；另有 target-select 节点业务控件分层注册；选择序 x-widget→有 properties 的 object 分组→类型结构默认→JSON 文本降级，oneOf 无统一 properties/裸插值串走旧 TextArea；ADR-2026-09-16 T17，零新依赖、不引 rjsf/Formily，M3 2026-09-16 立项、**2026-09-17 落码收口（2de5053→6d4a863）**，见 10 §4、04 §4.10、20 §2.4；**M4 2026-09-17 立项、批 1 已落码（49cb692→30d5009）**扩 ui:group/hiddenWhen/hideFields+字段文案层、HumanApproval/Trigger/Loop 迁移、dirty 脏标记骨架（Wait 判定保留手写；T17 两次复查不重开，见 08 M4 立项/批 1 落码条、20 §2.5），批 2 前端 L3 预判/分层调度/Problems 面板、批 3 quickFix v1 待落；M8 第三类 schema 来源时携实测案例重开） |
| 前端 L1 配置校验 | **手写最小 JSON Schema 子集解释器**（`frontend/src/lib/schemas/validateConfig.ts`，M1 已随 dev 双跑落地：九节点 oneOf 仅 1 处、零 if-then/dependencies，路径级零分歧；零新依赖、不引 AJV，ADR-2026-09-16 T16，见 10 文档 §4、04 §4.9、20 §2.3；M3 若白名单外结构扩张则携实测案例重开 ADR 评估 AJV；后端下发校验结果已排除——违背编辑流/提交流分离） |
| 出站适配器（Demo 渠道包） | `src/atlas/httpapi/`（http/request，同步 httpx，T9）、`src/atlas/database/`（database/query·execute，SQLAlchemy 自建 engine 工厂，T10）、`src/atlas/message/`（message/send 进程内 sink，无真实投递，T10；SMTP/IM/短信缓做 14 D24） |
| 编辑后台配套资产（Demo 形态） | 流程模板为随代码发布的**只读内置目录**（`src/atlas/template/`，5 个模板 Graph JSON，无 DB/CRUD、reset 不影响，ADR-2026-09-15 T11）；操作录制 v1 为**运行录制 + 回放比对**（`src/atlas/recording/`，录制时图快照冻结、客户端在 /run/stream 收集 node_end 入库、回放经 inputs.approvals 预置决策保证确定不挂起、易变值归一化后逐节点比对，进程内存储且 reset 保留，ADR-2026-09-15 T12；浏览器操作录制生成工具仍缓做 14 D22，批量回放/跨版本对比缓做 14 D26；均见 10 文档 §4）；单步调试与断点 v1 为**会话级调试**（`src/atlas/debug/`，断点存前端会话态随 /run/stream 传入、不进 Graph JSON，进程内 token+Event 会话统一执行器单点挂暂停，step/continue/stop 状态机 + 条件断点复用表达式白名单、变量快照只读，重启即失，ADR-2026-09-15 T13；持久化中断-恢复随 14 D19/D20，断点持久化/变量改写/子图断点/急停缓做 14 D27；见 10 文档 §4）；基础监控告警 v1 为**进程内运行观测 + 站内告警中心**（`src/atlas/monitoring/`，埋点在两个真实运行入口、零节点类型分支，ring 200、成功率/p50/p95/节点失败 Top，四内置规则（运行异常/节点失败/连续失败/窗口失败率）阈值可调、未关闭告警按图合并计数，acknowledge/resolve，reset 清空、重启即失，ADR-2026-09-15 T14；OTel/Prom/Grafana 随部署 D11，持久化/适配器级指标/规则 DSL/外部出口缓做 14 D28；见 10 文档 §4） |
| 发布与灰度（Demo 形态） | Graph 版本为**不可变发布快照**（`releaseVersion` 从 1 递增、仅发布版带、草稿不带，subgraph 引用递归钉 `graphId@N`，M6/ADR T19）；入站路由与灰度发布 v1 为**进程内 Router + 结构化 rollout 配置 + 指标门控自动回滚**（新包 `src/atlas/routing/`，零新依赖纯 stdlib，ADR-2026-09-18 T22，见 10 §4、04 §5.16、06 §6.17）：三段分桶按固定序 internal 租户 allowlist→低金额业务桶（`payload.amount<=N` 单一谓词）→canary（`sha256(graph_id:order_id)` 整型 %100<percent，同订单稳定、桶键缺失 fail-safe 落 stable）→full；入站流量复用 `/run[/stream]` 可选 `event` 字段（不新增 /ingress），一次 run 按解析版本钉不可变快照（pin-to-version，中断帧已冻结 graph_snapshot，回滚后在途实例续跑启动版本）；发布前批量回放门禁（`recording/gate`，坏版本 409 不产新版本，D26 部分取回）；门控指标为系统 run_error_rate + 业务结果三率（自动退款/转人工/退款金额差异，从 shop 终态 outputs 提取，与系统指标分列），观察窗 finished_at 时间过滤 + minSamples fail-safe，越阈**仅自动回滚**、放量唯一路径纯手动，回滚只切新流量不改外部已发生事实（金融硬条款）。真实 webhook/IM/embed 入站服务器、多实例路由表同步与热推送、配置中心/独立网关、自动放量、NATS/CD 随 D6/D10b/D24/D32，不解除缓做 |

## 二、编辑后台组件全景图

| 组件类别 | 核心作用 | 典型实例 |
| --- | --- | --- |
| 节点 | 执行单元，组成流程图 | 触发器、AI决策、工具调用、条件分支、循环、子图 |
| 工具 | 操作外部平台的能力 | 浏览器点击、API调用、数据库查询、发送邮件 |
| 逻辑 | 控制流程走向与状态 | 条件表达式、循环、并行、等待、异常处理、状态机 |
| 适配器 | 连接具体平台/系统 | Shopify适配器、钉钉适配器、企业微信适配器 |
| 数据 | 存储与传递业务信息 | 业务对象、数据表、上下文数据流 |
| 变量 | 存储临时/持久数据 | 全局变量、节点变量、环境变量、秘密变量 |
| 上下文 | 当前运行环境信息 | 用户上下文、会话上下文、系统上下文 |
| 知识 | 提供领域知识与文档 | FAQ知识库、SOP文档、产品手册 |
| 测试/发布 | 验证运营体正确性 | 沙盒测试、回放测试、灰度发布 |
| 运行时 | 实际执行与监控 | Loop引擎、Harness网关、日志中心 |

## #  与整体架构的映射关系
编辑后台的每个组件，都对应到产品底层架构的一个模块：

| 编辑后台组件 | 对应底层模块 |
| --- | --- |
| 节点 → 流程图 | LangGraph StateGraph 的节点和边 |
| 工具 → 适配器 | Harness 层的统一操作接口 |
| 逻辑 → 条件/循环 | Graph 的条件边、循环子图 |
| 变量/上下文 → 状态 | LangGraph State / Checkpointer |
| 知识 → 向量库/图谱 | RAG 检索 + 知识图谱 |
| 测试/发布 → 评估Harness | 借鉴 lm-evaluation-harness 的评估流水线 |
| 运行时监控 → Loop引擎 | OODA 循环 + 反思模块 |
