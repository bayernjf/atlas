# 项目级上线复审（2026-09-25，第三次评审）

> 评审链：**docs/29（初评）→ docs/34（2026-09-22 复审 + 同日两次追加注记）→ 本文（2026-09-25 第三次）**。本文不替代前两份，前两口的历史正文按治理规则保留不追溯改写；凡与本文冲突，以本文为最新事实，并在被订正文档就地加 dated 注。
>
> **判定：达到「可演示 / 可陪同试用的技术验证 MVP」；未达到「产品核心完全可用的生产 MVP」——PROD 不 GO。**
>
> 一句话理由：工程完整度是真的（三套门全绿、迁移干净、零 TODO），但**产品核心那条链在交付形态里是静默退化的**，且**存在四条可被外部直接利用的鉴权/隔离缺陷**，同时**PG 档——也就是上线形态——不在任何自动化门禁里**。

> **复审更新（2026-09-25 当日晚，打包 J/K 收口后；本注只追加，上文判定原文与 S1–S8 表照原文保留不改写）**
>
> 用户随后拍板推进，本文 §6 的四批**当日全部落码收口**：P0-1＋P0-2＋P1-1＝**打包 J**（契约 docs/64，`7600157`→`7d62f7e`），P1-2＝**打包 K**（契约 docs/65，`c965d22`→`f0ae1f8`）。
>
> **八条阻断逐条状态（每条我本轮回代码复核过，不是转抄收口自述）**：
>
> - **S1 ✅**（`4ab6edc`＋`0400be8`）——新增 `security/bootstrap.py` 真读 `ATLAS_ENV`，prod 缺 `ATLAS_APPROVAL_HMAC_SECRET`/`ATLAS_MASTER_KEY` **拒绝启动**；邮件决策 token 载荷加 `rcpt`（`email_token.py:102-114`，收件人小写归一），验签不匹配一律 404。
> - **S2 ✅**（`f0e50ed`）——`docker-entrypoint.sh:60-65` 在 `ATLAS_ENV=prod` 时跳过播种，`iam` 侧对种子口令登录拒绝。
> - **S3 ✅**（`ef3451a`）——`storage/pg.py:487-495` 加 `ORDER BY tenant_id, username`（字典序最小租户，不再依赖物理行序），并把"用户名全局唯一"写进契约（docs/03）。
> - **S4 ✅**（`de023ab`）——`main.py:884` 起 `ThreadPoolExecutor(max_workers=8)` 承接后台投递、`:1013` 加 **1MB body 上限（超限 413）**、绑定查询下沉不占事件循环。
> - **S5 ✅**（`4b0794a`）——`main.py:3781-3782` demo 模拟面 prod **fail-closed**（`ATLAS_ENABLE_DEMO_MOCK=1` 显式开）。
> - **S6 ✅，但两处改口径收口**（`c6a1afe`＋`2fdd9be`→`dd99b37`）——三处 `litellm.completion` 补 `timeout`（`ATLAS_LLM_TIMEOUT_SECONDS` 缺省 60s）与 `max_tokens=512`；**`limit` 的 `le=` 落地后被还原**：实测 10 处端点**早已自带门禁**（7 处 clamp ＋ 3 处手动 reject），统一 `le=` 反而把 clamp 语义破坏成 422，故 J-3b 以「**确认有界**」收口、不改代码（详见 docs/64 §10 勘误）。**这是有据的收窄，不是放松**。
> - **S7 ✅**（`4ab6edc`）——与 S1 同根因，环境档位与密钥门一并解决。
> - **S8 ✅**（`5aa51ca`／`2d3ec70`／`0082b1e`／`d171639`）——`run_retention.py`＋`PgBackend.prune_expired`（8 表；**`runs` 仅终止态、`interruptions` 仅已认领才淘汰**，与 docs/62 的 at-most-once 语义对齐；`iam_sessions` 过期全量清扫；TEXT 时间列一律 `::timestamptz` 比时刻）；备份轮转 `ATLAS_BACKUP_KEEP`（默认 7）＋异地 `ATLAS_BACKUP_REMOTE_DIR`＋`--dry-run`；`/metrics` prod Bearer 闸门（未配 token 即关闭）；`observability/logging.py` 统一 formatter ＋ `X-Request-Id` 中间件（`main.py:307/315` 已接线）。
>
> **§2 三条核心完整性**：① ✅ `f1c54aa` 启动打印决策器运行模式（规则兜底不再静默）；② ✅ `405161e` 新增 `tests/test_refund_e2e_full_chain.py`——**同一条测试内同时有 `human_approval` 与真实 `shop/process_refund`**，docs/08 §7.3 criterion 4 已加口径注记；③ **仍开放**——M7 验收链 `coordination/sandbox.py` 未动（docs/08 B 组候选行仍开）。
>
> **§4 结构性缺口 ✅ 已闭**（`9b4ec9a`）：CI 现在 `backend` 与新增 `backend-integration` 两个 job 都挂 postgres service（`.github/workflows/ci.yml:34/63`），integration 从此进门禁；既存 `PgAuditStore.record()` 缺 `seq` 红同时修掉。**本轮不再需要手动跑 `-m integration` 才发现它**。
>
> **本轮复跑的门（第一方实测，非转抄）**：后端 `.venv/bin/pytest` **1849 passed / 114 skipped / 0 failed**（703.37s；与打包 K 收口自述逐字一致）；前端 `pnpm lint` **0 error / 6 既有 warning**、`pnpm test` **728 passed / 2 skipped / 53 文件通过·1 跳过**、`pnpm build` ✓（1723.04 kB / gzip 534.50 kB）。
>
> **更新后判定**：**本文列出的仓库内阻断已全部清零**，"是否可上线"从**工程缺陷问题**转为**部署与商务问题**。生产 MVP 判定由「未达到（有已知工程缺陷）」更新为「**工程侧就绪（PROD-ready codebase）**」——**但 GO 仍有四条仓库外/另批前提，未满足前不得对外称"可上线"**：
>
> 1. **外部资源**：真实店铺/SMTP/IM 凭据、域名与 TLS、商业 embedding（docs/08 §八 D 组）；今天的 compose 仍是 dev 语义。
> 2. **一次真实部署演练**：`ATLAS_ENV=prod` 形态目前**零部署实例**，J-1a/J-1c/J-3e/K-C 四条 prod 收紧从未在真机跑过（收口文档自己也这么写）。
> 3. **种子客户实测**：docs/18 那句「假定 Go（沙盘假设，非实测）」仍在，是业务侧唯一挂着的 P0。
> 4. **多副本语义**：D19/D20/D27/D31/D32 **一律不解除**（登录节流、`/metrics` 聚合、灰度态、跨进程急停、审批决策跨实例仍是进程内），**单副本硬约束与三道闸照旧**；连带 **D36** 卡住帧 reconcile、**D37** 前端不认 `superseded` 帧（本轮复 grep：`frontend/src` 命中 0）。


## 0A. 判定撤回更新（2026-09-25 第四次复审；**撤回本文件上方「工程侧就绪」的推进结论**）

> **触发**：用户第二次要求「做一次项目级别的功能性、完整度、是否可上线的评审，必须能达到产品核心完全可用的 MVP」。本轮不复用上轮证据，改做**真机/真路径**核验（上轮只核到"守卫存在"，没核"守完之后还有没有可用路径"）。
>
> **撤回声明**：上方「复审更新」注记里两条**过宽**，现予撤回并保留原文不删：
> ① 「S5 ✅ demo 模拟面 prod fail-closed」——实况是 `_demo_mock_enabled()` 只守住 3 条 shopify-admin mock 路由（`api/main.py:3792/3806/3830`），`/api/demo/mock/orders`（`:3767`）、`/api/demo/mock/orders/{id}/receipt`（`:3775`）、`/api/demo/shop/login|orders`、`/demo/shop` 页面**仍无鉴权、仍无上限**；
> ② 「S1–S8 清零 ⇒ 工程侧就绪（PROD-ready codebase）」——**不成立**，理由见 N1/N2。修订后的判定在本节末尾。
>
> **教训（写死在文档里，防下次再犯）**：验证一个安全收紧是否完成，判据不能是"这段代码存在且被单测覆盖"，必须是**"收紧之后，合法的部署路径仍然走得通"**。J-1c 正是通过这个漏洞收口的——它有自己的单测（`tests/test_api_auth.py` +30 行），单测只测"prod 拒绝种子口令"，没人测"那 prod 首个管理员从哪来"。

### N1 · prod 全新安装＝登录死锁（阻断级；逐环读码闭合）

| 环节 | 证据 |
|---|---|
| 应用自己会播种，entrypoint 的"prod 跳过播种"被架空 | `iam/deps.py:43` 模块级 `user_store.seed()`（注释自陈「否则 PG 首启无管理员、无法登录」）；`scripts/ops/docker-entrypoint.sh:60-65` 的 prod 分支只是少跑一次 `seed_accounts` |
| 种进去的就是那四个已知弱口令 | `storage/pg.py:469` `hash_password(user.password)`，明文表在 `iam/principals.py:51-60` |
| prod 拒绝用种子口令登录 | `iam/deps.py:74-81`（`read_env_profile()=="prod"` 且口令等于种子口令 → 403 `AUTH_SEED_CREDENTIAL`，文案「请先通过管理员改密」） |
| 但改密的两条路都要先有身份 | `POST /api/auth/change-password` 需 `Depends(get_principal)`（`api/main.py:1404-1410`）；`POST /api/users/{username}/reset-password` 与 `POST /api/users` 需 `require("administer")`（`:1479`/`:1432`） |
| 没有任何带外建号手段 | `scripts/` 下只有 `seed_accounts.py`（同样只播种子账号）；全仓 grep `BOOTSTRAP\|FIRST_ADMIN\|CREATE_ADMIN` **0 命中**；`.env.example` 也无相关项 |

⇒ 结论：**新库 + `ATLAS_ENV=prod` 起来后，世界上不存在能登录的账号**。J-1c 把 S2 从"弱口令可登录"修成了"谁都进不去"。修法必须先定策略（三选一：首登允许种子口令＋强制改密令牌／带外 CLI 建首个 admin／启动时从 env 读一次性 admin 引导口令），**属产品决策，不宜由代码原子顺手决定**。

### N2 · 真实店铺退款在图里跑不出来（阻断"核心完全可用"；读码＋测试面同证）

`graph/loader.py:1699` 的通用 JSON 通道只放行 `{http, database, message, memory}`（`:241`）与 `openapi:` 前缀；渠道适配器 id 是 `channel:shopify:<binding>`（`channels/adapter.py:63`），于是落进"其余适配器按能力硬编码装配"分支（`loader.py:1729-1754`）——`create_refund` 不在特判名单里，只拿到 `{"note": <文本>}`，随后 `channels/adapter.py:110-114` 取 `params["order_id"]`/`params["amount"]` KeyError → `CHANNEL_INVALID_PARAMETER` → 节点 FAILED。全仓 `channel:shopify` 只有 `tests/test_api_channels.py:148/153` 断言**能不能被发现**，**无一条测试把图跑过它**。
公平口径：客户可用 `http`/`openapi` 节点自行拼 Shopify REST——那是"能绕"，不等于打包能力可用。
连带修正我上轮的引用：`tests/test_refund_e2e_full_chain.py` 确实把审批与退款串在一条测试里，但退款打的是 `DemoShopService`（`:80`）＝**演示店**，不是真实店铺。

### N3 · PRD 要求的浏览器自动化适配器未接入运行期（读码）

实现与单测都在（`web/adapter.py:42`、`tests/test_web_browser_integration.py`），但**未注册进运行期注册表**（`api/main.py` 的 `_demo_registry` 只注册 shop/http/database/message 等），且 `tests/test_graph_loader.py:79-80` 反向把"未注册"锁死（断言 `web-playwright` 走工具节点必 FAILED）。⇒ PRD §4.3 那一项在图上不可达，且**测试在阻止它被接通**。

### N4 · 定时触发只有校验、没有触发器（读码）

`graph/dsl.py:1433` 校验 `triggerType in ("schedule","cron")` 需 `cron`，但 `src/atlas/` 内无任何调度循环（grep `scheduler\|cron` 只命中校验与注释）。⇒ "运营体按时自己跑起来"目前依赖外部 HTTP 打触发端点。

### N5 · 出厂示例配置值非法（小，实跑）

`.env.example:11` 写 `ATLAS_ENV=development`，而 `security/bootstrap.py` 只收 `dev/test/prod`。本轮实跑：`read_env_profile()` 对 `'development'` **抛 ValueError**（`dev`/`prod` 正常）。compose 不注入 `ATLAS_ENV`，故容器路径不受影响；受影响的是"照 `.env.example` 配好再裸跑"的人（README:52 与 `docker-compose.yml:8` 都指向 `.env.example`）。

### 本轮复跑的门（第一方）

后端 **1849 passed / 114 skipped / 0 failed**；前端 `pnpm lint` 0 error／6 既有 warning、`pnpm test` **728 passed / 2 skipped / 53 文件**、`pnpm build` ✓ 1723.04 kB（gzip 534.50 kB）。S1/S3/S4/S7/S8 的修法本轮逐条回代码复核为真（fail-closed 启动门、登录 `ORDER BY tenant_id`、webhook 线程池＋1MB 上限、`prune_expired`＋备份轮转＋`/metrics` Bearer＋request-id、CI 两 job 挂 postgres）。**门全绿不等于流程能跑通**——N1/N2 都在绿门后面。

### 修订后判定

**「产品核心完全可用的 MVP」＝未达到。** 能力面（编辑器／编译校验／SSE 运行／人工审批／监控告警／多租户鉴权／i18n）确实可用；**核心闭环面（接真实店铺＋真实 LLM＋真实定时，并在 prod 里被运维登进去）不可用**：N1 卡在门口，N2 卡在退款落点，N3/N4 是 PRD 承诺与运行期的落差。
**交付口径**：demo / 陪同试用可交付；**自主上线不可交付**。本文上方的「工程侧就绪」自本注起失效。


## 0. 方法与证据分级（本文每条都带级，防"读起来像实测"）

| 级 | 含义 | 本文标记 |
|---|---|---|
| **实跑** | 我在本仓库执行过命令，输出可复现 | 〔跑〕 |
| **读码** | 我逐行读过该 file:line 并确认逻辑 | 〔码〕 |
| **勘察** | 子代理扫描得出，我抽样后**未逐条复**，只作候选线索 | 〔勘〕 |

本次并行派了三份勘察（前端可达性 / 里程碑完成度 / 生产加固面），其中标 〔跑〕〔码〕 的结论是我复核过的；〔勘〕 的结论**不得直接当上线判定依据**，只登记为待复项。

## 1. 工程完整度：达标

- 门〔跑〕：后端 `.venv/bin/pytest` **1811 passed / 108 skipped / 0 failed**；前端 `pnpm test` **722 passed / 2 skipped / 53 文件**、`pnpm lint` **0 error / 6 既有 warning**（命令与工作目录照 `.github/workflows/ci.yml` 原文抄）。
- 迁移〔跑+码〕：001–029 全量在独立库经 `scripts/ops/apply_migrations.py` 跑通；`storage/migrations.py:70-84` 每文件独立事务、成功才登记、全 `IF NOT EXISTS`、**零 DROP/TRUNCATE**。缺：无内容 checksum（改旧迁移不被发现）、无 advisory lock（并发副本会同时迁移）。〔码〕
- 半成品指标〔勘，我复跑同 grep 一致〕：`src` 与 `frontend/src` 的 TODO/FIXME **各 0**；`NotImplementedError` 3 处全在 `harness/base.py:212/216/241` 的 `@abstractmethod`，非阻断。
- 测试总量〔跑〕：collect 1918 条，其中 integration 标记 **44 条（2.3%）**。

## 2. 产品核心完整性：三条打到核心，不是边角

1. **交付形态里没有 AI**〔码〕。`src/atlas/llm/decision.py:93-97`：`LITELLM_MODEL` 未配置时 `get_decision_client()` **静默**返回 `RuleBasedDecisionClient`，不 warning、不报错；而 `docker-compose.yml:52` 写的正是 `LITELLM_MODEL: ${LITELLM_MODEL:-}`。⇒ 按文档 `docker compose up` 起来的是一套**没有 LLM 决策的编排壳**。规则兜底本身是设计（fail-safe），**"静默"与"交付缺配"不是**。
2. **退款端到端没有一条测试跑通全链**〔码，两处测试我都读了〕。`tests/test_refund_e2e.py:34` 覆盖 trigger→决策→真实 `shop/process_refund`（断言 `orders["12345"].status == "refunded"`）但**不含 human_approval 节点**；`tests/test_api_demo.py:199` 覆盖活体 SSE 审批打断→决策→续跑，但通过分支走 `op-approve`、**不落 shop 工具**。docs/08 §7.3「七条验收全部达成」之所以仍立，靠的是 criterion 4 原文那句「自动执行退款**或**触发人工审批」里的"或"字——**这是口径巧合，不是能力验证**。
3. **M7 的验收链是手写的**〔勘，代码位置由勘察给出，我未逐行复〕。`src/atlas/coordination/sandbox.py:29` 从不调 `compile_graph`/`run_graph`；`src/atlas/logistics/adapter.py:1` 自述"假物流…不接真实系统"。⇒ 该里程碑属沙盘演示，不得对外称端到端。

可达性（operator-reachable surface）：

- **waits 三条、tasks 两条端点前端 0 调用**〔跑，我复 grep `frontend/src` 命中 0〕：`GET /api/waits`（`api/main.py:3080`）、`POST /api/waits/events`（`:3088`）、`POST /api/waits/{token}/signal`（`:3107`）、`GET /api/tasks`（`:2992`）、`GET /api/tasks/{id}`（`:3012`）。**后果是运营侧只能"配置等待节点"，看不见 pending、也发不出信号**——事件等待是画布上一等节点（`frontend/src/components/propertyPanel/WaitConfig.tsx:170-288`），却没有任何运行时操作入口。
- **SSE 新增 `superseded` 帧前端 0 处命中**〔跑+码，双源一致〕：落在 `frontend/src/lib/apiClient.ts:1004` 的透传分支，流尾因拿不到 result 抛 `'SSE 流缺少最终运行结果'`。已登记 **docs/14 D37**。
- 其余无 UI 端点（`GET /api/debug`、`GET /api/runs/{run_id}`、`POST /api/openapi/imports/{id}/restore`）〔勘〕——待复，不据此下判定。

## 3. 上线安全：八条阻断级（前四条我逐行读过）

| # | 证据 | 后果 | 级 |
|---|---|---|---|
| S1 | `collaboration/email_token.py:26,48-62` + 路由 `api/main.py:3302-3307`（docstring 明写「无需登录」）+ `docker-compose.yml:50-53` 两密钥都不注入 | 邮件一键决策是**不记名 capability token**，签名密钥缺省值是**写死在仓库里的常量** `_DEV_TOKEN_SECRET`，缺密钥只 `logger.warning`。看到源码者可给**任意租户任意审批**签一条"退款通过"链接，且带 `tenant` 的载荷本就该被服务端强校验 | 〔码〕 |
| S2 | `iam/principals.py:51-60` + `scripts/ops/docker-entrypoint.sh:61`（`ON CONFLICT DO NOTHING` 播种进 `iam_users`） | `admin-a/admin123`、`operator-a/operator123`、`admin-b/admin123` 等四个已知口令账号在 day one 就是可用管理员；无禁用开关、无强制改密 | 〔码〕 |
| S3 | `storage/pg.py:407-413` `WHERE username = :u`，**不带 tenant、无 ORDER BY**，`.first()`；而 `db/migrations/010_iam_users.sql` PK 是 `(tenant_id, username)` | 登录按用户名全局查 ⇒ 跨租户重名时命中哪一行由 PG 决定，**可登进别人的租户**。内存档 `iam/accounts.py` 同形（`_USERNAME_INDEX` 以 username 为键，直接覆盖） | 〔码〕 |
| S4 | `api/main.py:842-852`（`async def shopify_webhook_ingress` 内跑同步 SQLAlchemy `_locate_binding`）+ 先 `await http_request.body()` 全量缓冲再验签 + 每订阅一条裸 `threading.Thread`；全仓 body 上限 grep **0 命中**〔跑〕 | 一条入站事件可卡住**整个事件循环**；签名校验前就吃掉任意大 body；并发投递无线程池 ⇒ 生产形态下这是自我 DoS 面 | 〔码〕 |
| S5 | `api/main.py` 正则枚举到 116 条路由签名（全仓装饰器实为 134 条，我的正则漏掉多行签名那批，所以**这是下限不是全量**）里 **13 条无鉴权依赖**〔跑，我脚本枚举〕，其中 7 条 `/api/demo/*`：`POST/GET/DELETE /api/demo/mock/shopify-admin/webhooks.json`（`api/main.py:3711-3757`，写入模块级 `_MOCK_SHOPIFY_WEBHOOKS`，**无上限、无鉴权**，只随 demo reset 清）；对比同区 `/api/demo/mock/orders` 却要 `X-Demo-Token`（`:3700`） | 产品镜像里带着**匿名可写的模拟面**，且守卫不一致（有的要 demo token、有的不要） | 〔码〕 |
| S6 | 10 处 `limit: int = 50\|100` **无一带 `le=`**〔跑，grep 计数 0/10〕；`llm/decision.py:66`、`llm/condition_classifier.py:68`、`llm/nl_generate.py:157` 三处 `litellm.completion` **无 timeout、无 max_tokens、无租户预算** | `?limit=10**9` 直连 SQL；一个慢供应商可长期占住线程池；LLM 成本无闸（唯一节流是登录失败 `iam/throttle.py`） | 〔码〕 |
| S7 | `ATLAS_ENV`/`DEBUG` 只出现在 `.env.example:9`，`src/atlas` **从不读**〔跑〕；`security/secrets.py:201-207` 缺主密钥 ⇒ 明文 secret provider，仅 warning | **没有任何一道能 fail-closed 的环境闸门**——S1/S2 的根因：不安全的实例照样起、照样可用 | 〔码〕 |
| S8 | 无 retention〔跑+码〕：`DELETE FROM audit_events` 只出现在 `storage/pg.py:2147 clear()`（demo reset）；`runs`/`webhook_deliveries`/`interruptions`/`graph_versions`/`openapi_imports` 同形；过期 `iam_sessions` 只惰性删自己那行 | 库只增不减；`/metrics` **无鉴权**且吐按租户聚合的业务计数（`api/main.py:517-522` 注释把访问控制推给反代） | 〔码〕 |

真实强项（别在返工中被推翻）：单 worker fail-closed 三道闸（`docker-entrypoint.sh:10-27`、`Dockerfile` CMD、compose `replicas: 1`）、DB 侧原子一次性认领（`storage/recovery.py:31-45`，docs/62 L2）、`/api/ready` 真打 `SELECT 1`（`observability/health.py:19-32`）、HTTP 出向有超时且只在幂等方法重试（`httpapi/service.py:148-152`、`httpapi/resilience.py:82`）、SSRF denylist（`security/egress.py:40-48`，但注意 `channels/registry.py:237-239` 设 `ATLAS_SHOPIFY_ADMIN_BASE_URL` 会换成 passthrough 关掉它，靠注释约束而非代码约束〔码〕）。

## 4. 结构性缺口：上线形态不在任何门禁里

**PG 档才是生产形态，CI 却一次都没跑过它**〔跑+码〕：`.github/workflows/ci.yml` backend job 只有 `pip install` + `pytest`，**没有 postgres service**、不设 `ATLAS_RUN_INTEGRATION`（`:43` 注释明写默认 skip）；我实测 integration 标记 **44 / 1918（2.3%）**。本次手动 `ATLAS_RUN_INTEGRATION=1` 跑，即抓出一条既存红：`storage/pg.py:2063` `PgAuditStore.record()` 返回投影缺 `seq`，而 `tests/test_audit_log.py:31` 的 `_EVENT_KEYS` 要求它。

⇒ 后果说准：**M5b 以来所有「已 PG 化 / 跨重启可见」的承诺，目前没有任何自动化在守**。这几批的卖点恰恰是持久化，这是评审里最该被记住的一条。

## 5. 对前两份评审的订正与增量

1. **docs/34 的「阻断项」清单不含本文 S1–S8**——两份上线评审审的是"能力有没有落"，从未审"不安全会怎样"。本次是仓库里第一次成体系的安全面复核。
2. **docs/08 §7.3 criterion 4 的口径要改**（§2.2）：现在的"或"字让一条从未跑通的链看起来已验收。建议改为"同一条测试里同时存在 human_approval 与真实退款工具节点"。
3. **单实例结论沿用不变**〔跑〕：多副本仍硬禁，docs/62 L2 只拆了"一次审批下游跑两遍"最贵那颗雷，`RoutingStore` 仍无 PG 实现（grep `class PgRouting` **0 命中**〔跑〕）、急停句柄与决策仍不跨进程，D19/D20/D27/D31/D32 一律不解除。
4. **业务侧 P0 仍未动**〔码〕：`docs/18:110` 那句「**假定 Go（沙盘假设，非实测）**」还在，真实试用数据一条没有——这条不是代码能修的。

## 6. PROD GO 的最小集（按依赖排序，不按工作量）

**P0-1 安全闸门批（建议编号 打包 J）—— ✅ 已收口 2026-09-25（`4ab6edc`/`0400be8`/`f0e50ed`/`ef3451a`，详见 docs/64）**——四条同源，根因都是"配置不安全照样起得来"：

1. `ATLAS_ENV` 真正被读，prod 下缺 `ATLAS_APPROVAL_HMAC_SECRET`/`ATLAS_MASTER_KEY` **拒绝启动**（S1/S7）；
2. 邮件 token 与收件人绑定（一次性 nonce 或"登录后确认"），不再是不记名长期凭证（S1）；
3. 种子账号：prod 下不播种 + 已知口令强制改密（S2）；
4. 登录查询补 `tenant_id` 与确定性 `ORDER BY`，并把"用户名全局唯一"写进契约（S3）。

**P0-2 核心完整性批—— ✅ 已收口 2026-09-25（`f1c54aa` 决策器启动打印／`405161e` 全链 e2e／`b220ba3` waits·tasks 操作台）**：交付形态默认带真实 `LITELLM_MODEL` 且启动打印当前决策器与降级状态（不再静默）；补一条**跨 human_approval 的真实退款全链测试**；waits/tasks 运行时操作台接 UI（D40）。

**P1-1 门禁批—— ✅ 已收口 2026-09-25（`9b4ec9a` CI+seq／`2fdd9be` limit（落码勘误：本仓 limit 端点均有 clamp/reject 门禁，`le=` 不引入，见 docs/64 §10）／`c6a1afe` LLM timeout／`de023ab` webhook 池与 body 上限／`4b0794a` demo 模拟面开关）**：CI 加 postgres service 并跑那 44 条 integration（顺手修 S4 之外那条 `seq` 红）；10 处 `limit` 补 `le=`；三处 `litellm.completion` 加 timeout/`max_tokens`；入站 webhook 改同步依赖下沉线程池 + body 上限；`/api/demo/*` 模拟面在产品构建里加统一开关（S5）。

**P1-2 运维批—— ✅ 已收口 2026-09-25（打包 K，契约 docs/65；`5aa51ca` retention＋会话清扫、`2d3ec70` 备份轮转/异地、`0082b1e` `/metrics` 闸门、`d171639` 日志 formatter＋request-id）**：retention 任务与过期会话清扫（8 表按保留期淘汰，`runs` 挂起态与 `interruptions` 未认领帧永不淘汰，过期会话全量清扫）、备份轮转/异地、`/metrics` prod fail-closed Bearer 闸门、日志 formatter + request-id（D38/D39）。**D36（卡住帧 reconcile）已因 docs/62 L2 落码而具备开工条件（仍未立项）。**

## 7. 本文不当成缺陷的项（避免下一个人重复问）

- 3 处 `@abstractmethod` 的 `NotImplementedError`；Phase 3/4 全部能力；D1–D6、D8、D21、D25–D30、D33–D35；内存档与 PG 档在小规模下的性能差；这些是**范围选择**，不是完成度缺陷。
- 〔勘〕级的那三条无 UI 端点：登记为待复，不进阻断清单。

## 8. 一句话给接手的人

工程纪律是这个仓库最强的资产（门、原子、反向门、契约回填都在）；**但它现在守的是"功能有没有落"，没守"上线了会不会出事"**。补上 P0-1 与 P1-1 之前，任何"可以上线"的表述都不该被写进对外材料。

---

### 附：本文引用的复现命令

```bash
.venv/bin/pytest -q                                              # 1811 / 108
(cd frontend && pnpm lint && pnpm test)                          # 0 error / 722 passed
.venv/bin/python -m pytest -m integration --collect-only -q      # 44 / 1918
ATLAS_RUN_INTEGRATION=1 DATABASE_URL=... .venv/bin/python -m pytest -m integration -q   # 43 passed / 1 failed
.venv/bin/python scripts/dev/multi_instance_resume_recon.py      # rc=0（docs/62 L2 已翻绿）
```
