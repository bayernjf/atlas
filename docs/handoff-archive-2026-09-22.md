# Handoff 归档补遗 — 2026-09-22

本文件保存 2026-09-22 自 handoff「Recently shipped」按 5 条上限整块滚出的历史变更，原条目编号保留；主文件里「Recently shipped N」的交叉引用按本原号定位。本次滚出系整栈 PG 装配缺口修复收口（`b2ac85d`）后新增 shipped 1 触发，原主文件 #4（M12 i18n 零依赖骨架）、#5（docs/28 批 4）两条滚出；主文件保留最近 5 条＝整栈 PG 装配缺口修复 / docs/33 / P0 批 3 / P0 批 2 / M12 dashboard。

---

## 第一次滚出（2026-09-22 整栈 PG 装配缺口修复收口）：原主文件 #4（M12 i18n 骨架）、#5（docs/28 批 4）

4. **feat(i18n)：M12 零依赖文案抽取骨架落码收口（2026-09-20；立项 `3197e07`＋骨架 `9f1963f`＋接线 `a894fa7`＋docs 收口原子；U212–U218 转正式；D12 部分取回不解除）**——触发条件（首个英文使用者）未到，先落无依赖前置：新增 `frontend/src/locales/`，`index.ts` 用纯 TS＋已有 react 的 `useSyncExternalStore` 实现对齐 i18next 签名的 `t()`/`useTranslation(ns)`（点路径嵌套、`{{var}}` 插值缺变量保留占位、`ns:` 前缀、缺键依次回退 common namespace→zh-CN、最终返回 key 本身；默认 zh-CN，changeLanguage/localStorage `atlas.locale` 预留但不接切换 UI/不做 navigator 探测）；`zh-CN/common.json` 放 role/auth.login/auth.session/error.auth 与少量 button/status 外壳键，editor/dashboard/demo 与整个 en-US 树空 `{}` 占位。接线 Login/UserBadge/`auth.ROLE_LABELS`：ROLE_LABELS 改从 common.json role 段派生、中文逐字不变（auth.test 零改动），种子账号重构为结构化数据经 accountLine 插值＋role.* 标签，登录失败仍显后端中文 detail 的过渡状态（D7 结构化 code 留触发批）。13 纯函数 vitest（U212–U218，node 环境无 jsdom 不做组件渲染）；oxlint exhaustive-deps 抓到一处多余 useCallback `lang` 依赖已修（重渲染由 useSyncExternalStore 保证）。收口门：前端 **495 passed/2 skipped/40 文件**（基线 482 净增 13）、tsc+vite build 过（仅既有 chunk>500kB 提示）、oxlint **0 error/2 既有 warning 不新增**；后端零改动 **782 passed/24 skipped 不变**；真实浏览器 admin-a 登录页全量中文与登录后 UserBadge（租户·显示名、金色管理员 Tag、退出登录）零视觉差异、退出登录正常回登录页，2 截图 `docs/assets/m12-{login,userbadge}.png`。零新依赖（纯 TS＋已有 react）、不引 i18next、不新增 ADR、**不解除 D12/D13**——i18next+react-i18next、语言切换 UI、en-US 翻译、AntD locale 联动、后端 D7 错误码仍随首个英文使用者触发，editor/dashboard/demo 页面后续照 common 样板分批抽取。详见 Active work 23、docs/17 §2/§4、08 M12 立项与收口条、13 U212–U218、14 D12、CHANGELOG 顶部。本批 commit 已全部 push（dev `5200fe0`）。

5. **feat(memory/versioning/frontend)：docs/28 批 4 收口——记忆 REST 新建/编辑 + 发布前子图升级体检（2026-09-20；`ec0fd81`→`ba97088`→`19f31b4` ＋ docs 回填原子；U192–U211 转正式；至此 docs/28 四批 11 项全部落码收口）**——⑩ D35：`POST/PUT /api/memories`（operate、source 固定 manual、extra=forbid、白名单 kind/content/scope/confidence/metadata、两档 store 同构 `update`、content 变才重算 256 维 embedding、id/created_at 不变、删除仍 admin）＋前端记忆页新建/编辑 Modal（`lib/memory.ts` parseStringMapText/buildMemoryPayload、JSON 非法或内容空时保存 disabled）；⑪ D21：`versioning/upgrades.py` 纯函数 `subgraph_upgrade_plan`（只扫顶层、to=子图最新版/无则 1、from=父图最新快照钉版、first_pin、仅列变化）＋`GET /api/graphs/{id}/subgraph-upgrades`（read、只读不产版本、草稿不存在 404）＋ReleaseModal 门禁表上方体检区（与门禁并行拉取、失败 fail-safe、首次钉版蓝 Tag/升级橙 Tag/空态、不阻断发布、不检测草稿 dirty）。三道门最终：后端 **782 passed/24 skipped**（净增 18 常跑＋2 PG integration skip）、PG integration 全量 **24 passed**（首跑 2 failed 为共享开发库瞬时污染、复跑全绿非回归）、前端 **482 passed/2 skipped/39 文件**、tsc+vite build 过、oxlint 0 error/2 既有 warning；d26/m11/d28 smoke PG 档全过；权限矩阵 curl 实测（operator POST 201/DELETE 403、viewer POST·PUT 403、跨租户 404）；真实浏览器两截图 `docs/assets/batch4-{memory-edit,subgraph-upgrades}.png`。零新依赖/不新增 ADR/不解除 D21·D35。详见 Active work 22、docs/28 §5.3、CHANGELOG 顶部。**已随 dev 推送并经 PR #42 合并 main（merge `1956379`）。**
---

## 第二次滚出（2026-09-22 对外交付层小批收口）：原主文件 #5（M12 dashboard 接线）

5. **feat(i18n)：M12 续批——dashboard namespace 首批生产接线（2026-09-20；U219–U221 转正式；D12 部分取回续批不解除）**——M12 骨架的非 common namespace 此前只在单测里跑过、生产零调用，本批首次在真实页面接通第二个 namespace（登录后落地页 Dashboard）。`zh-CN/dashboard.json` 填 demo.title/description/cards.graph·loop·harness；`zh-CN/common.json` 增补跨页 brand.appName 与 nav.openEditor/monitoring/memory；`pages/Dashboard.tsx` 改 `useTranslation('dashboard')`，页面文案走默认 ns、品牌名与主导航经显式 `common:` 前缀跨取（验证跨 namespace 前缀生产路径），Graph/Loop/Harness 技术专名 Card title 按 docs/17 §2.5 保留英文；`en-US/dashboard.json` 仍空 `{}`。i18n 纯函数测试 13→16（U219 dashboard ns 解析、U220 `common:` 前缀跨取、U221 en-US 空骨架 zh-CN 回退）。收口门：前端 **498 passed/2 skipped/40 文件**（M12 495 净增 3）、tsc+vite build 过、oxlint **0 error/2 既有 warning 不新增**；后端零改动 **782 passed/24 skipped 不变**；真实浏览器 admin-a 登录后 Dashboard 品牌名/三导航按钮/Demo 标题描述/三卡中文零视觉差异、UserBadge 与退出登录不受影响，1 截图 `docs/assets/m12-dashboard.png`。零新依赖、不引 i18next、不新增 ADR、**不解除 D12/D13**——语言切换 UI/en-US 翻译/AntD locale/后端错误码仍随首个英文使用者触发，editor（1359 行）/Monitoring（811）/Memory（481）大页面留后续触发批或小批分批抽取。本批 feat＋docs 收口原子已随 dev 合入 main（PR #51，merge `f0999f3`）。
