# Atlas 工程骨架与目录结构（AI 落码规范）

> **来源**：由 docs 01-08 的模块划分推导，本文件是**工程结构规范**——AI 落码时按此目录组织代码，**不是真实代码目录**。
> **状态**：骨架结构已定，各文件内容待填（待填项见下）。
> **配套**：模块职责与实现细节见对应文档（下表已标注）。

## 目录结构

```
docs/项目1/atlas/                       # 工程根（W1 骨架已创建）
├── README.md                           # 待填：工程说明、启动方式、Demo 范围指引（见 01）
├── pyproject.toml                      # Python 依赖与打包配置（依赖见 10 文档 §2）
├── Dockerfile                          # Phase 1 交付镜像：前端多阶段构建 + FastAPI 同源托管（待定项 3）
├── docker-compose.yml                  # 一键启动（单服务，8000 端口）
├── .dockerignore
├── .gitignore                          # 忽略规则
├── db/migrations/                      # SQL 迁移（规范见 MIGRATION_CONVENTION.md）
│   └── 001_enable_pgvector.sql         # 启用 pgvector 扩展；现有 001–009（009 schema_migrations 登记表，全部幂等，运行器 apply_migrations.py）；docs/31 候选 010_iam_users.sql（账号表）、011_session_expires_at.sql（会话绝对 TTL＋回填）；后续 012/013/014 已落（monitoring spans/audit_events/oauth_connections），015_channel_bindings.sql 已落（渠道绑定，reset 不清），docs/39 候选 016_channel_webhook_subscriptions.sql（channel_bindings 增 webhook_subscriptions TEXT 默认 '[]'）
├── scripts/
│   ├── benchmark.py                    # 性能基准脚本（stdlib，输出 BENCHMARK.md Results 行；14 D9，2026-09-14）
│   ├── ops/                            # 运维脚本（ADR T24，docs/30）：apply_migrations.py（schema_migrations 有序幂等迁移运行器，--mark-existing）、docker-entrypoint.sh（容器启动先迁移）、backup.sh/restore.sh（pg_dump/pg_restore）
│   └── dev/                            # 开发辅助脚本（不随运行时发布）：generate_l3_fixtures.py（M4 批 2 L3 跨运行时对拍夹具生成）、m4_batch3_smoke.py（M4 批 3 Condition 表单+quickFix 浏览器冒烟，CDP 9222）、p0_persistence_smoke.py（U226 HTTP 造数查行数增量）
├── frontend/                           # 编辑后台前端（Vite + React 19 + TS；ADR T3，单测 vitest ADR T6）
│   ├── package.json / pnpm-lock.yaml
│   ├── vite.config.ts                  # dev 端口 5174，/api 代理到 8000
│   ├── bench/validation.perf.test.ts   # 200/500 节点校验分层基准（M4 批 3，RUN_BENCH=1 门控，常规套件跳过；pnpm bench）
│   └── src/
│       ├── lib/                        # 纯逻辑（可单测）：variables（{{路径}}）/ scope（拓扑作用域 ScopeIndex + L2 引用校验，04 §6.5，M2 起 L2 产结构化 Diagnostic）/ nodeCatalog（目录+手写跨字段校验，M2 起转 Diagnostic）/ graphSerializer（Graph JSON v1）/ schemas（M1：MetaSchema + 九节点 Data Schema + SchemaRegistry，04 §4.9，ADR T16 定为正式 L1 形态）/ validation（M2 2026-09-16 落码，234a95f/30156a3：diagnostics 类型+rank、l1（validateConfig 解释器自 schemas 扶正迁入，RFC6901 pointer+FIELD_* code）、validateGraph 聚合、markers token 投影；**M4 批 1 增 `dirty.ts`（L1/L2/L3 失效范围 + revision 纯标记，30d5009）；批 2 增 l3（不可达/环前端预判同构 dsl.py，11 夹具跨运行时对拍）、engine（structureSignature 记忆化）、useValidationEngine（L1 同步/L2 防抖/L3 idle 分层调度）；批 3 增 `reverseDeps.ts`（target 8 处/4 类 + 模板引用双类反向索引、removeDanglingRef，与 schema x-ref 对拍），scope 两处 REF_NODE_NOT_FOUND 挂 DELETE_DANGLING_REF_FIX**；权威见 08 M2/M4 立项/落码条、03 `diagnostic`/`graph_diagnostics`、04 §6.5）/ forms（M3 2026-09-16 立项、**2026-09-17 落码收口 2de5053→6d4a863**：registry/defaultRegistry+resolveWidget+widgets 控件、formTree 结构树与不可变更新、FormRenderer 递归渲染、toolSchemas 第二来源、params 存储形态、nlWarnings NL 警告、types 契约；AntD 承载零新依赖，ADR T17；**M4 批 1（49cb692→30d5009）：uiSchema（groups/hiddenWhen/hideFields + labels/placeholders/optionLabels 文案层）、nodeWidgets（target-select）+ nodeRegistry（节点业务控件层）、nodeUiSchemas、NodeConfigForm 共享包装，内置控件 8→9（+radio），迁 HumanApproval/Trigger/Loop、Wait 保留手写；批 2（06b62fc）：嵌套通配文案键+rows/keyPlaceholders、ArrayView min/max 门控、第二业务件 saved-graph-select，迁 Parallel/Subgraph；批 3（79317e3/471b930）：condition 两处 target 补 x-widget、空串 label 隐藏数组标题，迁 Condition——九节点 Config 八迁一留**；权威见 08 M3/M4 立项/落码条、03 `form_renderer`/`ui_schema`、04 §4.10）
│       ├── store/editorStore.ts        # Zustand：节点/边/变量/选中态（+ __tests__）
│       ├── components/{canvas（FlowCanvas + AtlasNode 自定义节点）,nodePanel,propertyPanel,variablePanel,debugConsole}/
│       ├── pages/{Dashboard,Editor,Monitoring,Approvals,EmailApproval,AuditLog}.tsx  # 审批闭环批（2026-09-22，e2771ba，docs/36）：Approvals 本租户 pending 队列＋10s 可见时轮询＋viewer 只读；EmailApproval 免登录 /approvals/{token} 深链页（pending/resolved/invalid 三态＋卡片表单）；审计可见与审批结果补强批（2026-09-22，033619a，docs/37）：AuditLog admin 审计日志页（action 前缀/limit，Bearer blob 导出），Approvals 增「已处理」Tab（切 Tab 加载不轮询）
│       ├── theme/tokens.ts             # 设计 Token 单一事实源（17 文档 §3；✅ 2026-09-13 已落码，setup.ts 注入 --atlas-* 变量）
│       └── locales/                   # i18n 零依赖骨架（17 文档 §2.3；✅ 2026-09-20 M12 已落 index.ts〔t/useTranslation 对齐 i18next、useSyncExternalStore〕＋zh-CN·en-US 四 namespace，common 样板＋Login/UserBadge 接线；M12 续批接通 dashboard namespace（Dashboard 落地页 demo.*＋common 增补 brand/nav 经 `common:` 前缀跨取，Graph/Loop/Harness 专名不译，en-US/dashboard 仍空 `{}`）；i18next 仍触发式缓做，见 14 D12；✅ docs/33 批1（2026-09-21）续抽 editor/monitoring/memory 三 namespace 纯展示文案〔zh-CN 落词、en-US 各 json 仍空 {}〕，技术专名/动态枚举/后端中文 detail 不 key 化，跨页按钮走 common:button.*）
├── src/atlas/
│   ├── __init__.py
│   ├── engine/                         # Loop 主循环、决策节点
│   │   ├── __init__.py
│   │   ├── state.py                    # LoopState（06 §6.1 / 12 §1.1）
│   │   ├── nodes.py                    # OODA 五节点与路由（W1 占位）
│   │   └── loop.py                     # StateGraph 装配/run_loop
│   ├── harness/                        # Harness 契约层：适配器基类 + 注册发现
│   │   ├── __init__.py
│   │   ├── base.py                     # HarnessAdapter ABC + Action*/Capability/Permission（06 §6.4）
│   │   └── registry.py                 # 进程内注册/发现/心跳（04 §4.4，T4 进程内）
│   ├── graph/                          # Graph DSL 与编译执行
│   │   ├── __init__.py
│   │   ├── dsl.py                      # GraphDSL/NodeDSL（04 §5.2，version 1）+ 静态校验（含 condition/loop/parallel/wait/human_approval 图级校验、非法环检测）
│   │   └── loader.py                   # DSL→LangGraph 编译/run_graph + {{路径}} 插值；condition 条件边、loop 条件循环环、parallel 列表路由扇出/合成网关汇聚（outputs 按键合并 reducer）、wait 同步 sleep 定时等待、human_approval 审批信号阻塞双出口、subgraph 经 graph_resolver 引用已保存图并进程内重入（emit 压制/深度≤3/fail-safe）、http 通道 params JSON 通用透传 + recursion_limit 派生
│   ├── llm/                            # 模型接入层（T1 LiteLLM；W9-W10）
│   │   ├── __init__.py
│   │   ├── decision.py                 # 退款 DecisionClient：LiteLLM 实现 + 规则兜底（06 §9.2 黄金规则）
│   │   └── nl_generate.py              # 自然语言 → Graph 草稿（LLM 优先，退款模板兜底）
│   ├── shop/                           # 电商渠道包：HarnessAdapter 实现（待定项 2 新渠道建包约定；W9-W10）
│   │   ├── __init__.py
│   │   ├── service.py                  # DemoShopService：种子退款单 + 退款/转人工状态流转
│   │   └── adapter.py                  # ShopHarnessAdapter：login/list_pending_refunds/execute_refund/request_human_approval/process_refund
│   ├── nodes/                          # 节点/工具/逻辑/变量组件
│   │   └── __init__.py
│   ├── memory/                         # 记忆系统（M11 长期记忆，**已落码收口 2026-09-19**，docs/26/ADR T23）
│   │   ├── __init__.py
│   │   ├── settings.py                 # 环境变量单一读取点（DATABASE_URL，fail-closed）
│   │   ├── database.py                 # PostgreSQL 引擎/会话工厂 + ping + pgvector 版本探针（连接层，M11 不动）
│   │   ├── models.py                   # 〔M11〕MemoryItem/kind 枚举/入参校验（kind=fact|preference，对外 snake_case）
│   │   ├── embeddings.py               # 〔M11〕EmbeddingProvider Protocol + LocalDeterministicEmbedder（EMBED_DIM=256、signed hashing、中文 unigram+bigram、纯 stdlib）+ cosine_similarity
│   │   ├── items.py                    # 〔M11〕进程内 MemoryStore（remember/recall/list/update/delete/clear，租户计数 mem-N；update 为 docs/28 批 4⑩ 白名单字段手动编辑）
│   │   └── adapter.py                  # 〔M11〕MemoryHarnessAdapter（adapter_id=memory，双能力 memory/remember[write]·memory/recall[read]）
│   ├── skills/                         # 技能（Skill；M7 已落码 2026-09-17（0f934d6/ff4acf9/e46fc09）——Bot＝技能声明+适配器集合+受护栏 Coordinator，08 M7 立项条+落码条）
│   ├── coordination/                   # 任务信封执行层（M7 已落码 2026-09-17（0f934d6/e46fc09）：Envelope/TaskStore/sandbox，08 M7 立项条+落码条 + ADR T20）
│   ├── logistics/                      # 假物流适配器（M7 沙盘，ff4acf9；不接真实系统，不解除 D31）
│   ├── storage/                        # M5 存储抽象与恢复（契约设计 docs/24，2026-09-17；**M5a 已落码（base/memory，0358696）**、**M5b 已落码（pg/recovery/frame，2026-09-17 三批 bf185ed/b4abb59/280493e/1c1539a/f6a33e7）**，T18 已拍板 B）
│   │   ├── base.py                     # 八个已落码资源 Protocol（Graph/Recording/Feedback/Session/Approval/Debug/Run/Monitoring，方法与各 store API 一比一）+ for_tenant 工厂 + reset 分档 + StorageError（resettable/persistent）；〔M11〕拟加**第九个 MemoryRepository**（remember/recall/list/delete/clear，RESET_RESETTABLE，docs/26 立项未开工）
│   │   ├── memory.py                   # 进程内实现（默认/测试后端；store 一次性纯搬移收拢，GraphStore/FeedbackStore 自 api/main.py 内联移出，零行为变化）；〔M11 已落 5441902〕re-export memory.items.MemoryStore（第九个）
│   │   ├── pg.py                       # PG 实现（M5b；复用 memory/ 的 psycopg 连接层，行内 tenant_id 由工厂注入）；〔M11 已落 73e53bd〕PgMemoryStore + PgBackend.memory_store(tenant_id)，迁移 006_memory.sql（memory_items 表 vector(256) + ivfflat lists=100，created_at 落 TEXT）
│   │   └── recovery.py                 # 恢复扫描器（M5b：启动时逐帧重建 pending——approval/debug/wait，幂等，失败帧隔离）
│   │   └── __init__.py
│   ├── versioning/                     # Graph 版本化（M6 立项并落码 2026-09-17（1f49644），docs/20 §4.1 / ADR T19：graphId@version 不可变发布物 + subgraph 钉版）
│   │   ├── __init__.py
│   │   ├── publish.py                  # publish(graph) -> (graph_id, release_version)：冻结快照 + 递归钉版（子图 graphId@vN）
│   │   └── upgrades.py                 # 〔docs/28 批 4⑪ `ba97088`〕subgraph_upgrade_plan 发布前只读升级体检（草稿顶层子图 from→to/first_pin，配 GET /api/graphs/{id}/subgraph-upgrades）
│   ├── routing/                        # 入站路由与灰度发布（M9 已落码 2026-09-18 批 1-4＋收口，docs/20 §4.4 / 04 §5.16 / ADR T22：进程内 Router 三段分桶 + rollout 状态机 + pin-to-version + 门控自动回滚，纯 stdlib 零新依赖；真实网关/多实例随 D6/D10b/D32）
│   │   ├── __init__.py                 # 公开符号导出（TriggerEvent/RolloutConfig/RoutingStore/resolve_version 等）
│   │   ├── models.py                   # TriggerEvent + RolloutConfig/四段规则（InternalRule/BucketRule/CanaryRule/FullRule）/GateConfig pydantic 模型（19 §2.3.3 工程化承载，v1 无 when 表达式解析器）
│   │   ├── router.py                   # resolve_version 纯函数：internal→低金额桶→sha256(graph_id:order_id)%100 canary→full 固定序，缺桶键 fail-safe 落 stable
│   │   ├── store.py                    # RoutingStore（每租户一个挂 TenantServices、reset 清空）：configure/start/promote/rollback/resolve/snapshot + 分流计数
│   │   └── gate.py                     # evaluate_after_run(services, record)：观察窗+minSamples 门控求值，越阈自动 rollback + rollout_gate 告警（API 层 record_run 后调用，monitoring 不反向依赖）
│   ├── tracing/                        # span 级链路追踪（M10 已落码 2026-09-18，docs/20 §4.5 / ADR T21：与 OTel 同形最小 span、纯 stdlib 零新依赖，不引 OTel SDK，合流随 D11）
│   │   ├── __init__.py                 # 公开符号导出（Tracer/Span/start_span/current_span_context 等）
│   │   └── tracer.py                   # Span{traceId/spanId/parentSpanId/kind/attrs/status/internal} + contextvars 进程内传播 + to_tree(include_internal) 折叠开关
│   ├── collaboration/                  # 智能体协同
│   │   ├── __init__.py
│   │   ├── approvals.py                # 进程内审批信号 ApprovalBroker（token/Event，human_approval 节点挂起源，04 §5.6；M8 起 pending 携带 card_template_id/渲染上下文快照；审计可见与审批结果补强批（2026-09-22，ecfa325，docs/37）起增 `list_decided` 只读投影、`_Pending.notify_recipients` 与 `get_notify_recipients`；持久化中断缓做 D20）
│   │   └── cancellations.py            # B 包（2026-09-20，f9a1301）：RunCancelled(node_id) 协作式终止异常 + RunCancellationBroker（run_id→threading.Event，register 幂等/cancel 置位/unregister/reset，04 §5.12；持久化/多实例缓做 D27）
│   │   └── notifications.py          # docs/35 T2（2026-09-22，5b17784）：ApprovalNotifier 协议 + EmailApprovalNotifier（复用 MessageService 发纯文本审批挂起邮件；审批闭环批起正文带一键决策 capability URL；审计可见与审批结果补强批（ecfa325，docs/37）起 Protocol 增 `notify_decided` 决策结果邮件——无 token/链接、comment ≤200，旁路 fail-safe；D20 邮件子集，不解除缓做）
│   │   └── email_token.py            # 审批闭环批（2026-09-22，bd6ec7a，docs/36/ADR T27）：TokenIssuer HMAC 签名 capability token（base64url(json).base64url(HMAC-SHA256)，TTL ≤1h+300s，密钥 ATLAS_APPROVAL_HMAC_SECRET→ATLAS_MASTER_KEY→开发默认；verify 段数→compare_digest→JSON→exp），供公开 email-view/email-decision 免登录鉴权
│   │   └── event_waits.py           # D19 进程内 event v1（2026-09-23 落码收口，docs/47）：EventWaitBroker（per-tenant 内存、event_key 索引、token/广播/直投/超时/取消/reset，04 §5.5 事件等待追加段；中断帧/多实例缓做 D19）
│   ├── cards/                          # 交互卡片（M8 已落码 2026-09-18，docs/20 §4.3 / 04 §5.6 追加段：审批卡片双向 Schema + web/im/email 三渠道渲染降级；内置只读目录照 template 包，不解除 D33）
│   │   ├── __init__.py                 # 公开符号导出（list_cards/get_card/render_card/map_action_output/to_message_params）
│   │   ├── catalog.py                  # CardTemplate/sections/actions pydantic 模型 + CARDS 内置目录（v1 一张 refund-approval）+ list_cards/get_card
│   │   └── render.py                   # render_card（web 结构化投影 / im 文本+两按钮 URL / email 只读 HTML+token 链接，bindings 复用 graph.loader.interpolate）、map_action_output（action.output+form→decision/comment）、to_message_params（→message/send）
│   ├── httpapi/                        # API 渠道包（通用 HTTP）：HarnessAdapter 实现（待定项 2/7；Phase 2）
│   │   ├── __init__.py
│   │   ├── service.py                  # HttpApiClient：同步 httpx；管线 凭证解析→egress→熔断→重试→传输，method/url/headers/body/timeout → {status,headers(敏感头脱敏),body}
│   │   ├── resilience.py               # 批3（docs/32 §5）：RetryPolicy（幂等方法＋传输异常/429/502/503/504 指数退避全抖动、Retry-After）＋按 host CircuitBreaker（closed/open/half-open，HTTP_CIRCUIT_OPEN）
│   │   └── adapter.py                  # HttpApiHarnessAdapter（adapter_id=http，单能力 http/request；契约 04 §4.6）
│   ├── database/                       # 数据渠道包（通用 SQL）：HarnessAdapter 实现（待定项 2/8；Phase 2）
│   │   ├── __init__.py
│   │   ├── service.py                  # DatabaseClient：自建 SQLAlchemy engine（ATLAS_DATABASE_URL scheme 白名单）；query 触库前只读审查返结果集，外部连接 execute fail-closed；demo_engine 内置 SQLite seed（engine 直连）
│   │   ├── guard.py                    # 批3（docs/32 §4）：assert_read_only_sql 词法感知静态审查（去注释/拒多语句/只放单条 SELECT·WITH…SELECT → DB_SQL_NOT_READ_ONLY），外部只读连接 execute → DB_WRITE_FORBIDDEN
│   │   └── adapter.py                  # DatabaseHarnessAdapter（adapter_id=database，双能力 database/query|execute；契约 04 §4.7）
│   ├── security/                       # 出向安全准入（批3，docs/32；纯逻辑、可离线单测，不接真实 OAuth/SMTP/IM/数仓）
│   │   ├── __init__.py
│   │   ├── egress.py                   # SSRF 防护：validate_egress_url 纯函数＋EgressGuard（仅 http/https、私网/环回/链路本地/云元数据/CGNAT/保留地址恒拦、整数/进制 IP 绕过、可注入 resolver、白名单 fail-closed、不读 X-Forwarded-For；EGRESS_DENIED/EGRESS_INVALID_URL）
│   │   └── secrets.py                  # 凭证信封 enc$v1（AES-256-GCM，ADR T26=(a) cryptography；plain 仅 dev）、SecretProvider 协议、build_*_from_env 装配（未配主密钥 plain+WARNING、配了不可用 fail-closed）、secret:// 引用解析、redact_headers；SECRET_UNAVAILABLE/SECRET_DECRYPT_ERROR
│   ├── message/                        # 消息渠道包（进程内消息服务）：HarnessAdapter 实现（待定项 2；Phase 2）
│   │   ├── __init__.py
│   │   ├── service.py                  # MessageService：进程内消息记录（uuid4/sent_at，list/send/reset），群发上限 20，无真实投递
│   │   ├── adapter.py                  # MessageHarnessAdapter（adapter_id=message，单能力 message/send；契约 04 §4.8）
│   │   ├── smtp.py                     # delivery batch（docs/34）：SmtpConfig.from_env + SmtpSender（STARTTLS/login，HOST 缺省回退进程内 sink，失败 SMTP_SEND_FAILED 不落记录），channel=email 真实投递（D24 email 子集）
│   │   └── webhook.py                  # docs/35 T3（2026-09-22，54bf38d）：DefaultWebhookSender（先 EgressGuard.check 再 httpx POST、follow_redirects=False、10s；非2xx/网络错 WebhookDeliveryError，channel=webhook 单 URL，失败不落记录；D24 webhook 子集）
│   ├── connections/                    # OAuth2 连接管理 v1（docs/35 T4，2026-09-22 a57164d；generic 平台无关，D22 子集不解除缓做）
│   │   ├── __init__.py
│   │   ├── models.py                   # Connection dataclass（状态 draft/connected/error）+ public_view 驼峰投影（绝无信封/明文秘密，仅 hasClientSecret）
│   │   ├── oauth.py                    # HMAC-SHA256 自签名 state 防 CSRF（TTL 600s，密钥读 ATLAS_MASTER_KEY 缺失用固定 dev key+WARNING）、build_authorize_url、exchange_code/refresh_tokens（post_form 可注入不触网）、TokenBundle/is_expired（60s 提前量）
│   │   ├── store.py                    # 进程内 ConnectionStore（per-tenant，conn-N 序号，threading.Lock）
│   │   └── service.py                  # ConnectionService 状态机 + build_connection_service 工厂（进程单例，SecretProvider/EgressGuard 注入；create/update/authorize/exchange/refresh/test/delete；token 异常置 error，test 不调真实业务 API）；PG 实现见 storage/pg.py PgConnectionStore（迁移 014）
PG 实现见 storage/pg.py PgConnectionStore（迁移 014）
│   ├── channels/                       # 平台渠道适配层（真实渠道适配批 docs/38，2026-09-22 docs-only 立项，ADR T28；架在 T4 connections 之上，v1 仅 Shopify）
│   │   ├── __init__.py
│   │   ├── base.py                     # ChannelError（5 个 CHANNEL_* 错误码）、ChannelTransport Protocol/HttpChannelTransport（urllib，每调 EgressGuard.check，10s/follow_redirects=False）、ChannelBinding
│   │   ├── shopify.py                  # ShopifyChannelClient（shop 归一化/api_version 校验、list_orders/get_order 投影、create_refund 构造；token 注入不持久化）
│   │   ├── registry.py                 # ChannelStore Protocol + ChannelRegistry（bind/client_for/test、adapters_for_tenant 动态产出适配器）
│   │   ├── memory.py                   # 进程内 ChannelStore 内存实现（dict+锁，ch-N，per-tenant）
│   │   ├── pg.py                       # PgChannelStore（迁移 015 channel_bindings，reset 不清；裸 SQL 对齐 connections/audit）
│   │   ├── adapter.py                  # ShopifyHarnessAdapter（adapter_id=channel:shopify:{binding_id}，shop/list_orders·shop/get_order·shop/create_refund〔financial〕）
│   │   └── webhooks.py                 # 入站 Webhook（docs/39，ADR T29，2026-09-23 docs-only 立项）：verify_shopify_hmac（原始 body HMAC/compare_digest）、WebhookEnvelope/build_trigger_event（channel=webhook）、WebhookDeliverer（幂等环 ring200/1h、订阅 resolve 异步触发）
│   ├── observability/                  # 可观测交付层（delivery batch docs/34 + docs/35 T6）
│   │   ├── __init__.py
│   │   ├── health.py                   # GET /api/ready 就绪探针（内存档恒就绪、PG 档 SELECT 1，失败 503）；/api/health 存活探针保留
│   │   ├── metrics_export.py           # GET /metrics Prometheus 0.0.4 text exposition（零新依赖手写，7 个 gauge，仅聚合计数无 PII；D11 进程内子集）
│   │   └── audit.py                    # docs/35 T6（2026-09-22 f243e04）：AuditEvent（仅 8 元数据字段）/AuditStore ring 2000/租户 + PgAuditStore（迁移 013，reset 不清）+ export_jsonl；写操作 HTTP 中间件按路由模板记录、不记请求体/凭据（P1 #8）
│   ├── template/                       # 流程模板库（内置只读目录，随代码版本发布；Phase 2 能力项，待定项 9）
│   │   ├── __init__.py                 # 公开符号导出（list_templates/get_template/TemplateMeta）
│   │   ├── graphs.py                   # 5 个内置模板 Graph JSON（退款图单一事实源，nl_generate 从此导入）
│   │   └── catalog.py                  # TemplateMeta + TEMPLATES 目录 + list_templates/get_template（契约 04 §5.10）
│   ├── recording/                      # 操作录制（运行录制 + 回放比对；Phase 2 能力项，待定项 10）
│   │   ├── __init__.py                 # 公开符号导出（RecordingStore/compare_replay 等）
│   │   ├── cases.py                    # RecordingCase 模型（M9 补 graph_id、D26-b 补 subgraphs 纯超集字段）+ 进程内 RecordingStore（图快照/reset 不清除；契约 04 §5.11）
│   │   ├── replay.py                   # 易变值归一化/审批预置抽取/序列与逐节点比对（纯函数）
│   │   ├── snapshots.py                # D26-b（2026-09-19 已落码，29bb3d9）：collect_subgraph_snapshots 递归冻结子图 raw（深度≤3/visited 防环/缺失不阻断）+ inline_first_resolver（单用例 replay 内联优先、未命中回退租户 GraphStore；发布门禁刻意不内联）
│   │   ├── gate.py                     # M9 已落码：run_release_gate 发布前批量回放门禁（目标＝latest 草稿，按 case.graph_id 筛选用例逐例 run_graph + compare 产 GateReport，D26 部分取回；纯函数不碰存储，resolver 保持实时不内联）
│   │   ├── reports.py                  # D26 报告 v1（2026-09-18，U60）+ D26-a 导出（2026-09-19，8a37b4e）：ReleaseReport/ReportCaseRow pydantic + ReportStore 进程内 ring 100/租户（record/list_summary/get/reset）+ report_to_csv（stdlib csv/io），沉淀/两 GET/export 端点接线在 api/
│   │   └── shadow.py                   # docs/33 §3 ④（2026-09-21，cea7111）：ShadowRun/ToolIntent/ShadowDecision/HumanOutcome/ShadowComparison 模型 + extract_shadow_events（编译期 permission 表短路、读透传/写 SHADOW_DRY_RUN 意图）、compare_shadow/normalize_human_action 纯函数 + ShadowStore ring 100/租户（进程内、不 PG 化、reset 清空）；4 REST 端点在 api/
│   ├── debug/                          # 单步调试与断点（进程内调试会话/暂停状态机；Phase 2 能力项，待定项 11）
│   │   ├── __init__.py                 # 公开符号导出（DebuggerBroker/DebugController/DebugStopped）
│   │   ├── sessions.py                 # DebugSession + DebuggerBroker（dbg-uuid token + Event 暂停/恢复原语；B 包 f9a1301 起 BreakpointSpec 超集 expression/hitCount/logMessage、会话 hit_counts、_Pause.overrides、apply/take_overrides/bump_hits；契约 04 §5.12）
│   │   └── controller.py               # DebugController.before_node：step/断点/条件判定、paused 帧、wait、DebugStopped；B 包起注入 is_cancelled（调试流急停折叠 stopped）、_classify_hit 三态（logpoint 发 debug_log 不暂停/hitCount 每 N 次/条件异常 fail-safe）、resume globals 浅合并写回
│   ├── monitoring/                     # 基础监控告警（进程内运行记录 ring + 规则求值；Phase 2 能力项，待定项 12）
│   │   ├── __init__.py                 # 公开符号导出（MonitoringStore/RunRecord/RuleConfig/summarize 等）
│   │   ├── records.py                  # RunRecord/NodeResult + MonitoringStore（deque 200、单锁、streak、告警合并、reset；M9 加 resolved_version/business；docs/28 批3⑧加 ToolCallMetric/RunRecord.tool_calls，⑨新建 Alert 带 rule_name）
│   │   #   docs/33 ⑤加 RunRecord.spans（tracer.to_tree 根 dict，列表端点剔除、trace 端点懒加载，PG 迁移 012 JSONB）；⑩告警产生先过静默匹配、新建指派值班人、list/get 惰性升级
│   │   ├── metrics.py                  # extract_node_results 失败推断/nearest-rank 百分位/summarize（纯函数；M9 增 business 段；docs/28 批3⑧增 summarize_tools 按 tool 聚合 calls/failed/simulated/error_codes/p50/p95，summarize 增 tools 段）
│   │   ├── alerts.py                   # RuleConfig/Alert/默认规则/validate_rules/evaluate_rules（纯函数；M9 RuleId 增 rollout_gate、Alert 增 action；docs/28 批3⑨RuleId 放宽 str、增 CustomRule/RuleConfig.custom/rule_name，复用 graph.conditions 安全求值，白名单四变量 fail-safe）
│   │   #   docs/33 ⑩加 RuleConfig.escalation_ack_minutes（1-10080）、Alert.escalated_at/assignee、apply_escalation（open warning 超时读时惰性升 critical，幂等）
│   │   ├── business.py                 # M9 已落码（2026-09-18）：extract_business 业务结果提取纯函数（shop 终态 refunded/human_review → 自动退款/转人工/退款金额差异，金融灰度门控信号源）
│   │   └── silences.py                 # docs/33 §5 ⑩（2026-09-21，00bba8c）：Silence/OnCallSchedule 模型 + silence_matches/is_silence_active/current_assignee 纯函数 + OpsStore（单锁进程内：静默 sil-N 上限 100 惰性清过期、值班去重保序取模轮换、PG 档 assignee map、reset）；内存/PG 两档共用、不落库；6 REST 端点在 api/
│   ├── iam/                            # 多租户与权限（Phase 2 RBAC ＋ P0 批 2 生产认证，ADR T25/docs/31 已落码 2026-09-21；权威 04 §5.14/§5.17）
│   │   ├── __init__.py                 # 公开符号导出（Role/Principal/SessionStore/UserStore/TenantRegistry 等）
│   │   ├── principals.py               # Role/Principal 模型、SEED_TENANTS/SEED_USERS 种子常量、can 角色矩阵
│   │   ├── passwords.py                # docs/31 已落码：hashlib.scrypt 哈希/verify（scrypt$ 串、hmac 恒定时间比较）、口令策略（8–128、弱口令黑名单）
│   │   ├── accounts.py                 # docs/31 已落码：UserStore/PgUserStore（iam_users 迁移 010）、幂等 seeder（种子账号哈希落库、只插缺失不覆盖改密）、改密/禁用/重置的会话吊销
│   │   ├── throttle.py                 # docs/31 已落码：登录失败滑动窗口节流（键 username|client_ip、600s/5 次→429，进程内）
│   │   ├── sessions.py                 # SessionStore（sess- 不透明 token；内存/PG 两档，PG 落 iam_sessions 重启不丢，迁移 011 加 expires_at 绝对 TTL）
│   │   ├── registry.py                 # TenantServices 每租户服务 bundle + TenantRegistry 惰性装配/reset_tenant；**`peek` 只返回已存在 bundle 或 None、绝不创建**（审批闭环批 docs/36，公开邮件路径防租户枚举，2026-09-22 bd6ec7a）；docs/33 ④起 bundle 挂 shadow_store（ShadowStore），monitoring bundle 内含 OpsStore（静默/值班/assignee）
│   │   └── deps.py                     # FastAPI 依赖：get_principal（Bearer→401）、require（角色→403）、services_for
│   ├── api/                            # FastAPI Demo 后端入口（10 §2 /api/main.py；T5 网关接口的 Demo 宿主）
│   │   ├── __init__.py
│   │   └── main.py                     # /api/auth 登录/me/登出/改密（change-password）+ /api/users 用户管理（列/建/改/重置密码，admin）+ Bearer 会话、/api/graphs 保存/读取/编译/运行 + SSE 运行流、NL 生成、适配器发现、Demo 店铺控制台、/api/demo/mock/* HTTP 演示目标、/api/demo/messages 进程内消息查看、/api/templates 内置只读模板目录、/api/recordings 录制用例与回放、/api/debug 调试暂停与恢复、/api/monitoring 运行指标与规则、/api/alerts 站内告警中心、docs/33（2026-09-21）影子 4 端点（`POST /api/graphs/{id}/shadow-runs`、`GET /api/shadow-runs[/{sid}]`、`POST /api/shadow-runs/{sid}/compare`）＋`GET /api/monitoring/runs/{id}/trace` 懒加载＋静默 3 端点（`/api/monitoring/silences`）＋值班 3 端点（`/api/monitoring/on-call`、`.../rotate`）、M9 `PUT /api/graphs/{id}` 同图草稿更新（update_draft，覆盖 latest 不动发布版）、`POST /api/graphs/{id}/release-gate` 发布前批量回放门禁、`POST /api/graphs/{id}/publish` 可选 `{gate:true}` 拦截、`GET/PUT /api/graphs/{id}/rollout` 与 `.../rollout/{start,promote,rollback}` 灰度状态机、run[/stream] 可选 `event`（入站三段路由）/`releaseVersion`（钉版）（业务端点按租户分区 + 三角色白名单，契约 04 §5.14/§5.16）
│   └── web/                            # Web 操作适配器（Playwright）
│       ├── __init__.py
│       ├── location.py                 # 三层定位：选择器→视觉语义→全图推理（06 §6.5）
│       └── adapter.py                  # WebHarnessAdapter：navigate/click/type/screenshot
└── tests/                              # 测试（层次见 06）
    ├── __init__.py
    ├── test_engine_loop.py             # W1 OODA 循环冒烟测试（后续 test_*.py 按模块补齐）
    ├── test_harness_contract.py        # 注册发现/心跳/权限审计（04 §4.4、I8）
    ├── test_web_location.py            # 三层定位编排（假页面，I1-I5）
    ├── test_web_browser_integration.py # 真实 Chromium 集成（I1/I2/I5，integration 标记）
    ├── test_graph_dsl.py               # Graph DSL 解析与静态校验（W7-W8）
    ├── test_graph_loader.py            # DSL→LangGraph 编译/运行/插值（W7-W8）
    ├── test_api_graphs.py              # graphs 保存/读取/编译/运行 REST（TestClient，W7-W8）
    ├── test_decision_client.py         # 退款决策黄金用例 + LiteLLM 解析/fail-safe（W9-W10）
    ├── test_shop_adapter.py            # DemoShopService 状态流转 + ShopHarnessAdapter 能力/权限（W9-W10）
    ├── test_nl_generate.py             # NL 退款草稿生成/未识别意图（W9-W10）
    ├── test_refund_e2e.py              # DSL 编译运行至 shop 适配器的退款端到端黄金用例（W9-W10）
    ├── test_api_demo.py                # SSE/NL/adapters/Demo 店铺 REST（TestClient，W9-W10）
    ├── test_shop_platform_integration.py # 自动登录+抓取退款单集成（integration 标记，W9-W10）
    ├── test_database_adapter.py        # 通用 SQL 适配器（SQLite StaticPool 注入，query/execute/权限/脱敏，Phase 2）
    ├── test_message_adapter.py         # 进程内消息适配器（校验/记录/reset/权限，Phase 2）
    ├── test_templates.py               # 内置模板目录不变量（5 模板 parse_graph 合法/工具已注册/元数据一致，Phase 2 能力项）
    ├── test_recording.py               # 录制用例存储/归一化/审批预置/回放比对纯逻辑（U28，Phase 2 能力项）
    ├── test_debugger.py                # 调试会话/暂停状态机/条件 fail-safe/并行串行化（U29，Phase 2 能力项）
    ├── test_monitoring.py              # 运行记录 ring/失败推断/聚合/四规则/告警合并与状态迁移（U30，Phase 2 能力项）
    ├── conftest.py                     # autouse：为模块级 TestClient 注入 t1 admin Bearer（既有 API 用例默认租户 t1）
    ├── test_iam.py                     # 种子认证/SessionStore/角色矩阵/TenantRegistry 分区与 reset（U31，Phase 2 能力项）
    ├── test_api_auth.py                # 401/403/跨租户 404/分区列表/reset 范围/登录登出 REST（I19，Phase 2 能力项）
    └── test_database_integration.py    # PostgreSQL/pgvector 集成测试（标记 integration，默认跳过）
```

## 模块 → 文档映射

| 包路径 | 职责 | 依据文档 |
|---|---|---|
| `src/atlas/engine/` | Loop 主循环：感知→规划→执行→反馈；决策节点 | 06 运行时与质量保障 |
| `src/atlas/harness/` | Harness Gateway：平台抽象与能力集成总线 | 06 运行时与质量保障；02 技术架构 |
| `src/atlas/graph/` | Graph：可执行因果有向图（LangGraph 层） | 02 技术架构；04 组件设计 |
| `src/atlas/llm/` | 模型接入层：LiteLLM 决策客户端（规则兜底）、condition 语义分支分类器（离线兜底，D14，2026-09-23 落码 docs/48）与 NL 草稿生成 | 10 §3 T1；06 §9.2/§6.23；08 §7.3 |
| `src/atlas/shop/` | 电商渠道包：DemoShopService + ShopHarnessAdapter（退款业务能力） | 06 §6.4/§9.2；08 §7.1 |
| `src/atlas/nodes/` | 节点/工具/逻辑/变量组件（Schema 见 03） | 04 组件设计-编辑后台 |
| `src/atlas/memory/` | 记忆系统：连接层（settings/database，W1）+〔M11 已落码〕长期记忆内核 models/embeddings/items/adapter（fact/preference、本地确定性向量、remember/recall 两适配器工具）；memory_config 五层策略为愿景（见 03/05），working/summary/case/自动提取/PII 等产品化余部缓做 D35 | 05 五项核心；06 运行时；docs/26；ADR T23 |
| `src/atlas/skills/` | 技能（skill_schema 见 03/05） | 05 五项核心 |
| `src/atlas/collaboration/` | 智能体协同（collaboration_message 见 03/05）；docs/35 T2 加 `notifications.py` 审批挂起邮件通知（D20 邮件子集）；审批闭环批（docs/36，2026-09-22）加 `email_token.py` HMAC capability token 签发/校验（ADR T27，免登录邮件决策）；审计可见与审批结果补强批（docs/37，2026-09-22）加 broker `list_decided` 投影/`notify_recipients` 与 notifier `notify_decided` 结果邮件 | 05 五项核心；docs/35、36、37 |
| `src/atlas/cards/` | 交互卡片（M8）：CardTemplate 内置只读目录（catalog）+ web/im/email 三渠道渲染降级与 action 输出映射（render）；bindings 复用 graph 插值、actions 经 collaboration 审批决策端点；无状态/无存储，REST 只读投影在 api/ | 04 §5.6 追加段；05 §5.3；19 §2.2.3/§2.3.2 |
| `src/atlas/httpapi/` | API 渠道包（通用 HTTP）：HttpApiClient + HttpApiHarnessAdapter（单能力 http/request） | 04 §4.6；06 §6.6 |
| `src/atlas/database/` | 数据渠道包（通用 SQL）：DatabaseClient（自建 engine，ATLAS_DATABASE_URL 隔离 + SQLite demo）+ DatabaseHarnessAdapter（database/query 只读、database/execute 写） | 04 §4.7；06 §6.7 |
| `src/atlas/security/` | 出向安全准入（批3 docs/32，2026-09-21 落码）：`egress.py` SSRF 出向校验、`secrets.py` 凭证信封(AES-256-GCM)/运行时注入/脱敏；另 `database/guard.py` 只读 SQL 审查、`httpapi/resilience.py` 重试熔断 | 04 §4.6/§4.7；06 §6.6/§6.7；docs/32 |
| `src/atlas/connections/` | docs/35 T4（2026-09-22 落码）generic OAuth2 连接管理 v1：models（投影不泄密）/oauth（state CSRF、授权码交换/刷新）/store（内存）/service（状态机）；PG 实现 PgConnectionStore 在 storage/pg.py（迁移 014）；9 端点+回调页在 api/、连接管理 UI 在 frontend（D22 子集，平台无关） | docs/35 §4；03 `connection` |
| `src/atlas/channels/` | 真实渠道适配批（docs/38，2026-09-22 docs-only 立项，ADR T28）平台渠道适配层，架在 T4 connections 之上：base（ChannelError/transport/ChannelBinding）/shopify（ShopifyChannelClient 读订单·退款）/registry（ChannelRegistry bind·client_for·test、动态适配器产出）/memory（内存 store）/pg（PgChannelStore，迁移 015）/adapter（ShopifyHarnessAdapter，shop/* 三工具）；绑定 reset 不清；v1 仅 Shopify，真实店铺联调缓做 D22。**入站 Webhook 批（docs/39，2026-09-23 docs-only 立项，ADR T29）增 webhooks.py（Shopify HMAC 验签＋WebhookDeliverer 异步投递、幂等环），迁移 016 webhook_subscriptions，公开 hook 端＋订阅 GET/PUT；Shopify 侧注册/DLQ/Amazon 缓做 D22/D24** | docs/38；04 §5.18；03 `channel_binding`；**docs/39；04 §5.19；03 `channel_webhook`** |
| `src/atlas/observability/` | 交付层可观测：`health.py` /ready 就绪探针、`metrics_export.py` /metrics Prometheus 文本（docs/34 delivery batch）；`audit.py` 审计事件 ring+PG（迁移 013）+写操作中间件+jsonl 导出（docs/35 T6，P1 #8） | docs/34；docs/35 §6 |
| `src/atlas/message/` | 消息渠道包：MessageService（进程内记录 sink）+ MessageHarnessAdapter（message/send）；`smtp.py` channel=email 真实投递（docs/34）、`webhook.py` channel=webhook（docs/35 T3，过 EgressGuard） | 04 §4.8；06 §6.7；docs/35 §3 |
| `src/atlas/template/` | 流程模板库（内置只读）：5 个模板 Graph JSON + TemplateMeta 目录访问；无状态/无存储，REST 只读投影在 api/ | 04 §5.10；06 §6.8 |
| `src/atlas/recording/` | 操作录制：RecordingCase 模型与进程内 RecordingStore（图快照）、replay.py 归一化/审批预置/逐节点比对纯函数；M9 增 gate.py 发布前批量回放门禁（GateReport，D26 部分取回）；D26 报告 v1（2026-09-18，U60）reports.py 报告沉淀（ReleaseReport ring 100/租户、按图历史/通过率趋势）；**D26 收尾批（2026-09-19）**：snapshots.py 子图快照内联（仅 replay）、report_to_csv + export 端点、`.github/workflows/release-gate-cron.yml` 周一定时跑 d26_smoke（cron 仅 main 生效）；**docs/33 ④（2026-09-21，cea7111）增 shadow.py 影子模式（ShadowStore ring 100/租户、读透传/写短路、零污染，4 REST 端点）** | 04 §5.11/影子落码块；06 §6.9 |
| `src/atlas/debug/` | 单步调试与断点：DebugSession/DebuggerBroker 进程内会话（Event 暂停原语、step/continue/stop 状态机）、DebugController.before_node 统一执行器钩子；REST 在 api/ | 04 §5.12；06 §6.10 |
| `src/atlas/monitoring/` | 基础监控告警：RunRecord/NodeResult 与 MonitoringStore（ring 200、单锁、按图 streak、告警合并）、metrics 失败推断/百分位/汇总纯函数（M9 增业务结果三率 business 段；docs/28 批3⑧增 summarize_tools 适配器调用聚合 tools 段）、alerts 四内置规则/校验/求值纯函数（M9 增 rollout_gate/action；批3⑨增 custom 自定义规则 DSL，复用 graph.conditions 禁 eval、白名单四变量 fail-safe、rule_id=custom:{cid}/rule_name）、records 的 ToolCallMetric/RunRecord.tool_calls、business.py 业务结果提取；tool_metric 在 graph/loader 计时发事件、采集与 REST 在 api/；**docs/33 ⑤（4579843）RunRecord.spans＋trace 懒加载端点（PG 迁移 012）、⑩（00bba8c）silences.py 静默/惰性升级/值班 OpsStore＋6 REST 端点，进程内不 PG 化** | 04 §5.13/§5.15；06 §6.11 |
| `src/atlas/tracing/` | span 级链路追踪（M10）：与 OTel 同形最小 Span/Tracer、contextvars 进程内传播、`to_tree(include_internal)` 折叠；埋点在 loader（run/节点/工具/parallel/subgraph）与 coordination（任务信封），SSE 超集出口与 RunRecord.trace_id 在 api/；纯 stdlib 零新依赖、不引 OTel SDK | 04 §5.15；06 §6.15 |
| `src/atlas/routing/` | 入站路由与灰度发布（M9）：TriggerEvent/RolloutConfig 四段规则模型（models）、resolve_version 三段分桶纯函数（router，sha256(graph_id:order_id)%100 稳定桶、缺桶键 fail-safe）、RoutingStore rollout 状态机 configure/start/promote/rollback/resolve/snapshot（store，每租户一个、reset 清空）、evaluate_after_run 指标门控自动回滚（gate，API 层调用）；pin-to-version 经 run 端点可选 event 字段钉不可变发布快照；REST 在 api/；纯 stdlib 零新依赖 | 04 §5.16；06 §6.17；19 §2.3.3/§2.5 |
| `src/atlas/iam/` | 多租户与权限（Phase 2）＋生产认证（P0 批 2 已落码 2026-09-21）：种子租户/Principal、三角色能力矩阵、TenantServices 惰性注册表、Bearer/角色 FastAPI 依赖；`passwords.py` scrypt 哈希/口令策略、`accounts.py` UserStore 内存·PG 两档/幂等 seeder/会话吊销、`throttle.py` 登录节流、`sessions.py` 两档＋`expires_at` 绝对 TTL；REST 在 api/ | 04 §5.14/§5.17；06 §6.12 |
| `src/atlas/api/` | FastAPI Demo 后端入口与 REST 路由（12 文档后端 REST）；同时是 T5 Harness 网关接口的 Demo 宿主 | 10 §2；12 API 清单 |
| `src/atlas/web/` | Web 操作适配器（Playwright 实现） | 06 运行时 |
| `tests/` | 单元/集成/端到端测试（层次见 06） | 06 运行时与质量保障 |

## 待填项清单（AI 落码时的任务入口）

| 文件 | 待填内容 | 前置依赖 |
|---|---|---|
| `pyproject.toml` | ✅ 已完成（2026-09-13）：依赖清单、打包配置 | — |
| `src/atlas/engine/` | ✅ W1 最小 OODA 骨架已完成（state/nodes/loop + 3 冒烟用例）；LLM/Harness 接入待后续周次 | — |
| `frontend/` | ✅ W9-W10 已支持退款 Demo（退款单选择、SSE 流式运行与节点实时状态、NL 草稿载入、结果弹窗）；设计 Token 层已落（`theme/tokens.ts`+`setup.ts`，17 文档 §3） | — |
| `src/atlas/harness/` | ✅ W3-W4 契约层已完成（`base.py` 适配器 ABC + 权限/审计模板方法，`registry.py` 注册/发现/心跳，13 个单元/集成用例） | 无 |
| `src/atlas/graph/` | ✅ W9-W10 已接真实执行（`dsl.py` GraphDSL 契约 + 静态校验；`loader.py` DSL→LangGraph 编译/运行/插值，依赖注入决策客户端、适配器注册表与事件回调，webhook 载荷经 inputs 进入 trigger 节点）；Phase 2：condition 条件分支（2026-09-14）、loop 条件循环（while，回边白名单环 + fail-safe 退出，2026-09-14；2026-09-19 D17 增 break〔体内 condition→exitTarget，合成 `__break__<loop>` 网关、exitReason=break〕/continue〔回边重入〕）、parallel 静态扇出/汇聚（all_success/all_completed，列表路由 + 合成 `__join__` 网关 + outputs 按键合并，fail-safe 汇聚，2026-09-14；2026-09-19 D18 增 any_success OR-join〔未开始分支短路 skipped、已发起调用不回滚、全失败才 failed、网关幂等 done→END〕）、wait 定时等待（duration 1-600s 同步 sleep，2026-09-14）＋事件等待进程内 v1（event，EventWaitBroker 挂起/信号/超时，2026-09-23 落码 docs/47；中断帧/多实例缓做 D19）、**2026-09-23 立项并当日落码收口定时等待动态时长（durationMode=dynamic，durationExpression 条件引擎求值，坏表达式 WAIT_DURATION_INVALID，docs/49；8580fcb/4eaaddd，后端 1472/59、前端 621/2）**、**2026-09-23 docs-only 立项到点时刻等待（durationMode=absolute，absoluteTime ISO/epoch 解析差值，坏时刻/过点 WAIT_ABSOLUTE_TIME_INVALID，docs/50）**、human_approval 人机协作（ApprovalBroker 进程内审批信号阻塞 + conditional 双出口 + 超时自动决策，中断-恢复缓做 D20，2026-09-14）、subgraph 子图引用（graph_resolver 注入解析已保存图 + 进程内重入 run_graph + 跨图自引用/环/深度≤3 递归校验 + fail-safe，版本钉版缓做 D21，2026-09-14） | 无 |
| `src/atlas/graph/conditions.py` | ✅ Phase 2 首项（2026-09-14 落地）：condition 节点首版表达式求值器（tokenize + 递归下降 parse + 静态校验/运行时求值，纯 stdlib 禁 eval）；dsl 图级校验（branches/defaultTarget/出边覆盖/trigger 根可达 BFS）；loader condition 出边全走 `add_conditional_edges`；前端 TS 同构校验与配置 UI。契约 04 §5.1/§5.2，用例 13 U5/U6/U11/U12 | 无 |
| `src/atlas/llm/` | ✅ W9-W10 已完成（`decision.py` LiteLLM 决策 + 规则兜底 + fail-safe，`nl_generate.py` LLM/模板双通道；黄金用例单测）；2026-09-23 落码 `condition_classifier.py`（D14，docs/48：ConditionClassifier 三层 + Offline 兜底 + run_graph(condition_classifier=) 注入） | 无 |
| `src/atlas/shop/` | ✅ W9-W10 已完成（`service.py` 种子退款单与状态流转，`adapter.py` 五能力含 financial 权限；单元 + 平台集成测试） | 无 |
| `src/atlas/nodes/` | 节点 Schema→类实现（03/04 有 Schema） | 无 |
| `src/atlas/memory/` | ✅ W1 连接层已完成（`settings.py` 环境变量读取 + `database.py` 引擎/会话/ping/pgvector 探针，迁移 001 已启用扩展，集成测试 3 用例全绿）。**M11 长期记忆（2026-09-19 四批落码收口 `5441902`/`58d936c`/`73e53bd`/`75c4150`，U72–U99 已转正式见 13 §9；契约 docs/26/ADR T23）**：新增 `models.py`（MemoryItem fact/preference）、`embeddings.py`（EmbeddingProvider + 本地确定性 embedder 256 维纯 stdlib）、`items.py`（进程内 MemoryStore）、`adapter.py`（remember/recall 两能力）；storage 加第九个 MemoryRepository + PgMemoryStore（迁移 006 vector(256)）；REST 浏览/搜索/删除 + 前端记忆页；**docs/28 批 4⑩（`ec0fd81`/`19f31b4`，U192–U201）补 POST/PUT /api/memories 手动新建/编辑（operate、source 固定 manual、两档 store 同构 update、白名单字段、content 变重算 embedding，前端记忆页新建/编辑 Modal）**，批 4⑪（`ba97088`，U202–U211）另在 versioning/upgrades.py 加发布前子图版本升级体检（GET subgraph-upgrades 只读、ReleaseModal 体检区）。Redis 短期工作记忆、summary/case 层、自动提取、决策隐式注入、商业 embedding、PII/更新策略/字段级历史缓做 14 D35 | 无 |
| `src/atlas/skills/` | 技能注册与执行（03/05 有 skill_schema） | 无 |
| `src/atlas/collaboration/` | 协同消息协议（03/05 有 collaboration_message）；✅ Phase 2 第五项 `approvals.py` 进程内审批信号 ApprovalBroker（human_approval 节点挂起源，2026-09-14）；✅ B 包 `cancellations.py` RunCancelled + RunCancellationBroker 协作式急停（2026-09-20，f9a1301，挂 TenantServices、memory/pg 两档 registry 接线）；审批持久化中断-恢复随 D20、急停持久化/多实例随 D27；✅ D19 进程内 event v1 `event_waits.py` EventWaitBroker（2026-09-23 落码收口 docs/47，挂 TenantServices event_wait_broker、两档 registry 接线，中断帧/多实例仍随 D19） | 无 |
| `src/atlas/cards/` | ✅ M8 已落码收口（2026-09-18，2b35fc9/5f4384e/4b45571/25afa10/94000de/486a591，08 M8 落码条）：`catalog.py` CardTemplate + 内置 `refund-approval` 只读目录（照 template 包，无 DB/CRUD、reset 不影响），`render.py` 三渠道渲染 + map_action_output + to_message_params；human_approval 可选 cardTemplateId 接线（dsl 校验/loader/broker/frame 恢复）、REST `/api/cards`+`/api/approvals/{token}/card`+decision actionId 在 api/，前端 CardRenderer（FormRenderer 第三来源 kind:"card"）+ card-select + bindings L2；三渠道冒烟脚本 scripts/dev/m8_smoke.py + 证据图。卡片 CRUD/市场/版本化、真实 IM/邮件外发（D24）、邮件 GET 决策落地页、网页嵌入 SDK 缓做 14 D33 | 无 |
| `src/atlas/httpapi/` | ✅ Phase 2（2026-09-15）：`service.py` 同步 httpx 客户端（env/显式默认 baseUrl+headers，method/url/headers/body/timeout→{status,headers,body}），`adapter.py` 单能力 http/request（write）；契约 04 §4.6/06 §6.6；OAuth2/连接 CRUD/OpenAPI 导入缓做 14 D22；**凭证信封/SSRF egress/重试熔断已落码 docs/32（ADR T26=(a)，2026-09-21：`security/{secrets,egress}.py`＋`httpapi/resilience.py`，cryptography AES-256-GCM；egress 06b2473/resilience 5376e79/secrets 01a2f97）** | 无 |
| `src/atlas/database/` | ✅ Phase 2（2026-09-15）：`service.py` DatabaseClient（自建 SQLAlchemy engine 工厂，ATLAS_DATABASE_URL scheme 白名单 postgresql+psycopg/sqlite，与平台 DATABASE_URL 隔离；query 只读事务+rollback/limit 截断，execute commit 返 rowcount；demo_engine SQLite StaticPool seed 两笔订单），`adapter.py` 双能力 database/query（read）/execute（write）；契约 04 §4.7/06 §6.7；JDBC/ODBC/数仓、schema 表单、审计分页缓做 14 D23；**只读强制＋SQL 静态审查已落码 docs/32（2026-09-21 `database/guard.py`，a189dc1：query 两档强制审查、外部连接 execute 禁用 DB_WRITE_FORBIDDEN），真实最小权限只读账号仍随部署** | 无 |
| `src/atlas/message/` | ✅ Phase 2（2026-09-15）：`service.py` MessageService 进程内消息记录（uuid4/sent_at，群发上限 20，reset 清空，零真实投递），`adapter.py` 单能力 message/send（write）；契约 04 §4.8/06 §6.7；SMTP/IM/短信/webhook、模板系统、reply 线程、入站消费、限流重试缓做 14 D24 | 无 |
| `src/atlas/template/` | ✅ Phase 2 能力项（2026-09-15）：`graphs.py` 5 个内置模板 Graph JSON（退款/HTTP 分流/SQL 通知/SQL 写审批/审批超时），`catalog.py` TemplateMeta + 只读访问；契约 04 §5.10/06 §6.8；退款模板从 llm/nl_generate 移入（单一事实源，llm→template 依赖方向）；用户 CRUD/市场/远程模板/版本钉版缓做 14 D25 | 无 |
| `src/atlas/recording/` | ✅ Phase 2 能力项（2026-09-15）：`cases.py` RecordingCase（图快照冻结）+ 进程内 RecordingStore（reset 不清除），`replay.py` 易变值归一化/审批预置抽取/序列+逐节点比对；REST 五端点挂 api/；契约 04 §5.11/06 §6.9；批量回放/跨版本对比/影子模式/Mock/参数化缓做 14 D26，浏览器操作录制生成工具缓做 14 D22。**M9（2026-09-18 已落码收口，U57 转正式）部分取回 D26**：`cases.py` RecordingCase 补 `graph_id` 纯超集字段（创建端点已收未存），新增 `gate.py` run_release_gate 发布前批量回放门禁（目标＝latest 草稿、按 graph_id 筛例、逐例 run_graph+compare、GateReport blocked 409），REST `POST /api/graphs/{id}/release-gate` 与 publish `gate` 参数挂 api/；**D26 报告 v1（2026-09-18 落码收口，U60 转正式；`9dc9690`/`5c812c5`/`f5c789c`）新增 `reports.py`**：ReleaseReport 沉淀（manual/publish-gate，含 blocked/skipped）、ReportStore ring 100/租户（rr-N 租户级单调计数、reset 归零）、两 GET 历史/详情、reset 清空报告而录制用例保留，前端 ReleaseModal 通过率趋势折叠区；影子模式/PG 持久化等 D26 余部仍缓做 | 无 |
| `src/atlas/debug/` | ✅ Phase 2 能力项（2026-09-15）：`sessions.py` DebugSession/DebuggerBroker（dbg-uuid token + threading.Event 暂停/恢复原语，step/continue/stop 状态机，reset 按 stop 释放）+ `controller.py` DebugController.before_node 统一执行器钩子（node_start 后/节点逻辑前，条件断点复用 §5.1 表达式引擎、异常 fail-safe）；`DebugStopped` 冒泡由 /run/stream worker 转 stopped 帧；REST /api/debug/* 挂 api/；契约 04 §5.12/06 §6.10；**B 包（2026-09-20，f9a1301）已取回进程内三项：运行中协作式急停（cancellations broker + POST /api/runs/{id}/cancel + cancelled 帧/状态）、断点 hitCount 命中计数与 logMessage 日志断点（debug_log 帧不暂停）、暂停 resume globals 顶层键白名单浅合并改写续跑（422 非法键）**；**docs/28 批 2（`038dcaa`/`9b378c2`/`f6f7d30`，2026-09-20）已取回变量变化历史（paused 帧易失 history、上限 50）/异常断点（onException＋error 帧、resume 原样重抛）/子图内部断点（控制器透传子层、paused/debug_log 附 subgraphPath、DebugStopped 穿透）三项，前端 `c13886c`**；持久化中断-恢复/断点持久化进 Graph JSON/跨节点变量血缘/忽略异常继续/多会话锁粒度仍缓做 14 D27 | 无 |
| `src/atlas/monitoring/` | ✅ Phase 2 能力项（2026-09-15）：`records.py` RunRecord/NodeResult + MonitoringStore（deque 200 ring、threading.Lock 单锁、按图连续失败 streak、告警按 (rule,graph_id) 合并非 resolved 最新项、reset 清空并恢复默认规则），`metrics.py` 节点失败两形状推断/nearest-rank 百分位/summarize 纯函数，`alerts.py` RuleConfig/Alert/validate_rules/evaluate_rules 四内置规则纯函数；埋点在 api/main.py 两个真实运行入口（debug/回放/子图不计）；REST /api/monitoring/* 与 /api/alerts/* 挂 api/；契约 04 §5.13/06 §6.11；**docs/28 批3（2026-09-20）已进程内部分取回适配器调用级指标（tool_metric→tool_calls→tools 段，PG JSONB 迁移 008）与自定义规则 DSL（custom 段，conditions 安全求值）**；OTel/Prom/Grafana、持久化多实例、子图内/Mock 埋点、规则图级绑定、外部告警出口仍缓做 14 D28（不解除）。**M9（2026-09-18 已落码）**：新增 `business.py` extract_business（shop 终态 refunded/human_review → 自动退款/转人工/退款金额差异，金融硬条款业务结果信号源），RunRecord 加 resolved_version/business 纯超集字段，summarize 增 business 三率段（全局/per_graph/per_version，样本 0 为 null），alerts RuleId 增 rollout_gate、Alert 增可选 action（rollback 切流版本/原因/actor）；门控求值在 routing/gate.py（API 层 record_run 后调用，monitoring 不反向依赖 routing） | 无 |
| `src/atlas/api/` | ✅ W9-W10 `main.py` 已扩展（graphs 保存/读取/编译/运行 + `/run/stream` SSE 进度、`/nl/generate`、`/adapters`、Demo 店铺登录/订单与控制台页面，共享 Demo 服务单例，TestClient 10 用例）；其余 REST（operators/memories 等）待后续周次 | 无 |
| `src/atlas/tracing/` | ✅ M10 已落码收口（2026-09-18，ba9e0d2/1f21b1a/89f5645/c227f0c/2b1dbe9/0d4e3e9，08 M10 落码条，ADR T21）：`tracer.py` 与 OTel 同形最小 `Span`（traceId 32hex/spanId 16hex/parentSpanId/kind/attrs/status/internal/graphVersion）+ Tracer（root run span、contextvars 记当前 span、显式参数透传进 run_graph/compile_graph、`to_tree(include_internal)` 折叠 subgraph 内部）；纯 stdlib（uuid/time/contextvars）零新依赖。埋点：loader run/节点/tool/parallel/subgraph、coordination 任务信封 task_dispatch/task_done；SSE 三帧加 span 三元组超集、RunRecord 加 trace_id、recording.normalize 剔除 span 键，均在 api/loader；契约 04 §5.15/06 §6.15/03 `trace_span`；OTel SDK/Collector、跨服务传播（D5）、span 持久化/导出、前端瀑布 UI 缓做（D34/D11） | 无 |
| `src/atlas/routing/` | ✅ M9 已落码收口（2026-09-18，批 1-4＋收口，08 M9 落码收口条，ADR T22，U55–U59 转正式；落码与下列规划一致，细节见 handoff Recently shipped 1）：`models.py` TriggerEvent + RolloutConfig（四段规则 InternalRule/BucketRule/CanaryRule/FullRule + GateConfig 三业务指标/observeMinutes/autoRollback/minSamples，19 §2.3.3 结构化承载、v1 无 when 表达式解析器）；`router.py` resolve_version 纯函数（固定序 internal 租户 allowlist→低金额桶 payload.amount<=N→canary `sha256(graph_id:order_id)` 整型 %100<percent、桶键取 payload.order_id 缺省 id、缺失 fail-safe 落 stable→full，未配置/无 candidate 落 stable）；`store.py` RoutingStore（每租户一个挂 TenantServices、reset 清空：configure 非法 422/start 不足两发布版 409/promote 唯一放量路径仅手动/rollback 自动手动同一幂等函数/resolve 记分流计数/snapshot 投影）；`gate.py` evaluate_after_run（仅 candidate+canary+autoRollback 时按 finished_at 观察窗取 ring、样本 <minSamples 不判、run_error_rate 复用 is_healthy+业务两率、越阈 routing.rollback(actor=auto)+rollout_gate critical 告警带 action，无任何自动 promote 路径）。接线：run/run-stream body 增可选 event（带 event 经 resolve 钉发布版、无发布版 409、RunRecord.resolved_version；不带 event 草稿旧行为零回归；在途 pin 复用 M5b 帧 graph_snapshot 冻结不新增机制）；recording/gate.py 发布前批量回放门禁（POST /release-gate、publish 增 gate 参数 blocked 409 不产版本，RecordingCase 补 graph_id）；monitoring/business.py extract_business（shop 终态 refunded/human_review，expected/refunded amount，demo shop 不回金额回退 expected 不改造 demo）、RunRecord.business、summarize business 三率段（全局/per_graph/per_version）、Alert.action 纯超集；REST rollout GET/PUT/start/promote/rollback 在 api/；前端发布按钮+门禁 Modal+版本 Select+灰度 Modal+Monitoring 业务段在 frontend/；U55-U59、Router 分桶基准入 BENCHMARK、scripts/dev/m9_smoke.py 沙盘演练。真实 webhook/IM/embed 入站、多实例路由表同步/热推送、配置中心/独立网关、自动放量、NATS/CD、业务桶规则 DSL、demo shop 金额字段改造缓做（D6/D10b/D24/D26 余部/D32，不解除） | M6 版本化、M5b 中断帧、M8 告警中心 |
| `src/atlas/web/` | ✅ W3-W4 Web 适配器已完成（Playwright sync API；`location.py` 三层定位 + 选择器缓存，层 2/3 视觉组件协议注入待接模型；`adapter.py` navigate/click/type/screenshot；真实 Chromium 集成测试通过）；omni-parser/视觉 LLM 接入待模型阶段 | 无 |
| `tests/` | 各模块测试（06 测试层次） | 对应模块实现 |

## 待定项（需决策后落定）

1. ~~**技术选型锁定**~~：✅ 已收口（2026-09-13，T1-T5 全部决策，见 10 文档 §3）。`pyproject.toml` 可按 10 文档 §2 清单落码（版本号落码时解析）。
2. ✅ **`harness/` 与 `web/` 边界（2026-09-13 W3-W4 落码时决策）**：保持两包，不合并。`harness/` 为契约与注册层（`base.py` 适配器抽象接口 + ActionRequest/ActionResult/Capability/Permission 等同构 06 §6.4 Go 契约；`registry.py` 进程内注册发现/心跳，对应 04 §4.4）；`web/` 为 Playwright 浏览器适配器实现（`location.py` 三层定位、`adapter.py` WebHarnessAdapter + navigate/click/type/screenshot 工具集）。后续 API/mobile 等适配器新增 `src/atlas/<channel>/` 实现同一 `HarnessAdapter` 契约。
3. ✅ **`deployment/` 包位（2026-09-13 Phase 1 准备期决策）**：不新增 `deployment/` 包。Docker 化交付物放仓库根：`Dockerfile`（node 多阶段构建前端 → python:3.11-slim 安装包并把 dist 拷到 `/app/frontend/dist`，`ATLAS_FRONTEND_DIST` 指向后由 FastAPI 同源托管）+ `docker-compose.yml`（单服务 atlas，8000 端口）+ `.dockerignore`；不涉及 Python 部署模块代码。CI 部分（GitHub Actions 三道门：后端 pytest / 前端 vitest+build / gitleaks）已于 2026-09-14 提前启动（14 D10a，ADR T8 见 10 文档 §4）；CD 仍缓做（14 D10b，等远程部署目标）。
4. ~~**编辑后台（前端）**~~：✅ 已决策（2026-09-13，T3）：Demo 骨架含 `frontend/` 工程（React 18 + React Flow + Zustand + Ant Design），目录结构见上文树与 10 文档 §2，W1-W2 搭框架。
5. ✅ **FastAPI 入口包位（2026-09-13 落码时决策）**：新增 `src/atlas/api/` 承载 10 文档 §2 的 `/api/main.py` 与 12 文档后端 REST 路由，启动命令 `uvicorn atlas.api.main:app`（CONTRIBUTING 已如此约定）。`harness/` 保持网关/适配器契约与路由逻辑，`api/` 只做 HTTP 承载，两者不合并。
6. ✅ **graph_loader 包位与图存储（2026-09-13 W7-W8 落码时决策）**：08 §7.2 示例布局写的是 `engine/graph_loader.py`，按 09 权威骨架，DSL 契约与编译执行落在独立的 `src/atlas/graph/`（`dsl.py` + `loader.py`），`engine/` 保持 OODA 主循环运行时，两包不合并——用户流程图（trigger→decision→tool 的 DAG）与 OODA 循环是两种图形态。Graph 保存为 API 进程内字典存储（`graph-{n}` id），与 T4 进程内总线同一假设；PostgreSQL 持久化随 11 文档 S1 业务表 DDL 重启。新增 `POST /api/graphs/{id}/run`（12 文档原表只列 compile；run 为满足 08 §7.1"编译→运行"推导，已回填 12 文档）。
7. ✅ **HTTP 渠道包名与标识（2026-09-15 API 适配器落码时决策）**：channel 包取 `src/atlas/httpapi/`——`src/atlas/api/` 是 FastAPI 服务端包位（待定项 5），按「新渠道建包」约定（待定项 2）不可复用；`adapter_id="http"`、`adapter_type="api"`，工具名 `http/request`。v1 为通用 HTTP 单能力（契约 04 §4.6），OpenAPI 自动导入为后续形态。
8. ✅ **数据/消息渠道包与连接隔离（2026-09-15 DB/消息适配器落码时决策）**：按「新渠道建包」新增 `src/atlas/database/`（adapter_id=`database`、type=`database`，工具 database/query|execute）与 `src/atlas/message/`（adapter_id=`message`、type=`message`，工具 message/send）。数据适配器**自建 SQLAlchemy engine 工厂**（scheme 白名单 postgresql+psycopg/sqlite），连接读适配器专用 env `ATLAS_DATABASE_URL`，与平台自身 `DATABASE_URL`/`memory/database.py` 工厂物理隔离——防止图操作误伤平台库，也不放松平台工厂的 postgresql 锁；未配置时回退内置 SQLite StaticPool 内存 demo（seed 订单，reset 仅重建 demo，显式外部库不被触碰）。消息 v1 为进程内 sink 零真实投递。均无新依赖（SQLAlchemy 直依、sqlite3 stdlib；ADR T10 见 10 文档 §4）。
9. ✅ **流程模板库包位与形态（2026-09-15 模板库落码时决策）**：新增 `src/atlas/template/`（`graphs.py` 模板 Graph JSON + `catalog.py` TemplateMeta 目录）承载 08 Phase 2「模板库（5-10 个预置模板）」。v1 为**随代码版本发布的只读内置目录**（Python 常量，5 个模板，无 DB 表、无用户 CRUD、reset 不影响），REST 仅 `GET /api/templates` 与 `GET /api/templates/{id}` 两个只读投影挂在 api/；「从模板新建」复用编辑器 `loadGraph` 整画布替换（不做 id 重映射），加载后保存为普通图与模板无关联。退款 golden 图从 `llm/nl_generate.py` 移入 `template/graphs.py` 作单一事实源（依赖方向 llm→template）。用户自建/市场/远程/版本钉版缓做 14 D25（ADR T11 见 10 文档 §4）。
10. ✅ **操作录制包位与形态（2026-09-15 录制回放落码时决策）**：新增 `src/atlas/recording/`（`cases.py` RecordingCase + RecordingStore、`replay.py` 归一化/预置/比对纯函数）承载 08 Phase 2「操作录制」。v1 范围为**运行录制 + 回放比对**（04 §5.11），不是 04 §4.5 方式 2 的浏览器操作录制（仍缓做 D22）。录制复用唯一运行链路：客户端在 /run/stream 收集 node_end 事件后 POST 入库，服务端按 graph_id 存**图快照**（非活引用），不另设代跑端点；回放对快照走标准 run_graph，人工审批决策从 baseline 抽为 inputs.approvals 预置以保证确定性；逐节点深等前归一化易变值（token/sent_at/消息 uuid/HTTP date）。用例进程内存储、reset 不清除（同 feedback，重启清空随 11 S1）。批量回放门禁/报告/导出/subgraph 快照内联/定时 CI 已分别随 M9、D26 报告 v1 与 2026-09-19 收尾批落码；影子模式/Mock 工具响应/用例编辑参数化/PG 持久化多租户/跨图看板仍缓做 14 D26（ADR T12 见 10 文档 §4）。
11. ✅ **单步调试包位与形态（2026-09-15 调试落码时决策）**：新增 `src/atlas/debug/`（`sessions.py` DebugSession/DebuggerBroker、`controller.py` DebugController）承载 08 Phase 2「单步调试与断点」。暂停判定钩在统一执行器 `_make_executor.execute` 的 node_start 之后/节点分派之前（9 类节点零分支），不引入 LangGraph interrupt/checkpointer——进程内 token+`threading.Event` 会话复用 ApprovalBroker 已验证模式但**另建不合并**（无超时/路由，携带 step/continue/stop 状态机）。断点为**会话级**：存前端编辑器状态、随 /run/stream 的 `debug.breakpoints` 传入，不进 Graph JSON（node_schema 预留 breakpoint 位不启用），刷新即失；暂停帧 globals/outputs 深拷贝**只读**，改写续跑缓做。启动即 step 模式；continue 后仅断点停；条件断点复用 §5.1 白名单引擎，运行时异常 fail-safe 不命中；parallel 经会话锁串行、`__join__` 网关不暂停、subgraph 内部自 docs/28 批 2（`f6f7d30`）起可逐帧暂停（子层帧附 subgraphPath）；stop 仅在暂停点生效（DebugStopped 冒泡→stopped 帧）；reset 按 stop 释放全部会话。持久化中断-恢复随 14 D19/D20（11 S1）；**2026-09-20 B 包（f9a1301）已进程内取回急停（协作式、节点边界、不强杀阻塞）、hitCount/logpoint、resume globals 改写续跑三项**，变化历史易失形态/异常断点/子图内部断点已于 docs/28 批 2 取回（`038dcaa`/`9b378c2`/`f6f7d30`，U150–U165），断点持久化进 Graph JSON/跨节点变量血缘/忽略异常继续/阻塞中点强杀/多实例仍缓做 14 D27（ADR T13 见 10 文档 §4）。
12. ✅ **基础监控告警包位与形态（2026-09-15 监控落码时决策）**：新增 `src/atlas/monitoring/`（`records.py` RunRecord/MonitoringStore、`metrics.py` 聚合纯函数、`alerts.py` 规则纯函数）承载 08 Phase 2「基础监控告警」（04 §9.3 三条中的「实时监控运行状态，异常自动告警」）。**埋点在 API 层两个真实运行入口**（sync /run 与非 debug 的 /run/stream worker），不进 loader/节点执行路径——零节点类型分支，录制 replay 直调、subgraph 重入、debug 运行天然排除。节点成败由终态 outputs 推断（`status=="failed"` 折叠形状 / `result.status=="FAILED"` ActionResult），未捕获异常才是运行级 error。存储为进程内 `deque(maxlen=200)` ring + 单锁同步评估（写运行→按图 streak→四规则→告警新建/合并），无后台线程、无新依赖；四规则内置（run_error/node_failed warning|critical、consecutive_failures、failure_rate 滑动窗口）阈值经 GET/PUT /api/monitoring/rules 可调；告警站内中心（/api/alerts，acknowledge/resolve，未 resolved 按 (rule,graph_id) 合并 count）；reset 清空运行/告警并恢复默认规则，重启即失。正式 OTel/Prometheus/Grafana 栈随部署（14 D11），持久化/多实例随 11 S1，适配器调用级指标/规则表达式 DSL/外部出口缓做 14 D28（ADR T14 见 10 文档 §4）。
13. ✅ **多租户权限包位与形态（2026-09-16 D7 落码时决策）**：新增 `src/atlas/iam/`（`principals.py` 角色/种子账号/认证、`sessions.py` sess-token 会话、`registry.py` TenantServices 按租户注册表、`deps.py` Bearer/角色 FastAPI 依赖）承载 08 Phase 2 末项「多租户权限」（14 D7 触发条件沙盘假定满足后取回，契约 04 §5.14）。包名取 iam 而非 auth——`api/` 的认证是入站平台会话，http/database 等适配器带的是出向目标系统凭证，避免混淆。v1 为**进程内、零新依赖**：种子两租户四账号（t1 admin/operator/viewer、t2 admin，明文仅 Demo）、登录换 `sess-`+uuid 不透明 token（SessionStore 单锁，重启即失，与审批/调试 token 同模式）；viewer/operator/admin 三角色**端点级白名单**（无节点级门，随 14 D20）；图/录制/反馈/监控/审批/调试/消息**每租户独立 store 实例**（store 类零改动，TenantRegistry 惰性装配，计数各租户从 1 起；tenant 不进资源 JSON），模板/适配器/demo 店铺与 mock/出向连接为全局基础设施；跨租户对象 404、角色不足 403、未登录 401；reset 收窄为 admin 且只清本租户（录制/反馈不动）。无平台超管（平级两租户完全隔离）。**持久化会话已随 docs/30 落 PG；口令哈希/账号生命周期/会话绝对 TTL/登录节流已随 docs/31（ADR T25，2026-09-21）落码，权威 04 §5.17**；SSO/JWT/开放注册/账号审计/配额随 11 S1 + 14 D22/Phase 3，共享/市场随 14 D3/D25（ADR T15/T25 见 10 文档 §4）。
14. ✅ **前端 L1 校验器形态（2026-09-16 ADR T16 收口；M2 已于 2026-09-16 同日落码，234a95f→fbd9f77）**：不引 AJV，前端 JSON Schema L1 校验定为**手写最小子集解释器**——M1 已在 `frontend/src/lib/schemas/validateConfig.ts`（154 行）随 dev 双跑落地（type/required/enum/const/range/length/pattern/minItems/maxItems/additionalProperties/items 浅递归 + oneOf 候选分支语义），经 `dualRun.ts` 对九节点手写规则路径级零分歧（vitest 189/14 文件）；九份 schema 实测仅 1 处 oneOf、零 if-then/dependencies。M2（已落码，契约见 08 M2 立项条+落码条、03 `diagnostic`、04 §6.5）把该解释器扶正迁入 `lib/validation/l1.ts`（点号 path→RFC6901 pointer、rule→FIELD_* code、删 dev 双跑脚手架与已覆盖手写规则），与升级为 Diagnostic 的 L2 统一到 `lib/validation/diagnostics.ts`、由 `validateGraph.ts` 聚合；跨字段/语法规则继续手写不进 L1。package.json 不加依赖；M3 表单迁移若出现白名单外第二类结构且解释器需显著扩张、或候选分支策略无法维持文案/路径语义，携实测案例重开 T16 评 AJV（ADR T16 见 10 文档 §4，方案见 20 §2.3）。
15. ✅ **前端表单引擎形态（2026-09-16 ADR T17 收口；M3 同日立项、**2026-09-17 落码收口**）**：不引 rjsf/Formily，Schema 驱动表单定为**自研最小 WidgetRegistry + 递归 FormRenderer**——新前端包 `frontend/src/lib/forms/`（WidgetRegistry 注册名→组件、FormRenderer object 递归/array 增删行、resolveWidget 选择序 x-widget→类型结构默认→JSON 文本降级、TemplateTextArea 变量补全控件），内置 text/number/select/textarea/switch/json/expression/variable-input 八件全部由既有 AntD 承载；SchemaRegistry 增第二来源（工具 input_schema，键 `<adapter>/<tool>`，取自 /api/adapters 发现快照）；config.params 仍为 JSON 字符串（Graph v1 不变），表单只编辑解析后对象并 stringify 回写，整串裸插值/oneOf/无类型/空 schema/未注册工具一律降级旧 TextArea。M3 只迁 ToolCallConfig 一个手写组件，其余七个按 M4+ 渐进迁移（每迁一个一原子提交）。package.json 不加依赖；M4 UISchema/条件显示或 M8 卡片第三类 schema 来源致渲染层显著扩张时，携实测案例重开 T17 评 rjsf/Formily（ADR T17 见 10 §4，方案见 20 §2.4，契约见 04 §4.10、03 `form_renderer`）。**落码（2026-09-17，2de5053→6d4a863）**：前端 vitest 243（24 文件）、oxlint 0/0、build 过门，后端零改动；工具 params 表单进入 ToolCallConfig，旧 JSON 文本框路径降级保留。**M4 已于 2026-09-17 立项（契约见 08 M4 立项条、20 §2.5）**：lib/forms 增 `uiSchema.ts`（ui:group）+ CondResolver（条件显隐），lib/validation 增前端 L3（`l3.ts`，不可达 BFS/环 DFS 同构 dsl.py），新增 Problems 面板组件；复杂度 dump 后 T17 复查不重开（UISchema 真实需求仅分组 1 处 + 条件显隐 1 处）；本立项批零代码，落码按三批原子序。**批 1 已于 2026-09-17 落码收口（49cb692→30d5009，9 提交）**：uiSchema 落 groups/hiddenWhen/hideFields + 字段文案层、内置控件 8→9（+radio）、新增 target-select 节点业务控件层（nodeWidgets/nodeRegistry）与 NodeConfigForm 共享包装，HumanApproval/Trigger/Loop 三节点迁移（旧内联组件删除、等价测试 + 真实浏览器冒烟，human/loop 截图入 docs/assets），Wait 经落码判定保留手写（const + 禁用 event 选项 + D19 Tooltip 无法用 schema 表达），editorStore dirty 失效范围骨架（lib/validation/dirty.ts）；三道门 vitest 291/27、oxlint 0/0、build 过、后端零改动。**批 2 已于 2026-09-17 落码收口（0dc9b7f/1468e18/2ad4177/06b62fc/ac29737/aaf50bf，6 提交）**：l3.ts 不可达/环同构 dsl.py + 11 夹具跨运行时对拍、engine/useValidationEngine 分层调度消费 dirty + structureSignature 记忆化、独立 validationStore、Problems 面板点击定位、Parallel/Subgraph 迁移（新业务件 saved-graph-select、嵌套通配文案键、数组 min/max 门控）、后端一处 flaky 测试修复，vitest 336/30、后端 431/8，截图 m4-parallel/m4-subgraph-form-smoke.png。**批 3 已于 2026-09-17 落码收口（4b15d95/875922b/f496062/79317e3/471b930，5 提交）**：reverseDeps 反向索引（删节点清 target 索引化、L2 收窄引用方）、quickFix v1 删除悬空引用（Problems 内联按钮）、200/500 节点基准（`frontend/bench/`，RUN_BENCH=1 门控，数字入 BENCHMARK.md：稳态单节点 L2 增量 p95≈10ms@500）、Condition 迁移（最后一个手写节点 Config；空串 label 隐藏数组标题修复），vitest 359/32（另 2 基准场景门控跳过）、后端 431/8 零回归，冒烟脚本 `scripts/dev/m4_batch3_smoke.py`、截图 m4-condition/m4-quickfix-smoke.png。**M4 三批全部收口，九节点 Config 八迁一留（Wait）**。
