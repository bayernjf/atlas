# Atlas 工程骨架与目录结构（AI 落码规范）

> **来源**：由 docs 01-08 的模块划分推导，本文件是**工程结构规范**——AI 落码时按此目录组织代码，**不是真实代码目录**。
> **状态**：骨架结构已定，各文件内容待填（待填项见下）。
> **配套**：模块职责与实现细节见对应文档（下表已标注）。

## 目录结构

```
docs/项目1/atlas/                       # 工程根（W1 骨架已创建）
├── README.md                           # 待填：工程说明、启动方式、Demo 范围指引（见 01）
├── pyproject.toml                      # Python 依赖与打包配置（依赖见 10 文档 §2）
├── Dockerfile                          # Phase 1 交付镜像：前端多阶段构建 + FastAPI 同源托管（待定项 3）
├── docker-compose.yml                  # 一键启动（单服务，8000 端口）
├── .dockerignore
├── .gitignore                          # 忽略规则
├── db/migrations/                      # SQL 迁移（规范见 MIGRATION_CONVENTION.md）
│   └── 001_enable_pgvector.sql         # 启用 pgvector 扩展（已在开发库执行）
├── scripts/
│   └── benchmark.py                    # 性能基准脚本（stdlib，输出 BENCHMARK.md Results 行；14 D9，2026-09-14）
├── frontend/                           # 编辑后台前端（Vite + React 19 + TS；ADR T3，单测 vitest ADR T6）
│   ├── package.json / pnpm-lock.yaml
│   ├── vite.config.ts                  # dev 端口 5174，/api 代理到 8000
│   └── src/
│       ├── lib/                        # 纯逻辑（可单测）：variables（{{路径}}）/ nodeCatalog（目录+校验）/ graphSerializer（Graph JSON v1）
│       ├── store/editorStore.ts        # Zustand：节点/边/变量/选中态（+ __tests__）
│       ├── components/{canvas（FlowCanvas + AtlasNode 自定义节点）,nodePanel,propertyPanel,variablePanel,debugConsole}/
│       ├── pages/{Dashboard,Editor}.tsx
│       ├── theme/tokens.ts             # 设计 Token 单一事实源（17 文档 §3；✅ 2026-09-13 已落码，setup.ts 注入 --atlas-* 变量）
│       └── locales/{zh-CN,en-US}/      # i18n 文案 namespace（17 文档 §2；触发式落码，见 14 D12）
├── src/atlas/
│   ├── __init__.py
│   ├── engine/                         # Loop 主循环、决策节点
│   │   ├── __init__.py
│   │   ├── state.py                    # LoopState（06 §6.1 / 12 §1.1）
│   │   ├── nodes.py                    # OODA 五节点与路由（W1 占位）
│   │   └── loop.py                     # StateGraph 装配/run_loop
│   ├── harness/                        # Harness 契约层：适配器基类 + 注册发现
│   │   ├── __init__.py
│   │   ├── base.py                     # HarnessAdapter ABC + Action*/Capability/Permission（06 §6.4）
│   │   └── registry.py                 # 进程内注册/发现/心跳（04 §4.4，T4 进程内）
│   ├── graph/                          # Graph DSL 与编译执行
│   │   ├── __init__.py
│   │   ├── dsl.py                      # GraphDSL/NodeDSL（04 §5.2，version 1）+ 静态校验
│   │   └── loader.py                   # DSL→LangGraph 编译/run_graph + {{路径}} 插值；W9-W10 注入决策客户端/适配器/事件回调
│   ├── llm/                            # 模型接入层（T1 LiteLLM；W9-W10）
│   │   ├── __init__.py
│   │   ├── decision.py                 # 退款 DecisionClient：LiteLLM 实现 + 规则兜底（06 §9.2 黄金规则）
│   │   └── nl_generate.py              # 自然语言 → Graph 草稿（LLM 优先，退款模板兜底）
│   ├── shop/                           # 电商渠道包：HarnessAdapter 实现（待定项 2 新渠道建包约定；W9-W10）
│   │   ├── __init__.py
│   │   ├── service.py                  # DemoShopService：种子退款单 + 退款/转人工状态流转
│   │   └── adapter.py                  # ShopHarnessAdapter：login/list_pending_refunds/execute_refund/request_human_approval/process_refund
│   ├── nodes/                          # 节点/工具/逻辑/变量组件
│   │   └── __init__.py
│   ├── memory/                         # 记忆系统
│   │   ├── __init__.py
│   │   ├── settings.py                 # 环境变量单一读取点（DATABASE_URL，fail-closed）
│   │   └── database.py                 # PostgreSQL 引擎/会话工厂 + ping + pgvector 版本探针
│   ├── skills/                         # 技能（Skill）
│   │   └── __init__.py
│   ├── collaboration/                  # 智能体协同
│   │   └── __init__.py
│   ├── api/                            # FastAPI Demo 后端入口（10 §2 /api/main.py；T5 网关接口的 Demo 宿主）
│   │   ├── __init__.py
│   │   └── main.py                     # /api/graphs 保存/读取/编译/运行 + SSE 运行流、NL 生成、适配器发现、Demo 店铺控制台（W9-W10）
│   └── web/                            # Web 操作适配器（Playwright）
│       ├── __init__.py
│       ├── location.py                 # 三层定位：选择器→视觉语义→全图推理（06 §6.5）
│       └── adapter.py                  # WebHarnessAdapter：navigate/click/type/screenshot
└── tests/                              # 测试（层次见 06）
    ├── __init__.py
    ├── test_engine_loop.py             # W1 OODA 循环冒烟测试（后续 test_*.py 按模块补齐）
    ├── test_harness_contract.py        # 注册发现/心跳/权限审计（04 §4.4、I8）
    ├── test_web_location.py            # 三层定位编排（假页面，I1-I5）
    ├── test_web_browser_integration.py # 真实 Chromium 集成（I1/I2/I5，integration 标记）
    ├── test_graph_dsl.py               # Graph DSL 解析与静态校验（W7-W8）
    ├── test_graph_loader.py            # DSL→LangGraph 编译/运行/插值（W7-W8）
    ├── test_api_graphs.py              # graphs 保存/读取/编译/运行 REST（TestClient，W7-W8）
    ├── test_decision_client.py         # 退款决策黄金用例 + LiteLLM 解析/fail-safe（W9-W10）
    ├── test_shop_adapter.py            # DemoShopService 状态流转 + ShopHarnessAdapter 能力/权限（W9-W10）
    ├── test_nl_generate.py             # NL 退款草稿生成/未识别意图（W9-W10）
    ├── test_refund_e2e.py              # DSL 编译运行至 shop 适配器的退款端到端黄金用例（W9-W10）
    ├── test_api_demo.py                # SSE/NL/adapters/Demo 店铺 REST（TestClient，W9-W10）
    ├── test_shop_platform_integration.py # 自动登录+抓取退款单集成（integration 标记，W9-W10）
    └── test_database_integration.py    # PostgreSQL/pgvector 集成测试（标记 integration，默认跳过）
```

## 模块 → 文档映射

| 包路径 | 职责 | 依据文档 |
|---|---|---|
| `src/atlas/engine/` | Loop 主循环：感知→规划→执行→反馈；决策节点 | 06 运行时与质量保障 |
| `src/atlas/harness/` | Harness Gateway：平台抽象与能力集成总线 | 06 运行时与质量保障；02 技术架构 |
| `src/atlas/graph/` | Graph：可执行因果有向图（LangGraph 层） | 02 技术架构；04 组件设计 |
| `src/atlas/llm/` | 模型接入层：LiteLLM 决策客户端（规则兜底）与 NL 草稿生成 | 10 §3 T1；06 §9.2；08 §7.3 |
| `src/atlas/shop/` | 电商渠道包：DemoShopService + ShopHarnessAdapter（退款业务能力） | 06 §6.4/§9.2；08 §7.1 |
| `src/atlas/nodes/` | 节点/工具/逻辑/变量组件（Schema 见 03） | 04 组件设计-编辑后台 |
| `src/atlas/memory/` | 记忆系统（memory_config 见 03/05） | 05 五项核心；06 运行时 |
| `src/atlas/skills/` | 技能（skill_schema 见 03/05） | 05 五项核心 |
| `src/atlas/collaboration/` | 智能体协同（collaboration_message 见 03/05） | 05 五项核心 |
| `src/atlas/api/` | FastAPI Demo 后端入口与 REST 路由（12 文档后端 REST）；同时是 T5 Harness 网关接口的 Demo 宿主 | 10 §2；12 API 清单 |
| `src/atlas/web/` | Web 操作适配器（Playwright 实现） | 06 运行时 |
| `tests/` | 单元/集成/端到端测试（层次见 06） | 06 运行时与质量保障 |

## 待填项清单（AI 落码时的任务入口）

| 文件 | 待填内容 | 前置依赖 |
|---|---|---|
| `pyproject.toml` | ✅ 已完成（2026-09-13）：依赖清单、打包配置 | — |
| `src/atlas/engine/` | ✅ W1 最小 OODA 骨架已完成（state/nodes/loop + 3 冒烟用例）；LLM/Harness 接入待后续周次 | — |
| `frontend/` | ✅ W9-W10 已支持退款 Demo（退款单选择、SSE 流式运行与节点实时状态、NL 草稿载入、结果弹窗）；设计 Token 层已落（`theme/tokens.ts`+`setup.ts`，17 文档 §3） | — |
| `src/atlas/harness/` | ✅ W3-W4 契约层已完成（`base.py` 适配器 ABC + 权限/审计模板方法，`registry.py` 注册/发现/心跳，13 个单元/集成用例） | 无 |
| `src/atlas/graph/` | ✅ W9-W10 已接真实执行（`dsl.py` GraphDSL 契约 + 静态校验；`loader.py` DSL→LangGraph 编译/运行/插值，依赖注入决策客户端、适配器注册表与事件回调，webhook 载荷经 inputs 进入 trigger 节点） | 无 |
| `src/atlas/llm/` | ✅ W9-W10 已完成（`decision.py` LiteLLM 决策 + 规则兜底 + fail-safe，`nl_generate.py` LLM/模板双通道；黄金用例单测） | 无 |
| `src/atlas/shop/` | ✅ W9-W10 已完成（`service.py` 种子退款单与状态流转，`adapter.py` 五能力含 financial 权限；单元 + 平台集成测试） | 无 |
| `src/atlas/nodes/` | 节点 Schema→类实现（03/04 有 Schema） | 无 |
| `src/atlas/memory/` | ✅ W1 连接层已完成（`settings.py` 环境变量读取 + `database.py` 引擎/会话/ping/pgvector 探针，迁移 001 已启用扩展，集成测试 3 用例全绿）；Redis 短期记忆、记忆表 DDL 与存取路径待后续周次（11 文档 S1） | 无 |
| `src/atlas/skills/` | 技能注册与执行（03/05 有 skill_schema） | 无 |
| `src/atlas/collaboration/` | 协同消息协议（03/05 有 collaboration_message） | 无 |
| `src/atlas/api/` | ✅ W9-W10 `main.py` 已扩展（graphs 保存/读取/编译/运行 + `/run/stream` SSE 进度、`/nl/generate`、`/adapters`、Demo 店铺登录/订单与控制台页面，共享 Demo 服务单例，TestClient 10 用例）；其余 REST（operators/memories 等）待后续周次 | 无 |
| `src/atlas/web/` | ✅ W3-W4 Web 适配器已完成（Playwright sync API；`location.py` 三层定位 + 选择器缓存，层 2/3 视觉组件协议注入待接模型；`adapter.py` navigate/click/type/screenshot；真实 Chromium 集成测试通过）；omni-parser/视觉 LLM 接入待模型阶段 | 无 |
| `tests/` | 各模块测试（06 测试层次） | 对应模块实现 |

## 待定项（需决策后落定）

1. ~~**技术选型锁定**~~：✅ 已收口（2026-09-13，T1-T5 全部决策，见 10 文档 §3）。`pyproject.toml` 可按 10 文档 §2 清单落码（版本号落码时解析）。
2. ✅ **`harness/` 与 `web/` 边界（2026-09-13 W3-W4 落码时决策）**：保持两包，不合并。`harness/` 为契约与注册层（`base.py` 适配器抽象接口 + ActionRequest/ActionResult/Capability/Permission 等同构 06 §6.4 Go 契约；`registry.py` 进程内注册发现/心跳，对应 04 §4.4）；`web/` 为 Playwright 浏览器适配器实现（`location.py` 三层定位、`adapter.py` WebHarnessAdapter + navigate/click/type/screenshot 工具集）。后续 API/mobile 等适配器新增 `src/atlas/<channel>/` 实现同一 `HarnessAdapter` 契约。
3. ✅ **`deployment/` 包位（2026-09-13 Phase 1 准备期决策）**：不新增 `deployment/` 包。Docker 化交付物放仓库根：`Dockerfile`（node 多阶段构建前端 → python:3.11-slim 安装包并把 dist 拷到 `/app/frontend/dist`，`ATLAS_FRONTEND_DIST` 指向后由 FastAPI 同源托管）+ `docker-compose.yml`（单服务 atlas，8000 端口）+ `.dockerignore`；不涉及 Python 部署模块代码。CI 部分（GitHub Actions 三道门：后端 pytest / 前端 vitest+build / gitleaks）已于 2026-09-14 提前启动（14 D10a，ADR T8 见 10 文档 §4）；CD 仍缓做（14 D10b，等远程部署目标）。
4. ~~**编辑后台（前端）**~~：✅ 已决策（2026-09-13，T3）：Demo 骨架含 `frontend/` 工程（React 18 + React Flow + Zustand + Ant Design），目录结构见上文树与 10 文档 §2，W1-W2 搭框架。
5. ✅ **FastAPI 入口包位（2026-09-13 落码时决策）**：新增 `src/atlas/api/` 承载 10 文档 §2 的 `/api/main.py` 与 12 文档后端 REST 路由，启动命令 `uvicorn atlas.api.main:app`（CONTRIBUTING 已如此约定）。`harness/` 保持网关/适配器契约与路由逻辑，`api/` 只做 HTTP 承载，两者不合并。
6. ✅ **graph_loader 包位与图存储（2026-09-13 W7-W8 落码时决策）**：08 §7.2 示例布局写的是 `engine/graph_loader.py`，按 09 权威骨架，DSL 契约与编译执行落在独立的 `src/atlas/graph/`（`dsl.py` + `loader.py`），`engine/` 保持 OODA 主循环运行时，两包不合并——用户流程图（trigger→decision→tool 的 DAG）与 OODA 循环是两种图形态。Graph 保存为 API 进程内字典存储（`graph-{n}` id），与 T4 进程内总线同一假设；PostgreSQL 持久化随 11 文档 S1 业务表 DDL 重启。新增 `POST /api/graphs/{id}/run`（12 文档原表只列 compile；run 为满足 08 §7.1"编译→运行"推导，已回填 12 文档）。
