# Atlas 工程骨架与目录结构（AI 落码规范）

> **来源**：由 docs 01-08 的模块划分推导，本文件是**工程结构规范**——AI 落码时按此目录组织代码，**不是真实代码目录**。
> **状态**：骨架结构已定，各文件内容待填（待填项见下）。
> **配套**：模块职责与实现细节见对应文档（下表已标注）。

## 目录结构

```
docs/项目1/atlas/                       # 工程根（待创建，当前仅为文档定义）
├── README.md                           # 待填：工程说明、启动方式、Demo 范围指引（见 01）
├── pyproject.toml                      # 待填：依赖与打包配置（依赖清单待选型锁定）
├── .gitignore                          # 待填：忽略规则
├── src/atlas/
│   ├── __init__.py
│   ├── engine/                         # Loop 主循环、决策节点
│   │   └── __init__.py
│   ├── harness/                        # Harness Gateway、能力接入
│   │   └── __init__.py
│   ├── graph/                          # Graph 定义与执行
│   │   └── __init__.py
│   ├── nodes/                          # 节点/工具/逻辑/变量组件
│   │   └── __init__.py
│   ├── memory/                         # 记忆系统
│   │   └── __init__.py
│   ├── skills/                         # 技能（Skill）
│   │   └── __init__.py
│   ├── collaboration/                  # 智能体协同
│   │   └── __init__.py
│   └── web/                            # Web 操作适配器（Playwright）
│       └── __init__.py
└── tests/                              # 测试（层次见 06）
    ├── __init__.py
    └── test_*.py                       # 待填：各模块测试
```

## 模块 → 文档映射

| 包路径 | 职责 | 依据文档 |
|---|---|---|
| `src/atlas/engine/` | Loop 主循环：感知→规划→执行→反馈；决策节点 | 06 运行时与质量保障 |
| `src/atlas/harness/` | Harness Gateway：平台抽象与能力集成总线 | 06 运行时与质量保障；02 技术架构 |
| `src/atlas/graph/` | Graph：可执行因果有向图（LangGraph 层） | 02 技术架构；04 组件设计 |
| `src/atlas/nodes/` | 节点/工具/逻辑/变量组件（Schema 见 03） | 04 组件设计-编辑后台 |
| `src/atlas/memory/` | 记忆系统（memory_config 见 03/05） | 05 五项核心；06 运行时 |
| `src/atlas/skills/` | 技能（skill_schema 见 03/05） | 05 五项核心 |
| `src/atlas/collaboration/` | 智能体协同（collaboration_message 见 03/05） | 05 五项核心 |
| `src/atlas/web/` | Web 操作适配器（Playwright 实现） | 06 运行时 |
| `tests/` | 单元/集成/端到端测试（层次见 06） | 06 运行时与质量保障 |

## 待填项清单（AI 落码时的任务入口）

| 文件 | 待填内容 | 前置依赖 |
|---|---|---|
| `pyproject.toml` | 依赖清单、打包配置 | **技术选型锁定**（02 候选→定案，见下方待定项） |
| `src/atlas/engine/` | Loop 主循环骨架（06 有代码示例） | 无 |
| `src/atlas/harness/` | Harness Gateway 接口（06 有代码示例） | 无 |
| `src/atlas/graph/` | Graph 定义/执行器 | 无 |
| `src/atlas/nodes/` | 节点 Schema→类实现（03/04 有 Schema） | 无 |
| `src/atlas/memory/` | 记忆存取实现（03/05 有 memory_config） | 无 |
| `src/atlas/skills/` | 技能注册与执行（03/05 有 skill_schema） | 无 |
| `src/atlas/collaboration/` | 协同消息协议（03/05 有 collaboration_message） | 无 |
| `src/atlas/web/` | Web 适配器（06 有 Playwright 示例） | 无 |
| `tests/` | 各模块测试（06 测试层次） | 对应模块实现 |

## 待定项（需决策后落定）

1. **技术选型锁定**：02 文档给出的是候选对比（LangGraph/Cadence/Temporal、Playwright/影刀 等），未逐项拍板。`pyproject.toml` 的依赖清单需先完成选型决策记录。
2. **`harness/` 与 `web/` 边界**：06 中 Web 适配器属于 Harness 层，骨架暂拆两包；可合并为一个（如 `harness/adapters/web.py`）。
3. **是否新增 `deployment/`**：05 部署方式更偏运维配置，暂未设包；若需代码化部署（如 Dockerfile/配置模板）可再加。
4. **编辑后台（前端）**：04/05 大量篇幅描述拖拽编辑器，骨架未含前端目录；若 Demo 范围含可视化画布（08 验收标准第 5 条），需再定前端工程结构（或暂用脚本/CLI 替代）。
