# Atlas 测试用例清单

> **来源**：06 运行时"第九部分：测试与质量保障"（9.1 测试层次 / 9.2 评估 Harness / 9.3 上线前强制测试）+ 08 任务迭代计划"7.3 Demo 验收标准"。本文为**测试用例清单**：把文档定义的测试层次、强制测试和验收标准落成可执行的用例清单。
> **AI 使用提示**：写测试时按本清单建用例；用例必须覆盖 9.3 强制测试四道门；验收标准 7 条对应端到端用例。

## 1. 测试层次（原文 06 9.1）

| 层次 | 工具 | 内容 | 用例归属（本文节） |
|---|---|---|---|
| 单元测试 | Pytest | 节点逻辑、变量系统、表达式引擎 | §2 |
| 集成测试 | Playwright | Harness 操作正确性 | §3 |
| 回放测试 | 录制回放框架 | 运营体行为一致性 | §4 |
| 端到端测试 | 沙盒环境 | 完整业务场景 | §5 |
| AI 评估 | 评估 Harness | 决策质量、任务成功率 | §6 |

## 2. 单元测试用例（Pytest）

| # | 被测对象 | 用例 | 预期 | 关联 |
|---|---|---|---|---|
| U1 | 节点逻辑 | 触发器节点定时表达式解析 | Cron 表达式正确触发 | 04 3.1 |
| U2 | 节点逻辑 | AI 决策节点输出不合法 | 结构化输出校验失败，走重试/失败分支 | 03 node.retry |
| U3 | 变量系统 | 变量引用 `{{order.items[0].price}}` | 正确解析多级路径 | 04 6.3（附录 A1） |
| U4 | 变量系统 | 上游节点输出不存在 | 高亮/报错（依赖完整性检查） | 04 补充项 1 |
| U5 | 表达式引擎 | 条件表达式 `amount > 100 && status == 'paid'`（首版白名单：比较/逻辑/括号/字面量/`{{路径}}`，无算术函数；`atlas.graph.conditions`） | 布尔结果正确，&& 短路 | 04 §5.1 首版语法白名单 |
| U6 | 表达式引擎 | 表达式语法错误 / 字面量类型错误（`'a' > 1`） | 校验期聚合中文报错；含变量的运行时类型错误/缺失变量 fail-safe 走 defaultTarget 并写 expression_errors，不崩溃 | 04 §5.1/§5.2 |
| U7 | 流程校验 | 循环依赖检测 | 保存时检测并拒绝；**例外**：loop 节点「循环体回到自身」的白名单回边合法（移除白名单回边后再做环检测，见 04 §5.3），其余环仍拒绝 | 04 补充项 1；04 §5.3 |
| U8 | 流程校验 | 不可达节点检测（从无入边节点 BFS） | 保存时警告/拒绝 | 04 补充项 1 |
| U9 | Schema 校验 | 节点缺少必填字段 | 实时校验错误提示 | 04 补充项 1；9.3-1 |
| U10 | 重试策略 | 工具调用超时 | 按 backoff 重试，达 max_retries 后 on_error | 03 node.retry |
| U11 | condition 路由 | trigger→condition→两个 tool_call：大额命中分支 / 小额走默认 / 缺金额走默认 | 仅目标单侧节点产出存在；outputs 含 branch/target/evaluation；trace 有 `condition-x: branch=… → target` | 04 §5.2；06 §6.1 |
| U12 | condition 校验 | branches 为空、label 重复/为空、target 不存在或自环、target 重复或等于 defaultTarget、出边未被 branches∪default 覆盖、condition 直连 END、defaultTarget 缺失 | DSL 校验一次性聚合中文报错 | 04 §5.2 契约 |
| U13 | loop 路由 | trigger→loop（`{{loop-x.index}} < 3`）→循环体节点回边 loop→exit 节点：循环体恰好执行 3 次后 exitReason=condition_false 退出；产出含 iterations/index/target；trace 有 continue/exit 行；整体 completed | 04 §5.3；06 §6.1 |
| U14 | loop fail-safe | 恒真表达式：执行 maxIterations 轮后 exitReason=max_iterations 走 exitTarget（不触发 recursion_limit、仍 completed）；坏表达式/缺失变量：首轮即 exitReason=expression_error 退出且 expression_errors 非空 | 04 §5.3 fail-safe |
| U15 | loop 校验 | continueExpression 空/语法错、maxIterations 越界非整数、body/exit 缺失或相同或自环、出边数≠2 或直连 END、循环体无回边、循环体泄漏到 exitTarget、体内含 trigger/其他 loop（嵌套）、不经 loop 的非法环 | DSL 校验一次性聚合中文报错 | 04 §5.3 契约；U7 |
| U16 | parallel 扇出/汇聚 | trigger→parallel（all_success，两支不等长：一支单节点、一支含 condition 或自包含 loop）→joinTarget：两支入口均执行、joinTarget 的 node_start 恰好一次、outputs[parallel-id] 含 result 两个入口键且 status=success、trace 有 `fork N branches` 与 `joined (all_success) success` 行；整体 completed | 04 §5.4；06 §6.1 |
| U17 | parallel fail-safe | all_success 下一支工具未注册适配器/SIMULATED FAILED（路径上任一 `result.status=FAILED`）：parallel 聚合 status=failed、branches 中该支 status=failed/error 非空，但 joinTarget 仍执行恰好一次、run 仍 completed；all_completed 下同图同口径（不因失败改汇聚行为）；下游 condition 可按 `{{parallel-x.status}}` 分支 | 04 §5.4 fail-safe |
| U18 | parallel 校验 | joinStrategy 非法/缺失、branches 数越界（<2 或 >10）、label 为空或重复、target 缺失/不存在/自环/互同、joinTarget 缺失/等于入口/自环、出边数≠分支数或直连 END、分支出边交叉或外泄区域、分支入口不可达 joinTarget、joinTarget 有区域外入边、区域内含 trigger 或其他 parallel（嵌套） | DSL 校验一次性聚合中文报错（前缀「并行节点 {id}」）；区域内含自包含 loop 合法、U7 环检测结论不变（并行无环） | 04 §5.4 契约；U7 |
| U19 | wait 定时等待 | trigger→wait（durationSeconds=1s）→tool_call：wait 的 node_start 先于后继、node_end 与后继启动间隔≈1s（墙钟，容忍抖动），后继恰好执行一次、整体 completed；outputs[wait-id] = `{mode:"wait", waitType:"duration", durationSeconds:1}`，trace 有 `wait-x: waited 1s` 行；SSE 事件顺序 running→completed（单元测试 monkeypatch `time.sleep` 断言调用值与次数，不产生真实等待）；wait 位于 condition 默认分支/loop 体内时透传无特殊路由 | 04 §5.5；06 §6.1 |
| U20 | wait 校验 | waitType 非 duration（含 "event"，文案注明暂不支持）、durationSeconds 缺失/非整数/0/负数/601、出边 0 条（直连 END）或 2 条、出边指向不存在节点 | DSL 校验一次性聚合中文报错（前缀「等待节点 {id}」）；合法 wait 节点出现在 loop 回边白名单与 parallel 区域 BFS 中不被误判 | 04 §5.5 契约 |
| U21 | human_approval 审批路由 | trigger→human_approval→{tool-approve, tool-reject}：① inputs 预置 `{"approvals":{"human-1":"approved"}}` 秒过（resolvedBy="input"），仅通过侧节点产出存在；② 预置 reject 仅拒绝侧产出；③ 注入立即超时的等待，onTimeout=reject/approve 分别走拒绝/通过侧且 run 仍 completed（resolvedBy="timeout"）；④ ApprovalBroker 人工 resolve 路径（另一线程挂起→resolve("approved")→放行）；outputs[human-id] 形状 `{mode:"human_approval", decision, target, token, summary, approver, resolvedBy}`，trace 有 `human-x: approved (human) → tool-y` 行；SSE node_start data 含 approval 载荷且在阻塞前实时下发（真流式），整体 completed；节点位于 condition 分支/loop 体内时条件路由正确 | 04 §5.6；06 §6.1 |
| U22 | human_approval 校验 | summary 空/缺失、timeoutSeconds 缺失/非整数/bool/9/3601、onTimeout 非法值、approvedTarget/rejectedTarget 缺失/相同/自环/指向不存在节点、出边 0·1·3 条或与配置目标不一致、任一目标直连 END | DSL 校验一次性聚合中文报错（前缀「人机协作节点 {id}」）；合法双出口节点在 loop 回边白名单与 parallel 区域 BFS 中不被误判；REST 决策端点：未知 token 404、重复提交 409、非法 decision 422、reset 清空 pending | 04 §5.6 契约；12 REST |
| U23 | subgraph 引用执行 | trigger→subgraph→tool_call，内存假 resolver 提供子图：① inputs 映射 `{order_id:"{{trigger-1.context.payload.order_id}}"}` 渲染后到达子图 trigger `context.payload`（断言子图执行拿到父图值），子图各节点执行一次；② outputs[subgraph-id] = `{mode:"subgraph", graphId, status:"success", outputs:<子图 outputs>, trace}`，父图后继节点能经 `{{subgraph-x.outputs.<子图节点id>.<键>}}` 引用子图产出，整体 completed、单出边后继恰好执行；③ 子图 run_graph 抛错（resolver 取到的图内部执行异常）fail-safe 为 `status:"failed"`+error，父 run 仍 completed、后继照常执行；④ 子图事件上屏（A 包 `e594a4b` 改订，原「emit=None 无泄漏」断言作废）：内部 node_start/node_end 上屏且带 `subgraphPath:["subgraph-1"]`、仅节点级两型、顶层 node_start 集合不变、整图 run_end 恰好 1 个（子层 run_end 被吞）；⑤ subgraph 节点位于 condition 分支/loop 体内时引用正常 | 04 §5.7；06 §6.1 |
| U24 | subgraph 引用校验 | graphId 缺失/空、inputs 键为空/重复/值非 str、出边 0 或 2 条/自环/指向不存在节点（DSL 层）；编译期 resolver：graphId 不可解析（KeyError）、直接自引用（graphId==当前图）、跨图环 A→B→A、嵌套深度 4（父0→子1→孙2→曾孙3→玄孙4）、被引用子图自身非法（子图 validate_graph 错误以「子图 {graphId}：」前缀聚合）、resolver 未注入（None）报错 | DSL/compile 校验一次性聚合中文报错（前缀「子图节点 {id}」）；深度恰好 3 的引用链合法；合法 subgraph 在 loop 回边白名单与 parallel 区域 BFS 中不被误判 | 04 §5.7 契约；12 resolver |
| U25 | API 适配器参数与装配 | tool_call 节点 config.params 插值后 JSON：缺 url、method 非法（TRACE/小写缺失容忍？）、headers 非对象、timeout 非正数/非数字、params 非合法 JSON、空 params | loader http 分支 JSON 解析失败→节点产出 FAILED/INVALID_PARAMETER；adapter 缺 url→MISSING_PARAMETER、坏 method/headers/timeout→INVALID_PARAMETER；非 http 适配器仍走既有 shop 分派与 SIMULATED 路径不回归；build_demo_registry 含 http/request | 04 §4.6；06 §6.6 |
| U26 | DB/消息适配器参数契约 | ① database/query：缺 sql、params 非对象/数组、limit 非整数/0/1001；② database/execute：缺 sql、坏 SQL；③ message/send：缺 channel/to/subject/body、空字符串、to 数组 21 项、channel=email 地址无 `@`；④ 通用 JSON 透传：database/message 的 params 坏 JSON/非对象 | adapter 层 MISSING_PARAMETER/INVALID_PARAMETER、DB_SQL_ERROR；loader 通用 JSON 适配器集合 {http,database,message} 坏 JSON→FAILED/INVALID_PARAMETER；shop/SIMULATED 不回归；build_demo_registry 含 database/query、database/execute、message/send | 04 §4.7/§4.8；06 §6.7 |
| U27 | 内置模板目录不变量 | 目录恰好 5 个模板、id 唯一且 kebab-case、name/description/tags 非空；每个模板 graph 过 parse_graph 且 validate_graph 返空（condition 分支/双出边/可达性全满足）；元数据 node_count == graph 节点数；模板内 tool_call 工具名 ⊆ demo 已注册工具集（shop 五工具/http/request/database/query/database/execute/message/send）；get_template 未知 id 返 None；退款模板与 nl_generate 兜底为同一函数（单一事实源） | 04 §5.10；06 §6.8 |
| U28 | 录制用例存储/归一化/预置/比对纯逻辑 | ① normalize：递归删除 `token`/`sent_at`；同层含 sent_at 的消息记录 dict 删 uuid `id`；tool=="http/request" 的 `result.headers` 删 `date`；node_type=="human_approval" 删 `resolvedBy`（预置回放恒为 input）；node_type=="trigger" 删 `context.payload.approvals`（预置通道回显）；业务键（order_id/body/status/decision 等）原样保留；深拷贝不污染源数据。② preset_approvals：从含 human_approval 步骤的 output 抽出 `{node_id: decision}`（approved/rejected 混合多节点）。③ dedupe_steps：node_id 重复保末（parallel 两次 node_end、loop 重访）。④ compare：相同步骤 matches=true；篡改一个 output → false 且该节点 diff_keys 为差异顶层键；序列缺/多节点 → 该节点 match=false 且整体 false；baseline_status≠replay_status → false。⑤ RecordingStore CRUD：rec-N 自增、list/get/delete、未知 id 返 None/false | 04 §5.11；06 §6.9 |
| U29 | 调试会话/暂停状态机纯逻辑 | ① 初始 step 模式：before_node 首节点 reason="step" 并阻塞；resume step 后下一节点再停且 step_mode 保持；resume continue 清 step_mode，无断点节点不停、遇无条件断点 reason="breakpoint" 停。② 条件断点：表达式真 reason="condition" 停、假不停；求值异常 fail-safe（不抛、不暂停、last_condition_error 记录）。③ stop：resume action=stop 置 cancelled 且抛 DebugStopped；唤醒后复检 cancelled（reset 路径同收敛）。④ 首决生效：重复 resolve 同 token 第二次返 False。⑤ 并行：两线程同时到暂停点经会话锁串行（暂停序列不交错、同时刻仅一个活动暂停）。⑥ reset：全部会话 cancelled+Event set，等待线程释放不悬挂。⑦ paused 帧 globals/outputs 为深拷贝快照，恢复后改写 state 不影响已发帧。⑧ controller=None 路径零行为（compile/run 不暂停）；subgraph 重入不收 controller、子图整段执行不发 paused | 04 §5.12；06 §6.10 |
| U30 | 监控记录/聚合/规则/告警纯逻辑 | ① extract_node_results：健康 tool 产出判 success；`result.status=="FAILED"`（含 code/message）判 failed 且 error 取 message/code；`status=="failed"` 折叠形状（parallel/subgraph）判 failed 取 error；node_type 取自图定义；健康图不诬报。② percentile：nearest-rank p50/p95、空集 None。③ summarize：total/healthy/unhealthy/success_rate（空集 null）、p50/p95、per_graph 同口径、failed_nodes Top 按 count 降序带 last_error/last_seen。④ ring：deque 200 满则丢最旧。⑤ 四规则：error 运行触发 run_error（critical）；含失败节点触发 node_failed（warning）；同图连续不健康 streak 达 threshold 首触发 consecutive_failures（critical）其后每次失败继续触发交 store 合并；failure_rate 近 window 次（含本次）≥min_samples 且占比≥rate 触发，样本不足不触发。⑥ streak 被健康运行重置为 0；健康运行不产生告警。⑦ 合并：同 (rule_id,graph_id) 最新非 resolved 告警 count++/刷新 last_seen/last_run_id；acknowledged 仍合并；最新项 resolved 后新发为独立告警。⑧ acknowledge（open→ack）/resolve（→resolved）状态迁移，未知 id=None、重复迁移 False。⑨ validate_rules：enabled 非 bool、threshold/window/min_samples 非 1-200 整数、rate 非 0-1 聚合中文错误；reset 清空并恢复默认规则 | 04 §5.13；06 §6.11 |
| U31 | 种子认证/会话/角色矩阵/租户分区纯逻辑 | ① authenticate：四种子账号正确密码返回 Principal（tenant_id/tenant_name/username/display_name/role 正确）；用户名错、密码错、大小写不符返 None；admin-a 属 t1、admin-b 属 t2。② can 矩阵：viewer 仅 read；operator read+operate 不 administer；admin 三者皆可；未知能力拒绝。③ SessionStore：issue 返回 `sess-`+32 hex 且两次不重复；principal_for_token 命中返回等值 Principal；None/无前缀/未知 token/吊销后返 None；revoke 幂等；reset 清空全部。④ TenantRegistry：get(t1) 两次同一实例；get(t2) 为另一套实例，各自建图后 graph-1 在两边独立存在（互不可见、计数各从 1）；录制/反馈/消息/监控/审批/调试 bundle 组件均为不同实例。⑤ reset_tenant：清该租户 graph/approval/debug/monitoring（规则回默认）/message；recording/feedback 保留；他租户 bundle 不受影响（t2 图仍在、t2 规则改动保留） | 04 §5.14；06 §6.12；12 §3.10 |
| U32 | Capability Schema 子集校验 | ① 合法子集（type/properties/required/items/enum/oneOf/additionalProperties/minimum/maximum/边界 boolean 形态等，含空 schema {}）构造 Capability 成功；② 未知 keyword（`$ref`/`format`/`patternProperties`/自定义 `x-*` 等）构造即 ValueError 且信息含字段名与 keyword；③ 非法 type 值、properties 非对象、required 非数组、items 非对象递归拒绝；④ 空 dict（未声明）允许；⑤ 九工具（shop 五、http/request、database/query|execute、message/send）list_capabilities 的 input/output_schema 全部过校验，output_schema 与各服务真实 output 顶层键一致（shop login logged_in、list_pending_refunds orders[]、execute_refund/request_human_approval order_id+status、process_refund 同；http status/headers/body；database query columns/rows/row_count/truncated、execute rowcount；message id/channel/to/subject/body/sent_at）；⑥ registry list_adapters 投影每个 tool 含 input_schema/output_schema（未声明为 {}） | 04 §4.9；12 §3.2 |
| U33 | 拓扑作用域与 L2 引用校验纯逻辑（前端 lib/scope.ts） | ① ScopeIndex.visibleAt：trigger/global 恒可见；线性链仅上游可见（下游/自身不可见）；菱形两分支互不可见、汇聚后两支皆可见；loop 体区域内节点可见 loop 节点 index/iterations，体外（exitTarget 路径与汇聚后）不可见 loop.index 但 loop 节点在拓扑上游时其其他可见性规则一致；parallel.result 动态键不列出（仅 status/branches/joinStrategy/joinTarget）；subgraph 仅列 status/outputs 根。② 输出投影与 04 §6.5 表一致（九类节点）。③ extractRefs 带 token 区间（start/end 含 `{{}}`、空白裁剪后路径）。④ L2 判定：首段节点不存在→REF_NODE_NOT_FOUND；存在但不可达/越体区域→REF_NOT_IN_SCOPE；可见但路径不在投影或工具 output_schema properties→REF_PATH_NOT_FOUND；`global.未声明` 报 NOT_FOUND 类；trigger context.payload 下任意路径放行、空 schema/无 schema 工具不报 PATH；v1 不产生类型级错误。⑤ 同一图多引用一次性聚合；无引用/空串零诊断 | 04 §6.5；19 §1.4 |
| U34 | NL 草稿参数填充尽力校验（validate_param_fills） | ① 合法 params（必填齐全、类型/enum 正确）零警告；② 缺必填→中文警告含节点 id、工具名、字段名；③ 顶层浅类型不符（string 字段填整数、boolean 误判整数排除）与 enum 越界各产警告且一次聚合；④ 字段值为 `{{路径}}` 模板字符串时跳过类型/enum（但字段存在仍满足必填）；⑤ params 含裸插值非合法 JSON 时整体放行；⑥ 未知工具/空 schema `{}`/oneOf 字段/非 tool_call 节点放行；⑦ additionalProperties:false 时未声明字段告警，缺省（允许）不报 | 非阻塞警告列表，不影响草稿回显与兜底语义 | 04 §4.9 ⑤；03 nl_generate_request；12 §5 |
| U35 | 节点 Data Schema 包与 MetaSchema 自检（M1，2026-09-16 落） | ① 九份 `nodes/<kind>.schema.ts` 全部过 metaSchema 形状校验（20 个白名单 keyword + 四个 `x-*` 位置/类型合法）；② 各 schema 投影的 required/enum/range 与 §5.2–§5.7 契约一致（如 human_approval timeoutSeconds 10–3600、onTimeout enum 默认 reject、双 target required；loop maxIterations 1–100；wait durationSeconds 1–600）；③ 引用类字段（condition branches/defaultTarget、loop body/exit、parallel branches/join、wait/human/subgraph target）声明 x-ref 且 kinds 与实际允许种类相符；④ 九份 x-outputSchema 与 `lib/scope.ts` 现有九节点输出投影逐字段一致（含 loop 仅 index/iterations、parallel 无动态 result 键、subgraph status/outputs 根等特例）；⑤ 非法 metaSchema（x-widget 非 string、x-ref.kinds 非数组、x-variable 非 boolean、出现白名单外普通 keyword）自检抛错且含字段名 | 04 §4.9 前端 MetaSchema 扩展、§5.2-§5.7、§6.5；20 §2.2 |
| U36 | 手写校验与 schema 派生双跑等价（M1 迁移观测期，2026-09-16 落） | 以现有 nodeCatalog 校验矩阵用例为基准逐例扩成双跑：① 每个失败用例（缺必填/enum 越界/范围越界/分支配对缺失等）手写 validate 结论 == schema 派生结论，成功用例双方均通过；② 九类节点默认 config 过 schema；③ 出现分歧时测试失败并指出节点种类与字段；④ SchemaRegistry `get(kind, version)` 九类可取、未知 kind 拒绝；⑤ 浏览器冒烟：九类节点属性面板与错误提示零视觉差异（M1 不渲染新表单） | 双跑等价、UI 零差异；手写校验 M1 期不删 | 20 §2.2 验收；04 §4.9 |
| U37 | 结构化诊断模型与 L1 扶正/L2 升级（M2，2026-09-16 落码；前端 `lib/validation/`） | ① diagnostics.ts：Diagnostic 形状（severity/layer/code/message/loc{nodeId,pointer,token{start,end,raw}}/quickFix?）；rank 排序 error 优先、同严重度按节点拓扑稳定序（上游在前）→pointer→token.start，输入乱序输出确定。② l1.ts：M1 解释器自 lib/schemas 扶正迁入；点号/方括号 path 转 RFC6901（`branches[0].label`→`/branches/0/label`，根字段 `/durationSeconds`）；rule→code 全集 FIELD_REQUIRED/FIELD_TYPE/FIELD_ENUM/FIELD_CONST/FIELD_RANGE/FIELD_LENGTH/FIELD_PATTERN/FIELD_ITEMS_MIN/FIELD_ITEMS_MAX/FIELD_ADDITIONAL_PROPERTIES/FIELD_ONEOF；trigger oneOf 候选分支语义与 M1 一致；中文 message 与现 nodeCatalog 文案逐条一致。③ covered:false 手写跨字段/语法规则（名称必填、表达式语法、分支名非空/重复、分支目标存在/重复/自指、默认目标、双 target 必填/互异、subgraph 空输入键）产 layer:'field' 诊断且 code 稳定；名称类无 pointer 仅 nodeId。④ scope L2：RefDiagnostic 三码升级为 layer:'template' 诊断，loc 带 nodeId/字段 pointer（promptTemplate/params/continueExpression/summary、branches/i/expression、inputs/键），token.raw 为含 `{{}}` 的源串切片，start/end 与 M0 区间一致。⑤ validateGraph 聚合单节点 L1+L2 一次产出；layer:'graph' 类型存在但前端不产。⑥ PropertyPanel 消费 Diagnostic[] 渲染 message 列表、AtlasNode 角标 presence/tooltip 与现状一致（组件测试或等价纯函数断言）；validateNode:string[] 签名已移除、无残留调用。⑦ dev 双跑脚手架与已覆盖手写字段规则已删，九节点默认/失败矩阵零回归 | Diagnostic 形状/排序/定位/文案契约；零新依赖 | 08 M2 立项条；03 `diagnostic`；04 §6.5；19 §1.5.1；20 §2.3 |
| U38 | 编译 422 locations 定位侧车（M2，2026-09-16 落码；后端 dsl.py/loader.py） | ① 含节点级字段错误的图 compile/run/run/stream → 422，detail 中文列表与现状逐条一致（现有全部编译期用例零改动通过），并列 locations 与 detail 下标对齐；② 节点名称错误带 nodeId、无 pointer；condition 分支 label/expression/target 与 defaultTarget 错误带 nodeId+RFC6901 pointer；③ 图级错误（version、空图、连线端点、重复 id、全局变量重复）不出 location 条目（稀疏）；④ L2 引用错误带被审节点 nodeId；⑤ 子图递归子错误 v1 仅可定位时带顶层节点 id、不报错；⑥ locations 缺省不影响 detail 判定（形状向后兼容） | detail 零回归 + 侧车下标对齐/稀疏 | 06 §6.13；12 compile 行；03 `diagnostic`；20 §2.3 |
| U39 | WidgetRegistry/FormRenderer 与工具 params 表单化（M3，2026-09-16 立项、**2026-09-17 落码收口（2de5053→6d4a863）**；前端 `lib/forms/`） | ① WidgetRegistry：内置八件 text/number/select/textarea/switch/json/expression/variable-input 可取，registerWidget 扩展点导出且不注册业务控件，未知 widget 名降级；② resolveWidget 选择序：x-widget 优先（仅节点 schema 可带）→ enum/const→select、boolean→switch、integer/number→number、object(properties)→递归分组、array(items)→增删行、additionalProperties-only object→键值行、string→text/模板控件；③ 降级：oneOf（message/send `to`）、无类型/空 `{}`（database params、http body、shop/list_pending_refunds）、整串含裸 `{{}}` 非合法 JSON、未注册工具、发现失败 → JSON 文本框且旧占位符/插入变量路径不回归；④ FormRenderer 不可变更新产出下一整个 params 对象并 JSON.stringify 回写 config.params（Graph v1 存储格式不变）；⑤ SchemaRegistry 第二来源按 `<adapter>/<tool>` 取发现快照 input_schema，只持引用不复制；⑥ variable-input 复用 visibleAt 补全，命中字段 pointer 的 L1/L2 Diagnostic 红字与 token 区间可传给控件；NL paramWarnings 以非阻塞 warning 展示（wire 仍 string[]）；⑦ 九工具逐一生成表单（snapshot/交互：必填、enum Select、headers 键值、数组增删），保存→编译→运行黄金链路 sql-query-notify 不回归；⑧ 浏览器实测 database/query（sql/params/limit）、http/request（method enum/url/headers/body）、message/send（to oneOf 降级）表单与旧 JSON 框双路径 | 零新依赖；params 字符串存储/运行期插值/后端零改动；旧 JSON 路径完全保留 | 08 M3 立项条；03 `form_renderer`；04 §4.10；19 §1.1/§1.3.3/§1.3.4；20 §2.4 |
| U40 | UISchema 最小子集 + 手写 Config 迁移第二波（M4，**2026-09-17 立项、批 1/批 2/批 3 全部落码（转正式）**：uiSchema groups/hiddenWhen/hideFields + labels/placeholders/optionLabels 文案层（批 2 起嵌套通配键 + rows/keyPlaceholders）、内置控件 8→9（+radio）、target-select/saved-graph-select 两个节点业务控件与 NodeConfigForm 已落，HumanApproval/Trigger/Loop（批 1）/Parallel/Subgraph（批 2，06b62fc）/Condition（批 3，79317e3）已迁并真实浏览器冒烟；Wait 经判定保留手写不迁；前端 `lib/forms/`） | ① ui:group：groups 配置把同层字段归入分组容器，HumanApproval approved/rejected 以 layout:'row' 并排，未分组字段顺序不变；② hiddenWhen：trigger triggerType=schedule 显 cron、=webhook 显 webhookUrl、=manual 全隐，被隐藏字段不渲染且不参与 L1 校验（切到 schedule 后空 cron 才报错）、隐藏值跨切换保留；③ 不支持 ui:order/if-then 动态 dependencies（显式非目标）；④ HumanApprovalConfig 切 FormRenderer 后字段/文案/校验/变量补全/双目标选择与旧组件逐条等价，旧组件与 kind 分派迁完即删；⑤ Wait/Loop/Parallel/Subgraph/Condition 逐个迁移各带等价测试（Condition 动态 branches 数组 + validateExpression 置末位，批 3 已迁） | 零新依赖；Graph v1/后端零改动；迁移组件视觉与校验等价、不回归 | 08 M4 立项/批 1–批 3 落码条；03 `ui_schema`；04 §4.10 末扩展条；20 §2.5 |
| U41 | 前端 L3 预判 + Problems 面板 + reverseDeps/quickFix v1（M4，**2026-09-17 立项、批 2/批 3 全部落码（转正式）**：①②L3 同构 0dc9b7f（11 夹具跨运行时对拍，pytest+vitest 双侧守）、③Problems 面板 2ad4177（浏览器冒烟点击定位通过）、⑥前端 L3 预判已落，后端仅加对拍夹具+一处 flaky 测试修复（aaf50bf）、dsl.py 零改动；④reverseDeps 4b15d95（target 8 处/4 类 + 模板引用双类反向索引、删节点索引化清 target、L2 收窄引用方）、⑤quickFix 875922b（Problems 内联「删除悬空引用」，批 3 浏览器冒烟实测诊断消失）；前端 `lib/validation/{l3,reverseDeps}.ts` + `ProblemsPanel.tsx`） | ① GRAPH_UNREACHABLE：构造从 trigger BFS 不可达节点，前端诊断与后端 dsl.py `_validate_reachability` 逐条同构（无 trigger 时跳过）；② GRAPH_ILLEGAL_CYCLE：构造含环图（含 loop 白名单回边合法用例 + 白名单外成环非法用例），与 `_validate_illegal_cycles` 同构；③ Problems 面板聚合全图 Diagnostic[] 经 rank 排序，点击条目按 nodeId+pointer 定位节点并展开字段；④ reverseDeps：改节点 id 时列出引用方（v1 交付 referrersOf 定位，改名 UI 随 D30）；⑤ quickFix v1 仅「删除悬空引用」动作，挂 L2 REF_NODE_NOT_FOUND，执行后该诊断消失且无新错误（批 3 冒烟实测通过）；⑥ 前端 L3 仅预判，后端 dsl.py 仍唯一权威（M4 后端零改动） | L3 同构对拍零分歧；quickFix 幂等；零新依赖 | 08 M4 立项/批 2/批 3 落码条；03 `graph_diagnostics`；04 §6.5 末扩展条；20 §2.5 |
| U42 | 增量调度/脏标记 + 大图性能基准（M4，**2026-09-17 立项、批 1/批 2/批 3 全部落码（转正式）**：① dirty 失效范围纯模块 `lib/validation/dirty.ts` + editorStore 接入 + 单测批 1 已落（30d5009），批 3 reverseDeps 把删节点 L2 收窄到真实引用方（4b15d95）；②分层调度（L1 useLayoutEffect 同步/L2 300ms 防抖/L3 requestIdleCallback+setTimeout 回退）、③structureSignature 记忆化 ScopeIndex/L3 + 独立 validationStore 批 2 已落（1468e18）；④性能基准 f496062（`frontend/bench/` RUN_BENCH=1 门控，200/500 节点 6 场景 p50/p95 入 BENCHMARK.md）、⑤Worker 判据：稳态单节点 L2 增量 p95≈10ms@500 低于一帧，不引 Web Worker（掉帧实证后另开 ADR）；editorStore + BENCHMARK.md） | ① dirty 脏标记：改 config 仅标该节点 L1 失效、增删边/改 target/改名标受影响子图 L2/L3 失效，失效范围单测；② 分层调度 L1 同步/L2 防抖/L3 requestIdleCallback 不重复算、不漏算；③ 结构 hash 记忆化 ScopeIndex（同拓扑命中缓存）；④ 200/500 节点构造图 vitest + 浏览器基准，L1/L2/L3 各层耗时入 BENCHMARK.md，输入不卡顿（批 3 已落：500 节点全量冷启动 p95≈70ms 一次性、热态增量 <10ms）；⑤ 主线程未掉帧则不引 Web Worker（引 Worker 须另开 ADR；批 3 基准支持不引） | 增量结果与全量一致；性能阈值以实测报告为准 | 08 M4 立项条③⑦/批 1/批 3 落码条；20 §2.5 |
| U43 | 中断恢复：挂起帧序列化→模拟新进程→同 token 决策生效（M5b 候选，**2026-09-17 M5 契约设计轮登记（docs/24 §2.3/§6），T18 已拍板（B），**2026-09-17 已随 M5b 落码转正式（test_loader_resume.py）****；`src/atlas/storage/`） | ① human_approval 挂起后取挂起帧（`interruptions` 行）→ 重启服务（或以帧重建新进程内 broker）→ `POST /api/approvals/{token}/decision` 同 token 决策生效，run 从挂起节点续跑到 END，`resolvedBy:"human"` 不变；② `deadline_at` 绝对时刻：挂起等待 N 秒后重启，恢复后仅剩 N-k 秒（超时不重计），到点按 `onTimeout` 自动决策；③ 重启时 `running` 中断的运行标 `interrupted` 不重放副作用；④ 恢复扫描器幂等（重复启动不重复建帧）；⑤ integration 标记 + 本地 Docker PG 全绿，进程内 431+ 测试零回归 | 重启仅 suspended 可恢复；跨进程决策 1s 轮询兜底；续跑不重跑上游 | 08 M5 契约设计轮条；03 `interruption_frame`；docs/24 §2/§6；20 §3 |
| U44 | wait 剩余时长恢复（**2026-09-17 已随 M5b 落码转正式（test_loader_resume.py）**；docs/24 §3.1） | wait 挂起（durationSeconds=N）k 秒后重启，恢复后按剩余 N-k 秒继续（trace `wait-x: waited Ns` 语义不变，node_end 正常发出）；`deadline_at` 为空/过期的帧按已到期处理走安全默认 | 不重计；不悬挂线程 | 同 U43 |
| U45 | SSE 断线重连与挂起查询（**2026-09-17 已随 M5b 落码转正式（test_api_runs.py）**；docs/24 §3.3/§4） | ① `/run/stream` 断开后执行不取消，run 状态落库；② 重连后 `GET /api/runs/{run_id}` 返回 suspended/最终态，`GET /api/runs?status=suspended` 列出挂起（`kind=approval` 附 resumeToken）；③ 跨租户 run_id 404、viewer 可读 operator 写档不变 | 执行线程与 SSE 解耦；不新增写端点 | 同 U43 |
| U46 | Graph 版本化与 subgraph 钉版（M6，**2026-09-17 立项并落码（1f49644，转正式）**；`src/atlas/versioning/` + `storage/memory.GraphStore` 扩展 + 12 发布/版本端点） | ① 发布：`POST /api/graphs/{id}/publish` 冻结 Graph JSON → `releaseVersion=1`，再编辑草稿（latest 变）→ `POST /api/graphs/{id}/run` body `releaseVersion=1` 仍跑冻结快照（草稿改动不污染已发布版本）；② 已发布版本只读：无改写路径，publish 只从 latest 草稿产 v2；③ subgraph 钉版：父图含 subgraph 节点，发布父图时子图（未发布）先递归发布为 v1 并钉 `graphId@v1`；子图再发布 v2 后，父图已发布版本仍钉 v1（`GET /api/graphs/{id}?releaseVersion=N` 快照中 subgraph graphId 仍为 `@v1`），运行隔离；④ `GET /api/graphs/{id}/versions` 列版本、未知版本/未发布 404；⑤ 「保存即 latest」演示流程零回归（save/list/get 缺省 latest） | 发布版本不可变；钉版本号即冻结引用内容；草稿 latest 零回归 | 08 M6 立项条；03 `graph_definition` 版本化注记；12 §5 发布/版本端点；20 §4.1 |
| U47 | 任务总线信封状态机与 dispatch/join/escalate（**2026-09-17 立项并落码（转正式，test_coordination.py + test_return_refund_coordination.py，08 M7 立项条+落码条，docs/19 §2.2.2/§2.3.1）**） | ① dispatch：协调者发信封给单 Bot，状态机 `pending→accepted→running→done/failed/timeout`；② join：parallel 网关等两 Bot 任务 completed（all_completed 语义 + `result.<入口id>` 承载）；③ escalate：升级＝带审批卡片的特殊 human_approval；④ 退货退款沙盘链路（客服 Bot + 物流 Bot，dispatch→join→未签收禁放款 condition→escalate）端到端跑通 | 信封状态机合法迁移；join 复用现有网关；升级即 human_approval | 08 M7 立项条；03 `task_envelope`；12 任务端点；20 §4.2 |
| U48 | 冲突三层与超时三级链（**2026-09-17 立项并落码（转正式，test_skills.py + test_return_refund_coordination.py，08 M7 立项条+落码条，docs/19 §2.4）**） | ① L1 幂等键 `资源\|动作\|版本` 去重，重复请求返**首结果**；② L2 乐观锁 CAS：demo shop 订单 version 并发写，失败方重读已终态则放弃；③ L3 硬约束 condition（未签收禁放款）+ 升级 human_approval（不做 Agent 投票）；④ 超时三级链：任务级 deadlineMs + 节点级 retry.timeout + 审批级 timeoutSeconds，动作链＝重试（幂等键不变）→ 升级 → fail-safe（run 仍 completed） | 幂等返首结果；CAS 一方放弃；超时升级 fail-safe | 08 M7 立项条；19 §2.4；20 §4.2 |
| U49 | span 模型与 contextvars 父子树（M10，**2026-09-18 立项并落码（1f21b1a，转正式）**，`src/atlas/tracing/` + test_tracing.py 9 用例，ADR T21） | ① Tracer 建 root run span，traceId 全树一致、spanId 唯一、parentSpanId 正确闭合；② 同线程 contextvars 嵌套 span（run→node→tool）父子链正确；③ `span()` 上下文管理器异常时 status=error 且不泄漏当前 span；④ id 形状（traceId 32hex/spanId 16hex）；⑤ 纯 stdlib、零新依赖 | 与 OTel 同形不引 SDK；线程内传播；显式 tracer 透传 | 08 M10 立项条①；03 `trace_span`；04 §5.15；06 §6.15；19 §2.3.4 |
| U50 | run/节点/tool/subgraph 埋点 + SSE 三帧超集 + graphVersion（M10，**2026-09-18 立项并落码（89f5645 埋点 + c227f0c 入口透传，转正式）**，test_tracing_loader.py 7 用例 + test_tracing_api.py 2 用例） | ① run_graph 起 root run span 并标 graphVersion（发布 `id@<releaseVersion:int>`/草稿 `id@draft`，无 v 前缀、对齐 M6 `f"{sub_id}@{pin}"`）；② 每节点 node span，node_start/node_end 帧带 traceId/spanId/parentSpanId、run_end 带 graphVersion（现字段不删、帧型不变）；③ 工具调用产生 tool 子 span（attrs adapter/capability，FAILED→error）；④ subgraph 重入复用同一 tracer、子图 span 链到 subgraph span、子图 SSE 事件仍不外泄；⑤ 未传 tracer 自建、库直跑亦有 span；⑥ 现有运行/调试/回放测试零回归 | 纯超集前端零改动；span 不进 output；debug 不埋点 | 08 M10 立项条②③④⑥；12 run_graph/SSE；20 §4.5 |
| U51 | 退货协同 span 树端到端断言（M10 验收①，**2026-09-18 立项并落码（c227f0c，转正式）**，test_tracing_coordination.py 4 用例） | run_return_refund 导出一棵完整 span 树：root run → 两信封 task_dispatch/task_done（actor=bot.customer/bot.logistics、attrs orderId）→ 物流 check_receipt tool span；全树 traceId 一致、父子闭环无孤儿、Envelope.traceId 为真实 traceId（非 runId 占位）、graphVersion 透传；幂等重放不产生重复 task span | traceId 贯穿信封；父子闭环；幂等不重复 | 08 M10 立项条⑤；20 §4.5 验收①；19 §2.3.4 |
| U52 | span 不外泄开关 + 录制归一化补条（M10 验收②③，**2026-09-18 立项并落码（89f5645 折叠 + 2b1dbe9 归一化，转正式）**，test_tracing.py + test_tracing_loader.py + test_recording.py） | ① `to_tree(include_internal=False)` 把 subgraph 内部折叠为单个 subgraph span（保留 graphId/status），True 返回完整内部树；parallel 合成步不外泄；② `recording.normalize` 剔除产出内 traceId/spanId/parentSpanId/graphVersion 元数据键；③ 现有录制回放黄金用例（退款分流等）逐节点比对零回归（span 随机 id/时间戳不参与比对） | 折叠只影响投影不影响记录；回放零回归 | 08 M10 立项条③⑦；20 §4.5 验收②③；04 §5.7/§5.15 |
| U53 | cards 包：目录不变量/三渠道渲染/插值缺失/action 映射/消息入参（M8，**2026-09-18 立项并落码（批 1 `2b35fc9`，转正式）**，`src/atlas/cards/` + test_cards.py，04 §5.6 追加段/12 §3.11） | ① 目录：CARDS 非空、id 唯一 kebab-case、name/channels 非空、channels⊆{web,im,email}、sections/actions 形状合法（fields.bindings 与 form name 非空、action.style 枚举、output.decision∈approved|rejected）；list_cards/get_card 命中、未知 id 返 None；v1 含 refund-approval。② render_card web：fields bindings 插值为只读行、form sections 投影 `{type,name,label,required,default}`、actions 投影按钮、带 token/approver/timeoutSeconds。③ im：text 含摘要、buttons 两按钮带回调 URL、detailUrl 兜底；email：subject/html 只读摘要、links 两链接带 token、timeoutHint。④ 插值：bindings 复用 graph.loader.interpolate，缺失路径保留同一缺失语义（不抛/不臆造）。⑤ map_action_output：合法 actionId+form 回填 `{{form.<name>}}` 得 `{decision,comment}`；未知 actionId、output.decision 非法、缺 required form 字段 → ValueError（REST 422）。⑥ to_message_params：im/email 渲染产物产 message/send 入参 `{channel,to,subject,body}`，不自动外发。⑦ 渲染为纯函数、同上下文幂等、不改卡片目录 | 三渠道降级形状契约；只读不副作用；GET 链接不产生决策 | 08 M8 立项条；03 `card_template`；05 §5.3；19 §2.2.3/§2.3.2 |
| U54 | cardTemplateId 接线端到端（M8，**2026-09-18 立项并落码（批 2 `5f4384e`/`4b45571`、批 3 `25afa10`、收口 `94000de`/`486a591`，转正式）**，dsl/loader/approvals/frame/api + 前端，12 §3.11/§5） | ① 编译：human_approval config.cardTemplateId 命中目录编译通过；非空但 id 不命中 → 422 中文错误（前缀「人机协作节点 {id}」）；不填编译/运行走旧 summary 路径完全不回归。② 挂起：命中卡片的审批 node_start `approval` 载荷在 `{token,summary,approver,timeoutSeconds}` 上带 cardTemplateId；ApprovalBroker pending（request/restore/get/list_pending）透传 card_template_id/上下文快照。③ 渲染端点：`GET /api/cards` 目录投影；`GET /api/approvals/{token}/card?channel=web\|im\|email` 返对应渠道产物，未知 token 404、无卡片审批 404、非法 channel 422；GET 只读不产生决策。④ 决策：`POST …/decision` 收 `{actionId,form}` 经 map_action_output 回写 decision/comment（节点产出超集含 `comment` 与 `card:{templateId,actionId}`）；坏 actionId/缺必填 422；旧 `{decision,comment?}` 仍可用；首决 409、未知 404 不变。⑤ 中断恢复：M5b 帧带 cardTemplateId，重启恢复后 card 端点仍渲染（上下文从帧 outputs+trigger 重建），同 token 决策生效。⑥ 前端：配置态 card-select（拉 /api/cards、清空＝旧路径）、卡片 bindings `{{}}` 纳入该审批节点 L2（坏引用 REF_NODE_NOT_FOUND/REF_PATH_NOT_FOUND + Problems/角标）、运行态 CardRenderer（fields 只读 + form 经 FormRenderer kind:"card" + actions 必填校验后 actionId/form 提交）、无卡走旧 summary Modal 不回归 | 旧路径零回归；纯超集；坏 id/坏 action 422；沙盘不解除 D33 | 08 M8 立项条；03 `card_template`；04 §5.6/§6.5；12 §3.11/§5；20 §4.3 |
| U55 | Router 三段分桶纯逻辑（M9，**2026-09-18 立项并落码（批 1-4＋收口，转正式）**，`src/atlas/routing/router.py` + models.py，12 §3.12） | ① internal：event.tenant 命中 allowlist → candidate 且 segment="internal"，tenant 取 Principal 不取请求体；② lowValueBucket：payload.amount 数字且 ≤ value → candidate（percent=100 全进），amount 缺失/非数字/超阈 → 继续下一段；③ canary：桶键 payload.order_id 缺省 payload.id，`int(sha256(f"{graph_id}:{bucket_key}").hexdigest(),16)%100 < percent` 恒定（同对象多次解析同段、不横跳），percent=0 全不进、percent=100 全进；桶键缺失/非字符串 fail-safe 落 stable（segment="fallback"）；④ full：candidate 全量；⑤ 无 config/无 candidate/stable 缺失 → stable 或 (None,"unpublished")；⑥ 段固定序不可重排、规则形状非法在 configure 期 422；⑦ 纯函数无 IO、不改计数 | 固定序；哈希恒定；fail-safe 落 stable；纯函数 | 08 M9 立项条；03 `rollout_config`/`route_decision`；04 §5.16；06 §6.17；19 §2.5 |
| U56 | pin-to-version 与 event 接线（M9，**2026-09-18 立项并落码（批 1-4＋收口，转正式）**，api/main.py + routing/store，sync+stream） | ① 带 event 运行：经 resolve 得版本后按不可变发布快照加载（复用 M6 resolver 与 subgraph 钉版），RunRecord.resolved_version 与 result.graphVersion 为该版本；② 无任何发布版带 event → 409；③ 不带 event（编辑器手动运行）走 latest 草稿、resolved_version=None，旧行为零回归；④ 在途 pin：canary 期间以 candidate 发起 human_approval 挂起 → 手动 rollback → 同 token resume，续跑图快照为启动时 candidate（断言帧 graph_snapshot 含 candidate 版本号、subgraph 钉版不漂），新 event 已全落 stable；⑤ RoutingStore 挂 TenantServices，memory/PG 两分支装配、reset 清空（rollout 配置/状态/计数）；⑥ 跨租户 rollout 状态互不可见、直访 404；viewer 只读、operator 可配置/启动/放量/回滚 | 在途不漂版本；草稿零回归；两档装配不漏 | 08 M9 立项条；03 `route_decision`；04 §5.16；06 §6.17；12 §3.10/§5 |
| U57 | 发布前批量回放门禁（M9，**2026-09-18 立项并落码（批 1-4＋收口，转正式）**，`src/atlas/recording/gate.py` + release-gate 端点 + publish gate 参数，D26 部分取回） | ① 图有 N 个 graph_id 匹配的用例且草稿全过：GateReport total=N/passed=N/blocked=false/skipped=false，逐例 matches=true；② 草稿改坏某节点：该例 matches=false（逐节点差异并入 note 单行，GateReport case 行不另出 diff_keys）、blocked=true、failed=1；③ `POST publish {gate:true}` blocked → 409 带完整报告且**不产新版本**（versions 列表不增）；全过 → 正常产新版本；④ 无匹配用例（total=0）：skipped=true、blocked=false，publish 不被阻塞但报告明示未覆盖；旧用例 graph_id 空串不入选；⑤ release-gate 只跑门禁不发布（独立 POST 端点），图不存在 404；⑥ 门禁逐例走标准 run_graph 复用 replay.compare（preset_approvals/collect_steps），无录制专用运行时；⑦ RecordingCase.graph_id 纯超集落库、列表投影含 graph_id，旧用例读取不回归 | blocked 不产版本；skipped 明示；复用回放无新运行时 | 08 M9 立项条；03 `release_gate`/`recording_case`；04 §5.11 末；06 §6.17；12 §3.7/§5；14 D26 |
| U58 | 业务结果指标 + 门控自动回滚（M9，**2026-09-18 立项并落码（批 1-4＋收口，转正式）**，`monitoring/business.py` + `routing/gate.py`，金融硬条款） | ① extract_business 四形状：自动退款 tool_call result.status=="refunded" → auto_refunded=true；人工升级 status=="human_review" → manual_escalated=true；expected_amount 取 trigger payload.amount；refunded_amount 取退款 result.amount，demo shop 不回金额时回退 expected_amount（amount_diff=false，沙盘金额差异率恒 0）；无业务结果 None；② metrics business 段三率（auto_refund_rate/manual_escalation_rate/refund_amount_diff_rate）全局/per_graph/per_version 分列，分母＝有业务结果 run，样本 0 为 null；③ canary + autoRollback：构造 candidate 运行越任一阈值（run_error_rate>.02 / manual_escalation_rate>.10 / refund_amount_diff_rate>.005，样本≥min_samples、observeMinutes 窗内）→ 自动 rollback（actor="auto"）+ rollout_gate critical 告警带 action{type:rollback,from,to,reason,actor}，此后新 event 全落 stable；④ 健康运行/样本不足/autoRollback=false/非 candidate 运行均不回滚；⑤ 全仓无自动 promote（promote 仅手动端点）；⑥ 回滚不产生补偿/冲正动作（已退款等外部事实不被改写）；⑦ 门控仅在 sync/stream 真实运行 record_run 之后触发，replay/debug/subgraph 重入不触发；routing→monitoring 单向依赖无环 | 门控只切流不改外部事实；样本不足不判；无自动 promote | 08 M9 立项条；03 `business_metrics`/`monitoring`；04 §5.13 末/§5.16；06 §6.11/§6.17；19 §2.5 金融硬条款 |
| U59 | rollout REST 状态机 + 前端发布流（M9，**2026-09-18 立项并落码（批 1-4＋收口，转正式）**，REST + apiClient/Editor/Monitoring + 新灰度 Modal，浏览器冒烟） | ① REST：PUT rollout 存配置（非法 422 聚合）不启动；start 不足两发布版/未配置 409、成功 idle→canary；promote 非 canary 409、canary→full；rollback 任意态可调、重复幂等；GET 快照含 status/stable/candidate/traffic；权限 viewer 只读、operator 可写、跨租户 404。② 前端发布流：编辑器「发布」按钮带门禁选项（Modal 显示 GateReport 逐例 ✓/✗ 与 note 差异，blocked 时禁止发布并提示）；版本 Select（选发布版运行/草稿）；灰度 Modal（四段规则表单 internal 租户/lowValueBucket 字段阈值百分比/canary 百分比/full + gate 三指标阈值/observeMinutes/autoRollback 开关 + start/promote/rollback 按钮与状态/流量计数展示）；Monitoring 页展示 business 三率（per_version）与 rollout_gate 告警 action 徽标。③ vitest 覆盖 apiClient 新端点与 Modal 状态机。④ 浏览器全链路冒烟（scripts/dev/m9_smoke.py）：建图→录用例→v1→坏 v2 门禁拦截→好 v2→rollout start→分流断言（小额/internal/canary 哈希）→在途 pin→构造越阈→自动回滚+告警，截图入 docs/assets | 状态机非法迁移 409；前端闭环；沙盘不解除 D32 | 08 M9 立项条；03 `rollout_config`/`release_gate`；04 §5.16；06 §6.17；12 §3.12/§5；20 §4.4/§7.5 |

| U60 | 批量回放报告沉淀与通过率趋势（D26 报告 v1，**2026-09-18 立项并落码收口（转正式，后端 615/前端 404）**，`recording/reports.py` + release-reports 两端点 + ReleaseModal 历史区，08 D26 立项条+落码条，§9 落码行） | ① release-gate 沉淀 manual 报告且响应带 id，字段与 GateReport 一致；② publish gate 通过/blocked 均沉淀 publish-gate（409 报告体带 id）；③ total=0 skipped 也沉淀、pass_rate=null；④ 列表倒序摘要不含 cases、详情含 cases、跨图/跨租户取报告 404、图不存在 404；⑤ viewer 可读两 GET、不可跑门禁（既有不回归）；⑥ reset 清空报告而录制用例保留；⑦ ring 100 满后最旧淘汰；⑧ 前端历史区 mock 渲染、trigger/通过率中文展示、展开行看逐 case | 报告是运行产物 reset 清空（区别录制用例测试资产）；gate.py 保持纯函数不碰存储（06 §6.11 分层）；ReportStore 不 PG 化、PG/多租户随 D26 余部缓做；零新依赖 | 08 D26 立项条；03 `release_report`；04 §5.11 末；12 §3.7/§5；14 D26 |

| U61 | subgraph 引用快照内联（D26-b，**2026-09-19 落码（转正式，`29bb3d9`）**，`recording/snapshots.py` + RecordingCase.subgraphs，08 落码条） | ① 录制含 subgraph 节点的图，用例 `subgraphs` 递归冻结父图及嵌套子图 raw（深度≤3、visited 防跨图环、引用缺失不阻断录制，旧用例缺省 `{}`）；② reset/删除/改动被引子图后，单用例 replay 经 inline_first_resolver 仍可回放（先查内联、未命中回退 GraphStore）；③ **发布门禁 run_release_gate 仍走实时 resolver**：子图被删/改坏的 latest 草稿门禁 block（不被旧快照带过）；④ 后端 pytest test_api_demo/test_recording 覆盖收集与内联回放 | 仅 replay 内联、gate 刻意实时（验当前草稿，坏子图应 block，08 落码条订正立项原文）；纯超集字段旧用例零回归 | 08 D26/D30 落码条；03 `recording_case`；04 §5.11 末；12 recordings POST；14 D26 |
| U62 | 回放报告导出 CSV/JSON（D26-a，**2026-09-19 落码（转正式，`8a37b4e`）**，`reports.report_to_csv` + export 端点 + 前端导出列） | ① `GET .../export?format=json` 返完整报告 attachment（rr-N.json、Content-Disposition）；② csv 返元信息行+空行+逐 case 行（text/csv、UTF-8 BOM 供 Excel、通过率/触发/布尔中文化）；③ 非法 format 422、默认 csv；④ 图不存在/报告跨图或不存在 404 照详情；⑤ read 角色可导出；⑥ 前端 apiClient 走 Bearer fetch→blob→`a[download]`，ReleaseModal 历史表「导出」CSV/JSON 两按钮 stopPropagation 不展开行（releaseApi.test +2，node 环境 stub URL/document） | 不新增报告删除端点（ring 自然淘汰）；导出不另存、纯即时序列化；零新依赖 stdlib csv/io | 08 落码条；03 `release_report`；04 §5.11 末；12 export 行；14 D26 |
| U63 | 发布门禁定时批量回放 CI（D26-c，**2026-09-19 落码（`cea70f4`）**，`.github/workflows/release-gate-cron.yml`） | ① workflow 含 schedule（`17 3 * * 1` 周一 03:17 UTC 避整点）+ workflow_dispatch；② job checkout/setup-python 3.11/pip install -e ".[dev]"→后台起 127.0.0.1 uvicorn→登录轮询 30 次等就绪→跑 scripts/dev/d26_smoke.py；③ smoke 失败 job 失败并上传 uvicorn.log artifact（留存 7 天）；④ 脚本自登录自造数自 reset、无密钥；⑤ 本地端到端实测服务约 2s 就绪、smoke 24 断言全过、SMOKE_EXIT=0 | cron 仅默认分支 main 生效，dev 先落、合并 main 后启用（dev 用 workflow_dispatch 手验）；YAML 已本地校验 | 08 落码条；04 §5.11 末；09 recording 表；14 D26 |
| U64 | 变量数据依赖环 GRAPH_DATA_CYCLE（D30-b，**2026-09-19 落码（转正式，`9a357df`）**，后端 dsl `_validate_data_dependency_cycles` + 前端 l3 同构） | ① loop 的 continueExpression 引用体内节点 B、B 又引用 `{{loop.index}}` 形成数据环：后端编译 422 报 GRAPH_DATA_CYCLE（error、loc 挂回边引用方模板字段）、前端 L3 实时报同码；② 普通上游→下游引用、loop 自身 index/iterations 自引用不误报；③ 节点不存在/不可见/路径非法引用不纳数据边（由 L2 各码承接）；④ 前端 validateL3 聚合顺序非法环→不可达→数据环；⑤ 编辑 continueExpression/params 引入或消除数据环，L3 经 graphDataSignature 实时重算（engine.test 旧「模板不重算 L3」契约改写为 2 用例）；后端 test_graph_ref_validation +2、前端 l3 +3 | 数据环夹具不进 l3-fixtures.json（该夹具仅 parse 结构域、不含 check_refs），走独立用例；loop 运行时循环计数不纳边 | 08 落码条；03 `graph_diagnostics`；04 §6.5 末；14 D30；19 §1.4.3/§1.5.2 |
| U65 | 工具入参/输出标量类型校验 REF_TYPE_MISMATCH（D30-a，**2026-09-19 落码（转正式，`47ecf48`）**，纯前端 L2 warning） | ① tool_call params 中「值整体为单个模板」叶子，按 input_schema 期望 vs 上游 output_schema 末端实际比标量：相同不报；② 期望 number 接受 integer 源；③ 期望 integer 不接受 number 源（可能带小数）报 warning；④ string 期望给 integer/boolean 源报 warning；⑤ object/array/oneOf/开放 additionalProperties/拼接串/任一端缺 schema/params 非法 JSON 一律放行；⑥ severity=warning、loc=/params、不进后端编译 422、运行期仍原样插值；scope.test +8 | 仅 tool→tool、仅标量可判定时比对（控误报，守 19 §1.4.3 缺省降级仅存在性下限）；纯前端、后端不接线 | 08 落码条；03 `graph_diagnostics`；04 §6.5 末；14 D30；19 §1.4.3 |
| U66 | condition/loop 表达式函数库（D15，**2026-09-19 落码（转正式，`ac8c718`）**，前后端同构 conditions.py/.ts，禁 eval） | ① 算术 `+ - * / %` 与括号、比较、逻辑混合求值正确；② `/`、`%` 对零抛 ConditionEvalError（fail-safe），`%` 截断式 `-7%3==-1` 前后端一致；③ 白名单函数 abs/floor/ceil/round（round=floor(x+0.5)）/min/max/len/lower/upper 逐一对拍；④ date/year/month/day/daysBetween 确定性日期（后端 date、前端 DateValue 序数）同构；⑤ 常量折叠；⑥ 顶层非布尔（裸算术/数值/字符串/日期）报「条件表达式必须产出布尔值…」；⑦ 非白名单函数/未知变量/语法错误报错；⑧ 不含 now()/today()；后端 test_conditions.py + test_validation_locations.py、前端 conditions.test.ts 同构对拍 | 前后端同构零分歧；禁 eval；确定性（回放安全） | 08 A+B 立项条；04 §5.1 D15 扩展；14 D15 |
| U67 | loop break/continue 中断语义（D17，**2026-09-19 落码（转正式，`5f87654`）**，loader/dsl，纯拓扑不新增节点类型） | ① 体内 condition 分支直连 exitTarget＝break，运行时在该轮退出、产出 exitReason="break"、iterations/index 保留（`_loop_break_graph`）；② 含 break 出口但无回边的图通过静态校验；③ 非 condition 体内节点直连 exitTarget 编译 422（退出路径只能由循环节点或体内 condition break 分支出发）；④ continue＝体内节点连回 loop 重入重判条件（既有回边机制，无新机制）；⑤ break 网关 `__break__<id>` 以 loop 节点补发 node_end、不泄漏内部网关；⑥ 既有 while 循环（condition_false/max_iterations/expression_error）黄金用例零回归 | break 仅 condition 出口；内部网关不外泄；fail-safe 退出原因集 | 08 A+B 立项条；04 §5.3 D17 扩展；14 D17 |
| U68 | parallel any_success OR-join（D18，**2026-09-19 落码（转正式，`f45daa9`）**，loader `_branch_outcomes`/`_guard_any_success`/合成网关） | ① 短支先 success：未开始的长支节点被短路 `{skipped,reason:any_success_joined}` 不发起 tool/wait/approval，汇聚 status=success、joinTarget 恰好执行一次；② 同超步已启动节点保留执行完（不取消已发起调用/等待/审批）；③ 全部分支失败才 failed（joinTarget 仍执行 fail-safe）；④ 一成一败→success；⑤ skipped 分支末端不计 settled/failure；⑥ 网关幂等、迟到残留分支走 "done"→END 不重复执行 join/不二次 emit；⑦ DSL 非法策略仍 422（夹具改用 wrong_strategy）；后端 test_graph_loader `_any_success_graph` 3 用例、前端枚举/文案 nodeForms 对拍 | 只跳过未开始节点、外部事实不回滚；全失败才 failed；joinTarget 一次 | 08 A+B 立项条；04 §5.4 any_success 段；14 D18 |
| U69 | parallel.result 汇聚点可见性 + 入口 id 校验（D30 B1，**2026-09-19 落码（转正式，`ee534ef`）**，dsl + scope.ts） | ① `{{parallel-x.result.<入口id>}}` 在 joinTarget 及其下游可见、可编译运行；② 在并行分支区域内引用 result 报 REF_NOT_IN_SCOPE（汇聚后才可用）；③ result 下键非 branches 入口 id 报 REF_PATH_NOT_FOUND（附合法入口列表）；④ status/branches/joinStrategy/joinTarget 静态键可见性不回归；后端 test_graph_ref_validation、前端 scope.test | 汇聚前不可见；入口 id 白名单 | 08 A+B 立项条；04 §6.5 D30 三项；14 D30 |
| U70 | subgraph.outputs 深层路径展开（D30 B2，**2026-09-19 落码（转正式，`25db6df`）**，loader `_subgraph_output_index` 权威门 + scope 第四参） | ① `{{subgraph-x.outputs.<子图内部节点id>…}}` 后端按已解析子图内部节点 id 校验，合法深层路径编译运行通过、产出可达；② 内部节点不存在编译 422 REF_PATH_NOT_FOUND（附合法内部节点）；③ 子图未加载/解析失败时前端降级放行（不误报）；④ 既有 `{{subgraph-x.status}}`/浅引用不回归；后端 test_graph_loader 子图编译拒绝用例、前端 scope.test | 可解析则校验否则降级；后端编译期权威；前端子图结构拉取随 D21 | 08 A+B 立项条；04 §6.5 D30 三项②；14 D30/D21 |
| U71 | 改节点 id 的引用 quickFix 全量联动（D30 B3，**2026-09-19 落码（转正式，`8496b2d`）**，reverseDeps/editorStore/PropertyPanel） | ① 属性面板节点 ID 受控可编辑；② validateNodeId：空/非法字符/保留字 global/重复拒绝；③ renameNode 后所有模板引用头 `{{old.…}}`→`{{new.…}}`（含 parallel.result/subgraph.outputs/loop.index/condition 分支表达式）重写；④ 8 处 target 字段与出入边端点同步、无悬空 REF_NODE_NOT_FOUND/无 stranded；⑤ 改名后图编译通过、黄金链路不回归；reverseDeps.test + editorStore.test | 改名幂等、全量联动无悬空；id 规则稳定 | 08 A+B 立项条；04 §6.5 D30 三项③；14 D30 |
| U301 | OpenAPI HTTP Basic 凭证子集（D22，2026-09-23 立项 docs/46；parser/PUT/适配器） | ① parser：`type:http,scheme:basic` 收录为 kind=basic/param=Authorization/prefix="Basic "，digest 等仍忽略，basic 名参与 security OR-of-AND 组；② PUT：basic value=`{username,password}` 200 configured、缺 password/非字符串/空对象 422 OPENAPI_INVALID_CREDENTIAL、null 删除；basic 与 bearer 混合按名区分；③ 适配器：请求头 `Authorization: Basic base64(u:p)`（固定向量），信封 JSON 损坏 SECRET_DECRYPT_ERROR、缺信封 OPENAPI_CREDENTIAL_MISSING；④ docs/42/44 用例零回归、内存/PG store 同形；⑤ 前端 basic 双输入合并对象提交、已配置计数不变 | 值类型 string｜{username,password}｜null（后两者仅 basic）；信封明文 JSON 密文存储形状不变；无新依赖/迁移/REST/错误码/ADR | docs/46；12 §5 PUT credentials 行；14 D22 |
| U302 | wait 事件等待进程内 v1（D19，2026-09-23 落码收口 docs/47；broker/DSL/loader/REST/前端） | ① EventWaitBroker：request/wait 信号放行、超时 False、signal_key 广播计数、signal_token 未知 KeyError/重复 409、list_pending、reset、信号先于 wait、0.2s 取消切片；② DSL：event 四字段校验（eventKey 静态白名单/≤128、timeout 1-3600、onTimeout 枚举）、duration 零回归、allowed keys；③ loader：渲染后 key 复检失败 WAIT_EVENT_KEY_INVALID、signaled 带 payload、timeout continue/fail（WAIT_TIMEOUT_FAILED）、waitEvents 预置 resolvedBy=input、等待中 cancel；④ REST：三端点 200/404/409/422、operate/read 权限门、跨租户 404、payload 限制；⑤ 前端 WaitConfig event 模式与 L1 纯逻辑 | 零新依赖/零迁移/无 ADR；挂 TenantServices.event_wait_broker | docs/47；04 §5.5 追加段；06 §6.22；12 §3.17/§5；14 D19 |
| U303 | condition LLM 语义判断分支 v1（D14，2026-09-23 落码收口 docs/48；classifier/DSL/loader/前端，无新 REST） | ① 分类器纯逻辑：LiteLLM 实现正则抽 JSON 成功、坏 JSON/缺键/标签越界抛 ConditionClassifyError、Offline 必抛、工厂按 LITELLM_MODEL 切换（注入 completion 假函数不触网）；② DSL：llm 分支 label/description/target 校验（空/超长 422、带 expression 422）、classifierPrompt ≤500、conditionMode 非法 422、rule 模式零回归、拓扑约束两模式一致；③ loader：合法标签路由、`__default__`/解析失败/未知标签/抛异常四类 fail-safe 走 defaultTarget 且 run completed、llm_errors 中文文案、上下文 12000 截断、condition_classifier 注入与子图透传；④ 前端 L1：llm 分支跳过 validateExpression 改校验 description、禁混 expression、纯逻辑 vitest | 零新依赖/零迁移/无 ADR/无新错误码；run_graph 增 condition_classifier 参数 | docs/48；04 §5.2 追加段；06 §6.23；12 §3.18；14 D14 |
| U304 | wait 定时等待动态时长 v1（D19，2026-09-23 立项并落码收口 docs/49；DSL/loader/前端，无新 REST；8580fcb/4eaaddd） | ① DSL：durationMode 非法 422 `/durationMode`、dynamic + durationExpression 空/超长(>200) 422 `/durationExpression`、dynamic 允许保留 durationSeconds 不报混用、static 与旧图（无 durationMode）零回归、allowed keys、单出边拓扑两模式一致；② loader：dynamic 变量表达式求值成功（整数值 float 取 int(round)）、算术/白名单函数、求值秒数同时作帧 timeout_seconds、ConditionEvalError/异常→WaitNodeFailure WAIT_DURATION_INVALID 不 sleep、非数值/bool/非有限/0/601→WAIT_DURATION_INVALID、resume 路径不变；③ 前端 L1：dynamic 校验表达式非空≤200 且不产 durationSeconds 诊断、static 反之，纯逻辑 vitest；x-outputSchema 两新键 | 零新依赖/零迁移/无 ADR/无新端点；新增失败码 WAIT_DURATION_INVALID | docs/49；04 §5.5 追加段；06 §6 wait 叙述；12 §3.19/§5；14 D19 |
| U306 | IM 群机器人投递 v1（D24，**2026-09-23 落码收口 docs/51；后端 d4823d5、前端 80b2436**；im.py/service/adapter，无新 REST；test_message_im.py 36 用例） | ① im.py：dingtalk 带 secret 的 timestamp/sign URL 拼接（固定时钟向量逐字节）、wecom 不加签、feishu 空 key HMAC sign 与无 secret 省略键、三渠道 text payload 形状；2xx+errcode/code==0 成功；非 2xx/坏 JSON/平台码非 0/超时 → ImDeliveryError；guard.check 拦截内网（EgressDenied 不包装）；② service：三渠道 to 单 URL（数组 INVALID_PARAMETER）、secret 仅 dingtalk/feishu（非空 ≤200、其他渠道传 secret INVALID_PARAMETER）、失败 IM_SEND_FAILED 不落记录、成功 delivered 渠道名、未注入投递器回退 in_process；③ adapter：input_schema 含可选 secret、channel 描述与 capability description 更新 | 零新依赖/零迁移/无 ADR/无新端点；新增失败码 IM_SEND_FAILED | docs/51；04 §4.8 追加段；06 §6.7；12 §3.21；14 D24 |
| U305 | wait 到点时刻等待 v1（D19，2026-09-23 落码收口 docs/50：12de72d；DSL/loader/前端，无新 REST） | ① DSL：durationMode 非法 422 `/durationMode`、absolute + absoluteTime 空/超长(>64) 422 `/absoluteTime`、absolute 允许保留 durationSeconds/durationExpression 不报混用、static/dynamic 两模式与旧图零回归、allowed keys、单出边拓扑三模式一致；② loader：ISO 带时区/naive(按UTC)/尾缀 Z、epoch 秒数值字符串、`{{路径}}` 插值渲染、差值秒数作帧 timeout_seconds、注入时钟可控、插值未知变量/坏 ISO/epoch 越界→WaitNodeFailure WAIT_ABSOLUTE_TIME_INVALID 不 sleep、过点(delta<1)/delta>600→WAIT_ABSOLUTE_TIME_INVALID、resume remaining_seconds 不变；③ 前端 L1：absolute 校验 absoluteTime 非空≤64 且不产 durationSeconds/durationExpression 诊断、static/dynamic 反之，纯逻辑 vitest；x-outputSchema 新 absoluteTime 键 | 零新依赖/零迁移/无 ADR/无新端点；新增失败码 WAIT_ABSOLUTE_TIME_INVALID | docs/50；04 §5.5 追加段；06 §6 wait 叙述；12 §3.20/§5；14 D19 |

## 3. 集成测试用例（Playwright，Harness 正确性）

| # | 被测对象 | 用例 | 预期 | 关联 |
|---|---|---|---|---|
| I1 | Web 适配器-层1 | 提供精确 selector | 直接点击成功（timeout=3000） | 06 6.5 |
| I2 | Web 适配器-层2 | 仅元素描述，视觉定位 | omni_parser 解析 → llm_match 置信度>0.7 → 点击 | 06 6.5 |
| I3 | Web 适配器-层3 | 两层均失败 | LLM 全图推理返回坐标 → 点击 | 06 6.5 |
| I4 | Web 适配器-兜底 | 三层全失败 | 返回 failed("元素未找到") | 06 6.5 |
| I5 | Web 适配器-缓存 | 点击成功后 selector 缓存 | 下次同元素直接走层1 | 06 6.5 update_selector_cache |
| I6 | 工具调用 | 退款工具执行成功 | ActionResult.Status=SUCCESS | 06 6.4 |
| I7 | 工具调用 | 工具执行部分失败 | Status=PARTIAL，输出含部分结果 | 06 6.4 |
| I8 | 权限 | 无权限工具被调用 | 拒绝并记录审计 | 04 4.3 permission；06 安全 |
| I9 | API 适配器-成功 | httpx MockTransport 返回 200 JSON/非 JSON | SUCCESS，output=`{status,headers,body}`；JSON 自动解析、非 JSON 为文本；默认/节点 headers 键级合并；相对 URL 拼 baseUrl | 04 §4.6；06 §6.6 |
| I10 | API 适配器-HTTP 错误 | MockTransport 返回 4xx（401/404）/5xx | 仍 SUCCESS 带 status（图内 condition 判分支，run completed）；mock 端点无 token 真实 401 | 04 §4.6 |
| I11 | API 适配器-传输失败 | MockTransport 抛 httpx.TimeoutException / ConnectError | ActionResult FAILED，code=HTTP_TIMEOUT/HTTP_CONNECT_ERROR，不抛异常；端到端父图 trigger→http/request 打 `/api/demo/mock/orders` 与 POST receipt | 04 §4.6；06 §6.4 不抛错 |
| I12 | 数据适配器-query | 注入 SQLite StaticPool engine（seed orders 两笔）：无条件查询返回 columns/rows/row_count、命名绑定参数 `:min` 过滤生效、limit=1 截断且 truncated=true、查询后数据未被修改；未配置 engine 调用→FAILED DB_NOT_CONFIGURED；坏 SQL→FAILED DB_SQL_ERROR（含密码的 URL 在错误/observe 中脱敏） | ActionResult SUCCESS，输出契约 04 §4.7；PG 只读保障 `postgresql_readonly` 路径见集成（真实 PG 随 D23 补） | 04 §4.7；06 §6.7 |
| I13 | 数据适配器-execute 与权限 | 同一 SQLite engine：execute INSERT rowcount=1 且随后 query 可见新行；坏 SQL→DB_SQL_ERROR 不抛异常；只授 read 的适配器调 execute→PERMISSION_DENIED 且写审计；query 在无 read 权限时同样拒绝；未知能力 UNKNOWN_CAPABILITY；父图 trigger→database/query 经临时端口真实 uvicorn 打 demo SQLite 返两笔订单，reset 后恢复种子 | 04 §4.7；06 §6.4；I8 |
| I14 | 消息适配器-send 与权限 | message/send：to 字符串与数组均落记录（to 展平、id 为 uuid、sent_at ISO）、记录经 `GET /api/demo/messages` 可见、reset 清空；数组 21 项/email 缺 `@`/缺字段→INVALID_PARAMETER/MISSING_PARAMETER；只授 read 时 send→PERMISSION_DENIED 写审计；未知能力拒绝；父图 trigger→message/send 后 GET 端点可见 | ActionResult SUCCESS 输出消息记录；无真实投递（不产生网络 I/O）；04 §4.8 | 04 §4.8；06 §6.7；I8 |
| I15 | 模板库 REST 与端到端 | `GET /api/templates` 返 `{items:[{id,name,description,tags,node_count}]}` 且不含 graph、恰好 5 项；`GET /api/templates/{id}` 返完整元数据含 graph（与目录一致）；未知 id 404；detail.graph 直接 POST /api/graphs 保存后 compile 成功；`POST /api/demo/reset` 后列表/详情仍可用（目录为代码常量） | 模板只读投影正确；reset 不影响模板目录 | 04 §5.10；06 §6.8；12 §5 |
| I16 | 录制用例 REST 与回放端到端 | approval-timeout-reject 模板 detail→保存为 graph→（inputs 预置或超时跑完）以 node_end outputs 组装 steps 调 `POST /api/recordings`：201 返回完整用例（id=rec-N、graph 为快照、created_at）；graph_id 未知 404、steps 空/形状非法 422、name 空或超长 422；`GET /api/recordings` 投影恰为 `{id,name,node_count,step_count,status,created_at}` 且不含 graph/steps；`GET /api/recordings/{id}` 含快照与 steps，未知 404；`POST …/replay` matches=true、baseline_status==replay_status=="completed"、逐节点 match=true（含 human_approval 秒回 resolvedBy="input" 不挂起；token/sent_at/uuid/date/resolvedBy/载荷 approvals 归一化生效）；篡改某节点 output 再录一条 → replay matches=false 且 diff_keys 正确；reset 后用例仍在且可回放；DELETE 后 GET 404；快照内 subgraph graphId 缺失等回放异常折叠 replay_status="failed"/matches=false（不抛 500） | 录制/列表/详情/删除/回放投影与状态码全对；reset 保留；审批预置回放不阻塞；篡改可检出 | 04 §5.11；06 §6.9；12 §5 |
| I17 | 调试流式运行与 resume 端到端 | 小图（trigger→condition→tool 一类）POST /run/stream 带 `debug:{breakpoints:[…]}`：启动即 step，paused 帧序列的 node_id 与节点执行序一致（paused 在 node_start 后、node_end 前）；帧含 token(dbg- 前缀)/node_type/reason/globals/outputs，且 outputs 仅含已完成节点终态；逐个 `POST /api/debug/{token}/resume {action:"step"}` 推进至 result；continue 后无断点节点连发、命中断点 reason="breakpoint"；条件断点真路 reason="condition" 停、假路不停直达 result；resume stop → 收到 stopped 帧 `{node_id, reason:"user_stop"}` 且无 result 帧；未知 token resume 404、重复 resume 同一暂停 409；`GET /api/debug` 列活动暂停（暂停存在时可见、恢复后消失）；三类 422：breakpoints 引用未知 node_id、表达式白名单校验失败、同步 /run 带 debug；reset 后卡在暂停的会话按 stop 释放（线程不悬挂、可再发起新运行） | paused/stopped 帧形状与顺序正确；step/continue/stop 状态迁移正确；404/409/422 状态码与中文错误；reset 释放不悬挂 | 04 §5.12；06 §6.10；12 §3.8/§5 |
| I18 | 监控采集/聚合/告警中心 REST 端到端 | ① sync POST /run 正常模板（如 http-orders-branch）→ GET /api/monitoring/metrics total=1、healthy=1、success_rate=1.0、p50/p95 非 null、per_graph 一行；GET /api/monitoring/runs 元素含 mode="sync"、status="completed"、nodes 全 success 与 duration_ms。② stream POST /run/stream 跑 sql-query-notify 头部默认（query FAILED INVALID_PARAMETER 既有现象）→ run completed 但含 failed 节点（error 文本），metrics unhealthy=1、failed_nodes 含 query 节点，GET /api/alerts 出现 node_failed（warning，open，count=1，last_run_id）。③ 构造 error 运行（引用未注册适配器/坏图致异常冒泡）→ sync 端点仍 500（行为不变）但 runs 记录 status="error"、alerts 出现 run_error（critical）。④ 同图连跑 3 次不健康（阈值默认 3）→ consecutive_failures critical 一条、count=3（同键合并），中间一次健康运行后 streak 重置需再 3 次才发；failure_rate 默认窗口下超 50%（样本≥5）触发。⑤ runs：graph_id 过滤、limit 截断与上限 200、新→旧。⑥ rules：GET 默认值；PUT 改 threshold=2 生效；非法（enabled 字符串、threshold=0、rate=2、window 非整）422 中文聚合。⑦ alerts：acknowledge 200 且状态迁移、重复确认 409；resolve 后同键再发为新 id；未知 id 404；?status= 过滤投影。⑧ reset：metrics 归零、alerts 清空、rules 恢复默认。⑨ debug 运行（带 debug 入参，跑完或 stop）不入 runs；录制 replay 运行不入 runs | 采集口径（仅真实运行）、失败推断、聚合、四规则、合并、状态机、422/404/409、reset 与默认规则全部正确 | 04 §5.13；06 §6.11；12 §3.9/§5 |
| I19 | 登录鉴权/RBAC/租户隔离 REST 端到端 | ① 无 token GET /api/graphs → 401 中文 detail；坏 token（"xxx"、"sess-nope"、非 Bearer 格式）同 401；公开端点 login/health/静态/`/api/demo/mock/orders`（带 X-Demo-Token）不要求平台登录。② login：admin-a/admin123 返回 sess- token 与 principal（t1/演示企业 A/admin），错密码/未知用户 401「用户名或密码错误」；me 回显；logout 后旧 token 再用 401。③ viewer-a：GET 图/模板/监控/告警 200，POST /api/graphs 403、POST run 403、PUT rules 403、POST reset 403、GET /api/feedback 403；POST /api/feedback 201。④ operator-a：可保存图、编译运行（含 stream 带 Bearer）、可 acknowledge/resolve 告警、可审批决策/debug resume；PUT rules 403、reset 403、GET feedback 403。⑤ admin-b 隔离：登录后 GET /api/graphs 为空（admin-a 建的 graph-N 不可见），直接 GET admin-a 的图 id → 404；两租户审批 token、调试 token 互访 404；监控 metrics/runs/alerts 互不可见；t2 PUT rules 改阈值不影响 t1（t1 GET 仍默认）。⑥ admin-a reset：本租户图/消息/监控清空、规则回默认、调试与审批释放；录制与反馈仍可 GET；t2 数据不动。⑦ feedback：viewer 提交后 admin-a GET 可见本租户条目，admin-b GET 不含。⑧ demo reset 后可再用同 token（会话不随 reset 失效） | 401/403/404 中文语义正确；三角色白名单全覆盖；两租户数据完全隔离且不泄漏存在性；reset 作用域与保留项正确；登录/me/登出闭环 | 04 §5.14；06 §6.12；12 §3.10/§5 |
| I20 | Schema 投影与 DSL L2 引用复查端到端 | ① GET /api/adapters 九工具投影含 input_schema/output_schema（shop 五工具为 JSON Schema 子集，字段与 U32 一致；http/database/message 不回归）。② 保存/编译含坏引用的图：tool_call params 引用不存在节点 → 422 中文错误含节点 id 与 REF 类语义文案；引用不可达上游（下游/平行分支）→ 422 REF_NOT_IN_SCOPE 语义；tool_call 引用上游工具 output_schema 中不存在的深层路径（如 `result.bogus`）→ 422 REF_PATH_NOT_FOUND，而 `trigger-x.context.payload.anything` 放行；引用全局未声明变量 → 422。③ loop 体内节点引用 `{{loop-x.index}}` 合法；体外节点引用 → 422。④ 合法既有图（内置 5 模板、W9-W10 退款图）编译/运行零新增错误（运行期插值缺失保留原样的语义不变）。⑤ 编译期复查不改变运行期 outputs 与 run 结果（黄金链路回归） | 投影形状正确；三类坏引用 422 中文聚合；合法引用与 payload/无 schema 放行；既有图零回归 | 04 §4.9/§6.5；12 §5 |

## 4. 回放测试用例（录制回放框架）

> **满足状态（2026-09-15，Phase 2 操作录制 v1，04 §5.11）**：R1 由录制回放 v1 满足——操作序列（node_id 顺序）+ 逐节点变量归一化后逐项比对，落为 U28（纯逻辑）/I16（REST 端到端）；R3 的回放确定性经「录制人工决策→回放 inputs.approvals 预置」满足（实时人工交互存在于录制时的正常 /run/stream 链路）。R2（页面元素变化的三层定位兜底回放）属 Web 操作适配器范围，随浏览器操作录制一并缓做（14 D22），本项不覆盖。

| # | 用例 | 预期 |
|---|---|---|
| R1 | 录制一次完整退款流程 → 回放 | 行为与录制一致（操作序列、变量值逐项比对）——✅ 2026-09-15 由录制回放 v1 满足（U28/I16，04 §5.11；归一化规则见该节，跨版本/配置更新差异对比缓做 14 D26） |
| R2 | 回放中页面元素变化（属性变化但可定位） | 三层定位兜底后继续，不中断——⏳ 随 Web 操作适配器/浏览器操作录制缓做（14 D22） |
| R3 | 回放中关键节点人工审批 | 暂停等待，审批通过后继续——✅ 录制时为正常链路的实时人工审批；回放时决策从 baseline 预置（resolvedBy="input"），同用例可重复回放不挂起（04 §5.6 决策来源②、§5.11） |

## 5. 端到端测试用例（沙盒环境，对应 08 7.3 验收标准）

| # | 验收标准（08 7.3） | 端到端用例 | 预期 |
|---|---|---|---|
| E1 | 1.拖拽创建触发→AI决策→工具调用流程 | 画布创建 3 节点流程并保存 | 保存成功，DSL 可编译 |
| E2 | 2.自动登录电商平台获取退款订单 | 沙盒电商登录 + 拉取新退款单 | 登录成功，订单列表获取 |
| E3 | 3.AI 决策判断退款原因和金额 | 输入破损订单(299) / 不想要(5000) | 正确分类（见 9.2 示例） |
| E4 | 4.执行退款或触发人工审批 | 破损→自动退款；高额→人工审批 | 退款状态 completed / 审批单创建 |
| E5 | 5.画布实时显示运行进度 | 运行中刷新状态 | 节点高亮与 LoopState 同步 |
| E6 | 6.自然语言生成流程草稿 | 输入"每日9点导出昨日订单，超100单差评>5%发钉钉" | 生成可编辑草稿 |
| E7 | 7.变量在节点间正确传递 | 上游输出 → 下游引用 | 值一致 |

## 6. AI 评估用例（评估 Harness，原文 06 9.2）

### 6.1 evaluation_task 原文示例（refund_processing）

```yaml
evaluation_task:
  task_id: "refund_processing"
  description: "处理退款申请"
  test_cases:
    - input: {order_id: "12345", reason: "商品破损", amount: 299}
      expected: {action: "approve_refund", verify: "refund_status == 'completed'"}
    - input: {order_id: "12346", reason: "不想要了", amount: 5000}
      expected: {action: "request_human_approval", verify: "approval_request_created"}
  metrics: [task_success_rate, average_steps, decision_accuracy]
```

### 6.2 扩展评估用例（按 9.3 黄金测试集 ≥5 条要求补充）

| # | 输入 | 期望动作 | 验证 |
|---|---|---|---|
| A1 | 破损 299 | approve_refund | refund_status == completed |
| A2 | 不想要 5000 | request_human_approval | approval_request_created |
| A3 | 已发货商品退款 | 人工介入（涉及物流） | human_involved == true |
| A4 | 金额超阈值（如 10000） | request_human_approval | approval_request_created |
| A5 | 重复退款请求（幂等） | 拒绝或返回已处理 | duplicate_detected == true |
| A6 | 库存不足触发补货（Graph 分支） | urgent_replenish | replenish_created |

## 7. 上线前强制测试四道门（原文 06 9.3）

| 门 | 要求 | 对应用例 |
|---|---|---|
| 1. Schema 完整性校验 | 节点/工具/变量 Schema 全部合法 | U9 + 全量 Schema 校验脚本 |
| 2. 黄金测试集回归 | ≥5 个测试用例全通过 | §6.2 A1-A5 |
| 3. 沙盒模拟运行 | 成功执行 3 次 | §5 E1-E7 各跑 3 次 |
| 4. 安全审查 | 敏感操作确认机制有效 | I8 + 高金额二次确认 + 审计日志存在 |

## 8. 测试执行建议

- Demo 阶段（W1-W10）：先建 U1-U10、I1-I8、E1-E7；回放测试（§4）与评估用例（§6）随编辑器与反思模块（Phase 2-3）落地。
- 每次迭代回归：至少跑黄金测试集（A1-A5）+ 沙盒 3 次成功。
- 覆盖率目标：单元层节点/变量/表达式 100% 核心路径，不做无意义全量覆盖率追逐。

## 9. 实现进度（落码后回填）

| 日期 | 范围 | 测试 | 状态 |
|---|---|---|---|
| 2026-09-13 | W1 OODA 最小循环（engine state/nodes/loop，确定性占位） | `tests/test_engine_loop.py`：循环按 max_steps 收敛 completed、默认 3 步、max_steps=1 时 act 不执行（3 用例） | ✅ 全绿；属框架冒烟，不计入 U1-U10 |
| 2026-09-13 | W1 前端编辑器框架 | `cd frontend && pnpm build`（tsc + Vite）；浏览器人工验证节点添加/选中/改名/日志 | ✅ 通过；前端自动化测试（组件/E2E）待建 |
| 2026-09-13 | W1 PostgreSQL/pgvector 连接验证 | `tests/test_database_integration.py`（标记 integration，默认跳过）：`ping` SELECT 1、vector 扩展版本 ≥0.5、vector(3) 写入与余弦距离排序（3 用例） | ✅ 对本地 Docker `pgvector/pgvector:pg16` 全绿；开启方式 `ATLAS_RUN_INTEGRATION=1` + `DATABASE_URL` |
| 2026-09-13 | W3-W4 Harness 契约层 | `tests/test_harness_contract.py`：注册/发现/重复注册/心跳 TTL、权限拒绝+审计（I8）、未知能力、授权执行（7 用例） | ✅ 全绿；对应 I8 |
| 2026-09-13 | W3-W4 三层定位编排 | `tests/test_web_location.py`：层1 命中不截图、层1 miss→层2 坐标点击+缓存回填、置信度 0.7 边界落层3、层3 坐标、三层全失败兜底、缓存短路层1（6 用例，假页面） | ✅ 全绿；对应 I1-I5 编排（不含浏览器） |
| 2026-09-13 | W3-W4 真实浏览器 | `tests/test_web_browser_integration.py`（integration，Chromium）：I1 selector 点击、I2 层2 真实坐标点击、I5 缓存短路、type/observe（4 用例） | ✅ 全绿；I3/I4 编排已由单元测试覆盖，真实视觉模型接入后补 I2-I3 模型链路；I6/I7 待业务工具（退款场景） |
| 2026-09-13 | W5-W6 编辑器核心纯逻辑 | vitest 5（ADR T6）4 个测试文件 20 用例，`pnpm test`：`lib/__tests__/variables.test.ts`（引用提取/路径解析/插值/命名校验/路径清单）、`nodeCatalog.test.ts`（默认配置与校验规则）、`graphSerializer.test.ts`（契约形状 + 坐标取整）、`store/__tests__/editorStore.test.ts`（nextId 防碰撞、添加节点、删节点级联边） | ✅ 全绿；UI 交互（拖拽落点/校验角标/变量面板/Graph 导出弹窗）经浏览器人工验证零控制台错误；组件级/E2E 自动化待建 |
| 2026-09-13 | W7-W8 Graph DSL 解析校验 | `tests/test_graph_dsl.py`（7 用例）：合法 Graph JSON 解析、节点 id 重复、悬空连线、未支持类型/空名、分类型必填配置（cron/webhookUrl/promptTemplate/tool）、版本与空图、自环与重复变量 | ✅ 全绿（普通单测，TestClient/编译均进程内） |
| 2026-09-13 | W7-W8 DSL→LangGraph 编译运行 | `tests/test_graph_loader.py`（5 用例）：`{{路径}}` 插值（含下标/缺失保留）、线性 3 节点按边序执行产出、START/END 接线、运行时输入覆盖全局变量 | ✅ 全绿；三类节点执行器为确定性占位，LLM/真实工具链路待 LiteLLM/适配器联动 |
| 2026-09-13 | W7-W8 graphs REST | `tests/test_api_graphs.py`（FastAPI TestClient，4 用例）：health、保存→读取→编译（入口/终点/边）→运行（插值生效）全生命周期、未知 id 404、非法图 422 聚合错误列表 | ✅ 全绿；浏览器人工验证编辑器"编译并运行"（保存/编译/运行三请求，`{{global.approval_limit}}`→500）与 422 中文错误回显 |
| 2026-09-13 | W9-W10 退款决策黄金集（E3） | `tests/test_decision_client.py`（6 用例）：12345-12349 五笔黄金用例（质量原因限额内 approve_refund；主观/超额 request_human_approval）、LiteLLM JSON 解析成功、输出不可解析 fail-safe 转人工（fake litellm 注入） | ✅ 全绿；未配置 LITELLM_MODEL 走规则决策 |
| 2026-09-13 | W9-W10 店铺服务/适配器（E2/E4） | `tests/test_shop_adapter.py`（8 用例）：种子单状态、退款/转人工流转、demo 登录、process_refund 按动作路由、INVALID_ACTION/ORDER_NOT_FOUND/MISSING_PARAMETER/AUTH_FAILED 结构化错误、financial 权限拒绝、list_pending_refunds 与 observe | ✅ 全绿 |
| 2026-09-13 | W9-W10 NL 草稿（E6） | `tests/test_nl_generate.py`（2 用例）：退款意图生成可通过 DSL 校验的三节点模板、未配置 LLM 时非退款意图 422 | ✅ 全绿（规则兜底仅退款场景，任意描述需配置 LITELLM_MODEL） |
| 2026-09-13 | W9-W10 退款端到端（E3/E4/E7） | `tests/test_refund_e2e.py`（3 用例）：12345 破损→refunded、12346 主观→human_review 全链路；SSE 事件序列 node_start/node_end/run_end；payload 经 `{{路径}}` 渲染进提示词 | ✅ 全绿 |
| 2026-09-13 | W9-W10 Demo REST（E5/E6） | `tests/test_api_demo.py`（6 用例）：/api/adapters 能力发现、NL 草稿 200/422 且可直接保存、/run/stream SSE 事件序、退款流经共享注册表、店铺登录 401/200、控制台页面 | ✅ 全绿 |
| 2026-09-13 | W9-W10 平台集成（E2） | `tests/test_shop_platform_integration.py`（1 用例，integration 标记）：trigger→shop/login→shop/list_pending_refunds 真实适配器链路，自动登录后抓取 5 笔退款单 | ✅ ATLAS_RUN_INTEGRATION=1 下通过 |
| 2026-09-13 | W9-W10 前端 | vitest 23 用例（含 deserializeGraph 往返、loadGraph 草稿载入、节点运行状态置位/重置）；浏览器人工验证 12345→approve_refund→refunded、12346→request_human_approval→human_review、SSE 日志上屏、NL 草稿 3 节点载入、/demo/shop 登录拉单 | ✅ 23/23，`pnpm build` 通过 |
| 2026-09-13 | 设计 Token 等价替换（17 文档 §3） | vitest 23 全绿、`pnpm build` 通过；浏览器 computed-style 核对：`:root` 变量注入（primary #1677ff/border #e5e7eb/pulse rgba 一致）、body 文字 rgb(31,41,55)、header rgb(15,23,42)、画布 rgb(238,242,247)、节点三色 rgb(82,196,26)/rgb(114,46,209)/rgb(22,119,255)、running/completed 光晕与连线 stroke，控制台零错误 | ✅ 零视觉差异；`grep` 确认 src 下硬编码色值仅剩 theme/tokens.ts |
| 2026-09-13 | Phase 1 一键交付（静态托管 + reset） | `tests/test_api_demo.py` +2：reset 后店铺恢复种子单、图清空、登录态失效（401）；dist 存在时 GET / 返回构建产物。后端 60 passed/8 skipped。镜像实测：`docker compose up --build` 后 /api/health 200、静态 JS 200 application/javascript、/demo/shop 200、容器内黄金用例 12345→refunded / 12346→human_review、reset 后订单恢复；浏览器访问 :8000 编辑器与 token 正常 | ✅ 通过（本地 Docker Hub 偶发超时，重试可构建，最终镜像 932MB/内容 214MB） |
| 2026-09-13 | Phase 1 应用内反馈入口 | `tests/test_api_demo.py` +2：反馈提交→列表可见且 reset 后保留、非法 type/空 content 返回 422。后端 62 passed/8 skipped；前端 vitest 23、`pnpm build` 通过；构建产物同源（:8000）浏览器实测：反馈弹窗 bug/建议切换、内容+联系方式提交成功态、`GET /api/feedback` 两条落库，控制台零错误 | ✅ 通过 |
| 2026-09-15 | Phase 2 操作录制 v1（04 §5.11，U28/I16，运行录制+回放比对） | `tests/test_recording.py`（23 用例，新包 recording 纯逻辑）：normalize 五规则（token/sent_at 递归删、消息记录 uuid id、http/request headers.date、human_approval resolvedBy、trigger context.payload.approvals；业务键保留/不污染源；后两规则含「录制超时拒绝 vs 回放预置拒绝」端到端 compare 场景）、preset_approvals 混合抽取与忽略非审批节点、dedupe_steps 保末保位、compare 相同匹配/篡改 diff_keys/缺节点/多节点/状态不一致/parallel 折叠、collect_steps 保末、RecordingStore CRUD 与请求模型 422；`tests/test_api_demo.py` +5（I16：approval-timeout-reject 真实模板录制投影/node_count=4/step_count=3、未知 graph 404、四类 422、详情 404、预置 rejected 回放秒回（<5s）且重复回放一致、篡改 body 后 diff_keys=["result"]、reset 保留且仍可回放、DELETE→404、subgraph 引用缺失折叠 failed 不抛 500）。前端 vitest +3（`lib/__tests__/recordings.test.ts`：toSteps 过滤非 node_end、保末重排、空输入） | ✅ 后端 311 passed/8 skipped、前端 vitest 52（7 文件）、oxlint/tsc/build 通过；浏览器（chrome-devtools 对真实 :8000+:5174）实测：模板画布命名录制（10s 超时拒绝路径）入列 4 节点·3 步骤·completed；首轮回放暴露 trigger payload.approvals 与 approval resolvedBy 两处伪差异 → 补归一化规则与单测（42a82f3）；修复后回放秒回整体「匹配」、trigger/approval/message 三节点全 ✓（跨两次独立运行的 token/sent_at/uuid id 归一化生效）；reset 清空 GraphStore 后用例保留且回放仍匹配（快照自足）；Popconfirm 删除后列表即时空；硬重载后控制台零错误零告警 |
| 2026-09-14 | Phase 1 反馈入口镜像复验 | `docker compose build` 重建 `atlas-demo:latest` 后容器内 curl：POST 两类反馈 201、非法 type/空 content 422、GET 列表、reset 后反馈保留，构建 JS bundle 含反馈文案与 `/api/feedback`；经 CDP（9222，`~/.chrome-claude` 独立 profile，Playwright connectOverCDP，参照 agent-world `docs/browser-verification.md`）驱动真实 Chrome 实测容器同源 UI：弹窗提交两条（bug+联系方式 / 建议无联系方式）成功态上屏、GET 落库、reset 保留、console 零错误 | ✅ 通过 |
| 2026-09-15 | Phase 2 API 适配器（通用 HTTP，04 §4.6，U25/I9-I11） | `tests/test_httpapi_adapter.py`（24 用例，httpx MockTransport 零外呼）：200 JSON/非 JSON body、404 仍 SUCCESS 带 status、ReadTimeout→HTTP_TIMEOUT、ConnectError→HTTP_CONNECT_ERROR、headers 键级合并（节点优先）与相对 URL、绝对 URL 绕 base、无 base 相对路径→MISSING_PARAMETER、dict/string body、坏 method/url/headers/timeout/body 参数化、from_env Bearer+headers 与坏 headers、adapter 权限门/错误映射/observe；`test_graph_loader.py` +5（http params JSON 装配+插值/坏 JSON/非对象/shop 分派不回归/注册表含 http）；`test_api_demo.py` +5（mock orders 401/200、receipt 回显、临时端口真实 uvicorn 线程父图端到端 GET+POST、401 仍 SUCCESS）；`test_harness_contract.py` +1（list_adapters 续心跳）；`test_nl_generate.py` +1（prompt 广告已注册工具）。前端 vitest +3（`lib/adapters.test.ts` 分组/权限幂等标记/healthy 过滤） | ✅ 后端 209 passed/8 skipped、前端 vitest 49（6 文件）、`pnpm build` 通过；浏览器对真实 :8000+:5174 实测 GET 200/POST 插值回显/401 SUCCESS/坏 params FAILED，零应用控制台错误 |
| 2026-09-15 | Phase 2 DB 适配器 + 消息适配器（04 §4.7/§4.8，U26/I12-I14） | `tests/test_database_adapter.py`（35 用例，全部注入 SQLite StaticPool 内存 engine，零真实 PG）：seed 两笔订单/命名绑定参数/limit 截断与缺省 500/query 不产生写、坏 sql→DB_SQL_ERROR、缺 sql/坏 params/坏 limit 参数化、insert rowcount 后 query 可见/update、reseed 复原、未 seed engine 缺表、from_env 未配置返 None/sqlite 文件/未知 scheme ValueError、密码脱敏（masked URL 含 `***` 不含明文）、能力声明与 read/write 权限门+审计、unknown capability、未注入 client fail-closed DB_NOT_CONFIGURED 与 observe unconfigured/last_operation；`tests/test_message_adapter.py`（21 用例）：字符串/数组 to、记录形状（uuid/sent_at）、21 收件人拒绝、email 缺 `@` 拒绝、非 email 渠道跳过、必填校验参数化、reset、write 权限门+审计、unknown、observe 计数；`test_graph_loader.py` +4（database JSON 透传+插值绑定参数过滤订单、坏 JSON INVALID_PARAMETER、message/send 透传且服务落记录、注册表含 query/execute/send）；`test_api_demo.py` +4（/api/adapters 含 database/message、query 端到端返两笔订单、execute 可见+reset reseed、message/send 插值后 GET /api/demo/messages 可见+reset 清空）；`test_nl_generate.py` 扩断言（query/execute/send/绑定参数）。前端仅占位符示例（vitest 49 不变） | ✅ 后端 273 passed/8 skipped、前端 vitest 49（6 文件）、lint/build 通过；浏览器对真实 :8000+:5174 实测选择器 database（query read·幂等/execute write）、message（send write）分组与四类占位符、query 两笔订单、execute INSERT 后三行 reset 复原两行、send 消息可见 reset 清空、坏 JSON FAILED INVALID_PARAMETER，零应用控制台错误 |
| 2026-09-15 | Phase 2 单步调试与断点 v1（04 §5.12，U29/I17，逐步/断点/条件/只读变量） | `tests/test_debugger.py`（11 用例，新包 debug）：初始 step 模式首节点即停、step 连续暂停且节点序一致、continue 后 step 关闭仅断点再停、无条件断点 reason=breakpoint、条件断点真路 reason=condition/假路不停、ConditionEvalError fail-safe 不命中不抛出、stop 置 cancelled 抛 DebugStopped、重复 resolve 409（首决生效）、并行两线程经会话锁串行暂停（序列不交错）、reset 释放全部等待、paused globals/outputs 深拷贝快照与恢复后状态隔离；`tests/test_api_demo.py` +7（I17：debug 流式跑 trigger→condition→notify 小图 paused→resume step 序列与执行序一致、continue 后仅断点停、条件断点真假两路、stop 收 stopped 帧且无 result、未知 token 404、重复 resume 409、未知 node_id/坏表达式/坏 breakpoints/同步 /run 带 debug 四类 422、reset 释放暂停线程后可再跑；按 TestClient portal 语义以独立线程轮询 `GET /api/debug` 驱动 resume 决策）。前端 vitest +4（`store/__tests__/editorStore.test.ts` +2：断点 toggle/表达式/清空、删节点清理与 loadGraph 重置；`lib/__tests__/apiClient.test.ts` 新增 2：paused 帧不被误判为 result、请求体携带 debug.breakpoints、stopped 帧以 DebugRunStoppedError reject） | ✅ 后端 329 passed/8 skipped（基线 311，+18）、前端 vitest 56（8 文件，+4）、oxlint/tsc/build 通过；浏览器（chrome-devtools 对真实 :8000+:5174）六场景实测：① sql-query-notify 调试启动即停 trigger-1（单步），连续「下一步」trigger→query→condition→notify-no 逐帧推进、快照含上游产出、result 后工具条关闭；② 条件断点 `amount > 100` 真路「继续」停 condition-1（条件）、`> 9999` 假路直达结束、清空表达式后 reason=断点，变量过滤浅过滤 globals/outputs；③ approval-timeout-reject 暂停在审批节点前、下一步后审批 Modal 照常弹出（本轮 10s 超时自动拒绝走 rejected-msg，继续后正常完成）；④ 停止收 stopped 帧、日志「调试已停止（无运行结果）」、无 result 弹窗无错误 Alert、按钮复位；⑤ 节点红点 toggle/条件白心/属性面板坏表达式中文报错/硬刷新断点清空（会话级证据）；⑥ 暂停中 /api/demo/reset 释放会话（GET /api/debug 即时为空、前端关闭工具条）随后普通运行正常完成；控制台零错误（另发现 sql-query-notify 模板头部运行输入未提供 min_amount 致 query 参数非合法 JSON 的既有问题，与调试链路无关，不在本项范围） |
| 2026-09-15 | Phase 2 流程模板库 v1（04 §5.10，U27/I15，5 个内置只读模板） | `tests/test_templates.py`（6 用例）：5 个 id 唯一且恰为 refund-auto/http-orders-branch/sql-query-notify/sql-approval-write/approval-timeout-reject、逐模板 parse_graph + validate_graph==[]、node_count 与实际一致、tool_call config.tool ⊆ 9 个已注册 demo 工具、name/description/tags 非空、get_template 未知 id 返 None；`tests/test_api_demo.py` +4：list 投影恰为 `{id,name,description,tags,node_count}` 不含 graph 且顺序固定、detail 含 graph 且未知 id 404、5 个模板 graph 逐个 POST /api/graphs 保存+compile 200、reset 后目录仍在。前端仅列表/加载交互（vitest 49 不变） | ✅ 后端 283 passed/8 skipped、前端 vitest 49（6 文件）、oxlint/build 通过；浏览器（chrome-devtools 对真实 :8000+:5174）实测「从模板新建」Modal 列出 5 模板（节点数 3/5/5/4/4、Tags、描述、整画布替换警示），sql-query-notify/sql-approval-write/approval-timeout-reject 加载后画布节点边与属性面板 config（condition 分支、human_approval 双目标与超时策略、绑定参数 JSON）完整且配置校验通过；urllib 端到端实跑：五模板保存+编译全 200、sql-query-notify（min_amount=1000）走「有大额订单」分支 row_count=1、approval-timeout-reject 无人操作 10.0s 超时 rejected 走拒绝侧、http-orders-branch HTTP 200 走成功分支，三条 message/send 记录可见，reset 后模板仍 5 个而消息清空；控制台零错误零告警（含规避 antd v6 List/Space 弃用） |

| 2026-09-15 | Phase 2 基础监控告警 v1（04 §5.13，U30/I18，运行/节点指标+站内告警） | `tests/test_monitoring.py`（20 用例，新包 monitoring）：extract_node_results 两失败形状（output.status=="failed" 折叠 / result.status=="FAILED" 数据化，含 error 兜底文案）+未知 output 键跳过、percentile nearest-rank p50/p95/空集、summarize 全局+per_graph+失败节点 Top（计数并列按末次时间）、run_error 仅未捕获异常/健康运行零事件、node_failed warning 消息列节点、consecutive 阈值触发与之后每次合并、failure_rate 窗口/min_samples/比率、store 健康指标、streak 三连败合并+健康重置、resolved 后新发而 acknowledged 仍合并、ack/resolve 404/409 语义、ring 200 驱逐保新、runs graph_id/limit 过滤、规则更新/关闭生效/非法 ValueError/reset 回默认、validate_rules 中文错误聚合与 rules_from_raw；`tests/test_api_demo.py` +9（I18）：sync 健康运行 metrics+run 投影、stream FAILED 节点 node_failed 告警、monkeypatch run_graph 异常 sync 冒泡+stream error 帧均记 run_error、三连败 consecutive count=1/node_failed count=3 合并+健康重置 streak、runs 过滤 limit 422 与 alerts status 422、rules GET/PUT 422 聚合+阈值改 1 即时触发且关闭规则静默、ack/resolve 404/409、reset 清空且规则回默认、debug stopped 运行不入记录。前端 vitest +3（`lib/__tests__/monitoring.test.ts`：runHealth 三态/formatDuration/中文映射） | ✅ 后端 358 passed/8 skipped（基线 349，+9）、前端 vitest 59（9 文件，+3）、oxlint/build 通过；浏览器（chrome-devtools 对真实 :8000+:5174）实测：① 空态 0/0/0、成功率—、默认规则 3/20/5/0.5；② refund-auto 健康两跑 + sql-query-notify 三连败后指标 5/2/3、成功率 40.0%、p50/p95 有数；③ 告警表连续失败 critical+节点失败 warning，node_failed count=3 合并；④ 运行行展开逐节点表显示 query-1 失败「params 不是合法 JSON」；⑤ 确认→已确认且确认钮禁用、关闭→已关闭两钮禁用；⑥ 规则阈值保存成功提示；⑦ reset 后运行/告警清空、阈值回 3；⑧ debug 停止会话监控 0 新增；控制台零错误（修复 antd6 Statistic valueStyle / InputNumber addonAfter 弃用） |

| 2026-09-16 | Phase 2 多租户权限 v1（04 §5.14，U31/I19，账号登录+端点级 RBAC+租户分区） | `tests/test_iam.py`（17 用例，新包 iam，U31）：种子认证成功/坏密码/未知用户、Principal 形状与租户账号全集（t1 admin-a/operator-a/viewer-a、t2 admin-b 四账号）、SessionStore issue→principal_for_token→revoke 生命周期与坏 token/重启式清空、can 角色矩阵（viewer read 仅、operator operate、admin administer 含向下兼容）、TenantRegistry 惰性创建与同租户单例/跨租户独立实例（graph-N 各从 1 起、互不可见）、reset_tenant 清图/消息/审批/调试/监控（规则回默认、计数器保留）且录制/反馈保留、跨租户审批与调试 token 404 语义经注册表隔离；`tests/test_api_auth.py`（11 用例，I19）：无 token/坏 token 401、viewer 建图/PUT 规则/reset 403 且 GET 200、operator 可运行可告警处置但 reset/规则/反馈列表 403、admin 规则/reset/反馈列表放行、viewer 可 POST feedback、A 建图 B 列表互不可见且直访 404、两租户监控运行/规则阈值互不可见、登录/me/登出链路、reset 不清录制与反馈。`tests/conftest.py` 新建 autouse fixture 为模块级 TestClient 默认注入 t1 admin Bearer（358 旧用例零语义改动）。前端 vitest +5（`lib/__tests__/auth.test.ts` 3：会话存取/坏 JSON/角色矩阵与中文角色名/401 事件；apiClient 401 分流与 Bearer 注入 2），10 文件 | ✅ 后端 377 passed/8 skipped（基线 358，+28）、前端 vitest 64（10 文件，+5）、oxlint/tsc/build 通过；浏览器（chrome-devtools 对真实 :8000+:5174）八场景实测：① 未登录被拦回登录页、坏密码中文 Alert、admin-a 登录后硬刷新会话保持；② A 租户 reset 后建图运行、message/send 真实投递在 /api/demo/messages 可见、流式失败运行产生 open 告警、监控页 4 runs/2 不健康/50%/2 alerts；③ admin-b 登录图/消息/告警/运行全空，直访 A 的 graph-1 → 404「Graph 不存在」，规则阈值与 A 互不可见（t1=5 时 t2 仍为默认 3）；④ viewer-a：PUT 规则/建图/reset 全 403、GET graphs 200、POST feedback 201 但 GET 列表 403，编辑器写按钮全部不渲染（仅从模板新建/导出/反馈），监控规则卡不渲染、操作列空；⑤ operator-a：reset/规则 PUT/反馈列表 403，告警确认→已确认、关闭→已关闭，录制 rec-1 创建 201；⑥ admin-a reset 后图 4→0、消息/运行/告警清零、阈值回默认 3，rec-1 与 feedback-1 保留；⑦ 退出登录清 localStorage 回登录页，裸 /api/graphs 401；⑧ 控制台零错误（自引入的登录页 `#f0f2f5` 硬编码改 token、antd6 Space direction→orientation 两处以独立提交修复） |

| 2026-09-16 | 设计态内核前置项（04 §4.9/§6.5，08 Phase 2 延伸立项；U32/U33/U34/I20，① Capability Schema 子集 + ②③ 前端拓扑作用域/L2 实时校验 + ④ 编译期复查 + ⑤ NL 参数填充尽力校验） | ① 3582bf5：`tests/` 既有九工具 schema 经 Capability 构造期白名单校验（U32，子集 20 keyword/未知 keyword ValueError）。②③ 896c124+23eeadc：`frontend/src/lib/__tests__/scope.test.ts`（23 用例，U33）——extractTemplateRefs 区间、菱形/链向可见性、output_schema 深层展开、loop 体区域与自身 continueExpression 例外、三码诊断、trigger payload/空 schema/parallel.result/subgraph.outputs 放行、多字段聚合；`adapters.test.ts` 夹具补 input/output_schema。④ b20c86a：`tests/test_graph_ref_validation.py`（9 用例，I20 编译期）——合法模板引用编译通过、未声明 global/缺节点/平行分支不可达/工具 output_schema 深路径 miss/trigger 越键 422 且中文含 REF_* 码、loop 自身条件与体内合法/退出后拒绝、parallel 动态键与 subgraph outputs 深路径放行；`test_graph_loader.py` 3 处 fail-soft 夹具改为静态合法的 `trigger-1.context.payload.missing`（运行期缺失保留原语义不变）。⑤ 34d5cb5：`test_nl_generate.py` +7（U34）——合法零警告、缺必填含节点 id/工具名、浅类型/enum 聚合、模板值跳过但计必填、裸插值非法 JSON 放行、未知工具/空 schema/oneOf 放行、additionalProperties:false 告警 | ✅ 后端 **410 passed/8 skipped**（基线 394；+9 I20、+7 U34）、前端 vitest **87（11 文件）**、`pnpm build`（tsc+vite）/oxlint 通过；浏览器（Claude Browser preview 对真实 :8000+:5174）实测：GET /api/adapters 九工具均投影 input_schema/output_schema（shop execute_refund output 含 order_id/status）；含坏引用图保存后 compile → 422 中文聚合两条（REF_NODE_NOT_FOUND + REF_PATH_NOT_FOUND）；NL 退款草稿 200 且 `paramWarnings: []`；编辑器载入 NL 3 节点草稿后 AI 节点变量插入清单仅 global.approval_limit 与 trigger context 四路径（下游工具不可见）；属性面板实时出现「引用的节点不存在」「输出中不存在该路径」错误列表且节点出现 atlas-node-error-icon 角标（tooltip 同文案）；控制台仅 422 资源错误一条（预期），无应用错误 |
| 2026-09-16 | M1 内置节点 Data Schema 提取（04 §4.9 前端 MetaSchema 扩展，U35/U36，零 UI 变化） | 新包 `frontend/src/lib/schemas/`：`metaSchema.test.ts`（U35①⑤，20 keyword 白名单 + 四 `x-*` 位置/类型、required 须在同 schema properties 声明、x-outputSchema 仅根且内部拒任何 x-*、非法形状参数化拒绝）、`nodeSchemas.test.ts`（U35②③④，九类注册可取、x-outputSchema 与 scope.ts 投影逐字段一致含 parallel 无动态 result/subgraph outputs 无根下键、默认 config 仅触发 required/pattern）、`dualRun.test.ts`（U36①②③④，SchemaRegistry get/未知 kind/坏版本/九类注册序，九类节点逐例双跑等价矩阵覆盖全部既有 catalog 失败/边界/成功用例：wait 0/-1/601/1.5、human 9/3601/1.5、loop 0/101/1.5、trigger oneOf 条件必填、双 target/分支唯一性/表达式语法/subgraph 空输入键等 `covered:false` 手写规则显式排除不分歧，validateNode 失败路径 console.error 零调用） | ✅ 前端 vitest **189（14 文件，基线 87/11，+102）**、oxlint/tsc/build 通过，每原子提交过门；后端零改动、零新依赖；浏览器（chrome-devtools 对真实 :8000+:5174，U36⑤）admin-a 登录后 demo 图 + 合成拖入其余六类节点凑齐九类，九个属性面板逐一渲染：已配置 trigger/ai_decision/tool_call 与默认 wait 零错误，condition/loop/parallel/subgraph/human_approval 错误条目与手写中文文案逐条一致，控制台零错误零告警（含零 `[M1 双跑分歧]`），零视觉差异；手写 L1 校验本期保留（迁移观测期，迁完一个删一个） |

| 2026-09-16 | M2 L1 字段校验扶正 + 结构化诊断 + 422 locations 侧车（04 §6.5/06 §6.13，U37/U38） | M2 四个代码原子提交（234a95f/30156a3/d0b2e33 前端、fbd9f77 后端）：① 234a95f diagnostics 类型+rank+L1 扶正（`lib/validation/`：diagnostics/l1/markers，FIELD_* code 全集、点号路径转 RFC6901、中文文案零变化）；② 30156a3 scope L2 升 layer:'template' + validateGraph 聚合；③ d0b2e33 PropertyPanel/AtlasNode 切 Diagnostic[]、validateNode:string[] 下线、renderMarkers 预留挂点；④ fbd9f77 后端 `validate_graph_report` 配对核心 + `_Issues` 下标对齐 + compile 422 稀疏 locations（字段错带 pointer、节点拓扑仅 nodeId、图级/不可达无条目、子图子错误归顶层父节点），`tests/test_validation_locations.py`（10 用例，U38）；⑤ 浏览器九节点冒烟（本行）。vitest **168（17 文件，基线 M1 189 中迁移观测期双跑用例随扶正删除净减、Diagnostic/rank/markers/validateGraph 新增）**；后端 **420 passed/8 skipped**（基线 410，+10 U38，detail 中文逐条字节零回归） | ✅ 前端 oxlint/vitest/build、后端 pytest 每原子提交过门；浏览器（chrome-devtools 对真实 :8000+:5174）admin-a 登录实测：退款 Demo 三节点（trigger/ai_decision/tool_call）选中均「配置校验通过」、节点无角标；真实拖拽补齐其余六类凑齐九节点，loop/parallel/condition/subgraph/human_approval 节点均显红色「!」角标（默认 wait 合法无角标），逐一点击属性面板：loop 3 条（继续条件/循环体/退出）、parallel 5 条（两分支名+目标+汇聚）、condition 4 条（表达式/分支名/目标/默认分支）、subgraph 1 条（必须选择引用的已保存子图）、human 3 条（审批说明/通过/拒绝目标），bullet 文案与排序（rank）符合契约；「编译并运行」POST /api/graphs 实测 422：26 条中文 detail + **20 条 locations 下标对齐**（字段错 nodeId+pointer 如 `/branches/0/expression`、每节点拓扑错仅 nodeId、6 条不可达图级错误无条目），前端继续按 detail 文案回显（M2 不消费侧车）；控制台零应用错误（仅 1 条 antd addonBefore 既有弃用告警）；截图 `docs/assets/m2-nine-node-smoke.png` |

| 2026-09-17 | M3 WidgetRegistry + FormRenderer 工具 params 表单化（04 §4.10，U39） | 五提交 2de5053/003d19b/8d095a0/341b90e/6d4a863：`registry.test.ts`+`widgets.test.ts`（八控件可注册可取、同名覆盖、未注册抛错、未知 widget 名降级 json）、`resolveWidget.test.ts`（选择序与全降级情形）、`formTree.test.ts`（结构树：properties 顺序/required/pointer、keyvalue/array/嵌套、值形状不符按空容器、递归上限、工具来源 string 走模板控件；不可变更新：路径浅拷贝、入参不变、未声明键保留、数组增删、键值改名保持顺序/冲突 no-op、stringify 往返；`splitTokenSegments` 排序/越界/重叠/无标记）、`toolSchemas.test.ts`（第二来源 `<adapter>/<tool>`、持引用不复制、空 schema/未注册不可表单化）、`params.test.ts`（空文本→`{}`、裸 `{{}}` 整串→null、非对象→null、回写往返）、`nlWarnings.test.ts`（`节点「id」` 前缀归类、相似前缀不误归属、非阻塞 warning 无 pointer）、`l1.test.ts` 增 `validateParamFields`（required/enum/range/nested additionalProperties）、`editorStore.test.ts` 增 nlWarnings（写入与换图清空） | ✅ 前端 vitest **243（24 文件，立项前 189/21）**、oxlint 0/0、tsc+build 每提交过门；后端零改动、零新依赖。浏览器（Playwright 对真实 :8000+:5174，admin-a 登录）实测：默认 tool_call-1（shop/process_refund）order_id/note 模板文本框 + action enum Select；database/query sql（模板文本框+字段内变量补全追加 `{{global.approval_limit}}`）/params（无类型→JSON 降级）/limit（number），「导出 Graph JSON」为 `{"sql":"…{{global.approval_limit}}","limit":42}`——**config.params 仍是 JSON 字符串**；http/request method enum Select / headers 键值行可添加 / body JSON 降级 / timeout number（`0` 触发「数值超出允许范围」）；message/send `to`（oneOf）降级 JSON 文本框、其余字段走模板控件；加载 sql-query-notify 模板后 query-1 停留旧 JSON 文本框（裸 `{{}}` 整串不部分解析）、旧占位符与「插入变量引用」保留；控制台零错误零告警；截图 `docs/assets/m3-params-form-smoke.png`。金链 API 复核：sql-query-notify 保存（graph-2）→ 编译 200 → 运行（inputs.min_amount=1000）trace 四节点全 executed，零回归 |
| 2026-09-17 | M4 批 1 UISchema 最小子集 + 节点表单迁移二波 + dirty 骨架（04 §4.10，U40/U42，九提交 49cb692→30d5009） | uiSchema/CondResolver 单测（groups row/column、hiddenWhen 受控字段/判别值/非对象 fail-closed、按字段 key 匹配）、节点表单渲染基座（九内置控件+target-select 业务控件层、文案层）、HumanApproval/Trigger/Loop 迁移等价测试（字段/文案/分组并排/trigger oneOf 三态显隐且隐藏值保留/hideFields mode/integer precision）、`dirty.test.ts`（markConfigEdit 仅脏 L1、引用键/`{{` 值脏 L2、增删节点边置 L3、换图全量、markClean 不清 revision） | ✅ 前端 vitest **291（27 文件，批前 243/24）**、oxlint 0/0、tsc+build 每提交过门、后端零改动；浏览器（真实 :8000+:5174，admin-a）HumanApproval 11 项/trigger 8 项/Loop 11 项全过零控制台错误，截图 `docs/assets/m4-human-approval-form-smoke.png`、`m4-loop-form-smoke.png`；Wait 经判定保留手写（const + 禁用 event 项 + D19 Tooltip 无法 schema 表达） |
| 2026-09-17 | M4 批 2 前端 L3 预判 + 分层调度 + Problems 面板 + Parallel/Subgraph 迁移（04 §6.5/§4.10，U40/U41/U42，六提交 0dc9b7f/1468e18/2ad4177/06b62fc/ac29737/aaf50bf） | `l3.test.ts` 17 用例对拍共享夹具（11 结构域：合法/不可达/含环含 loop 白名单，消息字节对齐，UNREACHABLE loc 有意偏差）；`scripts/dev/generate_l3_fixtures.py` 后端实跑生成、后端 `tests/test_l3_fixtures.py` 重建 raw 图重跑双侧守；engine/useValidationEngine/validationStore 调度与记忆化单测（L1 同步/L2 防抖/L3 idle、structureSignature 命中、revision 防漏清）；Parallel/Subgraph 迁移等价测试（radio 长文案、target-select、branches 2-10 门控、saved-graph-select、inputs 键值行 variable-input rows=1）；后端 `test_human_approval_resolved_from_other_thread_routes_approved` flaky 改 2s 轮询（aaf50bf） | ✅ 前端 vitest **336（30 文件）**、oxlint 0/0、build 过；后端 **431 passed/8 skipped**（+11 夹具守）；浏览器冒烟：parallel 三分支增删门控/radio/target-select/红字聚合、subgraph saved-graph-select 下拉/入参键值行、L3 不可达实时入 Problems、点击条目居中+字段闪烁，零控制台错误，截图 `docs/assets/m4-parallel-form-smoke.png`、`m4-subgraph-form-smoke.png`；批末抓到 useSyncExternalStore getSnapshot 无限更新（缺省新数组）并修复（ac29737，单测环境不复现） |
| 2026-09-17 | M4 批 3 reverseDeps + quickFix v1 + 200/500 基准 + Condition 迁移 + 总收口（04 §6.5/§4.10，U40/U41/U42 转正式，五提交 4b15d95/875922b/f496062/79317e3/471b930） | `reverseDeps.test.ts` 13 用例（target 8 处/4 类与 schema x-ref 递归对拍、模板引用双类索引、removeDanglingRef 切片/清空、数组项 target 路径模式匹配）；dirty markNodeDeleted referrerIds 收窄 +1、scope REF_NODE_NOT_FOUND 挂 fix +2、editorStore updateNodeConfig/applyQuickFix +2；`bench/validation.perf.test.ts`（RUN_BENCH=1 门控，200/500 链式图 6 场景 warmup10+采样50，常规套件 2 skipped）；Condition 迁移 nodeForms 4 + uiSchema 空 label 1（数组显式空串标题隐藏、ArrayView/KeyValueView 不渲染孤立必填星号） | ✅ 前端 vitest **359 passed/2 skipped（32 文件，批前 336/30，净增 23）**、oxlint 0/0、tsc+vite build 过；后端 **431 passed/8 skipped**（零改动零回归）；浏览器冒烟脚本 `scripts/dev/m4_batch3_smoke.py`（CDP 真实 :8000+:5174，admin-a）实测：sql-query-notify condition-1 新表单（分支卡片/插入变量/target-select/默认分支/添加上不封顶/最后一个禁删）、添加第二分支填 `{{ghost-x.result.y}}` 后 Problems 出「引用的节点不存在」+ `/branches/1/expression` pointer +「删除悬空引用」按钮、点修复 expression 清空且诊断消失（L1 新分支 label/target 必填同显）、字段内 token 红字高亮，控制台零错误；截图 `docs/assets/m4-condition-form-smoke.png`、`m4-quickfix-smoke.png`；基准入 BENCHMARK.md（500 节点稳态单节点 L2 增量 p95≈10ms 低于一帧、冷启动全量 p95≈70ms 一次性，不引 Web Worker） |
| 2026-09-17 | M5a 进程内 Repository 重构（docs/24 §1，T18 后；零行为变化，`0358696`） | `tests/test_storage.py`（10 用例）：七实现满足 Protocol、Graph/Feedback 搬移后行为不变、reset 分档/StorageError；`storage/base.py` 七 Protocol、`storage/memory.py` 聚合 re-export、registry 消 import 环 | ✅ 后端 **441 passed/8 skipped**（431 基线零回归 +10）、`/api/*` 签名零变化、`import atlas.api.main` 无环、前端零改动、零新依赖 |
| 2026-09-17 | M6 Graph 版本化 + subgraph 钉版（03 graph_definition，ADR T19，D21/D32 部分取回，U46，`1f49644`） | `tests/test_versioning.py`（4 用例）：发布快照不可变+草稿隔离/list_versions/clear、未知图 KeyError、subgraph 递归钉版跨版本隔离、publish/versions/@vN/compile/run 端点 | ✅ 后端 **445 passed/8 skipped**（+4）、「保存即 latest」零回归、发布/版本前端 UI 随 M9、零新依赖 |
| 2026-09-17 | M5b PG 持久化 + 中断落库 + 恢复扫描 + GET /api/runs（docs/24，T18 选 B，D19/D20 取回，U43/U44/U45；五提交 `bf185ed`/`b4abb59`/`280493e`/`1c1539a`/`f6a33e7`） | 进程内全量 + Docker PG integration（`ATLAS_RUN_INTEGRATION=1`，13 passed）：002 九表 DDL、storage/pg 六 store、frame 帧序列化+尾图续跑、recovery 启动恢复扫描器、RunRepository/runs 状态端点、reset 分层、强杀重启同 token 决策续跑 | ✅ 进程内 **450 passed/13 skipped** 零回归、Docker PG **13 passed**、U43/U44/U45 转正式、脚本实测「挂起→强杀重启→同 token 决策→completed」、前端零改动、复用 psycopg 零新依赖 |
| 2026-09-17 | M7 多 Bot 任务总线 + 退货退款沙盘（19 §2.2.2/§2.3.1/§2.4，ADR T20，D31 沙盘期，U47/U48；三提交 `0f934d6`/`ff4acf9`/`e46fc09`） | coordination 信封 Envelope/状态机/幂等去重返首结果、skills Skill/Bot/Coordinator + logistics 假物流、shop RefundOrder version+CAS、沙盘 dispatch→join→未签收禁放款→escalate、`/api/tasks` 端点（+13：4 coordination/3 skills·CAS/4 沙盘/2 端点） | ✅ 后端 **463 passed/13 skipped**（+13）、退货沙盘端到端跑通、幂等重放/CAS 冲突放弃/超时 fail-safe 齐备、前端零改动、零新依赖；假物流 Bot 不解除 D31 |
| 2026-09-18 | M10 span 级链路追踪 进程内部分（19 §2.3.4/§2.5，ADR T21，D34 部分，U49–U52；四批 `1f21b1a`/`89f5645`/`c227f0c`/`2b1dbe9`，收口 `0d4e3e9`） | 新包 `tracing/`（Span/Tracer/contextvars/to_tree 折叠，纯 stdlib）、run/node/tool/parallel/subgraph 埋点、SSE 三帧纯超集加 traceId/spanId/parentSpanId/graphVersion、coordination task span、RunRecord.trace_id、recording.normalize 剔 span 键 | ✅ 后端 **486 passed/13 skipped**、前端 **359 passed**/build 过（SSE 纯超集前端零改动）、零新依赖、不引 OTel SDK；跨服务/OTel 合流随 D5/D11，不解除 D34 |
| 2026-09-18 | M8 交互模板系统——审批卡片双向 Schema + web/im/email 三渠道渲染降级（20 §4.3，19 §2.2.3/§2.3.2，D33 沙盘期，U53/U54，T17 复查不重开；批 1 `2b35fc9`、批 2 `5f4384e`/`4b45571`、批 3 `25afa10`、收口 `94000de`/`486a591`） | 批 1 新包 `cards/`（内置只读 refund-approval 目录 + render_card 三渠道 + map_action_output，U53 20 用例）；批 2 human_approval 接 cardTemplateId（DSL 未命中 422、loader/broker 透传、M5b 中断帧 card_template_id 恢复重建、`GET /api/cards`+`/approvals/{token}/card`+decision actionId/form，U54 12 用例）；批 3 前端 card-select/卡片 bindings 纳 L2/CardRenderer（FormRenderer 第三来源 `kind:"card"`）/Modal 集成（vitest cards 7/scope 4）；收口修审批 Modal footer 空指针（94000de） | ✅ 后端 **518 passed/13 skipped**（+32）、前端 oxlint 0/0 / **vitest 370 passed/2 skipped**（M10 基线 359，+11）/ tsc+vite build 过、零新依赖、未新增 ADR；三渠道冒烟 `scripts/dev/m8_smoke.py` 对真实 :8000+:5174 全绿（API web/im/email 渲染断言 + 浏览器搭挂卡图选 12346，CardRenderer 五字段全插值/同意·拒绝按钮/决策续跑 completed/控制台零错误），截图 `m8-smoke.png`/`m8-card-config.png`；真实 IM/邮件外发随 D24/D20，不解除 D33 |
| 2026-09-18 | M9 入站 Router + 灰度发布 + 指标门控自动回滚（20 §4.4，ADR T22，D26/D32 部分取回，U55–U59；批 1-4 `c863f86`/`6e3f74a`/`60d885b`/`57b9966`/`1dc6314`/`1380cff`/`dcd40d1`/`7f2b503`/`d6223ed`/`d53de4b`/`d7f0c77`，收口 `7bdbe72`/`67f82fa`/`e014cdf`/`97c4afb`） | 新包 `routing/`（models 四段规则模型/router `resolve_version` 三段分桶 sha256 稳定桶/store rollout 状态机 configure·start·promote·rollback·resolve/gate `evaluate_after_run` 门控自动回滚，纯 stdlib）；`recording/gate.py` `run_release_gate` 发布前批量回放（registry/graph_resolver 由 API 层注入避免反向依赖）；`monitoring/business.py` 业务三率 extract_business + rollout_gate 告警 action；api：`PUT /api/graphs/{id}` 同图草稿更新（update_draft）、release-gate、publish `{gate}`、rollout GET/PUT/start/promote/rollback、run[/stream] 可选 event/releaseVersion、RunRecord.resolved_version/business、metrics business 全局/per_graph/per_version；前端 release.ts + 发布门禁 Modal + RolloutModal（四段规则/门控表单/状态机/流量计数/模拟事件）+ Monitoring 业务三率与告警 action 徽标；收口修 Editor 每次运行/录制 POST 新图致门禁筛不到用例（draftGraphId 复用同 id PUT） | ✅ 后端 **602 passed/13 skipped**、前端 **396 passed/2 skipped（34 文件）**、tsc/oxlint/vite build 过、零新依赖；Router 基准 10k resolves **p50 0.0041ms/p99 0.0450ms（约 242000 resolves/s）** 入 BENCHMARK.md；API 全链路沙盘 `scripts/dev/m9_smoke.py` **32 断言全过**（图 A 门禁+三段分桶、图 B 门控自动回滚）；浏览器真实冒烟全链路（门禁 1/1 ✓→v1/v2→灰度配置→start canary→模拟事件落 candidate→manual_escalation_rate 100%>10% 自动回滚→新事件回落 stable v1），控制台 **0 error/0 warning**，4 截图 `docs/assets/m9-{release-gate,rollout-config,rollout-autorollback,monitoring}.png`；event 路由绑定 rollout 生命周期（仅 publish 未 start → 409，已写入 03/12）；PG 路由/业务 JSONB 在 13 skipped 内、本环境无 DATABASE_URL 未验证（随集成测试） |
| 2026-09-19 | D26/D30 收尾小项打包（14 D26/D30 部分取回，U61–U65；立项 `d4cd42b`、D26-b `29bb3d9`、D26-a `8a37b4e`、D26-c `cea70f4`、D30-b `9a357df`、D30-a `47ecf48`） | D26-b 新建 `recording/snapshots.py`（collect_subgraph_snapshots 递归冻结 + inline_first_resolver），RecordingCase 增 `subgraphs` 纯超集，**单用例 replay 内联优先、发布门禁 gate 保持实时 resolver 不内联**（验当前 latest 草稿，坏子图应 block；订正立项原文）；D26-a `reports.report_to_csv`（stdlib csv/io，元信息+逐 case 行、BOM）+ `GET release-reports/{rid}/export?format=csv|json`（read/attachment/非法 422/404 照详情）+ 前端 apiClient 下载与 ReleaseModal「导出」列；D26-c `.github/workflows/release-gate-cron.yml`（周一 03:17 UTC cron + workflow_dispatch，起 uvicorn→登录轮询→d26_smoke，失败传 uvicorn.log；cron 仅 main 生效）；D30-b 后端 `_validate_data_dependency_cycles`（GRAPH_DATA_CYCLE error 进编译 422，边为通过可见性的模板引用、loop 自身计数不纳边）+ 前端 scope.dataDependencyEdges/l3 同构，engine 新增 graphDataSignature（结构+模板投影）使 L3 随模板重算；D30-a 纯前端 REF_TYPE_MISMATCH warning（tool→tool 单模板叶子标量比对，number 接受 integer/integer 不接受 number/object·array·oneOf·拼接·缺 schema 放行，不进后端 422），useToolInputSchemas + runL2/validateRefsAt 增参接缓存失效 | ✅ 后端 **628 passed/13 skipped**（D30-b +2）、前端 **418 passed/2 skipped（35 文件）**（l3 +3、engine 模板缓存契约改写为 2、scope 类型校验 +8、releaseApi +2）、oxlint **0 error（2 既有 warning）**/tsc+vite build 过、零新依赖、不新增 ADR；d26_smoke 本地端到端 **24 断言全过**（服务约 2s 就绪、SMOKE_EXIT=0），subgraph 内联由 pytest test_api_demo/test_recording 覆盖；**不解除 D26/D30**（影子模式/Mock/参数化/PG/跨图看板与 secret 受限来源/动态路径/深层展开/quickFix 联动仍缓做） |
| 2026-09-18 | D26 用例集报告 v1——批量回放报告沉淀 + 通过率历史趋势（14 D26 部分取回，U60；立项 `71153a8`、后端 `9dc9690`/`5c812c5`、前端 `f5c789c`） | 新包 `recording/reports.py`（ReleaseReport/ReportCaseRow pydantic + ReportStore 进程内 ring 100/租户：record/list_summary/get/reset，rr-N 租户级单调计数且 reset 归零，pass_rate=round(passed/total,4)、total=0 为 null，不进 Repository/不 PG 化照 RoutingStore 先例）；release-gate 端点沉淀 trigger=manual、publish `{gate:true}` 沉淀 trigger=publish-gate（通过/blocked 均沉淀，409 报告体带 id；skipped 也沉淀），GateReport 纯超集加 `id`、gate.py 保持纯函数不碰存储；两 GET `release-reports`（倒序摘要剥 cases）/`release-reports/{rid}`（详情含 cases，read 角色，图不存在/跨图/跨租户 404）；TenantServices memory/PG 两档挂 report_store、reset_tenant 清报告而录制用例保留；前端 apiClient ReleaseReport(+Summary) 类型与 list/get、`lib/release.ts` 结论/trigger 中文 meta 与 MM-DD HH:mm 纯函数、ReleaseModal ghost Collapse「历史报告（通过率趋势）」（Progress 成功绿/blocked exception 红、pass_rate=null 显「未覆盖」Tag、展开行懒加载详情嵌套逐例 ✓/✗/note 表） | ✅ 后端 **615 passed/13 skipped**（M9 基线 602，+13＝6 存储单测+7 API）、前端 **404 passed/2 skipped（34 文件）**（基线 396，+8 纯函数/端点）、oxlint 0 error（2 warning 为既有）/tsc+vite build 过、零新依赖、不新增 ADR；API 全链路沙盘 `scripts/dev/d26_smoke.py` **24 断言全过**（两触发沉淀/倒序/摘要剥 cases/详情/跨图 404/图不存在 404/reset 清图清报告留用例）、`scripts/dev/d26_ui_seed.py` 供浏览器造数；真实 :8000+:5174 浏览器冒烟（6 条历史：未覆盖/通过/blocked × manual·publish-gate，展开 blocked 行懒加载逐例差异 note），2 截图 `docs/assets/d26-report-{trend,detail}.png`；**不解除 D26**（影子模式/Mock 工具响应/用例编辑参数化/subgraph 快照内联/PG 持久化多租户/跨图看板/定时 CI/导出仍缓做，触发条件不变） |
| 2026-09-19 | A+B 打包：Phase 2 运行时语义三项（A 组 stdlib/纯引擎）+ D30 作用域余部三项（B 组纯前端 L2/quickFix）——D15 表达式函数库/D17 loop break·continue/D18 parallel any_success；parallel.result 汇聚点可见性/subgraph.outputs 深层展开/改节点 id 引用联动 | 后端 `tests/test_conditions.py`（新增）+ `test_validation_locations.py`、`test_graph_loader.py`（`_loop_break_graph` 3 + `_any_success_graph` 3 + 子图深层编译拒绝）、`test_graph_dsl.py`（break 拓扑/非法策略改 wrong_strategy）、`test_graph_ref_validation.py`（result 汇聚可见性）；前端 `conditions.test.ts`（同构对拍）、`scope.test.ts`、`reverseDeps.test.ts`、`editorStore.test.ts`、`forms/nodeForms.test.ts`（any_success 枚举文案） | ✅ 后端 **645 passed/13 skipped/1 warning**、前端 **vitest 436 passed/2 skipped（35 文件）**、oxlint **0 error（2 既有 warning）**、tsc+vite **build 通过**、`d26_smoke.py` **24 断言全过**（发布/回放主链路零回归）；立项 docs-only `646ca79` + 六原子 `ac8c718`(D15)/`ee534ef`(B1)/`25db6df`(B2)/`8496b2d`(B3)/`5f87654`(D17)/`f45daa9`(D18) + build 类型修复 `4b9396e`；U66–U71 转正式；零新依赖、不新增 ADR、**不解除 D15/D17/D18/D30**（now()/today() 随时钟批、D14 LLM 分支/D16 foreach、D19/D20/D27、D21 子图契约〔含前端子图结构拉取接入 B2〕、secret/env/session 受限来源仍缓做） |
| 2026-09-19 | M11 记忆 / 长期上下文（docs/26，ADR T23，**U72–U99 已转正式**；四批 2026-09-19 落码 `5441902`/`58d936c`/`73e53bd`/`75c4150`） | 批 1 U72–U83（`tests/test_memory_store.py` 18 测：embedder 确定性/同文本同向量/L2 归一化、cosine 自相似与无关低（无共享 token 小负余弦断言 abs(c)<0.2）、中文 bigram 召回、remember 字段与 mem-N 计数、recall 排序/top_k/kind 过滤/scope 子集/min_score、confidence 默认与越界、delete 存在与否、clear、content 空/超长非法）；批 2 U84–U92（适配器两能力发现且 schema 过白名单、remember 需 WRITE/recall READ 幂等、适配器 remember→recall 端到端、GET /api/memories 列表与 kind 筛选、search 命中/score/q 空 422、DELETE admin only operator 403/跨租户 404、两租户隔离、reset 清空）；批 3 U93–U96（**integration** Docker pgvector：006 建表/vector(256)/索引、PgMemoryStore 与进程内 recall 对拍、scope @>/kind/delete/clear、ATLAS_STORAGE_BACKEND=pg 装配）；批 4 U97–U99（前端 `lib/__tests__/memoryApi.test.ts` 6 测 apiClient 三方法、`lib/__tests__/memory.test.ts` 7 测 kind 标签/颜色·score·scope·时间纯函数；U99 真实浏览器冒烟：列表 fact 蓝/preference 紫 Tag、语义搜索 score 进度条降序〔顺丰 57%＞邮件 35%〕、kind 筛选、admin Popconfirm 删除 4→3、viewer 无删除列，3 截图 docs/assets/m11-memory-*.png，零应用错误）。**结果：后端 673 passed/17 skipped（给 DATABASE_URL 时 PG 套件 9 passed 含本批 4）、前端 449 passed/2 skipped、m11_smoke 两档全过（recall score 0.434524 两档一致）、d26_smoke 24 断言零回归 | ✅ 已转正式（2026-09-19） |
| 2026-09-19 | C 包 可注入回放时钟 + today/now datetime 函数（docs/27 §2，14 D15 余部取回，**U100–U113 已转正式**；feat `3415377`） | 后端 `tests/test_clock.py` 9 端到端（now_override 决定 now()/today() 分支、naive 当 UTC、**子图重入继承同一冻结时钟**、clock_anchor 的 recorded_at 优先/created_at 回退/naive/缺失/非法、回放冻结到历史 recorded_at 走历史分支、RecordingStore.add 入库 stamp 使 recorded_at==created_at）+ `test_conditions.py` +5（注入时钟稳定与 year/month/day 接 now、datetime 构造与 hoursBetween 含秒/反向、hoursBetween 接 date 与 daysBetween 接 datetime、date/datetime 跨类型有序比较报错而 ==为 false、静态校验裸 today/now 非布尔·datetime arity 5-6·非法小时/日期/非整数折叠·hoursBetween 类型）+ PG 集成 `test_storage_pg_integration.py` +1（recorded_at 缺省同 created_at、显式锚点 get/list 往返，给 DATABASE_URL 跑）；前端 `conditions.test.ts` +4（today/now/datetime/hoursBetween 合法比较通过、裸 today/now 非布尔与 datetime arity、非法 datetime/日期/非整数常量折叠、hoursBetween 类型与 date/datetime 跨类型有序比较拦截）。**结果：后端 687 passed/18 skipped（+14；新增 PG 测无 DATABASE_URL 时 skip，给库 PG 集成 10 passed）、前端 453 passed/2 skipped（+4）、pnpm build 过、oxlint 0 error（2 warnings 基线既有）、内存档 d26_smoke 全过、m11_smoke 内存/PG 两档 M11_SMOKE_ALL_PASS；PG 档 POST /api/recordings 富字段 graph_id/subgraphs 既有 500 缺口归属 D26 用例 PG 持久化（见 docs/14 D26），HEAD 5e677ce 即如此、非本包引入，C 无浏览器动作** | ✅ 已转正式（2026-09-19） |
| 2026-09-19 | A 包 子图内部事件 SSE 上屏 + 子图内 human_approval 交互（docs/27 §3，14 D21 进程内形态取回，**U114–U123 已转正式**；feat `e594a4b`） | 后端新增 `tests/test_subgraph_events.py` 8 测（U114–U120）：内部 node_start/node_end 带 subgraphPath、顶层事件无 path 键、子层 run_end/result 被吞（整图仅 1 个 run_end）、嵌套路径累加 `[outer,inner]`、子图内 human_approval 经共享 ApprovalBroker 全局 token 后台 resolve 放行、fail-safe 直接单测 `_execute_subgraph`（resolver=None 折叠 failed 且零帧外泄）、collect_steps 排除内部 node_end、父 outputs 形状不变；更新 `test_graph_loader.py` 既有子图测试旧「事件不外泄」断言为上屏带 path/仅节点级/run_end 唯一。前端新增 `lib/subgraphEvents.ts` 纯函数 +3 测（U121 isSubgraphInternal/prefix 单层 `[sg-a] `·嵌套 `[sg-a › sg-b] `·顶层空、label 无尾分隔，U122）、`recordings.toSteps` 跳过内部 node_end +1 测（嵌套同样跳过、父 subgraph 节点自身 node_end 保留，U123）；`pages/Editor.tsx` 内部节点不写父图状态、日志加路径前缀、审批 Modal 显 `[path] nodeId` 与归属副文案。真实 HTTP SSE 端到端（内存档临时脚本）：建含 human_approval 子图+父图，stream 抓到带 approval+subgraphPath 的内部 node_start 后同账号 POST decision approved→200，断言内部事件全带 path/仅节点级/run_end 恰好 1/顶层无 path/result completed/走 approvedTarget。**真实浏览器冒烟**（临时注入只读 catalog 模板，截图后已 `git checkout` 还原、未提交）：父图 p-trigger→sg-sub(引用 graph-1)→tool-after，子图 c-approve 挂起时审批 Modal 显 `[sg-sub] c-approve` + 副文案「子图内审批（所属 subgraph 节点：sg-sub）」，点同意后调试控制台 18 条事件完整呈现 c-trigger/c-approve/c-yes 均带 `[sg-sub] ` 前缀、`⏸ 等待人工审批`/`✓ 人工审批：通过（人工）→ c-yes`、父 sg-sub 节点「子图完成 graph-1（成功）」与顶层 p-trigger/tool-after 无前缀、运行结束 completed，2 截图 `docs/assets/a-subgraph-approval-modal.png`/`a-subgraph-path-logs.png` | ✅ 后端 **695 passed/18 skipped**（C 基线 687，+8）、前端 **457 passed/2 skipped（38 文件，+4）**、pnpm build 过、oxlint 0 error（2 warnings 基线既有）、内存档 d26_smoke 全过、m11_smoke 内存档 ALL_PASS；零新依赖、无新端点（复用共享 broker 与既有 decision 端点）、不新增 ADR；**不解除 D21**（子图内单步断点、持久化/多实例审批随 D20、子图市场/共享/跨运营体/嵌套可视化编辑器/升级回归 UI 仍缓做） | ✅ 已转正式（2026-09-19） |
| 2026-09-20 | B 包 协作式运行急停 + hitCount/logpoint 断点 + 暂停 globals 改写续跑（docs/27 §4，14 D27 进程内三项取回，**U124–U138 已转正式**；feat `f9a1301`） | 后端新增 `tests/test_run_cancel.py` **17 测**：U124 broker register 幂等返 Event/cancel 置位（无句柄 False、重复置位 True）/unregister/reset；U125 普通流节点边界（emit start 后）抛 RunCancelled 收敛 cancelled；U126 parallel 分支传播（全分支不继续、网关不重复汇聚）；U127 `_execute_subgraph` fail-safe 前 `except RunCancelled: raise` 穿透（不折叠成节点 FAILED）；U128 logpoint 命中发一帧 debug_log（hits/message 原样）不暂停且 run completed；U129 hitCount 仅每第 N 次命中（hits%N==0）暂停；U130 条件断点仅表达式为真才 bump 计数、表达式异常 fail-safe 不暂停不计数（记 last_condition_error）；U131 resume globals 浅合并后 condition 按覆盖值路由到大额分支；U132 apply_overrides 拒非法键名/不可 JSON 序列化值/未知·已决 token（ValueError）；U133 端点链——cancel 在途 stream 发 cancelled 帧且 run/monitoring 记 cancelled（不触发 evaluate_after_run）、未知 run 404、已结束无句柄 409、viewer 403、调试 before_node 命中折叠 DebugStopped、调试流 stop 发 stopped 不发 cancelled、resume 非法 globals 键 422。前端新增 **15 测**：U134 `cancelActiveRun`（GET /api/runs 找本图 running/suspended 最新 run→POST cancel 返 runId、无在途返 null 且只查一次）；U135 streamRun 遇 cancelled 帧回调后抛 RunCancelledError、debug_log 帧正常 onEvent 转发且不影响后续 result；U136 editorStore setBreakpointHitCount（null/<1 归一 undefined）/setBreakpointLogMessage（trim 空白归一 undefined）且与 expression 共存互不覆盖（+3）；U137 resumeDebug 带 globals 时 body 含 globals、不带则不含（stop 不带）；U138 纯函数 `lib/debugOverrides.ts` parseGlobalsDraft（空白→{}、合法对象透传、非法 JSON/数组·标量·null 顶层拒、键名同后端标识符正则、下划线/字母开头通过）6 测。**真实 HTTP SSE 端到端**（内存档临时注入只读 catalog 三图，跑完 `git checkout` 还原未提交）：①wait duration sleep 中 cancel→200，sleep 结束于**下一节点 msg-1 边界**发 cancelled（不在阻塞中点强杀），状态 cancelled，流后再 cancel→409、未知 run→404；②首节点 step 暂停一次后 continue，msg-1 发一帧 debug_log（hits=1）不暂停、msg-2 completed；③暂停快照 globals、非法键 resume→422、覆盖 amount=9999 continue 后 condition 走 msg-big 无 msg-small。**真实浏览器三场景冒烟**（admin-a，临时模板截图后还原）：急停 Header 危险「急停」→日志「已请求急停 runId（下一节点边界生效）→wait-1 等待完成 20 秒→节点开始 msg-1→运行已急停：msg-1（协作式取消，无运行结果）」（`docs/assets/b-cancel.png`，11 条）；属性面板开 msg-1 断点 + 填 logMessage，调试 continue 后「📝 msg-1 日志断点（第 1 次）：…」不暂停、completed（`b-logpoint.png`，13 条）；选演示订单 12347（初始 global.amount=128 本应走小额）暂停在 globals TextArea 改 amount=9999（`b-globals-edit.png`），continue 后 condition「大额→msg-big」、无 msg-small、completed（`b-globals.png`），排他证明 globals 写回影响分支 | ✅ 后端 **712 passed/18 skipped**（A 基线 695，+17）、前端 **472 passed/2 skipped（39 文件，+15）**、pnpm build 过（仅既有 chunk 体积提示）、oxlint 0 error/2 warnings（ReleaseModal/RolloutModal 既有基线，未新增）；零新依赖（纯 stdlib threading.Event）、无新 ADR；**不解除 D27**（持久化中断/多实例随 D19/D20/S1、断点落 Graph JSON/团队共享、变量变化历史/来源追踪、异常断点、子图内部断点、阻塞中点强杀、多会话锁粒度仍缓做） | ✅ 已转正式（2026-09-20） |

| 2026-09-20 | docs/28 批 1 D26 四项进程内取回（14 D26 部分取回，**U139–U148 已转正式**；fix `d1f455b`、feat `c7bf138`/`89e21fc`、前端 `0e8ebe7`，迁移 008） | ①PG 富字段：迁移 008 recordings +graph_id/+subgraphs（monitoring_runs +tool_calls 批 3 预留）、PgRecordingStore 四读写路径与进程内同超集、base 协议补 add/update_meta；**U147**（integration，默认 skip）富字段 graph_id/subgraphs PG 往返。②Mock 回放（`c7bf138`）：**U139** build_tool_mocks 无工具步骤返空；**U140** 仅收顶层 node_type=tool_call 步骤、node_id 重复 dedupe 保末、取入库原始未归一化 output；**U141** inputs_override 顶层键浅合并进用例 inputs（一次性不落库）＋非对象 422；**U142** 未注册 ghost 适配器排他证明（mock=false 真实跑 FAILED、mock=true 命中桩 matches 且 mocked_tools=['msg-1']、不触达 registry/不发 tool span·metric）；子图重入 run_graph 不透传 tool_mocks（loader 注释明示）、gate.run_release_gate 不引用 tool_mocks（发布门禁不接 mock）为结构保证，无独立用例。③用例编辑（`89e21fc`）：**U143** 进程内 update_meta 仅改 name/inputs、model_copy 原地保序、无有效字段返原 item、不存在返 None；**U144** PUT /api/recordings/{id} 200 返完整 case/404/422（空体·空名·超长·inputs 非对象）/steps·graph 快照不动/viewer 403；**U148**（integration）PG update_meta 动态 UPDATE 往返＋rowcount=0 返 None。④跨图看板（`89e21fc`）：**U145** ReportStore.list_all_summary 跨本租户全部图倒序、limit 默认 100/clamp 1-200、摘要去 cases；**U146** GET /api/release-reports 两图 3 报告倒序/limit clamp/非整数 422/viewer 可读。前端 `0e8ebe7` 未新增测试（项目无组件测试惯例）：apiClient 补类型与三函数，Editor 录制 Modal 加 per-case Mock Checkbox/入参覆写 TextArea/编辑面板（模块内 parseInputsObject 纯函数以 ok 判别联合收窄，规避 Record 索引签名致 error 为 unknown 的 TS2345），Monitoring 加跨图报告 Card；校验口径后端已覆盖。**真实浏览器冒烟（PG 档 admin-a）三场景截图**：跨图报告 Card 3 行倒序（未覆盖 0/0 放行·0% 0/1 阻塞红·100% 1/1 放行绿）`docs/assets/batch1-cross-graph-board.png`；用例编辑面板名称/入参预填＋不可改说明 `batch1-edit-panel.png`；勾 Mock 回放显「匹配」＋「Mock 1 工具」蓝标、trigger-1/msg-1 全一致 `batch1-mock-replay.png` | ✅ 后端 **720 passed/20 skipped**（B 基线 712/18，净增 8 个非 integration＋2 个 PG integration 默认 skip）；PG integration 显式 **8 passed**；前端 **472 passed/2 skipped/39 文件**（不增不减）、pnpm build 过（仅既有 chunk 体积提示）、oxlint **0 error/2 既有 warning**（ReleaseModal/RolloutModal set-state-in-effect，非本次引入）；d26_smoke 内存＋PG 两档 24 断言全过、m11_smoke 两档 ALL_PASS、PG 档新端点一次性 HTTP 冒烟 14 项全过；零新依赖、迁移 008 幂等已应用 atlas-pg、3 处 REST 变更（PUT 编辑、GET 跨图报告、replay 可选 body＋mocked_tools 纯超集）；**不解除 D26**（影子模式/线上旁路、子图快照多租户共享、报告 PG 化、报告删除、跨版本 diff 仍缓做） | ✅ 已转正式（2026-09-20） |

| 2026-09-20 | docs/28 批 2 D27 三项进程内取回（14 D27 部分取回，**U150–U165 已转正式**；feat `038dcaa`/`9b378c2`/`f6f7d30`、前端 `c13886c`） | ⑤变量历史 `tests/test_debug_history.py` **7 测**：U150 首暂停 changes 空且 since_nodes 正确、U151 第二次暂停记新增与变更键 old→new、U152 未变与删除键不记录、U153 resume 手动改写 seed 基线不记为变化、U154 历史上限 50 丢最旧、U155 不可 JSON 序列化值 fail-safe 跳过不抛、U156 paused 帧端到端携带 history。⑥异常断点 `tests/test_debug_exception.py` **5 测**：U157 未配 onException 立即返回（零行为变化）、U158 命中异常先 reason=exception 暂停且帧带 error、continue 后原样重抛、U159 stop 抛 DebugStopped、U160 无异常断点时错误按旧路径传播、U161 `_validate_debug` 拒非布尔 onException（422）。⑦子图内部断点 `tests/test_debug_subgraph.py` **4 测**：U162 step 子层暂停帧带 subgraphPath、U163 continue 命中子图内断点后命名空间恢复（回顶层帧无 path）、U164 子图 logpoint 发命名空间 debug_log、U165 子层 stop 穿透 DebugStopped 不折叠为 subgraph failed；旧 `test_debugger.py::test_subgraph_interior_is_not_paused` **契约反转**为 `test_subgraph_interior_is_paused_with_path`（step 序 trigger→subgraph→child-trigger→child-tool→tool-after，子层帧 path==["subgraph-1"]、顶层帧无该键）。前端 `store/__tests__/editorStore.test.ts` **+1**：toggleBreakpointException 纯异常建条目/关闭后空对象删除/与条件字段共存/关异常保留条件。**SSE 端到端（httpx 并发脚本）**：父图 step 顺序 p-trigger→subgraph-1→c-trigger(path)→c-tool(path)→ai-1、history 随帧递增；异常断点 continue 帧 reason=exception/error={ValueError,"could not convert string to float: 'abc'"}后 SSE event:error。**真实浏览器冒烟（PG 档 admin-a）三场景截图**：异常断点暂停 Card 红 Tag＋ValueError 红 Alert＋画布节点红点 `docs/assets/batch2-exception-breakpoint.png`；变量变化历史 Collapse（trigger 单步/ai 异常两条；真实节点不写 global，显「本次暂停无 global 顶层键变化」）`batch2-variable-history.png`；子图内部暂停标题 geekblue 路径 Tag `subgraph-1`＋橙色子层节点 `c-trigger` `batch2-subgraph-pause.png`（临时内置模板 zz-smoke-subgraph 与前端坏订单 BAD-01 截图后均已 `git checkout` 还原，模板恢复 5 个内置项） | ✅ 后端 **736 passed/20 skipped**（批 1 基线 720，净增 16）；PG integration **8 passed**（批 2 无 DDL，沿用迁移 008）；前端 **473 passed/2 skipped/39 文件**（+1）、pnpm build 过、oxlint **0 error/2 既有 warning**（ReleaseModal/RolloutModal，非本次引入）；d26_smoke、m11_smoke PG 档全过；零新依赖、无新 ADR；**不解除 D27**（暂停帧持久化中断-恢复/多实例、断点落 Graph JSON/团队共享、跨节点变量血缘图、忽略异常继续、阻塞中点强杀、多会话锁粒度仍缓做） | ✅ 已转正式（2026-09-20） |
| 2026-09-20 | docs/28 批 3 D28 两项进程内取回（14 D28 部分取回，**U166–U191 已转正式**；后端 `a318d97`/`11b1ba7`、前端 `4256326`、HTTP 冒烟 `dc46f20`） | ⑧适配器埋点 `tests/test_monitoring_tool_metrics.py` **10 测**：U166 `_tool_metric_event` SIMULATED 归一（不计 code）、U167 SUCCESS 取外层 action_status 回退 result.status 大写、U168 FAILED 携带 result.code、U169 未注册适配器 FAILED 但 error_code=None（无结构化 code）、U170 executor 真实工具分支计时 emit、U171 mock 命中不发 tool_metric、U172 子图内部 tool_metric 经命名空间白名单被吞（只采顶层）、U173 summarize_tools 按 tool 聚合 calls/failed/simulated/error_codes 与 nearest-rank p50/p95（SIMULATED 纳 calls 不纳 failed/分位、真实样本 0 分位 null）、U174 无 runs 时 tools 空形态、U175 RunRecord.tool_calls 默认 [] 纯超集；另反转 `test_tracing_loader.py::test_sse_frames_are_superset_with_no_new_event_types`（白名单加 tool_metric）、更新 `test_refund_e2e.py` 黄金序（工具节点 node_start→tool_metric→node_end）。⑨自定义规则 DSL `tests/test_monitoring_custom_rules.py` **12 函数/18 例**：U176 custom 默认 []/model_dump 承载/旧配置反序列化不 422、U177 合法 custom 校验通过、U178 custom 缺省与空数组合法、U179 含未知变量表达式静态 unknown 放行、U180 非法聚合 7 子例（空 cid/超长 cid/空 name/bad severity/enabled 非布尔/表达式语法错/顶层非布尔）、U181 重复 cid 与 custom 非数组、U182 rules_from_raw 缺省 enabled=True/severity=warning、U183 status=error/hasError 上下文命中且 rule_id=custom:{cid}/rule_name/severity 正确、U184 durationMs/failedCount 命中与阈值下不命中、U185 非布尔结果与运行期类型异常均 fail-safe 不告警、U186 disabled 规则不触发、U187 进程内 store 端到端落 Alert 带 rule_name、同 (rule_id,graph) 合并计数、跨图不合并。**PG integration（`test_storage_pg_integration.py`，+2→10）**：U188 test_tool_calls_roundtrip（monitoring_runs.tool_calls JSONB 往返、富字段/空图缺省 []）、U189 test_custom_rules_roundtrip（custom 随 monitoring_rules.config JSONB 往返、custom:{cid} 落 monitoring_alerts.rule_id TEXT、旧配置写回 custom 缺省 []）。**前端 +2**：U190 `lib/__tests__/conditions.test.ts` 锁定 DSL 白名单表达式（status/durationMs/failedCount/hasError 与未知变量静态放行、纯算术非布尔仍拒）、U191 `lib/__tests__/monitoring.test.ts` ruleLabel 优先 rule_name、PG null/缺省回退 rule_id。**HTTP 全链路冒烟** `scripts/dev/d28_smoke.py`（PG 档 admin-a，全过）：message/send 真实运行 3 次落 SUCCESS tool_calls、未注册工具落 FAILED（code 可空）、metrics.tools 聚合（calls/p50/p95）、PUT custom 规则往返、非法表达式 422、坏运行命中 custom:{cid} critical 告警（PG rule_name=null 回退 rule_id）。**真实浏览器冒烟（PG 档）两截图**：`docs/assets/batch3-tool-metrics.png`（适配器调用 Card：message/send 6/0、坏工具 3 失败红 Tag、P50/P95/错误码列；告警表首行 custom:d28-smoke-error 严重、消息含规则名）、`docs/assets/batch3-custom-rules.png`（规则 Card 自定义规则 List 编辑器：名称/表达式等宽输入/严重度 Select/启用 Switch/删除＋新增、可用变量与示例提示、内置四规则开关） | ✅ 后端 **764 passed/22 skipped**（批 2 基线 736，净增 28＝埋点 10＋DSL 18；另 2 个 PG integration 默认 skip）；PG integration **10 passed**（批 3 无 DDL，沿用迁移 008 monitoring_runs.tool_calls）；前端 **475 passed/2 skipped/39 文件**（+2）、pnpm build（tsc+vite）过、oxlint **0 error/2 既有 warning**（ReleaseModal/RolloutModal，非本次引入）；d26_smoke、m11_smoke PG 档全过；零新依赖、无新 ADR；**不解除 D28**（OTel/Prometheus/Grafana 正式栈、运行记录长保留/时间序列、Trace 时间线钻取、子图内与 Mock 工具埋点、规则图级绑定/静默/升级、外部告警出口 IM/webhook/邮件仍缓做） | ✅ 已转正式（2026-09-20） |
| 2026-09-20 | docs/28 批 4 D35 记忆写入 + D21 子图升级体检（14 D35/D21 部分取回，**U192–U211 已转正式**；后端 `ec0fd81`/`ba97088`、前端 `19f31b4`） | ⑩记忆写入 `tests/test_memory_manual_write.py` **9 测 U192–U200**：merge_manual_update 白名单/source 强制 manual/id·created_at 不变、embedding 仅 content 变重算（U193 改为直接比对内部向量变化＋新词正向命中，确定性词法 embedder 单条 top1 总返回致旧词「不再命中」断言不可靠）、未知键 MemoryValidationError/不存在 None、POST 201/source 入参 extra=forbid 422、PUT 子集合并重索引、空体 422/404/他租户 404、权限 operate＋删除仍 admin；**U201**（integration，默认 skip）PgMemoryStore.update SELECT→merge→动态 SET/vector(256) 仅 content 变/meta 列/404 往返。⑪升级体检 `tests/test_versioning_upgrades.py` **9 测 U202–U210**：父首发子已发、子无发布版 to=1、from→to 升级、无变化不列、新增节点 first_pin、split_graph_ref（裸 id/id@N/id@draft/非数字 tail）、草稿 None＋只扫顶层不递归、只读不产版本、REST read/404/shape；**U211**（integration）PG 升级清单快照 JSON 往返且不产版本。前端 **+7（无 U 编号）**：memory.test 表单纯函数 4（parseStringMapText 空文本→{}/必须键值均字符串、buildMemoryPayload content trim·≤2000·confidence 0-1 中文 error）、memoryApi create/update 2、releaseApi getSubgraphUpgrades 1。**真实服务权限矩阵实测**：operator POST 201/DELETE 403/GET upgrades 200、viewer POST·PUT 403/GET 200、admin-b 跨租户 GET upgrades 404；构造 graph-375..379 验证 n2 @1→@2 橙升级＋n3 首次钉版@1 蓝、quiet 父 items=[]。**真实浏览器冒烟（PG 档 admin-a）两截图**：`docs/assets/batch4-memory-edit.png`（记忆页新建 Modal：偏好/内容/置信度/scope·metadata JSON/source=manual 提示/保存启用，提交后列表闭环＋编辑·删除操作列）、`docs/assets/batch4-subgraph-upgrades.png`（graph-378 ReleaseModal 体检区 n2 graph-376 @1→@2 橙 Tag＋n3 graph-377 首次钉版@1 蓝 Tag、只读副文案、门禁空态；经 React fiber 对指定 graphId 打开 Modal，未点发布不产版本） | ✅ 后端 **782 passed/24 skipped**（批 3 基线 764/22，净增 18 个常跑测＝记忆 9＋体检 9，另 2 个 PG integration 默认 skip＝U201/U211）；PG integration 全量 **24 passed**（首次跨文件跑曾 2 failed 为共享开发库瞬时污染、复跑全绿，非本批回归）；前端 **482 passed/2 skipped/39 文件**（+7）、tsc+vite build 过、oxlint **0 error/2 既有 warning**（ReleaseModal/RolloutModal，非本次引入）；d26_smoke、m11_smoke、d28_smoke PG 档全过；零新依赖、无新 ADR；**不解除 D35/D21**（商业 embedding、working/summary/case、自动提取、隐式注入、更新策略 overwrite/version/衰减/保留期/字段级历史审计、PII、记忆市场、PG RLS；子图市场/共享/跨运营体/嵌套可视化编辑器/草稿 pin 编辑器/草稿 dirty 检测仍缓做） | ✅ 已转正式（2026-09-20） |
| 2026-09-20 | M12 i18n 零依赖文案抽取骨架（D12 部分取回，**U212–U218 转正式**；前端 `locales/`） | **U212** 默认 zh-CN＋点路径嵌套查找；**U213** `{{var}}` 插值＋未知占位原样保留；**U214** 缺 key 返回 key 本身＋defaultValue；**U215** `ns:` 前缀＋空 namespace 回退 common＋未知前缀返回 key；**U216** en-US 空骨架经语言回退显 zh-CN＋changeLanguage 非法值忽略；**U217** Login·UserBadge 接线 key 清单在 zh-CN/common.json 均解析为非空中文、en-US 下不裸 key；**U218** accountLine 插值模板角色渲染。纯函数 vitest（node 环境、无 jsdom 不做组件渲染）共 13 测；Login/UserBadge 实际渲染由真实浏览器冒烟覆盖（项目无组件渲染测试基线） | ✅ 前端 **495 passed/2 skipped/40 文件**（基线 482 净增 13）、tsc+vite build 过、oxlint **0 error/2 既有 warning**（exhaustive-deps 抓到一处多余 useCallback 依赖已修、不新增）；后端零改动 **782 passed/24 skipped 不变**；真实浏览器 admin-a 登录页与登录后 UserBadge 中文零视觉差异，2 截图 `docs/assets/m12-{login,userbadge}.png`；零新依赖/不引 i18next/不新增 ADR/不解除 D12·D13 | ✅ 已转正式（2026-09-20） |
| 2026-09-20 | M12 续批 dashboard namespace 首批接线（D12 部分取回续批，**U219–U221 转正式**；`locales/` + `pages/Dashboard.tsx`） | **U219** dashboard namespace 页面专属文案 demo.title/description/cards.graph·loop·harness 经 `t(key,{ns:'dashboard'})` 全解析为非空中文、不显裸 key，标题含「电商退款自动化」；**U220** 跨页文案经 `common:` 前缀从 dashboard hook 取到 common.brand.appName（Atlas 运营体编排平台）与 nav.openEditor/monitoring/memory，4 键全中文；**U221** en-US/dashboard.json 仍空 `{}`（不翻译），切 en-US 后 dashboard 与 shared nav 键经 zh-CN 回退显中文、绝不裸 key。i18n.test.ts 13→16 测（新增 3 it）；Dashboard 实际渲染（品牌名/三导航按钮/Demo 标题描述/三卡，Graph·Loop·Harness 英文专名保留）由真实浏览器 admin-a 登录后冒烟覆盖 | ✅ 前端 **498 passed/2 skipped/40 文件**（M12 495 净增 3）、tsc+vite build 过、oxlint **0 error/2 既有 warning**；后端零改动 **782/24 不变**；真实浏览器登录后 Dashboard 中文零视觉差异，1 截图 `docs/assets/m12-dashboard.png`；零新依赖/不引 i18next/不新增 ADR/不解除 D12·D13；editor/Monitoring/Memory 大页面留后续触发批分批抽取 | ✅ 已转正式（2026-09-20） |


---

| 2026-09-21 | P0 批 1 持久化交付（docs/30，ADR T24；✅ 已落码收口） | **U222** apply_migrations 纯逻辑（排序/跳过已应用/失败回滚不登记/ensure 表/目录覆盖，8 例常跑，tests/test_migration_runner.py）；**U223** 会话后端选择＋PgSessionStore PG 集成（tests/test_iam_session_selection.py 2 例；roundtrip 在 test_storage_pg_integration.py）；**U224** 全新库 apply 001–009 全过＋二次零变更（PG 集成，通过）；**U225** `--mark-existing` 旧库登记不重跑（PG 集成，通过）；**U226** `scripts/dev/p0_persistence_smoke.py`（HTTP 造数据＋查库行数增量，通过；删卷恢复演练经 scripts/ops 真实执行，记录见 docs/30 顶部） | ✅ 结果：后端 792 passed/26 skipped；PG 集成 26 passed；演练八项行数一致、旧 token 有效、挂起审批可续跑 |
| 2026-09-21 | P0 批 2 生产认证（docs/31，ADR T25；✅ 已落码收口） | **U227** scrypt 口令哈希纯逻辑（`tests/test_iam_passwords.py`：hash/verify、`scrypt$16384$8$1$<salt>$<hash>` 串解析、盐随机、恒定时间比较、坏串/坏参数拒绝）；**U228** UserStore/PgUserStore＋幂等 seeder（`test_iam_accounts.py`：seeder 只插缺失不覆盖改密、CRUD、禁用、改密/禁用/重置的会话吊销范围）；**U229** 会话绝对 TTL 两档同构＋时钟注入（`test_iam_session_ttl.py`：过期拒绝并惰性清理、011 回填 NULL 不过期、ATLAS_SESSION_TTL_HOURS 1–168 校验、不滑动续期）；**U230** 登录节流滑动窗口（`test_iam_throttle.py`：阈值边界、窗口滑出、成功清零、username\|IP 多键独立）；**U231** API 集成（`test_api_users.py`：改密旧错 400/吊销范围、admin CRUD 404/409/422、停用登录 403、过期/坏 token 401、节流 429、operator 越权 403）；**U232** PG 集成（`test_iam_users_pg_integration.py`，默认 skip：iam_users 落库、跨 engine 哈希认证、禁用/重置持久生效、过期会话拒绝）；前端 +11 vitest（`lib/__tests__/users.test.ts`：generatePassword 口令策略、validateNewUser 用户名/显示名/角色/口令校验）。**curl 对内存档服务 23 项 HTTP 契约全绿**（建用户 201/重名 409/弱口令 422、改密吊销旧会话留当前、重置全吊销、越权 403、停用 403、跨租户 404、伪造 token 401、节流第 6 次起 429）；**真实浏览器冒烟 9 项过、8 截图 `docs/assets/p0b2-*.png`**（用户表/新建/编辑停用/重置密码/改密/停用登录红框/operator 无用户管理入口/会话吊销踢回登录页） | ✅ 结果：后端内存档 **822 passed/31 skipped**（批 1 基线 792/26，+30 常跑、+5 PG integration skip；U232 等 PG 集成已写、给 DATABASE_URL+ATLAS_RUN_INTEGRATION=1 时跑）；前端 **509 passed/2 skipped/40 Test Files**、oxlint 0 error/2 既有 warning、tsc＋build 过；commit `cd1133a`/`097c056`/`e65e4df`/`4224578`/`36ab54e`（后端）＋`222552d`（前端）。非目标 SSO/MFA/邮箱找回/JWT/审计/多实例节流仍缓做（14） | ✅ 已转正式（2026-09-21） |
| 2026-09-21 | P0 批 3 真实接入安全准入（docs/32，ADR T26=(a)；✅ 2026-09-21 落码收口：egress 06b2473 / guard a189dc1 / resilience 5376e79 / deps·env a61680b / secrets 01a2f97） | **U233** 秘密信封/SecretProvider 纯逻辑＋全链路脱敏（plain 往返、`secret://` 解析、缺失 SECRET_UNAVAILABLE、last_request/observe/异常/tool_metric/录制无明文）；**U234** AES-GCM provider（`importorskip cryptography`，T26 选 (a) 时启用：非确定性、错 key/篡改 SECRET_DECRYPT_ERROR、kid/版本、配主密钥无后端 fail-closed）；**U235** egress SSRF（注入 resolver：私网各段/环回/云元数据/IPv6 ::1·fc00::/7·fe80::/10/整数·八·十六进制 IP 绕过/非 http(s) scheme/白名单命中与否/相对路径拼 base）；**U236** SQL guard（SELECT/WITH…SELECT 放行，INSERT/UPDATE/DELETE/DDL/PRAGMA/ATTACH/VACUUM/多语句/注释藏写拒 DB_SQL_NOT_READ_ONLY，外部只读连接 execute DB_WRITE_FORBIDDEN，demo engine 直连不受影响）；**U237** retry（假 transport＋注入时钟：传输错重试、429 尊重 Retry-After、POST 默认不重试需幂等键、4xx 不重试、退避全抖动序列）；**U238** 熔断器（连续失败阈值开/open 快速失败 HTTP_CIRCUIT_OPEN/冷却 half-open/试探成败/按 host 隔离）；**U239** 适配器集成（内网 URL EGRESS_DENIED 且零外呼、外部库 execute 拒/query 写 SQL 拒、秘密注入 Authorization 正确但 observe 脱敏）；**U240**（可选）PG 只读连接集成；前端预计零改动零测试；起服务 curl 冒烟（内网 URL/只读 SQL/缺失秘密），无 UI 变化不强制浏览器截图 | 后端内存档 **1008 passed/31 skipped**（批2 基线 822，批3 净增 186：egress 60＋DB guard 58＋resilience 27＋secrets 25＋http/db 接线）；PG database 集成 3 passed；前端零改动 509 passed/2 skipped/40 文件、tsc＋build 过、oxlint 0 error/2 既有 warning；运行时离线冒烟：元数据/内网 URL→EGRESS_DENIED 零外呼、外部库 execute→DB_WRITE_FORBIDDEN、query 写 SQL→DB_SQL_NOT_READ_ONLY、缺秘密→SECRET_UNAVAILABLE、公网带秘密 GET→200 全过 | ✅ 正式（U233–U239 全转正式；U240 不新增——guard 触库前拦截无需真 PG，PG 通道由 database 集成 3 例覆盖） |
| 2026-09-21 | docs/33 影子模式＋监控告警补强＋i18n 续批打包（✅ 2026-09-21 落码收口，12 原子序；立项 `9fcf40b`，零新依赖、不新增 ADR、不解除 D12/D26/D28） | **批 1 M12 i18n 续批**（无独立 U，由 i18n.test.ts 静态键/插值键校验兜底）：editor 两原子（框架/画布/左栏＋属性面板/调试台）＋monitoring＋memory 三 namespace zh-CN 纯展示文案抽取、en-US 各 json 保持空 `{}`、技术专名（Graph/Loop/Harness/OODA/SSE/Trace/P50/P95/read/write/financial/administer/SHADOW_DRY_RUN）与后端中文 detail 不 key 化；**批 2 D26 影子模式 U250–U269**：新模块 recording/shadow.py（ShadowRun/ToolIntent/ShadowDecision/HumanOutcome/ShadowComparison＋编译期 adapter·capability→permission 表，READ 透传、写能力短路 SHADOW_DRY_RUN，infer/compare 纯函数，ShadowStore `sr-N` ring 100/租户进程内不 PG 化）、run_graph(shadow=True) 独立 tracer/broker 零污染旁路（不写 run_store、不 evaluate_after_run、不发 tool_metric、审批预置 approved）、4 REST（POST /graphs/{id}/shadow-runs、GET /shadow-runs、GET /shadow-runs/{sid}、POST .../compare）＋前端监控页影子卡/编辑器发起；**批 3 D28 Trace 钻取 U270–U278**：RunRecord.spans 纯超集（tracer.to_tree 根 dict）＋迁移 012 monitoring_runs.spans JSONB＋GET /monitoring/runs/{id}/trace 列表剔除懒加载（无 spans 返 null）＋前端 lib/traceTree.ts 纯函数与 TraceWaterfall 三级瀑布；**批 4 D28 静默/惰性升级/值班 U285–U299（U299 为 PG 集成）**：monitoring/silences.py OpsStore——静默 Silence `sil-N`（rule/graph 可空、duration 1-10080、reason≤200、惰性过期无定时器、上限 100、命中只 suppressed_count+1 不建/合/升级，POST/GET/DELETE）、未确认告警惰性升级（RuleConfig.escalation_ack_minutes 1-10080、缺省不升级、显式 0/非法 422、仅 open warning、读时按 first_seen 幂等评估记 escalated_at）、值班 OnCallSchedule（members 1-20 去重保序、index 取模轮换、空表 rotate 409、仅告警新建指派 assignee；GET/PUT/rotate），GET read/写 administer/viewer 写 403，escalated_at/assignee/静默/值班进程内不 PG 化；前端 OnCallBar/SilenceManager/规则卡升级开关/告警值班列与已升级 Tag；d28_smoke 内存档增 trace kinds/静默压下/升级 before·after/值班/viewer 403 断言（`8b29396`）；缺陷修复 `90b942a`（Popover 建静默后 SilenceManager 经 reloadKey 即时刷新，原先须整页 reload） | 后端内存档 **1051 passed/34 skipped**（约 12 分钟，与前端并行实测）；PG 直连 integration（ATLAS_RUN_INTEGRATION=1＋DATABASE_URL，不设 ATLAS_STORAGE_BACKEND）**43 passed**（含本包 U299、trace U278、M11 pgvector；整栈 PG uvicorn 与 U269 当时受 `PgUserStore.bind_session_store` 缺失阻断（已登记 docs/29、非本包回归；**该缺口 2026-09-22 由 `b2ac85d` 修复、U269 转整栈 PG 正式、整栈 PG uvicorn `/api/health` 200，见 docs/29 解决注记**）；前端 **555 passed/2 skipped/43 文件**、tsc+vite build ✓、oxlint **0 error/2 既有 warning**（ReleaseModal/RolloutModal set-state-in-effect，新代码零新增）；内存档 d26/m11/d28 三 smoke 全过；真实浏览器冒烟过 i18n 中文/值班轮换/静默闭环/未确认升级/适配器埋点表/Trace 三级瀑布/影子卡，4 截图 docs/assets/d28-{oncall-alerts,silence-oncall,trace-waterfall}.png | ✅ 正式（U250–U278、U285–U299 转正式；i18n 续批无独立 U、由静态键测试兜底；D12/D26/D28 仅部分取回、均不解除，余部与触发条件见 docs/14；实际编号 U279–U284、U290 区段留空未用，连续无冲突） | |
| 2026-09-22 | 真实接入交付批 T2–T6（docs/35，✅ 五包全部落码收口；T5 `0842243`/T6 `f243e04`/T3 `54bf38d`/T2 `5b17784`/T4 `a57164d`；U300 起文件级预留、未逐例命名，契约门见 docs/35 §7 测试矩阵） | **T4 连接** `tests/test_connections.py`（20 常跑 + 1 PG integration，共 21）：Connection/public_view 投影绝无信封/明文（hasClientSecret）、oauth state HMAC 签发/校验/过期/篡改拒、exchange/refresh 经可注入 post_form 不触网、token 异常置 error+last_error、CRUD 权限（建删改 administer、授权四作 operate、viewer 只读 403）、跨租户 404、update secret 留空保留、创建即过 EgressGuard、9 端点+无鉴权 /connections/callback 200、PG 档 oauth_connections（迁移 014）往返且 reset 不清。**T6 审计** `tests/test_audit_log.py`（16 常跑 + 1 PG，共 17）：写操作 POST/PUT/PATCH/DELETE 落审计、GET 不记、login 成功显式一条、仅元数据无请求体/Authorization、路由模板 path_format、actor/匿名、administer 403、GET /api/audit/events 分页过滤与 /export?format=jsonl、PG 档 audit_events（迁移 013）持久且 reset 不清。**T3 webhook** `tests/test_message_webhook.py`（22）：DefaultWebhookSender 先 EgressGuard.check 再 POST、follow_redirects=False、10s 超时、2xx 成功 delivered=webhook、非 2xx/网络错抛 WEBHOOK_SEND_FAILED 且不落记录、EGRESS_DENIED 透传、仅收单 URL 字符串（数组即使单元素 INVALID_PARAMETER）、未装配回退 in_process、registry 两档注入。**T2 审批通知** `tests/test_approval_notifications.py`（11）：notifyEmails DSL 校验（≤5、含 @、pointer /notifyEmails、{{}} 占位编译放行）、EmailApprovalNotifier 复用 SMTP 发信、收件人运行时插值去重剔空/无@ warning、notifier 异常 fail-safe 吞掉不阻断图、仅同步 run_graph 与流式 worker 装配（回放/影子/续跑不发）、审批载荷增 notified/notifyError。**T5** 纯配置零 Python（deploy/prometheus、deploy/grafana provisioning、dashboards/atlas-overview.json 7 面板对齐 /metrics、compose observability profile），无单测、由配置自检与启动验证覆盖。前端 i18n `locales/__tests__/i18n.test.ts` +2（connections namespace：zh 含 status.connected/nav.connections 中文、en-US 空骨架回退） | 后端内存档 **1141 passed/36 skipped**（T2 后 1121，T4 净增 20 常跑零回归；连接/审计 PG 用例无 DATABASE_URL 时 skip）；PG 直连（ATLAS_RUN_INTEGRATION=1）连接+审计 **38 passed**；前端 vitest **557 passed/2 skipped/44 文件**、oxlint **0 warning/0 error**、pnpm build 过；d26/m11/d28 三内存档 smoke 全过；T4 HTTP 端到端冒烟 ALL_PASS（建连投影无秘密/授权 state 不触网/回调页 200/viewer 建连 403/删除 deleted）。**T2–T6 均部分取回、不解除 D11/D20/D22/D24** | ✅ 正式（文件级登记；真实平台业务联调/IM/短信/OTel/前端审计页仍缓做 docs/14） | |

| 2026-09-22 | 审计可见与审批结果补强批（docs/37，✅ §7 原子 2–5 全部落码收口；②ecfa325/③bc3f116/④033619a；文件级登记，未逐例命名） | **broker/loader** `tests/test_collaboration_approvals.py`（+5）：`test_list_decided_empty_and_excludes_pending`（空与 pending 不入）、`test_list_decided_newest_first_with_limit_clamp`（resolved_at 倒序、clamp 1–200）、`test_list_decided_does_not_leak_card_context`（无 card_context/Event）、`test_request_retains_notify_recipients`（原样留存、不去重）、`test_timeout_records_resolved_at`。**notifier** `tests/test_approval_notifications.py`（+4）：`test_email_notifier_decided_renders_result_without_token`（结果/来源行、无 token/链接）、`test_email_notifier_decided_truncates_long_comment`（comment ≤200）、`test_preset_input_triggers_decided_notification_once`（input 来源触发一次、重复不重发）、`test_decided_notification_failure_does_not_block_graph`（异常 fail-safe）。**API** `tests/test_api_decided_approvals.py`（7）：`test_decided_requires_login`（401）、`test_decided_readable_by_all_roles_and_scoped_per_tenant`（三角色可读＋跨租户隔离）、`test_decided_excludes_pending_and_clamps_limit`、`test_decided_shape_fields`（snake_case node_id/graph_id＋camelCase 混合形状）、`test_human_decision_sends_result_email_once`、`test_email_link_decision_sends_result_email`、`test_result_notification_failure_does_not_block_decision`（通知抛异常决策仍 200）。前端用例数不变（571） | 后端内存档 **1190 passed/36 skipped**（基线 1174，净增 16：collab +5/notifier +4/API +7，零回归）；前端 **571 passed/2 skipped/45 文件**（计数不变）、oxlint/build 过；浏览器冒烟（chrome-devtools 对真实 :8000+5174）过：审计页 action 前缀过滤、limit 50/100/200 切换、jsonl 导出 blob 下载、空态；Approvals「已处理」Tab 数据/切 Tab 加载/手动刷新；控制台零错误；2 截图 `docs/assets/audit-{log-page,decided-tab}.png` | ✅ 正式（D11/D20/D24 边界不变、均不解除） | |
| 2026-09-23 | 真实渠道适配批（docs/38，✅ 六步原子全部落码收口；ADR T28；`d9592b5`→`3d6d28a`；零新依赖、零外部资源；不解除 D22） | 文件级登记（候选 U320 起编号未逐例命名，以实际文件计数为准）：**内核** `tests/test_channels_shopify.py`（**14**，立项候选约 18 收敛）shop 归一化/api_version 校验/请求构造/list·get 投影缺字段/401·403·5xx·坏 JSON 错误码/create_refund 全额·部分体构造/金额非法；**registry** `tests/test_channels_registry.py`（**10**）bind provider·connection·409/client_for 现取 token·未授权/test fake/租户隔离/404/级联 error；**API** `tests/test_api_channels.py`（**6**）401/viewer list·test/operate bind/DELETE admin 403/404/审计/reset 不清/跨租户引用 404；**PG** `tests/test_channel_bindings_pg_integration.py`（**4**，skipif 无 DATABASE_URL）015 往返/隔离/reset 不清/连接删除 error 保留。前端 `apiClient.test.ts` channels describe **4**（list/create POST/test POST/delete DELETE）＋`i18n.test.ts` channels **2**（zh 嵌套键解析＋en-US 空骨架回退）；浏览器冒烟 3 截图 `docs/smoke-shots/channels-smoke-{1-empty,2-bind-modal,3-test-error}.png` | 后端内存档 **1220 passed/40 skipped**（立项基线 1190，净增 30＝14+10+6 零回归；4 PG 例 skipif）；前端 **577 passed/2 skipped/45 files**（净增 6）、oxlint/build 过；浏览器冒烟三场景（空态/绑定弹窗/未授权 draft 连接探活→绑定 error 态「绑定连接未完成授权或令牌不可用」，test 端点 200 非 5xx），控制台仅 antd 既有 deprecation＋一次预期 401 | ✅ 正式（D22 不解除；真实店铺 OAuth 联调/入站 webhook/Amazon/订单同步仍缓做 docs/14） | |
| 2026-09-23 | 入站 Webhook 批（docs/39，✅ 落码收口；ADR T29；零新依赖、零外部资源；不解除 D22/D24） | 文件级登记（落码以实际计数为准）：`tests/test_channels_webhooks.py`（**21**）HMAC 成功/缺失/坏 base64/错 secret、必需头缺失、三主题 topic→event 映射、unknown topic ignored、重复 webhook_id duplicate、resolve 无版本跳过、投递异常不抛、审计；`tests/test_api_channel_webhooks.py`（**11**）公开入口 401/400/404/503、200 三态、GET 全角色、PUT administer（operator/viewer 403）、坏形状 422、订阅写入后按图触发（含全失败/轮询 run 状态）；`tests/test_channel_bindings_pg_integration.py`（**+4**，skipif）迁移 016 往返/默认 []/隔离/reset 不清。前端 apiTest **+2**（get/putWebhookSubscriptions） | 后端 **1252 passed/44 skipped**（1220 净增 32）；前端 **579 passed/2 skipped/45 files**；真实 HMAC HTTP 冒烟三态全绿、触发图 completed | ✅ 正式（D22/D24 不解除） | |
| 2026-09-23 | Shopify 侧 Webhook 注册批（docs/41，✅ 落码收口 2026-09-23；零新依赖、零迁移、无 ADR；D22 部分取回不解除） | 文件级登记（候选 **U380** 起编号，落码以实际计数为准）：`tests/test_channels_shopify.py`（**+6**）fake transport list 投影/register body·投影/delete/422→ALREADY_REGISTERED/topic 非法/非 https address；`tests/test_api_channel_remote_webhooks.py`（**11**）GET 列表·error 字段·401 落 binding error、POST 201/409/422 topic/422 https/404、角色 viewer 403·operator 201、DELETE 幂等 false→true、地址 path 钉版、mock 生命周期与 reset 清空；前端 apiClient **+3**（GET/POST/DELETE URL·方法·body） | 立项基线：后端 **1276 passed/48 skipped**、前端 **581 passed/2 skipped/45 files**（只许增测） | ✅ 正式（2026-09-23 落码收口；全后端门 **1293/48**、前端 **584/2**；mock 缝浏览器冒烟 3 截图 docs/screenshots/41-shot-*：未注册/已注册/取消后） | |
| 2026-09-23 | 入站可靠性补强批（docs/40，✅ 落码收口 2026-09-23；零新依赖、零外部资源、无 ADR；D24 部分取回不解除） | 文件级登记（候选 **U370** 起编号，落码以实际计数为准）：`tests/test_channels_webhook_deliveries.py`（**16**，含参数化 4 例）check/重复计数、ignored 不落表、全失败 dead+payload、部分成功 received、replay 成功清 payload/仍失败/无订阅 ignored/非 dead、store 异常 fail-safe；`tests/test_api_webhook_reliability.py`（**8**）dead-letters 列表/过滤/角色（viewer 读、operator 重放、删除 administer）、重放 200 与触发图、metrics 口径、404；`tests/test_channel_bindings_pg_integration.py`（**+4**，skipif）017 往返/计数、dead/replay、隔离、reset 不清。前端 apiClient **+2**（dead-letters/metrics） | 立项基线：后端 **1252 passed/44 skipped**、前端 **579 passed/2 skipped/45 files**（只许增测） | ✅ 正式（2026-09-23 落码收口；全后端门 1276 passed/48 skipped、前端 581/2） | |



| 2026-09-23 | OpenAPI 导入与工具自动生成批（docs/42，✅ 落码收口 2026-09-23；零依赖/零迁移/无 ADR；D22 部分取回不解除） | 文件级登记（候选 **U390** 起，落码以实际计数为准）：`tests/test_openapi_parser.py`、`tests/test_openapi_adapter.py`、`tests/test_api_openapi.py` 后端合计 **+59**（parser/schema 纯逻辑、store、执行适配器 path/query/header/body 组装与 EGRESS_DENIED 透传、preview/import/list/delete 与跨租户 404、删除后 /api/adapters 摘除、viewer 403）。前端 `lib/__tests__/openApiApi.test.ts`（**7**）五函数 URL/方法/body、201 投影、items 解包、structured error message；浏览器冒烟 4 截图 `docs/smoke-shots/openapi-smoke-{1-preview,2-imported,3-tool-picker,4-run-success}.png` | 立项基线：后端 **1293/48**、前端 **584/2/45**（只许增测） | ✅ 正式（全后端门 **1352 passed/48 skipped** 净增 59、前端 **591/2/46** 净增 7；mock 缝冒烟：oneOf skipped 灰行→导入→选择器 openapi:*＋M3 表单零编辑器改动→真实运行 SUCCESS HTTP 200；operator DELETE 403/admin 删除后消失 spec 404；控制台仅 antd 既有 warning＋一次预期 401） | |
| 2026-09-23 | condition LLM 语义判断分支 v1 批（docs/48，✅ 落码收口 2026-09-23；零新依赖/零迁移/无 ADR/无新 REST·错误码；D14 部分取回不解除） | **U303**：① 分类器单测（JSON 正则抽取/坏 JSON/缺键/未知标签抛错、Offline 必抛、工厂切换，注入 completion 不触网）；② DSL 五 422（llm 带 expression/description 空·超长/classifierPrompt 超长/conditionMode 非法）；③ loader fail-safe 四路径与 12000 截断、run_graph(condition_classifier=) 注入与子图透传；④ 前端 L1/隐藏字段诊断纯逻辑 vitest（5a955d2 修 rootScoped 隐藏行残留表达式诊断）。`.smoke/llm_condition_smoke.py` HTTP **11/11**（离线 fail-safe 路由 defaultTarget＋llm_errors、evaluation 带 label/description、rule 回归 amount=200→大额、五 422）；浏览器冒烟 mode 双向切换行内 expression↔description 字段与 L1 错误同步、3 截图 `docs/smoke-shots/llm-condition-{1-mode-rule,2-mode-llm,3-filled}.png` | 立项基线：后端 **1435/59**、前端 **606/2/46**（只许增测） | ✅ 正式（全后端门 **1457 passed/59 skipped** 净增 22、前端 **616/2/46 文件** 净增 10、lint 仅既有 warning/build 干净；浏览器零控制台错误；Demo 阶段不视为可上线，D14 置信度/多模型/字段脱敏等余部仍缓做 docs/14） | |
| 2026-09-23 | wait 到点时刻等待 v1 批（docs/50，✅ 落码收口 2026-09-23；零新依赖/零迁移/无 ADR/无新 REST；D19 部分取回不解除） | **U305**：① DSL（durationMode 非法 422、absolute 空·超长 absoluteTime 422、保留 stale seconds/expression 不报混用、旧图零回归）；② loader（ISO 带时区/naive 按 UTC/Z 尾缀/epoch 字符串/插值成功、差值作帧 timeout_seconds、注入时钟、未知变量/坏 ISO/epoch 越界/过点/>600 → WaitNodeFailure WAIT_ABSOLUTE_TIME_INVALID 不 sleep、resume 不变）；③ 前端 L1＋隐藏字段诊断纯逻辑 vitest；x-outputSchema 新 absoluteTime 键。`.smoke/absolute_time_smoke.py` HTTP **21/21**（① 带时区 elapsed=2.09s、② naive=3.07s、③ Z=2.16s、④ epoch=4.09s 且回写统一 ISO、⑤ 插值=1.07s〔建图到运行漂移使名义 2s 落到 1s〕、四坏用例 elapsed≤0.09s run failed、static 产出深等、三 422）；浏览器冒烟三模式切换、空值行内＋问题面板 /absoluteTime 同步、合法值配置校验通过、静态还原秒数 5，4 截图 `docs/smoke-shots/absolute-wait-{static-default,empty-error,valid,static-restored}.png` | 立项基线：后端 **1472/59**、前端 **621/2/46 文件**（只许增测） | ✅ 正式（全后端门 **1490 passed/59 skipped** 净增 18、前端 **624/2/47 文件** 净增 3、lint/build 干净；浏览器零 JS 错误（仅过期会话 401）；注：浏览器内编译运行未做——合成事件无法触发 React Flow 连线，运行路径以 HTTP 21/21＋loader 单测为准；目标时刻在图创建前计算会因建图→运行约 1s 漂移使 round(delta) 少 1，冒烟按 (名义,名义−1) 宽松断言、ISO 精确断言保留；Demo 阶段不视为可上线，>600 秒长时刻/时区日历控件/周期时刻/event timeout 时刻化仍缓做 docs/14） | |
| 2026-09-23 | IM 群机器人消息投递 v1 批（docs/51，✅ 落码收口 2026-09-23；零新依赖/零迁移/无 ADR/无新 REST·错误码 IM_SEND_FAILED；D24 部分取回不解除） | **U306**：test_message_im.py 36 例——① im.py 钉钉 timestamp/sign URL 加签固定向量、企微不加签、飞书空 key HMAC body 加签、无 secret 省略键、三渠道 text 包体，errcode/code==0 成功，非 2xx/坏 JSON/平台码非 0/网络错 ImDeliveryError，EgressDenied 不包装；② service 三渠道单 URL（数组 INVALID_PARAMETER）、secret 仅 dingtalk/feishu（非空 ≤200、其他渠道传 secret INVALID_PARAMETER）、失败 IM_SEND_FAILED 不落记录、delivered 标渠道名、未注入投递器回退 in_process；③ adapter schema 含可选 secret。`.smoke/im_robot_smoke.py` HTTP **20/20**（Layer A 假机器人逐字节向量：钉钉 `r%2B2we0q01lkZqZ…Q%3D`、飞书 `hIyyb/m07…TOEM=`、310000/19021/500 三失败；Layer B：loopback EGRESS_DENIED 且 messages 空、数组/错渠道 secret INVALID_PARAMETER、email/sms in_process 零回归）；浏览器冒烟 secret 字段 schema 驱动渲染、1 截图 `docs/smoke-shots/im-robot-1-secret-field.png` | 立项基线：后端 **1490/59**、前端 **624/2/47 文件**（只许增测） | ✅ 正式（全后端门 **1526 passed / 59 skipped** 净增 36〔全量并行下 test_loader_resume 一处既有计时 flake，隔离复跑通过〕、前端 **627/2/48 文件** 净增 3、oxlint 仅既有告警/build 干净；浏览器零 JS 错误；注：浏览器内运行未做——合成事件无法触发 React Flow 连线，运行路径以 HTTP 20/20＋im/service 单测为准；Demo 阶段不视为可上线，短信/富文本/@人/多 URL/OAuth 入站/模板/限流退避仍缓做 docs/14） | |
