# Atlas 缓做事项登记表（deferred items）

> **用途**：登记缓做/低优事项——每条带**触发条件**，条件满足时移回 [handoff.md](../handoff.md)「Active work」区并标注重启日期。本文档是缓做项的**单一事实源**，handoff 只给索引。
> **治理**：新增缓做项在此登记并同步 handoff 索引；触发条件满足移回待办时，从本表移除并在 handoff 标注重启日期。

## 缓做项

| # | 事项 | 来源 | 触发条件 | 状态 |
|---|---|---|---|---|
| D1 | 移动端自动化适配器（Appium + ADB） | 08 Phase 3 | 进入 Phase 3 平台化适配，且有移动端场景接入需求 | 缓做 |
| D2 | 策略离线训练（GRPO/DPO/RLHF，反思进化） | 08 Phase 4；10 T2 | 进入 Phase 4 生态化；评估/优化层接口（T2）先行落地 | 缓做 |
| D3 | 组件市场（适配器市场 + 模板市场，平台抽成 20-30%） | 08 商业化阶段三 | 商业化进入平台/生态模式，具备多租户与支付能力 | 缓做 |
| D4 | 模型路由器与成本优化 | 10 T2；08 Phase 4 | 有 ≥2 家模型供应商真实使用数据后 | 缓做 |
| D5 | NATS 消息队列 | 10 T4 | Demo 单进程验证结束，进入 Phase 2 多实例部署 | 缓做 |
| D6 | Go Harness 网关 | 10 T5 | 进入 Phase 2；Python 网关接口在 Demo 已验证 | 缓做 |
| D7 | 多租户权限体系 | 08 Phase 2 | Demo 验证核心价值，找到种子客户后 | 缓做 |
| D8 | 策略/运营体市场（用户发布训练好的运营体，一键导入） | 08 Phase 4 | 平台化适配完成（Phase 3）后 | 缓做 |
| D10b | CD 与自动化部署（远程环境发布、镜像推送等） | 原 D10 拆分 | 远程部署目标确定后 | 缓做 |
| D11 | 可观测性栈落地（OTel/Prometheus/Grafana） | 10 §1 | 有真实运行环境（部署后）；注：2026-09-15 基础监控告警 v1 已先落进程内形态（运行记录/指标/站内告警，见 D28），不替代正式栈，仅为其预留埋点位置与口径 | 缓做 |
| D12 | i18n 库落码（i18next+react-i18next）、zh-CN 文案抽取与 en-US 翻译 | 17 文档 §2；10 T7 | 出现首个英文使用者或明确出海需求 | 缓做（方案已定，契约先行） |
| D13 | 适配器/模板的组件描述多语言 | 04 §补充项 28 第 3 条；17 文档 §2.1 | 进入 Phase 2 模板库，适配器/模板元数据结构落地 | 缓做（模板元数据结构 2026-09-15 已随模板库落地——name/description/tags 均为可抽取的中文字符串，但多语言翻译仍随 i18n 批次，触发条件不完全满足） |
| D14 | condition 节点的 LLM 判断分支（自然语言/语义条件，而非规则表达式） | 04 §5.2；08 Phase 2 条件分支 | 规则表达式无法覆盖实际分支场景，且有稳定 LLM 供应商接入（fail-safe 仍须走默认分支） | 缓做（首版仅规则表达式，2026-09-14） |
| D15 | condition 表达式函数库与算术运算（白名单函数、数值计算、日期比较等） | 04 §5.1 首版语法白名单 | 出现需要计算/函数的真实分支需求；须先定白名单与超时/沙箱边界，仍禁 eval | 缓做（首版仅比较/逻辑/布尔，2026-09-14） |
| D16 | loop 遍历循环（foreach：数组逐项、item 变量、结果聚合） | 04 §5.3；08 Phase 2 循环 | 出现真实批量逐项处理需求，且数组变量链路就绪 | 缓做（loop v1 仅条件循环 while，2026-09-14） |
| D17 | loop 循环体内 break/continue 中断语义 | 04 §5.3；08 Phase 2 循环 | 循环体内出现需要条件中断/跳过单轮的真实流程 | 缓做（while v1 仅靠条件表达式与最大次数退出，2026-09-14） |
| D18 | parallel 节点 any_success 合并策略（任一成功 OR-join：首个成功分支即汇聚并取消其他分支） | 04 §5.4；08 Phase 2 并行 | 出现真实竞速/先到先得业务（LangGraph 静态扇入不支持提前汇聚+取消，需 Send/中断模型） | 缓做（parallel v1 仅 all_success/all_completed，2026-09-14） |
| D19 | wait 节点事件等待（waitType=event：挂起等外部信号/回调，中断-恢复持久化）与动态/表达式/到点时刻时长、等待竞速取消 | 04 §5.5；08 Phase 2 等待 | 出现真实事件驱动或分钟级以上长等待业务，且 11 文档 S1 持久化就绪（届时上 LangGraph interrupt/checkpointer 模型替代阻塞 sleep） | 缓做（wait v1 仅 duration 整数秒 1-600，2026-09-14） |
| D20 | human_approval 持久化中断-恢复（LangGraph interrupt/checkpointer 替代进程内 Event）、多实例/外部审批信号、IM/邮件/钉钉通知、动态审批人与角色权限、审批评论流/审批队列页面 | 04 §5.6；08 Phase 2 人机协作 | 11 文档 S1 持久化就绪，或真实人工审批业务上线（与 D19 事件等待同批中断模型） | 缓做（human_approval v1 仅进程内 ApprovalBroker：token+threading.Event、REST 放行、10-3600s 超时自动决策，2026-09-14） |
| D21 | subgraph 引用治理：子图版本钉版与手动升级+回归测试（04 §14）、子图市场/组织内共享、跨运营体远程引用、子图嵌套可视化编辑器、子图内部节点 SSE 事件上屏、子图内 human_approval 的交互审批 UI（v1 broker 共享但 node_start 不外泄，仅能 run inputs 预置/超时） | 04 §14/§5.7；07 §8；08 Phase 4 | 模板库/发布流建立，或进入多租户阶段；子图内人工审批交互随 D20 中断模型同批（注：2026-09-15 流程模板库 v1 已建立，但仅为内置普通图目录，不含子图引用/版本钉版/市场，本项触发条件未满足） | 缓做（subgraph v1 仅按 graphId 引用进程内已保存图、深度上限 3、单出边 fail-safe，2026-09-14） |
| D22 | API 适配器产品化：OpenAPI/Swagger 导入与工具自动生成、Schema 驱动参数表单、凭证加密存储与秘密变量运行时注入（04 §6.2）、OAuth2 token 刷新与连接测试/连接 CRUD UI、SSRF egress 白名单与私网拦截、自动重试/熔断（retry_policy）、操作录制生成适配器、NL 生成适配器 | 04 §4.2/§4.6；08 Phase 2 适配器 | 真实外部系统接入或进入多租户阶段（v1 仅通用 http/request：env/注册默认 baseUrl+公共 headers、节点 params JSON、无加密/无 OAuth/无 SSRF 防护/无重试，2026-09-15） | 缓做 |
| D23 | 数据适配器产品化：JDBC/ODBC 驱动与 MySQL/ClickHouse/数据仓库等多数据源、只读账号强制与 SQL 审查/拦截（INSERT/UPDATE/DDL 白名单）、Schema 浏览与表单元单驱动参数表单、查询历史与语句审计、分页游标与大结果集流式、多连接 CRUD UI、egress 与凭证加密（与 D22 同批） | 04 §4.2/§4.7；08 Phase 2 适配器 | 真实外部数据库接入（v1 仅 postgresql+psycopg/sqlite 两 scheme、自建 engine 工厂与平台库隔离、query/execute 两裸 SQL 能力，无审查/无表单/无审计，2026-09-15）；接入前必须先落只读账号/审查/egress | 缓做 |
| D24 | 消息适配器真实投递：SMTP 邮件、IM（钉钉/企微/飞书）机器人、短信、webhook 等渠道接入与凭证/OAuth、消息模板系统（变量/多语言/审批通知模板）、send_email/reply_message 分渠道专名与回复线程、入站消息消费/订阅型触发、限流退避与重试、投递状态追踪 | 04 §4.2/§4.8；08 Phase 2 适配器 | 出现真实通知/触达业务（v1 仅进程内 sink：单能力 message/send 记录不投递、群发上限 20、GET /api/demo/messages 查看，2026-09-15）；IM 审批通知可与 D20 同批 | 缓做 |
| D25 | 模板库产品化：用户自建/编辑/删除模板与分类管理、模板市场与组织内共享（含 D3 商业化抽成）、远程/URL 导入与模板包导出、模板版本钉版与一键升级回归、模板搜索/评分/使用统计、模板参数化向导（填空式实例化） | 04 §5.10；08 Phase 2 模板库/Phase 4 市场 | 出现用户沉淀自有流程或分享需求，或进入多租户阶段（v1 仅随代码发布的 5 个只读内置模板：Python 常量目录、GET 只读 API、整画布替换、无 DB/CRUD，2026-09-15） | 缓做 |
| D26 | 录制回放产品化：批量回放与用例集报告（通过率/趋势）、配置/模型更新后的跨版本差异对比（04 §补充11）、影子模式（线上旁路录制，§补充10）、Mock 工具响应（外部系统隔离回放）、用例编辑与入参参数化、subgraph 引用快照内联（reset/删除后子图仍可回放）、用例持久化与多租户共享 | 04 §5.11/§9.5/§补充10/§补充11；06 §6.9；08 Phase 2 操作录制 | 出现回归资产沉淀/版本升级回归/多实例需求（v1 仅单用例录制 + 一键回放：图快照冻结、客户端收集 node_end、inputs.approvals 预置保确定、显式易变键归一化逐节点比对，进程内存储 reset 保留/重启清空，2026-09-15）；注意 04 §4.5 的浏览器操作录制→工具生成仍在 D22，不属本项 | 缓做 |
| D27 | 单步调试产品化：持久化中断-恢复与多实例调试（LangGraph interrupt/checkpointer 替代进程内 Event，与 D19/D20 同批中断模型）、断点持久化进 Graph JSON/团队共享（node_schema 预留 breakpoint 位启用）、暂停时变量改写后续跑、变量变化历史与来源追踪（04 §三 9）、子图内部断点与逐帧上屏（随 D21 子图事件）、运行中急停（线程强杀/协作式取消，v1 仅暂停点可停）、断点命中计数/日志断点/异常断点、多会话并发调试与锁粒度 | 04 §5.12/§三 8/9；06 §6.10；08 Phase 2 单步调试与断点 | 11 文档 S1 持久化就绪（与 D19/D20 同批），或出现跨实例调试/团队共享断点需求（v1 仅进程内会话：会话级断点随 /run/stream 传入不落图、token+threading.Event 暂停、step/continue/stop 状态机、paused 帧 globals/outputs 深拷贝只读、条件断点白名单 fail-safe、parallel 会话锁串行、reset 按 stop 释放，2026-09-15） | 缓做 |
| D28 | 监控告警产品化：OTel 指标/追踪导出与 Prometheus 拉模型、Grafana 仪表盘（与 D11 正式栈合流）、运行记录持久化与多实例汇总（11 S1）、超过 200 条的长保留/报表导出与时间序列图表、版本对比（04 §9.2）、**适配器调用级埋点**（tool_call 耗时/错误码分位，v1 仅到节点级）、自定义规则表达式 DSL 与规则模板市场、外部告警出口（复用 message 适配器/webhook/邮件/IM，与 D24 同批）、告警静默/升级/值班轮换/自动恢复、按租户鉴权与指标隔离（随 D7 多租户）、告警详情钻取与 Trace 时间线 | 04 §5.13/§9.2/§9.3；06 §6.11；08 Phase 2 基础监控告警 | 真实部署（D11 触发）、S1 持久化就绪，或出现跨实例观测/外部通知/多租户需求（v1 仅进程内：API 层两入口埋点、ring 200、四内置规则阈值可调、站内告警中心 ack/resolve、reset 清空、零新依赖，2026-09-15） | 缓做 |

## 变更记录

- 2026-09-13 初版：由 handoff「缓做/低优项」段落迁移建立，触发条件依据 08/10 文档。
- 2026-09-13 新增 D12（i18n 库落码与英文翻译）/D13（组件描述多语言）：随 17 文档《前端国际化与设计 Token 方案》登记。
- 2026-09-14 D10 拆分为 D10a（CI+gitleaks 门禁，GitHub Actions，进行中）与 D10b（CD/自动化部署，仍缓做，触发条件：远程部署目标确定）；CI 不依赖部署目标，随种子试用前质量门提前启动，ADR T8 见 10 文档 §4。
- 2026-09-14 D10a 完成并移出本表：`.github/workflows/ci.yml` 三道门（gitleaks 全历史、后端 pytest、前端 oxlint+vitest+build），本地等价命令全绿；留痕 handoff Recently shipped。
- 2026-09-14 D9 完成并移出本表：新增 `scripts/benchmark.py`（stdlib 零依赖），BENCHMARK.md 填首份基线（OODA 循环 ~488 loops/s、退款端到端 p50 2.28ms 等，commit 407812e）；LLM 决策/记忆读写/并行扇出等待接入补测项在 BENCHMARK.md Scope 标注。
- 2026-09-14 新增 D14（LLM 判断分支）/D15（表达式函数库与算术）：condition 节点首版只落安全规则表达式（比较/逻辑/布尔，纯 stdlib 禁 eval），契约见 04 §5.1/§5.2。
- 2026-09-14 新增 D16（遍历循环 foreach/item/结果聚合）/D17（循环体内 break/continue）：loop 节点 v1 只落条件循环 while（continueExpression + maxIterations fail-safe，仅暴露 index），契约见 04 §5.3。
- 2026-09-14 新增 D18（parallel any_success OR-join 与分支取消）：parallel 节点 v1 只落 all_success/all_completed 两策略（all_success 分支失败 fail-safe 汇聚不短路），契约见 04 §5.4。
- 2026-09-14 新增 D19（wait 事件等待/中断-恢复/动态时长）：wait 节点 v1 只落 duration 定时等待（1-600 整数秒同步 sleep），事件等待需 interrupt/checkpointer + 持久化，契约见 04 §5.5。
- 2026-09-14 新增 D20（human_approval 持久化中断-恢复/多实例/通知/动态审批人）：v1 只落进程内 ApprovalBroker（token+threading.Event、REST 决策、10-3600s 超时自动 approve/reject），进程重启 pending 即失，契约见 04 §5.6；与 D19 同批中断模型（S1 就绪或真实审批业务触发）。
- 2026-09-14 新增 D21（subgraph 版本钉版/升级回归、市场与共享、远程引用、嵌套可视化、子图内审批交互）：v1 只按 graphId 引用已保存图（进程内重入 run_graph、深度上限 3、单出边 fail-safe、子图事件压制），契约见 04 §5.7；触发：模板库/发布流或多租户。
- 2026-09-15 新增 D22（API 适配器产品化：OpenAPI 导入/Schema 表单/凭证加密与秘密变量/OAuth2 刷新/SSRF egress/重试熔断/操作录制与 NL 生成适配器）：API 适配器 v1 只落通用 `http/request`（新包 httpapi，env/注册默认连接 + 节点 params JSON，4xx/5xx 仍 SUCCESS，无加密/无 OAuth/无 SSRF 防护/无重试），契约见 04 §4.6；触发：真实外部系统接入或多租户。
- 2026-09-15 新增 D23（数据适配器产品化：JDBC/ODBC/数仓多源、只读账号强制与 SQL 审查、Schema 表单、查询审计、分页流式）/D24（消息适配器真实投递：SMTP/IM/短信/webhook、模板系统、reply 线程、入站消费、限流重试）：DB v1 只落 database/query+execute 裸 SQL（新包 database，ATLAS_DATABASE_URL 与平台库隔离 + SQLite demo），消息 v1 只落进程内 message/send（新包 message，记录不投递），契约见 04 §4.7/§4.8、ADR T10；触发：真实外部数据库/通知业务接入。
- 2026-09-15 新增 D25（模板库产品化：用户 CRUD、市场/组织共享、远程导入导出、版本钉版升级、搜索评分、参数化向导）：模板库 v1 只落随代码发布的 5 个只读内置模板（新包 template，GET 只读 API、整画布替换、无 DB/CRUD、reset 不影响），契约见 04 §5.10、ADR T11；D13（元数据多语言）加注：模板元数据结构已落地，翻译仍随 i18n 批次；D21 加注：模板库 v1 不含子图市场/钉版，触发条件未满足。
- 2026-09-15 新增 D26（录制回放产品化：批量回放与报告、跨版本/配置更新差异对比、影子模式、Mock 工具响应、用例编辑与入参参数化、subgraph 快照内联、持久化与共享）：操作录制 v1 只落单用例运行录制 + 一键回放比对（新包 recording，图快照冻结、客户端收集 node_end、审批预置保确定、显式易变键归一化，进程内 reset 保留/重启清空），契约见 04 §5.11、06 §6.9、ADR T12；D22 中「操作录制生成适配器」指 04 §4.5 浏览器操作录制→工具生成，仍随 Web 适配器在 D22 缓做，与本项的运行录制不是同一含义；R2 三层定位回放同样随 D22。
- 2026-09-15 新增 D27（单步调试产品化：持久化中断-恢复/多实例、断点持久化与共享、变量改写续跑、变化历史与来源追踪、子图断点、运行中急停、命中计数）：调试 v1 只落进程内会话（新包 debug，会话级断点随 /run/stream 传入不进 Graph JSON、step/continue/stop、paused 只读快照、条件 fail-safe、reset 释放），契约见 04 §5.12、06 §6.10、ADR T13；持久化中断与 D19/D20 同批（S1 就绪触发）。
- 2026-09-15 新增 D28（监控告警产品化：OTel/Prometheus/Grafana 正式栈、持久化多实例、适配器调用级指标、规则表达式 DSL、外部告警出口、静默/升级/值班、按租户隔离、长保留报表、Trace 钻取）：监控 v1 只落进程内运行观测 + 站内告警中心（新包 monitoring，API 层两入口埋点、ring 200、运行级+节点级、成功率/p50/p95/失败节点 Top、四内置规则阈值可调、未关闭按图合并、ack/resolve、reset 清空、零新依赖），契约见 04 §5.13、06 §6.11、ADR T14；正式栈与 D11 在部署后合流，持久化随 S1，外部出口与 D24 同批，租户隔离随 D7；D11 加注 v1 不替代正式栈。
