# Atlas 缓做事项登记表（deferred items）

> **用途**：登记缓做/低优事项——每条带**触发条件**，条件满足时移回 [handoff.md](../handoff.md)「Active work」区并标注重启日期。本文档是缓做项的**单一事实源**，handoff 只给索引。
> **治理**：新增缓做项在此登记并同步 handoff 索引；触发条件满足移回待办时，从本表移除并在 handoff 标注重启日期。

## 缓做项

| # | 事项 | 来源 | 触发条件 | 状态 |
|---|---|---|---|---|
| D1 | 移动端自动化适配器（Appium + ADB） | 08 Phase 3 | 进入 Phase 3 平台化适配，且有移动端场景接入需求 | 缓做 |
| D2 | 策略离线训练（GRPO/DPO/RLHF，反思进化） | 08 Phase 4；10 T2 | 进入 Phase 4 生态化；评估/优化层接口（T2）先行落地 | 缓做 |
| D3 | 组件市场（适配器市场 + 模板市场，平台抽成 20-30%） | 08 商业化阶段三 | 商业化进入平台/生态模式，具备多租户与支付能力 | 缓做 |
| D4 | 模型路由器与成本优化 | 10 T2；08 Phase 4 | 有 ≥2 家模型供应商真实使用数据后 | 缓做 |
| D5 | NATS 消息队列 | 10 T4 | Demo 单进程验证结束，进入 Phase 2 多实例部署 | 缓做 |
| D6 | Go Harness 网关 | 10 T5 | 进入 Phase 2；Python 网关接口在 Demo 已验证 | 缓做 |
| D7 | 多租户权限体系 | 08 Phase 2 | Demo 验证核心价值，找到种子客户后（沙盘假定满足，2026-09-16 取回 v1） | v1 已落码（2026-09-16，端点级 RBAC+租户分区，04 §5.14）；剩余边界：PG 档会话 token 已随 docs/30 持久化（2026-09-21，跨重启/多实例共享）；**密码哈希/账号 CRUD（管理员代建/改显示名·角色·停用/重置密码）/本人改密/会话绝对 TTL/登录滑动窗口节流已随 docs/31（ADR T25，2026-09-21）落码收口（04 §5.17，迁移 010 iam_users/011 session TTL，commit cd1133a 等＋前端 222552d）**；开放注册/租户 CRUD/platform_admin 随 11 S1，SSO/OIDC/SAML、MFA/2FA、邮箱找回、JWT/吊销黑名单/并发会话上限、完整账号审计、多实例共享节流（PG login_attempts）、首次登录强制改密仍缓做（docs/31 §1.2 非目标；SSO/JWT 随 D22），节点级权限随 D20，配额随 Phase 3，共享/市场随 D3/D25 |
| D8 | 策略/运营体市场（用户发布训练好的运营体，一键导入） | 08 Phase 4 | 平台化适配完成（Phase 3）后 | 缓做 |
| D10b | CD 与自动化部署（远程环境发布、镜像推送等） | 原 D10 拆分 | 远程部署目标确定后 | 缓做（**2026-09-22 Caddy TLS 反代样例已落 `deploy/Caddyfile`＋compose edge profile＋/api/ready 就绪探针**，仅部署样例非 CD 流水线，本条不解除，详见文末 2026-09-22 注记） |
| D11 | 可观测性栈落地（OTel/Prometheus/Grafana） | 10 §1 | 有真实运行环境（部署后）；注：2026-09-15 基础监控告警 v1 已先落进程内形态（运行记录/指标/站内告警，见 D28），不替代正式栈，仅为其预留埋点位置与口径 | 缓做（**2026-09-22 进程内 Prometheus `/metrics` 拉取端点已落**，零新依赖 text exposition，仅单实例进程内 gauge；OTel SDK/Collector/长期存储/多实例仍缓做；**2026-09-22 docs/35 T5 Grafana/Prometheus provisioning 样例已落码收口（`0842243`，纯配置零 Python、observability profile 默认不起、7 面板对齐 /metrics 不用 rate()，不解除本条）**，详见文末注记；**2026-09-22 docs/37 包 A 应用内审计日志页已落码收口（033619a，admin 页复用既有 events/export 端点，action 前缀＋limit 50/100/200，不解除本条；SIEM、actor/时间过滤、分页仍缓做）**） |
| D12 | i18n 库落码（i18next+react-i18next）、zh-CN 文案抽取与 en-US 翻译 | 17 文档 §2；10 T7 | 出现首个英文使用者或明确出海需求 | 缓做（方案已定，契约先行）。**2026-09-20 M12 已落码零依赖前置骨架（`9f1963f`/`a894fa7`，U212–U218，不引 i18next、不解除本条）**：新建 `frontend/src/locales/` 自写极小 t()/useTranslation（接口对齐 i18next、纯 TS+已有 react、默认 zh-CN、点路径查找/`{{var}}` 插值/`ns:` 前缀/缺 key 返回 key、localStorage 语言位与 changeLanguage 预留不启用），`zh-CN/common.json` 样板（role/auth.login/auth.session/error.auth/通用 button·status）+ Login/UserBadge/auth.ROLE_LABELS 接线，editor/dashboard/demo 与 en-US 空对象占位；**i18next+react-i18next 引入、语言切换 UI、en-US 完整翻译仍缓做，触发条件（首个英文使用者/出海）不变**，触发时自写 t 零返工替换为 i18next；后端 D7 结构化错误码（AUTH_INVALID_CREDENTIALS 等）仍为触发第一批债。**落码收口（2026-09-20）**：前端 495 passed/2 skipped/40 文件（净增 13 纯函数测）、tsc+build 过、oxlint 0 error/2 既有 warning，后端零改动 782 不变；真实浏览器登录页与 UserBadge 中文零视觉差异，2 截图 docs/assets/m12-{login,userbadge}.png。**2026-09-20 M12 续批把样板接到登录后落地页 Dashboard（首个非 common namespace 的真实生产接通，仍不引 i18next、不解除本条）**：填 `zh-CN/dashboard.json`（demo.title/description/cards.graph·loop·harness）、common 增补跨页 `brand.appName` 与 `nav.openEditor/monitoring/memory`，Dashboard.tsx 用 `useTranslation('dashboard')`、跨页文案经显式 `common:` 前缀取用，Graph/Loop/Harness 技术专名 Card title 按 17 §2.5 保留英文，`en-US/dashboard.json` 仍空 `{}`，U219–U221，前端 498/40 文件，浏览器截图 m12-dashboard.png；editor（1359 行）/Monitoring/Memory 大页面文案量大，仍留触发批或后续小批分批抽取。**2026-09-21 docs/33 打包立项再取回三页文案抽取（批 1，✅ 已于 2026-09-21 落码收口，见文末 2026-09-21 注记；仍不解除本条）**：照 M12 dashboard 样板抽 editor（框架/画布/左栏＋属性面板/调试台两原子）、monitoring、memory 三个 namespace（zh-CN 填实、en-US 空 `{}` 占位、技术专名 Graph/Loop/Harness/OODA/SSE/Trace 不译、后端错误码不接），发布流 ReleaseModal/RolloutModal 与协作组件 FeedbackButton/CardRenderer 不抽；i18next/切换 UI/en-US 翻译/AntD locale 触发条件（首个英文使用者/出海）不变。零新依赖（纯 TS＋已有 react） |
| D13 | 适配器/模板的组件描述多语言 | 04 §补充项 28 第 3 条；17 文档 §2.1 | 进入 Phase 2 模板库，适配器/模板元数据结构落地 | 缓做（模板元数据结构 2026-09-15 已随模板库落地——name/description/tags 均为可抽取的中文字符串，但多语言翻译仍随 i18n 批次，触发条件不完全满足） |
| D14 | condition 节点的 LLM 判断分支（自然语言/语义条件，而非规则表达式） | 04 §5.2；08 Phase 2 条件分支 | 规则表达式无法覆盖实际分支场景，且有稳定 LLM 供应商接入（fail-safe 仍须走默认分支） | **2026-09-23 进程内 llm v1 子集落码收口（[docs/48](48-condition节点LLM语义判断分支v1批契约设计.md)；D14 部分取回、本条不解除；后端 1457/59、前端 616/2、HTTP 冒烟 11/11）**：conditionMode=llm，分支用 description，单次 LiteLLM 调用返唯一标签；未配置模型/坏 JSON/未知标签/异常全 fail-safe 走 defaultTarget；置信度阈值/多候选/澄清重问、每分支独立 prompt 或调用、模型可选与按租户配置、上下文字段级脱敏、structured outputs、LLM 路由录制回放断言仍缓做，触发条件不变 |
| D15 | condition 表达式函数库与算术运算（白名单函数、数值计算、日期比较等） | 04 §5.1 首版语法白名单 | 出现需要计算/函数的真实分支需求；须先定白名单与超时/沙箱边界，仍禁 eval | 缓做（首版仅比较/逻辑/布尔，2026-09-14；**2026-09-19 A3 已落算术 `+ - * * / %` + 白名单函数 abs/floor/ceil/round/min/max/len/lower/upper + 确定性日期 date/year/month/day/daysBetween（D15 主体取回，`ac8c718`）；now()/today() 等非确定日期函数随可注入回放时钟批**——2026-09-19 已立项并落码收口（docs/27 C 包，`3415377`）：run_graph `now_override` 单次运行固定 UTC + `today()`/`now()`/`datetime()`/`hoursBetween` 前后端 conditions 同构 + `RecordingCase.recorded_at` 录制 baseline 与 replay/gate 共享冻结锚点（旧用例回退 created_at）；随机/UUID/命名时区等其余非确定函数仍缓做）**） |
| D16 | loop 遍历循环（foreach：数组逐项、item 变量、结果聚合） | 04 §5.3；08 Phase 2 循环 | 出现真实批量逐项处理需求，且数组变量链路就绪 | 缓做（loop v1 仅条件循环 while，2026-09-14）。**2026-09-23 主体取回并落码收口（[docs/45](45-循环foreach批契约设计.md) §7，efe5de4→1e79121→01296d5；零新依赖/无 ADR）**：同一 loop 节点 mode=foreach——itemsExpression 首轮求值冻结（≤100）、串行逐项暴露 `{{loop-x.item}}/index/results`、collectTarget 回边按序聚合 results（break 补在途结果）、exitReason 增 completed/empty/expression_error/items_too_large；后端 1391/59、前端 597/2，HTTP＋画布冒烟双过。**整体缓做不解除**：嵌套循环、并行 map-reduce、裸 item 全局短名、skip-current、>100 分批触发条件不变 |
| D17 | loop 循环体内 break/continue 中断语义 | 04 §5.3；08 Phase 2 循环 | 循环体内出现需要条件中断/跳过单轮的真实流程 | 缓做（while v1 仅靠条件表达式与最大次数退出，2026-09-14；**2026-09-19 A2 已落 break（体内 condition 分支连 exitTarget，合成 `__break__` 网关、exitReason=break）与 continue（复用回边重入），不新增节点类型（`5f87654`）；foreach/item/结果聚合仍随 D16 缓做**） |
| D18 | parallel 节点 any_success 合并策略（任一成功 OR-join：首个成功分支即汇聚并取消其他分支） | 04 §5.4；08 Phase 2 并行 | 出现真实竞速/先到先得业务（LangGraph 静态扇入不支持提前汇聚+取消，需 Send/中断模型） | 缓做（parallel v1 仅 all_success/all_completed，2026-09-14；**2026-09-19 A1 已落 any_success OR-join（`f45daa9`）：任一分支成功即汇聚，只短路未开始节点、不回滚已发起 tool/wait/approval，全失败才 failed；动态 map-reduce/运行期生成分支仍缓做**） |
| D19 | wait 节点事件等待（waitType=event：挂起等外部信号/回调，中断-恢复持久化）与动态/表达式/到点时刻时长、等待竞速取消 | 04 §5.5；08 Phase 2 等待 | 出现真实事件驱动或分钟级以上长等待业务，且 11 文档 S1 持久化就绪（届时上持久化中断-恢复模型——自研暂停帧落库+恢复扫描器，T18 已拍板 B，docs/24） | 缓做（wait v1 仅 duration 整数秒 1-600，2026-09-14）。**2026-09-17 M5 契约设计轮完成（docs/24）**：中断帧+恢复扫描器+绝对 deadline 剩余时长语义已设计；**T18 已于 2026-09-17 用户拍板选 B（10 §4，自研暂停帧）**，本行触发条件原文「上 LangGraph interrupt/checkpointer 模型」措辞已同步改写，触发时点不变；**M5a 已落码（0358696，进程内重构）但不解锁本项——**M5b 已落码（2026-09-17）：wait duration 的中断帧 + 绝对 deadline 剩余时长恢复已落码；但事件等待（waitType=event）+ 动态/表达式/到点时刻时长 + 等待竞速取消仍缓做。**2026-09-23 进程内 event v1 落码收口（[docs/47](47-wait事件等待进程内v1批契约设计.md)；D19 部分取回、本条不解除）**：waitType=event——per-tenant EventWaitBroker 挂起、登录态 REST 广播/直投信号或 waitEvents 预置恢复、timeoutSeconds 1-3600、onTimeout continue/fail（f647c96→aa82eed→1608002→ce8c4f0，后端 1435/59、前端 606/2，HTTP 冒烟 19 检查＋浏览器 2 截图）；跨重启/多实例中断帧与恢复扫描器、动态/表达式时长、绝对时刻、多事件竞速取消、公开免登录信号口仍缓做，触发条件不变**；**2026-09-23 定时等待动态时长 v1 立项并当日落码收口（[docs/49](49-wait节点定时等待动态时长v1批契约设计.md)；D19 部分取回、本条不解除）**：durationMode=dynamic + durationExpression 经条件引擎运行时求值 1-600 秒（int(round)，bool/非有限拒绝），秒数作帧 timeout_seconds，坏表达式/非法结果 run failed WAIT_DURATION_INVALID 不 sleep；8580fcb/4eaaddd，后端 1472/59、前端 621/2，HTTP 冒烟 21/21＋浏览器 2 截图；到点时刻等待、event timeoutSeconds 表达式化、多表达式/jitter、跨重启中断余部仍缓做，触发条件不变**；**2026-09-23 到点时刻等待 v1 立项并当日落码收口（[docs/50](50-wait节点到点时刻等待v1批契约设计.md)；D19 部分取回、本条不解除）**：durationMode=absolute + absoluteTime（ISO 8601/epoch 秒，支持 {{}} 插值，naive 按 UTC）运行时解析目标时刻差值 1-600 秒，坏时刻/过点/越界 run failed WAIT_ABSOLUTE_TIME_INVALID 不 sleep；12de72d/4f82a1c，前端 624/2/47 文件，HTTP 冒烟 21/21＋浏览器 4 截图；>600 秒长时刻、时区日历控件、周期时刻语义、event timeoutSeconds 时刻化仍缓做，触发条件不变**2026-09-23 wait 事件跨重启持久化 v1 docs-only 立项（docs/53；D19 部分取回、本条不解除）**：PG 单实例 event 挂起帧（kind=wait+帧内 wait{waitType:event,eventKey,onTimeout,timeoutSeconds}）+EventWaitBroker.restore 同 token/event_key 剩余超时+恢复分支；零迁移/零依赖/无新 REST，多实例跨进程路由、停摆期信号排队、公开免登录信号口仍缓做，触发条件不变； **2026-09-23 B5 再取回「event timeoutSeconds 表达式化」进程内形态，✅ 同日落码收口（不解除本条）**：event wait 增 timeoutMode（static 默认 / expression）+ timeoutExpression（≤200），经条件引擎运行时求值 1-3600 秒（复用抽取出的 `_resolve_wait_expression`，duration 1-600 / event 1-3600 共用；坏表达式/非数值/越界 run failed WAIT_DURATION_INVALID）；旧图缺省 static 零回归，跨重启帧存已求值秒数、resume 不重求值。前端 wait.schema event oneOf 加 radio/字段 + L1 按 mode 校验（static 缺 timeoutSeconds 报 1-3600、expression 缺表达式报必填）。净增图级 3 测 + dsl 6 测，全量内存门 **1591/60**、前端 636/2（零新增前端测）。>3600 长时刻、多实例跨进程路由仍缓做，触发条件不变。**2026-09-23 打包 A docs/54 ✅ 落码收口（部分取回、本条不解除；76fa61c/dcf4626/2e86862）**：duration 上限放开 600→3600、event 超时 3600→86400、duration static jitter（jitterSeconds 0-300、均匀 randint、rng 可注入、resume 不二次抖动）、event 多事件 OR 竞速（eventKeys 1-8 与 eventKey 互斥、首决 matchedEventKey、余键三路径清理，含 PG 跨重启 restore，dcf4626）；多实例跨进程路由、停摆期信号排队、AND 竞速、jitter 分布选择/作用 event 超时、公开免登录信号口仍缓做，触发条件不变。 |
| D20 | human_approval 持久化中断-恢复（自研暂停帧落库替代进程内 Event）、多实例/外部审批信号、IM/邮件/钉钉通知、动态审批人与角色权限、审批评论流/审批队列页面 | 04 §5.6；08 Phase 2 人机协作 | 11 文档 S1 持久化就绪，或真实人工审批业务上线（与 D19 事件等待同批中断模型） | 缓做（human_approval v1 仅进程内 ApprovalBroker：token+threading.Event、REST 放行、10-3600s 超时自动决策，2026-09-14）。**2026-09-17 M5 契约设计轮完成（docs/24）**：审批挂起帧（token/node_id/deadline_at/graph_snapshot/resume_state）+ 恢复扫描器 + 决策双写（Event+PG 轮询）已设计，绝对 deadline 恢复后不重计超时；**T18 已于 2026-09-17 拍板选 B（自研暂停帧，同 D19 注记；M5a 已落码但不解锁本项，**M5b 已落码（2026-09-17：单实例审批挂起帧 + 恢复扫描器 + 决策 Event+PG 轮询）；多实例/外部信号/IM/钉钉通知/动态审批人/评论流/队列页仍缓做；**2026-09-22 docs/35 T2 审批挂起邮件通知已落码收口（`5b17784`，notifyEmails≤5 编译期校验＋EmailApprovalNotifier fail-safe 不阻断图，不解除本条；IM/钉钉/决策结果邮件/一键决策落地页/持久化中断余部仍缓做）**；**2026-09-22 审批闭环批（docs/36，bd6ec7a/8923473/e2771ba）再次部分取回：邮件一键决策（HMAC capability URL＋公开 email-view/email-decision＋email-link 审计）与应用内审批队列页（10s 轮询、viewer 只读、租户隔离），不解除本条；KMS/账号邮箱绑定/多实例/IM/钉钉/动态审批人/节点级角色仍缓做（ADR T27，04 §5.6 邮件决策追加段）**；**2026-09-22 docs/37 包 B 已决审批历史已落码收口（ecfa325/bc3f116：`list_decided` 只读投影＋GET /api/approvals/decided＋队列页「已处理」Tab，不解除本条；历史进程内、reset/重启即失，PG 持久化/多实例仍缓做）**）** |
| D21 | subgraph 引用治理：子图版本钉版与手动升级+回归测试（04 §14）、子图市场/组织内共享、跨运营体远程引用、子图嵌套可视化编辑器、子图内部节点 SSE 事件上屏、子图内 human_approval 的交互审批 UI（v1 broker 共享但 node_start 不外泄，仅能 run inputs 预置/超时） | 04 §14/§5.7；07 §8；08 Phase 4 | 模板库/发布流建立，或进入多租户阶段；子图内人工审批交互随 D20 中断模型同批（注：2026-09-15 流程模板库 v1 已建立，但仅为内置普通图目录，不含子图引用/版本钉版/市场，本项触发条件未满足） | 缓做（subgraph v1 仅按 graphId 引用进程内已保存图、深度上限 3、单出边 fail-safe，2026-09-14）。**2026-09-17 M6 立项（docs/20 §4.1 / ADR T19）**：取回「子图版本钉版」——发布动作递归把 subgraph 引用钉为 `graphId@vN`（版本不可变故钉号即冻结引用）；本项其余（子图市场/共享/跨运营体/嵌套编辑/子图 SSE 上屏/子图内审批 UI/升级回归 UI）仍缓做、触发条件不变。**2026-09-19 进程内形态已落码收口（docs/27 A 包，feat `e594a4b`，U114–U123 已转正式、真实浏览器冒烟过）：子图内部 node_start/node_end 以可选 `subgraphPath`（每层父图 subgraph 节点 id）上 SSE、子层 run_end 吞掉防误判终帧、子图内 human_approval 审批 payload 上屏后经共享 ApprovalBroker + 现有审批 Modal/决策端点交互（无新端点）；子图内单步断点、持久化/多实例审批（仍随 D20）、市场/共享/跨运营体/嵌套可视化编辑器/升级回归 UI 仍缓做**。**2026-09-20 docs/28 批 4⑪ 已落码收口（`ba97088` 后端 + `19f31b4` 前端，U202–U211 转正式、真实浏览器截图 `docs/assets/batch4-subgraph-upgrades.png`）**：子图版本升级体检——发布前只读对比「草稿下次发布将钉版本 vs 父图最新发布快照所钉」（新纯函数 `versioning/upgrades.py` 与 publish.py 同包两档同构 + `GET /api/graphs/{id}/subgraph-upgrades`（read、纯只读不产版本、草稿不存在 404）+ ReleaseModal 门禁表上方只读体检区（首次钉版蓝 Tag / 升级橙 Tag / 空态，loadUpgrades 与门禁并行、失败 fail-safe 不阻断）；不做草稿 pin 编辑器、不阻断发布、不检测未发布草稿 dirty、v1 只扫顶层子图）；子图市场/组织共享/跨运营体远程引用/嵌套可视化编辑器仍缓做，触发条件不变 |
| D22 | API 适配器产品化：OpenAPI/Swagger 导入与工具自动生成、Schema 驱动参数表单、凭证加密存储与秘密变量运行时注入（04 §6.2）、OAuth2 token 刷新与连接测试/连接 CRUD UI、SSRF egress 白名单与私网拦截、自动重试/熔断（retry_policy）、操作录制生成适配器、NL 生成适配器 | 04 §4.2/§4.6；08 Phase 2 适配器 | 真实外部系统接入或进入多租户阶段（v1 仅通用 http/request：env/注册默认 baseUrl+公共 headers、节点 params JSON、无加密/无 OAuth/无 SSRF 防护/无重试，2026-09-15；**2026-09-16 M3 立项取回其中「Schema 驱动参数表单」的内部形态**——仅 /api/adapters 已发现 input_schema 驱动的 tool_call params 表单，不含 OpenAPI 导入/OAuth/凭证加密/SSRF/连接 CRUD，见 08 M3 立项条）。**触发口径澄清（2026-09-16 用户拍板）**：「多租户」指**真实租户数据/部署实例**，不含种子账号沙盘形态（iam v1）；故 iam v1 落码**不触发**本项整体取回。另设**外部系统接入安全准入清单**（不随产品化功能缓做，任一真实外部系统接入时先于功能落地，与 D23 只读账号/审查/egress 同批）：① SSRF egress 白名单 + 私网/元数据地址拦截；② 秘密变量运行时注入（复用设计态前置项的字段级可见机制，见 08 Phase 2 延伸项与 D30）；③ 凭证加密存储（不写明文 env/配置） | 缓做（功能包整体缓做；**安全准入三项＋重试/熔断的纯逻辑层已随 [docs/32](32-真实接入安全准入契约设计.md) 于 2026-09-21 落码收口**（security/egress·secrets＋httpapi/resilience，ADR T26=(a) cryptography AES-GCM，U233–U239，取回**不解除本条**）；OpenAPI 导入仍随首个真实接入；**2026-09-22 docs/35 T4 generic OAuth2 连接管理 v1 已落码收口（`a57164d`：connections 包＋迁移 014＋9 端点＋无副作用回调页＋连接管理 UI，HMAC state CSRF、token/client_secret AES-GCM 信封、投影永不回传秘密，平台无关 provider 自由字符串；不解除本条——真实平台 Shopify/Amazon 等 URL·scope 适配与业务 API 联调、OpenAPI 导入、token 轮换/KMS、前端自动承接回调路由仍随首个真实接入）**；**2026-09-22 真实渠道适配批 docs-only 立项（[docs/38](38-真实渠道适配批契约设计.md)，ADR T28）：平台渠道代码层落位——新增 channels/ 包（Shopify Admin API 适配器：读订单/发起退款、ChannelRegistry/Store、按绑定动态 Harness 适配器）＋迁移 015_channel_bindings＋REST /api/channels 五端点（绑定 reset 不清）；**本条不解除**——真实店铺 OAuth 授权联调（需真实账号）、入站 webhook、Amazon 等其他平台、订单增量同步仍缓做，触发条件不变**）；**2026-09-23 入站 Webhook 批 docs-only 立项（[docs/39](39-入站Webhook批契约设计.md)，ADR T29）：入站代码层落位——channels/webhooks.py Shopify HMAC-SHA256 验签（原始 body/compare_digest、先于解析）＋WebhookDeliverer 经 M9 Router 异步触发图，迁移 016 webhook_subscriptions（按绑定 topic×graph 订阅）、公开免登录 hook 端＋订阅 GET/PUT、幂等环进程内；**本条不解除**——Shopify 侧 webhook 注册 API（v1 仅人工后台填 URL、需真实店铺联调）、Amazon/其他平台、订单增量同步、OpenAPI 导入仍缓做，触发条件不变**） **2026-09-23 docs/41「Shopify 侧 webhook 注册 API」子集已落码收口 ✅**：client list/register/delete webhooks（422→ALREADY_REGISTERED/409）＋三端点＋订阅弹窗注册区，地址服务端 ATLAS_PUBLIC_URL 钉版；后端 1293/48、前端 584/2，mock 缝冒烟 3 截图；**本条仍不解除**——真实店铺注册联调需公网 HTTPS 域名、Amazon/其他平台、订单增量同步、OpenAPI 导入、双向自动同步仍缓做，触发条件不变**）；**2026-09-23 [docs/42](42-OpenAPI导入与工具自动生成批契约设计.md)「OpenAPI 导入与工具自动生成」docs-only 立项**：粘贴 OpenAPI 3.x JSON（或经 egress 守卫抓 URL）→ 按 operation 自动生成适配器与 Capability（input_schema 派生、参数表单零编辑器改动），进程内 per-tenant 存储；YAML/Swagger2、securitySchemes 接线、PG 持久化、真实外部 API 联调仍缓做，**本条仍不解除**，触发条件不变）；**2026-09-23 [docs/42](42-OpenAPI导入与工具自动生成批契约设计.md) 已落码收口**（OpenAPI 3.x JSON 导入、operation 级 skipped、REST 五端点、前端导入页，后端 1352/48、前端 591/2）；**2026-09-23 [docs/43](43-OpenAPI导入规格PG持久化批契约设计.md)「导入规格 PG 持久化」docs-only 立项**：PG 档表 openapi_imports（迁移 018）＋PgImportStore 跨重启/多实例，REST/前端零改动；原始文档留存/导入去重/软删除/共享仍缓做，**本条仍不解除**，触发条件不变）；**2026-09-23 docs/43 已落码收口**（迁移 018 openapi_imports＋PgImportStore 两档装配，PG 直连 8 passed、PG 档冒烟重启后运行 SUCCESS 200，REST/前端零改动）：原始文档留存/导入去重/软删除/共享/YAML/securitySchemes 仍缓做，**本条仍不解除**，触发条件不变）；**2026-09-23 [docs/44](44-OpenAPI-securitySchemes静态密钥批契约设计.md)「securitySchemes 静态密钥」docs-only 立项**：apiKey header/query 与 HTTP Bearer 有效要求解析＋密钥 SecretProvider 信封加密（T26）随规格落内存/PG（迁移 019）、适配器逐次解密注入、缺密钥 fail-closed、PUT credentials 端点；basic/cookie/oauth2/openIdConnect、真实外部 API 联调仍缓做，**本条仍不解除**，触发条件不变）。**2026-09-23 HTTP Basic 子集 docs-only 立项（[docs/46](46-OpenAPI-HTTPBasic凭证子集批契约设计.md)，零新依赖/迁移/REST/错误码、无 ADR）**：parser 加 http/basic（kind=basic），PUT 凭证值按 kind 分化——basic 用 `{username,password}`（信封明文 JSON），适配器解密渲染 `Authorization: Basic base64(u:p)`，前端双输入；digest/cookie/oauth2/连接测试/凭证轮换仍缓做。**2026-09-23 本子集已落码收口 ✅**（2f3b12b/685c3a6/11b3cbc＋f24ee8a openapi params 修复；后端 1398/59、前端 602/2，真实 httpbin 冒烟 4 截图）：cookie/oauth2/连接测试/凭证轮换仍缓做；**2026-09-23 B2 HTTP Digest 已落码收口（[docs/46](46-OpenAPI-HTTPBasic凭证子集批契约设计.md) §8，httpx.DigestAuth RFC2617 挑战流、零新依赖；cookie 因 OpenAPI 无标准 scheme、属会话管理仍缓做）**，**本条整体不解除**，触发条件不变 | |
| D23 | 数据适配器产品化：JDBC/ODBC 驱动与 MySQL/ClickHouse/数据仓库等多数据源、只读账号强制与 SQL 审查/拦截（INSERT/UPDATE/DDL 白名单）、Schema 浏览与表单元单驱动参数表单、查询历史与语句审计、分页游标与大结果集流式、多连接 CRUD UI、egress 与凭证加密（与 D22 同批） | 04 §4.2/§4.7；08 Phase 2 适配器 | 真实外部数据库接入（v1 仅 postgresql+psycopg/sqlite 两 scheme、自建 engine 工厂与平台库隔离、query/execute 两裸 SQL 能力，无审查/无表单/无审计，2026-09-15）；接入前必须先落只读账号/审查/egress | 缓做（**只读强制＋SQL 静态审查已随 [docs/32](32-真实接入安全准入契约设计.md) 于 2026-09-21 落码收口**（database/guard.py，query 两档强制审查、外部连接 execute DB_WRITE_FORBIDDEN，取回**不解除本条**）；最小权限只读账号授权/JDBC/数仓/Schema 浏览/审计落库/多连接仍随真实数据库接入） |
| D24 | 消息适配器真实投递：SMTP 邮件、IM（钉钉/企微/飞书）机器人、短信、webhook 等渠道接入与凭证/OAuth、消息模板系统（变量/多语言/审批通知模板）、send_email/reply_message 分渠道专名与回复线程、入站消息消费/订阅型触发、限流退避与重试、投递状态追踪 | 04 §4.2/§4.8；08 Phase 2 适配器 | 出现真实通知/触达业务（v1 仅进程内 sink：单能力 message/send 记录不投递、群发上限 20、GET /api/demo/messages 查看，2026-09-15）；IM 审批通知可与 D20 同批 | 缓做（**2026-09-22 SMTP email 子集已落**：message/send channel=email 经 stdlib smtplib 真实投递、ATLAS_SMTP_* 装配、失败 SMTP_SEND_FAILED 不记录，未配置回退进程内；IM/短信/模板系统/入站消费仍缓做；**2026-09-22 docs/35 T3 webhook 渠道已落码收口（`54bf38d`，单 URL POST JSON、先过 EgressGuard、follow_redirects=False、WEBHOOK_SEND_FAILED、失败不落记录，不解除本条；签名/重试/限流/入站/IM/短信/模板仍缓做）**，详见文末注记；**2026-09-22 docs/37 包 C 决策结果邮件已落码收口（ecfa325/bc3f116：`notify_decided` 纯文本结果邮件、无 token/链接、comment ≤200，决策后旁路 fail-safe，不解除本条；HTML 模板化、失败重试、IM/钉钉结果通知仍缓做）**）；**2026-09-23 docs/39（ADR T29）入站 webhook 订阅型触发的代码层落位——公开 Shopify hook 端验签后按绑定订阅异步触发图（首个入站消费形态：平台签名 webhook，非 IM/短信/通用入站）；**本条不解除**——入站消息消费的通用形态、IM/短信入站、限流退避/重试调度/投递状态追踪、模板系统仍缓做，触发条件不变**）；**2026-09-23 入站可靠性补强批落码收口 ✅（[docs/40](40-入站可靠性补强批契约设计.md)，零新依赖/无 ADR）：投递去重记录 PG 化（webhook_deliveries，迁移 017，跨重启/多实例、duplicates 计数）、死信捕获（全订阅未触发才 dead）＋人工一键重放、投递指标实时聚合；**本条不解除**——自动/定时重试调度、DLQ 定时扫描、外部告警出口、IM/短信入站、模板系统仍缓做，触发条件不变**）；**2026-09-23 IM 群机器人 v1 落码收口（[docs/51](51-IM机器人消息投递v1批契约设计.md)，零新依赖/无 ADR；后端 `d4823d5`、前端 `80b2436`）：dingtalk/wecom/feishu 群机器人 Webhook 单 URL（EgressGuard）＋secret 钉钉/飞书加签，平台 errcode/code 非 0 IM_SEND_FAILED、失败不落记录；HTTP 冒烟 20 项全绿、浏览器 secret 字段截图。**本条不解除**——短信、富文本/@人、多 URL 群发、IM 应用 OAuth/入站、模板系统、限流退避/投递追踪仍缓做，触发条件不变**） **2026-09-23 B4 勘察判空集、不落码**：审批/告警/消息正文的 {{...}} 插值接缝已由 `graph/interpolation.interpolate` 全覆盖——所有 tool_call params（message/send 各渠道 to/subject/body）loader 执行前统一插值，human_approval summary/approver/notifyEmails、cards、eventKey、prompt 均已接线；告警正文由系统 alert 字段拼装、无用户可写模板（自定义规则模板随 D28 市场）。故无代码可落；消息模板系统其余（多语言/模板 CRUD/富文本）仍缓做，触发条件不变。 |
| D25 | 模板库产品化：用户自建/编辑/删除模板与分类管理、模板市场与组织内共享（含 D3 商业化抽成）、远程/URL 导入与模板包导出、模板版本钉版与一键升级回归、模板搜索/评分/使用统计、模板参数化向导（填空式实例化） | 04 §5.10；08 Phase 2 模板库/Phase 4 市场 | 出现用户沉淀自有流程或分享需求，或进入多租户阶段（v1 仅随代码发布的 5 个只读内置模板：Python 常量目录、GET 只读 API、整画布替换、无 DB/CRUD，2026-09-15） | 缓做 |
| D26 | 录制回放产品化：批量回放与用例集报告（通过率/趋势）、配置/模型更新后的跨版本差异对比（04 §补充11）、影子模式（线上旁路录制，§补充10）、Mock 工具响应（外部系统隔离回放）、用例编辑与入参参数化、subgraph 引用快照内联（reset/删除后子图仍可回放）、用例持久化与多租户共享 | 04 §5.11/§9.5/§补充10/§补充11；06 §6.9；08 Phase 2 操作录制 | 出现回归资产沉淀/版本升级回归/多实例需求（v1 仅单用例录制 + 一键回放：图快照冻结、客户端收集 node_end、inputs.approvals 预置保确定、显式易变键归一化逐节点比对，进程内存储 reset 保留/重启清空，2026-09-15）；注意 04 §4.5 的浏览器操作录制→工具生成仍在 D22，不属本项 | 缓做。**2026-09-18 M9 已落码部分取回（不解除本条）**：取回「发布前批量回放 + 跨版本 diff」的发布门禁形态——`recording/gate.py` 对 latest 草稿逐例重放 `graph_id` 匹配用例（RecordingCase 补存 graph_id），GateReport 供 publish `gate:true` 拦截（契约 03 `release_gate`、04 §5.11 末、12 §3.7/§5，U57）；**2026-09-18 立项并于同日落码收口再取回「用例集报告（通过率/趋势）」最小集（D26 报告 v1，U60 转正式，`9dc9690`/`5c812c5`/`f5c789c`，不解除本条）**：门禁/publish-gate 运行沉淀 ReleaseReport（rr-N，ring 100/租户、reset 清空、不 PG 化）、按图历史两 GET、ReleaseModal 通过率趋势折叠区；影子模式、Mock 工具响应、用例编辑/参数化、subgraph 快照内联、持久化与多租户共享仍缓做，触发条件不变。**2026-09-19 立项再取回三项收尾小项（D26 收尾批，不解除本条）**：①报告导出 CSV/JSON（release-reports/{rid}/export）；②subgraph 引用快照内联（RecordingCase 增 subgraphs，录制时递归冻结、回放内联优先，reset/删除子图后仍可回放）；③定时 CI 批量回放（release-gate-cron workflow 跑 d26_smoke）；范围/原子序见 08 D26/D30 收尾小项立项条，**三项均已于 2026-09-19 落码收口（`29bb3d9` 快照内联/`8a37b4e` 导出/`cea70f4` 定时 CI，见文末落码注记）**；另记一处**现状缺口（2026-09-19 docs/27 C 包勘察实测）**：PG 档 `PgRecordingStore.add` 未实现富字段 `graph_id`/`subgraphs`（INSERT 仅基础 8 列），而 `POST /api/recordings` 一直传二者，故 **PG 后端下「录为用例」端点 500（`TypeError: unexpected keyword argument 'graph_id'`；进程内档正常，d26_smoke 仅内存档全过）**；C 包 `3415377` 的 `recorded_at` 已做到进程内/PG 两档 store 层一致，graph_id/subgraphs 的 PG 化（recordings 加列 + add/get/_row_to_case + Repository 协议）随本条「用例 PG 持久化」同批。影子模式/Mock/用例编辑参数化/PG 持久化多租户/跨图看板仍缓做，触发条件不变。**2026-09-20 立项再取回四项（docs/28 批 1，本批只落文档）**：①PG 档 recordings 富字段缺口修复（迁移 008 给 recordings 加 graph_id/subgraphs，现状 PG 档「录为用例」500、d26_smoke 仅内存档全过）；②Mock 工具响应回放（tool_mocks 键＝节点 id，仅单用例 replay 端点、发布门禁不接、子图不传）；③用例编辑（PUT 仅 name/inputs 可改）与回放入参浅合并覆写；④跨图报告看板（ReportStore.list_all_summary + `GET /api/release-reports` + Monitoring Card，报告仍进程内 ring 不 PG 化）。**四项均已于 2026-09-20 落码收口（docs/28 批 1，U139–U148 转正式，不解除本条）**：`d1f455b` 迁移 008 修复 PG recordings 富字段持久化（graph_id/subgraphs，前述 PG 档「录为用例」500 缺口就此闭合；monitoring_runs.tool_calls 列同批 DDL 预留批 3）；`c7bf138` 单用例 replay 增可选 body {mock_tools,inputs_override} 与响应 mocked_tools（桩键＝节点 id、仅顶层工具、门禁不接、子图不传）；`89e21fc` PUT /api/recordings/{id} 仅编辑 name/inputs＋GET /api/release-reports 跨图看板；`0e8ebe7` 前端录制 Modal Mock 勾选/入参覆写/编辑面板＋监控页跨图报告 Card。三道门后端 720 passed/20 skipped、PG integration 8 passed、前端 472 passed/build 过/lint 0 error，d26_smoke 内存＋PG 两档全过、m11_smoke 两档 ALL_PASS、浏览器冒烟 3 截图 `docs/assets/batch1-{cross-graph-board,edit-panel,mock-replay}.png`。**仍缓做（触发条件不变）**：影子模式/线上旁路录制、子图快照多租户共享、报告 PG 化（ReportStore 仍 ring 100/租户进程内）、报告删除端点（ring 自然淘汰）、跨版本配置 diff。**2026-09-21 docs/33 打包立项再取回影子模式进程内 v1（批 2，✅ 已于 2026-09-21 落码收口，见文末 2026-09-21 注记；仍不解除本条）**：dry-run 读写判定依据 `Capability.permission`（编译期建 adapter/capability→permission 表，READ 透传真实执行、WRITE/DELETE/FINANCIAL 不调 adapter.execute、短路返 SHADOW_DRY_RUN 意图回执，http/request 声明 WRITE 保守短路），shadow 全链透传含子图、不写 RunRecord/run_store、不调 evaluate_after_run（不污染告警/灰度门控）、不发 tool_metric、human_approval 预置 approved 秒过记意图；新模块 recording/shadow.py（ShadowStore ring 100/租户、sr-N、不 PG 化、reset 清空），4 REST 端点（shadow-runs 创建/列表/详情/补录对比，仅 sync 无 SSE）＋编辑器 ShadowRunModal＋监控页 Card；线上自动旁路、SSE 实时影子、影子 PG 化/多租户共享、趋势报表仍缓做。**2026-09-23 B3 再取回「跨版本配置结构 diff」进程内形态，✅ 同日落码收口（不解除本条）**：新纯函数 `graph/diff.py`——`diff_graph(base,candidate)` 输出 nodes/edges/variables 的 added/removed/changed，节点 changed 细分 type/name 与 config 键级（新增/删除/值变）、position 等画布布局不参与，附 `diff_summary`/`has_changes`，零依赖；`GET /api/graphs/{id}/diff`（read、纯只读不产版本；toVersion 缺省＝latest 草稿、fromVersion 缺省＝最近发布版，该图从未发布则基线取空图〔首次发布前全为新增〕，目标/基线快照缺失 404、跨租户不泄漏）；前端 ReleaseModal「与 vN/空图 的配置差异」折叠区（与门禁并行加载、失败 fail-safe 不阻断）。纯函数 6 测、端点 4 测，全量内存门 1582/60（以收口实跑为准）、前端 636/2。影子模式线上自动旁路、Mock 工具响应、用例编辑与入参参数化、用例/报告 PG 持久化与多租户共享仍缓做，触发条件不变 |
| D27 | 单步调试产品化：持久化中断-恢复与多实例调试（自研暂停帧落库替代进程内 Event，与 D19/D20 同批中断模型，T18 已拍板 B）、断点持久化进 Graph JSON/团队共享（node_schema 预留 breakpoint 位启用）、暂停时变量改写后续跑、变量变化历史与来源追踪（04 §三 9）、子图内部断点与逐帧上屏（随 D21 子图事件）、运行中急停（线程强杀/协作式取消，v1 仅暂停点可停）、断点命中计数/日志断点/异常断点、多会话并发调试与锁粒度 | 04 §5.12/§三 8/9；06 §6.10；08 Phase 2 单步调试与断点 | 11 文档 S1 持久化就绪（与 D19/D20 同批），或出现跨实例调试/团队共享断点需求（v1 仅进程内会话：会话级断点随 /run/stream 传入不落图、token+threading.Event 暂停、step/continue/stop 状态机、paused 帧 globals/outputs 深拷贝只读、条件断点白名单 fail-safe、parallel 会话锁串行、reset 按 stop 释放，2026-09-15）。**2026-09-19 已立项取回进程内形态（docs/27 B 包，**已于 2026-09-20 落码收口 `f9a1301`**，U124–U138 转正式、HTTP SSE 与真实浏览器三场景冒烟过，见 docs/27 顶部 B 注记）：运行中急停＝节点边界协作式取消（RunCancellationBroker + `POST /api/runs/{id}/cancel` + run_graph `is_cancelled` + SSE cancelled 帧/状态，普通与调试统一、不在 wait/approval/tool 阻塞中点强杀）、断点命中计数 hitCount（每 N 次命中暂停）+ 日志断点 logpoint（命中只发 debug_log 不暂停）、暂停时 resume 带 globals 顶层键白名单浅合并改写续跑（帧仍只读、stop 忽略覆盖）。持久化中断/多实例调试（仍随 D19/D20/S1）、断点落 Graph JSON/团队共享、变量变化历史与来源追踪、异常断点、子图内部断点、多会话锁粒度仍缓做** | 缓做（进程内 v1 三项——协作式急停/hitCount·logpoint/globals 改写——已取回并收口 `f9a1301`，余部触发条件不变）。**2026-09-20 立项再取回三项进程内形态（docs/28 批 2）**：⑤暂停帧变量变化历史（DebugSession 易失 list 上限 50、相邻暂停 global 顶层键 diff + 经过节点区间、resume 用户改写同步基线不入历史）；⑥异常断点（BreakpointSpec.exception/onException，命中发 reason=exception 帧，resume/continue 后原样重抛、不改变失败语义、不做「忽略继续」）；⑦子图内部断点与逐帧上屏（debug_controller 透传子层重入、_namespaced_emit 放行 paused/debug_log、终帧仍吞）；持久化中断-恢复/多实例调试、断点落 Graph JSON/团队共享、变量血缘图、多会话锁粒度仍缓做，触发条件不变。**三项均已于 2026-09-20 落码收口（docs/28 批 2，U150–U165 转正式，不解除本条）**：`038dcaa` 变量历史（易失 list 上限 50、JSON-stable 基线、resume 改写 seed 基线不入变化）、`9b378c2` 异常断点（onException、reason=exception+error 帧、resume 原样重抛不忽略、纯异常 before 早退防误停、非布尔 422）、`f6f7d30` 子图内部断点（controller 透传、paused/debug_log 附 subgraphPath、终帧仍吞、DebugStopped 穿透，旧「子图内部不暂停」测试反转为 is_paused_with_path）、前端 `c13886c`（异常 Switch/历史 Collapse/子图路径 Tag）；三道门后端 736 passed/20 skipped、PG integration 8 passed（批 2 无 DDL）、前端 473 passed/build 过/lint 0 error，d26/m11 smoke PG 档全过、SSE 端到端与浏览器冒烟 3 截图 `docs/assets/batch2-{variable-history,exception-breakpoint,subgraph-pause}.png`；持久化中断-恢复/多实例、断点落 Graph JSON/团队共享、跨节点变量血缘图、忽略异常继续、阻塞中点强杀、多会话锁粒度仍缓做，触发条件不变 |
| D28 | 监控告警产品化：OTel 指标/追踪导出与 Prometheus 拉模型、Grafana 仪表盘（与 D11 正式栈合流）、运行记录持久化与多实例汇总（11 S1）、超过 200 条的长保留/报表导出与时间序列图表、版本对比（04 §9.2）、**适配器调用级埋点**（tool_call 耗时/错误码分位，v1 仅到节点级）、自定义规则表达式 DSL 与规则模板市场、外部告警出口（复用 message 适配器/webhook/邮件/IM，与 D24 同批）、告警静默/升级/值班轮换/自动恢复、按租户鉴权与指标隔离（**v1 已随 D7 于 2026-09-16 落地**：运行/告警/规则按租户分区，OTel 多租户导出仍随正式栈）、告警详情钻取与 Trace 时间线 | 04 §5.13/§9.2/§9.3；06 §6.11；08 Phase 2 基础监控告警 | 真实部署（D11 触发）、S1 持久化就绪，或出现跨实例观测/外部通知/多租户需求（v1 仅进程内：API 层两入口埋点、ring 200、四内置规则阈值可调、站内告警中心 ack/resolve、reset 清空、零新依赖，2026-09-15；租户分区 2026-09-16） | 缓做（租户隔离部分已随 D7 v1 落地）。**2026-09-20 立项再取回两项进程内形态（docs/28 批 3）**：⑧适配器调用级埋点（executor tool 三出口 monotonic 计时发 tool_metric，SIMULATED 照记、mock/子图不发，入 RunRecord.tool_calls，迁移 008 给 monitoring_runs 加 tool_calls JSONB，metrics 增 tool 段 p50/p95/错误码分布）；⑨自定义告警规则表达式 DSL（RuleConfig.custom 纯超集缺省 []，复用 graph.conditions 白名单递归下降求值、禁 eval，扁平上下文 status/durationMs/failedCount/hasError，结果必须 bool、异常 fail-safe 不告警，PG config JSONB 自动兼容无 DDL）；OTel/Prometheus/Grafana 正式栈（随 D11）、运行记录 PG 持久化与多实例汇总、超 200 条长保留/时序报表、规则模板市场、外部告警出口、静默/升级/值班轮换仍缓做，触发条件不变。**两项均已于 2026-09-20 落码收口（docs/28 批 3，U166–U191 转正式，不解除本条）**：`a318d97` 适配器埋点（loader 真实工具分支 monotonic 计时发 tool_metric、只采顶层无 subgraphPath、mock/子图/debug/回放不采、RunRecord.tool_calls、PG monitoring_runs.tool_calls JSONB 沿用迁移 008 无新 DDL、summarize_tools 按 tool 聚合 calls/failed/simulated/error_codes/p50/p95）；`11b1ba7` 自定义规则 DSL（RuleConfig.custom 纯超集、RuleId 放宽 str、custom:{cid}、复用 graph.conditions 禁 eval、白名单四变量 status/durationMs/failedCount/hasError、非布尔或异常 fail-safe、validate_rules 中文聚合校验）；前端 `4256326`（适配器调用 Card、自定义规则 List 编辑器、ruleLabel 三级回退）、HTTP 冒烟 `dc46f20`（scripts/dev/d28_smoke.py）。三道门后端 764 passed/22 skipped（净增 28）、PG integration 10 passed（+2）、前端 475 passed/2 skipped/build 过/lint 0 error/2 既有 warning；d26/m11/d28 smoke PG 档全过、真实浏览器两截图 docs/assets/batch3-{tool-metrics,custom-rules}.png。**落码偏差（相对立项）**：DSL 上下文 durationMs 取 int(round) 毫秒（条件引擎有序比较要求严格同型，float 对整数字面量会被 fail-safe 吞掉）；PG monitoring_alerts 表 v1 不持久化 rule_name（读回 null、前端回退 rule_id，custom 规则本身随 monitoring_rules.config JSONB 承载）；未注册适配器失败 error_code=null（无结构化 code）。OTel/Prometheus/Grafana 正式栈（随 D11）、运行记录长保留/时序报表、Trace 钻取、子图内与 Mock 埋点、规则图级绑定/模板市场。**2026-09-21 docs/33 打包立项再取回三项进程内形态（批 3/批 4，✅ 已于 2026-09-21 落码收口，见文末 2026-09-21 注记；仍不解除本条）**：①Trace 时间线钻取——复用 M10 已埋 span 树，RunRecord 增 spans 纯超集字段（存 tracer.to_tree() 根 dict）、迁移 012 给 monitoring_runs 加 spans JSONB、列表端点不返 spans、新端点 GET /api/monitoring/runs/{id}/trace 懒加载、前端 lib/traceTree.ts 纯函数＋TraceWaterfall 瀑布，采集口径不变（仅两真实运行入口，debug/回放/子图重入不采）；②告警静默（Silence sil-N，rule_id/graph_id 皆空＝全局，纯函数 silence_matches、命中不新建/不合并/不升级并 suppressed_count+1、惰性过期不引定时器、上限 100，POST/GET/DELETE 三端点）＋未确认告警惰性升级（RuleConfig 增 escalation_ack_minutes 纯超集，open+warning 超时未 ack 升 critical、读时评估回写，ack/resolved 不升级）；③值班手动轮换（OnCallSchedule members/index，新告警**新建**时指派当前值班 assignee、合并/静默不指派，GET/PUT/rotate 三端点、空表轮换 409、v1 不校验用户名）；escalated_at/assignee/静默/值班照 rule_name 先例 v1 进程内不 PG 化（仅 spans 走迁移 012）。OTel/Prometheus/Grafana 正式栈（随 D11）、外部告警出口（随 D24）、按日自动轮换/排班表/IM 通知、长保留时序报表仍缓做，触发条件不变；**2026-09-23 docs/52 再取回「外部告警出口」进程内形态，✅ 同日落码收口（70b4889/07baa89/6a7d846/b352638；冒烟 8ae7d76）：租户单例 AlertChannel（dingtalk/wecom/feishu/webhook/email＋minSeverity）仅新建告警时经 MessageService 旁路投递固定纯文本、fail-safe，迁移 020、GET/PUT /api/monitoring/alert-channel；后端 1548/59、前端 636/2、两层冒烟 ALL PASS 15 checks。本条不解除——多渠道路由、模板富文本/@人、限流退避/投递历史仍缓做，触发条件不变。**2026-09-23 B1 lifecycle 增补落码收口（991cd19，详见 [docs/52](52-监控告警外部通知v1批契约设计.md) §7）：合并归并 / 未确认升级 / 解决三类已在内存档经 AlertNotifier 投递（净增 4 测、全量内存门 1570/60、零失败）；PG 档 lifecycle、OTel 仍缓做，本条不解除，触发条件不变。**2026-09-23 打包 A docs/54 ✅ 落码收口（部分取回、本条不解除；2d53654）**：健康运行自动 recovery 通知（record_run 一次健康即把该图 open 且 rule_id≠rollout_gate 内置告警 resolve、复用 last_seen 不加列、acknowledged/门禁不自动恢复、锁外发 transition=recovery）＋lifecycle 同(alert,transition) 默认 60s 限流退避（new 首条不限、成败都计时、进程内不持久化）；PG 档 lifecycle、连续 N 次健康/flapping 抑制、OTel、规则模板市场仍缓做，触发条件不变** |
| D29 | Schema 驱动配置内核通用化：SchemaRegistry（节点/工具/技能/记忆/部署/卡片六类实体多来源 schema）、UISchema 与 WidgetRegistry 控件注册表、JSON Schema L1 字段校验（形态经 ADR T16 收口为手写最小子集，不引 AJV；M1 双跑解释器 2026-09-16 已验证；**M2「L1 扶正 + 结构化诊断」已于 2026-09-16 立项并同日按五段原子序落码收口**（234a95f→fbd9f77：解释器扶正至 lib/validation/l1.ts、Diagnostic（nodeId+RFC6901 pointer+token range）统一 L1/L2、compile 422 稀疏 locations 侧车，契约见 08/03/04 §6.5/06 §6.13））、嵌套/数组/枚举/if-then 条件显示、自定义控件扩展契约、结构化诊断（nodeId+JSONPointer+token range）与 quickFix、增量调度/Web Worker 图级校验 | 19 文档 §1（题一） | 技能/记忆/部署/交互模板任一实体需要配置表单，或 D22/D23 的「Schema 驱动参数表单」进入落码（注：2026-09-16 已立项的设计态前置项只做 Capability schema 声明 + 拓扑作用域 + L2 引用校验，**不含**表单生成，见 08 Phase 2 延伸立项） | 缓做 |
| D30 | 变量作用域 v2：secret/env/session 受限来源与字段级可见、parallel `result.<入口id>.*` 动态路径与汇聚点可见性、subgraph outputs 深层路径展开、outputSchema 类型级校验（v1 仅存在性）、变量数据依赖环检测、改节点 id/输出键的引用 quickFix 联动 | 19 文档 §1.4；04 §6.3 | 设计态前置项落地后出现真实嵌套输出/类型校验需求，或秘密变量进入落码（与 D22 同批，会话变量随 11 S1） | 缓做。**2026-09-19 立项取回两项（D30 首批，不解除本条）**：①outputSchema 类型级校验 REF_TYPE_MISMATCH（仅前端 L2 warning，标量不符、缺 schema 降级仅存在性，不进编译 422）；②变量数据依赖环检测 GRAPH_DATA_CYCLE（与拓扑环分开建图，只收通过可见性的引用边，新增价值在 loop 回边区，前端 l3 + 后端 dsl 同构 error）；范围见 08 D26/D30 收尾小项立项条，**两项均已于 2026-09-19 落码收口（`9a357df` GRAPH_DATA_CYCLE 前后端同构/`47ecf48` REF_TYPE_MISMATCH 前端 warning，见文末落码注记）**；**2026-09-19 A+B 批再取回三项余部（B1 `ee534ef` parallel.result 汇聚点可见性 + 入口 id 校验、B2 `25db6df` subgraph.outputs 按已解析子图内部节点深层校验〔后端权威门、前端子图结构拉取随 D21〕、B3 `8496b2d` 改节点 id 的引用 quickFix 全量联动）**；secret/env/session 受限来源与字段级可见仍缓做；**输出键（非节点 id）改名联动经 2026-09-19 勘察判为空集（docs/27 §0/§1.3）：v1 产出键（result/target/index/iterations/result 等）由节点类型固定、节点 schema 无自定义输出键/别名/输出映射配置、B3（8496b2d）已覆盖改节点 id 联动、工具输出键来自适配器外部 schema 图内不可改名，故当前无作用对象，随未来「命名输出/输出映射」特性启用再比照 B3 做联动**；触发条件不变 |
| D31 | 多智能体任务总线：任务信封（taskId/idempotencyKey/deadline/状态机 pending→accepted→running→done/failed/timeout）、dispatch/join/escalate 三协同模式、任务级幂等去重返首结果、资源乐观锁 CAS、决策冲突不投票走升级仲裁 | 19 文档 §2.1/§2.4（题二） | 第二个真实 Bot 角色（如物流 Bot）接入，或出现跨 Bot 时序依赖业务（退货签收→放款）；先于它需要 11 S1 持久化 | 缓做（当前图直接调适配器，skills/ 空包，无 Bot 执行体抽象）。**2026-09-17 M7 已立项并三批落码（08 M7 立项条+落码条 + ADR T20）：进程内任务总线 + CAS 模拟的沙盘语义期，假物流 Bot 不解除本条目（真实 Bot 接入仍需触发）** |
| D32 | Graph 版本化与灰度发布：graphId@version 不可变（含模板/技能/子图引用冻结，与 D21 钉版同批）、入站 Router（租户 allowlist/业务桶/订单哈希百分比）、在途实例 pin-to-version、指标门控自动回滚、发布前批量回放门禁（D26） | 19 文档 §2.5；08 Phase 2 之后 | 真实生产发布或多实例部署（与 D5 NATS/D6 Go 网关/D10b CD、11 S1 同批；多租户分桶已随 D7 v1 具备维度基础） | 缓做（当前 GraphStore 进程内、id 即最新）。**2026-09-17 M6 立项（docs/20 §4.1 / ADR T19）**：取回「graphId@version 不可变 + 子图引用冻结」；本项其余（入站 Router/在途 pin-to-version/指标门控自动回滚/批量回放门禁 D26）仍缓做、随 M9，触发条件不变。**2026-09-18 M9 已落码（docs/20 §4.4 / ADR T22，批 1-4＋收口 2026-09-18）**：进程内沙盘语义期取回入站 Router（internal/lowValueBucket/canary 哈希/full 四段）、在途 pin-to-version（复用 M6 钉版 + M5b 帧快照）、指标门控自动回滚（业务三率 + run_error_rate）、发布前批量回放门禁（D26）、rollout 状态机 REST 与前端发布流（契约 04 §5.16、06 §6.17、12 §3.12/§5，U55–U59）；**本条不解除**——真实生产发布/多实例部署（配置中心与路由表多实例热同步、独立网关、真实 webhook/IM/embed ingress 与签名验签、RoutingStore 持久化、自动/智能放量）仍随 D5/D6/D10b/11 S1 同批，触发条件不变 |
| D33 | 交互模板系统：审批/通知卡片双向 Schema（sections.bindings 变量展示 + actions.output 结构化回写）、web/IM/邮件三渠道渲染降级与链接兜底、卡片模板作为第六类实体走 D29 内核与 L2 变量校验 | 19 文档 §2.2.3/§2.3.2（题二）；05 自定义前端模板 | 首个真实 IM/邮件渠道接入（与 D24 真实投递、D20 IM 通知同批） | **部分取回（进程内，已落码收口 2026-09-18，U53/U54 转正式；本条不解除）**：M8 于 2026-09-18 立项并三批落码（批 1 `2b35fc9`、批 2 `5f4384e`/`4b45571`、批 3 `25afa10`、收口 `94000de`/`486a591`；08 M8 立项条+落码条，契约见 03 `card_template`、04 §5.6 追加段、05 §5.3 边界、12 §3.11/§5）——新包 `src/atlas/cards/`（内置只读目录照 template 包，v1 一张 refund-approval），取回审批卡片双向 Schema 最小子集（fields 只读绑定 + textarea/input 可编辑 + actions.output 回写）、web/im/email 三渠道渲染降级（web 经 FormRenderer 第三类来源 kind:"card"、IM 文本+两按钮回调 URL、邮件只读 HTML+token 链接，GET 不产生决策副作用）、human_approval 可选 cardTemplateId（不填旧 summary 路径不回归、坏 id 编译 422）、bindings 走同一 ScopeIndex/L2、决策端点 actionId/form 超集、M5b 中断帧带卡恢复；进程内 message/send 落记录、**不自动外发**。**仍缓做、沙盘期不解除本条**：真实 IM/邮件渠道接入与凭证/OAuth（随 D24）、自动通知触发（随 D20）、邮件 GET 决策落地页/网页嵌入 SDK、卡片 CRUD/市场/版本化（随 D25/D3）、05 §5.3 愿景大 Schema（style/layout/conditional_display/可视化编辑器/模板市场）；触发条件不变 |
| D34 | span 级链路追踪：traceId/spanId/parentSpanId 跨任务与跨服务传播、parallel/subgraph 内部 span（可选不外泄）、graphVersion 标注、与 D11 OTel 正式栈合流 | 19 文档 §2.3.4；06 §6.11/§6.15；04 §5.15 | D31 任务总线落地（进程内也需父子 span），或 D11 OTel/Prometheus 栈接入 | **部分取回（进程内，已落码收口）**：触发条件 D31 已由 M7 满足，M10 于 2026-09-18 立项并于同日落码收口（08 M10 立项条，ADR T21，U49–U52 转正式；四批原子提交 1f21b1a/89f5645/c227f0c/2b1dbe9，后端 486 passed、前端 359 passed）——进程内同形最小 span + contextvars 传播 + SSE 超集 + 任务信封/RunRecord 贯穿 + 录制归一化剔除 span 键，**不引 OTel SDK**；跨服务传播（随 D5）与 OTel/Prometheus/Tempo 正式栈合流仍缓做（随 D11），沙盘期不解除本条 |
| D35 | 长期记忆 / 长期上下文产品化：商业 embedding provider（LiteLLM embedding，替换本地词法向量）、working memory（Redis 短期态）、summary（LLM 阈值摘要 / 长对话压缩）、case（跨运行轨迹经验检索，与 recording 区分）、自动事实 / 偏好提取（auto_extract + 置信度）、决策节点隐式记忆注入（06 §6.2 memory_retriever.query 进 decide 第 1 步）、记忆更新策略（overwrite/version/ask）与时间衰减 decay / 保留期、PII 脱敏（Presidio）与用户删除 / 加密、运营体 memory_config 策略表单与 AI 节点注入开关（D29 第六类实体入册）、记忆跨运营体共享 / 市场、REST 写入 / 编辑端点、PG RLS / 多实例、ANN 索引调参与 embedding 维度升级迁移 | 05 §2 五层记忆；06 §6.2/§6.3；11 §2.3；12 §4；docs/26；ADR T23 | 商业 embedding：本地词法向量召回不满足种子试用且商业 API 可用（dim 变化须向量重建 / 双写）；working/Redis：多实例会话级短期共享；summary/自动提取/隐式注入：长对话压缩或个性化决策需求；PII/保留期/RLS/加密：真实租户数据与合规需求；策略表单：运营体级记忆配置进入 UI；记忆市场：跨运营体共享需求 | 缓做（v1 最小可用长期记忆已于 2026-09-19 落码：进程内/pgvector 两档、fact+preference、本地确定性向量、显式 remember/recall 工具 + REST 浏览/搜索/删除 + 前端记忆页；产品化余部仍缓做）。**2026-09-19 M11 最小可用长期记忆已落码收口（不解除本条；四 commit `5441902`/`58d936c`/`73e53bd`/`75c4150`，后端 673/前端 449/两档 m11_smoke 全过）**：统一 memory_items（kind=fact/preference）、EmbeddingProvider 抽象 + 本地确定性 embedder（256 维 hashing/中文 bigram、纯 stdlib、离线回放确定）、memory/remember+memory/recall 两适配器工具（零新节点/DSL）、进程内 + PG/pgvector 两档（第九个 MemoryRepository、迁移 006）、REST 浏览/语义搜索/删除、前端记忆浏览页（08 M11 立项条、ADR T23、docs/26）；上述产品化能力仍缓做、触发条件不变。**2026-09-20 docs/28 批 4⑩ 已落码收口（`ec0fd81` 后端 + `19f31b4` 前端，U192–U201 转正式、真实浏览器截图 `docs/assets/batch4-memory-edit.png`）**：REST 写入/编辑端点（`POST /api/memories`（operate、201、source 固定 manual、extra forbid）、`PUT /api/memories/{id}`（operate、白名单子集、空体 422、不存在/他租户 404），两档 store 同构 `update(memory_id, **fields)`、白名单常量 REMEMBER_EDITABLE_FIELDS 与共享 merge_manual_update、合并整体过校验、content 变才重算 256 维 embedding、id/created_at 不变、无字段级历史；删除维持 admin；前端记忆页新建/编辑 Modal）；商业 embedding、working(Redis)/summary/case、自动事实偏好提取、决策隐式注入、更新策略 overwrite/version/衰减/保留期/字段级历史审计、PII 脱敏、memory_config 策略表单与第六类实体入册、记忆跨运营体共享/市场、PG RLS/多实例、ANN 调参与维度迁移仍缓做，触发条件不变 |

## 变更记录

- 2026-09-13 初版：由 handoff「缓做/低优项」段落迁移建立，触发条件依据 08/10 文档。
- 2026-09-13 新增 D12（i18n 库落码与英文翻译）/D13（组件描述多语言）：随 17 文档《前端国际化与设计 Token 方案》登记。
- 2026-09-14 D10 拆分为 D10a（CI+gitleaks 门禁，GitHub Actions，进行中）与 D10b（CD/自动化部署，仍缓做，触发条件：远程部署目标确定）；CI 不依赖部署目标，随种子试用前质量门提前启动，ADR T8 见 10 文档 §4。
- 2026-09-14 D10a 完成并移出本表：`.github/workflows/ci.yml` 三道门（gitleaks 全历史、后端 pytest、前端 oxlint+vitest+build），本地等价命令全绿；留痕 handoff Recently shipped。
- 2026-09-14 D9 完成并移出本表：新增 `scripts/benchmark.py`（stdlib 零依赖），BENCHMARK.md 填首份基线（OODA 循环 ~488 loops/s、退款端到端 p50 2.28ms 等，commit 407812e）；LLM 决策/记忆读写/并行扇出等待接入补测项在 BENCHMARK.md Scope 标注。
- 2026-09-14 新增 D14（LLM 判断分支）/D15（表达式函数库与算术）：condition 节点首版只落安全规则表达式（比较/逻辑/布尔，纯 stdlib 禁 eval），契约见 04 §5.1/§5.2。
- 2026-09-14 新增 D16（遍历循环 foreach/item/结果聚合）/D17（循环体内 break/continue）：loop 节点 v1 只落条件循环 while（continueExpression + maxIterations fail-safe，仅暴露 index），契约见 04 §5.3。
- 2026-09-14 新增 D18（parallel any_success OR-join 与分支取消）：parallel 节点 v1 只落 all_success/all_completed 两策略（all_success 分支失败 fail-safe 汇聚不短路），契约见 04 §5.4。
- 2026-09-14 新增 D19（wait 事件等待/中断-恢复/动态时长）：wait 节点 v1 只落 duration 定时等待（1-600 整数秒同步 sleep），事件等待需 interrupt/checkpointer + 持久化，契约见 04 §5.5。
- 2026-09-14 新增 D20（human_approval 持久化中断-恢复/多实例/通知/动态审批人）：v1 只落进程内 ApprovalBroker（token+threading.Event、REST 决策、10-3600s 超时自动 approve/reject），进程重启 pending 即失，契约见 04 §5.6；与 D19 同批中断模型（S1 就绪或真实审批业务触发）。
- 2026-09-14 新增 D21（subgraph 版本钉版/升级回归、市场与共享、远程引用、嵌套可视化、子图内审批交互）：v1 只按 graphId 引用已保存图（进程内重入 run_graph、深度上限 3、单出边 fail-safe、子图事件压制），契约见 04 §5.7；触发：模板库/发布流或多租户。
- 2026-09-15 新增 D22（API 适配器产品化：OpenAPI 导入/Schema 表单/凭证加密与秘密变量/OAuth2 刷新/SSRF egress/重试熔断/操作录制与 NL 生成适配器）：API 适配器 v1 只落通用 `http/request`（新包 httpapi，env/注册默认连接 + 节点 params JSON，4xx/5xx 仍 SUCCESS，无加密/无 OAuth/无 SSRF 防护/无重试），契约见 04 §4.6；触发：真实外部系统接入或多租户。
- 2026-09-15 新增 D23（数据适配器产品化：JDBC/ODBC/数仓多源、只读账号强制与 SQL 审查、Schema 表单、查询审计、分页流式）/D24（消息适配器真实投递：SMTP/IM/短信/webhook、模板系统、reply 线程、入站消费、限流重试）：DB v1 只落 database/query+execute 裸 SQL（新包 database，ATLAS_DATABASE_URL 与平台库隔离 + SQLite demo），消息 v1 只落进程内 message/send（新包 message，记录不投递），契约见 04 §4.7/§4.8、ADR T10；触发：真实外部数据库/通知业务接入。
- 2026-09-15 新增 D25（模板库产品化：用户 CRUD、市场/组织共享、远程导入导出、版本钉版升级、搜索评分、参数化向导）：模板库 v1 只落随代码发布的 5 个只读内置模板（新包 template，GET 只读 API、整画布替换、无 DB/CRUD、reset 不影响），契约见 04 §5.10、ADR T11；D13（元数据多语言）加注：模板元数据结构已落地，翻译仍随 i18n 批次；D21 加注：模板库 v1 不含子图市场/钉版，触发条件未满足。
- 2026-09-15 新增 D26（录制回放产品化：批量回放与报告、跨版本/配置更新差异对比、影子模式、Mock 工具响应、用例编辑与入参参数化、subgraph 快照内联、持久化与共享）：操作录制 v1 只落单用例运行录制 + 一键回放比对（新包 recording，图快照冻结、客户端收集 node_end、审批预置保确定、显式易变键归一化，进程内 reset 保留/重启清空），契约见 04 §5.11、06 §6.9、ADR T12；D22 中「操作录制生成适配器」指 04 §4.5 浏览器操作录制→工具生成，仍随 Web 适配器在 D22 缓做，与本项的运行录制不是同一含义；R2 三层定位回放同样随 D22。
- 2026-09-15 新增 D27（单步调试产品化：持久化中断-恢复/多实例、断点持久化与共享、变量改写续跑、变化历史与来源追踪、子图断点、运行中急停、命中计数）：调试 v1 只落进程内会话（新包 debug，会话级断点随 /run/stream 传入不进 Graph JSON、step/continue/stop、paused 只读快照、条件 fail-safe、reset 释放），契约见 04 §5.12、06 §6.10、ADR T13；持久化中断与 D19/D20 同批（S1 就绪触发）。
- 2026-09-15 新增 D28（监控告警产品化：OTel/Prometheus/Grafana 正式栈、持久化多实例、适配器调用级指标、规则表达式 DSL、外部告警出口、静默/升级/值班、按租户隔离、长保留报表、Trace 钻取）：监控 v1 只落进程内运行观测 + 站内告警中心（新包 monitoring，API 层两入口埋点、ring 200、运行级+节点级、成功率/p50/p95/失败节点 Top、四内置规则阈值可调、未关闭按图合并、ack/resolve、reset 清空、零新依赖），契约见 04 §5.13、06 §6.11、ADR T14；正式栈与 D11 在部署后合流，持久化随 S1，外部出口与 D24 同批，租户隔离随 D7；D11 加注 v1 不替代正式栈。
- 2026-09-16 D7 取回并落 v1（触发条件沙盘假定满足，与监控同批：无真实 C1-C5 数据）：新增 `src/atlas/iam/`——种子两租户四账号（t1 admin/operator/viewer、t2 admin，明文仅 Demo）、`POST /api/auth/login` 换进程内 `sess-` token（SessionStore 单锁、重启即失）、viewer/operator/admin 三角色端点级白名单 RBAC（401 未登录/403 角色不足/404 跨租户对象，不泄漏存在性）、图/录制/反馈/监控/审批/调试/消息每租户独立 store 实例（TenantRegistry 惰性装配，计数各自从 1，tenant 不进资源 JSON）、无平台超管两租户完全隔离、reset 收窄为 admin 本租户（录制/反馈保留）；模板/适配器/demo shop 与 mock/出向连接为全局基础设施。契约见 04 §5.14、06 §6.12、12 §3.10、ADR T15。**仍缓做的 D7 愿景部分及去向**：持久化用户/多实例会话随 11 S1；密码哈希/注册/改密/SSO/JWT/秘密变量角色可见随 D22；节点/工具/变量级权限与动态审批人随 D20；配置/执行审计与配额随 Phase 3（04 §二7/§五16）；组织内共享/跨租户市场/计费随 D3/D25；platform_admin 跨租户视角、CORS 与真实网关集成随 D6。D28 的「按租户鉴权与指标隔离」子项随本批落地。
- 2026-09-16 新增 D29-D34（来源：docs/19 题述设计方案，提案未立项部分登记触发条件）：D29 Schema 驱动配置内核通用化（SchemaRegistry/UISchema/WidgetRegistry/L1 AJV/结构化诊断，触发：第六类实体表单需求或 D22/D23 表单化）；D30 变量作用域 v2（受限来源/动态路径/类型级校验/数据依赖环，触发：嵌套输出真实需求或秘密变量批次）；D31 多智能体任务总线（任务信封/幂等/CAS/冲突升级，触发：第二个真实 Bot 角色）；D32 Graph 版本化与灰度发布（不可变版本/Router/在途钉版/指标门控回滚，触发：真实生产发布，与 D5/D6/D10b/S1 同批）；D33 交互模板系统（双向卡片 Schema 与渠道渲染降级，触发：首个真实 IM/邮件渠道，与 D20/D24 同批）；D34 span 级链路追踪（触发：D31 任务总线或 D11 OTel 栈）。同期在 08 立「设计态内核前置项」（Capability input/outputSchema 声明 + 拓扑作用域索引 + L2 模板引用校验），该项不属缓做、见 08 Phase 2 延伸立项。
- 2026-09-16 **D29 进度注记（M1 立项，不解除缓做）**：08 正式立项 M1「内置节点 Data Schema 提取」（docs/20 §2.2），启动 D29 的**前半段「节点 Schema 内化」**——九类内置节点 config 提取为声明式 Data Schema 包（`frontend/src/lib/schemas/`，MetaSchema 四 `x-*` keyword 权威见 04 §4.9 扩展条，索引用例 U35/U36）；SchemaRegistry 仅内置节点单一来源，零 UI 变化。D29 整体仍缓做：工具/技能/记忆/部署/卡片五类实体多来源入册、UISchema/WidgetRegistry/表单生成（M3/M4 另行立项）、L1 校验器（M2，先收口 ADR T16）、结构化诊断/quickFix/Web Worker 均未启动，原触发条件不变。
- 2026-09-16 **D29 进度注记（M1 落码，不解除缓做）**：M1 十个原子提交（4ade416→51cb57f）落地——MetaSchema 递归自检、九份节点 schema（含 x-outputSchema 与 scope.ts 逐字段对齐）、最小只读 SchemaRegistry、schema 派生 L1 与手写校验 dev 双跑（U35/U36 转正式，13 §9 验收：vitest 189/14 文件、浏览器九节点冒烟零差异零分歧）。手写 L1 校验按迁移观测期保留（迁完一个删一个），`x-widget` 仅声明不消费。D29 其余部分（五类实体入册、WidgetRegistry/FormRenderer、UISchema、独立 L1 校验器、结构化诊断/quickFix/Worker）仍全部缓做，触发条件不变；下一前置门原为 ADR T16（M2 立项前收口），**T16 已于 2026-09-16 收口（手写最小子集、不引 AJV，见下条变更记录），M2 待 08 立项**。
- 2026-09-16 D22 触发口径澄清（用户拍板）：iam 多租户 v1（种子账号沙盘，2026-09-16 落地）**不触发** D22 整体取回——「多租户」触发条件指真实租户数据/部署实例；OpenAPI/OAuth2/连接 CRUD/重试熔断/录制与 NL 生成适配器等产品化功能整体仍缓做。同时在 D22 内嵌「外部系统接入安全准入清单」，不随功能包缓做，任一真实外部系统接入时先于功能落地（与 D23「接入前必须先落只读账号/审查/egress」同批）：① SSRF egress 白名单 + 私网/云元数据地址拦截（http 适配器 v1 允许绝对 URL，SSRF 为既有服务端可达面）；② 秘密变量运行时注入（承载点为设计态前置项的字段级可见机制 `x-secret-allowed`，见 08 Phase 2 延伸项、D30）；③ 凭证加密存储（不写明文 env/配置）。
- 2026-09-16 **ADR T16 收口注记（不解除 D29 缓做）**：前端 L1 校验器形态经用户拍板定为**手写最小 JSON Schema 子集，不引 AJV**（10 文档 §4 T16 行）；M1 已落的 `lib/schemas/validateConfig.ts`（154 行）经 102 个双跑用例对九节点手写规则零分歧，九份 schema 实测仅 1 处 oneOf、零 if-then/dependencies。M3 重开判据：白名单外第二类结构落地且解释器需显著扩张、或候选分支语义无法维持时，携实测案例重开 T16 评 AJV。D29 整体仍缓做，M2（L1 扶正 + 结构化诊断）待 08 立项。
- 2026-09-16 **D29 进度注记（M2 立项，不解除缓做；本批只落文档）**：08 正式立项 M2「L1 字段校验器扶正 + 结构化诊断」（docs/20 §2.3）——M1 解释器扶正迁入新包 `frontend/src/lib/validation/l1.ts`（RFC6901 pointer、FIELD_* code，删 dev 双跑脚手架与已覆盖手写规则），`diagnostics.ts` 落 19 §1.5.1 Diagnostic 模型（severity/layer/code/message/loc{nodeId,pointer,token}/quickFix?）与 rank，scope.ts L2 三码升级为同构诊断（补 raw/字段 pointer），`validateGraph.ts` 聚合供 PropertyPanel/角标共用（renderMarkers 仅预留挂点）；后端 compile 422 在 detail:string[] 之外增稀疏 `locations` 侧车（06 §6.13，文案/状态/顺序不变，410 零回归）。候选用例 U37/U38。**D29 整体仍缓做**：quickFix 动作（仅留类型位）、Problems 面板、L3 前端预判、增量调度/Web Worker 随 M4；UISchema/WidgetRegistry/表单生成随 M3；五类实体多来源入册触发条件不变。
- 2026-09-16 **D29 进度注记（M3 立项，不解除缓做；本批只落文档）**：08 立项 M3「WidgetRegistry + FormRenderer（工具参数表单化首期）」（docs/20 §2.4；ADR T17 同日收口：自研最小表单引擎、既有 AntD 承载、零新依赖，不引 rjsf/Formily，M4/M8 设重开判据）。取回 D29 中段：新前端包 `lib/forms/`（WidgetRegistry 八内置控件 + registerWidget 扩展点只导出、FormRenderer 递归表单、resolveWidget 选择/降级序、TemplateTextArea 变量控件），SchemaRegistry 增第二来源（工具 input_schema 取自 /api/adapters 发现快照，键 `<adapter>/<tool>`），首个消费者仅 tool_call 的 params（config.params 仍为 JSON 字符串，Graph v1 不变；oneOf/无类型/空 schema/未注册工具/裸插值串降级旧 TextArea）。手写 Config 只迁 ToolCallConfig 一个，其余七个渐进迁移（HumanApprovalConfig 随 M4），每迁一个一原子提交。**D29 整体仍缓做**：UISchema/ui:order/ui:group/ui:hidden 与 if/then 条件显示、Problems 面板、增量调度/Web Worker、quickFix 动作随 M4；业务自定义控件/控件市场、技能/记忆/部署/卡片四类实体入册（卡片随 M8）触发条件不变；本立项是 D22「Schema 驱动参数表单」的内部形态，不含 D22 功能包。
- 2026-09-16 **D29 进度注记（M2 落码，不解除缓做）**：四代码提交 234a95f→fbd9f77 按 08 五段原子序落地——新包 `frontend/src/lib/validation/`（diagnostics 类型+rank、l1 扶正迁入、validateGraph 聚合 L1+L2、markers 挂点），PropertyPanel/AtlasNode 切 Diagnostic[] 且 validateNode:string[] 下线无残留，后端 `validate_graph_report()` 产 (messages, locations)、compile 422 稀疏侧车（图级无条目/拓扑仅 nodeId/字段 RFC6901 pointer，子图归顶层父节点），detail 中文逐条字节零回归（U37/U38 转正式，13 §9：后端 420 passed/8 skipped、前端 vitest 168/17 文件、真实浏览器九节点冒烟 + 实测 422 26 detail/20 locations 下标对齐、截图 docs/assets/m2-nine-node-smoke.png）。D29 整体仍缓做：quickFix 动作（仅类型位）、Problems 面板、L3 前端预判、增量调度/Web Worker 随 M4；UISchema/WidgetRegistry/FormRenderer/表单生成随 M3（M3 立项时 T16 重开判据激活）；五类实体多来源入册触发条件不变。
- 2026-09-17 **D29 进度注记（M3 落码，不解除缓做；补登）**：五提交 2de5053→6d4a863 按 08 五段原子序落地 `lib/forms/`（registry/resolveWidget/widgets/formTree/FormRenderer/toolSchemas/params/nlWarnings），ToolCallConfig 切 FormRenderer，config.params 仍为 JSON 字符串、后端零改动、零新依赖，U39 转正式（前端 vitest 243/24 文件，浏览器双路径 + sql-query-notify 金链零回归，截图 docs/assets/m3-params-form-smoke.png）。
- 2026-09-17 **D29 进度注记（M4 立项，部分取回、不解除整体缓做；本批只落文档）**：08 立项 M4「UISchema 最小子集 + 手写 Config 迁移第二波 + 增量调度 + 前端 L3 预判 + Problems 面板 + reverseDeps/quickFix v1」（docs/20 §2.5；契约见 08 M4 立项条、03 `ui_schema`/`graph_diagnostics`、04 §4.10/§6.5 末扩展条，候选用例 U40/U41/U42）。立项前复杂度 dump 后 **ADR T17 复查不重开**（8/9 节点零条件结构，UISchema 真实需求仅 ui:group 1 处 + 条件显隐 1 处，自研 <100 行）。**本次部分取回 D29 的形态**：① UISchema 仅 ui:group + hiddenWhen 两件最小能力（不引完整规范/if-then 动态依赖）；② **Problems 面板是 D29「结构化 Diagnostics 面板」的首次落地**（全图 Diagnostic 聚合 + rank + nodeId/pointer 点击定位）；③ quickFix v1 仅「删除悬空引用」一个动作 + reverseDeps 改 id 引用定位；④ 前端 L3 首批两条（不可达/环）同构 dsl.py 实时预判（后端仍唯一权威）；⑤ 增量脏标记 + L1 同步/L2 防抖/L3 idle 调度 + 200/500 节点基准（Web Worker 阈值证实前不引）；⑥ HumanApprovalConfig 必迁、其余五个手写 Config 从简到繁逐个原子迁移。**D29 整体仍缓做**：技能/记忆/部署/卡片五类实体多来源入册（卡片随 M8 成为第三类 schema 来源，届时按 T17 重开判据评 rjsf/Formily）、业务自定义控件/控件市场、完整 UISchema 规范、Web Worker、secret/env/session 来源、REF_TYPE_MISMATCH（D30）触发条件均不变。
- 2026-09-18 **D34 进度注记（M10 立项，部分取回、不解除整体缓做；本批只落文档）**：D34 触发条件「D31 任务总线落地」已由 M7（0f934d6→e46fc09）满足，08 立项 M10「span 级链路追踪」（docs/20 §4.5；ADR T21 同日收口：自研与 OTel 同形最小 span、纯 stdlib 零新依赖、**不引 OTel SDK**；候选用例 U49–U52）。取回范围＝进程内：traceId/spanId/parentSpanId 贯穿 run→节点→parallel/subgraph 内部→M7 任务信封→工具、graphVersion 标注、SSE 三帧升级为 19 §2.3.4 超集、RunRecord 挂 traceId、contextvars 进程内传播、内部 span 可选不外泄折叠、录制归一化剔除 span 键。**仍缓做**：跨服务/跨进程传播（随 D5 NATS/W3C Trace Context）、OTel SDK/Collector/Exporter 与 Prometheus/Tempo 正式栈合流（随 D11）、span 持久化/导出后端、前端 trace 瀑布 UI；触发条件不变，沙盘期不解除 D34。**2026-09-18 落码收口**：上述进程内取回范围已全部落地（四批原子提交 1f21b1a span 模型 / 89f5645 埋点+SSE 超集 / c227f0c 信封+RunRecord+api 入口+debug 不埋点 / 2b1dbe9 录制归一化），U49–U52 转正式（13 文档），后端 486 passed/13 skipped、前端 lint 0 错/359 passed/build 成功、前端零改动；仍缓做项（跨服务、OTel 合流、持久化、瀑布 UI）与触发条件不变。
- 2026-09-18 **D33 进度注记（M8 立项，部分取回、不解除整体缓做；本批只落文档）**：08 立项 M8「交互模板系统（审批卡片双向 Schema + 三渠道渲染降级）」（docs/20 §4.3；候选用例 U53/U54；T17 复查不重开、不引 rjsf/Formily、零新依赖、不新增 ADR）。取回范围＝进程内沙盘：新包 `src/atlas/cards/`（内置只读目录照 template 包，v1 一张 refund-approval，无 DB/CRUD、reset 不影响）、CardTemplate 双向最小子集（fields 只读 bindings + textarea/input 可编辑 + actions.output 经 map_action_output 回写 decision/comment）、render_card 三渠道（web 结构化投影由前端 CardRenderer 经 FormRenderer kind:"card" 渲染、im 文本+两按钮回调 URL+detailUrl、email 只读 HTML+带 token 链接+timeoutHint；邮件链接为 GET 落地页、GET 不产生决策副作用）、human_approval config 可选 cardTemplateId（不填＝summary 旧路径完全不回归、非空未命中目录编译 422）、bindings 复用 graph.loader.interpolate 并纳入该审批节点 visibleAt 的 L2、决策端点纯超集 actionId/form、M5b 中断帧带 cardTemplateId 恢复时从帧 outputs+trigger 重建上下文、to_message_params 产 message/send 入参但 v1 不自动外发。**仍缓做、沙盘期不解除 D33**：真实 IM/邮件渠道接入与凭证/OAuth（随 D24）、自动通知触发（随 D20）、邮件 GET 决策落地页与网页嵌入 SDK、卡片 CRUD/市场/持久化/版本化（随 D25/D3）、05 §5.3 愿景大 Schema（style 主题/布局拖拽编辑器/conditional_display/channel_adaptations/模板市场）、表单回写任意节点；触发条件「首个真实 IM/邮件渠道接入（与 D24/D20 同批）」不变。
- 2026-09-18 **D26 进度注记（用例集报告 v1 立项，部分取回、不解除整体缓做；本批只落文档）**：M9 已取回发布前批量回放门禁（即时 GateReport 不沉淀、无历史），本次立项再取回「批量回放与用例集报告（通过率/趋势）」中的报告沉淀/历史/趋势最小集——新模块 `recording/reports.py`（ReleaseReport + ReportStore ring 100/租户，照 RoutingStore 先例不进 Repository 抽象/不 PG 化），release-gate 与 publish gate 运行沉淀（trigger=manual/publish-gate，含 skipped/blocked），GateReport 纯超集加 id，`GET /api/graphs/{id}/release-reports` 与 `.../{rid}` 历史/详情，reset 清空报告而用例保留，前端 ReleaseModal「历史报告（通过率趋势）」折叠区（AntD Progress/Tag，零新依赖）；候选用例 U60，原子序与同步面见 08 D26 立项条。影子模式/Mock 工具响应/用例编辑参数化/subgraph 快照内联/PG 持久化多租户/跨图看板/定时 CI/导出仍缓做，触发条件不变。
- 2026-09-18 **D26 进度注记（用例集报告 v1 落码收口，部分取回、不解除整体缓做；后端 615/前端 404，U60 转正式）**：立项当日按四批原子序落码——立项 docs-only `71153a8`；`9dc9690` 新模块 `recording/reports.py`（ReleaseReport/ReportCaseRow + ReportStore ring 100/租户，rr-N 租户级单调计数、reset 归零，pass_rate total=0 为 null）+ release-gate/publish-gate 两触发沉淀（manual/publish-gate，含 blocked/skipped，409 体带 id）+ GateReport 纯超集加 id + TenantServices 两档装配/reset 清报告留用例；`5c812c5` 两 GET 历史倒序摘要/详情（read 角色，图不存在/跨图/跨租户 404）；`f5c789c` 前端 ReleaseModal「历史报告（通过率趋势）」折叠区（Progress/未覆盖 Tag/展开懒加载逐例）+ release.ts 纯函数与 vitest。API 沙盘 `scripts/dev/d26_smoke.py` 24 断言全过、真实浏览器冒烟 2 截图 `docs/assets/d26-report-{trend,detail}.png`。**仍不解除 D26**：影子模式、Mock 工具响应、用例编辑与入参参数化、subgraph 引用快照内联、用例/报告 PG 持久化与多租户共享、跨图报告看板、定时 CI 批量回放、报告导出仍缓做，触发条件不变。
- 2026-09-18 **D32/D26 进度注记（M9 已落码，部分取回、不解除整体缓做；批 1-4＋收口，后端 602/前端 396）**：08 立项 M9「入站 Router + 灰度发布 + 指标门控自动回滚」（docs/20 §4.4；ADR T22 同日收口：进程内 RolloutConfig 模型、固定序三段分桶纯函数、RoutingStore 进程内照 TaskStore 先例、pin-to-version 复用 M6/M5b 不新增机制、门控求值置 API 层 record_run 之后、零新依赖后端纯 stdlib；候选用例 U55–U59）。D32 取回范围＝进程内沙盘：四段规则（internal 租户 allowlist / lowValueBucket payload.amount 阈值 / canary 订单 id 哈希百分比 / full）、rollout 状态机（configure/start/promote/rollback，promote 仅手动、rollback 幂等）、在途实例钉版、业务指标门控（run_error_rate/manual_escalation_rate/refund_amount_diff_rate，越阈自动回滚 + rollout_gate 告警 action，回滚只切流不改外部事实）、发布门禁与前端发布/版本/灰度 UI。D26 同步部分取回「发布前批量回放 + 跨版本 diff」（见 D26 行）。**仍缓做、沙盘期不解除 D32**：真实 ingress 服务器与签名验签、多实例路由表热同步/配置中心、独立网关进程、自动/智能放量、when/业务桶规则 DSL、A/B 显著性分析、RoutingStore PG 持久化（随 11 S1 批次评估）；触发条件「真实生产发布或多实例部署（与 D5/D6/D10b/S1 同批）」不变。

- 2026-09-19 **D26/D30 进度注记（收尾小项打包立项，部分取回、不解除整体缓做；本批只落文档）**：主线 M1–M10 与 D26 报告 v1 闭合后，取回五个工程内可闭环小项，范围/非目标/契约门/原子序见 08「D26/D30 收尾小项打包立项」条。D26 取三项：报告导出 CSV/JSON、subgraph 引用快照内联（录制冻结、回放内联优先）、定时 CI 批量回放（cron workflow）；D30 取两项：REF_TYPE_MISMATCH 类型级校验（前端 L2 warning）、GRAPH_DATA_CYCLE 数据依赖环（前后端同构 L3 error）。零新依赖、不新增 ADR；D26 的影子模式/Mock 工具响应/用例编辑参数化/PG 持久化多租户/跨图看板，D30 的受限来源/动态路径/深层展开/quickFix 联动均仍缓做，触发条件不变。

- 2026-09-19 **D26/D30 进度注记（收尾小项落码收口，部分取回、不解除整体缓做；后端 628/前端 418）**：上条立项五项按原子序全部落码——立项 docs-only `d4cd42b`；D26-b `29bb3d9` 新建 `recording/snapshots.py`（collect_subgraph_snapshots 递归冻结 + inline_first_resolver），RecordingCase 增 `subgraphs` 纯超集，**内联仅用于单用例 replay 端点；发布门禁 gate 保持实时租户 resolver 不内联**（门禁验当前 latest 草稿，子图被删/改坏应 block，靠旧快照会让坏草稿假通过；此为对照立项原文的契约订正）；D26-a `8a37b4e` report_to_csv + `release-reports/{rid}/export?format=csv|json`（read、attachment、BOM、非法 format 422、404 照详情）+ 前端导出列；D26-c `cea70f4` `.github/workflows/release-gate-cron.yml`（周一 03:17 UTC cron + workflow_dispatch，起 uvicorn→登录轮询→d26_smoke，本地实测 24 断言全过；cron 仅 main 默认分支生效）；D30-b `9a357df` 后端 `_validate_data_dependency_cycles`（GRAPH_DATA_CYCLE，error 进编译 422）+ 前端 scope/l3 同构，engine 新增含模板投影的 graphDataSignature（数据环依赖模板、L3 须随模板重算）；D30-a `47ecf48` 纯前端 REF_TYPE_MISMATCH warning（仅 tool→tool 单模板叶子标量比对，number 接受 integer、integer 不接受 number、object/array/oneOf/拼接串/缺 schema 放行，不进后端 422）。零新依赖、不新增 ADR。**D26/D30 均不解除**：D26 影子模式/Mock 工具响应/用例编辑参数化/PG 持久化多租户/跨图看板，D30 secret/env/session 受限来源/parallel.result 动态路径/subgraph.outputs 深层展开/quickFix 联动，触发条件均不变。

- 2026-09-19 **D15/D17/D18/D30 进度注记（A+B 打包立项，部分取回、不解除整体缓做；本批只落文档）**：D26/D30 收尾批闭合后再取六项工程内可闭环小项，范围/非目标/契约门/原子序见 08「A+B 打包立项」条，三处语义已经用户拍板。**D15** 取 condition 表达式函数库——手写递归下降扩算术层（+ - * / %、括号、一元负号，除零 fail-safe）与白名单函数（数值 abs/floor/ceil/round/min/max、字符串 len/lower/upper、日期限纯构造与比较 date/year/month/day/daysBetween），禁 eval 红线不变、前后端同构，**不含 now()/today() 等非确定函数**（留可注入回放时钟批）。**D17** 取 loop 循环体 break/continue——拍板纯拓扑+控制状态、不新增节点类型：体内 condition 连 exitTarget 即 break（loop 置控制态强制退出、exitReason=break）、体内连回 loop 即 continue，DSL 放开对应回边/出口白名单。**D18** 取 parallel any_success（OR-join）——拍板只跳过未开始节点、不回滚已发起 tool/wait/human 外部事实，全分支皆失败才 failed。**D30** 再取三项余部：parallel.result.<入口id> 动态路径汇聚点可见性（入口 id 存在性 + 仅 joinTarget 之后可见）、subgraph.outputs 深层展开（可解析子图则按内部节点校验、解析不到降级放行，不建显式输出契约）、改节点 id/输出键的引用 quickFix v2 联动（重命名改写，区别于 v1 删除悬空）。**显式非目标**：D14 LLM 语义分支、D16 foreach、非确定日期函数、中断进行中分支与中断帧清理（D19/D20）、显式子图输出契约/钉版（D21）、D30 secret/env/session 受限来源与字段级可见。零新依赖、不新增 ADR；D14–D18/D30 整体仍缓做、触发条件不变。

- 2026-09-19 **D15/D17/D18/D30 进度注记（A+B 打包落码收口，部分取回、不解除整体缓做；后端 645/前端 436，U66–U71 转正式）**：上条立项六项按原子序全部落码——立项 docs-only `646ca79`；**A3/D15** `ac8c718` conditions.py/.ts 同构递归下降重写（算术 + 白名单函数 + 确定性日期 + 常量折叠 + 顶层布尔门，禁 eval，不含 now()/today()）；**B1** `ee534ef` parallel.result 仅汇聚后可见 + 入口 id 校验（dsl + scope）；**B2** `25db6df` subgraph.outputs 按已解析子图内部节点 id 深层校验（后端 `loader._subgraph_output_index` 编译期权威门，前端 buildScopeIndex 第四参就位但编辑器拉子图结构随 D21 接入、未解析降级放行）；**B3** `8496b2d` 节点 ID 属性面板可编辑 + validateNodeId + renameNode 经 rewriteTemplateHeads/renameNodeRefs 全量联动模板头/target/边；**A2/D17** `5f87654` break＝体内 condition 连 exitTarget（DSL 放开 + 合成 `__break__` 网关 + exitReason=break）、continue 复用回边，并修 conditional path map 键值不一致的真实 KeyError；**A1/D18** `f45daa9` any_success（`_branch_outcomes`/`_guard_any_success`，只跳过未开始节点、已发起外部事实不回滚、全失败才 failed、网关幂等 + done→END 防迟到残留重复执行）；另有 build tsc -b 类型修复 `4b9396e`。三道门后端 645 passed/前端 vitest 436 passed/oxlint 0 error/tsc+vite build 过，d26_smoke 24 断言全过零回归，零新依赖、不新增 ADR。**D15/D17/D18/D30 均不解除**：非确定日期函数（时钟批）、D14 LLM 分支、D16 foreach、D19/D20/D27 持久化中断/多实例/通知、D21 显式子图输出契约（含前端子图结构拉取接入 B2）、D22–D24 适配器产品化、D30 secret/env/session 受限来源与输出键改名联动，触发条件均不变。

- 2026-09-19 **新增 D35（长期记忆/长期上下文产品化余部）**：M11 立项（docs-only，设计全文 docs/26、ADR T23、08 M11 立项条）取回最小可用长期记忆——统一 memory_items（fact/preference 两类、kind 区分）、EmbeddingProvider 抽象 + 本地确定性 embedder（EMBED_DIM=256，signed hashing + 中文 unigram/bigram，纯 stdlib 离线且回放确定，不引 numpy；商业 embedding 留 provider 接缝缓做）、memory/remember(WRITE)+memory/recall(READ) 两适配器工具（走现有 tool_call/harness，params 由 Schema 自动生成，零新节点/DSL）、进程内 MemoryStore + PG/pgvector 两档（第九个 MemoryRepository、租户分区、迁移 006）、REST GET /api/memories[/search] + DELETE（admin）、前端记忆浏览页。**D35 整体缓做、不解除**：商业 embedding、working(Redis)、summary、case、自动提取、决策隐式注入、更新策略/衰减/保留期、PII/加密、memory_config 策略表单与第六类实体入册、跨运营体共享/市场、REST 写入、PG RLS/多实例、ANN 调参/维度迁移，触发条件见 D35 行；记忆空洞（docs/25 盘点六类实体唯一未动）由此立项取回。
- 2026-09-20 **D21/D26/D27/D28/D35 进度注记（工程内可闭环缓做项打包立项，部分取回、不解除整体缓做；本批只落文档）**：C+A+B 收口后，08 立项「工程内可闭环缓做项打包」（设计全文 docs/28，11 项四批，零新依赖/不新增 ADR/全部进程内 v1）。**D26** 取四项：PG 档 recordings 富字段缺口修复（迁移 008）、Mock 工具响应回放（仅单用例 replay、门禁不接）、用例编辑（仅 name/inputs）与入参覆写、跨图报告看板（报告仍不 PG 化）；**D27** 取三项：暂停帧变量变化历史、异常断点（resume 后原样重抛）、子图内部断点逐帧上屏；**D28** 取两项：适配器调用级埋点（tool_metric 耗时/错误码分位，迁移 008 加 monitoring_runs.tool_calls）、自定义告警规则 DSL（复用 conditions 白名单、禁 eval、fail-safe）；**D35** 取一项：记忆 REST 写入/编辑端点（POST/PUT operate、source=manual，删除仍 admin）；**D21** 取一项：子图版本升级体检（发布前只读、不阻断发布、v1 只扫顶层）。各条产品化余部（影子模式/持久化多租户/OTel 正式栈/商业 embedding/子图市场等）仍缓做、触发条件不变，沙盘期不解除上述五条。

- 2026-09-20 **D27 进度注记（docs/28 批 2 落码收口，部分取回、不解除整体缓做；后端 736/前端 473，U150–U165 转正式）**：立项三项进程内调试能力按原子序全部落码——`038dcaa` 暂停帧变量变化历史（DebugSession 易失 history 上限 50、相邻暂停 global 顶层键 diff、JSON-stable 基线防不可序列化值、resume 改写 seed 基线不入历史）；`9b378c2` 异常断点（BreakpointSpec.onException、节点逻辑 debug 下包 try、命中发 reason="exception"+error 帧、stop 抛 DebugStopped／step·continue 原样重抛不忽略、纯异常断点 before 早退防误停、非布尔 422）；`f6f7d30` 子图内部断点（_execute_subgraph 透传同一 controller/session、paused/debug_log 附 subgraphPath、终帧仍吞、DebugStopped 穿透不折叠 failed，旧 test_subgraph_interior_is_not_paused 契约反转为 is_paused_with_path）；前端 `c13886c`（apiClient 类型、editorStore.toggleBreakpointException、属性面板异常 Switch、暂停 Card 历史 Collapse/红 Alert/子图路径 Tag）。三道门后端 736 passed/20 skipped（净增 16）、PG integration 8 passed（批 2 无 DDL）、前端 473 passed/2 skipped/build 过/lint 0 error/2 既有 warning；d26/m11 smoke PG 档全过、httpx SSE 端到端验证、真实浏览器三场景截图 docs/assets/batch2-{variable-history,exception-breakpoint,subgraph-pause}.png。零新依赖、无新 ADR。**D27 仍不解除**：暂停帧持久化中断-恢复/多实例（随 D19/D20/S1）、断点落 Graph JSON/团队共享、跨节点变量血缘来源图、忽略异常继续、wait/approval/tool 阻塞中点强杀、多会话并发锁粒度，触发条件不变。
- 2026-09-20 **D28 进度注记（docs/28 批 3 落码收口，部分取回、不解除整体缓做；后端 764/前端 475，U166–U191 转正式）**：立项两项进程内监控能力按原子序全部落码——`a318d97` 适配器调用级埋点（ToolCallMetric/RunRecord.tool_calls 纯超集、loader 真实工具分支计时发 tool_metric、两真实运行入口只采顶层、mock 命中不发/子图内被命名空间白名单吞掉/debug 发而不采/回放门禁子图重入不采、PG 第 13 列 JSONB 沿用迁移 008 无 DDL、summarize_tools 按 tool 聚合，SIMULATED 纳 calls/simulated 不纳 failed 与延迟分位、真实样本 0 分位 null）；`11b1ba7` 自定义告警规则 DSL（CustomRule/RuleConfig.custom 缺省 []、RuleId 放宽 str 承载 custom:{cid}、Alert/AlertEvent 加 rule_name、validate_rules 中文聚合校验、evaluate_rules 末尾以扁平白名单上下文复用 graph.conditions.evaluate_expression 禁 eval、非布尔或任何异常 fail-safe 不告警不阻塞、合并键仍 rule_id+graph_id；关键坑：durationMs 取 int 毫秒满足严格同型比较；PG alerts v1 无 rule_name 列读回 null）；前端 `4256326`（apiClient tool_metric 帧与 custom 类型、monitoring ruleLabel 三级回退、Monitoring 页适配器调用 Card＋自定义规则 List 编辑器 crypto.randomUUID/逐行校验禁提交）；HTTP 冒烟 `dc46f20` d28_smoke.py PG 档全过。三道门后端 764 passed/22 skipped（净增 28＝埋点 10＋DSL 18）、PG integration 10 passed（+2，批 3 无 DDL）、前端 475 passed/2 skipped/39 文件（+2）、tsc+vite build 过、oxlint 0 error/2 既有 warning（ReleaseModal/RolloutModal）；d26/m11/d28 smoke PG 档全过、真实浏览器两截图 docs/assets/batch3-{tool-metrics,custom-rules}.png。零新依赖、无新 ADR。**D28 仍不解除**：OTel/Prometheus/Grafana 正式栈（随 D11）、运行记录 PG 长保留/多实例汇总/时序报表与版本对比、Trace 时间线钻取、子图内与 Mock 工具埋点、规则图级绑定/模板市场/静默/升级/值班轮换/自动恢复、外部告警出口（IM/webhook/邮件，与 D24 同批），触发条件不变。

- 2026-09-20 **D35/D21 进度注记（docs/28 批 4 落码收口，部分取回、不解除整体缓做；后端 782/PG 24/前端 482，U192–U211 转正式）**：docs/28 打包最后一批两项按原子序全部落码——`ec0fd81` feat(memory) REST 记忆新建/编辑（POST/PUT operate、source 固定 manual、白名单字段、两档同构 update、content 变重算 embedding，U192–U201）；`ba97088` feat(versioning) 发布前子图升级体检（versioning/upgrades.py 纯函数 + GET subgraph-upgrades read 只读不产版本，只扫顶层、不阻断、不检测草稿 dirty，U202–U211）；`19f31b4` feat(frontend) 记忆新建/编辑表单 + ReleaseModal 升级体检区（vitest 净增 7）。三道门后端 782 passed/24 skipped（净增 18 常跑＋2 PG integration skip）、PG integration 全量 24 passed、前端 482 passed/2 skipped/39 文件、tsc+vite build 过、oxlint 0 error/2 既有 warning；d26/m11/d28 smoke PG 档全过；真实服务权限矩阵实测（operator POST 201/DELETE 403、viewer POST·PUT 403、跨租户 GET 404）；真实浏览器两截图 docs/assets/batch4-{memory-edit,subgraph-upgrades}.png。零新依赖、无新 ADR。**至此 docs/28 四批 11 项工程内可闭环缓做项全部落码收口；D35/D21 均不解除**，产品化余部（商业 embedding、working/summary/case、自动提取、隐式注入、更新策略/衰减/PII/记忆市场/RLS；子图市场/共享/跨运营体/嵌套编辑器/草稿 pin 编辑器/草稿 dirty 检测）触发条件不变。

- 2026-09-21 **D7 进度注记（P0 批 2 生产认证落码收口，部分取回、不解除整体缓做；后端 822 passed/31 skipped、前端 509 passed/2 skipped，U227–U232 转正式）**：依据 docs/29 阻断项 #2、ADR T25、契约 docs/31，认证从「明文沙盘」升级为可交付账号口令体系，五后端原子＋一前端原子全部落码（`cd1133a` stdlib scrypt 哈希＋种子账号改哈希、`097c056` `iam_users` 迁移 010＋UserStore 内存·PG 两档/幂等 seeder、`e65e4df` 账号生命周期五端点、`4224578` 会话绝对 TTL 迁移 011、`36ab54e` 登录滑动窗口节流、`222552d` 用户管理页＋改密 UI），权威块 04 §5.17；curl 23 项 HTTP 契约全绿、真实浏览器 9 项冒烟过＋8 截图 `docs/assets/p0b2-*.png`。**仍缓做、触发条件不变（docs/31 §1.2 非目标）**：SSO/OIDC/SAML/企业微信飞书登录、MFA/2FA、邮箱找回/验证、开放注册/租户 CRUD/platform_admin、JWT/吊销黑名单/并发会话上限/「在其他设备下线」、完整登录与账号操作审计、多实例共享节流计数（PG login_attempts）、首次登录强制改密标记。出向适配器凭证加密/OAuth2 token 刷新/SSRF egress 白名单/DB 只读账号＋SQL 审查/重试熔断属 **D22/D23 安全准入清单**，纯逻辑校验层已随 [docs/32](32-真实接入安全准入契约设计.md) **于 2026-09-21 落码收口**（security/egress·secrets(AES-GCM,T26=a)＋database/guard＋httpapi/resilience，U233–U239、后端 1008 passed/31 skipped），真实渠道连接与真实审批送达仍随 D24。

- 2026-09-21 **D12/D26/D28 进度注记（docs/33「影子模式与监控告警补强打包」落码收口，部分取回、三条均不解除整体缓做；后端 1051 passed/34 skipped，U250–U278、U285–U299 转正式）**：立项 docs/33（`9fcf40b`）后按 12 原子序全部落码，零新依赖、无新 ADR。
  - **D12（i18n 续批，不解除）**：照 M12 dashboard 样板续抽 editor（框架/画布/左栏＋属性面板/调试台两原子，`97d61dc`/`ad12f98`、`0c299ef`/`4bf7c7f`、`3445576`/`f11dad5`、`bf9cdb6`/`888daf8`）、monitoring（`0c299ef` 批内）、memory 三 namespace 纯展示文案，zh-CN 填实、en-US 各 json 仍空 `{}`；技术专名（Graph/Loop/Harness/OODA/SSE/Trace/P50/P95/read/write/financial/administer/SHADOW_DRY_RUN）与后端中文 detail 不 key 化，跨页按钮走 `common:button.*`，i18n.test.ts 静态键/插值键校验。i18next+react-i18next、语言切换 UI、en-US 完整翻译、AntD locale 触发条件（首个英文使用者/出海）不变。
  - **D26（影子模式进程内 v1，后端 `cea7111`/前端 `c29ce15`，U250–U269，不解除）**：新模块 `recording/shadow.py`（ShadowRun 族模型＋`extract_shadow_events` 编译期 adapter/capability→permission 表短路〔READ 透传、写能力 SHADOW_DRY_RUN〕＋`compare_shadow` 纯函数＋ShadowStore ring 100/租户进程内、不 PG 化、reset 清空）；`run_graph(shadow=True)` 旁路装配（独立 tracer/approval broker、预置全部 human_approval approved、零污染——不写 run_store/RunRecord、不 evaluate_after_run、不发 tool_metric、不进告警/灰度，异常也沉淀 status=error）；4 REST（POST `/graphs/{id}/shadow-runs` 201、GET `/shadow-runs`、GET `/shadow-runs/{sid}`、POST `/shadow-runs/{sid}/compare`，发起/compare 为 operate、查询 read）；前端监控页「影子运行」卡＋编辑器对已保存图发起。线上流量自动旁路录制、定时/批量对比、影子 PG 化/多租户共享、与自动回滚联动仍缓做，触发条件不变。
  - **D28（Trace 钻取＋静默/惰性升级/值班，后端 `4579843`/`00bba8c`、前端 `ce6cc23`/`1804255`，U270–U278、U285–U299，不解除）**：⑤ `RunRecord.spans`（tracer.to_tree 根 dict）＋**迁移 012 `monitoring_runs.spans JSONB`**＋`GET /monitoring/runs/{id}/trace` 懒加载（列表端点剔除、无 spans 返 null）＋前端 Trace 时间线 Tab 三级瀑布（lib/traceTree.ts 纯函数）。⑩ `monitoring/silences.py` OpsStore——**静默** Silence（rule_id/graph_id 可空组合、duration 1-10080 分钟、reason ≤200、惰性过期无定时器、上限 100，命中只 `suppressed_count+1` 不建/不合/不升级，POST/GET/DELETE）；**未确认惰性升级** RuleConfig.escalation_ack_minutes（1-10080、缺省/null 不升级、显式 0/非法 422，仅 open warning、list/get 读时按 first_seen 幂等评估、记 escalated_at 按 critical 视口）；**值班轮换** OnCallSchedule（members 1-20 去重保序、index 取模、空表 rotate 409，仅告警新建指派 assignee、合并/静默不指派，GET/PUT/rotate）；GET read、写 administer、viewer 写 403。escalated_at/assignee/静默/值班全部进程内不 PG 化（PG 档告警在库、assignee 走 OpsStore map、escalated_at 读回幂等重评，reset 清空）。前端 OnCallBar、SilenceManager（建静默经 reloadKey 即时联动，缺陷修复 `90b942a`）、规则卡未确认升级开关、告警值班列/红色「已升级」Tag。OTel/Prometheus/Grafana 正式栈（随 D11）、外部告警出口 IM/webhook/邮件（随 D24）、自动轮换定时器/排班表、静默值班升级 PG 持久化与多实例、长保留时序报表仍缓做，触发条件不变。
  - 验证：内存档 d26/m11/d28 三 smoke 全过（d28 断言 trace kinds=['node','run','tool']、静默压下计数、升级 before=2/after=2、值班轮换、viewer 403）；PG 直连 integration（`ATLAS_RUN_INTEGRATION=1`＋DATABASE_URL，不设 ATLAS_STORAGE_BACKEND）43 passed 含 U299/trace U278/M11 pgvector；真实浏览器冒烟 4 截图 `docs/assets/d28-{oncall-alerts,silence-oncall,trace-waterfall}.png`。前端 vitest/build/oxlint 三道门全绿（oxlint 仍仅 ReleaseModal/RolloutModal 两处既有 warning，新代码零新增），数字以 13 落码行收口记录为准。

- 2026-09-22 **D10b/D11/D24 进度注记（docs/34 评审后的「对外交付层小批」落码，部分取回、三条均不解除整体缓做；后端 1073 passed/34 skipped、前端 555 passed/2 skipped/43 文件、oxlint 0 error/0 warning）**：依据 [docs/34](34-MVP上线就绪评审-2026-09-22复审.md) §五 P0/P1 清单，取回一批工程内可闭环、零新依赖的交付层能力：
  - **D10b（TLS 反代样例，不解除 CD）**：新增 `deploy/Caddyfile`（Caddy 2 终结 TLS、填域名后自动 Let's Encrypt、gzip、`/metrics` 按 remote_ip 网段白名单拦截）、`deploy/README.md`（启动/探针/Prometheus scrape/生产去 8000 直连说明）、docker-compose 新增 `caddy` 服务（`profiles: ["edge"]`，caddy-data/config 两卷，默认不起），atlas 服务补 `/api/ready` healthcheck。**CD 流水线（镜像推送/远程发布/自动回滚）仍缓做，触发条件「远程部署目标确定」不变**。
  - **D11（进程内 Prometheus 导出，不解除正式栈）**：新增 `observability/health.py`（就绪探针，内存档恒就绪、PG 档 `SELECT 1`，失败 503）与 `observability/metrics_export.py`（零新依赖手写 Prometheus 0.0.4 text exposition，label 转义、None/非有限数跳过）；新端点 `GET /api/ready`（无鉴权，200/503）与 `GET /metrics`（无鉴权，导出 atlas_up/storage_backend_info/tenants_active/runs_total{status}/success_rate/run_duration_ms{quantile}/tool_calls_total{tool,status}，仅聚合计数不含图名/错误文本/PII）；`TenantRegistry.all_tenant_ids()` 只读快照不触发惰性创建；测试 `tests/test_observability.py` 8 例。**OTel SDK/Collector 导出、Grafana dashboard、长期时序存储、多实例聚合仍缓做，触发条件「真实部署后」不变**；/metrics 无鉴权，靠反代网段白名单/安全组隔离。
  - **D24（SMTP email 子集，不解除 IM/短信/webhook）**：新增 `message/smtp.py`（`SmtpConfig.from_env` 读 ATLAS_SMTP_HOST/PORT/USERNAME/PASSWORD/FROM/USE_TLS，HOST 缺省返 None 回退进程内、FROM 缺失 fail-closed、STARTTLS + login、可注入 smtp_factory、进程级惰性单例 `get_smtp_sender`）；`MessageService(email_sender=...)` 在 channel=email 且注入 sender 时真实发信、记录标 `delivered="smtp"`、失败折算 `SMTP_SEND_FAILED` 且不落记录，非 email 渠道与未配置时维持进程内记录；registry 内存/PG 两档装配注入 sender；`.env.example` 增 SMTP 段；测试 `tests/test_message_smtp.py` 14 例（全 fake 不触网）。**IM（钉钉/企微/飞书）、短信、webhook、消息模板系统、分渠道专名、入站消息消费、限流退避/投递状态追踪仍缓做，触发条件「真实通知/触达业务」不变**；SMTP 口令当前走 env（与 LITELLM_API_KEY 同信任边界），生产应改 secret 信封注入。
  - 同期工程项：根目录落 **MIT `LICENSE`**（pyproject.toml `license="MIT"`、frontend/package.json `"license":"MIT"`，解除 docs/29 阻断 #4 的 LICENSE 子项）；落 `.pre-commit-config.yaml`（pre-commit-hooks 基础检查＋gitleaks＋前端 oxlint 本地钩子，对齐 CI 三道门，不引 ruff/black）；清掉 ReleaseModal/RolloutModal 两处历史 oxlint `set-state-in-effect` warning（状态重置改为渲染期 prev-open 追踪，数据获取 effect 仅保留 await 后 setState 并对 fetch-on-mount 加 scoped oxlint-disable，前端 555 全绿零回归），**oxlint 首次 0 error/0 warning**。

### 2026-09-22 注记（docs/35 真实接入交付批立项，T2–T6）

- 承接 docs/34 §五 P0/P1，工程内可闭环五包一次立项（形状权威 [docs/35](35-真实接入交付批契约设计.md)，落码按其 §9 原子序）：
  - **D20 邮件子集（T2）**：human_approval 可选 `notifyEmails` 挂起邮件通知（collaboration/notifications.py，fail-safe 不阻断图）；审批持久化中断余部/多实例/IM/钉钉/动态审批人/评论流/队列页/邮件一键决策落地页仍缓做，触发条件不变。
  - **D24 webhook 子集（T3）**：channel=webhook 单 URL POST JSON、过 SSRF egress、WEBHOOK_SEND_FAILED；IM（钉钉/企微/飞书）/短信/模板系统/入站消费/限流队列仍缓做。
  - **D22 OAuth2 子集（T4）**：generic OAuth2 授权码连接管理框架（CRUD/state CSRF/token AES-GCM 加密/刷新/连接测试/迁移 014/9 端点/管理 UI/无副作用回调页）；**不写死任何真实平台、不含真实业务 API 调用**，真实平台适配与联调仍随首个真实接入触发；OpenAPI 导入仍缓做。
  - **D11 Grafana 子集（T5）**：deploy/prometheus + deploy/grafana provisioning + atlas-overview.json + compose observability profile（纯配置、默认不起）；OTel SDK/Collector/长期存储仍缓做。
  - **P1 #8（T6）**：审计日志 ring+PG（迁移 013）+ 写操作中间件 + jsonl 导出（administer）；前端审计页/SIEM 缓做。
- 五包均**部分取回、不解除对应缓做条目**；真实种子客户验证（阻断 #5）、真实域名 TLS/CD（D10b）触发条件不变。
