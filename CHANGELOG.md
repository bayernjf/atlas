# Changelog

本文件记录 Atlas 仓库的可追溯变更里程碑。详细过程与状态见 [handoff.md](handoff.md)。

## [Unreleased]

### feat / iam+api+frontend（2026-09-16，dev 未推送）

- Phase 2 新增能力收口项「多租户权限 v1（账号登录 + 端点级 RBAC + 用户数据租户分区）」端到端落地，04 §9.4「多租户隔离，数据权限控制」首版实现（组件级权限/跨租户共享/审计合规仍为愿景，随 D20/D3/Phase 3）。契约：04 新增 §5.14 权威 blockquote（§9.4 与 §二 5-7 加注 v1 边界、§5.13 缓做注记更新），03 新增 `identity_session`（login 请求/响应、token、Principal、me/401/403 字段概览）并在 graph_definition/recording_case/feedback_item/monitoring/debug_session/collaboration_message 概览补「v1 按 tenant_id 分区、tenant 由 token 推断不进资源 JSON」，06 新增 §6.12 认证鉴权运行时（Depends 链、TenantServices 惰性装配、SSE 线程 principal 显式透传），09 目录树/模块映射新增 `iam/`（待定项 13 收口），10 §4 ADR T15（进程内不透明 token vs JWT、端点白名单 vs 节点级、每租户 store 注册表 vs tenant_id 透传、明文种子 vs 哈希依赖、无平台超管；10→02→09 三处同步），12 §3.10 内部接口 + REST 表 auth 三行与全表公开/viewer/operator/admin 四档鉴权注记（reset 改 admin·本租户范围，401/403/404 语义统一），13 新增 U31/I19 + §9 浏览器回填，14 D7 改 v1 落码并留边界注记（D28 同步），17 补认证界面 i18n 契约与错误码首批债（AUTH_INVALID_CREDENTIALS/UNAUTHENTICATED/FORBIDDEN/NOT_FOUND 建议码）。四项用户拍板：① 种子账号 + 进程内 token——`POST /api/auth/login` 返 `sess-`+uuid4.hex，`Authorization: Bearer`，SessionStore 单锁进程内、重启即失（同审批 token 模式，零新依赖；JWT/SSO/持久化会话随 11 S1/D22），401 前端自动回登录页；② 三角色端点级 RBAC 白名单制默认拒绝——viewer=全部 GET 只读 + `POST /api/feedback`，operator 加图 CRUD/编译/运行/流式/NL/录制写/审批决策/调试 resume/告警 acknowledge/resolve，admin 再加监控规则 PUT、`POST /api/demo/reset`、`GET /api/feedback`；不做节点级/工具级角色门（随 D20）；③ 用户数据按租户全分区——graphs/recordings/feedback/messages/approvals/debug/monitoring 每租户独立 store 实例（id 计数各从 1 起），templates/adapter 发现/demo shop+mock/demo SQLite 种子/出向 HttpApiClient·DatabaseClient 为全局共享基础设施，跨租户对象 404 不泄漏存在性、角色不足 403、未登录 401；审批/调试 uuid token 绑定所属租户 broker；④ 无平台超管——t1「演示企业 A」/t2「演示企业 B」两个平级租户完全隔离，跨租户市场/共享/platform_admin 缓做 D3/D25。种子：admin-a/admin123（admin）、operator-a/operator123（operator）、viewer-a/viewer123（viewer）、admin-b/admin123，明文仅 Demo（无注册/改密/哈希）。新包 `src/atlas/iam/`：`principals.py`（Role/Principal/SEED_TENANTS/SEED_USERS/authenticate/can）、`sessions.py`（SessionStore）、`registry.py`（TenantServices 七字段 + TenantRegistry.get 惰性创建 + reset_tenant：图/审批/调试/监控（规则回默认、计数器保留）/消息清空，录制与反馈不动）、`deps.py`（get_principal/require/services_for）。api/main 模块单例改由 TenantRegistry 持有，全部业务端点加鉴权依赖；sync /run 与 SSE worker 经 `_runtime_registry(services)` 显式透传租户 bundle（克隆全局适配器、只换租户绑定的 MessageHarnessAdapter）；`/api/demo/reset` 改 admin 门+本租户清理并保留全局 demo shop/SQLite 重建；/api/demo/**、/demo/shop、health、静态托管不加平台鉴权（模拟外部系统自带 X-Demo-Token/demo 登录）。`tests/conftest.py` autouse fixture 为既有模块级 TestClient 注入 t1 admin token，358 旧用例零语义改动。前端：`lib/auth.ts`（Principal/角色 rank 与 roleCan/ROLE_LABELS/localStorage 会话/UNAUTHORIZED_EVENT）、apiClient 统一 Bearer + 401 清会话发事件 + login/me/logout、pages/Login.tsx（antd 卡片表单、中文错误 Alert、四种子账号提示、底色走 `--atlas-color-bg-page` token）、App 启动恢复会话并监听事件、三页 Header UserBadge（租户·显示名·角色 Tag·退出登录）、Monitoring 规则卡仅 admin/操作列按角色、Editor viewer 隐藏写按钮。测试：后端 377 passed/8 skipped（+test_iam.py U31 十七例 +test_api_auth.py I19 十一例），前端 vitest 64（10 文件，+5），oxlint/tsc/build 绿；浏览器对真实 :8000+:5174 八场景实测：未登录拦截/坏密码/登录刷新保持、租户 A 建图运行+消息投递+失败告警+监控指标、admin-b 全空且跨租户 graph 404/阈值独立、viewer 全写操作 403 且 UI 写按钮不渲染/feedback 201 列表 403、operator 告警确认关闭/录制 201/reset 403、admin reset 范围正确（录制反馈保留、阈值回默认）、登出 401、控制台零错误。7 提交 7cbd509..5660718（7cbd509 docs 契约 / 3620545 iam 包+U31 / 64d34b3 API 鉴权分区+I19 / 7a2ed14 前端 / b0bf5d4+5660718 登录页 token/antd6 修正 / 2759a13 docs/17 补认证界面契约）；至此 08 Phase 2 新增能力清单全部落地；PR 待用户推送 dev 后发起。

### feat / monitoring+api+frontend（2026-09-15，dev 未推送）

- Phase 2 能力项「基础监控告警 v1（运行/节点指标 + 站内告警中心）」端到端落地，04 §9.3「实时监控运行状态，异常自动告警」首版实现（正式 OTel/Prometheus/Grafana 栈缓做 D11/D28）。契约：04 新增 §5.13 权威 blockquote（§9.3 三条加注 v1 以 §5.13 为准），03 新增 `monitoring`（总览行+RunRecord/metrics 投影/RuleConfig/Alert 字段概览），06 新增 §6.11 监控运行时，09 目录树/模块映射，10 §4 ADR T14（进程内 ring+同步评估而非 OTel/Prom 拉模型、API 层端点包装而非 loader 内埋点、内置阈值规则而非表达式 DSL、站内告警中心而非 message/webhook 出口，无新依赖；10→02→09 三处同步），12 新增 §3.9 内部接口+REST 六行（metrics/runs/rules GET+PUT/alerts 列表/acknowledge/resolve，reset 行补清空监控并恢复默认规则），13 新增 U30/I18 + §9 浏览器回填，14 新增 D28（持久化/多实例随 11 S1+D11、OTel 导出、适配器调用级指标与耗时分位、自定义规则 DSL、message/webhook/IM 出口、租户鉴权随 D7、超长保留报表、时间序列图表）。四项用户拍板：① 采集运行级+节点级——每 run 记 graph_id/mode/status/起止/耗时/逐节点成败与错误，聚合运行次数/成功率/p50/p95/节点失败 Top，纯 API 层埋点零节点类型分支，不到适配器调用级；② 站内告警中心——进程内库+独立前端监控页+未处理徽标+确认 acknowledge/关闭 resolve，不接 message 适配器/webhook/IM；③ 内置四类规则+阈值可调——run_error critical、node_failed warning、consecutive_failures 默认 3 critical、failure_rate 默认近 20 次/≥5 样本/≥50% critical，同 (rule_id, graph_id) 最新非 resolved 告警合并 count++/刷新 last_seen/last_run_id，resolved 后新发独立告警；④ 独立 /monitoring 页（App.tsx 手工 page union，Dashboard 加入口，Editor 不动）。v1 语义：健康=completed 且无失败节点，节点失败两形状 `output.status=="failed"`（parallel/subgraph fail-safe 折叠）或 `output.result.status=="FAILED"`（ActionResult 数据化失败），未捕获异常冒泡=run error（sync 500 / SSE error 帧）；只统计 sync /run 与非 debug /run/stream 两个真实入口，录制回放（replay-N 直调 run_graph）、subgraph 重入（emit=None）、debug 运行不记录；ring deque(maxlen=200)、单锁同步评估无后台线程、reset 清空运行/告警/streak 并恢复默认规则（不重置计数器）、重启即失；零新依赖、不碰 Graph/EdgeDSL version 1 与 node schema。新包 `src/atlas/monitoring/`：`records.py` RunRecord/NodeResult/MonitoringStore（ring+threading.Lock+streak+合并/确认/关闭/reset）、`alerts.py` RuleConfig/默认规则/validate_rules 中文错误/evaluate_rules 纯函数、`metrics.py` extract_node_results/percentile nearest-rank/is_healthy/summarize。api/main 模块单例 `_monitoring`：sync /run 包装记 completed/error（error 记录后原样 raise 保持 500），SSE worker 非 debug 时 recording_emit 收 node_end 再透传（error 帧用已采集 outputs 记部分节点，DebugStopped 不记）；`GET /api/monitoring/metrics`、`GET /api/monitoring/runs?graph_id=&limit=`（1-200，越界 422）、`GET|PUT /api/monitoring/rules`（PUT 全量替换，校验失败聚合中文 422）、`GET /api/alerts?status=open|acknowledged|resolved`、`POST /api/alerts/{id}/acknowledge|resolve`（未知 404、重复迁移 409，中文 detail）。前端 pages/Monitoring.tsx（指标六卡总运行/健康/不健康/成功率/p50/p95、告警表+状态过滤+确认/关闭按钮随态禁用、规则卡四 Switch+连续阈值/窗口/最小样本/失败率 InputNumber 保存、最近运行表图过滤与错误 Alert/逐节点嵌套表展开、5s 轮询、未处理徽标）、lib/monitoring.ts 纯映射（runHealth/formatDuration/formatTime/ruleLabel/严重度与状态色，3 vitest）、Dashboard hero「监控告警」入口；antd v6.6.3 两项弃用以独立 fix 收口（Statistic valueStyle→styles.content、InputNumber addonAfter→suffix）。测试：后端 358 passed/8 skipped（基线 329：+test_monitoring.py U30 二十例 +Demo API I18 九例——I18 以 reset 后种子单 12345 构造健康运行、sql-query-notify 空入参 params 非法 JSON 的既有 FAILED 路径作失败夹具、monkeypatch run_graph 抛 RuntimeError 作 error 夹具、独立线程轮询 /api/debug 驱动 debug 排除用例），前端 vitest 59（9 文件，+3），oxlint/tsc/build 绿；浏览器对真实 :8000+:5174 实测八场景：空态、refund-auto 健康运行指标全绿（5 总/2 健康/3 不健康/40.0%/p50 32ms/p95 56ms 实测口径）、sql-query-notify 三连败 node_failed warning 合并 count=3 last_run_id=run-5 与 consecutive_failures critical、运行行展开错误与逐节点表、确认/关闭后按钮禁用与状态 Tag、阈值 PUT 即时生效与 reset 恢复默认 3、debug 逐步/停止运行均不入监控，控制台零错误。5 提交 2362fdf..8551f90（2362fdf docs 契约 / c474a80 监控核心+U30 / c721f47 API+I18 / 91f6bcc 前端 / 8551f90 antd6 弃用属性修正）；PR 待用户推送 dev 后发起。

### feat / debug+api+frontend（2026-09-15，dev 未推送）

- Phase 2 能力项「单步调试与断点 v1（逐步执行/节点断点/条件断点/变量只读查看）」端到端落地，04 §三「调试与测试增强细节」8/9 首版实现。契约：04 新增 §5.12 权威 blockquote（§三 8/9 加注 v1 以 §5.12 为准、变量变化历史不在 v1），03 新增 `debug_session`（总览行+run/stream debug 入参、paused/stopped 帧、resume 请求响应字段概览），06 新增 §6.10 调试运行时，09 目录树/模块映射/待定项 11 收口，10 §4 ADR T13（进程内 Event broker 而非 LangGraph interrupt、会话级断点而非 node_schema 持久化位、统一执行器钩子而非节点类型分支、变量只读而非改写续跑，无新依赖），02 选型总览同步，12 新增 §3.8 内部接口+REST（POST /api/debug/{token}/resume、GET /api/debug、run/stream debug 字段、reset 释放调试会话），13 新增 U29/I17 + §9 浏览器回填，14 新增 D27（持久化中断-恢复/多实例随 D19/D20、断点持久化进图与团队共享、变量改写续跑、变化历史与来源追踪、子图内断点、运行中急停、命中计数）。三项用户拍板：① 会话级断点——存前端编辑器状态随请求传入，不进 Graph JSON（EdgeDSL/Graph version 1 不变、node_schema breakpoint 预留位不启用），刷新即失；② 变量只查看——paused 帧下发 globals+outputs 深拷贝只读快照；③ 单个「调试」入口——Header「调试」启动即 step 模式（首节点前暂停），工具条「下一步(step)/继续(continue)/停止(stop)」；continue 关逐节点后仅断点（含条件）命中再停。新包 `src/atlas/debug/`：`sessions.py` DebugStopped/DebugSession（dbg-uuid、step_mode 初始 True、cancelled、条件错误记录、per-session `_gate` 锁串行化并行暂停、首决生效 Event、深拷贝快照）/DebuggerBroker（create/get_session/list_pending/reset）；`controller.py` DebugController.before_node 在 node_start 之后节点逻辑之前单点判定 step/breakpoint/condition，条件复用 graph.conditions.evaluate_expression 同形 context，ConditionEvalError fail-safe 不命中。loader 唯一包装点 `_make_executor.execute` 接入钩子（零节点类型分支），human_approval 调试路径先发普通 node_start、暂停、注册审批后再发携 approval 的第二个 node_start，subgraph 重入不传控制器（子图整段执行），合成 `__join__` 网关天然不暂停。API：/run/stream 带 debug 建会话（未知节点/坏表达式/坏 breakpoints 聚合中文 422），worker 捕 DebugStopped 发 `event: stopped` 哨兵帧后正常结束流（无 result）；同步 /run 带 debug 422 引导流式；reset 对全部调试会话按 stop 放行防线程悬挂；录制回放不接控制器。前端：editorStore 会话级 breakpoints（toggle/表达式/清空，loadGraph 与删节点联动，不进 serializer）；AtlasNode 左侧断点圆点（hover 显现、启用常亮、条件白心、点击不触发选中/拖拽）+ 琥珀色 paused 描边与「已暂停」标；PropertyPanel「调试」区块（Switch+条件 Input，validateExpression 实时中文校验与 fail-safe 提示）；apiClient streamRun debug 入参与 paused/stopped 帧分流（result 判 `id&&outputs`，paused 无 id 不误判；stopped 抛 DebugRunStoppedError 区别于请求失败）+resumeDebug；Editor 悬浮调试工具条（暂停节点+原因 Tag、三按钮逐按钮 loading、变量过滤、globals/outputs 只读 JSON），调试中审批 Modal 照常。测试：后端 329 passed/8 skipped（+test_debugger.py U29 11 例、Demo API I17 7 例），前端 vitest 56（8 文件，+4）；浏览器对真实 :8000+:5174 六场景实测：sql-query-notify 逐步序列与快照、条件断点真假两路与无条件 reason、approval 节点暂停先于 Modal（超时拒绝路径）、停止无 result 无错误、断点标记/表达式校验/刷新清空、reset 释放暂停会话，控制台零错误。6 提交 2d7fa28..ecb9605（含 5e13cdd 允许空 breakpoints 纯单步、ecb9605 resume 按钮下一暂停到达时复位两个修正）；PR 待用户推送 dev 后发起。

### feat / recording+api+frontend（2026-09-15，dev 未推送）

- Phase 2 能力项「操作录制 v1（运行录制 + 回放比对）」端到端落地：一次真实 Graph 运行录为回归用例，一键回放做操作序列与逐节点变量比对。契约：04 新增 §5.11 权威 blockquote（RecordingCase 形状、录制/回放/比对/边界语义），03 新增 `recording_case`（总览行+用例/列表投影/replay 响应字段概览），06 新增 §6.9 无独立运行时，09 目录树/模块映射/待定项 10 收口，10 §4 ADR T12（录制事件客户端组装而非服务端代跑、Graph 快照冻结而非活引用、归一化深等而非 LLM 评判、无新依赖），12 §3.7 内部接口+REST 五行（reset 行补注用例不清除），13 新增 U28/I16 且标注 R1（序列+变量逐项回放）由本项满足、R3 经预置审批决策满足，14 新增 D26（批量回放与报告/跨版本差异对比/影子模式/Mock 工具响应/用例编辑与入参参数化/subgraph 快照内联）并对 D22 补注浏览器操作录制仍缓做。三项用户拍板：① v1 = 运行录制+回放（非浏览器插件/Playwright codegen，非 07 行为挖掘）；② 录制时 Graph JSON 原样冻结进用例，回放恒跑冻结快照不随后续编辑漂移；③ 用例是测试资产（同 /api/feedback），`/api/demo/reset` 不清除，进程内存储重启清空（持久化随 11 S1）。新包 `src/atlas/recording/`：`cases.py` pydantic RecordStep/RecordingCase/RecordingCreateRequest（name 1-100、steps ≥1、output 任意 JSON）+ RecordingStore（rec-N 计数、add/list/get/delete、reset 不接线）；`replay.py` 纯函数 normalize/preset_approvals/dedupe_steps/compare/collect_steps——回放从 baseline human_approval 步骤抽 `{node_id: decision}` 合并进 inputs.approvals（resolvedBy="input" 秒回不挂起），parse_graph 冻结快照后走标准 run_graph 收集 node_end；比对先序列对齐（多余/缺失节点即分支漂移）再逐节点归一化深等并给顶层 diff_keys。归一化五规则：递归丢 `token`/`sent_at`；同层含 sent_at 的消息记录丢 uuid `id`；http/request result.headers 丢 `date`；human_approval 产出丢 `resolvedBy`；trigger 产出丢 `context.payload.approvals`（后两条由浏览器首轮回放实测暴露——预置决策经 trigger 载荷回显、决策来源 input 与录制时 timeout/human 不同，均属运行时来源非业务结果，补 3 个针对性单测后以独立 fix 42a82f3 收口并同步 04/06/13）。API：`POST /api/recordings`（快照按 graph_id 取已保存图原样入库，图未知 404、steps 形状 422）、`GET /api/recordings`（投影无 graph/steps）、`GET /api/recordings/{id}`、`DELETE`（未知 404）、`POST /api/recordings/{id}/replay`（回放异常如 subgraph 引用被 reset 删除折叠 replay_status=failed/matches=false，不抛 500）。前端：apiClient 录制类型与四函数；新纯逻辑 `lib/recordings.ts` `toSteps(events)`（node_end 按 node_id 保末并重排位置，parallel 双发/loop 重访安全，3 个 vitest）；Editor Header「录制与回放」720 宽 Modal——用例名 Input（默认 `录制 <时间>`）+「录制当前画布一次运行」复用唯一保存→/run/stream 路径（事件在既有 SSE 回调收集，审批 Modal/SSE 日志照常，无专用运行端点），卡片列表（名称/终态 Tag/节点·步骤数/时间）+「回放」（行内展开整体匹配 Tag、基线→回放终态、逐节点 ✓/✗ 与差异键）+ Popconfirm 危险「删除」。测试：后端 311 passed/8 skipped（基线 283：+test_recording.py U28 二十三例 + Demo API I16 五例，全部进程内/静态数据零真实外呼），前端 vitest 52（7 文件，+recordings 三例）、oxlint/tsc/build 绿；浏览器对真实 :8000+:5174 实测 approval-timeout-reject 录制（4 节点·3 步骤·completed）→回放秒回三节点全匹配（跨运行 token/消息 uuid/sent_at/resolvedBy/预置 approvals 归一化生效）→reset 清 GraphStore 后用例保留且回放仍匹配→删除后列表空，硬重载后控制台零错误。5 提交 4f7af04..42a82f3；PR 待用户推送 dev 后发起。

### feat / template+api+frontend+llm（2026-09-15，dev 未推送）

- Phase 2 能力项「流程模板库 v1」端到端落地：5 个内置只读模板随代码发布。契约：04 §5.10 权威 blockquote（TemplateMeta `{id,name,description,tags,graph}`、只读语义、整画布替换语义），03 新增 `template_catalog`（总览行+字段概览），06 §6.8 无独立运行时，09 目录树/模块映射/待定项 9 收口，10 §4 ADR T11（代码版本化 Python 常量 vs DB 表/远程市场），12 §3.6 内部接口+REST 两行，13 U27/I15，14 新增 D25（用户模板 CRUD/市场/远程导入/版本钉版/参数化向导）并对 D13/D21 加触发注记。新包 `src/atlas/template/`：`graphs.py` 5 个模板图（`refund-auto` 退款 golden 三节点——由 `llm/nl_generate.py` 原样迁入、NL 兜底改 import，U27 固化单一事实源；`http-orders-branch` http/request GET mock/orders 按状态 200 分流双消息；`sql-query-notify` database/query `:min` 绑定参数按 row_count 分流（运行输入 min_amount）；`sql-approval-write` human_approval 3600s 审批通过后 UPDATE、拒绝走消息（运行输入 order_id）；`approval-timeout-reject` 10s 超时自动拒绝双消息），`catalog.py` pydantic TemplateMeta + TEMPLATES 五元组 + list_templates/get_template。三项用户拍板：① 5 个模板卡区间下限且全部只用已注册 demo 适配器零配置可跑；② 只读 Python 常量、无 DB/CRUD、reset 不影响，REST 仅 `GET /api/templates`（列表投影 `{items:[{id,name,description,tags,node_count}]}` 不含 graph）与 `GET /api/templates/{id}`（含 graph，未知 404）；③ 「从模板新建」整画布替换（复用 loadGraph，节点 id/config/边/`{{node-id.path}}` 引用不重映射），前端 Modal 顶部警示未保存修改会丢失，保存后成为普通 graph-N 与模板脱钩。前端 apiClient 增 listTemplates/getTemplate 与 TemplateSummary/TemplateDetail 类型，Editor Header 增「从模板新建」Modal（名称/Tags/节点数/描述卡片列表）；规避 antd v6.6.3 两项弃用（List 组件将移除、Space direction→orientation）。测试：后端 283 passed/8 skipped（+模板目录 U27 六例、API I15 四例），前端 vitest 49 不变；浏览器对真实 :8000+:5174 实测五模板列表/加载/属性面板 config 完整、五模板保存+编译 200、sql-query-notify（min_amount=1000）走有单分支、approval-timeout-reject 无人操作 10.0s 超时拒绝、http-orders-branch 200 成功分支、三条 message/send 记录可见、reset 后模板仍在消息清空、控制台零错误零告警。6 提交 8f3238b..dffd7e3；PR 待用户推送 dev 后发起。

### feat / database+message+graph+api+frontend+llm（2026-09-15，经 PR #11 合并 main）

- Phase 2 适配器方向第二、三项「DB 适配器（通用 SQL）」「消息适配器（进程内消息服务）」端到端落地，适配器清单（API/DB/消息/Web）完成三类。契约：04 §4.7/§4.8 两个权威 blockquote，03 新增 `db_sql_params`/`message_send_params`，06 §6.7 运行时，09 目录树/模块映射/待定项 8 收口，10 §4 ADR T10（自建 engine 工厂、env 隔离、无新依赖），12 §3.5 接口与 REST，13 U26/I12-I14，14 新增 D23（DB：JDBC/ODBC/数仓驱动、只读账号、SQL 审查、schema 表单、审计分页）与 D24（消息：SMTP/IM/短信/webhook、OAuth、模板、reply、入站、限流重试）。新包 `src/atlas/database/`（adapter_id/type=`database`，`database/query` read·幂等返 `{columns,rows,row_count,truncated}`，limit 1-1000 缺省 500；`database/execute` write 返 `{rowcount}`，DDL 允许）与 `src/atlas/message/`（adapter_id/type=`message`，`message/send` write 非幂等，`{channel,to,subject,body}` 全必填、to ≤20、email 须含 `@`，零真实投递）。四项用户拍板：① DB 双能力映射 READ/WRITE 权限门；② 适配器专用 `ATLAS_DATABASE_URL`（scheme 白名单 postgresql+psycopg/sqlite，独立 engine 工厂与平台 DATABASE_URL 物理隔离），未配置且未注入时 fail-closed DB_NOT_CONFIGURED，api/main 与 build_demo_registry 显式注入 StaticPool SQLite 内存 demo（seed 两笔 orders，reset 重建；显式配置的外部 PG 不被 reset 触碰），DB_SQL_ERROR URL 经 hide_password 脱敏，PG 查询连接 readonly+rollback；③ 消息 v1 仅进程内 sink，`GET /api/demo/messages` 查看，重启/reset 清空；④ 模板复用 `{{路径}}` 插值。loader 通用 JSON 通道扩为 `GENERIC_JSON_ADAPTERS={http,database,message}`；前端零功能改动（选择器由 /api/adapters 驱动），ToolCallConfig 占位符扩四类 JSON 示例；NL prompt 广告三工具并强调绑定参数。`.env.example` 补 ATLAS_DATABASE_URL 段。测试：后端 273 passed/8 skipped（+database 35/message 21/loader 4/Demo API 4，全部 SQLite 内存/进程内零真实外呼），前端 vitest 49（6 文件）不变；浏览器对真实 :8000+:5174 实测选择器分组与权限标记、query 两笔订单、execute INSERT 后可见且 reset 复原、send 消息 /api/demo/messages 可见且 reset 清空、坏 JSON FAILED，控制台零应用错误。8 提交 b24c9c5..；经 PR #11 于 2026-09-15 合并 main。

### docs / 发布同步（2026-09-15）

- Phase 2 全部在途工作经 PR #4-#11 合并 main：#4 condition、#5 loop、#6 parallel、#7 wait、#8 human_approval、#9 subgraph（2026-09-14）、#10 通用 HTTP API 适配器（2026-09-15，merge `4385a00`，9 提交 3c60f3d..8a9ddc3）、#11 DB+消息适配器（2026-09-15 合并）；每个 PR 与合并后 main 的 GitHub Actions CI（gitleaks/后端 pytest/前端 oxlint+vitest+build）均实跑全绿。handoff 仓库状态与质量门、docs/08 落码 blockquote 同步；本地 main 快进至 `4385a00`。

### feat / httpapi+graph+api+frontend+llm（2026-09-15）

- Phase 2 适配器方向首项「API 适配器（通用 HTTP 请求）」端到端落地。契约（04 §4.6 权威 blockquote；09 待定项 7 收口——FastAPI 服务端已占 `api/` 包名，新包定名 `src/atlas/httpapi/`，adapter_id=`http`、adapter_type=`api`、单工具 `http/request`）：tool_call 节点 config.params 经 `{{路径}}` 插值后须为 JSON 对象 `{method,url,headers,body,timeout}`——method 限 GET/POST/PUT/PATCH/DELETE 默认 GET，url 必填（相对路径拼默认 baseUrl、绝对 URL 直连），dict/list body 走 application/json、string 原样发送，timeout 默认 30s。四项用户拍板决策：① v1 仅单一通用能力，OpenAPI/Swagger 导入与工具自动生成、Schema 驱动参数表单、OAuth2、操作录制/NL 生成适配器缓做 14 D22；② 混合连接配置——env/注册注入 `ATLAS_HTTPAPI_BASE_URL`/`ATLAS_HTTPAPI_HEADERS`（JSON 对象）/`ATLAS_HTTPAPI_TOKEN`（Bearer，显式 header 优先），节点 headers 与默认 headers 键级合并、节点优先；v1 无加密存储，凭证加密与秘密变量缓做 D22；③ 结果语义——任何 HTTP 响应（含 4xx/5xx）均为 SUCCESS，输出 `{status,headers,body}`（body 优先 JSON 解析、失败给 text），由图中 condition 判 `result.status` 分支；仅传输层失败 FAILED（HTTP_TIMEOUT/HTTP_CONNECT_ERROR），坏参数 MISSING_PARAMETER/INVALID_PARAMETER；request 能力挂 WRITE/非幂等；v1 无重试/熔断、无 SSRF egress 防护（04 §4.6 与 D22 明确风险标注）；④ 内置 FastAPI mock 端点 `GET /api/demo/mock/orders`（X-Demo-Token 鉴权，401/200 两笔订单）与 `POST /api/demo/mock/orders/{id}/receipt`（body 回显 + received:true）支撑离线浏览器实测，reset 无涉；单测全部 httpx MockTransport，Demo API 端到端用例经临时端口真实 uvicorn daemon 线程打 loopback（同步 Client 不支持 ASGITransport），零外部外呼。`graph/loader.py` `_execute_tool` 在 shop 硬编码分派前新增 http 通用 JSON 通道（坏 JSON/非对象 → FAILED INVALID_PARAMETER），`build_demo_registry()` 与 `api/main.py` 两处注册全权限单例。前端工具选择器首次接 `GET /api/adapters`：ToolCallConfig 抽为独立组件，Select 按适配器分组、复合值 `适配器/工具`、权限/幂等标记、过滤 unhealthy、发现失败回退手填、已保存图未注册工具补「当前值」组；新纯逻辑 `lib/adapters.ts`；Editor SSE 日志补 tool_call 分支（SUCCESS（HTTP N）/FAILED（code message））；serializer/DSL 零改动。NL 生成 prompt 广告已注册工具（禁捏造，http/request params 为 JSON 字符串支持 `{{路径}}`、4xx/5xx 仍成功，枚举五个 shop/* 工具）。ADR：httpx 同步 Client（执行于 FastAPI 线程池）记 10 文档 §4。测试：后端 209 passed/8 skipped（+httpapi 24/loader 5/Demo API 5/NL 1），前端 vitest 49（6 文件，+adapters 3）；浏览器对真实 :8000+:5174 实测 GET 200/POST 插值回显/401 仍 SUCCESS/坏 params FAILED，控制台零应用错误。

### fix / harness（2026-09-15）

- `AdapterRegistry.list_adapters` 列举前为进程内适配器统一续心跳：常驻单例注册超过 30s TTL 后被误判 unhealthy，导致前端工具选择器拉到的 `/api/adapters` 全部 healthy:false、只剩手填；进程内适配器被发现即可达，TTL 置灰保留给远程注册（D6）。新增心跳续期回归用例（后端 208 passed）。

### feat / graph+api+frontend+llm（2026-09-14）

- Phase 2 第六项「子图节点（subgraph，引用已保存图、进程内嵌套执行）」端到端落地，Phase 2 六类节点（condition/loop/parallel/wait/subgraph/human_approval）全部完成。契约（04 §5.7 权威 blockquote，原状态机/异常处理顺延 §5.8/§5.9；Graph JSON 仍为 version 1、EdgeDSL 不变）：单节点 config 挂 `{graphId, inputs}`——graphId 必填指向 GraphStore 已保存图，inputs 可选为「子图入参键 → 父图 `{{路径}}`/字面量」映射；恰好 1 条普通出边（同 wait，无成功/失败双出口），可位于 condition 分支/loop 体内/parallel 区域。两项用户拍板决策：① v1 仅按 graphId 引用进程内已保存图，版本钉版/手动升级回归、子图市场/组织共享、跨运营体远程引用、嵌套可视化编辑器缓做 14 D21（触发：模板库/发布流或多租户）；② 单出边 **fail-safe**——子图运行期异常折叠为节点产出 `{mode:"subgraph",status:"failed",error,outputs:{},trace:[]}`，父图 run 仍 completed，下游可用 condition 判 `{{subgraph-x.status}}`。运行时执行器内进程内重入 `run_graph`（复用同一 registry/decision_client/approval_broker，emit=None 压制子图事件，父图 SSE 只见 subgraph 节点自身一次 node_end），嵌套深度上限 3（父=0）；编译期经注入的 `graph_resolver` 递归校验引用不存在/直接自引用/跨图环（A→B→A）/超深/子图内部非法（中文加前缀「子图 {graphId}：」，422）。新增 `GET /api/graphs`（已保存图 id/节点数/更新时间列表），compile/run/stream 三处注入 resolver。产出 `{mode:"subgraph",graphId,status,outputs:<子图 outputs>,trace:[...]}`、trace 行 `subgraph-x: graph-7 success (N nodes)`，下游引用 `{{subgraph-x.outputs.<子图节点id>.<键>}}`；子图内 human_approval 因 node_start 不外泄，v1 仅能 run inputs 预置/超时（D21 注明）。前端新增 SubgraphConfig（挂载拉 `GET /api/graphs` 的 graphId 选择器、空列表引导先保存子图、inputs 键值映射增删行+变量插入；实测 antd6 `Space.Compact` 内 Select 选中后 onChange 不触发，插入 Select 移出 Compact 为独立全宽行解决）、默认单 Handle（画布/Store 零改动）、SSE 日志「子图完成：graphId（成功/失败：error）」，节点配色 indigo6（第九色）；NL prompt 枚举八类→九类并补 graphId 必填/inputs/单出边/不得捏造 id 契约。测试：后端 173 passed/8 skipped（+DSL subgraph 5/loader 9/Demo API 3/NL 1），前端 vitest 46（5 文件）；浏览器对真实后端实测父图 trigger→subgraph→tool_call：入参映射 X-9 到达子图 trigger payload、父图工具渲染子图产出路径、trace 形状正确、子图事件零外泄、缺失引用 graph-ghost 编译 422、控制台零错误。

### feat / graph+collaboration+api+frontend+llm（2026-09-14）

- Phase 2 第五项「人机协作节点（human_approval，进程内审批挂起）」端到端落地。契约（04 §5.6 权威 blockquote，原状态机/异常处理顺延 §5.7/§5.8；Graph JSON 仍为 version 1、EdgeDSL 不变）：单节点 config 挂 `{summary, approver, timeoutSeconds, onTimeout, approvedTarget, rejectedTarget}`——summary 必填支持 `{{路径}}`、approver 可选仅展示不鉴权、timeoutSeconds 整数 10-3600、onTimeout approve/reject 默认 reject（超时沿拒绝分支继续、run 仍 completed）、双目标须存在/互异/非自身，恰好 2 条出边对配通过/拒绝且不直连 END；可位于 condition 分支/loop 体内/parallel 区域，阻塞不产生超步故 recursion_limit 不变。新增 `atlas.collaboration.approvals.ApprovalBroker`（uuid4 token + `threading.Event`，模块单例可注入；首决生效、list_pending/reset）；决策三来源——REST `POST /api/approvals/{token}/decision`（resolvedBy="human"，404 未知/409 重复/422 坏值）、run inputs 预置 `{"approvals":{"<node-id>":"approved|rejected"}}`（resolvedBy="input"）、超时（resolvedBy="timeout"，与并发人工竞速人工胜出）；新增 `GET /api/approvals` 调试列表，`/api/demo/reset` 按超时拒绝放行并清空。node_start 在登记后阻塞前发出且携带 `approval:{token,summary,approver,timeoutSeconds}`；产出 `{mode:"human_approval",decision,target,token,summary,approver,resolvedBy}`、trace `human-x: approved (human) → tool-y`、下游可引 `{{human-x.decision}}`。**连带项：`/run/stream` 由「先跑完再回放」改真流式**——run_graph 在 daemon 线程执行、`queue.Queue` 实时下发（旧实现下节点阻塞等审批、浏览器收不到 node_start 会死锁），终帧 `event: result`、运行期异常新增 `event: error` 哨兵帧；同步 /run 不变。前端新增 HumanApprovalConfig（说明 TextArea+变量插入/审批人/超时秒/策略 Radio/双目标 Select）、双 Handle（approved/rejected，通过/拒绝边标签）、删除清理、运行中 AntD 审批 Modal（node_end 自动关窗，409/404 窗内提示，mask 不可关闭）、SSE 日志「人工审批：通过/拒绝（人工/超时/预置）」，节点配色 slate900（避开既有七色）；NL prompt 枚举七类→八类。持久化中断-恢复（LangGraph interrupt/checkpointer）、多实例/外部信号、IM/邮件/钉钉通知、动态审批人与角色权限、审批评论流/队列页面缓做 14 D20（与 D19 事件等待同批，触发：11 S1 就绪或真实审批业务）。测试：后端 155 passed/8 skipped（+collaboration broker 7/DSL 13/loader 5/Demo API 3），前端 vitest 43；浏览器实测人工同意（仅通过侧执行）、人工拒绝（仅拒绝侧执行）、10s 超时自动拒绝且弹窗自动关闭、空说明即时报错、pending 列表可见。

### feat / graph+frontend+llm（2026-09-14）

- Phase 2 第四项「等待节点（wait，定时挂起）」端到端落地。契约（04 §5.5 权威 blockquote，Graph JSON 仍为 version 1、EdgeDSL 不变）：单节点 config 挂 `{waitType:"duration", durationSeconds}`，整数秒常量 1-600；恰好 1 条出边指向已存在后继且不直连 END，无新增拓扑规则（可位于 condition 分支/loop 体内/parallel 区域，recursion_limit 公式不变）。运行时执行器内同步 `time.sleep`（SSE 同步生成器跑在 Starlette 线程池，不卡事件循环；600s 上限即 worker 占用上限），等待不失败不重试；产出 `{mode:"wait",waitType:"duration",durationSeconds}`、trace `wait-x: waited Ns`、下游可引用 `{{wait-x.durationSeconds}}`；普通边装配零改动。事件等待（interrupt/checkpointer+外部信号恢复）与动态时长缓做 14 D19（触发：事件驱动/分钟级长等待业务 + 11 S1 持久化就绪）。DSL 中文聚合校验覆盖坏 waitType（event 明确「暂不支持」）/非整/bool/越界/出边数与目标。前端新增 WaitConfig（事件等待 Radio 禁用带 tooltip、InputNumber 1-600 越界钳制+空值即时报错，antd6 Space.Compact 替代弃用 addonAfter）、默认单 Handle（画布/Store 零改动）、SSE 日志「等待完成：N 秒」、节点配色 orange6（purple6 已属 AI 节点）；NL prompt 枚举六类→七类。测试：后端 127 passed/8 skipped（+DSL 12/loader 2/NL 1），前端 vitest 39；浏览器实测 trigger→wait(1s)→tool_call 真实 1s 墙钟、后继执行、output/trace 形状正确，面板校验零新增控制台错误。

### feat / graph+frontend+llm（2026-09-14）

- Phase 2 第三项「并行节点（parallel，扇出/汇聚网关）」端到端落地。契约（04 §5.4 权威 blockquote，Graph JSON 仍为 version 1、EdgeDSL 不变）：单节点 config 挂 `{joinStrategy, branches:[{label,target}], joinTarget}`，分支 2-10 个、label 非空唯一、target 互异、出边恰好 N 条且不直连 END；分支区域由 BFS 界定（遇 joinTarget 与 parallel 节点停止），区内允许 condition/自包含 loop，禁嵌套 parallel/trigger/分支交叉与外泄/分支不可达 join，中文聚合报错。两策略：`all_success`（任一分支 FAILED 则汇聚 status=failed，但走 **fail-safe 汇聚**——joinTarget 照常执行、run 仍 completed，下游可判 `{{parallel-x.status}}`）与 `all_completed`（各分支走到汇聚即成功）；any_success（OR-join+取消分支）缓做 14 D18。运行时关键事实：spike 实测 LangGraph 普通多入边不构成 barrier，故编译期注入合成网关 `__join__<id>`（分支末端 retarget 网关，网关就绪计数 self-loop wait 超步等待不等长分支，齐后聚合再路由 joinTarget），`GraphState.outputs` 改 `Annotated[dict, _merge_outputs]` key 级合并、`status` 用 last-write、执行器只回自身分片；网关事件不外泄，汇聚完成以 parallel 节点自身补发第二次 node_end（fork 时 running）。聚合产出 `{mode,joinStrategy,status,branches:[{label,target,status,error}],result:{<入口节点id>:末端产出},joinTarget}` 与 fork/joined trace 行。前端新增 ParallelConfig（策略 Radio/分支增删/汇聚目标）、N 出口 Handle 与分支边标签、删除清理、SSE 日志（并行启动/汇聚成功/分支失败 fail-safe），节点配色 magenta6；NL 生成 prompt 枚举补 parallel。测试：后端 112 passed/8 skipped（+DSL 10/loader 3/退款 e2e 1/NL 1），前端 vitest 37；浏览器对真实后端实测三场景——不等长双分支汇聚一次（汇聚节点渲染 `状态=success`）、一支未注册适配器 fail-safe 文案、坏配置三条即时报错。

### feat / graph+frontend+llm（2026-09-14）

- Phase 2 第二项「循环节点（loop）」端到端落地，v1 仅条件循环 while。契约（04 §5.3 权威 blockquote，Graph JSON 仍为 version 1、EdgeDSL 不变）：节点 config 挂 `{mode:"while", continueExpression, maxIterations(1-100, 默认10), bodyTarget, exitTarget}`，恰好两条出边对配循环体/退出目标；循环体由 BFS 界定，体→同 loop 的回边是**唯一合法环**（DSL 摘除白名单回边后对余图 DFS 环检测，调和 U7），体内禁嵌套 loop/trigger、禁泄漏 exitTarget、无回边/游离体分支均中文聚合报错。运行时：可重入执行器每轮重求 continueExpression（首轮播种 `{{loop-x.index}}`，从 1 起），为真进体、为假退 `condition_false`；达最大次数与表达式异常均 **fail-safe 退出**（路由 exitTarget、`exitReason=max_iterations/expression_error`、运行仍 completed）；出边全 conditional，`recursion_limit` 按循环体规模派生。产出 `{mode,iterations,index,target,exitReason,expression_errors}` 与 continue/exit trace 行。前端新增 LoopConfig（表达式实时校验/变量插入/最大次数/体·退出目标）、双出口 Handle 与「循环体」「退出」边标签、删除目标清理、SSE 日志中文化（条件不满足/达到最大次数/表达式异常），节点配色 cyan6；NL 生成 prompt 枚举补 loop。测试：后端 97 passed/8 skipped（+DSL 8/loader 3/退款 e2e 1/NL 1），前端 vitest 34；浏览器对真实后端实测三场景——`{{loop-1.index}} < 2` 体执行 2 轮后条件不满足退出、恒真 3 轮撞上限 fail-safe、坏变量 0 轮表达式异常退出。遍历循环（foreach/item/聚合）与 break/continue 缓做（14 D16/D17），嵌套循环 v1 拒绝。

### feat / graph+frontend+llm（2026-09-14）

- Phase 2 首项「条件分支节点（condition）」端到端落地。契约（04 §5.1/§5.2，Graph JSON 仍为 version 1、EdgeDSL 不变）：节点 config 挂 `branches:[{label,expression,target}]` + 必填 `defaultTarget`，按序短路；label/target 节点内唯一、target≠default、每个目标须有出边且每条出边须被分支覆盖、不允许直连 END。后端新增 `atlas.graph.conditions`（纯 stdlib 手写递归下降表达式：`{{路径}}`、比较 `> >= < <= == !=`、逻辑 `&& || !`、括号、数字/字符串/true/false/null；禁 eval/算术/函数；校验期报语法与纯字面量类型错误，运行时错误 fail-safe 走 defaultTarget），`dsl.py` 增 condition 图级校验与 trigger 根可达 BFS，`loader.py` condition 出边全部 `add_conditional_edges`（执行一次写 outputs，router 只读 target），产出 `{branch,target,evaluation,expression_errors}` 与 trace 行。前端新增 ConditionConfig 属性面板（实时中文校验/变量插入/目标 Select）、节点多出口 Handle 与出边分支标签、删除目标自动清引用、运行 trace 分支行；`lib/conditions.ts` 为后端同构的 TS 校验。NL 生成 prompt 枚举补 condition。测试：后端 84 passed/8 skipped，前端 vitest 32；浏览器实测 12346（¥5000）单侧转人工 human_review、12347（¥128）默认分支 refunded。LLM 判断分支、函数库/算术缓做（14 D14/D15）。

### docs（2026-09-14）

- 沙盘假定 Phase 1 种子验证 Go、进入 Phase 2（用户指示，非真实试用结论）：docs/18 文首加假定声明、新增 §8 验证结论表（注明无 §5 实测值、无 §6.1 C1-C5 记录，真实试用 No-Go 时回退）；08 Phase 1 补假定结论并记 PR #3（dev→main，19 提交，merge f069b53，main CI 全绿）；handoff 阶段口径、仓库状态（PR 已合并）、Active work（Phase 2 范围与 D6/D7 触发提示）、Quality gate（gitleaks 403 修复、PR/main 实跑全绿）同步。

### chore / bench（2026-09-14）

- D9 BENCHMARK 首版落地：新增 `scripts/benchmark.py`（纯 stdlib，零新依赖；10% warmup + 300 次/场景，p50/p99，输出 BENCHMARK Results 格式 Markdown，`DATABASE_URL` 时追加 DB ping）。场景：OODA 循环吞吐、退款 3 节点图编译时延、12345 自动退款端到端时延（规则决策路径）、Harness 进程内调用开销。首份基线（commit 407812e，CPython 3.11.15 / macOS arm64）：OODA 2.05ms p50（≈488 loops/s）/ 编译 1.81ms / 退款端到端 2.28ms / Harness 0.002ms；LLM 决策、记忆层读写、并行扇出、长流程内存增长在 BENCHMARK Scope 标注待接入补测。09 目录树补 `scripts/`，08/14/handoff 同步。

### chore / ci（2026-09-14）

- D10a CI 质量门落地：`.github/workflows/ci.yml`（push main/dev 与 PR 触发，同 ref 并发取消）三道门——gitleaks 全历史密钥扫描（沿用根 `.gitleaks.toml`）、后端 Python 3.11 `pytest`（integration 默认跳过）、前端 Node 22 / pnpm 10 `pnpm lint`（oxlint）+ `pnpm test`（vitest 23）+ `pnpm build`。本地等价命令全绿（后端 62 passed/8 skipped、gitleaks 55 commits 无泄漏）；Actions 实跑待 dev 推送后验证。14 D10 拆为 D10a（完成）/D10b（CD，缓做至远程部署目标确定），ADR T8 落 10 文档 §4，08/09/14/handoff 同步。

### docs（2026-09-14）

- docs/18 新增 §6.1 试用进度跟踪表（C1-C5 一行一客户：决策路径、三场景结果与耗时、黄金用例认同数、意向、反馈分类、跟进项）与每家详细记录模板；handoff 新增「Active feedback」入口区并指向该表，试用反馈按 16 文档流程闭环。

### feat / api+web（2026-09-13）

- Phase 1 应用内反馈入口落地（18 文档 §6）：后端新增 `POST /api/feedback`（type=bug/suggestion、content 1-2000 字、contact 选填，201 + id/created_at）与 `GET /api/feedback`（陪同试用导出），进程内 `FeedbackStore`（重启清空，与 Demo 存储同假设；`/api/demo/reset` 不清除反馈）；非法 type/空 content 经 pydantic 校验 422。前端编辑器头部新增"反馈"按钮与弹窗（类型切换、字数计数、联系方式选填、提交成功态、错误回显），`apiClient.submitFeedback`。测试：后端新增 2 用例（62 passed/8 skipped），前端 vitest 23、`pnpm build` 通过；浏览器同源（:8000）实测两类反馈提交与 GET 导出落库，控制台零错误。03 新增 feedback_item 契约、12 REST 表补两端点、13 进度补行。

### docs（2026-09-13）

- Phase 1 种子验证准备：新增 `docs/18-种子客户验证计划.md`（客户画像与 3-5 家招募标准、对齐 08 §7.3 的三场景试用脚本、Go/No-Go 量化指标、应用内+试用表双反馈机制与边界）与根目录客户向一页纸 `TRIAL.md`（docker compose 启动、12345 自动退款 / 12346 转人工 / NL 生成草稿三场景、reset 用法、5 题反馈表）。08 Phase 1 补准备记录，00 文档地图加 18，AGENTS 文档范围改 00-18，README/handoff 索引同步。

### feat / api+infra（2026-09-13）

- Phase 1 种子客户一键交付：根目录新增 `Dockerfile`（node:22-slim 多阶段构建前端 → python:3.11-slim 安装后端，dist 拷至 `/app/frontend/dist`，不装 Playwright 浏览器）、`docker-compose.yml`（单服务 8000 端口，`LITELLM_MODEL` 透传）、`.dockerignore`。`api/main.py` 在 `ATLAS_FRONTEND_DIST`（默认 `frontend/dist` 相对于 cwd）存在时用 StaticFiles 同源挂载到 `/`（显式路由优先，dev 仍走 Vite 5174 代理）；新增 `POST /api/demo/reset`（店铺恢复 5 笔种子退款单、清空已保存图与登录态），`DemoShopService.reset()`/`GraphStore.clear()`。测试新增 reset 全链路与静态托管条件用例，后端 60 passed/8 skipped；镜像 `docker compose up --build` 实测编辑器、/api/health、/demo/shop 同源可访问。

### feat / web（2026-09-13）

- 设计 Token 等价替换落地（17 文档 §3）：新增 `frontend/src/theme/tokens.ts`（primitive/semantic 两层，唯一事实源，导出 `antdTheme` 与 `token()`）与 `setup.ts`（main.tsx 引入一次，注入 `--atlas-*` CSS 变量）；`App.tsx` ConfigProvider 改消费 `antdTheme`，`index.css` 全部硬编码色值/rgba 光晕换为语义变量（含 keyframes pulse），`nodeCatalog.ts` 节点三色与 `FlowCanvas.tsx` 连线色改引 `token()`。现值 1:1 搬迁、零新依赖；`pnpm test` 23/23、`pnpm build` 通过，浏览器 computed-style 逐页核对零视觉差异、控制台零错误，src 下硬编码色值仅剩 tokens.ts。

### docs（2026-09-13）

- 新增 `docs/17-前端国际化与设计Token方案.md`（工程推导，需求依据 04 §补充项 28）：i18n 选型 i18next + react-i18next + AntD locale（T7，触发式引入，当前不落码），冻结 locales 目录/key 命名/后端错误码本地化/金额日期 Intl 格式化/NL 多语言边界等契约；设计 Token 三层模型（primitive/semantic/component），`frontend/src/theme/tokens.ts` 单一事实源派生 AntD theme 与 `--atlas-*` CSS 变量（零新依赖），含硬编码色值迁移清单与零视觉变化验收。10 文档 §1/§4 记 T7 与 token ADR，09 前端树补 `theme/`、`locales/`（待落码），14 文档新增 D12/D13，handoff/00 索引同步。
- 同步 README/CONTRIBUTING 阶段口径至 W1-W10 Demo 完成，03 契约索引补 W9-W10 运行时 Schema（refund_decision/refund_order/run_event/nl_generate_request）。

### feat / llm+shop+graph+api+web（2026-09-13）

- W9-W10 电商退款端到端 Demo，08 §7.3 七条验收全部达成。新增 `src/atlas/llm/`：`decision.py` 定义 DecisionClient 协议，配置 `LITELLM_MODEL` 时经 LiteLLM 决策（只取 JSON，解析失败 fail-safe 转人工），未配置时 RuleBasedDecisionClient 按 06 §9.2 黄金规则确定性兜底（质量原因且金额 ≤ 限额 → approve_refund，其余 → request_human_approval）；`nl_generate.py` 自然语言生成 Graph 草稿（LLM 优先、退款模板兜底、无法识别 422）。新增渠道包 `src/atlas/shop/`：`service.py` DemoShopService（五笔种子退款单 12345-12349、登录态、退款/转人工状态流转），`adapter.py` ShopHarnessAdapter（login/list_pending_refunds/execute_refund[financial]/request_human_approval/process_refund 五能力，按上游决策路由，权限门与 StructuredError）。`graph/loader.py` 改为依赖注入（decision_client/registry/emit/trigger_payload），webhook 载荷经 run inputs 进入 trigger `context.payload`（同名键覆盖全局变量），执行事件 node_start/node_end/run_end；dsl 修复 triggerType `schedule` 与 `cron` 均触发 cron 校验（对齐前端取值）。`api/main.py` 新增 `POST /api/graphs/{id}/run/stream`（SSE 实时进度）、`POST /api/nl/generate`、`GET /api/adapters`、Demo 店铺登录/订单接口与 `GET /demo/shop` 模拟商家控制台页面，共享 Demo 服务单例。前端：退款单选择器、SSE 流式运行（节点 running 脉冲/completed 描边 + 调试台事件）、NL 生成弹窗载草稿（deserializeGraph）、种子图改为退款三节点、Dashboard 文案 W9-W10。测试：后端新增 26 用例（决策 6/店铺 8/NL 2/退款端到端 3/Demo API 6/店铺平台集成 1/schedule 1），默认 58 passed/8 skipped，opt-in 集成全绿；前端 vitest 23 全绿、`pnpm build` 通过；浏览器实测 12345 破损→approve_refund→refunded、12346 主观→request_human_approval→human_review、NL 草稿载入、控制台 demo/demo 登录拉单。文档 08（落码记录+验收映射）/09（llm、shop 包树+清单）/12（新端点）/13（W9-W10 测试行）同步；`.env.example` 补 `LITELLM_MODEL`。

### feat / graph+api（2026-09-13）

- W7-W8 编辑器→DSL→LangGraph→运行链路打通：`src/atlas/graph/dsl.py` 承接前端 Graph JSON（version 1，pydantic GraphDSL/NodeDSL/EdgeDSL）并做静态校验（版本、节点/连线 id 唯一、边端点存在且禁止自环、trigger/ai_decision/tool_call 分类型必填配置、未支持节点类型拒绝），错误聚合为中文列表；`graph/loader.py` 将 DSL 编译为 LangGraph StateGraph（节点 1:1、边按 DSL、START/END 自动接线），`run_graph` 播种全局变量并在运行时执行 04 §6.3 `{{路径}}` 插值（可读上游节点产出，缺失引用原样保留），三类节点为确定性占位执行器（LLM/真实工具待后续接入）。`api/main.py` FastAPI 提供 `GET /api/health`、`POST /api/graphs`、`GET /api/graphs/{id}`、`POST /api/graphs/{id}/compile`、`POST /api/graphs/{id}/run`（进程内图存储，校验失败统一 422）。前端新增 `lib/apiClient.ts`，编辑器新增"编译并运行"按钮（拓扑+产出结果弹窗、422 中文错误 Alert），Dashboard 文案更新至 W7-W8。测试：新增 `test_graph_dsl.py`（7）、`test_graph_loader.py`（5）、`test_api_graphs.py`（TestClient 4）共 16 用例，默认 pytest 32 passed/7 skipped；浏览器对真实 uvicorn 验证三请求链路（`{{global.approval_limit}}` 插值为 500）与 422 错误路径。决策记录：graph_loader 落 `graph/` 包（不进 engine）、进程内图存储、新增 /run 端点（09 待定项 6）；03 新增 graph_definition 契约索引，12 REST 表补 /run。

### feat / web（2026-09-13）

- W5-W6 编辑器核心功能：`frontend/src/lib/` 新增纯逻辑模块——`variables.ts`（04 §6.3 `{{路径}}` 模板语法：引用提取、插值（缺失引用原样保留）、点号/下标路径解析、变量名校验、全局变量与节点输出路径清单）、`nodeCatalog.ts`（trigger/ai_decision/tool_call 三类节点目录、默认 config/retry、中文实时校验规则）、`graphSerializer.ts`（按 03/04 §3.2 node_schema 契约序列化 Graph JSON，version 1，坐标取整）；`store/editorStore.ts` 重写为类型化 store（`nextId` 扫描已有 id 消除 React Flow 节点 id 碰撞、选中/配置更新/删除级联连线/变量增删/连线日志，种子改为 OA 审批示例）；UI 新增自定义 `AtlasNode`（类型配色 + 校验错误角标）、`VariablesPanel`（变量 CRUD + 标识符校验），重写 `FlowCanvas`（HTML5 拖拽 + screenToFlowPosition 落点）、`NodePanel`（拖拽面板）、`PropertyPanel`（三类节点分类型配置表单、插入 `{{变量}}` 引用、retry 策略、实时校验清单）、`Editor`（节点/变量双 Tab + Graph JSON 导出预览弹窗）；Dashboard 文案同步到 W5-W6 状态。测试：新增 vitest 5（选型 ADR 见 10 文档 §4），4 个测试文件 20 个用例全绿；`pnpm build` 通过；浏览器人工验证拖拽新增、错误角标与属性面板联动、变量插入恢复校验、导出 JSON 结构正确，应用零控制台错误。
- chore：修复根 `.gitignore` 的 Python 规则 `lib/` 误伤 `frontend/src/lib/`（导致前端逻辑目录被静默忽略），改为 `/lib/`、`/lib64/` 锚定仓库根目录。

### feat / web（2026-09-13）

- W3-W4 Harness Web 适配器：`harness/base.py` 落地 06 §6.4 同构契约（HarnessAdapter 抽象类、ActionRequest/ActionResult/Capability/Observation、ActionStatus 与 Permission 枚举），execute 模板方法内置权限校验与审计回调（I8）；`harness/registry.py` 进程内适配器注册/发现/心跳健康（04 §4.4，TTL 30s）。`web/location.py` 实现三层定位（层1 选择器 3000ms 超时 + 成功选择器缓存，层2 视觉语义置信度严格 >0.7 并回填缓存，层3 全图推理坐标，全失败返回"元素未找到，三层定位均失败"），层 2/3 视觉组件以 Protocol 注入；`web/adapter.py` 为 Playwright sync API 适配器，工具集 navigate/click/type/screenshot，headless 受 `PLAYWRIGHT_HEADLESS` 控制。测试：契约/定位 13 个单元用例（假页面）全绿，`test_web_browser_integration.py` 4 个真实 Chromium 用例（I1 selector、I2 语义层真实坐标、I5 缓存短路、type/observe）全绿；I3/I4 编排由单元测试覆盖，真实视觉模型链路待 LiteLLM 接入。包边界决策（harness=契约/注册、web=Playwright 实现，不合并）记入 09 待定项 2 与 08 §7.2；12 一致性清单勾选 4 项。

### feat / memory（2026-09-13）

- PostgreSQL + pgvector 连接层验证：本地 Docker 运行 `pgvector/pgvector:pg16`（容器 `atlas-pg`），迁移 `db/migrations/001_enable_pgvector.sql` 启用 vector 扩展（0.8.6）；`memory/settings.py`（`DATABASE_URL` 单一读取点，缺失 fail-closed）与 `memory/database.py`（SQLAlchemy 引擎/会话工厂，限定 `postgresql+psycopg` 即 psycopg 3，`ping`/`pgvector_version` 探针）；`tests/test_database_integration.py` 3 个 `integration` 标记用例（连通性、扩展版本、vector 写入与余弦距离排序），默认跳过，经 `ATLAS_RUN_INTEGRATION=1` + `DATABASE_URL` 开启，对容器全绿。`.env.example` 连接串改为 psycopg 3 写法；本地 `.env` 已建且被 gitignore。

### docs（2026-09-13）

- 项目文档与 W1 实现状态对齐：00 文档地图更新 09/10 描述；12 对齐检查表勾选 LoopState 一致性（实现于 `engine/state.py`）；13 新增 §9 实现进度（后端冒烟用例、前端构建与人工验证）；AGENTS 文档分层范围更正为 00-16；handoff 仓库行反映 dev 分支与未推送状态。

### feat / web（2026-09-13）

- W1 前端编辑器框架：Vite 8 + React 19 + TypeScript 6，接入 `@xyflow/react` 12（替代已弃用的 `react-flow-renderer`）、Zustand 5、Ant Design 6；实现 Dashboard/Editor 页面与 FlowCanvas/NodePanel/PropertyPanel/DebugConsole，初始 触发→AI决策→工具调用 三节点，支持添加节点、选中编辑名称、连线状态与调试日志。`pnpm build` 通过，浏览器交互验证无控制台错误；dev 端口 5174，`/api` 代理 8000。

### feat / engine（2026-09-13）

- W1 最小 OODA 循环跑通：`engine/state.py`（LoopState，契约 06 §6.1/12 §1.1）、`engine/nodes.py`（observe/orient/decide/act/reflect 确定性占位节点 + 三条 conditional 路由，签名遵循 12 §1.2）、`engine/loop.py`（StateGraph 拓扑同 12 §1.3，`build_graph/initial_state/run_loop`）；3 个 pytest 冒烟用例全绿，`run_loop` 端到端收敛 `completed`。LLM 决策与 Harness 感知为后续周次接入点。

### chore / scaffold（2026-09-13）

- W1 工程骨架：按 09 文档建 `src/atlas/{engine,harness,graph,nodes,memory,skills,collaboration,api,web}/` + `tests/`（`__init__.py` 占位）；`pyproject.toml`（Python >=3.11、src layout、pytest 配置），依赖按 10 文档 §2 锁定，版本下限取当日 PyPI 稳定版；Python 3.11 venv 可编辑安装验证通过。
- 09 文档记录包边界决策：新增 `atlas.api` 承载 FastAPI 入口与 REST 路由（待定项 5），harness 保持网关契约。

### docs（2026-09-13）

- 技术选型 T1-T5 收口为正式 ADR（10 文档 §3/§4）：Demo 模型经 LiteLLM 对接主流商业 API（`.env` 切换，不引入 vLLM）；评估与优化层仅留抽象接口；编辑后台前端进 Demo 骨架（`frontend/`，W1-W2）；进程内事件总线替代 NATS（Phase 2 重启）；Harness 网关 Demo 用 Python/FastAPI 实现同构接口（Go 化留 Phase 2）。02/08/09 同步，14 文档 D5/D6 已登记缓做触发条件。

### docs（2026-09-13）

- 移除 `LICENSE`（用户指示：现在不需要，许可证暂未定）；README / CONTRIBUTING 的 License 段落与 handoff / CHANGELOG 引用同步清理。
- 补齐 agent-world 惯例规范文档：根目录 `CONTRIBUTING.md` / `MIGRATION_CONVENTION.md` / `BENCHMARK.md` / `.gitleaks.toml` / `.env.example`；docs 新增 `14-缓做事项登记表` / `15-环境与分支策略` / `16-反馈工作流`。索引同步 00 文档地图、README、handoff。

### chore / infra（2026-09-13）

- 仓库初始化：`git init -b main`，添加 Python 项目 `.gitignore`；创建 GitHub **private** 仓库 `bayernjf/atlas` 并推送（3 个原子提交：chore / docs specs / docs meta）。

### docs（2026-09-13）

- 建立根目录元文档体系（参考 agent-world 惯例）：`handoff.md` / `README.md` / `AGENTS.md` / `CLAUDE.md` / `git-commit-message.md` / `CHANGELOG.md`。
- 源文档切分完成并核对：两份原始方案文档按主题切分为 `docs/` 00-13 共 14 份 AI 导向文档，274 章节全映射、逐段覆盖率机器核对通过，源文档已删除（详见 [00-文档索引与治理.md](docs/00-文档索引与治理.md)）。
