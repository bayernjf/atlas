# Handoff — Atlas

更新时间：2026-09-16

## 项目概况

Atlas 是 AI 运营体（Agent）编排平台：以 **Harness（能力接入）/ Graph（可执行因果图）/ Loop（OODA 主循环）** 三位一体为内核，面向企业流程自动化场景（首个目标场景：企业内部 OA 审批），目标是让非技术人员通过自然语言/拖拽定义流程，由运营体自动执行、自愈与进化。

当前阶段：**Phase 1 沙盘假定通过，进入 Phase 2（2026-09-14，用户指示的假定，非真实试用结论）**。W1-W10 Demo 全部完成（W9-W10 电商退款端到端 Demo 于 2026-09-13 跑通，08 §7.3 七条验收全部达成）。**Phase 2 六项节点「条件分支节点 condition」「循环节点 loop（v1 条件循环 while）」「并行节点 parallel（扇出/汇聚网关，v1 all_success/all_completed）」「等待节点 wait（v1 定时等待 duration 1-600s）」「子图节点 subgraph（v1 引用已保存图，进程内嵌套执行，深度上限 3）」「人机协作节点 human_approval（v1 进程内审批挂起，10-3600s 超时默认 reject）」均已于 2026-09-14 端到端落地**：condition 节点 config 挂 branches/defaultTarget；loop 节点 config 挂 mode/continueExpression/maxIterations/bodyTarget/exitTarget，循环体回边是唯一合法环（U7 调和），达最大次数/表达式异常 fail-safe 退出；parallel 节点 config 挂 joinStrategy/branches/joinTarget，合成 `__join__` 网关节点做汇聚屏障（spike 实测普通多入边不构成 barrier），all_success 分支失败 fail-safe 汇聚不短路（any_success 缓做 14 D18）；wait 节点 config 挂 waitType/durationSeconds，执行器同步 sleep（线程池边界，600s 上限），恰好一条普通出边纯透传，事件等待/动态时长缓做 14 D19；human_approval 节点 config 挂 summary/approver/timeoutSeconds/onTimeout/approvedTarget/rejectedTarget，`collaboration/` 新增 ApprovalBroker（uuid4 token+`threading.Event`，REST 放行；决策三来源：人工/run inputs 预置/超时，首决生效重复 409），固定双出口（通过/拒绝），阻塞不产生超步，持久化 interrupt/checkpointer/多实例/通知/动态审批人缓做 14 D20；subgraph 节点 config 挂 graphId/inputs（graphId 引用 GraphStore 已保存图，inputs 为子图入参→父图 `{{路径}}`/字面量映射），执行器进程内重入 run_graph（复用 registry/decision/approval broker，子图事件 emit 压制），编译期递归校验缺失/自引用/跨图环/超深/子图非法，恰好一条普通出边，子图运行期异常 fail-safe 折叠为 `status:"failed"`，版本钉版/升级回归/子图市场/远程引用缓做 14 D21；六项均 EdgeDSL 与 Graph version 不变。**Phase 2 适配器方向首项「API 适配器（通用 HTTP 请求）」已于 2026-09-15 端到端落地**：新包 `httpapi/`（adapter_id=`http`，09 待定项 7 收口避名 FastAPI `api/`），单工具 `http/request`，节点 params 为 JSON `{method,url,headers,body,timeout}` 支持 `{{路径}}` 插值，env/注册默认 baseUrl+公共 headers 与节点 headers 键级合并；任何 HTTP 响应（含 4xx/5xx）均 SUCCESS 输出 `{status,headers,body}`，仅传输层失败 FAILED（HTTP_TIMEOUT/HTTP_CONNECT_ERROR），v1 无重试/无 SSRF 防护/无凭证加密，OpenAPI 导入/OAuth/秘密变量/录制生成等缓做 14 D22（触发：真实外部系统接入或多租户）；内置 `/api/demo/mock/*` 端点供离线实测，前端工具选择器首次接 `GET /api/adapters`（按适配器分组、权限/幂等标记）。**Phase 2 适配器方向第二、三项「DB 适配器（通用 SQL）」「消息适配器（进程内消息服务）」已于 2026-09-15 端到端落地**：新包 `database/`（adapter_id=`database`，`database/query` read·幂等返结果集、`database/execute` write 返 rowcount，04 §4.7）与 `message/`（adapter_id=`message`，`message/send` write，`{channel,to,subject,body}`、to ≤20、零真实投递，04 §4.8）；适配器专用 `ATLAS_DATABASE_URL`（scheme 白名单 pg+psycopg/sqlite，自建 engine 工厂，与平台 DATABASE_URL 物理隔离；ADR T10，09 待定项 8 收口），未配置时 api/main 与 demo registry 显式注入 StaticPool SQLite 内存库（seed 两笔订单，reset 重建），适配器自身未注入则 fail-closed DB_NOT_CONFIGURED；消息落进程内记录 + `GET /api/demo/messages` 陪同查看；真实数据库驱动/SQL 审查/只读账号缓做 14 D23，SMTP/IM/短信/webhook 真实投递缓做 14 D24；**DB/消息适配器工作经 PR #11 于 2026-09-15 合并 main**。**Phase 2 能力项「流程模板库 v1（5 个内置只读模板）」已于 2026-09-15 端到端落地**：新包 `template/`（`graphs.py` 5 个模板图、`catalog.py` TemplateMeta+TEMPLATES+访问器，ADR T11，04 §5.10），模板 `refund-auto`/`http-orders-branch`/`sql-query-notify`/`sql-approval-write`/`approval-timeout-reject` 全部只用已注册 demo 适配器、零配置可跑；只读 Python 常量随代码发布（无 DB/CRUD、reset 不影响），REST `GET /api/templates`（列表投影无 graph）+ `GET /api/templates/{id}`（含 graph，未知 404）；前端「从模板新建」Modal 整画布替换（复用 loadGraph，不做 id 重映射）+ 未保存警示；退款 golden 图移入 template 包，nl_generate 改 import（单一事实源，U27 固化）；用户自建/市场/远程导入/版本钉版缓做 14 D25。**Phase 2 能力项「操作录制 v1（运行录制 + 回放比对）」已于 2026-09-15 端到端落地**：新包 `recording/`（RecordingCase/RecordingStore 进程内、normalize/preset_approvals/compare 纯函数，04 §5.11，ADR T12），录制=前端在正常 /run/stream 收集 node_end（无专用运行端点），回放=冻结 Graph 快照走标准 run_graph（人工决策预置 inputs.approvals 秒回不挂起），序列+逐节点归一化深等（token/sent_at/消息 uuid/HTTP date/resolvedBy/载荷 approvals 五规则），用例 reset 不清除（测试资产）重启清空；批量回放/跨版本对比/影子模式/Mock 响应缓做 14 D26，浏览器操作录制仍随 D22。**Phase 2 能力项「单步调试与断点 v1（逐步执行/节点断点/条件断点/变量只读查看）」已于 2026-09-15 端到端落地**：新包 `debug/`（DebugSession/DebuggerBroker 进程内会话与 step/continue/stop 状态机、DebugController 统一执行器单点钩子，04 §5.12，ADR T13），断点会话级存前端不进 Graph JSON（EdgeDSL/Graph version 1 不变、node_schema breakpoint 预留位不启用），paused 帧下发 globals/outputs 深拷贝只读快照，条件断点复用 conditions 引擎且表达式异常 fail-safe 不命中，并行暂停会话锁串行、合成 `__join__` 不停、subgraph 整段执行、wait 停在 sleep 前、human_approval 停在审批注册前（Modal 放行后照常），停止仅暂停点生效（运行中急停缓做 14 D27）；REST `POST /api/debug/{token}/resume`+`GET /api/debug`，同步 /run 带 debug 422，reset 按 stop 释放全部调试会话；前端 Header 单个「调试」入口（启动即逐节点）、节点左侧断点圆点（条件白心）、属性面板断点+表达式实时校验、悬浮工具条（下一步/继续/停止+变量过滤只读快照）；持久化中断-恢复/断点持久化进图/变量改写续跑/变化历史/子图内断点/命中计数缓做 14 D27。后端 329 单元全绿（+U29 11/I17 7），前端 vitest 56（8 文件，+4），浏览器六场景实测（逐步序列、条件真假两路、审批暂停先于 Modal、停止无 result、断点标记/刷新清空、reset 释放悬挂会话）零控制台错误。**Phase 2 能力项「基础监控告警 v1（运行/节点指标 + 站内告警中心）」已于 2026-09-15 端到端落地**：新包 `monitoring/`（records.py MonitoringStore ring 200 单锁、alerts.py 四内置规则阈值可调、metrics.py 两形状失败推断与 nearest-rank 分位聚合，04 §5.13，ADR T14），埋点纯在 API 层包装 sync /run 与非 debug /run/stream 两个真实运行入口（零节点类型分支；debug/录制回放/subgraph 重入天然不记），健康=completed 且无失败节点；REST 新增 metrics/runs/rules(GET+PUT)/alerts(列表+acknowledge+resolve)，reset 清空运行/告警/streak 并恢复默认规则；独立前端 /monitoring 页（Dashboard 入口、指标六卡、告警表确认/关闭、规则阈值、最近运行展开、5s 轮询），后端 358 passed/8 skipped（+U30 20/I18 9）、前端 vitest 59（9 文件，+3），浏览器八场景实测；OTel/Prom/Grafana 正式栈随 D11、适配器调用级指标/IM 出口/自定义规则 DSL 等缓做 14 D28。**Phase 2 新增能力收口项「多租户权限 v1（端点级 RBAC + 租户分区）」已于 2026-09-16 端到端落地**：新包 `iam/`（种子两平级租户四账号、三角色 viewer/operator/admin、进程内 `sess-` token 会话、TenantRegistry 按租户惰性持有 stores/brokers/monitoring，04 §5.14，ADR T15），除登录/health/demo 模拟系统外全部端点 Bearer 鉴权（401/403/跨租户 404），用户数据（graphs/recordings/feedback/messages/approvals/debug/monitoring）全分区、全局基础设施（模板/适配器发现/demo shop+mock/demo SQLite/出向 client）共享，admin reset 只清本租户且保留录制/反馈；前端登录页 + localStorage 会话 + 401 自动回登录 + UserBadge + 按角色隐藏写操作 UI；后端 377 passed/8 skipped（+U31 17/I19 11）、前端 vitest 64（10 文件，+5），浏览器八场景实测零控制台错误（登录/坏凭证/刷新保持、租户 B 全空且跨租户 graph 404、viewer 只读 403、operator 告警处置、admin reset 范围、登出、阈值分区）。**至此 08 Phase 2「新增能力」清单全部落地；2026-09-16 新增方案文档 docs/19（Schema 驱动配置内核 + 多智能体端到端，题述设计提案），其「设计态内核前置项」（Capability input/outputSchema + 拓扑作用域索引 + L2 引用校验）已在 08 Phase 2 延伸区立项待落码，提案其余部分登记 14 D29-D34。****连带治理：`/run/stream` 由「先跑完再回放」改为真流式**（run_graph 在 daemon 线程执行、queue.Queue 实时下发 node_start/node_end，终帧 `event: result`，运行期异常 `event: error`）——审批阻塞期间浏览器可实时收到 node_start 并放行，同时清掉 wait 落码时记载的假流式局限。纯 stdlib 安全规则表达式（禁 eval/算术/函数）为 condition/loop 两类节点共用，DSL 图级校验、LangGraph 全 conditional 出边齐备，前端配置面板/多出口 Handle/出边标签齐备，浏览器实测金额分流、循环三场景（条件收敛/撞上限 fail-safe/表达式异常）、并行三场景（不等长双分支汇聚/一支失败 fail-safe/坏配置报错）与审批三路径（人工同意/人工拒绝/10s 超时自动拒绝且弹窗自动关闭）。退款链路：webhook 退款单 → AI 决策或条件分支 → shop 适配器执行退款或转人工；SSE 节点事件实时上屏，自然语言可生成退款流程草稿。Phase 1 种子验证工程侧齐备（Docker 一键交付/reset/反馈入口/docs 18+TRIAL），但**无真实 C1-C5 试用数据**，Go 结论为沙盘假定（18 文档 §8）；真实试用若 No-Go 需回退。

结构：
- `docs/` — 全部规格文档（入口：`00-文档索引与治理.md`）
- `README.md` / `AGENTS.md` / `CLAUDE.md` / `git-commit-message.md` / `CHANGELOG.md` — 根目录元文档
- `src/atlas/` + `frontend/` — OODA 最小循环、Graph DSL 编译运行（FastAPI）、前端编辑器核心（拖拽/配置/变量/一键编译运行）已跑通；规划见 `docs/09-工程骨架与目录结构.md`

## Project documents（文档地图）

**文档地图（按场景怎么读 + 治理规则）**：[docs/00-文档索引与治理.md](docs/00-文档索引与治理.md) 是单一事实源与 AI 工作入口，本区只做直达索引：

* [docs/01-PRD-产品需求规格.md](docs/01-PRD-产品需求规格.md) — 产品概念（Harness/Graph/Loop 定义、全场景图谱、DIY 机制）+ 产品定义 + Demo 范围
* [docs/02-技术架构设计.md](docs/02-技术架构设计.md) — 三位一体架构、系统分层、技术选型、组件全景
* [docs/03-数据模型与Schema-契约索引.md](docs/03-数据模型与Schema-契约索引.md) — 全部 Schema 位置速查与字段概览
* [docs/04-组件设计-编辑后台.md](docs/04-组件设计-编辑后台.md) — 编辑后台组件（节点/工具/适配器/逻辑/变量/上下文）
* [docs/05-组件设计-运营体五项核心.md](docs/05-组件设计-运营体五项核心.md) — Skill / Memory / 智能体协同 / 部署方式 / 自定义前端模板
* [docs/06-运行时与质量保障.md](docs/06-运行时与质量保障.md) — Loop 主循环、决策节点、Harness Gateway、Web 适配器、测试层次、安全清单
* [docs/07-递归自动化引擎设计.md](docs/07-递归自动化引擎设计.md) — 平台探索器 / 行为挖掘器 / 诊断修复器 / 元运营体 / 自我进化
* [docs/08-任务迭代计划.md](docs/08-任务迭代计划.md) — 开发路线图、Demo 计划 W1-W10、Phase 1-4 迭代路线、商业化路径、验收标准
* [docs/09-工程骨架与目录结构.md](docs/09-工程骨架与目录结构.md) — 工程目录树、模块→文档映射、待填项清单 ★落码必读
* [docs/10-技术选型决策记录.md](docs/10-技术选型决策记录.md) — 选型 ADR（✅ 已定 / ⏳ 待决策）、Demo 依赖清单 ★落码必读
* [docs/11-存储与数据持久化设计.md](docs/11-存储与数据持久化设计.md) — 存储分层、数据对象→表映射、读写路径
* [docs/12-API与模块接口清单.md](docs/12-API与模块接口清单.md) — 引擎内部接口、Harness Gateway、后端 REST、协同/评估接口
* [docs/13-测试用例清单.md](docs/13-测试用例清单.md) — 单元/集成/回放/端到端/AI 评估用例 + 上线前四道门
* [docs/14-缓做事项登记表.md](docs/14-缓做事项登记表.md) — 缓做/低优事项登记表（每条带触发条件，单一事实源）
* [docs/15-环境与分支策略.md](docs/15-环境与分支策略.md) — 环境划分（DEV/TEST/PROD）+ 分支→环境映射（main/dev/feature/*）
* [docs/16-反馈工作流.md](docs/16-反馈工作流.md) — 用户与 AI 协作者的反馈方式（截图+标签 / computer use）
* [docs/17-前端国际化与设计Token方案.md](docs/17-前端国际化与设计Token方案.md) — i18n（i18next 触发式引入/错误码边界）+ 设计 Token 三层模型（需求依据 04 §28；Token 已落码，i18n 触发式引入，存量零星硬编码见 17 §3.5）
* [docs/18-种子客户验证计划.md](docs/18-种子客户验证计划.md) — Phase 1 种子客户画像/招募标准、三场景试用脚本、Go/No-Go 指标、反馈机制（客户向操作指南见根目录 TRIAL.md）
* [docs/19-Schema驱动配置内核与多智能体端到端方案.md](docs/19-Schema驱动配置内核与多智能体端到端方案.md) — 题述设计方案（2026-09-16）：Schema 驱动前端配置内核（SchemaRegistry/Widget 注册表/拓扑作用域索引/三级校验/性能）与多智能体协同/交互模板/部署灰度回滚/可观测端到端，逐节标注 Atlas 现状与缺口；**提案未立项**，落码前先走 10 ADR/08/03 流程
* [docs/20-题述方案工程化落地路线.md](docs/20-题述方案工程化落地路线.md) — docs/19 的工程化落地拆解（2026-09-16）：M0 前置项已落 + M1–M10 里程碑（题一 Schema 内核 M1–M4、持久化 M5、题二版本化/任务总线/交互卡片/灰度回滚/span M6–M10），每期含仓内改动面、前置门、不做什么、验收口径，附契约同步矩阵与 D29–D34 触发总表；**M1 起未立项**
* [docs/21-题述原题与方案作答.md](docs/21-题述原题与方案作答.md) — 两道题的题卷+答卷合订阅读版（2026-09-16）：§1 原题照录，§2/§3 按题面「请给出」条目逐条方案作答（模块/数据流/Schema 形状/伪代码/机制策略，不含实现代码），§4 接缝与落地状态，§5 脱离 Atlas 约束的绿地视角（通用骨架 vs Atlas 塑造的选型分期）；细节以 19 为准、排期以 20 为准
* [docs/22-题述方案绿地独立设计.md](docs/22-题述方案绿地独立设计.md) — 两道题的绿地完整独立方案（2026-09-16）：**显式不受 Atlas 架构/技术栈/单进程 Demo 约束**，按可上线多租户 SaaS 从零设计（rjsf/Formily+AJV+Monaco、Temporal 主协调+Worker Versioning 钉版、独立 Bot 服务、NATS、KMS、OTel/Prometheus、Rollout Controller 灰度），§3 两版选型分叉总对照、§4 七条两版不变通用原则；**非 Atlas 落码依据**，落码以 19/20 为准

根目录元文档：[CHANGELOG.md](CHANGELOG.md)（变更日志）/ [CONTRIBUTING.md](CONTRIBUTING.md)（贡献指南）/ [AGENTS.md](AGENTS.md)（AI 行为规范，**新会话必读**）/ [TRIAL.md](TRIAL.md)（种子客户试用一页纸）/ [git-commit-message.md](git-commit-message.md)（commit 详细规范）/ [MIGRATION_CONVENTION.md](MIGRATION_CONVENTION.md)（DB 迁移规范）/ [BENCHMARK.md](BENCHMARK.md)（性能基准记录表）/ [.gitleaks.toml](.gitleaks.toml)（密钥扫描配置）/ [.env.example](.env.example)（环境变量模板）

**代码阶段待补（遵循 agent-world 惯例）**：`.pre-commit-config.yaml`。CI 工作流（`.github/workflows/ci.yml`，gitleaks/后端 pytest/前端 vitest+build 三道门）已于 2026-09-14 落地（14 D10a，ADR T8）；CD 仍缓做（14 D10b）。`Dockerfile` + `docker-compose.yml` + `.dockerignore` 已在 Phase 1 准备期落地（2026-09-13）。`pyproject.toml` 与前端工程已在 W1 创建。

## Current state（当前状态）

- **阶段**：W9-W10 电商退款端到端 Demo 已完成（2026-09-13，08 §7.3 七条验收全达成）。新增 `llm/`（LiteLLM 退款决策 + 规则兜底 + NL 草稿生成）、`shop/`（DemoShopService + ShopHarnessAdapter：login/list_pending_refunds/execute_refund/request_human_approval/process_refund）两个包；`graph/loader.py` 经依赖注入接真实决策/适配器/事件回调；API 新增 SSE 运行流、NL 生成、适配器发现与模拟商家控制台；前端退款单选择、节点实时状态、NL 草稿载入。Phase 2 六类节点 + 适配器三项（通用 HTTP API 适配器 `httpapi/`、通用 SQL 适配器 `database/`、进程内消息适配器 `message/`）+ 能力项「流程模板库 v1」（只读内置目录 `template/`，5 个零配置模板 + 两个 GET 端点 + 前端「从模板新建」整画布替换）+ 能力项「操作录制 v1」（新包 `recording/`：进程内用例存储 reset 不清除 + 冻结 Graph 快照回放 + 序列/逐节点归一化比对，五个 REST 端点 + 编辑器「录制与回放」Modal，04 §5.11）全部落地后，Phase 2 能力项「单步调试与断点 v1」（新包 `debug/`：进程内调试会话与统一执行器暂停钩子、会话级断点不进 Graph JSON、step/continue/stop、条件断点 fail-safe、globals/outputs 只读快照，04 §5.12）亦已落地，能力项「基础监控告警 v1」（新包 `monitoring/`：进程内 ring 200 运行记录、四内置规则阈值可调、站内告警中心，04 §5.13）亦已落地；后端 358 单元全绿、前端 vitest 59（9 文件）全绿（明细见 Quality gate），浏览器实测 12345→refunded / 12346→human_review、子图引用真实保存图的父图运行（入参映射到达子图、父图引用子图产出）、http/request 对内置 mock 端点 GET 200/POST 插值/401 仍 SUCCESS/坏 params FAILED，database/query 打 demo SQLite 返两笔订单、execute INSERT 后 query 可见且 reset 复原、message/send 记录在 `/api/demo/messages` 可见且 reset 清空，模板库五模板加载/编译与 sql-query-notify（min_amount=1000 走有单分支）、approval-timeout-reject（10s 无人操作超时拒绝）、http-orders-branch（200 成功分支）三条实跑链路，以及录制 approval-timeout-reject 一次真实运行→回放秒回三节点全匹配（跨运行 token/消息 uuid/sent_at/resolvedBy/预置 approvals 归一化生效）→reset 后用例保留且回放仍匹配→Popconfirm 删除用例消失；能力项「多租户权限 v1」（新包 `iam/`：种子两租户四账号、三角色端点级 RBAC、进程内 token 会话、TenantRegistry 租户惰性分区，04 §5.14）亦已落地，全部业务端点 Bearer 鉴权、用户数据按租户全分区（跨租户对象 404、角色不足 403、未登录 401），前端登录页/会话/角色门 UI 齐备，后端 377 单元全绿、前端 vitest 64（10 文件）全绿，浏览器八场景实测登录/分区/角色矩阵/reset 范围/登出全通过（明细见 Quality gate）。2026-09-16 当日再落地 **08 Phase 2 延伸立项「设计态内核前置项」①–⑤**（docs/19 提案驱动：Capability input/output_schema JSON Schema 子集白名单与发现投影、前端拓扑作用域 ScopeIndex、L2 模板引用三错误码实时校验、DSL 编译期复查反绕过、NL 草稿参数非阻塞警告，零新依赖，后端 410/前端 87；①–⑤当日经 **PR #19 合并 main**（merge `6b8ce5a`；提案与 handoff 同步随 **PR #18** merge `27e331f`，三道门 CI 实跑全绿，详见 Recently shipped 2）。当日随后完成题述方案文档包：[docs/20](docs/20-题述方案工程化落地路线.md) 工程化落地路线（M1–M10，每个里程碑首行标注对应题面 A1–A6/B1–B6；§1.2 就绪度快照与推荐推进次序——**M1 可直接立项开工、M2 被待决 ADR T16 卡住、M3 依赖 M2、M4 依赖 M3、M5 需先补一轮持久化契约设计并拆 M5a/M5b、M8 是唯一不被 M5 阻塞的题二里程碑（M3 完成后可插队）**）、[docs/21](docs/21-题述原题与方案作答.md) 题卷+答卷合订（§1 原题照录、§2–§4 不含实现代码的方案作答、§5 脱离 Atlas 约束的绿地视角）、[docs/22](docs/22-题述方案绿地独立设计.md) 不受 Atlas 架构/技术栈约束的完整绿地方案（**非 Atlas 落码依据**）；[docs/10](docs/10-技术选型决策记录.md) §4 登记 **ADR T16（前端 L1 JSON Schema 校验器：手写最小子集 / AJV / 后端校验，M2 启动前必须收口；推荐手写起步但未拍板）**。该文档包共 7 个 docs 提交（`b0a1f2a`→`fc67668`）目前仅在 dev 本地未推送；**M1 起所有里程碑均未立项**）。
- **仓库**：git 仓库，**已推送 GitHub private 仓库 `bayernjf/atlas`**（main 为生产分支、dev 为集成分支，分支策略见 15 文档）。Phase 1 工作经 PR #3 于 2026-09-14 合并（merge `f069b53`，19 提交）；Phase 2 工作经 PR #4-#10 陆续合并——#4 condition、#5 loop、#6 parallel、#7 wait、#8 human_approval、#9 subgraph（2026-09-14）、**#10 API 适配器 http/request（2026-09-15，merge `4385a00`）**、**#11 DB/消息适配器 database/query|execute + message/send（2026-09-15 合并）**；PR 与合并后 main 的 GitHub Actions CI 均实跑全绿。流程模板库 v1（2026-09-15，6 提交 8f3238b 起）、操作录制 v1（2026-09-15，5 提交 4f7af04→857924f→7282cb2→19b39ee→42a82f3）、单步调试与断点 v1（2026-09-15，6 提交 2d7fa28→6693023→7e5a23a→5e13cdd→8d5bdd7→ecb9605）与基础监控告警 v1（2026-09-15，5 提交 2362fdf→c474a80→c721f47→91f6bcc→8551f90）、多租户权限 v1（2026-09-16，4 提交 7cbd509→3620545→64d34b3→7a2ed14，外加登录页 token/orientation 与 docs/17 补契约 b0bf5d4/5660718/2759a13）随后经 PR #12-#16 全部合并 main——#12 模板库（merge `9c849ff`）、#13 录制（`1e6475e`）、#14 调试（`ebffa12`）、#15 监控（`96a9d12`）、#16 多租户（`6bbd77c`）；另经 PR #17（merge `34304b5`）将 `.codegraph/` 加入 `.gitignore`；**#18 docs/19 题述提案 + handoff 同步（merge `27e331f`）与 #19 设计态内核前置项（merge `6b8ce5a`）亦于 2026-09-16 合并 main**，各 PR 三道门 CI 均实跑全绿。当前本地 main 仍停在 `34304b5`（未拉取），origin/main 在 `6b8ce5a`；dev 领先 origin/dev 的提交均为文档（`b0a1f2a` 起：docs/20/21/22 文档包 + T16 + handoff 同步），未推送。
- **技术选型**：✅ 已全部收口（2026-09-13，T1-T5 见 10 文档 §3）：Python 3.11+ 引擎 / Go Harness 网关为产品化目标、**Demo 用 Python/FastAPI 实现同构接口** / TypeScript + React 19（满足 React 18+）/ LangChain+LangGraph / LiteLLM（Demo 对接主流商业 API，`.env` 切换，不引入 vLLM）/ Playwright / PostgreSQL+pgvector / Redis / **Demo 进程内事件总线替代 NATS** / FastAPI / `@xyflow/react` 12 + Zustand 5 + AntD 6 / 评估优化层仅留接口；剩余 ⏳（vLLM、策略训练、多语言）均为后续阶段范围。
- **Demo 依赖清单已落码**：后端在 `pyproject.toml`，前端在 `frontend/package.json` / `pnpm-lock.yaml`（版本为 2026-09-13 解析的稳定版）。
- **关键文件**：后端 `src/atlas/`（18 个包；engine 最小循环、graph DSL 契约+编译器（W9-W10 注入决策/适配器/事件）、llm 退款决策+NL 生成、shop Demo 店铺服务+适配器、httpapi 通用 HTTP API 适配器（httpx，单工具 http/request）、database 通用 SQL 适配器（database/query|execute，ATLAS_DATABASE_URL 白名单工厂，demo SQLite 回退）、message 进程内消息适配器（message/send 零真实投递）、template 内置只读流程模板目录（graphs.py 5 图 + catalog.py 元数据/访问器，无 DB/CRUD）、recording 运行录制与回放（cases.py pydantic 用例+进程内 store reset 不清除、replay.py 归一化/预置审批抽取/序列与逐节点比对）、debug 进程内调试会话与暂停控制器（sessions.py DebugSession/DebuggerBroker step/continue/stop 状态机、controller.py 统一执行器 before_node 钩子，会话级断点/条件 fail-safe/只读快照）、monitoring 进程内运行指标与告警（records.py ring+单锁 store、alerts.py 四规则 RuleConfig/evaluate_rules、metrics.py 节点失败两形状推断/分位聚合，04 §5.13）、iam 种子身份与租户分区（principals.py 两租户四账号/三角色矩阵、sessions.py 进程内 sess- token、registry.py TenantRegistry 按租户惰性装配服务、deps.py get_principal/require/services_for，04 §5.14）、api FastAPI（auth login/me/logout、graphs/SSE/NL/adapters/templates/recordings 五个录制端点/debug resume+列表/monitoring metrics·runs·rules+alerts 确认关闭/Demo 控制台/mock 端点/messages）、memory 连接层、harness 契约/注册、collaboration 审批 broker、web 三层定位适配器）、`db/migrations/001_enable_pgvector.sql`，前端 `frontend/`（登录页/Dashboard/Editor/监控页 + 画布/节点/属性/变量面板 + `lib/` 纯逻辑（auth 会话与角色矩阵/变量/节点目录/序列化/适配器分组/录制事件转换/监控中文映射/API client 含 Bearer/SSE 与监控告警）+ Zustand store + vitest 单测）；模块填充顺序见 09 文档待填项清单。

## Active work / 待办

按优先级降序：

1. ✅ **W1-W2 基础骨架落地**（08 文档 7.1，2026-09-13 全部完成）：工程骨架/venv、LangGraph 最小 OODA 循环、前端编辑器框架、PostgreSQL+pgvector 连接均已跑通（详见 Recently shipped）。
2. ✅ **W5-W6 编辑器核心功能**（08 文档 7.1，2026-09-13 完成）：拖拽画布、3 种节点类型化配置 + 实时校验、全局变量与 `{{路径}}` 引用、Graph JSON 导出；vitest 20 个单测全绿。
3. ✅ **W7-W8 编译与运行**（08 文档 7.1，2026-09-13 完成）：Graph DSL 契约 + 静态校验、DSL→LangGraph 编译/运行、FastAPI graphs 保存/读取/编译/运行、编辑器一键"编译并运行"；后端新增 16 个用例。
4. ✅ **W9-W10 端到端 Demo**（08 文档 7.1，2026-09-13 完成）：电商退款完整链路跑通（LiteLLM 决策 + 规则兜底、shop 退款业务能力、SSE 实时进度、NL 退款草稿、模拟商家控制台），08 §7.3 七条验收逐条达成（映射见 08 W9-W10 落码记录）。
5. ✅ **Phase 2 已完成并全部合并 main（2026-09-14 沙盘假定 Go，2026-09-16 收口）**：18 文档 §8 记录假定验证通过。**首项「条件分支节点」、第二项「循环节点（while）」、第三项「并行节点（parallel 扇出/汇聚）」、第四项「等待节点（wait 定时等待）」、第五项「人机协作节点（human_approval 进程内审批挂起）」、第六项「子图节点（subgraph 引用已保存图，进程内嵌套执行，深度上限 3，单出边 fail-safe）」已完成（2026-09-14，见 Recently shipped 10-15；至此 08 Phase 2 节点清单六项全部落地）；适配器方向首项「API 适配器（通用 HTTP 请求 http/request，进程内 httpx）」已完成（2026-09-15，见 Recently shipped 9），第二、三项「DB 适配器（通用 SQL：database/query|execute，ATLAS_DATABASE_URL 隔离 + demo SQLite）」「消息适配器（message/send，进程内消息服务零真实投递）」已完成（2026-09-15，**经 PR #11 合并 main**，见 Recently shipped 8），能力项「流程模板库 v1（5 个内置只读模板 + GET 目录端点 + 前端「从模板新建」整画布替换）」已完成（2026-09-15，见 Recently shipped 7，经 PR #12 合并 main），能力项「操作录制 v1（一次真实 Graph 运行录为回归用例：前端收集 node_end、Graph 快照冻结、回放预置审批秒回、序列+逐节点归一化比对、reset 保留）」已完成（2026-09-15，见 Recently shipped 6，经 PR #13 合并 main），能力项「单步调试与断点 v1（会话级断点随 /run/stream 请求传入不进 Graph JSON、step/continue/stop、条件断点 fail-safe、globals/outputs 只读快照，04 §5.12）」已完成（2026-09-15，见 Recently shipped 5，经 PR #14 合并 main），能力项「基础监控告警 v1（运行级+节点级采集 ring 200、四内置规则阈值可调、站内告警中心 acknowledge/resolve、独立 /monitoring 页，04 §5.13）」已完成（2026-09-15，见 Recently shipped 4，经 PR #15 合并 main）**，Phase 2 新增能力收口项「多租户权限 v1（端点级 RBAC + 用户数据按租户全分区 + 登录会话，04 §5.14，14 D7 v1 落码）」已完成（2026-09-16，见 Recently shipped 3，经 PR #16 合并 main）。**至此 08 Phase 2「新增能力」清单（六类节点 + 三类适配器 + 模板库 + 录制 + 调试 + 监控告警 + 多租户权限）全部落地。**2026-09-16 新增 [docs/19](docs/19-Schema驱动配置内核与多智能体端到端方案.md)（题述方案提案），并在 08 Phase 2 延伸区**立项「设计态内核前置项」，当日①–⑤全部落码（提交 8606aa3→34d5cb5，**当日经 PR #19 合并 main**，merge `6b8ce5a`，详见 Recently shipped 2）**：适配器 Capability 补 input/output_schema（JSON Schema 子集白名单，九工具全部声明）+ 前端拓扑作用域索引 lib/scope.ts 替代 listVariablePaths 平铺 + L2 模板引用校验（REF_NODE_NOT_FOUND/REF_NOT_IN_SCOPE/REF_PATH_NOT_FOUND，面板错误清单+节点角标）+ DSL 编译期复查反绕过（仅 compile_graph opt-in）+ NL 草稿参数填充尽力校验（非阻塞 paramWarnings），零新依赖、不做表单生成（D22/D29）与类型级跨节点校验（D30），范围与原子提交序见 08；题二其余提案（任务总线/版本灰度/交互卡片/span 追踪/Schema 内核通用化/作用域 v2）登记 14 D29-D34。同日产出 [docs/20](docs/20-题述方案工程化落地路线.md)（19 的 M1–M10 落地路线，§1.2 就绪度快照：M1 可立即立项、M2 待 T16 收口、M5 需先补持久化契约设计、M8 完成 M3 后可插队）、[docs/21](docs/21-题述原题与方案作答.md)（题卷+答卷合订，无实现代码）、[docs/22](docs/22-题述方案绿地独立设计.md)（绿地完整独立方案，**非 Atlas 落码依据**），10 §4 登记待决 **ADR T16**（前端 L1 校验器，M2 启动前必须收口）；**M1–M10 均未立项，立项前不动代码**。API 适配器的 OpenAPI 导入/凭证加密/秘密变量/OAuth2/SSRF/重试熔断/录制与 NL 生成适配器缓做（14 D22，触发：真实外部系统接入或多租户；2026-09-16 用户拍板口径——iam 种子沙盘不算「多租户」，D22 整体不取回，但 SSRF egress/秘密变量注入/凭证加密三项安全准入随首个真实接入先行，与 D23 同批）；DB 适配器的 JDBC/ODBC/数仓驱动、只读账号强制、SQL 审查、schema 表单、查询审计/分页游标缓做（14 D23），消息适配器的 SMTP/IM（钉钉/企微/飞书）/短信/webhook 真实投递、OAuth、消息模板、reply 线程、入站消费、限流重试缓做（14 D24，触发均为真实渠道/外部系统接入）。condition 的 LLM 判断分支与表达式函数库/算术缓做（14 D14/D15），loop 的遍历循环与 break/continue 缓做（14 D16/D17），parallel 的 any_success（OR-join+取消分支）缓做（14 D18），wait 的事件等待（中断-恢复）与动态时长缓做（14 D19，触发：事件驱动/长等待业务 + 11 S1 持久化就绪），human_approval 的持久化中断-恢复/多实例/IM 通知/动态审批人缓做（14 D20，与 D19 同批中断模型，触发：11 S1 就绪或真实审批业务），subgraph 的版本钉版/升级回归/子图市场/远程引用/嵌套可视化/子图内审批交互缓做（14 D21，触发：模板库/发布流或多租户；模板库 v1 已落地但不涉子图市场/钉版，D21 不解除；子图内审批交互随 D20 同批）；模板库 v1 只读内置目录，用户模板 CRUD/市场共享/远程导入/版本钉版/参数化实例化向导缓做（14 D25），模板元数据多语言随 14 D13。缓做触发：D6 Go 网关触发条件字面为"进入 Phase 2"，但建议待多实例部署需求明确再启动（与 D5 NATS/D10b CD 同批）。
6. 文档缺口登记：`09-工程骨架` 待定项 1/2/3/4/5/6 均已收口（待定项 3：不新增 deployment/ 包，Dockerfile + docker-compose.yml 放仓库根，2026-09-13）。新增测试运行器选型 vitest 已记 10 文档 §4 ADR（W5-W6）。
8. ✅ **Phase 1 种子客户交付准备（工程侧已齐，试用为沙盘假定）**：Docker Compose 一键启动（`Dockerfile` 多阶段 + FastAPI 同源托管 `frontend/dist` + `POST /api/demo/reset` 重置种子数据，镜像实测黄金用例通过，2026-09-13）、种子验证计划 [docs/18](docs/18-种子客户验证计划.md) + 客户向 [TRIAL.md](TRIAL.md)（2026-09-13）、应用内反馈入口（编辑器"反馈"按钮 + `POST/GET /api/feedback`，进程内存储、reset 不清除，后端 62 测试/浏览器实测通过，2026-09-13）。业务动作（招募 3-5 家、回填 18 §6.1）在沙盘假定下标记完成但无真实数据；若真实试用启动，仍按 18 文档执行并以实测覆盖假定。
9. ✅ **D10a CI 质量门（2026-09-14 完成并验证）**：14 D10 拆分后 CI 部分提前触发（ADR T8 见 10 文档 §4，08 Phase 1 已记录）。`.github/workflows/ci.yml` 三道门——gitleaks 全历史密钥扫描、后端 pytest（Python 3.11，integration 默认跳过）、前端 oxlint + vitest + `pnpm build`（Node 22 / pnpm 10）；main/dev 推送与 PR 触发，重复运行并发取消。PR #3 实跑发现 gitleaks 在 pull_request 事件需 `pull-requests: read`（403，commit `247372e` 修复），修复后 PR 全绿、合并后 main（f069b53）CI success。CD 留 14 D10b（远程部署目标确定后）。
10. ✅ **D9 BENCHMARK 实测（2026-09-14 完成）**：新增 `scripts/benchmark.py`（纯 stdlib，零新依赖：10% warmup + 300 次/场景，p50/p99，输出 Markdown 行，DATABASE_URL 时追加 DB ping）；[BENCHMARK.md](BENCHMARK.md) 填首份基线（commit 407812e，macOS arm64 / CPython 3.11.15）：OODA 循环 2.05ms / ~488 loops/s、3 节点图编译 1.81ms、退款端到端（规则路径）2.28ms、Harness 进程内调用 0.002ms。LLM 决策延迟、记忆层读写、并行扇出、长流程内存增长在 BENCHMARK Scope 标注待接入补测。

**Active feedback**：暂无（种子试用开始后，由 docs/18 §6.1 跟进项迁移至此，解决后留痕 Recently shipped；流程见 16 文档）。
7. 📋 **i18n 与设计 Token 方案已定（2026-09-13，docs/17）**：设计 Token 等价替换**已落码**（`frontend/src/theme/tokens.ts` 单一事实源 + `setup.ts` 注入 `--atlas-*` 变量 + AntD theme，三页面主体零视觉差异；2026-09-16 17 §3.5 收尾清单五项已全部清零，`frontend/src` 仅 tokens.ts primitive 定义含 hex，浏览器逐项核对）；i18n 库（i18next+react-i18next）按触发条件引入（14 D12：首个英文使用者/出海需求），组件描述多语言随 Phase 2 模板库（14 D13）。

> 缓做/低优项：统一登记在 [docs/14-缓做事项登记表.md](docs/14-缓做事项登记表.md)（每条带触发条件，条件满足移回本区并标注重启日期）。当前含移动端适配器、策略训练、组件市场、模型路由器、NATS、Go 网关、多租户 v2（真实账号/节点级权限/审计/跨租户共享，v1 端点级 RBAC+分区已落，见 14 D7 边界注记）、运营体市场、CD 自动化部署、可观测性、i18n 落码、组件描述多语言等项（D10a CI、D9 BENCHMARK 均于 2026-09-14 完成移出）。

## Recently shipped（最近变更）

1. **docs(plan): 题述方案文档包——docs/20 落地路线 + docs/21 题卷答卷 + docs/22 绿地独立方案 + 待决 ADR T16（2026-09-16，纯文档 7 提交 `b0a1f2a`→`fc67668`，dev 本地未推送；零代码改动，测试基线不变 410/87）**——M0 前置项合并后围绕 docs/19 题述方案补齐四份工程推导文档与一个待决 ADR，全部登记 08/00 索引：**①** `b0a1f2a` 新增 [docs/20](docs/20-题述方案工程化落地路线.md)——19 的工程化落地拆解 M1–M10（题一 Schema 内核 M1–M4、持久化前置 M5a/M5b、题二版本化/任务总线/卡片/灰度回滚/span M6–M10），每期含仓内改动面/前置门/不做什么/验收口径 + 契约同步矩阵 + D29–D34 触发总表；`2e77311` 给 11 个里程碑标题各补首行「对应题面」标记（题一 A1–A6、题二 B1–B6 按 19 §1.1 映射）。**②** `d60a6e5` 在 docs/20 新增 §1.2「当前就绪度快照（2026-09-16）」：M1 可直接启动（只需 08 立项 + 03/04 x-keyword 微调，无需 ADR）、M2 被 T16 卡住、M3 依赖 M2、M4 依赖 M3 与 Worker benchmark ADR、M5 不能照路线直接落码需先补一轮契约设计（拆 M5a 进程内 Repository 重构 / M5b PostgreSQL）、M6–M10 无额外就绪度阻塞但硬依赖明确（M8 只硬依赖 M3、不依赖 M5，是唯一可在 M5 前插队的题二里程碑；M9 需同批取回 D26）；同提交在 [docs/10](docs/10-技术选型决策记录.md) §4 登记 **ADR T16（⏳ 待决，M2 启动前必须收口）**——前端 JSON Schema L1 校验器三选项：手写最小子集（对齐 04 §4.9 二十个白名单 keyword、零新依赖，推荐起步非拍板）/ AJV（推迟到 M3 证明 oneOf、if/then 复杂度后再评）/ 后端校验（已否决，违背编辑流与提交流分离），决策时同步 02/09 与 package.json。**③** `51ed2f0` §1.2 追加「前置任务的性质判断」（题一只有治理前置、题二有 M5 工程硬前置）与推荐推进次序四步（M1 立项开工并行开 T16 → M2→M3→M4，M3 后 M8 可插队 → 题二先补 M5 契约再 M5a→M6→M5b→M7→M9→M10）。**④** `c93956f` 新增 [docs/21](docs/21-题述原题与方案作答.md)：§1 两道题原题照录（题一六项要求+六项交付物、题二六个方向+五项交付物），§2/§3 按题面「请给出」条目逐条方案作答（模块/数据流/Schema 形状/作用域/校验伪代码/性能；总体架构/关键流程/四个核心 Schema/冲突三层/灰度五条/可观测四件套），§4 接缝与落地状态，全程不写实现代码；`1081094` 追加 §5「脱离 Atlas 约束的绿地视角」（哪些原则不依赖 Atlas、绿地选型五点不同、分层结论表）。**⑤** `fc67668` 新增 [docs/22](docs/22-题述方案绿地独立设计.md)：开篇立场声明**不受 Atlas 架构/技术栈/阶段约束、不构成本仓落码依据**，§0 五条绿地前提，题一（rjsf/Formily + AJV standalone + Monaco 语言服务 + Schema Registry 服务 + React Flow + Worker）与题二（Temporal durable execution 主协调 + Worker Versioning 在途钉版 + 独立 Bot 服务 + NATS 仅传领域事件 + 独立 Approval Service + KMS/Vault + Rollout Controller + OTel/Prometheus 第一天接入）全部交付物从零重写，§3 两版选型分叉 14 行总对照、§4 七条两版逐字一致的不变原则。docs/00 文档地图与 docs/08 三条 blockquote 随新文档原子同步（22 明确标注「不触发任何选型变更，落码以 19/20、10 ADR、01-08 为准」）。

2. **feat(harness/graph/editor/llm): 设计态内核前置项——Capability JSON Schema 子集 + 拓扑作用域 + L2 引用校验 + NL 参数警告（2026-09-16，08 Phase 2 延伸立项①–⑤当日全部落地；7 提交 8606aa3→34d5cb5 + 7ebae81，**当日经 PR #19 合并 main，merge `6b8ce5a`，三道门 CI 实跑全绿**）**——[docs/19](docs/19-Schema驱动配置内核与多智能体端到端方案.md) 题述方案提案驱动，契约写入 04 新增 §4.9（JSON Schema 子集与参数校验）与 §6.5（拓扑作用域与 L2 引用校验）两个权威 blockquote，03 tool/adapter_schema 字段概览补 input_schema/output_shape 子集注记、graph_definition 补「模板引用与拓扑作用域注记」、nl_generate_request 补 paramWarnings，12 GET /api/adapters 投影与 POST /api/nl/generate 响应同步，13 新增 U32/U33/U34/I20 + §9 日期回填；零新依赖（不引 AJV，选型 ADR T16 仅登记未触发）、EdgeDSL/Graph version 1 与运行期 fail-soft 插值语义不变；明确缓做：参数表单生成/秘密·环境·会话变量来源/结构化 Diagnostics 面板·quickFix·Web Worker（D22/D29）、类型级跨节点校验（D30）、parallel.result 动态键与 subgraph.outputs 子图深路径 v1 放行不校验。五项内容：**①** harness Capability 增可选 `input_schema/output_schema`（JSON Schema 子集 20 个白名单 keyword，空 `{}`=未声明任意形，Capability 构造期白名单校验 ValueError 中文报错），九个 demo 工具全部声明 schema，`GET /api/adapters` 投影携带两者；**②** 前端新纯逻辑 `frontend/src/lib/scope.ts` 拓扑 ScopeIndex（trigger/global 恒可见 + 沿入边反向 BFS 上游产出；loop.index/iterations 仅循环体可见且 loop 节点自身 continueExpression 可引用；parallel.status 静态可见、result 动态键 v1 排除；subgraph.outputs 根可见），变量插入 Select 仅列 visibleAt 当前节点的路径，替代旧 listVariablePaths 平铺；**③** L2 跨节点模板引用校验纯函数（extractRefs 带 token 区间）→ `REF_NODE_NOT_FOUND`/`REF_NOT_IN_SCOPE`/`REF_PATH_NOT_FOUND` 三诊断码（工具 result.* 深路径仅 output_schema 已知时校验，trigger 仅 context.triggerType|cron|webhookUrl|payload 四键、payload 内部不校验，动态键与模板插值原样透传），接入实时 validateNode、PropertyPanel 错误清单与画布 AtlasNode 角标 tooltip；**④** 后端 `dsl.py validate_graph(check_refs=True)` + `loader.compile_graph` 编译期同规则复查反绕过（parse_graph/save 仍只做结构校验；中文聚合错误 422），前后端规则同构，运行期未解析 `{{...}}` 保留原样不变；**⑤** `llm/nl_generate.py validate_param_fills` 对 NL 草稿 tool_call 参数做尽力静态校验（必填缺失、顶层浅类型、enum、additionalProperties:false），未知工具/空 schema/oneOf 字段/含 `{{` 模板值/不可解析 params JSON（裸插值）一律放行，`POST /api/nl/generate` 响应随草稿返回非阻塞 `paramWarnings:[string]`，前端生成草稿后逐条写运行日志、草稿照常载入。测试：后端 **410 passed/8 skipped**（基线 394：+tests/test_graph_ref_validation.py I20 九例 +tests/test_nl_generate.py U34 七例），前端 vitest **87（11 文件，+23 左右：scope/validateNode/变量插入联动）**，oxlint/tsc/vite build 全绿；浏览器对真实 uvicorn :8000 + Vite :5174 preview 实测——/api/adapters 九工具均带 input/output_schema、保存含坏引用的图后 compile 返回 422 聚合中文 REF_NODE_NOT_FOUND+REF_PATH_NOT_FOUND、NL 生成 200 且 paramWarnings 为 []、属性面板变量插入仅列 global.approval_limit 与 trigger 四个 context 键、注入 `{{ai_decision-1.bogus}}` 后面板错误清单与节点红色角标 tooltip 同步显示「输出中不存在该路径」、控制台无新增错误（除预期的 422 网络错误）。commits：8606aa3（docs 契约立项）/3582bf5（① schema 白名单+发现投影）/896c124（②③ 前端 scope+L2）/23eeadc（fix：loop 自身条件可引用 own index）/b20c86a（④ 编译期复查+I20）/0e1fbbd（paramWarnings 契约）/34d5cb5（⑤ NL 尽力校验+U34）。

3. **feat(iam/api/frontend): Phase 2 收口项——多租户权限 v1（端点级 RBAC + 用户数据按租户全分区 + 登录会话，2026-09-16，经 PR #16 合并 main）**——契约写入 04 新增 §5.14 多租户与权限 v1 权威 blockquote（§9.4 与 §二 5-7 愿景条款加注 v1 以 §5.14 为准、组件级权限/跨租户共享/审计随 D20/D3/Phase 3），03 新增 `identity_session` 契约并在六个分区资源概览补「tenant 由 token 推断不进资源 JSON」，06 新增 §6.12 认证鉴权运行时，09 目录树/模块映射新增 `iam/`（待定项 13 收口包名），10 §4 ADR T15（进程内不透明 token vs JWT、端点白名单 vs 节点级、每租户 store 实例注册表 vs tenant_id 透传、明文种子 vs 哈希依赖、无平台超管 vs platform_admin；10→02→09 三处同步），12 §3.10 内部接口 + REST 表 auth 三行与全表鉴权分档，13 新增 U31/I19 + §9 浏览器回填，14 D7 改 v1 落码并留边界注记（D28 同步），17 补 D7 认证界面 i18n 契约与错误码债。四项用户拍板：① 种子账号 + 进程内 token 登录（`POST /api/auth/login` → `sess-`+uuid4.hex，Bearer 头，401 回登录，零新依赖，重启即失；真实账号体系随 11 S1/D22）；② 三角色端点级 RBAC（viewer=全部 GET 只读 + POST feedback；operator 加图 CRUD/编译/运行/流式/NL/录制写/审批决策/调试放行/告警确认关闭；admin 加监控规则 PUT、demo reset、feedback 列表；默认拒绝白名单制）；③ 用户数据全租户分区（graphs/recordings/feedback/messages/approvals/debug/monitoring 每租户独立 store 实例、id 各从 1 起；templates/adapter 发现/demo shop+mock/demo SQLite 种子/出向 HttpApiClient·DatabaseClient 为全局共享基础设施；跨租户对象 404 不泄漏存在性、角色不足 403、未登录 401）；④ 无平台超管，t1「演示企业 A」/t2「演示企业 B」两个平级租户完全隔离。新包 `src/atlas/iam/`：`principals.py`（Role/Principal/SEED_TENANTS/SEED_USERS/authenticate/can 纯函数）、`sessions.py`（SessionStore issue/principal_for_token/revoke，单锁）、`registry.py`（TenantServices 七字段 bundle + TenantRegistry 惰性创建 + reset_tenant：graph/broker/debug/monitoring/message 清空、规则回默认计数器保留、录制反馈不动）、`deps.py`（get_principal/require(*caps)/services_for）。api/main：模块单例改由 TenantRegistry 持有，全部业务端点加鉴权依赖，store/broker/monitoring 一律按 principal 取租户实例；sync /run 与 SSE worker 显式透传 services bundle（含 `_runtime_registry` 克隆全局适配器只换租户 MessageHarnessAdapter），审批/调试 uuid token 绑定所属租户 broker；新增 login/me/logout 三端点；`/api/demo/reset` 改 admin+本租户范围并保留全局 demo shop/SQLite 重建；demo 模拟系统（/api/demo/**、/demo/shop、X-Demo-Token）、health、静态托管不加平台鉴权。`tests/conftest.py` autouse fixture 为既有模块级 TestClient 注入 t1 admin token（358 旧用例零语义改动）。前端：`lib/auth.ts`（Principal/角色矩阵/中文角色名/localStorage 会话，3 vitest）、apiClient 统一 Bearer + 401 清会话发 `atlas:unauthorized` 事件、pages/Login.tsx（antd 卡片表单 + 中文错误 Alert + 四种子账号提示）、App 启动恢复会话/监听登出事件、三页 Header UserBadge（租户·显示名·角色 Tag·退出登录）、Monitoring 规则卡仅 admin 与操作列按角色、Editor viewer 隐藏全部写按钮（从模板新建/导出/反馈保留）。测试：后端 **377 passed/8 skipped**（基线 358：+test_iam.py U31 十七例 +test_api_auth.py I19 十一例；既有引用 `_monitoring`/`_approval_broker` 模块单例处改走 t1 租户 bundle），前端 vitest **64（10 文件，+5）**，oxlint/tsc/build 绿；浏览器对真实 :8000+:5174 实测八场景——未登录拦截/坏密码中文错误/admin-a 登录硬刷新保持；A 租户建图运行+message/send 真实投递可见+失败运行产生 open 告警+监控页 4 runs/50%/2 alerts；admin-b 图/消息/告警/运行全空且直访 graph-1 → 404、阈值独立；viewer-a PUT 规则/建图/reset 全 403、GET 200、feedback 201 但列表 403、写按钮不渲染；operator-a reset/规则/feedback 列表 403、告警确认→已确认/关闭→已关闭、录制 rec-1 201；admin-a reset 后图/消息/运行/告警清零、阈值回默认 3、rec-1 与 feedback-1 保留；登出清存储并回登录页、裸 /api/graphs 401；控制台零错误（修复自引入的登录页底色硬编码 token 违规与 antd6 Space direction 弃用）。commits：7cbd509（docs 契约）/3620545（iam 包+U31）/64d34b3（API 鉴权分区+I19）/7a2ed14（前端登录/会话/角色 UI）/b0bf5d4+5660718（登录页 token/antd6 修正）/2759a13（17 文档补认证界面契约）。

4. **feat(monitoring/api/frontend): Phase 2 能力项——基础监控告警 v1（运行/节点指标 + 站内告警中心，2026-09-15，经 PR #15 合并 main）**——契约写入 04 新增 §5.13 权威 blockquote（§9.3 三条加注 v1 以 §5.13 为准、日志中心/可视化回放正式形态随 D11），03 新增 `monitoring` 索引（总览行+RunRecord/metrics 投影/RuleConfig/Alert 字段概览），06 新增 §6.11 监控运行时，09 目录树/模块映射，10 §4 ADR T14（进程内 ring+同步评估而非 OTel/Prom 拉模型、API 层端点包装零节点分支而非 loader 内埋点、内置阈值规则而非表达式 DSL、站内告警中心而非 message/webhook 出口，无新依赖；10→02→09 三处同步），12 §3.9 内部接口+REST 六行（reset 行补清空监控运行告警并恢复默认规则），13 新增 U30/I18 + §9 浏览器回填，14 新增 D28。四项用户拍板：① 采集运行级+节点级（每 run 记图/模式/状态/起止/耗时/逐节点成败与错误，聚合计数/成功率/p50/p95/节点失败 Top，不到适配器调用级）；② 站内告警中心（进程内库+独立前端监控页+未处理徽标+确认 acknowledge/关闭 resolve，不接 message 适配器/webhook/IM）；③ 内置四类规则阈值可调（run_error critical、node_failed warning、consecutive_failures 默认 3 critical、failure_rate 默认近 20 次/≥5 样本/≥50% critical；同 (rule_id, graph_id) 最新非 resolved 告警合并 count++/刷新 last_seen/last_run_id，resolved 后新发独立告警）；④ 独立 /monitoring 页（App.tsx 手工 page union 加 monitoring，Dashboard 加入口，Editor 不动）。v1 语义：健康=status completed 且无失败节点，节点失败两形状 `output.status=="failed"`（parallel/subgraph fail-safe 折叠）或 `output.result.status=="FAILED"`（ActionResult 数据化失败），未捕获异常冒泡=run error（sync 500/SSE error 帧）；只统计 sync /run 与非 debug /run/stream 两个真实运行入口，录制回放（graph_id replay-N 直调 run_graph）、subgraph 重入（emit=None）、debug 运行不记录；ring deque(maxlen=200)、单锁同步评估无后台线程、reset 清空运行/告警/streak 并恢复默认规则（不重置计数器）、重启即失；零新依赖、不碰 Graph/EdgeDSL version 1 与 node schema。新包 `src/atlas/monitoring/`：`records.py` RunRecord/NodeResult/MonitoringStore、`alerts.py` RuleConfig/默认规则/validate_rules 中文 422/evaluate_rules、`metrics.py` extract_node_results/percentile nearest-rank/is_healthy/summarize。api/main 模块单例 `_monitoring`：sync /run 包装记 completed/error（error 记录后原样 raise 保持 500），SSE worker 非 debug 时 recording_emit 收 node_end（error 帧用已采集 outputs 记部分节点，DebugStopped 不记）；`GET /api/monitoring/metrics|runs|rules`、`PUT /api/monitoring/rules`、`GET /api/alerts?status=`、`POST /api/alerts/{id}/acknowledge|resolve`（未知 404、重复迁移 409、参数 422，中文 detail）。前端 pages/Monitoring.tsx（指标六卡、告警表+状态过滤+确认/关闭、四 Switch+阈值 InputNumber 保存、最近运行表图过滤与逐节点/错误展开、5s 轮询）、lib/monitoring.ts 纯映射（runHealth/formatDuration/ruleLabel/状态色，3 vitest）、Dashboard「监控告警」入口；规避 antd6 弃用（Statistic valueStyle→styles.content、InputNumber addonAfter→suffix）。测试：后端 **358 passed/8 skipped**（基线 329：+test_monitoring.py U30 二十例 +Demo API I18 九例），前端 vitest **59（9 文件，+3）**，oxlint/tsc/build 绿；浏览器对真实 :8000+:5174 实测八场景——空态、refund-auto 健康运行指标全绿（12345 种子单）、sql-query-notify 三连败→node_failed warning 合并 count=3 与 consecutive_failures critical、指标卡成功率/p50/p95/节点失败 Top、运行行展开错误与逐节点表、确认/关闭后按钮禁用与状态 Tag、阈值保存即时生效与 reset 恢复默认 3、debug 运行/停止均不入监控，控制台零错误。commits：2362fdf（docs 契约）/c474a80（监控核心+U30）/c721f47（API+I18）/91f6bcc（前端）/8551f90（antd6 弃用属性修正）。

5. **feat(debug/api/frontend): Phase 2 能力项——单步调试与断点 v1（逐步/节点断点/条件断点/变量只读，2026-09-15，经 PR #14 合并 main）**——契约写入 04 新增 §5.12 权威 blockquote（§三 8/9 加注 v1 以 §5.12 为准、变量变化历史不在 v1），03 新增 `debug_session` 索引（总览行+debug 入参/paused/stopped/resume 字段概览），06 新增 §6.10 调试运行时，09 目录树/模块映射/待定项 11 收口，10 §4 ADR T13（进程内 Event broker 而非 LangGraph interrupt、会话级断点而非 node_schema 持久化位、统一执行器钩子而非节点类型分支、变量只读而非改写续跑，无新依赖；10→02→09 三处同步），12 §3.8 内部接口+REST（POST /api/debug/{token}/resume、GET /api/debug、reset 释放），13 新增 U29/I17 + §9 浏览器回填，14 新增 D27。三项用户拍板：① 会话级断点——断点存前端编辑器状态随请求传入，不进 Graph JSON（EdgeDSL/Graph version 1 不变、node_schema `breakpoint` 预留位不启用），刷新即失，持久化缓做 D27；② 变量只查看——paused 帧下发 globals+outputs 深拷贝只读快照，改写续跑缓做 D27；③ 单个「调试」入口——Header「调试」启动即 step 模式（首节点前暂停），工具条下一步(step)/继续(continue)/停止(stop)；continue 关逐节点后仅断点（含条件）命中再停。新包 `src/atlas/debug/`：`sessions.py` DebugStopped/DebugSession（dbg-uuid、step_mode 初始 True、cancelled、条件错误记录、per-session `_gate` 锁串行化并行暂停、首决生效 Event、深拷贝快照）/DebuggerBroker（create/get_session/list_pending/reset，reset 按 stop 放行防线程悬挂）；`controller.py` DebugController.before_node 在 node_start 之后节点逻辑之前单点判定 step/breakpoint/condition，条件复用 graph.conditions.evaluate_expression 同形 context，ConditionEvalError fail-safe 不命中。loader 唯一包装点 `_make_executor.execute` 接钩子（零节点类型分支）；human_approval 调试路径先发普通 node_start、暂停、注册审批后再发携 approval 的第二个 node_start（审批 Modal 放行后照常）；`_execute_subgraph` 重入不传控制器（子图整段执行）；合成 `__join__` 不经执行器天然不暂停。api/main：/run/stream 带 debug 建会话（未知节点/坏表达式/坏 breakpoints 聚合中文 422），worker 捕 DebugStopped 发 `event: stopped` 哨兵帧后正常结束流（无 result）；同步 /run 带 debug 422 引导流式；reset 接线 `_debug_broker.reset()`；录制回放不接控制器。前端：editorStore breakpoints 会话状态（toggle/表达式/清空，loadGraph 与删节点联动，不进 serializer）；AtlasNode 左侧断点圆点（hover 显现、启用常亮红点、条件白心、stopPropagation 不触发选中/拖拽）+ 琥珀色 paused 描边与「已暂停」标；PropertyPanel「调试」区块（Switch+条件 Input 复用 lib/conditions validateExpression 实时中文校验与 fail-safe 提示）；apiClient streamRun 增 debug 入参与 paused/stopped 分流（result 判 `id&&outputs`，paused 无 id 不误判；stopped 抛 DebugRunStoppedError 区别于请求失败）+resumeDebug；Editor 悬浮调试工具条（暂停节点+原因 Tag、三按钮逐按钮 loading 且下一 paused 到达复位、变量过滤 Input、globals/outputs 两个只读 JSON 快照块）。测试：后端 **329 passed/8 skipped**（基线 311：+test_debugger.py U29 十一例 +Demo API I17 七例——I17 按 TestClient portal 语义以独立线程轮询 GET /api/debug 驱动 resume），前端 vitest **56（8 文件，+4：store 断点 reducer 2、apiClient SSE 分流 2）**，oxlint/build 绿；浏览器对真实 :8000+:5174 实测六场景——sql-query-notify 启动即停 trigger-1（单步）并连续下一步 trigger→query→condition→notify-no 逐帧推进、快照含上游产出、result 后工具条关闭；条件断点 amount>100 真路「继续」停 condition-1（条件）、>9999 假路直达结束、清空表达式 reason=断点，变量过滤浅过滤；approval-timeout-reject 暂停先于审批 Modal（本轮 10s 超时自动拒绝走 rejected-msg）；停止收 stopped 帧无 result 无错误 Alert 按钮复位；红点 toggle/白心/属性面板坏表达式中文报错/硬刷新断点清空；暂停中 reset 释放会话（GET /api/debug 即时为空、工具条关闭）随后普通运行正常完成；控制台零错误。commits：2d7fa28（docs 契约）/6693023（debug 核心+U29）/7e5a23a（API 帧+resume+I17）/5e13cdd（允许空 breakpoints 纯单步）/8d5bdd7（前端）/ecb9605（resume 按钮复位修正）。

6. **feat(recording/api/frontend): Phase 2 能力项——操作录制 v1（运行录制 + 回放比对，2026-09-15，经 PR #13 合并 main）**——契约写入 04 新增 §5.11 权威 blockquote，03 新增 `recording_case` 索引（总览行+用例/列表投影/replay 响应字段概览），06 新增 §6.9 无独立运行时，09 目录树/模块映射/待定项 10 收口，10 §4 ADR T12（录制事件客户端组装 vs 服务端代跑、快照冻结 vs 活引用、归一化深等 vs LLM 评判），12 §3.7 内部接口+REST 五行（reset 行补注用例不清除），13 新增 U28/I16 并标注 R1 由本项满足、R3 经预置决策满足，14 新增 D26（批量回放/跨版本对比/影子模式/Mock 响应/用例编辑参数化/subgraph 快照内联）并对 D22 补注浏览器操作录制仍缓做。三项用户拍板：① v1 = 一次真实 Graph 运行录为回归用例（入参、逐节点终态产出、终态），一键回放做操作序列+逐节点变量比对，非浏览器插件/Playwright codegen（D22）非 07 行为挖掘；② 录制时 Graph JSON 原样冻结进用例，回放恒跑冻结快照不随后续编辑漂移，跨版本 diff 缓做 D26；③ 用例是测试资产（同 /api/feedback）`POST /api/demo/reset` 不清除，进程内存储重启清空（持久化随 11 S1）。新包 `src/atlas/recording/`：`cases.py` pydantic RecordStep/RecordingCase/RecordingCreateRequest（name 1-100、steps ≥1、output 任意 JSON）+ RecordingStore（rec-N 计数，add/list/get/delete，reset 不接线）；`replay.py` 纯函数 normalize/preset_approvals/dedupe_steps/compare/collect_steps——回放先从 baseline human_approval 步骤抽 `{node_id: decision}` 合并进 inputs.approvals（resolvedBy="input" 秒回不挂起），parse_graph 冻结快照后走标准 run_graph 收集 node_end；比对先序列对齐（多余/缺失节点=分支漂移）再逐节点归一化深等，差异给顶层 diff_keys。**归一化五规则**：递归丢 `token`/`sent_at`；同层含 sent_at 的消息记录丢 uuid `id`；http/request 的 result.headers 丢 `date`；human_approval 产出丢 `resolvedBy`；trigger 产出丢 `context.payload.approvals`——后两条是浏览器首轮回放实测暴露（回放预置决策经 trigger 载荷回显、决策来源 input 与录制时 timeout/human 不同，均属运行时来源非业务结果），补单测后以独立 fix 提交 42a82f3 收口并同步 04/06/13。API 五端点：POST/GET 列表（投影无 graph/steps）/GET {id}/DELETE/POST {id}/replay（图未知 404、steps 形状 422、回放异常折叠 replay_status=failed/matches=false 不抛 500）。前端：apiClient 录制类型与四函数；新纯逻辑 `lib/recordings.ts` `toSteps(events)`（node_end 按 node_id 保末并重排位置，parallel 双发/loop 重访安全，配 3 个 vitest）；Editor Header「录制与回放」Button+720 宽 Modal——用例名 Input（默认 `录制 <时间>`）+「录制当前画布一次运行」复用唯一保存→/run/stream 路径（事件在既有 SSE 回调收集，审批 Modal/SSE 日志照常），卡片列表（名称/状态 Tag/节点·步骤数/时间）、「回放」行内展开整体匹配 Tag+基线→回放终态+逐节点 ✓/✗ 差异键、Popconfirm 危险「删除」。测试：后端 **311 passed/8 skipped**（基线 283：+test_recording.py U28 二十三例（归一化五规则/预置抽取/序列与篡改比对/保末去重/store CRUD）+Demo API I16 五例（录制列表/回放匹配/篡改不匹配/reset 保留/subgraph 缺失折叠）），前端 vitest **52（7 文件）**、oxlint/tsc/build 绿；浏览器对真实 :8000+:5174 实测 approval-timeout-reject 录制（4 节点·3 步骤·completed）→回放秒回三节点全匹配→reset 清 GraphStore 后用例仍在且回放匹配→删除后列表空，硬重载后控制台零错误。commits：4f7af04（docs 契约）/857924f（recording 包+U28）/7282cb2（API+I16）/19b39ee（前端 Modal+lib 测试）/42a82f3（归一化补两规则+04/06/13 同步）。

7. **feat(template/api/frontend/llm): Phase 2 能力项——流程模板库 v1（5 个内置只读模板，2026-09-15，经 PR #12 合并 main）**——契约写入 04 §5.10 权威 blockquote，03 新增 `template_catalog` 索引（总览行+字段概览），06 §6.8 无独立运行时，09 目录树/模块映射/待定项 9 收口，10 §4 ADR T11（代码版本化只读 Python 常量 vs DB 表/远程市场），12 §3.6 内部接口+REST 两行，13 U27/I15，14 新增 D25 并对 D13/D21 加触发注记。新包 `src/atlas/template/`：`graphs.py` 5 个模板图（helper `_node/_edge`，显式 retry/坐标）、`catalog.py` pydantic `TemplateMeta{id,name,description,tags,graph}` + `TEMPLATES` 五元组 + `list_templates()/get_template(id)`、`__init__.py` 导出。三项用户拍板：① 5 个模板卡 5-10 区间下限，全部只用已注册 demo 适配器零配置可跑——`refund-auto`（退款 golden 三节点，**从 llm/nl_generate.py 原样迁入 template/graphs.py，NL 兜底改 import，U27 固化单一事实源**）、`http-orders-branch`（http/request GET 绝对 URL `http://localhost:8000/api/demo/mock/orders`+X-Demo-Token → condition `{{http-1.result.status}} == 200` → 成功/失败两个 message/send）、`sql-query-notify`（database/query SQL `:min` 绑定参数由 run 输入 min_amount 插值为数字 → condition `{{query-1.result.row_count}} > 0` → 两个 message/send）、`sql-approval-write`（human_approval 3600s/onTimeout reject → approvedTarget database/execute UPDATE / rejectedTarget message/send，运行输入 order_id）、`approval-timeout-reject`（timeoutSeconds=10 reject → 双 message/send，正文引 `{{approval-1.decision}}`）；② 只读内置目录——Python 常量随代码发布，无 DB 表、无用户 CRUD、不受 /api/demo/reset 影响，REST 仅 `GET /api/templates`（`{items:[{id,name,description,tags,node_count}]}` 列表投影不含 graph）与 `GET /api/templates/{id}`（完整元数据含 graph，未知 id 404，沿用 graphs/{id} HTTPException 模式）；③ 「从模板新建」=整画布替换——前端复用 editorStore `loadGraph()`（deserializeGraph 整体替换，节点 id/config/边/`{{node-id.path}}` 引用原样不重映射，语义同 NL 草稿载入），保存后成为普通 graph-N 与模板再无关联。前端：apiClient 加 `TemplateSummary/TemplateDetail` 类型与 `listTemplates/getTemplate`，Editor Header 加「从模板新建」Button+Modal（卡片列表名称/Tags/节点数/描述、顶部 Alert 替换警示、每项「使用此模板」，打开拉列表、应用拉详情+loadGraph+appendLog）；规避 antd v6.6.3 两项弃用（`List` 组件将移除改用原生卡片布局、`Space direction` 改名 `orientation`）。测试：后端 **283 passed/8 skipped**（基线 273：+tests/test_templates.py U27 六例（id 唯一 kebab/访问器/元数据/逐图 parse+validate==[]/工具名⊿demo 九工具/退款图单一事实源）+Demo API I15 四例（列表形状无 graph/detail 含 graph/404/五模板保存+编译/reset 不变）），前端 vitest 49 不变、oxlint/build 绿；浏览器对真实 :8000+:5174 实测——Modal 列出 5 模板（节点数 3/5/5/4/4）与警示、加载 sql-query-notify 画布 5 节点 4 边（边标签「有大额订单」「默认」）且 query-1 属性面板工具/绑定参数 JSON 完整校验通过、加载 sql-approval-write 双出边「通过/拒绝」且 approval-1 面板 3600/超时拒绝/write-1·reject-msg 双目标完整；API 链路五模板全部保存+编译 200；实跑 sql-query-notify inputs `{"min_amount":1000}` 走「有大额订单」→ notify-yes SUCCESS（row_count=1）、approval-timeout-reject 无人操作 10.0s 后 decision=rejected/resolvedBy=timeout → rejected-msg SUCCESS、http-orders-branch HTTP 200 → notify-ok SUCCESS；/api/demo/messages 三条记录、reset 后模板仍 5 个消息清空；控制台零错误零告警。commits：8f3238b（docs 契约）/a7368bd（template 包+迁移+U27）/fc844da（API+I15）/c7e35c9（前端 Modal）/9da499d+dffd7e3（antd6 弃用修正）。

8. **feat(database/message/graph/api/frontend/llm): Phase 2 适配器第二、三项——DB 适配器（通用 SQL）与消息适配器（进程内消息服务，2026-09-15，经 PR #11 合并 main）**——契约写入 04 §4.7/§4.8 两个权威 blockquote，03 新增 `db_sql_params`/`message_send_params` 索引，06 §6.7 运行时，09 目录树/模块映射/待定项 8 收口，10 §4 ADR T10，12 §3.5 内部接口与 REST（GET /api/demo/messages、reset 联动），13 U26/I12-I14，14 新增 D23（DB：JDBC/ODBC/数仓驱动、只读账号、SQL 审查、schema 表单、审计分页）与 D24（消息：SMTP/IM/短信/webhook、OAuth、模板、reply、入站、限流重试）。新包 `src/atlas/database/`（adapter_id/type=`database`，`database/query` read·幂等返 `{columns,rows,row_count,truncated}`，limit 1-1000 缺省 500；`database/execute` write 提交事务返 `{rowcount}`，DDL 允许；service+adapter 两层同 httpapi）与 `src/atlas/message/`（adapter_id/type=`message`，单工具 `message/send` write 非幂等，`{channel,to,subject,body}` 全必填，to 字符串或 ≤20 地址数组，channel=email 须含 `@`，输出完整消息记录含 uuid 与 sent_at）。四项用户拍板：① 双能力直接映射 READ/WRITE 权限门；② 适配器专用 `ATLAS_DATABASE_URL`（scheme 白名单 postgresql+psycopg/sqlite，自建 engine 工厂**不复用** memory/database.py 锁 PG 工厂，与平台 DATABASE_URL 物理隔离），未配置且未注入 client 时适配器 fail-closed `DB_NOT_CONFIGURED`（不静默回退），api/main 与 build_demo_registry 显式注入 StaticPool 单连接内存 SQLite（seed `orders(order_id,reason,amount,status)` 两笔与 mock 订单同形），SQLAlchemyError→`DB_SQL_ERROR`（URL 经 `hide_password` 脱敏），PG 查询连接 `postgresql_readonly`+rollback，reset 仅 reseed demo 不触碰显式配置的外部 PG；③ 消息 v1 仅进程内 sink，记录即成功零真实投递，`GET /api/demo/messages` 查看、重启/reset 清空；④ 模板复用既有 `{{路径}}` 插值不做独立模板系统。loader `_execute_tool` 通用 JSON 通道由 http 单支扩为显式集合 `GENERIC_JSON_ADAPTERS={http,database,message}`（坏 JSON/非对象 INVALID_PARAMETER 不变），shop 仍硬编码。前端零功能改动（选择器由 /api/adapters 驱动自动出现分组与权限标记），ToolCallConfig 参数占位符扩为 http/database-query/database-execute/message 四类 JSON 示例；NL prompt 广告三工具并强调绑定参数与工具禁捏造。测试：后端 **273 passed/8 skipped**（基线 209：+database 35 +message 21 +loader 4 +Demo API 4），前端 vitest 49/6 文件不变，lint/build 绿；浏览器对真实 :8000+:5174 实测选择器 database（query read·幂等/execute write）、message（send write）分组与四类占位符、query 返两笔订单、execute INSERT 后三行且 reset 复原、send 插值消息在 /api/demo/messages 可见且 reset 清空、坏 JSON FAILED INVALID_PARAMETER、控制台零应用错误。commits：b24c9c5（docs 契约）/b158f95（database）/e16dda5（message）/042ed60（loader 通用集合）/f899cda（api 单例+messages 端点+.env.example）/3ecbc72（frontend 占位符）/4e03752（LLM prompt）。

9. **feat(httpapi/graph/api/frontend/llm): Phase 2 适配器首项——API 适配器（通用 HTTP 请求，进程内 httpx，2026-09-15）**——契约写入 04 §4.6 权威 blockquote（03 adapter/tool 索引指向、06 §6.6 运行时语义、09 模块映射+待定项 7、10 §4 ADR、12 mock 端点与内部接口、13 U25/I9-I11）；09 待定项 7 收口——FastAPI 服务端已占 `api/` 包名，新包定名 `src/atlas/httpapi/`（adapter_id=`http`、adapter_type=`api`、单工具 `http/request`）。四项用户拍板决策：① v1 只做单一通用能力——tool_call 节点 config.params 经 `{{路径}}` 插值后为 JSON 对象 `{method(GET/POST/PUT/PATCH/DELETE 默认GET),url(必填),headers,body(dict→JSON/string→原样),timeout(默认30)}`，OpenAPI/Swagger 导入、Schema 表单、OAuth、操作录制/NL 生成适配器缓做 14 D22；② 混合连接配置——env/注册注入 `ATLAS_HTTPAPI_BASE_URL`/`ATLAS_HTTPAPI_HEADERS`（JSON 对象）/`ATLAS_HTTPAPI_TOKEN`（Bearer，显式 header 优先），相对 URL 拼 base、绝对 URL 直连，节点 headers 与默认 headers 键级合并且节点优先；v1 无加密存储，凭证加密与秘密变量（04 §6.2）缓做 D22；③ 结果语义——任何 HTTP 响应（含 4xx/5xx）均 SUCCESS，输出 `{status:<int>,headers:<dict>,body}`（body 优先 JSON 解析失败给 text），下游 condition 判 `{{tool-x.result.status}}`；仅传输层失败 FAILED HTTP_TIMEOUT/HTTP_CONNECT_ERROR，坏 method/缺 url/坏 headers/坏 timeout/坏 body → MISSING_PARAMETER/INVALID_PARAMETER；request 能力挂 WRITE/非幂等；v1 无重试（retry_policy 未定型）、**无 SSRF 防护**（04 §4.6 与 D22 标注风险）；④ 内置 mock 实测——FastAPI 新增 `GET /api/demo/mock/orders`（需 X-Demo-Token: demo-token，否则 401 JSON，返两笔演示订单）与 `POST /api/demo/mock/orders/{id}/receipt`（body 回显 + received:true），无状态 reset 不动；单测全部 httpx MockTransport（同步 Client 不能驱 ASGITransport，Demo API 用例在临时 127.0.0.1 端口起真实 uvicorn daemon 线程、httpx 就绪探测后打 loopback，零外部外呼）。`graph/loader.py` `_execute_tool` 在 shop 硬编码分派前新增 http 通用 JSON 通道（坏 JSON/非对象 → FAILED INVALID_PARAMETER），`build_demo_registry()` 与 api/main.py 两处注册全权限单例；observe() 返回 baseUrl 与 last_request 摘要。**连带修复**：`registry.list_adapters` 列举前为进程内适配器统一续心跳——常驻单例注册超 30s TTL 被误判 unhealthy，前端工具选择器因此只显示「当前值（未注册）」；TTL 置灰保留给远程注册（D6）。前端：`apiClient.listAdapters()` 裸数组类型、新纯逻辑 `lib/adapters.ts`（按健康适配器分组、value=`id/tool`、label 带权限/幂等标记）、ToolCallConfig 从 PropertyPanel 抽为独立组件（Select showSearch 挂载 /api/adapters，发现失败回退 AutoComplete 手填，已保存图未注册工具补「当前值」分组，http 工具的 params 占位符改 JSON 示例）、Editor node_end 新增 action_status 日志分支（SUCCESS 带 HTTP 状态码、FAILED 带 code/message）；serializer/DSL 零改动。NL system prompt 广告已注册工具（必须取自 GET /api/adapters 禁捏造；http/request params 为 JSON 字符串、4xx/5xx 仍成功后续 condition 分支；枚举五个 shop/*）。测试：后端 209 passed/8 skipped（基线 173 +httpapi 24 +loader 5 +Demo API 5 +注册表心跳 1 +NL 1），前端 vitest 49/6 文件（+adapters 3），lint/build 全绿；浏览器对真实 :8000+:5174 实测选择器 shop/http 分组与权限标记、GET 200 两笔订单、POST `{{路径}}` 插值签收回显、无 token 401 但 SUCCESS（日志 HTTP 401）、坏 params FAILED INVALID_PARAMETER、控制台零应用错误。commits：5da9522（httpapi service+adapter+tests）/ee1a95e（loader http 通道）/d24cf9a（api 单例+mock 端点）/9e9ed64（registry 心跳修复）/29fe4b9（frontend 选择器+日志）/cba7e80（LLM prompt）。

10. **feat(graph/api/frontend/llm): Phase 2 第六项——子图节点 subgraph（引用已保存图，进程内嵌套执行，2026-09-14）**——契约写入 04 §5.7 权威 blockquote（原状态机/异常处理顺延 §5.8/§5.9；单节点 config `{graphId, inputs}`，**EdgeDSL 与 Graph version 1 仍不变**），至此 08 Phase 2 六类节点全部落地。两项用户拍板决策：① **v1 引用已保存图**——graphId 指向 GraphStore 已保存图，inputs 为「子图入参键 → 父图 `{{路径}}`/字面量」可选映射；版本钉版与手动升级+回归（04 §14）、子图市场/组织共享、跨运营体远程引用、嵌套可视化编辑器缓做 14 D21（触发：模板库/发布流或多租户）；② **单出边 + fail-safe**——恰好 1 条普通出边（同 wait，不做成功/失败双出口），子图运行期异常折叠为节点产出 `status:"failed"`+error，父图 run 仍 completed，下游可 condition 判 `{{subgraph-x.status}}`。运行时：`_execute_subgraph` 在节点执行器内**进程内重入 `run_graph`**（共享 registry/decision_client/approval_broker，emit=None 压制子图事件，子图 trace/outputs 收入产出，父图 recursion_limit 不变、子图 invoke 独立限额）；`compile_graph/run_graph` 新增 `graph_resolver` 注入参数，编译期 `_validate_subgraph_refs` 递归校验：引用不存在/直接自引用/跨图环（A→B→A）/**嵌套深度上限 3**（父=0）/子图内部非法（中文加前缀「子图 {graphId}：」）聚合 422；解析器未注入（纯库直跑）同样中文报错。API：GraphStore 新增 `list()`（id/node_count/updated_at）与 `GET /api/graphs`，compile/run/stream 三处注入 `_resolve_saved_graph`（缺失抛 KeyError→校验文案）。产出 `{mode:"subgraph",graphId,status,error?,outputs:<子图 outputs>,trace}`，trace `subgraph-x: graph-7 success (N nodes)`，下游引用 `{{subgraph-x.outputs.<子图节点id>.<键>}}`。子图内 human_approval：broker 共享故端点可用，但子图 node_start 不外泄，v1 仅能 run inputs 预置/超时（06 与 D21 注明）。前端：indigo6 第九色节点（`color-node-subgraph`）、SubgraphConfig（graphId Select 挂载拉列表显示「id（N 节点）」、空列表引导先保存子图、inputs 键值映射增删行 + 每行全宽变量插入 Select；实测 antd6 Space.Compact 包裹 Select 选中不触发 onChange，移出 Compact 解决）、默认单 Handle（画布/连线/删除清理零改动）、变量路径 `subgraph-x.outputs`、SSE 日志「子图完成：graphId（成功/失败：error）」。NL prompt 枚举八类→九类。测试：后端 173 单元（+DSL subgraph 5/loader 9/Demo API 3/NL 1，8 integration 跳过），前端 vitest 46（catalog 校验/variables，5 文件）；浏览器对真实后端实测选择器列 graph-1（2 节点）、映射增删/变量追加、未选 graphId 即时报错，真实保存父图 trigger→subgraph→tool_call 运行：入参 X-9 到达子图 trigger payload、父图工具渲染 `{{subgraph-1.outputs.child-trigger...}}`、trace `graph-1 success (2 nodes)`、子图事件不外泄、缺失引用编译 422。commits：96bd319（docs 契约）/f895500（DSL）/8b29259（loader）/4faecc7（API list+resolver）/6860bd3（frontend lib）/0a5f20b（frontend UI）/9e9ec17（LLM prompt）。

11. **feat(graph/collaboration/api/frontend/llm): Phase 2 第五项——人机协作节点 human_approval（进程内审批挂起，2026-09-14）**——契约写入 04 §5.6 权威 blockquote（原状态机/异常处理顺延 §5.7/§5.8；单节点 config `{summary, approver, timeoutSeconds, onTimeout, approvedTarget, rejectedTarget}`，**EdgeDSL 与 Graph version 1 仍不变**）。三项用户拍板决策：① v1 用**进程内审批信号**——新增 `collaboration/approvals.py` `ApprovalBroker`（uuid4 token + `threading.Event`，模块单例可注入；pending dict、首决生效、list_pending/reset），真正的 LangGraph interrupt/checkpointer 持久化中断-恢复、多实例、IM/邮件通知、动态审批人与角色权限缓做 14 D20（触发：11 S1 持久化就绪或真实审批业务上线，与 D19 同批中断模型）；② **超时自动决策**——timeoutSeconds 必填整数 10-3600，onTimeout approve|reject 默认 **reject**，超时沿拒绝分支继续、run 仍 completed（与并发人工作业竞速时人工胜出，broker `complete_timeout` 保证）；③ 固定双出口控制节点（loop 双目标先例），恰好 2 条出边对配通过/拒绝目标（互异/存在/非自环/不直连 END），可位于 condition 分支/loop 体内/parallel 区域，阻塞不产生 LangGraph 超步故 recursion_limit 不变。决策三来源：REST `POST /api/approvals/{token}/decision`（resolvedBy="human"，重复提交 409、未知 token 404、坏 decision 422）、run inputs 预置 `{"approvals":{"<node-id>":"approved"|"rejected"}}`（resolvedBy="input"，非交互秒过）、超时（resolvedBy="timeout"）；新增 `GET /api/approvals`（pending 调试列表），`POST /api/demo/reset` 按超时拒绝放行并清空 pending。node_start 在登记 broker 之后、阻塞之前发出，携带 `approval:{token,summary,approver,timeoutSeconds}`；产出 `{mode:"human_approval",decision,target,token,summary,approver,resolvedBy}`，trace `human-x: approved (human) → tool-y`，下游可引用 `{{human-x.decision}}`。**连带治理：`/run/stream` 由「先跑完再回放」改真流式**——run_graph 在后台 daemon 线程执行、事件经 `queue.Queue` 实时 yield（旧实现下节点阻塞等审批、浏览器收不到 node_start 会死锁），终帧 `event: result`，运行期异常新增 `event: error` 哨兵帧；同步 /run 不变。前端：slate900 深色节点（新增 `color-node-human`，避开既有七色）、HumanApprovalConfig（审批说明 TextArea+变量插入、审批人、超时 Space.Compact InputNumber 10-3600、超时策略 Radio、双目标 Select）、双 Handle（approved/rejected，标签 通过/拒绝）与边标签、删除清理、运行中弹 AntD Modal（同意/拒绝调决策端点，node_end 自动关窗去重，409/404 窗内中文提示，`mask.closable=false`）、SSE 日志「人工审批：通过/拒绝（人工/超时/预置）→ target」。NL prompt 枚举七类→八类。测试：后端 155 单元（+collaboration broker 7/DSL human 13/loader 5/Demo API 3，8 integration 跳过），前端 vitest 43（catalog 校验矩阵/variables decision/store 清理，5 文件）；浏览器对真实后端实测人工同意（仅通过侧执行）、人工拒绝（仅拒绝侧执行）、10s 超时自动拒绝且弹窗自动关闭、说明为空即时报错、GET /api/approvals pending 可见。commits：803bada/2b43705/75451e2/c70db00（docs 契约与 collaboration/DSL/loader）/0ff1a46（SSE 真流式+决策端点）/42d0ccc（frontend lib）/a96aa28（frontend UI）/4397135（LLM prompt）。

12. **feat(graph/frontend/llm): Phase 2 第四项——等待节点 wait（定时挂起，2026-09-14）**——契约写入 04 §5.5 权威 blockquote（单节点 config `{waitType:"duration", durationSeconds}`，**EdgeDSL 与 Graph version 1 仍不变**）。两项用户拍板决策：① v1 仅定时等待（执行器内同步 `time.sleep`，node_start 在 sleep 前发出，到点沿唯一普通出边继续），事件等待（外部信号/回调恢复，需 LangGraph interrupt/checkpointer）与动态/表达式时长一并缓做 14 D19（触发：真实事件驱动或分钟级长等待业务 + 11 S1 持久化就绪）；② 时长为整数常量 1-600 秒。运行时边界：SSE 同步生成器跑在 Starlette 线程池 worker，阻塞不卡事件循环但占用一个 worker，600s 即占用上限；等待不失败不重试。纯透传控制节点：恰好 1 条出边（目标存在、≠自身、不直连 END），无新增拓扑规则（可位于 condition 分支/loop 体内/parallel 区域），recursion_limit 公式与普通边装配零改动。产出 `{mode:"wait",waitType:"duration",durationSeconds}`，trace `wait-x: waited Ns`，下游可引用 `{{wait-x.durationSeconds}}`。DSL `_validate_wait_config`（waitType 非 duration——含 event 明确文案「事件等待暂不支持」——/非整/bool/越界/出边 0 或 2/自环/悬空中文聚合报错）。前端：橙色 orange6 节点（新增 `color-node-wait`；purple6 已属 AI 节点，不撞色）、WaitConfig（定时 Radio 可选/事件 Radio 禁用 tooltip「随持久化层开放」、InputNumber 1-600 precision 0 越界失焦钳制 + 空值即时中文报错，antd6 弃用 addonAfter 改 Space.Compact）、默认单 Handle（AtlasNode/FlowCanvas/editorStore 零改动）、变量路径 `wait-x.durationSeconds`、SSE 日志「等待完成：N 秒」。NL prompt 枚举六类→七类。测试：后端 127 单元（+DSL wait 12/loader 2/NL prompt 1，8 integration 跳过），前端 vitest 39（catalog wait 校验矩阵/variables，5 文件）；浏览器对真实后端实测 trigger→wait(1s)→tool_call 非流式运行真实 1s 墙钟（总 1.09s）、后继恰好执行、outputs 形状与 `waited 1s` trace 正确，面板 0/601/1.5 钳制为 1/600/2、空值报错，控制台零新增错误。落码时记载的「`/run/stream` 先跑完再回放」假流式局限已在第五项 human_approval 连带治理中改真流式（见上条）。commits：232ea16（docs 契约）/d072db5（DSL 校验）/132d785（loader sleep）/d45b0ae（frontend lib）/2f7c5b1（frontend UI）/29d027e（addonAfter 修正）/ff37311（LLM prompt）。

13. **feat(graph/frontend/llm): Phase 2 第三项——并行节点 parallel（扇出/汇聚网关，2026-09-14）**——契约写入 04 §5.4 权威 blockquote（单节点 config `{joinStrategy, branches:[{label,target}], joinTarget}`，**EdgeDSL 与 Graph version 1 仍不变**）。三项用户拍板决策：① v1 仅 `all_success`/`all_completed` 两策略，`any_success`（真 OR-join，需提前汇聚+取消分支，LangGraph 静态扇入不支持）缓做 14 D18（触发：真实竞速/先到先得业务）；② all_success 下分支 FAILED 走 **fail-safe 汇聚**——joinTarget 照常执行、parallel 产出 `status=failed`+失败分支错误、run 仍 completed，下游可 condition 判 `{{parallel-x.status}}`；③ 分支区域（branch targets BFS、遇 joinTarget/parallel 停止）内允许 condition/自包含 loop，禁嵌套 parallel/trigger/交叉/外泄，各入口必须可达 joinTarget。关键技术事实：spike 实测 LangGraph 普通多入边**不构成 barrier**——`GraphState.outputs` 改 `Annotated[dict, _merge_outputs]`（key 级合并）、`status` last-write、各执行器只回自身分片（横切五处执行器，零回归）；编译期注入合成网关 `__join__<parallel-id>`（冒号为 LangGraph 保留名），分支末端 retarget 网关，网关就绪计数 self-loop wait 超步（空 messages 写）等待不等长分支，齐后聚合再路由真 joinTarget；`__join__` 事件不外泄，汇聚完成时以 parallel 节点自身补发第二次 node_end（fork 时第一次为 running）。DSL `_validate_parallel_config`（2-10 分支/label 非空唯一/target 互异/出边恰好 N 条不直连 END/区域泄漏/交叉/嵌套/trigger/不可达 join 中文聚合报错）；聚合产出 `{mode,joinStrategy,status,branches:[{label,target,status,error}],result:{<入口节点id>:末端产出},joinTarget}`，trace `fork N branches → …`/`joined (strategy) …`。前端：品红 magenta6 节点、ParallelConfig（策略 Radio 中文说明/分支增删 2-10/汇聚目标 Select）、N source Handle（b{index}）与分支边标签、删除清理、变量路径 `parallel-x.status`、SSE 日志（并行启动 N 分支/汇聚全部成功/N 支失败 fail-safe 文案）。NL prompt 枚举补 parallel。测试：后端 112 单元（+DSL parallel 10/loader 3/退款 e2e 1/NL prompt 1，8 integration 跳过），前端 vitest 37（catalog/variables/store parallel 用例，5 文件）；浏览器对真实后端三场景实测——trigger→parallel→{短支/长支}→join 两支都执行且 join 恰好一次（params 渲染 `状态=success`，result 按入口 id 聚合）、长支配未注册适配器触发「1 个分支失败：长支（适配器未注册：bogus）（汇聚节点仍执行）」、空 label/重复 target/缺 joinTarget 属性面板三条即时报错，控制台零新增错误。commits：9bbadb6（frontend lib）/7d9ff89（gate SSE emit）/b27055e（frontend UI）/918831b（LLM prompt）。

14. **feat(graph/frontend/llm): Phase 2 第二项——循环节点 loop（条件循环 while，2026-09-14）**——契约写入 04 §5.3 权威 blockquote（config `{mode:"while", continueExpression, maxIterations(1-100, 默认10), bodyTarget, exitTarget}`，**EdgeDSL 与 Graph version 1 仍不变**）；两项用户拍板决策：① v1 仅 while，复用 condition 表达式白名单，循环变量只暴露 `index`（遍历循环/item/break-continue 缓做 14 D16/D17）；② 达最大次数 fail-safe 退出（正常路由 exitTarget、`exitReason=max_iterations`、运行仍 completed），表达式运行时异常同样 fail-safe（`expression_error`）。拓扑：loop 恰好两出边对配 body/exit，循环体 BFS 界定，回边（体→同 loop）是唯一合法环（DSL 临时摘除白名单回边后 DFS 环检测，调和 U7），体内禁嵌套 loop/trigger、禁泄漏 exitTarget、游离体分支拒绝，中文聚合报错。后端：`dsl.py` `_validate_loop_config`/`_loop_body_set`/`_validate_illegal_cycles`；`loader.py` 可重入 `_execute_loop`（重访覆盖 outputs、首轮播种 `{{loop-x.index}}`）、出边全 conditional、`recursion_limit=2N+2·ΣmaxIter·(bodySize+1)+10` 经 invoke config 传入；产出 `{mode,iterations,index,target,exitReason,expression_errors}`，trace 行 `continue (i/max) → body` / `exit (reason) after N → exit`。前端：循环节点面板（青色 cyan6）、LoopConfig（表达式实时校验/变量插入/最大次数/体·退出目标）、双 Handle（body/exit 纯视觉）、出边「循环体」「退出」标签、删除目标清理、SSE 日志中文化（继续第 N 轮；退出：条件不满足/达到最大次数/表达式异常）。NL prompt 枚举补 loop。测试：后端 97 单元（+DSL loop 8/loader 3/退款 e2e 1/NL prompt 1，8 integration 跳过），前端 vitest 34（catalog/variables/store loop 用例）；浏览器对真实后端三场景实测——条件收敛（体执行 2 轮→条件不满足退出）、恒真撞上限（3 轮→达到最大次数）、坏变量（0 轮→表达式异常），边标签/节点高亮/控制台无新增错误。

15. **feat(graph/frontend/llm): Phase 2 首项——条件分支节点 condition（2026-09-14）**——契约写入 04 §5.1（首版语法白名单）/§5.2（condition config 权威契约）：分支挂节点 config `{branches:[{label,expression,target}], defaultTarget}`，**EdgeDSL 与 Graph version 1 均不变**；branches 按序短路、default 必填、label/target 唯一、target 须存在且有出边、每条出边须被分支覆盖、不允许直连 END。后端：新增 `graph/conditions.py`（纯 stdlib 手写递归下降：`{{路径}}` 变量、比较 `> >= < <= == !=`、逻辑 `&& || !`、括号、数字/字符串/true/false/null；校验期中文报语法与纯字面量类型错误，运行时错误 fail-safe 走 defaultTarget，禁 eval 零新依赖）；`dsl.py` condition 图级校验 + 从 trigger 根做可达性 BFS；`loader.py` condition 出边全部走 `add_conditional_edges`（执行器只求值一次写 outputs，router 只读 target，规避同源普通/条件边双激活），产出 `{branch,target,evaluation,expression_errors}`，trace 行 `condition-x: branch=… → target`。前端：节点面板新增「条件分支」（金色）、ConditionConfig 属性面板（分支增删/表达式实时中文校验/插入 `{{变量}}`/目标节点 Select/默认分支）、节点多 source Handle（b{index}/default 纯视觉不序列化）、出边按 target 反查显示分支标签、删除目标节点自动清空引用并报错、运行 trace 显示分支与终态工具结果；`lib/conditions.ts` 与后端同构的 TS 校验（不做求值）。NL 生成 system prompt 枚举补 condition 与 config 形状。测试：后端 84 单元（+conditions 9/DSL 5/loader 4/e2e 3/NL prompt 1 等，8 integration 跳过），前端 vitest 32（conditions 4 等）；浏览器对真实后端实测 trigger→condition→两个 tool_call：订单 12346（¥5000）只执行转人工（human_review），12347（¥128）走默认分支自动退款（refunded），坏表达式面板中文报错，删除目标节点报「必须配置默认分支」。LLM 判断分支、函数库/算术缓做（14 D14/D15）。

16. **chore(bench): D9 性能基准脚本与首份基线（2026-09-14）**——新增 `scripts/benchmark.py`（纯 stdlib 零新依赖：每场景 10% warmup + 300 次采样，p50/p99 + 吞吐，输出 BENCHMARK.md Results 格式 Markdown 行；`DATABASE_URL` 存在时追加 DB ping）。场景：OODA `run_loop(max_steps=2)` 吞吐、退款 3 节点图 `compile_graph` 编译时延、12345 自动退款 `run_graph` 端到端时延（规则决策路径，`service.reset` 在计时外）、`shop/list_pending_refunds` Harness 调用开销。首份基线（commit 407812e，CPython 3.11.15 / macOS arm64）：OODA p50 2.05ms（≈488 loops/s）、编译 1.81ms、退款端到端 2.28ms、Harness 调用 0.002ms。BENCHMARK.md Scope 标注四项待接入补测（LLM 决策延迟随真实供应商、Redis/pgvector 记忆读写随 11 S1、并行扇出随 Phase 2 节点、长流程内存增长随递归引擎）；Known limits 说明本基线仅为进程内确定性回归锚点。09 目录树补 `scripts/`；08 Phase 1 记 D9 重启（零新依赖故无 ADR）。

17. **chore(ci): D10a GitHub Actions CI 质量门（2026-09-14）**——新增 `.github/workflows/ci.yml`：gitleaks 全历史密钥扫描（沿用根 `.gitleaks.toml`，关闭 PR 评论/产物上传，仅 contents:read）、后端 pytest（Python 3.11，pip 缓存；integration 标记默认跳过）、前端 oxlint + vitest + `pnpm build`（Node 22 / pnpm 10，frozen-lockfile）；push main/dev 与 PR 触发，同 ref 并发取消。D10 拆分为 D10a（CI，本次完成）/D10b（CD，仍缓做，触发条件：远程部署目标确定），ADR T8 落 10 文档 §4，08 Phase 1、09 待定项 3 注记、14 登记表同步。本地等价命令全绿：后端 62 passed/8 skipped、前端 vitest 23、`pnpm build` 通过（AntD 体积提示为已知非阻塞项）、gitleaks 55 commits 无泄漏；Actions 实跑结果待 dev 推送后验证。

18. **feat(llm/shop/graph/api/web): W9-W10 电商退款端到端 Demo（2026-09-13）**——新增 `llm/`（`decision.py` DecisionClient：LITELLM_MODEL 配置时经 LiteLLM 决策、JSON 解析失败 fail-safe 转人工；未配置时 RuleBasedDecisionClient 按 06 §9.2 黄金规则确定性兜底；`nl_generate.py` NL→Graph 草稿，LLM 优先退款模板兜底）与 `shop/`（`service.py` DemoShopService 五笔种子退款单 + 退款/转人工状态流转；`adapter.py` ShopHarnessAdapter 五能力 login/list_pending_refunds/execute_refund(financial)/request_human_approval/process_refund，按上游决策路由，结构化错误 + 权限门）。`graph/loader.py` 依赖注入 decision_client/registry/emit/trigger_payload，webhook 载荷经 run inputs 进入 trigger context.payload（同名键覆盖全局变量），node_start/node_end/run_end 事件回调。`api/main.py` 新增 /run/stream SSE、/nl/generate、/adapters、Demo 店铺登录/订单接口与 /demo/shop 控制台页面，共享 Demo 服务单例；dsl 顺带修复 triggerType schedule/cron 取值对齐前端。前端：退款单选择器、SSE 流式运行（节点脉冲/完成描边 + 调试台事件日志）、NL 生成弹窗载草稿、退款三节点种子图。测试：后端 58 单元（新增决策 6/店铺 8/NL 2/端到端 3/API 6/schedule 1）+ 店铺平台集成 1（opt-in），前端 vitest 23；浏览器实测 12345 破损→approve_refund→refunded、12346 主观→request_human_approval→human_review、NL 草稿 3 节点、控制台 demo/demo 登录拉单。文档 08/09/12/13 已同步。
4. **feat(graph/api): W7-W8 编译与运行（2026-09-13）**——`graph/dsl.py` 承接前端 Graph JSON（version 1）：pydantic GraphDSL/NodeDSL/EdgeDSL + 静态校验（版本、节点/连线 id 唯一、边端点存在、禁自环、三类节点分类型必填配置、未支持类型拒绝），错误一次性聚合为中文列表；`graph/loader.py` 将 DSL 编译为 LangGraph StateGraph（DSL 节点 1:1 成图节点，边原样装配，无前驱接 START、无后继接 END），`run_graph` 播种全局变量并在运行时做 04 §6.3 `{{路径}}` 插值（可读上游节点产出，缺失引用原样保留），三类节点执行器为确定性占位（LLM 随 LiteLLM、真实工具随适配器联动替换）。`api/main.py` FastAPI 落地：`GET /api/health`、`POST /api/graphs`、`GET /api/graphs/{id}`、`POST /api/graphs/{id}/compile`（返回拓扑/入口/终点）、`POST /api/graphs/{id}/run`（返回状态/产出/traces），进程内图存储（与 T4 同假设，持久化随 11 S1），校验失败统一 422。前端新增 `lib/apiClient.ts` 与编辑器"编译并运行"按钮（结果弹窗 + 422 中文错误 Alert）。测试新增 16 个后端用例（dsl 7/loader 5/API 4），默认 `pytest` 32 passed/7 skipped；浏览器对真实后端验证保存→编译→运行（`{{global.approval_limit}}`→500）与 422 错误路径。边界决策（graph 独立包、进程内存储、/run 端点）记 09 待定项 6，03 新增 graph_definition 契约索引，12 REST 表补 /run。
5. **feat(web): W5-W6 编辑器核心功能（2026-09-13）**——`lib/variables.ts`（04 §6.3 `{{路径}}` 语法：extractRefs/interpolate/resolvePath、标识符校验、全局变量+节点输出路径清单）、`lib/nodeCatalog.ts`（trigger/ai_decision/tool_call 三类目录、默认 config/retry、中文实时校验）、`lib/graphSerializer.ts`（03/04 §3.2 node_schema 形状的 Graph JSON，version 1，位置取整）；`store/editorStore.ts` 重写（addNodeAt 用 `nextId` 扫已有 id 防 React Flow id 碰撞、选中/配置/删除级联边/变量 CRUD/连线日志，种子审批示例 3 节点 + 1 变量）；`AtlasNode` 自定义节点（类型配色 + 错误角标）、`FlowCanvas` HTML5 拖拽落点（screenToFlowPosition）、`NodePanel` 拖拽面板、`VariablesPanel` 变量增删、`PropertyPanel` 分类型配置表单（含插入变量引用、retry 策略、实时错误清单）、Editor 加"导出 Graph JSON"预览弹窗。测试：vitest 5 新增（ADR 记 10 §4），4 个测试文件 20 用例全绿（变量/校验/序列化/store 碰撞与级联）；`pnpm build` 通过；浏览器人工验证拖入节点、校验角标联动、变量插入、导出 JSON（version 1、4 节点、2 变量、2 边），应用零控制台错误。修复：根 `.gitignore` 的 Python 规则 `lib/` 误伤 `frontend/src/lib/`，改为 `/lib/` 锚定根目录。4. **feat(web/harness): W3-W4 Harness Web 适配器（2026-09-13）**——`harness/base.py` 落地 06 §6.4 同构契约（HarnessAdapter ABC、ActionRequest/ActionResult/Capability/Observation、Permission 枚举 read/write/delete/financial），execute 模板方法做权限校验+审计（I8）；`harness/registry.py` 进程内注册/发现/心跳健康（04 §4.4）；`web/location.py` 三层定位（层1 选择器 3000ms 超时 + 选择器缓存、层2 视觉语义置信度严格 >0.7、层3 全图推理坐标、全失败兜底文案），层2/3 视觉组件以协议注入（模型接入前为桩）；`web/adapter.py` Playwright sync 适配器，工具集 navigate/click/type/screenshot，headless 受 `PLAYWRIGHT_HEADLESS` 控制。测试 17 个：契约/定位单元测试 13 个全绿，真实 Chromium 集成 4 个（I1/I2/I5 + type/observe）全绿；默认 `pytest` 16 passed/7 skipped，`ATLAS_RUN_INTEGRATION=1` 全量 23 passed。包边界决策（harness=契约/注册，web=Playwright 实现，不合并）已记 09 待定项 2 + 08 §7.2。5. **feat(memory): PostgreSQL + pgvector 连接层验证（2026-09-13）**——本地 Docker 跑 `pgvector/pgvector:pg16`（容器 `atlas-pg`，5432）；迁移 `db/migrations/001_enable_pgvector.sql` 启用 vector 0.8.6；`memory/settings.py`（环境变量单一读取点，`DATABASE_URL` 缺失 fail-closed）、`memory/database.py`（SQLAlchemy 引擎/会话工厂，强制 `postgresql+psycopg` 即 psycopg 3 驱动，`ping`/`pgvector_version` 探针）；`tests/test_database_integration.py` 3 个 integration 标记用例（ping、扩展版本、vector 类型写入+余弦距离排序），默认跳过，`ATLAS_RUN_INTEGRATION=1` + `DATABASE_URL` 开启，对容器全绿。`.env` 已本地创建（gitignored），`.env.example` 连接串同步为 psycopg 写法。6. **feat(web): 前端编辑器框架跑通（2026-09-13）**——`frontend/` 采用 Vite 8 + React 19 + TS 6 + `@xyflow/react` 12（替代弃用包 `react-flow-renderer`）+ Zustand 5 + AntD 6；页面含 Dashboard/Editor，组件含 FlowCanvas/NodePanel/PropertyPanel/DebugConsole，Zustand 管理 nodes/edges/选中节点/日志；`pnpm build` 通过，浏览器验证初始 3 节点、添加节点、属性编辑与日志，控制台零错误。dev 端口 5174，`/api` 代理 8000。7. **feat(engine): LangGraph 最小 OODA 循环跑通（2026-09-13）**——`engine/state.py`（LoopState 按 06 §6.1/12 §1.1 契约，messages/observations 加 add 归约器）、`engine/nodes.py`（observe/orient/decide/act/reflect 五节点 + 三条 conditional 路由，确定性占位、签名不偏离 12 §1.2）、`engine/loop.py`（StateGraph 装配，拓扑同 12 §1.3；`build_graph/initial_state/run_loop`）；`tests/test_engine_loop.py` 3 用例全绿；venv（Python 3.11.15）依赖安装与端到端 `run_loop` 验证通过。8. **chore: W1 工程骨架落地（2026-09-13）**——按 09 文档建 `src/atlas/{engine,harness,graph,nodes,memory,skills,collaboration,api,web}/` + `tests/` 包树（`__init__.py` 占位）；`pyproject.toml` 锁定 10 文档 §2 Demo 依赖（Python >=3.11，src layout，版本下限取当日 PyPI 稳定版），已用 Python 3.11 venv 验证可编辑安装；09 文档同步记录 `api/` 包位决策（FastAPI 入口，待定项 5）。9. **docs: 技术选型 T1-T5 收口（2026-09-13）**——10 文档 §3 待决策项全部转为正式 ADR（LiteLLM+商业 API / 评估层仅留接口 / 前端进骨架 / 进程内事件总线替代 NATS / FastAPI 实现网关接口），§4 补变更记录；02 选型总览、09 待定项与目录树（含 `frontend/`）、08 §7.2 决策注记三处同步；NATS/Go 网关缓做条目已在 14 文档 D5/D6。10. **docs: 移除 LICENSE（2026-09-13，用户指示）**——MIT 许可证不需要，LICENSE 文件删除，README/CONTRIBUTING 的 License 段落改为"暂无（未定）"，handoff 元文档索引与 CHANGELOG 同步清理。11. **docs: 补齐 agent-world 惯例规范文档（2026-09-13）**——根目录：`CONTRIBUTING.md`（贡献指南，Python/09 骨架适配）、`MIGRATION_CONVENTION.md`（DB 迁移规范，PostgreSQL 版）、`BENCHMARK.md`（性能基准记录表）、`.gitleaks.toml`（密钥扫描）、`.env.example`（环境变量模板，按 10 文档 Demo 栈）；docs 体系：`14-缓做事项登记表`、`15-环境与分支策略`、`16-反馈工作流`。索引已同步 00 文档地图与 README。12. **chore/docs: 仓库初始化并推送 GitHub private（2026-09-13）**——`git init -b main`；3 个原子提交：`6fc7728`（chore .gitignore）、`d3c63c6`（docs 规格文档 00-13，commit 原文为 atlas-docs）、`2413ab5`（docs 根元文档）；`gh repo create atlas --private` 创建并推送至 `https://github.com/bayernjf/atlas`（PRIVATE，默认分支 main）。13. **docs: 源文档切分完成并核对（2026-09-13）**——两份原始文档（《产品与总体方案.md》《技术实现设计.md》）按主题切分为 `docs/` 00-13 共 14 份 AI 导向文档；274 个章节标题全部映射、逐段覆盖率机器核对通过；源文档已删除（血统与治理见 00 文档）。14. **docs: 切分核对报告（2026-09-12）**——段落级最长公共子串覆盖率验证，无信息丢失。
## Quality gate（质量门）

- **文档侧**：00 文档"切分核对报告"已确认 274 章节全映射、逐段覆盖无丢失。改规格文档必须遵循 00 文档治理规则（唯一事实源 / 改内容流程 / Schema 契约防漂移）。
- **代码侧**：最小质量门已建立——GitHub Actions CI（`.github/workflows/ci.yml`，2026-09-14 落地；push main/dev 与 PR 触发）：gitleaks 全历史扫描 + 后端 pytest + 前端 oxlint/vitest/build；**PR #3-#19 与合并后 main 均已实跑全绿**（gitleaks 初版 403 经补 `pull-requests: read` 修复；#12-#16 对应模板库/录制/调试/监控/多租户五项能力，#17 为 `.codegraph/` 忽略规则，#18 为 docs/19 题述提案与 handoff 同步文档，#19 为设计态内核前置项，后两者 2026-09-16 合并）。本地后端 `.venv/bin/pytest`（当前 410 个单元用例：engine 3 + harness 契约 8 + 三层定位 6 + graph conditions 9 + graph DSL 61 + 编译器/condition/loop/parallel/wait/subgraph/human_approval/http/database/message 通道路由 40 + collaboration 审批 broker 7 + graphs API 4 + 决策 6 + 店铺 8 + NL 15（含 NL 草稿参数尽力校验 U34 七例）+ 退款端到端 8 + Demo API/mock/messages 50（含模板 I15 四例、录制 I16 五例、调试 I17 七例、监控 I18 九例）+ httpapi 通用 HTTP 适配器 24 + database 通用 SQL 适配器 35 + message 进程内消息适配器 21 + 内置模板目录 U27 六例 + 操作录制 U28 二十三例 + 调试会话/暂停状态机 U29 十一例 + 基础监控 U30 二十例 + iam 种子身份/会话/角色矩阵/租户注册表 U31 十七例 + API 鉴权与租户分区 I19 十一例 + L2 模板引用编译期复查 I20 九例；DB/浏览器/店铺 8 个 integration 用例默认跳过；DB/消息/模板/录制/调试/监控/鉴权/作用域新增用例全部 SQLite 内存/进程内/静态常量，零真实外呼），全量 `ATLAS_RUN_INTEGRATION=1`（+`DATABASE_URL`）含真实 Chromium 与店铺平台集成；前端 `cd frontend && pnpm test`（vitest，87 个单元用例，11 个文件）与 `pnpm build`（TS 检查 + Vite 构建）均通过（有 AntD 首包 >500kB 的体积提示，暂不阻塞）。后续按 13 测试用例清单分层：单元 → 集成 → 回放 → 端到端 → AI 评估，上线前过"四道门"。

## How to run

```bash
python3.11 -m venv .venv
.venv/bin/pip install -e ".[dev]"
.venv/bin/pytest                       # 单元测试（integration 标记默认跳过）
.venv/bin/python -c "from atlas.engine.loop import run_loop; print(run_loop('demo', max_steps=2)['status'])"

# FastAPI graphs 接口（W7-W8；前端 dev 经 /api 代理到此端口）
.venv/bin/uvicorn atlas.api.main:app --reload --port 8000

# PostgreSQL + pgvector（本地开发）
docker run -d --name atlas-pg -e POSTGRES_USER=atlas -e POSTGRES_PASSWORD=atlas \
  -e POSTGRES_DB=atlas -p 5432:5432 pgvector/pgvector:pg16
cp .env.example .env                   # 填 DATABASE_URL（本地开发库：atlas/atlas）
docker exec -i atlas-pg psql -U atlas -d atlas < db/migrations/001_enable_pgvector.sql
DATABASE_URL=postgresql+psycopg://atlas:atlas@localhost:5432/atlas \
  ATLAS_RUN_INTEGRATION=1 .venv/bin/pytest -m integration

# Playwright 浏览器（W3-W4 起，浏览器集成测试需要）
.venv/bin/playwright install chromium
ATLAS_RUN_INTEGRATION=1 .venv/bin/pytest -m integration   # 同时含 DB + Chromium 用例

# 前端
cd frontend
pnpm install
pnpm dev                               # http://localhost:5174
pnpm test                              # vitest 单元测试
pnpm build                             # 类型检查 + 生产构建
```

Demo 里程碑（08 文档 7.1）：
- ✅ W1-W2：LangGraph 最小循环、前端编辑器框架、PostgreSQL+pgvector 连接均已跑通（2026-09-13）。
- ✅ W3-W4：Harness 契约层（注册发现/权限审计）+ Web 适配器（三层定位、navigate/click/type/screenshot，真实 Chromium 验证）已跑通（2026-09-13）；视觉语义层（omni-parser 类解析 + vision LLM）为协议注入桩，待 T1 LiteLLM 接入后替换。
- ✅ W5-W6：编辑器核心（拖拽画布、trigger/ai_decision/tool_call 三类节点类型化配置 + 实时校验、全局变量与 `{{路径}}` 引用、Graph JSON 导出，vitest 20 用例）已跑通（2026-09-13）。
- ✅ W7-W8：Graph DSL 契约 + 静态校验、DSL→LangGraph 编译/运行（`graph/` 包）、FastAPI graphs 保存/读取/编译/运行、编辑器一键编译运行（后端 16 新用例 + 浏览器验证）已跑通（2026-09-13）；节点执行器为确定性占位，待 LiteLLM/业务工具替换。
- ✅ W9-W10：电商退款端到端 Demo（LiteLLM 决策 + 规则兜底、shop 退款业务能力、SSE 实时进度、NL 退款草稿、模拟商家控制台，08 §7.3 七条验收全部达成；后端 58 单元 + 前端 23 用例 + 浏览器实测，2026-09-13）。
- API 已可启动：`uvicorn atlas.api.main:app --reload --port 8000`（graphs/SSE/NL/adapters/Demo 店铺接口；前端 dev 端口 5174，`/api` 代理到 8000；模拟控制台 http://localhost:8000/demo/shop）。

## Known issues / 注意点

- **文档口径**：01-08 为规格唯一事实源；10-13 为工程化推导文档，与正文冲突时以 01-08 为准（00 文档已声明）。
- **技术选型已锁定（2026-09-13 T1-T5 收口）**：`pyproject.toml` 可按 10 文档 §2 清单落码（版本号落码时解析）；Demo 不引入 NATS/vLLM/Go 网关/评估层实现，缓做项见 14 文档触发条件。
- **改 Schema 必须同步所有引用位置**（03 契约索引 + 所在文档正文），防漂移（00 治理规则第 3 条）。
- **新决策必须回写**：08 任务迭代计划记录 + 10 ADR 记录 + 本文档"Active work"同步，三者一致。

## Conventions（约定）

- **commit 消息**：英文、`<type>(<scope>): <subject>` 格式；原子提交；不 push（除非用户明确说）；作者保持用户身份，不加 AI co-author。详见 [git-commit-message.md](git-commit-message.md)。
- **AI 行为规范**：见 [AGENTS.md](AGENTS.md)（文档分层 / 治理规则 / 技术约束，新会话必读）。
- **新增文档后**：在本文档"Project documents"区补一行索引；改规格后同步 00 文档治理规则与 08 计划。
