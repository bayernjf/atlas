# Handoff — Atlas

更新时间：2026-09-26（**✅ 打包 N 落码收口（docs/68 §7，Active work #72；`dae7c77` 调度包＋迁移 030 → `e1d5869` lifespan 装配＋端点＋两档 store → `4e8f2a4` U874–U891 → `14f7127` cron 预演端点 → `509d6f3`/`fa2abb3` 控制台调度页与画布预演 → `1f318c4` 真机端到端；门 `pytest` **1939 passed / 119 skipped / 0 failed**（基线 1874/114）＋PG 直连 5 passed＋真机四场景全过＋浏览器实测三态）**：图第一次会自己按时跑——自研最小 5 字段 cron 子集零新依赖（**ADR T30**，坏表达式与"5 年内永不触发"都在保存期 422 拒掉，不做"看不懂就当 `*`"兜底）、**槽位一次性认领**（`schedule_fires` PK＋ON CONFLICT DO NOTHING ⇒ 同槽至多一次、重启**不补跑**、双进程也只一次）、UTC-only、只跑发布钉版、重叠跳过且不消耗认领。**三条反向门各自植缺陷并实跑转红**（认领恒真⇒双发／busy 判假⇒没有跳过／放宽回看窗口⇒开始补跑）。store 取**全局**而非按租户（`TenantRegistry` 惰性装配会让"重启后没被请求碰过的租户"永远不被调度），`slot_utc` 取 **TIMESTAMPTZ**（唯一键不能依赖文本序列化）。**⑤⑥ 已补齐：控制台「定时调度」页＋画布 cron 预演（走后端唯一解释器，前端不再写第二个解析器）＋真机端到端（停服 130 秒后停摆窗那一分钟既无 run 也无认领行）** ⇒ 本批收口、**docs/63 §0A N4 已闭合**、单副本约束与三道闸一条不撤；**N3（浏览器自动化算不算 MVP）仍是产品表态**，PROD 判定不变）；**📝 打包 N 立项＝定时触发调度器契约（docs/68；用户「先开 N4 定时触发」；docs-only 立项原子、零代码/零迁移/零依赖改动）**：解 docs/63 §0A N4——`schedule/cron` 只有 `dsl.py:1433` 的"填没填"校验、`src/atlas` 内无任何调度器，而 docs/05 三处承诺定时触发（现状＝配了定时的图永远不会自己跑）。七条决策：自研最小 5 字段 cron 子集零新依赖（ADR T30，坏表达式 422 拒保存、不做"看不懂就当 `*`"兜底）、UTC-only、只跑发布钉版、注册＝发布派生、**槽位一次性认领 ⇒ 重启不补跑、双进程也只触发一次**（照 docs/62 L2 纪律）、重叠跳过且**不消耗认领**、**不解锁多副本**。验收 U874–U891 含三条反向门＋一次真进程端到端（`* * * * *` 等真实派发落 `runs`、重启确认不补跑）；**（该立项时的口径，现已过期：✅ 同日收口，见本块开头）**，N3 仍开 ⇒ PROD 判定不变；**✅ 打包 M 落码＝渠道工具接通通用参数通道（docs/67，Active work #71；`fix(graph)` 一行前缀 ＋ U868–U873 六条测试，只假在 HTTP 传输层；关闭 docs/63 §0A N2——图里现在能真调 Shopify 退款；`pytest` **1874 passed / 114 skipped / 0 failed**（净增 6 条常跑）；**N3 是产品范围表态（浏览器自动化算不算 MVP，留用户）、N4 调度器未做 ⇒ PROD 仍不 GO**；**✅ 打包 L 落码＝prod 首任管理员引导（docs/66，Active work #70；`66e977e`/`4f980df`＋收口 docs 原子；关闭 docs/63 §0A N1 的新 prod 库登录死锁：`ATLAS_ADMIN_BOOTSTRAP_PASSWORD` 必填、缺失或弱口令拒绝启动，prod 只播各租户 admin＋引导口令，dev/test 逐键不变、真机验过 import 即抛；同批修 N5 示例值与 README 过期措辞；**N2/N3/N4 未动，PROD 仍不 GO**；**✅ 治理批收口（Active #69，docs＋test 原子）：迁移约定 §3 与 handoff 索引两条纸面约定改为有机检——新增 `tests/test_migration_convention.py`（命名/连续/首行注释＋`tmp_path` 反向门）与 `tests/test_handoff_integrity.py`（编号唯一/引用可解析/Recently shipped≤5）；并修掉守护一跑就现形的三处事实：Active work 里 **打包 G／J／K 三批从来没有条目**（两条 `Active work #66` 悬空、J 的 `#65` 指向别处）、Recently shipped 被逐批追加到 **10 行**（已滚 5 条进归档）、docs/29 状态行仍自称「清单权威」（已更正为被 docs/63 取代）。门：`pytest` 1856 passed/114 skipped**；**⚠️ 第四次复审撤回（docs/63 §0A，晚于同日打包 J/K）：上轮「工程侧就绪」判定失效——新发现 N1 `ATLAS_ENV=prod` 全新安装登录死锁（J-1c 把弱口令问题修成了谁都进不去）与 N2 真实店铺退款在图里跑不出来（`channel:shopify/*` 拿不到 order_id/amount），另有 N3 浏览器适配器未接入运行期／N4 定时触发无调度器／N5 `.env.example` 的 `ATLAS_ENV=development` 值非法；S5 更正为"只守住 3 条 mock 路由"。当前判定＝产品核心完全可用的 MVP 未达到；demo/陪同试用可交付、自主上线不可交付；待你拍板＝docs/08 A 组 L-1（首任管理员引导策略）**；**✅ 打包 K 全批收口＝docs/63 的 P1-2 运维批全部落码（2026-09-25，契约 docs/65，Active work #68；retention 8 表＋过期会话清扫／备份轮转·异地／`/metrics` prod Bearer 闸门／日志 formatter·request-id；门＝后端 1849/114/0、PG 直连 1962/1/0、前端零改动沿用 728/2/54、oxlint 0/6、build ✓；D38/D39 取回不解除 D11；push 状态唯一权威＝`git rev-list --count refs/remotes/origin/dev..dev`，合 main 须用户明确授权）**；**✅ 打包 J 全批收口＝docs/63 的 P0-1 安全闸门＋P0-2 核心完整性＋P1-1 门禁三批全部落码（2026-09-25，契约 docs/64，Active work #67；11 个功能原子＋2 修正原子；门＝后端 1834/108/0、PG 直连 1941/1/0〔原 audit seq 红消除〕、前端 728/2/54、oxlint 0/6、build ✓；D40 取回不解除 D38/D39；limit `le=` 落码勘误见 docs/64 §10；push 状态唯一权威＝`git rev-list --count refs/remotes/origin/dev..dev`，合 main 须用户明确授权）**；**✅ 第三次项目级上线复审已出＝docs/63（Active work #64）：判定 技术验证 MVP 达到／生产 MVP 未达到，PROD 不 GO；8 条阻断 S1–S8＋3 条新缓做 D38/D39/D40＋A 组 3 批候选；关键结构性事实＝CI 无 postgres service ⇒ 44/1918 integration 永不跑，PG 承诺无自动化在守**；**✅ 打包 I L2 挂起帧一次性认领落码收口（docs/62 §3，Active work #63；`af38176` 迁移＋认领原语 → `593bed0` 三处挂起门与四入口分流 → `df5a18c` 测试 → docs 收口原子；新增迁移 029、零新依赖、零新 REST 端点与错误码、新增一个 SSE 终帧 `superseded`；端到端硬门 `scripts/dev/multi_instance_resume_recon.py` 退出码 **1→0**＝一次审批下游恰落一次；门 `pytest` **1811 passed / 108 skipped**、`-m integration` 43/1（唯一红＝既存 audit `seq` 投影缺陷）；**D19/D20/D27/D31/D32 一律不解除、单副本硬约束与三道闸一条都不撤**，L2 ≠ 支持多副本；新增缓做 D37＝前端未认 `superseded` 帧，D36 为有意引入的收紧）**；**✅ 打包 H 五项全部落码收口（docs/61，Active work #60；用户拍板「打包 1–5 先做」→「开始推进任务」；原子与提交以 docs/61 顶部哈希链为准（不记计数）；push 状态见本文件顶部收口块；零新依赖/迁移 027·028/无 ADR/零新 REST 端点·错误码；D11/D20/D26 部分取回、不解除；测试 U743–U806 转正式）**：**三道门实跑全绿**——后端内存档 **1799 passed / 105 skipped / 0 failed**（213.73s；基线 1758/86，净增 41 常跑＋19 PG skip）、PG 直连 **89 passed**（027/028 由 `storage/migrations.py` glob+sorted 自动 apply、`scripts/ops/apply_migrations.py` 登记 028）、前端 vitest **722 passed / 2 skipped / 53 文件**（基线 694/2/52，净增 26）、tsc 0、oxlint **0 error / 6 既有 warning（零新增）**、`pnpm build` 过（**1712.68 kB / gzip 531.12 kB，相对基线仅 +4.4 kB / +1.4 kB gz**）。RangePicker 曾落码，因 +112 kB 且 dayjs 在 pnpm 下不可解析（中文面板渲染 `2026年Sep`、本地化需把 dayjs 提为直接依赖＝选型决策）→ **回退原生 `datetime-local`**，缺陷与体积同消、最终 bundle 仅 +4.4 kB（详因见 docs/61 顶部注记）。**冒烟**：`scripts/dev/h1_diagnostics_smoke.py` **12/12**（4 截图 `docs/assets/h1-*.png`）、`scripts/dev/h3_audit_filters_smoke.py` **22/22**（连跑 3 次全绿；6 截图 `docs/assets/h3-*.png`，时间边界**双向验真**：清 actor 取 >0 基线→未来 since 掉到 0 行→一年前 since 回到 100 行，并断言带出的 since 已是 `+00:00` UTC ISO）；H2/H4 以 PG 直连「新 store 实例读同一库」等价验证跨重启可见。**顺带修掉一个 M4 起就存在的 bug（单独 `adb9b7c` fix 原子）**：Problems 面板「点击按 pointer 定位并闪烁字段」**从未生效**——`.side-card` 被节点面板/变量面板/属性面板共用、`document.querySelector('.side-card')` 恒命中不含 `[data-pointer]` 的节点面板，且只在 selectNode 后固定等 60ms 查一次；现改 `findFieldAnchor` 逐面板查找＋有界重试 1.2s（`ProblemsPanel.tsx:26/19/55`），由 H1 冒烟首轮「闪烁字段 0 个」暴露。**七处落码偏差（带文件:行号记录于 docs/61 顶部注记，已回填 00/03/04/06/08/09/12/13/14/17/20/CHANGELOG）**：H1 快照落 `validationStore` 而非 editorStore、陈旧判定改 `computedRevision` 门控（严于原口径：任何编辑即失效）、判别轴用 `layer:'server'` 而非 `source`、**给 `t()` 新增可选 `lng` 使语言成为真依赖**（oxlint 正确指出它在 memo 内是隐式全局读；缺省行为对既有调用方零变化）；H2 落定点实为 `resolve()`/`complete_timeout()` 两方法共享 `_record_history`（锁外写、异常只 warning 绝不阻断放行）、预告的 `createdAt` float→UTC ISO＋新增 `resolvedAt` **破坏性变更已执行且未触发退化预案**（实测消费点仅「已处理」Tab 一处）；H3 `list()` 保持返回 list、`nextCursor` 在 API 层推导（tuple 会打破 3 个现有调用点）、**时间边界改按时刻比**（TEXT 字典序只在 `isoformat()` 微秒为 0 省略小数位这一巧合下成立，且前端 `toISOString()` 的 `Z` 与存储侧 `+00:00` 不可直比；PG 侧 `at::timestamptz`＋`CAST(:x AS timestamptz)` 绑参）；H4 `inputs` 改**可空 JSONB**（内存档「无入参」存 None，落 `{}` 会让两档投影分叉）。**测试初稿自误 4 处均为改测试、产品正确**（ISO 断言 2、翻页漏算 fixture 内 login 审计＋末页多请求一页、H4 夹具用了 `_refund_class` 不认的工具名且误期望 `match=True`）。**H5 已消除立项动因**：三处「实现已 PG 化但注释仍写不 PG 化」勘误（`55c1553`，alerts/silences/main docstring；差点重复实现 021/022/024 已完成的工作）。**git 状态勘误（本批收口实测）**：下文与 Active work 多处「打包 G 14 个功能原子在 dev、**未 push**」「合 main PR 须用户明确授权」已过时——`origin/dev` 实含 `8ba3a29`，且 `git merge-base --is-ancestor 8ba3a29 origin/main` 为真＝**打包 G 已合并 main**；**push 状态（唯一权威，其它批次条目只引用不重述）**：批 H 提交已由用户推送至 `origin/dev`（推送时 tip `a53f36f`）。此后任何新增提交都会让该 SHA 过期，故**不给计数、按需现测**：`git rev-list --count refs/remotes/origin/dev..dev` 取本地待 push 数、`git rev-list --count origin/main..dev` 取未合 main 数（合 main 须用户明确授权）。**我全程未执行 push**（origin/dev 曾两次前进、含中途一次非我所为），最终由用户推送。立项原文（设计口径，与上文的收口事实冲突处以收口注记为准）：五项＝①**H1** 前端接线后端编译 422 的稀疏 `locations` 侧车（后端 422 已含 detail/codes/params/locations 四路，但 `apiClient.ts:253/268` 丢弃 locations 且把逐条映射 join 成单串，M2 侧车从未被消费；本批增 CompileIssue/buildCompileIssues/CompileValidationError〔message 仍为 join 串防回归〕＋editorStore serverIssues＋Problems 面板三元组去重并复用既有 `[data-pointer]` 闪烁定位，**后端零改动**、不新增 i18n 键）②**H2** 已决审批历史 PG 化（`collaboration/history.py` 两档＋迁移 027，四决策来源单一落定点、写入 fail-safe 不阻断放行，投影补 resolvedAt 且 createdAt 由 float epoch 改 UTC ISO〔破坏性、退化预案 docs/61 §3.3〕，pending 挂起不动）③**H3** 审计 actor/时间过滤＋游标分页（AuditEvent 增 `seq: int = 0` 使两档共用游标，actor 精确等值、since/until 按 TEXT ISO 字典序闭区间〔前提以不变量测试锁死〕、cursor 取 `seq < cursor`，响应超集 nextCursor，export 受过滤不受 cursor，**零迁移**，端点内手工中文 422）④**H4** 影子运行 PG 化（`recording/pg_shadow.py` 照 `pg_reports.py` 全套抄形＋迁移 028，四子模型 JSONB、ring 100 惰性裁剪、`sr-{n}` 与内存同形，`TenantServices.shadow_store` 改联合类型，**REST 四端点与前端零改动**）⑤**H5** 三处「实现已 PG 化但注释仍写不 PG 化」勘误（`monitoring/alerts.py:74/76/78`、`monitoring/silences.py:5-6`、`api/main.py:1704` docstring）＋docs/33·14 加勘误注记不改写历史正文。**立项动因之一＝勘察发现注释与实现矛盾会误导落码**：原候选清单中「告警三字段 PG 化」「ReportStore PG 化」「DSL 422 code 化＋前端按 code i18n」**三项已完成**（打包 B 迁移 021 / 打包 C 迁移 022 / i18n 第二批债），故剔出范围、H1 只补真缺口。同步面已落：docs/61 新建＋08 立项条＋14 D11/D20/D26 注记与 D26 勘误＋00 地图＋03 三处占位（decided_approval/audit_event 行、monitoring_shadow_run 段）＋CHANGELOG＋本文件。**（其后②–⑦ 五项功能与本批收口已全部落码，见本块开头收口事实）**）；**✅ 打包 G 五项全部落码收口（docs/60，Active work #66–#72；AI 承接「一口气搞了」总授权；零新依赖/迁移 025·026/无 ADR；docs/17 遗留①②＋D22/D24/D27/D28 部分取回、不解除；14 个功能原子在 dev、未 push）**：①**G1** condition/loop/wait 运行结果在中文 expression_errors/message 全保留兜底之外**纯超集**挂 `errorCode`（CONDITION_EVAL_INVALID/WAIT_DURATION_INVALID/WAIT_ABSOLUTE_TIME_INVALID）与等长 `expressionErrorCodes[]`，前端新增 `lib/runtimeError.ts`＋`locales/*/runtime.json`（zh/en，缺码/未知码回退中文、按码定位分支字段），`2b00bbb`/`478c45c`/`9a2836a`；②**G2** OpenAPI `GET imports?include_deleted=true`（administer）＋`DELETE {id}?hard=true`（仅已软删可物理删、未软删 409 OPENAPI_NOT_SOFT_DELETED），内存/PG 双档，前端 admin「显示已删除」+「彻底删除」二次确认、无硬删恢复 UI，`a853700`/`1df73e2`/`1ee7482`；③**G3** `PUT /api/monitoring/silences/{id}` 编辑 reason/到期（已过期 409）＋OnCallSchedule 增 rotation_interval_days/last_rotated_at、纯函数 maybe_auto_rotate 在 get_oncall/告警取值班人读路径惰性按 UTC 日取模轮换（不引定时器/分布式锁），迁移 026，前端 Popover 编辑＋值班间隔 InputNumber，`9dfc593`/`6ba5816`/`89cc9ec`；④**G4** 断点随 Graph JSON 顶层可选 `debugSettings.breakpoints` 往返（裁剪空字段、孤儿断点加载丢弃、三处 serializeGraph 全传），**后端零产品改动/零迁移/零白名单**（GraphDSL 无 extra=forbid、save 存原始 dict 天然透传），普通运行无 debug 不暂停，`c8f1172`/`b46e12b`；⑤**G5** 抽 `message/deliveries.py` DeliveryStore（record/list/clear），InMemory 搬现 ring 200、PgDeliveryStore 落 `message_deliveries`（跨重启保留、惰性裁 200、reset 清本租户、REST 零改动），registry PG 档注入，record `_safe_record` fail-safe 不阻断发送主链路（§11 补漏），迁移 025，`532a3fe`/`e0d7fe1`/`5714db9`。**三道门实跑全绿**：后端内存门 **1758 passed / 86 skipped / 0 failed**（134.97s；净增 24 常跑＋9 PG skip＝33 新测试函数，候选 U663–U742 文件级登记 docs/13 §9）、PG 直连集成（migrations/storage/database/openapi/monitoring/message 六文件）**55 passed**（025/026 按 sorted glob 自动 apply）、前端 vitest **694 passed / 2 skipped / 52 文件**、tsc 0、oxlint **0 error / 6 既有 warning**（cards react-hooks set-state-in-effect，非本批）、`npm run build` 过（dist/assets/index-L91FdD1n.js，1708.29 kB/gzip 529.73 kB，chunk>500 kB 为既有提示）。**三处落码偏差（已回填 03/04/06/09/12/13/14/00/08/CHANGELOG）**：G2 列表删除标记沿用 snake_case `deleted_at`（投影整体 model_dump snake_case；无 restore 入口未扩大）；G3 迁移 026 `last_rotated_at` 用 TEXT 存 YYYY-MM-DD（非 TIMESTAMPTZ，与 024 同表时间列统一）；G5 主键 `(tenant_id,seq)` 而非草拟 `(tenant_id,id)`（群发逐目标多条共享同一 message id，seq 取 storage_id_seq）。仍缓做、D22/D24/D27/D28 均不解除：YAML/Swagger2、oauth2/凭证轮换/连接测试、模板 CRUD/多语言、真正定时器/多值班组/排班日历/分布式锁、团队共享断点权限、变量血缘、持久化中断-恢复/多实例调试、DLQ/短信/入站通用消费/跨实例投递聚合、OTel 正式栈（触发条件见 docs/14）。docs 收口原子后待 push dev；合 main PR 须用户明确授权。）

## 项目概况

Atlas 是 AI 运营体（Agent）编排平台：以 **Harness（能力接入）/ Graph（可执行因果图）/ Loop（OODA 主循环）** 三位一体为内核，面向企业流程自动化场景（首个目标场景：企业内部 OA 审批），目标是让非技术人员通过自然语言/拖拽定义流程，由运营体自动执行、自愈与进化。

当前阶段：**工程主线已于 2026-09-16 切换到题一/题二落地路线（docs/19/20，M1–M10）；M1（九类内置节点 config → `frontend/src/lib/schemas/` 声明式 Data Schema 提取，零 UI 变化/零新依赖/后端零改动）已于 2026-09-16 落码（十提交 4ade416→51cb57f，当日随 PR #24 合并 main），**ADR T16 亦于同日照 M1 真实复杂度案例收口（用户拍板：手写最小子集、不引 AJV、M3 设重开判据，见 10 §4；package.json 不变，10→02→09 已同步），**M2（L1 字段校验扶正 + 结构化诊断：Diagnostic nodeId+RFC6901 pointer+token range，compile 422 locations 稀疏侧车）已于 2026-09-16 按五段原子序全部落码（四代码提交 234a95f/30156a3/d0b2e33/fbd9f77 + 文档收口 cdc2211，当日随 PR #24 合并 main；后端 420 passed/8 skipped、前端 vitest 168/17 文件，真实浏览器九节点冒烟零应用错误，U37/U38 转正式，见 Recently shipped 15 与 08 落码条）；**M3（WidgetRegistry + FormRenderer 工具 params 表单化首期）已于 2026-09-17 落码收口（五提交 2de5053→6d4a863；ADR T17 同日收口：自研最小表单引擎、既有 AntD 承载、零新依赖，oneOf/无类型降级 JSON 文本框，T16 不重开；契约 08 M3 立项条+落码条/04 §4.10/03 `form_renderer`，U39 已转正式），**M4（UISchema 最小子集 + 手写 Config 迁移二波 + 增量调度 + 前端 L3 预判 + Problems 面板 + reverseDeps/quickFix v1）已于 2026-09-17 立项并完成批 1/批 2/批 3 全部落码收口（立项 docs-only 批 81eb2e3；批 1 九提交 `49cb692→30d5009`：uiSchema groups/hiddenWhen/hideFields+字段文案层、内置控件 8→9〔+radio〕、target-select 节点业务控件层与 NodeConfigForm、HumanApproval/Trigger/Loop 迁移、Wait 判定保留手写、editorStore dirty 失效范围骨架；批 2 六提交 `0dc9b7f`/`1468e18`/`2ad4177`/`aaf50bf`/`06b62fc`/`ac29737`：前端 L3 不可达/环同构 dsl.py+11 夹具跨运行时对拍、L1 同步/L2 防抖/L3 idle 分层调度+结构签名记忆化、Problems 面板点击定位、Parallel/Subgraph 迁移 FormRenderer〔新业务件 saved-graph-select、嵌套通配文案键、数组 min/max 门控〕、后端一处 flaky 测试修复与冒烟修复；T17 复查不重开，前端 vitest 336/30、后端 431/8、真实浏览器冒烟零控制台错误，截图入 docs/assets；批 3 五提交 `4b15d95`/`875922b`/`f496062`/`79317e3`/`471b930`：reverseDeps 双类反向索引（target 8 处/4 类与 schema x-ref 对拍、删节点清 target 索引化、L2 收窄真实引用方）、quickFix v1「删除悬空引用」（Problems 内联按钮，仅挂 REF_NODE_NOT_FOUND）、200/500 节点基准（`frontend/bench/` RUN_BENCH 门控、数字入 BENCHMARK.md：稳态单节点 L2 增量 p95≈10ms@500 低于一帧、冷启动全量 p95≈70ms 一次性，不引 Web Worker）、Condition 迁移 FormRenderer（最后一个手写节点 Config，九节点八迁一留 Wait）+ 冒烟修复空串 label 数组标题；前端 vitest 359/32（另 2 基准场景门控跳过）、后端 431/8 零改动零回归，浏览器冒烟脚本 `scripts/dev/m4_batch3_smoke.py` 实测 quickFix 诊断消失零控制台错误，截图 m4-condition/m4-quickfix-smoke.png；契约 08「M4 立项/批 1/批 2/批 3 落码」条，U40/U41/U42 转正式）；**题一 M1–M4 主线至此闭合；M5 契约设计轮已于 2026-09-17 完成（docs/24，四缺口全收口＋同步面已落），唯 ADR T18 已于 2026-09-17 用户拍板选 B（自研暂停帧+续跑），M5a 已落码**（0358696，441 passed/8 skipped 零行为变化，08 M5a 立项条+落码条；M6 已落码（1f49644，ADR T19 收口），M5b 已落码（三批 bf185ed/b4abb59/280493e/1c1539a/f6a33e7），M7 已落码（三批 0f934d6/ff4acf9/e46fc09，08 M7 立项条+落码条 + ADR T20），门控 M6 落码→M5b→M7→M8/M9，M5 只到单实例重启可恢复，见 Active work 11）；**M10 span 级追踪已于 2026-09-18 立项并落码收口（08 M10 立项条+落码条，ADR T21 不引入 OTel SDK，U49–U52 转正式，立项 `ba9e0d2` + 四批 `1f21b1a`/`89f5645`/`c227f0c`/`2b1dbe9`，后端 486 passed/前端 359 passed，详细过程已滚入归档补遗）；M8 交互模板系统亦于 2026-09-18 立项并三批落码收口（U53/U54 转正式，后端 518 passed/前端 370 passed，见 Recently shipped·打包 E 收口条目 与 Active work 14），**M9 入站 Router + 灰度发布 + 指标门控自动回滚已于 2026-09-18 立项并落码收口（ADR T22 同日收口；新包 routing/ + recording/gate.py + monitoring/business.py + 前端发布流＋同图草稿 PUT，U55–U59 转正式，后端 602/前端 396，见 Recently shipped·i18n 第一批：认证/L1 英文态条目 与 Active work 15），题二 M1–M10 主线至此全闭合**。原主线 Phase 3 与 docs/18 真实种子试用降为并行/后置，触发条件不变，沙盘假定 Go 口径不变**。此前阶段：Phase 1 沙盘假定通过、Phase 2 全部范围于 2026-09-16 收口并合并 main（2026-09-14 进入 Phase 2 为用户指示的假定，非真实试用结论）。W1-W10 Demo 全部完成（W9-W10 电商退款端到端 Demo 于 2026-09-13 跑通，08 §7.3 七条验收全部达成）。**Phase 2 六项节点「条件分支节点 condition」「循环节点 loop（v1 条件循环 while）」「并行节点 parallel（扇出/汇聚网关，v1 all_success/all_completed）」「等待节点 wait（v1 定时等待 duration 1-600s）」「子图节点 subgraph（v1 引用已保存图，进程内嵌套执行，深度上限 3）」「人机协作节点 human_approval（v1 进程内审批挂起，10-3600s 超时默认 reject）」均已于 2026-09-14 端到端落地**：condition 节点 config 挂 branches/defaultTarget；loop 节点 config 挂 mode/continueExpression/maxIterations/bodyTarget/exitTarget，循环体回边是唯一合法环（U7 调和），达最大次数/表达式异常 fail-safe 退出；parallel 节点 config 挂 joinStrategy/branches/joinTarget，合成 `__join__` 网关节点做汇聚屏障（spike 实测普通多入边不构成 barrier），all_success 分支失败 fail-safe 汇聚不短路（any_success 缓做 14 D18）；wait 节点 config 挂 waitType/durationSeconds，执行器同步 sleep（线程池边界，600s 上限），恰好一条普通出边纯透传，事件等待/动态时长缓做 14 D19；human_approval 节点 config 挂 summary/approver/timeoutSeconds/onTimeout/approvedTarget/rejectedTarget，`collaboration/` 新增 ApprovalBroker（uuid4 token+`threading.Event`，REST 放行；决策三来源：人工/run inputs 预置/超时，首决生效重复 409），固定双出口（通过/拒绝），阻塞不产生超步，持久化 interrupt/checkpointer/多实例/通知/动态审批人缓做 14 D20；subgraph 节点 config 挂 graphId/inputs（graphId 引用 GraphStore 已保存图，inputs 为子图入参→父图 `{{路径}}`/字面量映射），执行器进程内重入 run_graph（复用 registry/decision/approval broker，子图事件 emit 压制），编译期递归校验缺失/自引用/跨图环/超深/子图非法，恰好一条普通出边，子图运行期异常 fail-safe 折叠为 `status:"failed"`，版本钉版/升级回归/子图市场/远程引用缓做 14 D21；六项均 EdgeDSL 与 Graph version 不变。**Phase 2 适配器方向首项「API 适配器（通用 HTTP 请求）」已于 2026-09-15 端到端落地**：新包 `httpapi/`（adapter_id=`http`，09 待定项 7 收口避名 FastAPI `api/`），单工具 `http/request`，节点 params 为 JSON `{method,url,headers,body,timeout}` 支持 `{{路径}}` 插值，env/注册默认 baseUrl+公共 headers 与节点 headers 键级合并；任何 HTTP 响应（含 4xx/5xx）均 SUCCESS 输出 `{status,headers,body}`，仅传输层失败 FAILED（HTTP_TIMEOUT/HTTP_CONNECT_ERROR），v1 无重试/无 SSRF 防护/无凭证加密，OpenAPI 导入/OAuth/秘密变量/录制生成等缓做 14 D22（触发：真实外部系统接入或多租户）；内置 `/api/demo/mock/*` 端点供离线实测，前端工具选择器首次接 `GET /api/adapters`（按适配器分组、权限/幂等标记）。**Phase 2 适配器方向第二、三项「DB 适配器（通用 SQL）」「消息适配器（进程内消息服务）」已于 2026-09-15 端到端落地**：新包 `database/`（adapter_id=`database`，`database/query` read·幂等返结果集、`database/execute` write 返 rowcount，04 §4.7）与 `message/`（adapter_id=`message`，`message/send` write，`{channel,to,subject,body}`、to ≤20、零真实投递，04 §4.8）；适配器专用 `ATLAS_DATABASE_URL`（scheme 白名单 pg+psycopg/sqlite，自建 engine 工厂，与平台 DATABASE_URL 物理隔离；ADR T10，09 待定项 8 收口），未配置时 api/main 与 demo registry 显式注入 StaticPool SQLite 内存库（seed 两笔订单，reset 重建），适配器自身未注入则 fail-closed DB_NOT_CONFIGURED；消息落进程内记录 + `GET /api/demo/messages` 陪同查看；真实数据库驱动/SQL 审查/只读账号缓做 14 D23，SMTP/IM/短信/webhook 真实投递缓做 14 D24；**DB/消息适配器工作经 PR #11 于 2026-09-15 合并 main**。**Phase 2 能力项「流程模板库 v1（5 个内置只读模板）」已于 2026-09-15 端到端落地**：新包 `template/`（`graphs.py` 5 个模板图、`catalog.py` TemplateMeta+TEMPLATES+访问器，ADR T11，04 §5.10），模板 `refund-auto`/`http-orders-branch`/`sql-query-notify`/`sql-approval-write`/`approval-timeout-reject` 全部只用已注册 demo 适配器、零配置可跑；只读 Python 常量随代码发布（无 DB/CRUD、reset 不影响），REST `GET /api/templates`（列表投影无 graph）+ `GET /api/templates/{id}`（含 graph，未知 404）；前端「从模板新建」Modal 整画布替换（复用 loadGraph，不做 id 重映射）+ 未保存警示；退款 golden 图移入 template 包，nl_generate 改 import（单一事实源，U27 固化）；用户自建/市场/远程导入/版本钉版缓做 14 D25。**Phase 2 能力项「操作录制 v1（运行录制 + 回放比对）」已于 2026-09-15 端到端落地**：新包 `recording/`（RecordingCase/RecordingStore 进程内、normalize/preset_approvals/compare 纯函数，04 §5.11，ADR T12），录制=前端在正常 /run/stream 收集 node_end（无专用运行端点），回放=冻结 Graph 快照走标准 run_graph（人工决策预置 inputs.approvals 秒回不挂起），序列+逐节点归一化深等（token/sent_at/消息 uuid/HTTP date/resolvedBy/载荷 approvals 五规则），用例 reset 不清除（测试资产）重启清空；批量回放/跨版本对比/影子模式/Mock 响应缓做 14 D26，浏览器操作录制仍随 D22。**Phase 2 能力项「单步调试与断点 v1（逐步执行/节点断点/条件断点/变量只读查看）」已于 2026-09-15 端到端落地**：新包 `debug/`（DebugSession/DebuggerBroker 进程内会话与 step/continue/stop 状态机、DebugController 统一执行器单点钩子，04 §5.12，ADR T13），断点会话级存前端不进 Graph JSON（EdgeDSL/Graph version 1 不变、node_schema breakpoint 预留位不启用），paused 帧下发 globals/outputs 深拷贝只读快照，条件断点复用 conditions 引擎且表达式异常 fail-safe 不命中，并行暂停会话锁串行、合成 `__join__` 不停、subgraph 整段执行、wait 停在 sleep 前、human_approval 停在审批注册前（Modal 放行后照常），停止仅暂停点生效（运行中急停缓做 14 D27）；REST `POST /api/debug/{token}/resume`+`GET /api/debug`，同步 /run 带 debug 422，reset 按 stop 释放全部调试会话；前端 Header 单个「调试」入口（启动即逐节点）、节点左侧断点圆点（条件白心）、属性面板断点+表达式实时校验、悬浮工具条（下一步/继续/停止+变量过滤只读快照）；持久化中断-恢复/断点持久化进图/变量改写续跑/变化历史/子图内断点/命中计数缓做 14 D27。后端 329 单元全绿（+U29 11/I17 7），前端 vitest 56（8 文件，+4），浏览器六场景实测（逐步序列、条件真假两路、审批暂停先于 Modal、停止无 result、断点标记/刷新清空、reset 释放悬挂会话）零控制台错误。**Phase 2 能力项「基础监控告警 v1（运行/节点指标 + 站内告警中心）」已于 2026-09-15 端到端落地**：新包 `monitoring/`（records.py MonitoringStore ring 200 单锁、alerts.py 四内置规则阈值可调、metrics.py 两形状失败推断与 nearest-rank 分位聚合，04 §5.13，ADR T14），埋点纯在 API 层包装 sync /run 与非 debug /run/stream 两个真实运行入口（零节点类型分支；debug/录制回放/subgraph 重入天然不记），健康=completed 且无失败节点；REST 新增 metrics/runs/rules(GET+PUT)/alerts(列表+acknowledge+resolve)，reset 清空运行/告警/streak 并恢复默认规则；独立前端 /monitoring 页（Dashboard 入口、指标六卡、告警表确认/关闭、规则阈值、最近运行展开、5s 轮询），后端 358 passed/8 skipped（+U30 20/I18 9）、前端 vitest 59（9 文件，+3），浏览器八场景实测；OTel/Prom/Grafana 正式栈随 D11、适配器调用级指标/IM 出口/自定义规则 DSL 等缓做 14 D28。**Phase 2 新增能力收口项「多租户权限 v1（端点级 RBAC + 租户分区）」已于 2026-09-16 端到端落地**：新包 `iam/`（种子两平级租户四账号、三角色 viewer/operator/admin、进程内 `sess-` token 会话、TenantRegistry 按租户惰性持有 stores/brokers/monitoring，04 §5.14，ADR T15），除登录/health/demo 模拟系统外全部端点 Bearer 鉴权（401/403/跨租户 404），用户数据（graphs/recordings/feedback/messages/approvals/debug/monitoring）全分区、全局基础设施（模板/适配器发现/demo shop+mock/demo SQLite/出向 client）共享，admin reset 只清本租户且保留录制/反馈；前端登录页 + localStorage 会话 + 401 自动回登录 + UserBadge + 按角色隐藏写操作 UI；后端 377 passed/8 skipped（+U31 17/I19 11）、前端 vitest 64（10 文件，+5），浏览器八场景实测零控制台错误（登录/坏凭证/刷新保持、租户 B 全空且跨租户 graph 404、viewer 只读 403、operator 告警处置、admin reset 范围、登出、阈值分区）。**至此 08 Phase 2「新增能力」清单全部落地；2026-09-16 新增方案文档 docs/19（Schema 驱动配置内核 + 多智能体端到端，题述设计提案），其「设计态内核前置项」（Capability input/outputSchema + 拓扑作用域索引 + L2 引用校验）已在 08 Phase 2 延伸区立项待落码，提案其余部分登记 14 D29-D34。****连带治理：`/run/stream` 由「先跑完再回放」改为真流式**（run_graph 在 daemon 线程执行、queue.Queue 实时下发 node_start/node_end，终帧 `event: result`，运行期异常 `event: error`）——审批阻塞期间浏览器可实时收到 node_start 并放行，同时清掉 wait 落码时记载的假流式局限。纯 stdlib 安全规则表达式（禁 eval/算术/函数）为 condition/loop 两类节点共用，DSL 图级校验、LangGraph 全 conditional 出边齐备，前端配置面板/多出口 Handle/出边标签齐备，浏览器实测金额分流、循环三场景（条件收敛/撞上限 fail-safe/表达式异常）、并行三场景（不等长双分支汇聚/一支失败 fail-safe/坏配置报错）与审批三路径（人工同意/人工拒绝/10s 超时自动拒绝且弹窗自动关闭）。退款链路：webhook 退款单 → AI 决策或条件分支 → shop 适配器执行退款或转人工；SSE 节点事件实时上屏，自然语言可生成退款流程草稿。Phase 1 种子验证工程侧齐备（Docker 一键交付/reset/反馈入口/docs 18+TRIAL），但**无真实 C1-C5 试用数据**，Go 结论为沙盘假定（18 文档 §8）；真实试用若 No-Go 需回退。

结构：
- `docs/` — 全部规格文档（入口：`00-文档索引与治理.md`）
- `README.md` / `AGENTS.md` / `CLAUDE.md` / `git-commit-message.md` / `CHANGELOG.md` — 根目录元文档
- `src/atlas/` + `frontend/` — OODA 最小循环、Graph DSL 编译运行（FastAPI）、前端编辑器核心（拖拽/配置/变量/一键编译运行）已跑通；规划见 `docs/09-工程骨架与目录结构.md`

## Project documents（文档地图）

**文档地图（按场景怎么读 + 治理规则）**：[docs/00-文档索引与治理.md](docs/00-文档索引与治理.md) 是单一事实源与 AI 工作入口，本区只做直达索引：

* [docs/01-PRD-产品需求规格.md](docs/01-PRD-产品需求规格.md) — 产品概念（Harness/Graph/Loop 定义、全场景图谱、DIY 机制）+ 产品定义 + Demo 范围
* [docs/02-技术架构设计.md](docs/02-技术架构设计.md) — 三位一体架构、系统分层、技术选型、组件全景
* [docs/03-数据模型与Schema-契约索引.md](docs/03-数据模型与Schema-契约索引.md) — 全部 Schema 位置速查与字段概览
* [docs/04-组件设计-编辑后台.md](docs/04-组件设计-编辑后台.md) — 编辑后台组件（节点/工具/适配器/逻辑/变量/上下文）
* [docs/05-组件设计-运营体五项核心.md](docs/05-组件设计-运营体五项核心.md) — Skill / Memory / 智能体协同 / 部署方式 / 自定义前端模板
* [docs/06-运行时与质量保障.md](docs/06-运行时与质量保障.md) — Loop 主循环、决策节点、Harness Gateway、Web 适配器、测试层次、安全清单
* [docs/07-递归自动化引擎设计.md](docs/07-递归自动化引擎设计.md) — 平台探索器 / 行为挖掘器 / 诊断修复器 / 元运营体 / 自我进化
* [docs/08-任务迭代计划.md](docs/08-任务迭代计划.md) — 开发路线图、Demo 计划 W1-W10、Phase 1-4 迭代路线、商业化路径、验收标准
* [docs/09-工程骨架与目录结构.md](docs/09-工程骨架与目录结构.md) — 工程目录树、模块→文档映射、待填项清单 ★落码必读
* [docs/10-技术选型决策记录.md](docs/10-技术选型决策记录.md) — 选型 ADR（✅ 已定 / ⏳ 待决策）、Demo 依赖清单 ★落码必读
* [docs/11-存储与数据持久化设计.md](docs/11-存储与数据持久化设计.md) — 存储分层、数据对象→表映射、读写路径
* [docs/12-API与模块接口清单.md](docs/12-API与模块接口清单.md) — 引擎内部接口、Harness Gateway、后端 REST、协同/评估接口
* [docs/13-测试用例清单.md](docs/13-测试用例清单.md) — 单元/集成/回放/端到端/AI 评估用例 + 上线前四道门
* [docs/14-缓做事项登记表.md](docs/14-缓做事项登记表.md) — 缓做/低优事项登记表（每条带触发条件，单一事实源）
* [docs/15-环境与分支策略.md](docs/15-环境与分支策略.md) — 环境划分（DEV/TEST/PROD）+ 分支→环境映射（main/dev/feature/*）
* [docs/16-反馈工作流.md](docs/16-反馈工作流.md) — 用户与 AI 协作者的反馈方式（截图+标签 / computer use）
* [docs/17-前端国际化与设计Token方案.md](docs/17-前端国际化与设计Token方案.md) — i18n（i18next 触发式引入/错误码边界）+ 设计 Token 三层模型（需求依据 04 §28；Token 已落码，i18n 触发式引入，存量零星硬编码见 17 §3.5）
* [docs/18-种子客户验证计划.md](docs/18-种子客户验证计划.md) — Phase 1 种子客户画像/招募标准、三场景试用脚本、Go/No-Go 指标、反馈机制（客户向操作指南见根目录 TRIAL.md）
* [docs/19-Schema驱动配置内核与多智能体端到端方案.md](docs/19-Schema驱动配置内核与多智能体端到端方案.md) — 题述设计方案（2026-09-16）：Schema 驱动前端配置内核（SchemaRegistry/Widget 注册表/拓扑作用域索引/三级校验/性能）与多智能体协同/交互模板/部署灰度回滚/可观测端到端，逐节标注 Atlas 现状与缺口；**提案未立项**，落码前先走 10 ADR/08/03 流程
* [docs/20-题述方案工程化落地路线.md](docs/20-题述方案工程化落地路线.md) — docs/19 的工程化落地拆解（2026-09-16）：M0 前置项已落 + M1–M10 里程碑（题一 Schema 内核 M1–M4、持久化 M5、题二版本化/任务总线/交互卡片/灰度回滚/span M6–M10），每期含仓内改动面、前置门、不做什么、验收口径，附契约同步矩阵与 D29–D34 触发总表；**M1/M2 均已落码并随 PR #24 合并 main（2026-09-16；M2＝L1 扶正+结构化诊断+422 locations 侧车，U37/U38 转正式），M3 已立项并于 2026-09-17 落码收口（五提交 2de5053→6d4a863，ADR T17 自研最小表单引擎零新依赖/U39 转正式，仅迁 ToolCallConfig；ADR T16 手写最小子集/不引 AJV 继续有效）、**M4 批 1/批 2/批 3 已全部落码收口（2026-09-17，T17 复杂度 dump 复查不重开，U40–U42 转正式，题一主线闭合，见 Recently shipped 11–13 与 Active work 12）**、**M5–M10 已全部落码收口（2026-09-18，题一/题二主线全闭合；ADR T18–T22 均已收口，详见 Recently shipped·i18n 第二批：Graph DSL 422 码化条目–5/归档补遗 与 Active work 12–17）**
* [docs/21-题述原题与方案作答.md](docs/21-题述原题与方案作答.md) — 两道题的题卷+答卷合订阅读版（2026-09-16）：§1 原题照录，§2/§3 按题面「请给出」条目逐条方案作答（模块/数据流/Schema 形状/伪代码/机制策略，不含实现代码），§4 接缝与落地状态，§5 脱离 Atlas 约束的绿地视角（通用骨架 vs Atlas 塑造的选型分期）；细节以 19 为准、排期以 20 为准
* [docs/22-题述方案绿地独立设计.md](docs/22-题述方案绿地独立设计.md) — 两道题的绿地完整独立方案（2026-09-16）：**显式不受 Atlas 架构/技术栈/单进程 Demo 约束**，按可上线多租户 SaaS 从零设计（rjsf/Formily+AJV+Monaco、Temporal 主协调+Worker Versioning 钉版、独立 Bot 服务、NATS、KMS、OTel/Prometheus、Rollout Controller 灰度），§3 两版选型分叉总对照、§4 七条两版不变通用原则；**非 Atlas 落码依据**，落码以 19/20 为准
* [docs/23-题述两题性质与投入产出分析.md](docs/23-题述两题性质与投入产出分析.md) — 会话问答留档（2026-09-16）：M5 为何是题二硬前置与四契约缺口、题一（重构为主）/题二（增强为主）逐里程碑定性、两题优缺点与一页总结；**讨论整理，非契约非选型决策、不触发立项**，M5 缺口权威登记在 08
* [docs/24-M5持久化与中断恢复契约设计.md](docs/24-M5持久化与中断恢复契约设计.md) — **M5 契约设计轮产物（2026-09-17，M5a/M5b 的形状权威，零代码）**：① Repository＝六资源 Protocol（方法与八 store API 一比一）+ for_tenant/reset 分档统一约定 + `src/atlas/storage/` 包位（不做单一胖接口）；② 中断落库 ADR T18 **✅ 已拍板 B**（自研暂停帧+`loader` 续跑+恢复扫描器，A=LangGraph checkpointer 超出 20 §3 边界；2026-09-17 用户拍板）；③ 04 §5.5/§5.6 v2 语义（绝对 deadline 不重计、决策 Event+轮询双写、SSE 解耦、running 中断标 interrupted、reset PG 档分层）；④ `GET /api/runs` 查询端点（不新增写端点）；§5 表结构、§6 验收对齐；同步面已落 03/12/13（U43–U45）/09/14/11
* [docs/25-行业趋势对照与能力现状盘点.md](docs/25-行业趋势对照与能力现状盘点.md) — 外部行业五项（MCP/A2A、记忆/长期上下文、Agent eval、NL chat-first、Durable execution）逐条对照 Atlas 现状（2026-09-19 dev HEAD `a9a2414`）；三个真实空洞＝记忆/eval/MCP-A2A，记忆空洞直接催生 M11；非契约非 ADR、不直接触发立项
* [docs/26-M11记忆长期上下文契约设计.md](docs/26-M11记忆长期上下文契约设计.md) — **M11 形状权威（2026-09-19 立项、同日四批落码收口 `5441902`/`58d936c`/`73e53bd`/`75c4150`，顶部落码注记为实测最终事实）**：四决策 ADR T23、MemoryItem 字段与 `memory/` 包落位（models/embeddings/items/adapter）、EmbeddingProvider 抽象 + 本地确定性 embedder（EMBED_DIM=256、纯 stdlib、回放确定）、第九个 MemoryRepository 进程内/PG-pgvector 两档 + 迁移 006、memory/remember·recall 两能力 schema 白名单、REST 浏览/搜索/删除（DELETE admin）＋ docs/28 批 4⑩ POST/PUT 手动新建编辑（operate、source=manual，取代原「无写入」口径）、前端记忆页（含新建/编辑 Modal）、U72–U99 与批 4 U192–U201、非目标 D35
* [docs/27-进程内运行体验补强契约设计.md](docs/27-进程内运行体验补强契约设计.md) — **C+A+B 打包形状权威（2026-09-19 立项、2026-09-20 全部落码收口；C `3415377`、A `e594a4b`、B `f9a1301`/`2ad0b52`，U100–U138 转正式）**：C 可注入时钟 + today/now/datetime/hoursBetween + RecordingCase.recorded_at 回放冻结；A 子图内部节点事件 subgraphPath 上 SSE（吞子层 run_end）+ 子图内审批进程内交互；B 协作式急停（RunCancellationBroker + POST /runs/{id}/cancel + cancelled 帧/状态）+ 断点 hitCount/logpoint（debug_log）+ 暂停 resume globals 白名单浅合并；D 输出键改名判空集（§1.3）；REST/SSE/Schema 变更汇总、U100–U138 候选、非目标（持久化/多实例/子图断点/命名输出仍缓做）
* [docs/28-工程内可闭环缓做项打包契约设计.md](docs/28-工程内可闭环缓做项打包契约设计.md) — **11 项工程内可闭环缓做项打包形状权威（2026-09-20 立项；批 1 D26 已收口 `d1f455b`/`c7bf138`/`89e21fc`/`0e8ebe7`、批 2 D27 已收口 `038dcaa`/`9b378c2`/`f6f7d30`/`c13886c`、批 3 D28 已收口 `a318d97`/`11b1ba7`/`4256326`/`dc46f20`（U166–U191）、批 4 D35·D21 已收口 `ec0fd81`/`ba97088`/`19f31b4`（U192–U211），**四批 11 项 2026-09-20 全部落码收口**）**：批 1 D26（迁移 008 修 PG recordings 富字段、Mock 工具回放仅单用例 replay、用例 PUT 仅 name/inputs＋入参覆写、跨图报告看板）、批 2 D27（暂停帧变量变化历史、异常断点 resume 后原样重抛、子图内部断点逐帧上屏）、批 3 D28（tool_metric 适配器调用埋点 p50/p95/错误码、自定义告警规则 DSL 复用 conditions 禁 eval）、批 4（D35 记忆 POST/PUT operate 写入编辑、D21 子图版本升级体检只读 UI）；§6 四个新端点（GET /api/release-reports、POST/PUT /api/memories、GET /api/graphs/{id}/subgraph-upgrades）、§8 原子序与 U139–U186 候选、§9 契约同步矩阵；零新依赖/不新增 ADR/不解除 D21·D26·D27·D28·D35
* [docs/29-MVP上线就绪评审.md](docs/29-MVP上线就绪评审.md) — **MVP 上线就绪评审留档（2026-09-21 另一会话产出、接力已复核）**：双口径结论——可演示/陪同试用技术验证 MVP **达到**（真实 HTTP/浏览器实测、三道门 782/498）；客户自助接真实店铺的生产 MVP **未达到**，六项阻断项＝①compose 纯内存无 PG/数据卷 ②IAM 明文密码+进程内会话 ③无凭证加密/OAuth/SSRF 白名单/只读/SQL 审查、消息不真实投递、物流 stub ④无 CD/TLS/反代/多实例、LICENSE 缺失 ⑤docs/18 §8「假定 Go」零真实客户数据 ⑥无 key 仅黄金退款规则；含 P0（PG 卷+备份演练、密码哈希+JWT、真实接入安全准入、一个真实电商渠道+真实审批送达）/P1（TLS/SLA/正式可观测栈/LICENSE）最小必做清单。**P0/P1 仅为立项候选，未开工未排期**
* [docs/30-持久化交付契约设计.md](docs/30-持久化交付契约设计.md) — **P0 批 1 形状权威（2026-09-21 立项并落码收口，ADR T24）**：compose 缺省起 `pgvector/pgvector:pg16`＋命名卷、atlas 走 PG 档；迁移运行器＝`schema_migrations`（009）＋`scripts/ops/apply_migrations.py` 手写有序幂等 SQL（不引 Alembic，entrypoint 先迁移后启动）；会话 PG 接线；pg_dump/pg_restore 脚本＋真实删卷恢复演练；U222–U226、原子序 6 步
* [docs/31-生产认证契约设计.md](docs/31-生产认证契约设计.md) — **P0 批 2 形状权威（2026-09-21 立项并落码收口，ADR T25；运行时权威块 04 §5.17）**：stdlib scrypt 口令哈希（零新依赖）、`iam_users`（迁移 010）管理员代管账号与幂等 seeder、不透明 token＋绝对 TTL（迁移 011，默认 12h）不引 JWT、进程内登录滑动窗口节流、口令策略；五个新 REST 端点；后端 `cd1133a`/`097c056`/`e65e4df`/`4224578`/`36ab54e`＋前端 `222552d`，后端 822/前端 509、curl 23 项、浏览器 8 截图；U227–U232 转正式
* [docs/32-真实接入安全准入契约设计.md](docs/32-真实接入安全准入契约设计.md) — **P0 批 3 形状权威（2026-09-21 立项并落码收口，dev 待 PR #48；ADR T26 定稿 (a) 引 cryptography 落 AES-256-GCM）**：纯逻辑安全准入层——凭证信封 enc$v1＋SecretProvider 注入/脱敏、HTTP SSRF egress 私网/云元数据拦截＋白名单、DB 只读强制＋SQL 静态审查、出向重试/按 host 熔断；五原子 egress 06b2473/guard a189dc1/resilience 5376e79/deps a61680b/secrets 01a2f97，7 错误码，后端 1008 passed，U233–U239 转正式；不立 OAuth2/真实邮件/真实数仓，D22/D23/D24 不解除
* [docs/33-影子模式与监控告警补强打包契约设计.md](docs/33-影子模式与监控告警补强打包契约设计.md) — **已落码收口（2026-09-21，12 原子全 ✅）**：影子模式＋监控告警补强＋i18n 续批打包（6 项四批、进程内 v1、零新依赖、不解除 D12/D26/D28）：①i18n 续批四原子（editor 框架/画布/左栏＋属性面板/调试台、monitoring、memory；不引 i18next、en-US 空占位、专名不译）②D26 影子模式（Capability.permission 判定 dry-run、旁路录制、4 端点、ring 不 PG 化）③D28 Trace 钻取（迁移 012 spans JSONB＋单运行 trace 端点＋前端瀑布）④D28 告警静默/未确认惰性升级/值班手动轮换；共 11 个新端点，仅 spans 走 PG。12 原子序、U240–U309、三道门基线固化于 docs/33 §8
* [docs/34-MVP上线就绪评审-2026-09-22复审.md](docs/34-MVP上线就绪评审-2026-09-22复审.md) — **项目级 MVP 上线就绪复审（2026-09-22）**：docs/29 初评后 P0 三批 + docs/33 + 整栈 PG 装配修复全部落码后的第二次判定。双口径结论不变：陪同试用 MVP 达到且更扎实；客户自助生产 MVP 未达到。阻断 #1/#2/#3 纯逻辑子集已解除（compose 已 PG、scrypt+TTL、SSRF/AES-GCM/SQL guard）；阻断 #4（TLS/反代/CD/LICENSE）、#5（零真实客户数据）、#3 真实渠道投递（OAuth2/SMTP/IM/webhook）仍缺。含实测证据表、测试基线（后端 collect 1084 / 前端 555）、分场景建议、P0/P1 最小必做清单；**2026-09-25 追加复审更新注记**（§五 P0/P1 逐项对活代码复验：OAuth2/SMTP/IM/webhook 真实投递、TLS 反代样例、审计与 jsonl 导出**均已落码**，阻断 #4 的 LICENSE 子项已拍板不开放；**唯 P1-7 真实客户数据仍为空**；并新增此前两份评审均漏列的阻断项＝**单实例硬约束**）
* [docs/35-真实接入交付批契约设计.md](docs/35-真实接入交付批契约设计.md) — **✅ 已全部落码收口（2026-09-22；T2/T3/T4/T5/T6＋⑨总收口文档回填均完成，仅本地 dev 未 push、合 main 待授权）**：T2 审批挂起邮件通知（D20 邮件子集，human_approval 可选 notifyEmails、notifier fail-safe）、T3 webhook 渠道（D24 子集，过 SSRF egress、WEBHOOK_SEND_FAILED）、T4 generic OAuth2 连接管理 v1（D22 子集：CRUD+授权码+HMAC state CSRF+token AES-GCM 信封+刷新+连接测试+迁移 014+9 端点+连接管理 UI+无副作用回调页，平台无关不写死真实平台）、T5 Grafana/Prometheus provisioning 样例（D11 子集，observability profile）、T6 审计日志 ring+PG+中间件+jsonl 导出（迁移 013，P1 #8）。五包部分取回不解除缓做；形状权威＝docs/35，落码按其 §9 九步原子序
* [docs/36-审批闭环批契约设计.md](docs/36-审批闭环批契约设计.md) — **✅ 五原子全部落码收口 2026-09-22（e97ec37/bd6ec7a/8923473/e2771ba，浏览器六场景冒烟过；dev 未 push）**：A 邮件一键决策（HMAC capability token、公开 email-view/email-decision、GET 防预取、/approvals/ 深链落地页、email 卡片、ADR T27）、B 审批队列页（pending 聚合、10s 轮询、viewer 只读）、broker created_at；后端 1174/前端 571；D20 再次部分取回不解除
* [docs/37-审计可见与审批结果补强批契约设计.md](docs/37-审计可见与审批结果补强批契约设计.md) — **✅ 落码收口 2026-09-22（ecfa325/bc3f116/033619a，§7 原子 2–5 全完成；后端 1190/前端 571，浏览器冒烟过）**：A 审计日志页（admin，复用现有 audit events/export 端点，action 前缀＋blob 下载）、B 已决审批历史（broker list_decided＋GET /api/approvals/decided＋已处理 Tab）、C 决策结果邮件（notify_recipients 留存＋notify_decided，人工/邮件链接/超时/预置全来源 fail-safe）；零新依赖/无 ADR；不解除 D11/D20/D24
* [docs/38-真实渠道适配批契约设计.md](docs/38-真实渠道适配批契约设计.md) — **✅ 全部落码收口 2026-09-23（ADR T28；`34472ce`→`4fd167e` 在 dev、未 push）**：`channels/` Shopify Admin API（ShopifyChannelClient/ChannelRegistry/动态 Harness 适配器/shop/* 三工具）、迁移 015、REST /api/channels 五端点；后端 1220/40、前端 577/2/45；形状权威＝docs/38；D22 不解除
* [docs/39-入站Webhook批契约设计.md](docs/39-入站Webhook批契约设计.md) — **✅ 收口 2026-09-23（ADR T29）**：channels/webhooks.py Shopify HMAC 验签＋WebhookDeliverer 异步投递、迁移 016 webhook_subscriptions、公开 hook 端＋订阅 GET/PUT、前端订阅 Modal；零新依赖；不解除 D22/D24；后端 1252/44、前端 579/2；形状权威＝docs/39
* [docs/40-入站可靠性补强批契约设计.md](docs/40-入站可靠性补强批契约设计.md) — **✅ 落码收口 2026-09-23（零新依赖/无 ADR，六原子）**：投递去重 PG 化（webhook_deliveries，迁移 017）、死信捕获＋一键重放、投递指标；D24 部分取回不解除；形状权威＝docs/40
* [docs/41-Shopify侧Webhook注册批契约设计.md](docs/41-Shopify侧Webhook注册批契约设计.md) — **✅ 落码收口 2026-09-23（零新依赖/零迁移/无 ADR）**：Shopify Admin webhooks 注册/查询/取消注册、REST 三端点、订阅弹窗注册区、demo mock 缝；D22 部分取回不解除；形状权威＝docs/41
* [docs/42-OpenAPI导入与工具自动生成批契约设计.md](docs/42-OpenAPI导入与工具自动生成批契约设计.md) — **📋 docs-only 立项 2026-09-23（零新依赖/零迁移/无 ADR）**：OpenAPI 3.x 解析→自动生成适配器与工具、进程内 ImportStore、REST 五端点、前端导入页、编辑器零改动；D22 部分取回不解除；形状权威＝docs/42
* [docs/42-OpenAPI导入与工具自动生成批契约设计.md](docs/42-OpenAPI导入与工具自动生成批契约设计.md) — **✅ 落码收口 2026-09-23（零新依赖/无 ADR）**：OpenAPI 3.x 解析→自动生成适配器与工具、ImportStore、REST 五端点、前端导入页、编辑器零改动；D22 部分取回不解除；形状权威＝docs/42
* [docs/43-OpenAPI导入规格PG持久化批契约设计.md](docs/43-OpenAPI导入规格PG持久化批契约设计.md) — **✅ 落码收口 2026-09-23（零新依赖/无 ADR）**：迁移 018 openapi_imports＋PgImportStore，PG 档跨重启/多实例；REST/前端/编辑器零改动；形状权威＝docs/43
* [docs/43-OpenAPI导入规格PG持久化批契约设计.md](docs/43-OpenAPI导入规格PG持久化批契约设计.md) — **✅ 落码收口 2026-09-23（零新依赖/无 ADR）**：迁移 018 openapi_imports＋PgImportStore，PG 档跨重启/多实例；REST/前端/编辑器零改动；形状权威＝docs/43
* [docs/44-OpenAPI-securitySchemes静态密钥批契约设计.md](docs/44-OpenAPI-securitySchemes静态密钥批契约设计.md) — **📋 docs-only 立项 2026-09-23（零新依赖/无 ADR）**：apiKey header/query＋Bearer 静态密钥接线，信封加密随规格落内存/PG（迁移 019），缺密钥 fail-closed；形状权威＝docs/44
* [docs/45-循环foreach批契约设计.md](docs/45-循环foreach批契约设计.md) — **📋 docs-only 立项 2026-09-23（零新依赖/零迁移/无 ADR）**：loop mode=foreach，itemsExpression 冻结逐项、item 暴露、collectTarget 聚合；D16 主体取回不解除；形状权威＝docs/45
* [docs/46-OpenAPI-HTTPBasic凭证子集批契约设计.md](docs/46-OpenAPI-HTTPBasic凭证子集批契约设计.md) — **✅ 落码收口 2026-09-23（零新依赖/无 ADR）**：parser http/basic、PUT {username,password}、适配器 Authorization: Basic；D22 部分取回不解除；形状权威＝docs/46
* [docs/47-wait事件等待进程内v1批契约设计.md](docs/47-wait事件等待进程内v1批契约设计.md) — **✅ 落码收口 2026-09-23（后端 1435/59、前端 606/2；零新依赖/零迁移/无 ADR）**：wait waitType=event，EventWaitBroker 挂起、REST 广播/直投信号或 waitEvents 预置、超时 continue/fail；D19 部分取回不解除；形状权威＝docs/47
* [docs/61-编译诊断定位-审批历史PG-审计过滤分页-影子PG化-口径勘误v1批契约设计.md](docs/61-编译诊断定位-审批历史PG-审计过滤分页-影子PG化-口径勘误v1批契约设计.md) — **✅ 落码收口 2026-09-25（打包 H；已落码，形状权威＝docs/61）**：H1 前端接线后端编译 422 的稀疏 `locations` 侧车（CompileIssue/buildCompileIssues/CompileValidationError、Problems 面板三元组去重＋复用 `[data-pointer]` 闪烁定位，**后端零改动**，收口 docs/20 M2 侧车遗留）、H2 已决审批历史 PG 化（`collaboration/history.py` 两档＋迁移 027、四决策来源单一落定点 fail-safe 不阻断放行、投影补 resolvedAt 且 createdAt 改 ISO）、H3 审计 actor/时间过滤＋游标分页（AuditEvent 增 seq、TEXT ISO 字典序、nextCursor 超集，**零迁移**）、H4 影子运行 PG 化（`recording/pg_shadow.py` 照 pg_reports 抄形＋迁移 028、四子模型 JSONB，REST/前端零改动）、H5 三处「已 PG 化但注释仍写不 PG 化」勘误（alerts.py/silences.py/main.py:1704）＋docs/33·14 注记；零新依赖/无 ADR/零新 REST 端点/零新错误码；D11/D20/D26 部分取回不解除；测试候选 U743–U806；门：后端 1799/105/0、PG 直连 89、前端 722/2/53 文件、oxlint 0/6、build ✓（仅 +4.4 kB）；冒烟 h1 12/12＋h3 22/22；七处落码偏差带行号见 docs/61 顶部注记
* [docs/62-挂起帧一次性认领-单副本硬约束护栏-v1批契约设计.md](docs/62-挂起帧一次性认领-单副本硬约束护栏-v1批契约设计.md) — **打包 I（2026-09-25 立项）**：§0 实测前提＝两份上线评审把多实例当缓做、实测是「开了会重复扣款」（双实例一次审批下游落库 2 次且分支相反，单实例对照组只落 1 次）；L1 单副本护栏（Dockerfile 显式 `--workers 1`＋entrypoint fail-closed＋compose `replicas: 1`＋部署文档明文）**当批落码**；L2 挂起帧**一次性认领**（迁移 029 `resumed_at`/`resumed_by`＋原子 `UPDATE ... WHERE resumed_at IS NULL`，认领点在 `graph/loader.py` 三处挂起（审批／事件等待／时长·到点等待）之后、经注入的 `resume_claim(token)` 判定，输家停止驱动、终态让给赢家；恢复扫描与其续跑线程保持不变）**只出契约、待拍板不落码**；取向已从「owner＋惰性租约」自我修正为 at-most-once，代价登记为新缓做 **D36**；验收含反向门。零新依赖/无 ADR/零新端点·错误码；D19/D20/D27/D31/D32 不解除但触发条件重述
* [docs/63-项目级上线复审-2026-09-25第三次.md](docs/63-项目级上线复审-2026-09-25第三次.md) —— **第三次项目级上线复审（当前上线判定权威）**：判定＝技术验证 MVP 达到／生产 MVP 未达到（PROD 不 GO）；三条核心完整性缺口（交付形态静默无 LLM 决策、退款全链无一条 e2e 测试、M7 验收链手写）＋八条安全阻断 S1–S8＋结构性缺口「CI 无 postgres service ⇒ 44/1918 integration 永不跑」；每条带证据分级〔跑/码/勘〕。docs/29·34 历史正文保留，冲突以 63 为准
* [docs/66-prod首任管理员引导批-v1批契约设计.md](docs/66-prod首任管理员引导批-v1批契约设计.md) —— **打包 L：prod 首任管理员引导（2026-09-25 落码，解 docs/63 §0A N1）**：`ATLAS_ADMIN_BOOTSTRAP_PASSWORD` prod 必填、缺失或弱口令**拒绝启动**；prod 只播各租户 ADMIN＋该口令，dev/test 逐键不变；含被否三方案与四条残余风险
* [docs/68-定时触发调度器批-v1批契约设计.md](docs/68-定时触发调度器批-v1批契约设计.md) —— **打包 N：定时触发调度器（2026-09-26 立项，解 docs/63 §0A N4；**✅ 同日落码收口（六代码原子＋真机端到端，N4 已闭合）**，形状权威与**十条落码偏差**＝该文顶部进度注记＋§7 收口注记）**：七条决策＝自研最小 5 字段 cron 子集零新依赖（**ADR T30**，坏表达式 422 拒保存不做兜底）／UTC-only／只跑发布钉版／注册＝发布派生／**槽位一次性认领**（`schedule_fires` PK＋ON CONFLICT DO NOTHING ⇒ 重启不补跑、双进程也只一次）／重叠跳过且**不消耗认领**／**不解锁多副本**；形状＝`scheduling/` 三文件＋迁移 030 两表＋lifespan（在 `recover_pending` 之后）＋3 端点＋前端调度表；验收 U874–U891 含**三条反向门**与一次**真进程端到端**（`* * * * *` 等真实派发落 `runs`，重启确认不补跑）；六条残余风险照实写
* [docs/67-渠道工具接通通用参数通道批-v1批契约设计.md](docs/67-渠道工具接通通用参数通道批-v1批契约设计.md) —— **打包 M：`channel:*` 接入通用 JSON 参数通道（解 docs/63 §0A N2）**：一行前缀路由改动＋U868–U873；图里现在能带 `order_id`/`amount` 真调 Shopify 退款；**关闭证据是真适配器＋假 HTTP，不是真店铺 200**
* [docs/60-运行时错误i18n-OpenAPI硬删-静默值班轮换-断点持久化-投递PG化v1批契约设计.md](docs/60-运行时错误i18n-OpenAPI硬删-静默值班轮换-断点持久化-投递PG化v1批契约设计.md) — **✅ 落码收口 2026-09-24（打包 G；2b00bbb…5714db9 14 原子；零新依赖/迁移 025·026/无 ADR；D22/D24/D27/D28 部分取回不解除）**：G1 运行时错误码化＋前端 runtimeError zh/en、G2 OpenAPI include_deleted/硬删除、G3 静默编辑/值班惰性日轮换、G4 断点随 Graph JSON（后端零改动）、G5 投递日志 DeliveryStore PG 化；后端 1758/86/0、PG 55、前端 694/2；形状权威＝docs/60
* [docs/59-告警规则模板市场-静默值班PG化v1批契约设计.md](docs/59-告警规则模板市场-静默值班PG化v1批契约设计.md) — **✅ 落码收口 2026-09-24（打包 F；迁移 024）**：内置只读告警规则模板市场＋一键应用（无写端点）、静默/值班/assignee PG 化（仅 PG 档、内存档不动）；后端 1734/77、PG 44、前端 675/2；形状权威＝docs/59
* [docs/58-消息渠道余部-webhookHMAC-IM富文本At-多URL群发v1批契约设计.md](docs/58-消息渠道余部-webhookHMAC-IM富文本At-多URL群发v1批契约设计.md) — **✅ 落码收口 2026-09-24（打包 E；后端、零迁移）**：webhook HMAC-SHA256 签名、IM markdown 富文本/@人（钉钉/企微/飞书）、webhook/IM 多 URL 群发 1-20；后端 1713/69、im smoke 27/27；形状权威＝docs/58
* [docs/57-i18n-enUS翻译-语言切换-发布协作组件抽取v1批契约设计.md](docs/57-i18n-enUS翻译-语言切换-发布协作组件抽取v1批契约设计.md) — **✅ 落码收口 2026-09-24（打包 D；纯前端/后端零改动）**：编辑后台 en-US 全量翻译、运行时语言切换（localStorage/antd locale）、发布/审批/反馈/卡片组件补抽、英文态浏览器冒烟；前端 662/2；形状权威＝docs/57
* [docs/56-报告PG持久化-OpenAPI去重软删-消息投递追踪退避v1批契约设计.md](docs/56-报告PG持久化-OpenAPI去重软删-消息投递追踪退避v1批契约设计.md) — **✅ 落码收口 2026-09-24（打包 C；迁移 022/023）**：D26 release_reports PG、D22 OpenAPI 指纹去重/软删除/restore、D24 DeliveryRecord ring 200＋退避重试；后端 1674/69、PG 33、前端 645/2；形状权威＝docs/56
* [docs/55-AND竞速-信号排队-告警PG生命周期-flapping抑制v1批契约设计.md](docs/55-AND竞速-信号排队-告警PG生命周期-flapping抑制v1批契约设计.md) — **✅ 落码收口 2026-09-24（打包 B；迁移 021）**：eventWaitMode any/all AND 竞速＋per-key 排队 ring、PG 档告警 lifecycle（rule_name/escalated_at/assignee）＋recovery gating/冷却＋flapping 抑制；形状权威＝docs/55
* [docs/54-wait时长放开-jitter-竞速-告警恢复限流v1批契约设计.md](docs/54-wait时长放开-jitter-竞速-告警恢复限流v1批契约设计.md) — **✅ 落码收口 2026-09-23（打包 A；零迁移/无 ADR；D19/D28 部分取回不解除）**：duration 上限 3600/event 86400、duration jitter、eventKeys 1-8 OR 竞速跨重启、健康运行自动恢复 open 告警、lifecycle 60s 限流；后端 1630/61、PG 14、前端 643/2；形状权威＝docs/54
* [docs/53-wait事件跨重启持久化v1批契约设计.md](docs/53-wait事件跨重启持久化v1批契约设计.md) — **📋 docs-only 立项 2026-09-23（零迁移/零依赖/无 ADR/无新 REST/前端零改动；D19 部分取回不解除）**：event 挂起帧 + wait 帧字段、EventWaitBroker.restore、启动恢复，PG 单实例；候选 U400-U402
* [docs/52-监控告警外部通知v1批契约设计.md](docs/52-监控告警外部通知v1批契约设计.md) — **✅ 落码收口 2026-09-23（70b4889/07baa89/6a7d846/b352638；零新依赖/无 ADR；D28/D24 部分取回不解除）**：AlertChannel/AlertNotifier 仅新建告警五渠道旁路投递、fail-safe，迁移 020、GET/PUT alert-channel、监控页配置卡；后端 1548/59、前端 636/2、两层冒烟 ALL PASS 15 checks；形状权威＝docs/52
* [docs/51-IM机器人消息投递v1批契约设计.md](docs/51-IM机器人消息投递v1批契约设计.md) — **✅ 落码收口 2026-09-23（d4823d5/80b2436；零新依赖/零迁移/无新 REST/无 ADR；D24 部分取回不解除）**：dingtalk/wecom/feishu 群机器人 Webhook，secret 加签、平台码非 0 IM_SEND_FAILED、失败不落记录；形状权威＝docs/51
* [docs/50-wait节点到点时刻等待v1批契约设计.md](docs/50-wait节点到点时刻等待v1批契约设计.md) — **✅ 落码收口 2026-09-23（零新依赖/零迁移/无新 REST/无 ADR）**：wait durationMode=absolute，absoluteTime（ISO/epoch，支持 {{}}）解析差值 1-600 秒，坏时刻/过点 WAIT_ABSOLUTE_TIME_INVALID；12de72d/4f82a1c，后端 1490/59、前端 624/2、HTTP 冒烟 21/21；D19 部分取回不解除；形状权威＝docs/50
* [docs/49-wait节点定时等待动态时长v1批契约设计.md](docs/49-wait节点定时等待动态时长v1批契约设计.md) — **✅ 落码收口 2026-09-23（零新依赖/零迁移/无新 REST/无 ADR；257ba38→8580fcb→4eaaddd）**：wait durationMode=dynamic，durationExpression 运行时求值 1-600 秒，坏表达式 WAIT_DURATION_INVALID；HTTP 冒烟 21/21；D19 部分取回不解除；形状权威＝docs/49
* [docs/48-condition节点LLM语义判断分支v1批契约设计.md](docs/48-condition节点LLM语义判断分支v1批契约设计.md) — **✅ 落码收口 2026-09-23（零新依赖/零迁移/无新 REST·错误码/无 ADR）**：condition conditionMode=llm，分支用 description，LiteLLM 单次调用返唯一标签，四类失败全 fail-safe 走 defaultTarget；D14 部分取回不解除；形状权威＝docs/48
* [docs/handoff-archive-2026-09-18.md](docs/handoff-archive-2026-09-18.md) — 2026-09-18 自 handoff 归档的 Recently shipped #6–#38 历史变更（原编号保留，主 handoff 交叉引用按号定位）
* [docs/handoff-archive-2026-09-19.md](docs/handoff-archive-2026-09-19.md) — 2026-09-19 滚出的 M9（原 #5）/D26 报告 v1 等历史变更（原编号保留）
* [docs/handoff-archive-2026-09-24.md](docs/handoff-archive-2026-09-24.md) — 2026-09-24 docs/55/56 收口时滚出的顶部更新流水（docs/34 尾批~docs/54）与旧 Recently shipped·打包 E 收口条目 条（docs/47/44/43/38/37），原编号保留
* [docs/handoff-archive-2026-09-21.md](docs/handoff-archive-2026-09-21.md) — 2026-09-21 P0 批 3 收口时滚出的原 #5（C+A+B 之 B 包：协作式急停/hitCount·logpoint/globals 续跑，f9a1301）
* [docs/handoff-archive-2026-09-22.md](docs/handoff-archive-2026-09-22.md) — 2026-09-22 两次滚出：整栈 PG 修复收口时原 #4/#5（M12 i18n 骨架、docs/28 批4）、对外交付层小批收口时原 #5（M12 dashboard 接线）

根目录元文档：[CHANGELOG.md](CHANGELOG.md)（变更日志）/ [CONTRIBUTING.md](CONTRIBUTING.md)（贡献指南）/ [AGENTS.md](AGENTS.md)（AI 行为规范，**新会话必读**）/ [TRIAL.md](TRIAL.md)（种子客户试用一页纸）/ [git-commit-message.md](git-commit-message.md)（commit 详细规范）/ [MIGRATION_CONVENTION.md](MIGRATION_CONVENTION.md)（DB 迁移规范）/ [BENCHMARK.md](BENCHMARK.md)（性能基准记录表）/ [.gitleaks.toml](.gitleaks.toml)（密钥扫描配置）/ [.env.example](.env.example)（环境变量模板）

**代码阶段待补（遵循 agent-world 惯例）**：`.pre-commit-config.yaml`。CI 工作流（`.github/workflows/ci.yml`，gitleaks/后端 pytest/前端 vitest+build 三道门）已于 2026-09-14 落地（14 D10a，ADR T8）；CD 仍缓做（14 D10b）。`Dockerfile` + `docker-compose.yml` + `.dockerignore` 已在 Phase 1 准备期落地（2026-09-13）。`pyproject.toml` 与前端工程已在 W1 创建。

## Current state（当前状态）

- **阶段**：W9-W10 电商退款端到端 Demo 已完成（2026-09-13，08 §7.3 七条验收全达成）。新增 `llm/`（LiteLLM 退款决策 + 规则兜底 + NL 草稿生成）、`shop/`（DemoShopService + ShopHarnessAdapter：login/list_pending_refunds/execute_refund/request_human_approval/process_refund）两个包；`graph/loader.py` 经依赖注入接真实决策/适配器/事件回调；API 新增 SSE 运行流、NL 生成、适配器发现与模拟商家控制台；前端退款单选择、节点实时状态、NL 草稿载入。Phase 2 六类节点 + 适配器三项（通用 HTTP API 适配器 `httpapi/`、通用 SQL 适配器 `database/`、进程内消息适配器 `message/`）+ 能力项「流程模板库 v1」（只读内置目录 `template/`，5 个零配置模板 + 两个 GET 端点 + 前端「从模板新建」整画布替换）+ 能力项「操作录制 v1」（新包 `recording/`：进程内用例存储 reset 不清除 + 冻结 Graph 快照回放 + 序列/逐节点归一化比对，五个 REST 端点 + 编辑器「录制与回放」Modal，04 §5.11）全部落地后，Phase 2 能力项「单步调试与断点 v1」（新包 `debug/`：进程内调试会话与统一执行器暂停钩子、会话级断点不进 Graph JSON、step/continue/stop、条件断点 fail-safe、globals/outputs 只读快照，04 §5.12）亦已落地，能力项「基础监控告警 v1」（新包 `monitoring/`：进程内 ring 200 运行记录、四内置规则阈值可调、站内告警中心，04 §5.13）亦已落地；后端 358 单元全绿、前端 vitest 59（9 文件）全绿（明细见 Quality gate），浏览器实测 12345→refunded / 12346→human_review、子图引用真实保存图的父图运行（入参映射到达子图、父图引用子图产出）、http/request 对内置 mock 端点 GET 200/POST 插值/401 仍 SUCCESS/坏 params FAILED，database/query 打 demo SQLite 返两笔订单、execute INSERT 后 query 可见且 reset 复原、message/send 记录在 `/api/demo/messages` 可见且 reset 清空，模板库五模板加载/编译与 sql-query-notify（min_amount=1000 走有单分支）、approval-timeout-reject（10s 无人操作超时拒绝）、http-orders-branch（200 成功分支）三条实跑链路，以及录制 approval-timeout-reject 一次真实运行→回放秒回三节点全匹配（跨运行 token/消息 uuid/sent_at/resolvedBy/预置 approvals 归一化生效）→reset 后用例保留且回放仍匹配→Popconfirm 删除用例消失；能力项「多租户权限 v1」（新包 `iam/`：种子两租户四账号、三角色端点级 RBAC、进程内 token 会话、TenantRegistry 租户惰性分区，04 §5.14）亦已落地，全部业务端点 Bearer 鉴权、用户数据按租户全分区（跨租户对象 404、角色不足 403、未登录 401），前端登录页/会话/角色门 UI 齐备，后端 377 单元全绿、前端 vitest 64（10 文件）全绿，浏览器八场景实测登录/分区/角色矩阵/reset 范围/登出全通过（明细见 Quality gate）。2026-09-16 当日再落地 **08 Phase 2 延伸立项「设计态内核前置项」①–⑤**（docs/19 提案驱动：Capability input/output_schema JSON Schema 子集白名单与发现投影、前端拓扑作用域 ScopeIndex、L2 模板引用三错误码实时校验、DSL 编译期复查反绕过、NL 草稿参数非阻塞警告，零新依赖，后端 410/前端 87；①–⑤当日经 **PR #19 合并 main**（merge `6b8ce5a`；提案与 handoff 同步随 **PR #18** merge `27e331f`，三道门 CI 实跑全绿，详见 Recently shipped 18）。当日随后完成题述方案文档包：[docs/20](docs/20-题述方案工程化落地路线.md) 工程化落地路线（M1–M10，每个里程碑首行标注对应题面 A1–A6/B1–B6；§1.2 就绪度快照与推荐推进次序——**M1 可直接立项开工、M2 被待决 ADR T16 卡住、M3 依赖 M2、M4 依赖 M3、M5 需先补一轮持久化契约设计并拆 M5a/M5b、M8 是唯一不被 M5 阻塞的题二里程碑（M3 完成后可插队）**）、[docs/21](docs/21-题述原题与方案作答.md) 题卷+答卷合订（§1 原题照录、§2–§4 不含实现代码的方案作答、§5 脱离 Atlas 约束的绿地视角）、[docs/22](docs/22-题述方案绿地独立设计.md) 不受 Atlas 架构/技术栈约束的完整绿地方案（**非 Atlas 落码依据**）；[docs/10](docs/10-技术选型决策记录.md) §4 登记 **ADR T16（前端 L1 JSON Schema 校验器：手写最小子集 / AJV / 后端校验，M2 启动前必须收口；推荐手写起步但未拍板）**。该文档包共 7 个 docs 提交（`b0a1f2a`→`fc67668`）现已在 origin/dev；**M1/M2 均已落码并于当日随 PR #24 合并 main（见 Recently shipped 16/15）；M3 已于 2026-09-17 落码收口（ADR T17/U39 转正式，五提交 2de5053→6d4a863，已推送 origin/dev），**M4 批 1/批 2/批 3 已全部落码收口（T17 复查不重开，U40–U42 转正式，题一主线闭合，见 Active work 12 与 Recently shipped 11–13）；**M5–M10 已全部落码收口（M5a/M6/M5b/M7/M10/M8/M9，ADR T18–T22 均收口，题二主线 2026-09-18 全闭合，见 Recently shipped·i18n 第二批：Graph DSL 422 码化条目–5 与归档补遗）**）。
- **仓库**：git 仓库，**已推送 GitHub private 仓库 `bayernjf/atlas`**（main 为生产分支、dev 为集成分支，分支策略见 15 文档）。Phase 1 工作经 PR #3 于 2026-09-14 合并（merge `f069b53`，19 提交）；Phase 2 工作经 PR #4-#10 陆续合并——#4 condition、#5 loop、#6 parallel、#7 wait、#8 human_approval、#9 subgraph（2026-09-14）、**#10 API 适配器 http/request（2026-09-15，merge `4385a00`）**、**#11 DB/消息适配器 database/query|execute + message/send（2026-09-15 合并）**；PR 与合并后 main 的 GitHub Actions CI 均实跑全绿。流程模板库 v1（2026-09-15，6 提交 8f3238b 起）、操作录制 v1（2026-09-15，5 提交 4f7af04→857924f→7282cb2→19b39ee→42a82f3）、单步调试与断点 v1（2026-09-15，6 提交 2d7fa28→6693023→7e5a23a→5e13cdd→8d5bdd7→ecb9605）与基础监控告警 v1（2026-09-15，5 提交 2362fdf→c474a80→c721f47→91f6bcc→8551f90）、多租户权限 v1（2026-09-16，4 提交 7cbd509→3620545→64d34b3→7a2ed14，外加登录页 token/orientation 与 docs/17 补契约 b0bf5d4/5660718/2759a13）随后经 PR #12-#16 全部合并 main——#12 模板库（merge `9c849ff`）、#13 录制（`1e6475e`）、#14 调试（`ebffa12`）、#15 监控（`96a9d12`）、#16 多租户（`6bbd77c`）；另经 PR #17（merge `34304b5`）将 `.codegraph/` 加入 `.gitignore`；**#18 docs/19 题述提案 + handoff 同步（merge `27e331f`）与 #19 设计态内核前置项（merge `6b8ce5a`）亦于 2026-09-16 合并 main；M1/M2 全部提交（docs/20-23 文档包、T16、十提交 M1、五提交 M2）随 **#24 合并 dev→main（merge `0451d20`，origin/main 当前位置）**，各 PR 三道门 CI 均实跑全绿。**M5a–M10（含 M8 审批卡片、M9 Router 灰度与门控回滚）已全部落码并推送 origin/dev；2026-09-18 HEAD `1d4d2d8` 与 origin/dev 同步，dev 不再领先；主线 M1–M10 全闭合，后续按 docs/14 缓做项与 Phase 3/真实种子试用另行立项，立项前不动代码；**M11/C+A+B/docs/28 四批（含 A+B 六项）于 2026-09-20 经 PR #42 合并 dev→main（merge `1956379`），dev `1dcaea5` 已完全并入 main。****P0 上线阻断三批随后依次合并 dev→main：批 1 持久化 PR #46（merge `16dc32d`）、批 2 生产认证 PR #47（merge `95bfd84`）、批 3 真实接入安全准入 PR #48（merge `5a5976c`，2026-09-21，CI 三道门全绿）；现 origin/main=`5a5976c`、origin/dev=`8f8f0b0`（dev 保留为长期集成分支）。**
- **技术选型**：✅ 已全部收口（2026-09-13，T1-T5 见 10 文档 §3）：Python 3.11+ 引擎 / Go Harness 网关为产品化目标、**Demo 用 Python/FastAPI 实现同构接口** / TypeScript + React 19（满足 React 18+）/ LangChain+LangGraph / LiteLLM（Demo 对接主流商业 API，`.env` 切换，不引入 vLLM）/ Playwright / PostgreSQL+pgvector / Redis / **Demo 进程内事件总线替代 NATS** / FastAPI / `@xyflow/react` 12 + Zustand 5 + AntD 6 / 评估优化层仅留接口；剩余 ⏳（vLLM、策略训练、多语言）均为后续阶段范围。
- **Demo 依赖清单已落码**：后端在 `pyproject.toml`，前端在 `frontend/package.json` / `pnpm-lock.yaml`（版本为 2026-09-13 解析的稳定版）。
- **关键文件**：后端 `src/atlas/`（26 个包；engine 最小循环、graph DSL 契约+编译器（W9-W10 注入决策/适配器/事件）、llm 退款决策+NL 生成、shop Demo 店铺服务+适配器、httpapi 通用 HTTP API 适配器（httpx，单工具 http/request）、database 通用 SQL 适配器（database/query|execute，ATLAS_DATABASE_URL 白名单工厂，demo SQLite 回退）、message 进程内消息适配器（message/send 零真实投递）、template 内置只读流程模板目录（graphs.py 5 图 + catalog.py 元数据/访问器，无 DB/CRUD）、recording 运行录制与回放（cases.py pydantic 用例+进程内 store reset 不清除、replay.py 归一化/预置审批抽取/序列与逐节点比对）、debug 进程内调试会话与暂停控制器（sessions.py DebugSession/DebuggerBroker step/continue/stop 状态机、controller.py 统一执行器 before_node 钩子，会话级断点/条件 fail-safe/只读快照）、monitoring 进程内运行指标与告警（records.py ring+单锁 store、alerts.py 四规则 RuleConfig/evaluate_rules、metrics.py 节点失败两形状推断/分位聚合，04 §5.13）、iam 种子身份与租户分区（principals.py 两租户四账号/三角色矩阵、sessions.py 进程内 sess- token、registry.py TenantRegistry 按租户惰性装配服务、deps.py get_principal/require/services_for，04 §5.14）、api FastAPI（auth login/me/logout、graphs/SSE/NL/adapters/templates/recordings 五个录制端点/debug resume+列表/monitoring metrics·runs·rules+alerts 确认关闭/Demo 控制台/mock 端点/messages）、memory 连接层、harness 契约/注册、collaboration 审批 broker、web 三层定位适配器；**M5–M10 新增** `storage/`（七 Repository 协议 + memory 聚合 + PG 六 store/帧序列化/恢复扫描）、`versioning/`（不可变发布 + subgraph `@N` 钉版）、`coordination/`（任务信封 Envelope/TaskStore/退货退款沙盘 run_return_refund）、`skills/`（Skill/Bot/CoordinatorConfig）、`logistics/`（假物流 check/sign_receipt 适配器）、`tracing/`（M10，与 OTel 同形最小 Span/Tracer + contextvars + to_tree 折叠，纯 stdlib 零依赖）、`cards/`（M8，内置只读审批卡片目录 refund-approval + render_card web/im/email 三渠道渲染降级 + map_action_output）、`routing/`（M9，RolloutConfig 判别联合三段分桶 Router 纯函数 + RoutingStore per-tenant 灰度状态机 + gate 门控自动回滚））、`db/migrations/001_enable_pgvector.sql`，前端 `frontend/`（登录页/Dashboard/Editor/监控页 + 画布/节点/属性/变量面板 + `lib/` 纯逻辑（auth 会话与角色矩阵/变量/节点目录/序列化/适配器分组/录制事件转换/监控中文映射/API client 含 Bearer/SSE 与监控告警）+ Zustand store + vitest 单测）；模块填充顺序见 09 文档待填项清单。

## Active work / 待办

按优先级降序：
> **工程内可闭环的「下一批候选」已收敛到 [docs/08](docs/08-任务迭代计划.md) §八「下一批候选（open checklist）」**（2026-09-25 立；此前 §八只有落码日志、读不出还欠什么）。取项前先看那节的「别当待办」与「存疑待核」两段。

1. ✅ **W1-W2 基础骨架落地**（08 文档 7.1，2026-09-13 全部完成）：工程骨架/venv、LangGraph 最小 OODA 循环、前端编辑器框架、PostgreSQL+pgvector 连接均已跑通（详见 Recently shipped）。
2. ✅ **W5-W6 编辑器核心功能**（08 文档 7.1，2026-09-13 完成）：拖拽画布、3 种节点类型化配置 + 实时校验、全局变量与 `{{路径}}` 引用、Graph JSON 导出；vitest 20 个单测全绿。
3. ✅ **W7-W8 编译与运行**（08 文档 7.1，2026-09-13 完成）：Graph DSL 契约 + 静态校验、DSL→LangGraph 编译/运行、FastAPI graphs 保存/读取/编译/运行、编辑器一键"编译并运行"；后端新增 16 个用例。
4. ✅ **W9-W10 端到端 Demo**（08 文档 7.1，2026-09-13 完成）：电商退款完整链路跑通（LiteLLM 决策 + 规则兜底、shop 退款业务能力、SSE 实时进度、NL 退款草稿、模拟商家控制台），08 §7.3 七条验收逐条达成（映射见 08 W9-W10 落码记录）。
5. ✅ **Phase 2 全部能力已落地并合并 main**（2026-09-14~16；六类节点 condition/loop/parallel/wait/human_approval/subgraph + 三适配器 httpapi/database/message + 模板库/录制/调试/监控/多租户 v1，PR #4–#16；设计态内核前置项①–⑤ PR #19）。详细过程见 archive #20–#33 与 Current state。
6. 文档缺口登记：`09-工程骨架` 待定项 1/2/3/4/5/6 均已收口（待定项 3：不新增 deployment/ 包，Dockerfile + docker-compose.yml 放仓库根，2026-09-13）。新增测试运行器选型 vitest 已记 10 文档 §4 ADR（W5-W6）。
8. ✅ **Phase 1 种子客户交付准备（工程侧）**：Docker Compose 一键启动 + [docs/18](docs/18-种子客户验证计划.md) + [TRIAL.md](TRIAL.md) + 应用内反馈入口（2026-09-13）；真实招募/回填在沙盘假定下标记完成，真实试用启动按 18 文档执行。
9. ✅ **D10a CI 质量门**（2026-09-14）：`.github/workflows/ci.yml` 三道门 gitleaks + 后端 pytest + 前端 oxlint/vitest/build，main/dev 与 PR 触发；CD 留 14 D10b。
10. ✅ **D9 基准**（2026-09-14）：`scripts/benchmark.py` + [BENCHMARK.md](BENCHMARK.md) 首份基线（commit 407812e，OODA 2.05ms / 编译 1.81ms / 退款端到端 2.28ms）。

11. ✅ **M5 契约设计轮与 M5 子阶段**（2026-09-17 收口＝[docs/24](docs/24-M5持久化与中断恢复契约设计.md)，T18 拍板 B；M5a `0358696` / M6 `1f49644` / M5b `f6a33e7` / M7 `e46fc09` 均已落码）。详见 archive #6–#12。


12. ✅ **工程主线：题一/题二 M1–M10 全闭合**（2026-09-18）：M1/M2 随 PR #24 合并 main、M3/M4 题一闭合（U39–U42 转正式）、M5a/M6/M5b/M7/M10/M8/M9 题二闭合（ADR T18–T22 收口，U43–U59 转正式）。M1–M7 详细过程见 archive #6–#18，M9 见 Recently shipped·打包 E 收口条目、M8/M10 见归档补遗（D26 报告 v1 见 4；D26/D30 收尾批见 2、Active 17；A+B 批见 1、Active 18）。Phase 3 与真实种子试用后置，触发条件见 14 文档。

**Active feedback**：暂无（种子试用开始后，由 docs/18 §6.1 跟进项迁移至此，解决后留痕 Recently shipped；流程见 16 文档）。

13. ✅ **M10 span 级链路追踪**（2026-09-18，立项 `ba9e0d2` + 四批 `1f21b1a`/`89f5645`/`c227f0c`/`2b1dbe9`，ADR T21，U49–U52 转正式）。详细过程 2026-09-19 按 5 条上限滚出，见 docs/handoff-archive-2026-09-18.md「2026-09-19 补遗·原主文件 #5」。

14. ✅ **M8 交互模板系统（审批卡片双向 Schema + web/im/email 三渠道渲染降级）**（2026-09-18，立项 `e994ac5` + 三批 `2b35fc9`/`5f4384e`/`4b45571`/`25afa10` + 收口 `94000de`/`486a591`，U53/U54 转正式）。详见归档补遗（2026-09-19 第二批滚出原 #5）。
15. ✅ **M9 入站 Router + 灰度发布 + 指标门控自动回滚**（2026-09-18，立项 `ce57355` + 批 1–4 + 总收口，ADR T22，U55–U59 转正式；m9_smoke 32 断言、Router p50 0.0041ms、浏览器 0 console error）。详见 Recently shipped·打包 E 收口条目。
16. ✅ **D26 用例集报告 v1（批量回放报告沉淀 + 通过率历史趋势）**（2026-09-18 用户拍板提前取回、立项当日落码收口；立项 `71153a8`、后端 `9dc9690`/`5c812c5`、前端 `f5c789c`，U60 转正式；后端 615 passed/前端 404 passed，d26_smoke 24 断言、真实浏览器 6 条历史趋势 + 展开懒加载详情冒烟、2 截图）。不解除 D26（影子/Mock/参数化/subgraph 快照/PG 多租户等仍缓做），详见 docs/handoff-archive-2026-09-19.md（原 #5）。
17. ✅ **D26/D30 收尾小项打包（报告导出 + 子图快照内联 + 定时回放 CI + GRAPH_DATA_CYCLE + REF_TYPE_MISMATCH）**（2026-09-19 用户拍板「搞一口气能搞定的」，立项 `d4cd42b` + 五项原子 `29bb3d9`/`8a37b4e`/`cea70f4`/`9a357df`/`47ecf48`，U61–U65 转正式；后端 628 passed/前端 418 passed，d26_smoke 24 断言）。不解除 D26/D30，不新增 ADR；本批 6 个 commit 默认未 push。详见 Recently shipped·i18n 第一批：认证/L1 英文态条目。
18. ✅ **A+B 打包：Phase 2 运行时语义三项 + D30 作用域余部三项（D15 表达式函数库 / D17 loop break·continue / D18 parallel any_success；parallel.result 汇聚点可见性 / subgraph.outputs 深层展开 / 改节点 id quickFix 联动）**（2026-09-19 用户拍板「B和A」六项全做、三处语义分叉均取推荐项；立项 `646ca79` + 六原子 `ac8c718`/`ee534ef`/`25db6df`/`8496b2d`/`5f87654`/`f45daa9` + build 修复 `4b9396e`，U66–U71 转正式；后端 645/前端 436、d26_smoke 24 断言零回归）。不解除 D15/D17/D18/D30，不新增 ADR；本批 8 commit 默认未 push。详见 Recently shipped·i18n 第二批：Graph DSL 422 码化条目。

19. ✅ **M11 记忆 / 长期上下文——四批全部落码收口（2026-09-19 用户拍板「一口气搞，M11 也搞」）**（契约 [docs/26](docs/26-M11记忆长期上下文契约设计.md) 为形状权威、ADR T23、08 M11 立项条、14 D35 非目标；文档矩阵 03/05/09/12/13/14/00 已同步）。四决策用户已拍板：① EmbeddingProvider 抽象 + 沙盘本地确定性 embedder（EMBED_DIM=256、signed hashing + 中文 unigram/bigram、纯 stdlib、离线回放确定，商业 embedding 缓做）；② 首批层级仅 fact+preference（working 由 run outputs/globals 承担，summary/case 缓做）；③ 图接入用 `memory/remember`(WRITE)+`memory/recall`(READ) 两适配器工具，零新节点类型、零 DSL/编译器改动；④ 进程内 MemoryStore + PG/pgvector 两档都做（第九个 MemoryRepository、迁移 `006_memory.sql`）。**落码分四批已全部完成**（批 1 进程内内核 U72–U83 / 批 2 适配器+REST U84–U92 / 批 3 PG+integration U93–U96 / 批 4 前端记忆页 U97–U99），每批过三道门（只许增测）。REST 按租户 `GET /api/memories`、`GET /api/memories/search`（viewer+）+ `DELETE /api/memories/{id}`（admin），不开 POST/PUT 写入；reset 清空、跨租户 404。**2026-09-19 用户拍板"一口气搞"开工**：批 1 进程内内核已落（`5441902`：memory/models.py+embeddings.py+items.py、storage/base.py 第九个 MemoryRepository Protocol、storage/memory.py 聚合，U72–U83 共 18 测；后端 663 passed/13 skipped；实测注记：signed hashing 无共享 token 时碰撞可致小负余弦 |c|<0.2、min_score=0 默认过滤，收口回填 docs/26 §3.2）；批 2 适配器+REST 已落（`58d936c`：memory/adapter.py remember(WRITE)/recall(READ)、loader.py GENERIC_JSON_ADAPTERS 加 memory〔关键坑：否则 tool_call 走旧专用通道 params 不透传〕、registry 注入、main.py 三 REST 端点、scripts/dev/m11_smoke.py，U84–U92 共 10 测；真实服务 m11_smoke 全过、recall score=0.434524）；批 3 PG/pgvector 已落（`73e53bd`：迁移 006 memory_items vector(256)+ivfflat lists=100〔空表建索引实测成功未降级〕、PgMemoryStore 裸 SQL 用 CAST(:q AS vector(256))〔SQLAlchemy text() 中 :p::type 与绑定参数冲突〕、registry pg 档切换、test_memory_pg_integration U93–U96 两档对拍 score 容差 1e-2；后端 673 passed/17 skipped〔含 4 新 PG 集成〕、PG 档真实服务 m11_smoke 全过 mem-78 score=0.434524 两档一致；两处落码偏离 docs/26 草稿待收口回填：created_at 用 TEXT 非 timestamptz 对齐 002、强转用 CAST 而非 ::）；**批 4 前端记忆页已落（`75c4150`：apiClient listMemories/searchMemories/deleteMemory、lib/memory.ts 纯函数、pages/Memory.tsx 语义搜索 score 进度条+kind 筛选+admin Popconfirm 删除+viewer 无操作列+顶部「本地词法向量非真实语义」提示、Dashboard/App 入口，U97–U98 共 13 vitest；U99 真实浏览器两角色冒烟全过——列表 fact 蓝/preference 紫、搜索顺丰 57%＞邮件 35% 降序、筛选、删除 4→3、viewer 无删除列，3 截图 docs/assets/m11-memory-*.png，零应用错误）**。**M11 收口完成**：最终门后端 673 passed/17 skipped（给 DATABASE_URL 时 PG 套件 9 passed）、前端 449 passed/2 skipped/37 文件、tsc+vite build 过、oxlint 0 error（基线 2 warning）、m11_smoke 进程内档与 PG 档全过（recall score 0.434524 两档一致）、d26_smoke 24 断言零回归；U72–U99 转正式；两处落码细化（signed hash 小负余弦、created_at TEXT/CAST）已回填 docs/26 顶部落码注记与 §3.2/§4.4，03/08/09/12/13/14/00/CHANGELOG/README 已同步；零新依赖、不新增 ADR、不解除 D35。详见 Recently shipped·打包 F 收口条目。
20. ✅ **进程内运行体验补强 C+A+B 打包——C（`3415377`）/A（`e594a4b`）/B（`f9a1301`）三包全部落码收口（2026-09-19 立项、B 于 2026-09-20 收口；形状权威 [docs/27](docs/27-进程内运行体验补强契约设计.md)、08 立项条）**。用户拍板（docs/27 §0）：**D（D30 输出键〔非节点 id〕改名）判空集、本轮不落码**（勘察确认 v1 产出键 result/target/index/iterations/result 全由节点类型固定、节点 schema 无自定义输出键/别名/映射、B3 `8496b2d` 已覆盖改节点 id，无作用对象，仅在 14 D30 登记随命名输出特性启用）；**A、B 取进程内 v1 形态**（不碰 D19/D20/S1 持久化、多实例、断点落图/团队共享、IM/邮件通知）；**C 直接做**。三包范围：**C（D15 余部）** run_graph `now_override`（单次运行固定 UTC）+ `today()`/`now()`/`datetime()`/`hoursBetween` 前后端 conditions 同构（禁 eval）+ `RecordingCase.recorded_at` 录制 baseline 与 replay/gate 共享冻结锚点（旧用例回退 created_at）；**A（D21 两项，进程内）** 子图重入不再 `emit=None`，内部 node_start/node_end 以可选 `subgraphPath`（每层父图 subgraph 节点 id）上 SSE、吞掉子层 run_end 防误判终帧、子图内 human_approval 审批 payload 上屏后复用共享 ApprovalBroker + 现有审批 Modal/决策端点（无新端点），前端日志路径前缀 + 画布高亮父 subgraph 节点；**B（D27 三项，进程内）** 运行中急停＝节点边界协作式取消（新增 RunCancellationBroker + `POST /api/runs/{id}/cancel` + run_graph `is_cancelled` + SSE cancelled 帧/状态 cancelled，普通与调试统一、不在 wait/approval/tool 阻塞中点强杀）、断点 hitCount（每 N 次命中暂停）+ logpoint（命中只发 debug_log 不暂停）、暂停时 resume 带 globals 顶层键白名单浅合并写回续跑（帧仍只读、stop 忽略覆盖）。**显式非目标**：子图内单步断点、持久化中断/多实例调试、断点落 Graph JSON、变量变化历史、异常断点、嵌套可视化编辑器、命名输出新特性、命名时区/随机/UUID。零新依赖、不新增 ADR、不解除 D15/D21/D27/D30。**原子序**：① 立项（本提交 27+08+14+00+handoff+CHANGELOG）→ ② C `feat(conditions): injectable clock and today/now datetime functions` → ③ A `feat(graph): stream subgraph node events and surface in-subgraph approvals` → ④ B `feat(debug): cooperative cancel, hit-count/logpoint breakpoints, variable overrides` → ⑤ 收口（三道门 + m11_smoke 两档 + d26_smoke 24 断言 + 录制回放黄金用例零回归 + 浏览器冒烟截图 + 03/04/06/09/12/13/14 回填 + docs/27 顶部落码注记）。测试编号 U100 起（C U100–U113、A U114–U123、B U124–U138，候选以 13 落码为准）；三道门基线后端 673 passed/17 skipped、前端 449 passed/2 skipped/37 文件，A/B/C 动运行/条件/SSE 核心，每批后必跑 d26/m11/录制黄金用例与 A1/A2/A3 用例防回归。**【进度 2026-09-19】原子序② C 已落码收口**（单原子 `3415377` feat(conditions)：后端 687 passed/18 skipped、前端 453 passed/2 skipped、内存档 d26 与两档 m11_smoke 全过，U100–U113 转正式；两处落码细化＝recorded_at 锚点取入库时刻、前端 conditions 仅静态校验，一处既有缺口＝PG 档 recordings 富字段 graph_id/subgraphs 历来 500 归属 D26，均见 docs/27 顶部 C 落码注记；详见 Recently shipped·打包 F 收口条目）。**③ A 已落码收口**（单原子 `e594a4b` feat(graph)：后端 695 passed/18 skipped〔+8〕、前端 457 passed/2 skipped/38 文件〔+4〕，U114–U123 转正式，真实 HTTP SSE 端到端＋真实浏览器子图审批冒烟过、2 截图，无新端点；两处落码细化＝嵌套路径在最深层 wrapper 一次性构造〔已带 path 不覆盖〕、内部节点不写父图状态而由父 subgraph 节点自身无 path 事件驱动高亮〔订正 §3.3 映射设想〕，见 docs/27 顶部 A 落码注记；详见 Recently shipped·打包 G 五项落码收口条目）。**【进度 2026-09-19】④ B 后端已落码、测试全绿（工作区已改未提交，前端待续）**：新建 collaboration/cancellations.py（RunCancelled＋RunCancellationBroker register/cancel 幂等/unregister/reset，挂 TenantServices 两档＋reset）；debug/sessions.py 重写（BreakpointSpec expression/hit_count/log_message、hit_counts、_Pause.overrides、apply/take_overrides 标识符＋JSON 可序列化校验、Broker.create 解析 hitCount/logMessage）；debug/controller.py 重写（注入 is_cancelled、调试流急停折叠 DebugStopped、_classify_hit 三态 step/pause/log、bump 计数、logpoint 发 debug_log 不暂停、hitCount 取模、resume 浅合并写回 globals、stop 忽略）；graph/loader.py 全链透传 is_cancelled（_make_executor/compile_graph/run_graph/_execute_subgraph，普通流节点边界抛 RunCancelled、子图 fail-safe 前 re-raise 防折叠）；monitoring records/metrics（status 加 cancelled、is_healthy 认 completed/cancelled 不刷失败）、storage/base finish Literal 加 cancelled、api/main.py（_validate_debug 校验透传 hitCount/logMessage、stream worker 请求线程 register/finally unregister、controller 与 run_graph 注入 is_cancelled、catch 序 DebugStopped→RunCancelled→Exception、SSE event:cancelled、新端点 POST /api/runs/{id}/cancel operate〔200 含重复幂等/404/409〕、ResumeDebugRequest.globals＋resume apply_overrides 422、GET /runs 白名单加 cancelled）。新增 tests/test_run_cancel.py 17 测（U124–U133 后端：broker/线性·parallel·子图穿透取消/logpoint/hitCount/条件计数/globals 续跑分流/覆盖校验/REST 200·404·409·403/调试折叠 stopped/422）。**后端全量 712 passed/18 skipped**（A 基线 695＋17，零回归）。**【进度 2026-09-19】④ B 前端已落码、三道门全绿（工作区已改未提交，浏览器冒烟/双 commit 待续）**：apiClient.ts（DebugBreakpoint 加 hitCount?/logMessage?、新增 DebugLogFrame/CancelledFrame 帧与 RunCancelledError、RunEvent 联合扩两帧、resumeDebug 加 globals? 参〔不带则请求体不含 globals〕、新增 cancelActiveRun(graphId)〔GET /api/runs?limit=50 找本图 running/suspended 最新 run→POST cancel，无在途返 null〕、streamRun 解析 cancelled 帧回调后抛 RunCancelledError、debug_log 走通用 onEvent）；editorStore.ts（Breakpoint 类型加 hitCount?/logMessage?、新增 setBreakpointHitCount〔null/<1 归一 undefined〕/setBreakpointLogMessage〔空白归一 undefined〕两 setter 及接口签名）；PropertyPanel.tsx（断点启用块内新增 hitCount InputNumber〔min1 留空每次〕与 logMessage Input〔非空即日志断点〕两 Field，含 v1 不组合/不插值说明——配置入口在属性面板而非 DebugConsole）；Editor.tsx（debugRequest 组装透传 hitCount/logMessage、事件回调加 cancelled 复位＋debug_log 经 appendLog 带 subgraphPath 前缀显示、catch 加 RunCancelledError 分支不 setRunError、Header running 时显 danger「急停」按钮 handleEmergencyStop〔调试暂停中走 resume stop、否则 cancelActiveRun〕、暂停工具条 globals 由只读 pre 改为可编辑 TextArea＋JSON/对象/标识符键名校验＋step/continue 浅合并提交、stop 不带 globals、outputs 维持只读）；新增纯函数 `lib/debugOverrides.ts`（parseGlobalsDraft，规则与后端 apply_overrides 对齐，空键名用 !==undefined 判 falsy 坑）。新增前端测试 15 个（debugOverrides 6、editorStore setter 3、apiClient cancelled/debug_log/resume globals/cancelActiveRun 6，U134–U137 逻辑层；U138 outputs 只读与按钮态由声明式 JSX＋构建＋浏览器冒烟覆盖）。**前端全量 472 passed/2 skipped/39 文件**（A 基线 457＋15，零回归）、pnpm build 过（仅既有 chunk>500kB 提示）、oxlint 0 error/2 warnings（ReleaseModal/RolloutModal 既有基线，未新增）。**【进度 2026-09-20】④ B 已全部落码收口**：feat 原子 `f9a1301` feat(debug)（18 文件 +1349/-42：后端新建 collaboration/cancellations.py 与 tests/test_run_cancel.py 17 测、重写 debug/sessions.py·controller.py、loader.py 12 处 is_cancelled 透传、main/monitoring/storage/iam 接线；前端 apiClient/editorStore/PropertyPanel/Editor＋新建 lib/debugOverrides.ts 与 15 测）；后端 **712 passed/18 skipped**、前端 **472 passed/2 skipped/39 文件**、pnpm build 过、oxlint 0 error/2 既有 warning；真实 HTTP SSE 端到端三场景（wait 阻塞中 cancel→200 并于下一节点边界 cancelled/logpoint 发 debug_log 不暂停/globals 改值 condition 分流，临时 catalog 注入跑完 `git checkout` 还原净零）＋真实浏览器三场景冒烟全过、4 截图 docs/assets/b-cancel.png/b-logpoint.png/b-globals.png/b-globals-edit.png；U124–U138 转正式。docs 回填（docs/27 顶部 B 注记、03、04 §5.12、06 §6.10、09、12 §5、13、14 D27＋handoff/CHANGELOG，4 截图随 docs commit）随 B docs 原子提交。零新依赖（纯 stdlib threading.Event）、不新增 ADR、不解除 D27（持久化/多实例随 D19/D20/S1、断点落 Graph JSON/团队共享、变量变化历史、异常断点、子图内部断点、阻塞中点强杀、多会话锁粒度仍缓做）。**C+A+B 三包至此全部收口**。
21. ✅ **总收口（C+A+B+M11 后全量回归，2026-09-20 收口）**：全绿——后端 `.venv/bin/pytest` **712 passed/18 skipped/1 warning**；前端 `pnpm test` **472 passed/2 skipped/39 文件**、`pnpm build`（tsc -b && vite build）过（仅既有 chunk>500kB 提示）、`pnpm run lint`（oxlint）**0 error/2 warnings**（ReleaseModal/RolloutModal 既有 set-state-in-effect 基线）；`scripts/dev/m11_smoke.py` 内存档与 PG/pgvector 档均 **M11_SMOKE_ALL_PASS**（两档 recall score=0.434524 一致、跨租户隔离/删除/reset 通过）；`scripts/dev/d26_smoke.py` 内存档报告 v1 全过（manual/publish-gate 两触发沉淀、倒序历史、详情 cases、跨图 404、reset 清报告保留用例）；A1/A2/A3 防回归单测在全量内零回归（`tests/test_graph_loader.py` 的 `test_parallel_any_success_short_circuits_unstarted_nodes_d18_a1`/`..._failed_only_when_all_branches_fail_d18_a1`、`test_loop_break_via_body_condition_exits_with_break_reason_d17_a2`/`..._validates_d17_a2`，`tests/test_conditions.py`/`tests/test_clock.py` 算术·时钟函数）。**总收口抓到并修复一处真实回归**：B 新增 resumeDebug globals 测试的 fetch mock 未声明参数，vitest 经 esbuild 不做类型检查故全绿，但 `tsc -b` 报 TS2493/TS2352/TS6133 致 build 红；已补 mock 参数类型（`async (_url: string, _init?: RequestInit)`、读取改 `init?.body`）修复，单原子 `b521f5a` fix(debug)，修复后 build/lint/test 复跑全绿。本批 commit：B feat `f9a1301`、B docs `d265203`、tsc fix `b521f5a`。已知非回归缺口（不阻塞、随 D26）：PG 档 recordings 富字段 graph_id/subgraphs 未持久化、POST /api/recordings 500（HEAD `5e677ce` 即如此，d26_smoke 因此仅内存档全过）。**本批已随 dev 推送并经 PR #42 合并 main（merge `1956379`）。**

22. ✅ **工程内可闭环缓做项 11 项打包——四批全部落码收口（2026-09-20，#32–#36 完成；用户拍板「能一起推进的话，一起搞了」）**。形状权威 [docs/28](docs/28-工程内可闭环缓做项打包契约设计.md)（10 章：§0 已拍板边界/§1 范围表与非目标/§2–§5 四批设计/§6 REST·SSE·Schema 汇总/§7 前端落点/§8 原子序/§9 契约同步矩阵/§10 风险），立项条见 08（2026-09-20），14 已对 D21/D26/D27/D28/D35 五行加部分取回注记（不解除）。**四批 11 项**：批 1 D26＝①迁移 `008_recording_graph_subgraphs_tool_calls.sql` 修 PG 档 POST /api/recordings 500（recordings 加 graph_id/subgraphs、PgRecordingStore 四读写路径＋Repository 协议同步；批 3 的 monitoring_runs.tool_calls 同批 DDL）②Mock 工具响应回放（tool_mocks 键＝节点 id、值＝录制原始 output；仅单用例 replay body `{mock_tools?,inputs_override?}`，发布门禁不接、子图不传、桩不发 span/metric）③PUT /api/recordings/{id} 仅 name/inputs 可改＋回放入参浅合并 ④GET /api/release-reports 跨图看板（ReportStore 进程内 ring 不 PG 化）；批 2 D27＝⑤暂停帧变量变化历史（易失上限 50、相邻暂停 global 顶层键 diff、resume 改写同步基线不入历史）⑥异常断点（onException，命中 reason=exception 帧＋error，resume/continue 后原样重抛、不做忽略继续）⑦子图内部断点（controller 透传子层重入、_namespaced_emit 放行 paused/debug_log、终帧仍吞）；批 3 D28＝⑧tool_metric 适配器调用埋点（三出口 monotonic 计时、SIMULATED 照记、mock/子图不发、RunRecord.tool_calls＋metrics tool 段 p50/p95/错误码、同步 /run 造收集 emit、顺手订正 record_run 协议签名漂移）⑨自定义告警规则 DSL（RuleConfig.custom 缺省 []、复用 graph.conditions 白名单求值禁 eval、上下文 status/durationMs/failedCount/hasError 不含 graphId、结果必须 bool/异常 fail-safe、rule_id=custom:{cid}、Alert 加 rule_name、PG config JSONB 自动兼容无 DDL）；批 4＝⑩D35 记忆 POST/PUT（operate、source=manual、两档 store update 整体过校验、content 变重算 256 维 embedding、删除维持 admin）⑪D21 versioning/upgrades.py 升级体检（GET /api/graphs/{id}/subgraph-upgrades read、ReleaseModal 只读区、不阻断发布、v1 只扫顶层、不做草稿 pin 编辑器/不检测 dirty）。**原子序**（docs/28 §8）：立项 docs-only（本原子，28+08+14+00+handoff+CHANGELOG）→ 批 1 fix(storage)→feat(recording)×2→前端 → 批 2 feat(debug) history→exception→subgraph→前端 → 批 3 feat(monitoring) metrics→DSL→前端 → 批 4 feat(memory)→feat(versioning)→前端 → 总收口。测试编号 U139 起（批 1 U139–U149、批 2 U150–U162、批 3 U163–U174、批 4 U175–U186，落码在 13 定稿）。**验收硬点**：迁移 008 手动应用 atlas-pg 后 ATLAS_STORAGE_BACKEND=pg 跑 d26_smoke.py 24 断言全过（现状仅内存档全过）、内存档不回归；三道门基线后端 712 passed/18 skipped、前端 472 passed/2 skipped/39 文件、build 过、oxlint 0 error/2 既有 warning（只许增测）；收口另跑 m11_smoke 两档＋真实浏览器冒烟（PG 录制/Mock/编辑、调试三特性、记忆新建编辑、升级体检 UI）。**当前进度**：立项 docs-only `b0d8023`（hash 回填 `f133829`）。✅ **批 1 D26 四项已落码收口（#32）**——`d1f455b` 迁移 008 修 PG recordings 富字段（graph_id/subgraphs，PG「录为用例」500 缺口闭合，monitoring_runs.tool_calls 列同批预留批 3）、`c7bf138` 单用例 replay 可选 body {mock_tools,inputs_override}＋响应 mocked_tools（门禁不接、子图不传）、`89e21fc` PUT 用例 name/inputs 编辑＋GET /api/release-reports 跨图看板、`0e8ebe7` 前端 Modal Mock/覆写/编辑＋监控跨图 Card；U139–U148 转正式（净增 8 非 integration＋2 PG integration）；三道门后端 **722 passed/20 skipped**、PG integration **8 passed**、前端 **472 passed/2 skipped**＋build 过＋lint 0 error/2 既有 warning，d26_smoke 内存·PG 两档 24 断言全过、m11_smoke 两档 ALL_PASS、PG 档新端点 HTTP 冒烟 14 项全过、真实浏览器三场景全过（跨图看板/编辑面板/Mock 回放，3 截图 `docs/assets/batch1-{cross-graph-board,edit-panel,mock-replay}.png`）；契约回填 docs/28 顶部注记、03、12 §5、13、14 D26、CHANGELOG＋本文件。✅ **批 2 D27 三项已落码收口（#33）**——`038dcaa` 变量变化历史（易失上限 50、JSON-stable 基线防 Lock、resume 改写 seed 基线不入历史）、`9b378c2` 异常断点（onException、reason=exception＋error 帧、resume 原样重抛不忽略、纯异常 before 早退防误停、非布尔 422）、`f6f7d30` 子图内部断点（controller 透传子层重入、paused/debug_log 附 subgraphPath、终帧仍吞、DebugStopped 穿透不折叠 failed，旧 is_not_paused 测试反转为 is_paused_with_path）、`c13886c` 前端（apiClient 类型/editorStore.toggleBreakpointException/属性面板异常 Switch/暂停 Card 历史 Collapse·红 Alert·geekblue 子图路径 Tag）；U150–U165 转正式（净增 16＝history 7/exception 5/subgraph 4，前端 +1）；三道门后端 **736 passed/20 skipped**、PG integration **8 passed**（批 2 无 DDL，沿用迁移 008）、前端 **473 passed/2 skipped**＋build 过＋lint 0 error/2 既有 warning，d26/m11 双 smoke PG 档全过、httpx 并发 SSE 端到端验证子层 subgraphPath 与异常 error、真实浏览器三场景全过（异常断点/变量历史/子图内部暂停，3 截图 `docs/assets/batch2-{exception-breakpoint,variable-history,subgraph-pause}.png`；临时模板 zz-smoke-subgraph 与坏订单 BAD-01 已 `git checkout` 还原、后端用还原代码重启、模板恢复 5 内置项）；契约回填 docs/28 顶部注记＋§3.4、03 debug_session、04 §5.12（三处旧口径反转）、12、13、14 D27、CHANGELOG＋本文件。✅ **批 3 D28 两项已落码收口（#34）**——`a318d97` 适配器调用埋点（records 新增 ToolCallMetric/RunRecord.tool_calls 纯超集、loader 真实工具分支 monotonic 计时经 `_tool_metric_event` emit tool_metric〔mock 不发/子图内被命名空间白名单吞/debug 发而不采/回放门禁不采、只采顶层无 subgraphPath〕、两真实运行入口收集、PG monitoring_runs 第 13 列 tool_calls JSONB〔迁移 008 已含、无 DDL〕并顺手订正 PgMonitoringStore.record_run 签名漂移 status 补 cancelled/补齐 trace_id·resolved_version·business·tool_calls、metrics 新增 summarize_tools 按 tool 聚合 calls/failed/simulated/error_codes/nearest-rank p50·p95〔SIMULATED 纳 calls/simulated 不纳 failed 与分位、真实样本 0 分位 null、未注册工具 error_code=null〕）、`11b1ba7` 自定义告警规则 DSL（RuleId 放宽 str、CustomRule/RuleConfig.custom 缺省 []/Alert·AlertEvent 加 rule_name、validate_rules custom 段中文聚合校验 cid 非空·≤64·唯一/name 非空·≤50/severity 枚举/expression 经 graph.conditions 静态校验且顶层须布尔、evaluate_rules 末尾 `_evaluate_custom_rules` 以扁平白名单上下文 {status,durationMs,failedCount,hasError}〔不含 graphId〕逐条 enabled 调 conditions.evaluate_expression、非 True 不告警/任何异常 fail-safe 不阻塞、命中 rule_id=custom:{cid}；**关键坑**条件引擎有序比较严格同型、duration_ms 是 float 对整数字面量会被 fail-safe 吞，故 durationMs 取 int(round)；**PG alerts v1 不持久化 rule_name、读回 null 前端回退 rule_id，custom 规则随 monitoring_rules.config JSONB 承载无 DDL**）、`4256326` 前端（apiClient tool_metric 帧/ToolCallMetric·ToolMetricsRow·CustomRuleConfig 类型/RunRecord status 补 cancelled·tool_calls、monitoring.ruleLabel 三级回退 rule_name→内置映射→rule_id、Monitoring 页「适配器调用」Card＋自定义规则 List 编辑器〔name/expression monospace/severity Select/enabled Switch/删除＋新增、cid=crypto.randomUUID()、saveRules 前逐行 validateExpression 非法禁提交〕）、`dc46f20` scripts/dev/d28_smoke.py（纯 HTTP PG 档：好工具 SUCCESS/坏工具 FAILED、tool_calls 落库、metrics.tools 聚合、PUT custom 往返、非法表达式 422、坏运行命中 custom:{cid} critical）；U166–U191 转正式（埋点 test_monitoring_tool_metrics 10、DSL test_monitoring_custom_rules 12 函数/18 例、PG integration +2＝test_tool_calls_roundtrip/test_custom_rules_roundtrip、前端 +2 conditions DSL 白名单/ruleLabel 回退；另反转 test_tracing_loader 白名单、更新 test_refund_e2e 黄金序）；三道门后端 **764 passed/22 skipped**（净增 28）、PG integration **10 passed**（+2，批 3 无 DDL）、前端 **475 passed/2 skipped/39 文件**（+2）＋build 过＋lint 0 error/2 既有 warning，d26/m11/d28 三 smoke PG 档全过、真实浏览器两场景全过（适配器调用 Card/自定义规则编辑器，2 截图 `docs/assets/batch3-{tool-metrics,custom-rules}.png`）；契约回填 docs/28 顶部注记＋§4.3、03 monitoring、04 §5.13（批 3 blockquote＋缓做边界反转）、06 §6.11、09 records/metrics/alerts 注释＋两处模块映射、12 §3.9＋REST 6 行、13 U166–U191、14 D28、CHANGELOG＋本文件；零新依赖/不新增 ADR/**不解除 D28**（OTel 正式栈/长保留时序/Trace 钻取/子图·Mock 埋点/规则图级绑定·模板市场·静默升级/外部出口仍缓做）。**批 4 已落码收口（#35 完成，2026-09-20）**：三原子 `ec0fd81` feat(memory) REST create/update（source 固定 manual）→ `ba97088` feat(versioning) 子图升级体检端点 → `19f31b4` feat(frontend) 记忆表单＋体检区；测试 U192–U211（记忆手动写入 U192–U201、升级体检 U202–U211），前端 vitest 净增 7。三道门最终：后端 782 passed/24 skipped、PG integration 全量 24 passed（首跑 2 failed 为共享开发库被旧 uvicorn 连接的瞬时跨文件时序污染、单文件复现正常、复跑全绿，非批 4 回归）、前端 482 passed/2 skipped/39 文件、build 过、lint 0 error/2 既有 warning；d26/m11/d28 三 smoke PG 档全过；记忆 POST/PUT/权限矩阵与升级体检 curl 实测全过（operator POST 201/DELETE 403、viewer POST·PUT 403、跨租户 GET 404）；真实浏览器两截图 docs/assets/batch4-{memory-edit,subgraph-upgrades}.png（graph-378 经 React fiber 对指定 graphId 打开 ReleaseModal，未点发布不产版本）。**#36 总收口完成**：docs 回填原子覆盖 docs/28（顶部收口注记＋新增 §5.3 落码注记＋§8.2 编号定稿）、03、12、13、14、26、09、04、06、00、CHANGELOG、handoff，两张 batch4-*.png 随 docs 原子入库。v1 边界：记忆仅白名单字段/source 强制 manual/删除仍 admin/无字段级历史审计；升级体检只扫顶层/不阻断发布/不做 pin 编辑器/不检测子图草稿 dirty。**批 4 三原子与 docs 原子已随 dev 推送并经 PR #42 合并 main（merge `1956379`）。工程内可闭环缓做 11 项已全清；下一步候选：i18n（触发条件＝首个英文使用者，未到；设计 token 已落码）或进入 Phase 3 真实种子试用准备。**
7. 📋 **i18n 与设计 Token 方案已定（2026-09-13，docs/17）**：设计 Token 等价替换**已落码**（`frontend/src/theme/tokens.ts` 单一事实源 + `setup.ts` 注入 `--atlas-*` 变量 + AntD theme，三页面主体零视觉差异；2026-09-16 17 §3.5 收尾清单五项已全部清零，`frontend/src` 仅 tokens.ts primitive 定义含 hex，浏览器逐项核对）；i18n 库（i18next+react-i18next）按触发条件引入（14 D12：首个英文使用者/出海需求），组件描述多语言随 Phase 2 模板库（14 D13）。**〔2026-09-25 口径更新〕本条的两个触发条件都已过去或被替代：en-US 全量翻译＋语言切换 UI＋AntD locale 联动已由 docs/57（打包 D，2026-09-24）落码，且该批明文拍板**继续零依赖自写 `t()`、不换 i18next**（776 键四批抽取验证自写实现够用，i18next 独有的复数/懒加载/Intl 无需求；组件签名不变，未来要换零返工）；组件/模板描述多语言仍未做，但触发条件已从"随 Phase 2 模板库"改挂在 14 D13 的自身条件下。i18next 本体/navigator 探测/复数·Intl 三项仍是 14 D12 的缓做余部，本条不再作为待办入口。**

> 缓做/低优项：统一登记在 [docs/14-缓做事项登记表.md](docs/14-缓做事项登记表.md)（每条带触发条件，条件满足移回本区并标注重启日期）。当前含移动端适配器、策略训练、组件市场、模型路由器、NATS、Go 网关、多租户 v2（真实账号/节点级权限/审计/跨租户共享，v1 端点级 RBAC+分区已落，见 14 D7 边界注记）、运营体市场、CD 自动化部署、可观测性、i18n 落码、组件描述多语言等项（D10a CI、D9 BENCHMARK 均于 2026-09-14 完成移出）。

23. ✅ **M12 i18n 零依赖文案抽取骨架（2026-09-20 立项并当日落码收口；用户拍板「现在推进 D12 骨架」，两处分叉选零依赖＋样板先行）**：触发条件（首个英文使用者）未到，提前落无依赖前置——`frontend/src/locales/index.ts` 自写极小 t()/useTranslation（react `useSyncExternalStore`、接口对齐 i18next：默认 zh-CN、点路径嵌套、`{{var}}` 插值、`ns:` 前缀、缺键 common→zh-CN 回退、缺键返回 key、changeLanguage/localStorage 预留不启用），`zh-CN/common.json` 样板（role/auth.login/auth.session/error.auth＋克制 button·status）＋Login/UserBadge/`auth.ROLE_LABELS` 接线（ROLE_LABELS 从 common.json role 派生、中文逐字不变；种子账号结构化经 accountLine 插值），editor/dashboard/demo 与 en-US 四件空 `{}` 占位；**不装 i18next、不做切换 UI、不翻译 en-US、不动后端错误码、不解除 D12/D13**。立项 docs-only `3197e07`、骨架 `9f1963f`（13 纯函数 vitest U212–U218）、接线 `a894fa7`（exhaustive-deps 抓到一处多余 useCallback 依赖已修）。收口门：前端 **495 passed/2 skipped/40 文件**（基线 482 净增 13）、tsc+vite build 过、oxlint **0 error/2 既有 warning**；后端零改动 **782 不变**；真实浏览器 admin-a 登录页全量中文与登录后 UserBadge（金色管理员 Tag/退出登录）零视觉差异、退出登录正常，2 截图 `docs/assets/m12-{login,userbadge}.png`。本批立项/两 feat＋docs 收口原子已全部 push，并经 PR #43（merge `5a41f9e`）合入 main（当时 dev tip `5200fe0`）。方案权威 docs/17、立项/收口条 docs/08、U212–U218 docs/13、D12 注记 docs/14。

24. ✅ **M12 续批：dashboard namespace 首批生产接线（2026-09-20 落码收口；用户「一口气推进可以搞的任务」，M12 既定「后续页面照 common 样板分批抽取」第一批，不新立 ADR、不越触发边界）**：填 `zh-CN/dashboard.json`（demo.title/description/cards.graph·loop·harness）、`zh-CN/common.json` 增补 brand.appName 与 nav.openEditor/monitoring/memory；`pages/Dashboard.tsx` 改 `useTranslation('dashboard')`，页面文案走默认 ns、品牌名/主导航经显式 `common:` 前缀跨取，Graph/Loop/Harness 技术专名 Card title 不译，`en-US/dashboard.json` 仍空 `{}`；U219–U221（i18n 纯函数测 13→16）；前端 **498 passed/2 skipped/40 文件**、tsc+build 过、oxlint 0 error/2 既有 warning，后端零改动 782 不变；真实浏览器 admin-a 登录后 Dashboard 中文零视觉差异，1 截图 docs/assets/m12-dashboard.png。不引 i18next/不做切换 UI/不翻译 en-US/不接 AntD locale/不动后端错误码，不解除 D12·D13；editor/Monitoring/Memory 大页面留后续。详见 Recently shipped·打包 G 五项落码收口条目。

25. ✅ **P0 批 1 持久化交付——已落码收口（2026-09-21，dev，未 push；形状权威 [docs/30](docs/30-持久化交付契约设计.md)）**：compose 缺省切 PG（pgvector:pg16＋命名卷、entrypoint 自动迁移）、迁移运行器（schema_migrations 009＋scripts/ops/apply_migrations.py，不引 Alembic）、会话 PG 接线（重启不全登出；密码哈希/JWT 仍归批 2）、pg_dump 备份脚本＋真实删卷恢复演练（八项行数一致、旧 token 有效、挂起审批可续跑）；U222–U226 全绿（后端 792、PG 集成 26、前端 498）。docs/29 阻断项 #1 解除（单实例口径）。

26. ✅ **P0 批 2 生产认证——已落码收口（2026-09-21，dev 未 push；形状权威 [docs/31](docs/31-生产认证契约设计.md)，ADR T25，运行时权威块 04 §5.17）**：后端五原子 `cd1133a`（scrypt 哈希＋种子账号改哈希）/`097c056`（iam_users 迁移 010＋UserStore 内存·PG 两档/幂等 seeder）/`e65e4df`（账号生命周期五端点）/`4224578`（会话绝对 TTL 迁移 011）/`36ab54e`（登录滑动窗口节流）＋前端 `222552d`（用户管理页＋Header 改密 UI）；三道门后端内存档 822 passed/31 skipped（PG 集成 U232 已写、给 DATABASE_URL+ATLAS_RUN_INTEGRATION 时跑）、前端 509 passed/2 skipped/40 文件、oxlint 0 error/2 既有 warning、tsc＋build 过；curl 23 项 HTTP 契约全绿、真实浏览器 9 项冒烟＋8 截图 docs/assets/p0b2-*.png；U227–U232 转正式。docs/29 阻断项 #2（生产鉴权）解除（单实例口径）。批 3＝真实接入安全准入纯逻辑层（凭证加密/SSRF/DB 只读 SQL 审查/重试熔断），docs-only 契约待立项。

27. ✅ **P0 批 3 真实接入安全准入——已落码收口（2026-09-21，dev 五原子未 push、待 PR #48；形状权威 [docs/32](docs/32-真实接入安全准入契约设计.md)，ADR T26 定稿 (a) 引 cryptography 落 AES-256-GCM，U233–U239 转正式）**：四块全部落码——①凭证信封 enc$v1（AES-256-GCM/plain）＋SecretProvider 运行时主密钥注入＋secret:// 解析＋全链路脱敏（`security/secrets.py`，`01a2f97`，依赖/环境 `a61680b`）；②HTTP SSRF egress 校验（`security/egress.py`，`06b2473`，私网/环回/链路本地/云元数据/CGNAT/保留恒拦、可注入 resolver、白名单 fail-closed、不跟跨 host 重定向、permit_cidrs 仅测试缝）；③DB 只读强制＋SQL 静态审查（`database/guard.py`，`a189dc1`，外部连接 execute fail-closed、query 触库前审查、demo engine 直连播种不受影响）；④出向重试＋按 host 进程内熔断（`httpapi/resilience.py`，`5376e79`）。新增 7 错误码；无新 REST 端点/无迁移/前端零改动。收口门后端 1008 passed/31 skipped、PG database 集成 3 passed、前端 509 passed/2 skipped、tsc＋build 过、oxlint 0 error/2 既有 warning，离线冒烟元数据/内网→EGRESS_DENIED 零外呼、外部库 execute→DB_WRITE_FORBIDDEN、写 SQL→DB_SQL_NOT_READ_ONLY、缺秘密→SECRET_UNAVAILABLE、公网带秘密 GET→200 全过。docs/29 阻断项 #3 的纯逻辑安全准入子集解除；**D22/D23/D24 不解除**——OAuth2/token 刷新/连接 CRUD、真实 SMTP/IM/webhook、真实数仓驱动/只读账号、多实例熔断/KMS/密钥轮换、连接级 DNS-pinning transport 仍随真实渠道/部署批。详见 Recently shipped·打包 G 五项落码收口条目。

28. ✅ **影子模式＋监控告警补强＋i18n 续批打包（docs/33，实际用例 U250–U299，2026-09-21 全包落码收口；12 原子全 ✅）**：用户拍板「打包一+打包三共 6 项」，从 docs/14 D12/D26/D28 取回工程内可闭环的进程内 v1 形态，零新依赖、不新增 ADR、**不解除缓做条目**；形状权威＝docs/33。**四批**：①i18n 续批（editor 框架/画布/左栏＋属性面板/调试台两原子、monitoring、memory；不引 i18next、en-US 空 `{}`、技术专名不译、后端错误码不接；ReleaseModal/RolloutModal/FeedbackButton/CardRenderer 不抽）；②D26 影子模式（dry-run 依 `Capability.permission`：READ 透传、WRITE/DELETE/FINANCIAL 短路 SHADOW_DRY_RUN 意图回执；shadow 全链透传含子图、不写 RunRecord/run_store、不调 evaluate_after_run、不发 tool_metric、审批预置 approved 秒过记意图；recording/shadow.py 的 ShadowStore ring 100/租户不 PG 化；4 REST 端点＋编辑器 ShadowRunModal＋监控 Card，仅 sync 无 SSE）；③D28 Trace 钻取（RunRecord 增 spans 纯超集、迁移 012 给 monitoring_runs 加 spans JSONB、GET /api/monitoring/runs/{id}/trace 懒加载、列表不返、前端 lib/traceTree.ts 纯函数＋TraceWaterfall 瀑布；采集口径不变，仅两真实运行入口）；④D28 告警静默（sil-N、rule/graph/全局、惰性过期、suppressed_count、POST/GET/DELETE）＋未确认惰性升级（RuleConfig.escalation_ack_minutes、open warning 超时升 critical、读时评估、不引定时器）＋值班手动轮换（OnCallSchedule members/index、GET/PUT/rotate、新告警**新建**时指派 assignee）；escalated_at/assignee/静默/值班照 rule_name 先例进程内不 PG 化（仅 spans 走迁移 012）。共 **11 个新 REST 端点**。**12 原子序**：①✅ docs-only 立项（`9fcf40b`：33+08+14+00+handoff+CHANGELOG）→②✅ i18n editor-a（`97d61dc`，含 monitoring/memory namespace 空骨架注册；Editor.tsx 框架/全部 Modal/暂停 Card/日志模板＋FlowCanvas 5 边标签＋NodePanel/VariablesPanel，141 静态键＋插值键，业务数据/专名不译，i18n.test 净增 7 用例）→③✅ i18n editor-b（`0c299ef`：PropertyPanel 属性/会话级断点/重试/AI 决策配置＋DebugConsole，property/breakpoint/retry/decision/debugConsole 五组 29 新键全引用、技术专名与文档引用不译、后端校验 message 原样，i18n.test 净增 2 用例） →④✅ i18n monitoring（`3445576`：监控告警全页外壳——页头/六指标卡/告警·运行·跨图报告·业务·适配器五表列与 Tag/规则编辑器/自定义规则区/空态；zh-CN/monitoring.json 填实约 100 键；lib/monitoring.ts 的 RULE_LABELS/ALERT_STATUS_LABELS 改返 i18n key、组件 t() 解析，自定义 ruleName 与 custom:{cid} 缺键原样显示；纯英文表达式 placeholder、P50/P95 技术专名、后端告警/校验 message 不抽；i18n.test 净增 5 用例含 custom.hint 字面 {{status}}/{{hasError}} 与 en-US 回退守护，lib 测试断言改 key；前端 523 passed/2 skipped/40 文件、build 过、oxlint 0 error/2 既有 warning，后端零改动 1008 不变） →⑤✅ i18n memory（`bf9cdb6`：长期记忆页头/说明 Alert/语义搜索/记忆列表/新建编辑 Modal 全外壳，列表·搜索两表列与 Popconfirm、三处 kind 筛选、Modal 按钮走 common:save/cancel/delete；zh-CN/memory.json 39 键；lib/memory.ts 的 MEMORY_KIND_LABELS 改返 i18n key、组件 t() 解析、未知 kind 原样；buildMemoryPayload/parseStringMapText 表单校验 message 照 conditions 校验先例原样不抽、JSON 示例 placeholder 保留；i18n.test 净增 3 用例，前端 526 passed/2 skipped/40 文件、build 过、oxlint 0 error/2 既有 warning，后端零改动 1008 不变） →⑥✅ 影子后端（`cea7111`：loader shadow 全链透传含子图、编译期 `_tool_permissions` 权限表 READ 透传/WRITE·DELETE·FINANCIAL 短路 SHADOW_DRY_RUN〔非法 JSON 仍 INVALID_PARAMETER 不伪造〕、影子不发 tool_metric；新建 recording/shadow.py 模型＋纯函数〔extract/infer/normalize/compare〕＋ShadowStore ring 100/租户两档进程内不 PG 化；TenantServices 两档接线＋reset；4 REST 端点 POST graphs/{id}/shadow-runs·GET shadow-runs·GET shadow-runs/{sid}·POST shadow-runs/{sid}/compare，独立 tracer＋独立 ApprovalBroker＋预置全 approved 秒过、不写 run_store/不触发门控告警；tests/test_shadow_mode.py U250–U269 净增 21 常跑＋1 PG integration，后端 **1029 passed/32 skipped**） →⑦✅ 影子前端（`c29ce15`：apiClient 四端点＋类型、lib/shadow.ts 纯函数〔parseShadowInputs/comparisonVerdict/intentKind/autoActionLabel/formatParameters〕＋9 vitest、components/shadow/ShadowRunModal.tsx 编辑器入口〔inputs JSON/可选人工结果/对比结论/工具意图表/路由决策表〕＋ShadowRunsCard.tsx 监控页列表＋事后补录、Editor 工具栏「影子运行」按钮 operate 可见、Monitoring 挂载影子 Card；monitoring ns shadow 段 46 键＋editor header.shadowRun，en-US 保持空；前端 **536 passed/2 skipped/41 文件**〔净增 10〕、tsc＋build 过、oxlint **0 error/2 既有 warning**〔新组件 set-state-in-effect 已改 afterClose/异步首载消除、未新增〕，后端零改动 1029 不变） →⑧✅ trace 后端（`4579843`：RunRecord 增 spans、record_run 透传、内存 get_run；PG record_run/INSERT spans JSONB、拆 _RUN_LIST_COLS〔列表/告警评估投影不取 spans〕与 _RUN_COLS〔含 spans〕＋get_run；base Protocol 同步；五个 record_run 点传 spans〔sync 成功 result[traceTree]/异常 tracer.to_tree()、stream 成功 traceTree/cancelled·error 条件 to_tree〕；列表端点投影剔除 spans；新端点 GET /api/monitoring/runs/{id}/trace〔read，历史/debug/回放 spans=null、404/401〕；迁移 012 spans JSONB NOT NULL DEFAULT '{}' 已应用 atlas-pg；tests/test_monitoring_trace_spans.py U270–U278〔8 常跑＋U278 PG integration 往返/投影/空归一〕；后端 **1037 passed/33 skipped**〔+8 常跑、+1 集成 skip〕，U278 PG 档单跑过）→⑨✅ trace 前端（`ce6cc23`：apiClient TraceSpanNode 递归类型/RunTrace/getRunTrace；lib/traceTree.ts 纯函数〔flattenSpans DFS 深度行＋collapseInternal 折叠、layoutWaterfall 以 root startedAt/durationMs 算 leftPct/widthPct〔最小 0.5%/clamp 不越右界/root duration 0 兜底〕、spanKindLabelKey 返 i18n key＋未知原样、spanKindColor、spanBarColor〔ok 蓝/error 红/internal 灰〕、formatSpanAttrs〕＋12 vitest；components/monitoring/TraceWaterfall.tsx〔懒加载＋模块级缓存＋失败 Alert 重试＋无 spans 空态、kind Tag/缩进名称/attrs Tooltip/百分比横条/右侧 formatDuration〕；Monitoring 最近运行展开改 Tabs「节点结果/Trace 时间线」、rowExpandable 恒真；monitoring ns trace 段 17 键、en-US 保持空、i18n.test 加 15 静态键；前端 **548 passed/2 skipped/42 文件**〔净增 12〕、tsc＋build 过、oxlint **0 error/2 既有 warning**〔新组件零新增〕，后端零改动 1037 不变）→⑩✅ 告警三件套后端（`00bba8c`：新 monitoring/silences.py〔Silence/OnCallSchedule 模型、silence_matches/is_silence_active/current_assignee 纯函数、OpsStore 单锁进程内状态——静默 sil-N 上限 100 惰性清过期、值班去重保序取模轮换、PG 档 assignee map〕；alerts.py 加 RuleConfig.escalation_ack_minutes〔validate 1-10080、rules_from_raw 透传〕＋Alert.escalated_at/assignee＋apply_escalation 纯函数〔open warning 超时 first_seen 兜底 last_seen 升 critical、ack/resolved/critical/关配置/已升级幂等不升〕；内存 MonitoringStore 与 PgMonitoringStore 两档接线〔告警产生先静默检查命中则不建/不合/不升仅 suppressed_count++、新建告警指派值班人、list/get 读时惰性升级、PG 不持久化 escalated_at/assignee 读回重评幂等、reset 清静默/值班〕；base Protocol 补 6 签名；6 端点 POST/GET/DELETE silences〔administer/read/administer、duration 1-10080 reason 非空≤200 中文 422、active 过滤带 active 字段、不存在 404〕＋GET/PUT/POST rotate on-call〔read/administer/administer、members 1-20 去重保序重置 index、空表 409〕；tests U285–U299〔14 常跑＋U299 PG integration 过〕；后端 **1051 passed/34 skipped**〔+14 常跑、+1 集成 skip〕）→⑪✅ 告警三件套前端（`1804255`：apiClient 加 Silence/OnCallSchedule 类型＋6 端点、AlertItem 加 escalated_at/assignee、RuleConfig 加 escalation_ack_minutes；lib/onCall.ts parseMembers〔逗号/中文逗号/分号/换行/空白分隔、trim 去重保序〕＋validateMembers 纯函数＋7 vitest；components/monitoring/OnCallBar.tsx〔当前值班 Tag＋轮换＋设置轮值表 Modal，写操作仅 admin、只读可见当前〕、SilenceManager.tsx〔折叠静默规则表 规则/图/原因/创建人/到期/压下次数/状态/解除＋SilencePopButton 操作列静默〔时长 30/60/240/1440＋自定义 1-10080、原因必填〕〕；Monitoring 告警 Card 顶部值班条、表加值班列与「已升级」Tag〔Tooltip 时间〕、操作列静默按钮、折叠静默表，规则卡加升级开关＋分钟 InputNumber 随 PUT 保存；monitoring ns 加 onCall/silence/escalation/col.assignee/ruleForm 升级共约 45 键＋3 插值键、en-US 保持空、i18n.test 加静态/插值守护；前端 **555 passed/2 skipped/43 文件**〔净增 7〕、tsc＋build 过、oxlint **0 error/2 既有 warning**〔新组件零新增〕，后端零改动 1051 不变）→⑫✅ 总收口（**已完成 2026-09-21**：最终三道门后端 **1051 passed/34 skipped**、前端 **555 passed/2 skipped/43 文件**、tsc+vite build ✓、oxlint **0 error/2 既有 warning**（ReleaseModal/RolloutModal，新代码零新增）；内存档 d26/m11/d28 三 smoke 全过；PG 直连 integration **43 passed**（含 U299/trace U278/M11 pgvector；整栈 PG uvicorn 与 U269 受既有 `PgUserStore.bind_session_store` 缺失阻断、非本包回归、已登记 docs/29）；真实浏览器冒烟 4 截图 docs/assets/d28-{oncall-alerts,silence-oncall,trace-waterfall}.png；契约 03/04/06/09/12/13/14/17/00/08/29/33＋CHANGELOG 全回填；另修静默联动 `90b942a`、d28_smoke 扩充 `8b29396`）。三道门基线（只许增测，立项时）：后端 **1008 passed/31 skipped**、前端 **509 passed/2 skipped/40 文件**、pnpm build 过、oxlint **0 error/2 既有 warning**（ReleaseModal/RolloutModal，本批不碰）。**非目标/保留缓做**：i18next/切换 UI/en-US 翻译/AntD locale/后端错误码、影子自动旁路/SSE/PG 化/报表趋势、OTel/Prometheus/Grafana（随 D11）/span 实时 SSE/长保留时序、外部告警出口（随 D24）/按日自动轮换/排班表、静默值班升级 PG 化、新依赖/新 ADR/新节点类型/DSL 版本变更。**打包二（LICENSE 类型 MIT/Apache-2.0/闭源待拍板、Prometheus 埋点、.pre-commit）用户未选、不在本包、不阻塞**。

29. ✅ **整栈 PG 装配缺口修复（补 `PgUserStore.bind_session_store`）＋ U269 转正（2026-09-22 用户「开搞」一口气包内；单代码原子 `b2ac85d`）**：docs/29 line 121 实证登记的具体装配缺口——[iam/deps.py](src/atlas/iam/deps.py):39 模块级无条件 `user_store.bind_session_store(session_store)`，内存版 `UserStore`（iam/accounts.py:44）有此方法、PG 版 `PgUserStore`（storage/pg.py）缺，故 `ATLAS_STORAGE_BACKEND=pg` 时 import api.main 即在装配期抛 `AttributeError: 'PgUserStore' object has no attribute 'bind_session_store'`，整栈 PG uvicorn 不可启动、U269 同因阻断（PG 直连 integration 绕过 deps 整栈装配故未暴露）。修法＝给 `PgUserStore` 补与内存版同构的显式 no-op `bind_session_store`：PG 档会话吊销本就在 `update`/`set_password` 内经 `self._backend.session_store()` 单例现取（操作同一 `iam_sessions` 表），无需像内存档外部持有引用；会话 PG 化本身由 `PgSessionStore` 承担（docs/30 §4），不在此重复绑定。另给 U269 的 skipif 补 `ATLAS_STORAGE_BACKEND=='pg'` 条件，使其在内存档/PG 直连档正确 skip、仅整栈 PG 档执行（原仅判 ATLAS_RUN_INTEGRATION，直连档会撞函数内 assert）。验证：① PG 档 `import atlas.iam.deps` 与 `from atlas.api.main import app` 不再 AttributeError；② 真实 `ATLAS_STORAGE_BACKEND=pg` 起 uvicorn（:8124）轮询 `/api/health` 于 13s 得 **HTTP 200 `{"status":"ok"}`**、日志 Application startup complete 无 Traceback；③ U269 整栈 PG 档 **PASSED**；④ 后端内存档全量 **1051 passed/34 skipped**（与 docs/33 基线逐字一致、零回归）；⑤ PG 直连套件（ATLAS_RUN_INTEGRATION=1＋DATABASE_URL、不设 backend，7 文件）**56 passed/1 skipped**，唯一 skip 即 U269（直连档 backend=memory 正确跳过、不再撞 assert）。前端零改动（本修仅 src/atlas/storage/pg.py ＋ tests/test_shadow_mode.py 两后端文件，+14/-2），无变化面故未跑前端门；无新依赖/无新 REST 端点/无迁移/不新增 ADR/不解除任何 D 项。docs/29 该具体缺口标记解除、13 U269 转整栈 PG 正式、CHANGELOG 记 fix。

30. ✅ **docs/34 对外交付层小批（A 档＋B 档九项，2026-09-22 用户「A和B吧，尽量搞一下」一口气包；零新依赖，dev 待 push）**：依据 docs/34 §五 P0/P1 清单，工程内可闭环项全部落码——A1 MIT LICENSE（根目录 LICENSE＋pyproject/frontend license 字段；**同日 2026-09-22 已由用户拍板删除、保持不开放＝`f721c18`，现仓库无 LICENSE 文件、两清单也无 license 字段，决策记录见 docs/08 与 docs/34 更新注记**）；A2 `.pre-commit-config.yaml`（pre-commit-hooks v5＋gitleaks v8.21.2＋前端 oxlint local hook，刻意不引 ruff/black）；A3 CHANGELOG 补 docs/34 与本批条目；A4 清 ReleaseModal/RolloutModal 两处历史 oxlint set-state-in-effect warning（React 官方渲染期 prev-open 重置模式＋fetch-on-mount scoped `// oxlint-disable-next-line react/set-state-in-effect`，规则名必须斜杠写法，括号写法无效，经最小复现实测 oxlint 1.82.0）；A5 PR 核实——用户已自行合 PR #52（merge `a799fa6`），无需再开。B4 `/api/ready` 就绪探针（新包 observability/health.py，PG 档 SELECT 1 fail-closed 503，无鉴权）；B3 `/metrics` Prometheus 端点（observability/metrics_export.py 纯函数手写 text 0.0.4，atlas_up/backend_info/tenants_active/runs_total/success_rate/duration p50p95/tool_calls，无鉴权，单租户快照失败只 warning 不 500，TenantRegistry.all_tenant_ids() 加锁快照不触发惰性创建）；B2 SMTP email 真实投递（message/smtp.py SmtpConfig.from_env/SmtpSender/get_smtp_sender 惰性单例，STARTTLS，channel=email 注入 sender 真实发信、delivered=smtp、失败 SMTP_SEND_FAILED 不落记录，HOST 缺省 None 回退进程内，registry 内存/PG 两档注入、demo 兜底保持进程内，.env.example SMTP 段）；B1 TLS 反代样例（deploy/Caddyfile＋deploy/README.md＋compose caddy edge profile 两卷＋atlas /api/ready healthcheck，`docker compose config` 默认/edge 两档合法）。新测试 test_observability.py 8 例、test_message_smtp.py 14 例（假 smtp_factory 模拟完整建连契约：factory 内即 starttls/login，ActionRequest 仅 capability_name/parameters/context/timeout，ActionResult.status 用 `is ActionStatus.SUCCESS`）。收口门后端 **1073 passed/34 skipped**（1051＋22，187s 零回归）、前端 **555 passed/2 skipped/43 文件**、tsc＋vite build 过、oxlint **0 error/0 warning/137 files**、gitleaks 本地无泄漏。文档同步 docs/14（D10b/D11/D24 三行＋文末 2026-09-22 注记，三条均部分取回不解除）、docs/34（当日落码注记，双口径结论不变）、00 地图、CHANGELOG。**未做/待拍板**：~~LICENSE 是否确认 MIT~~（**已于同日拍板＝删除、保持不开放 `f721c18`，本项不再是待办**）；Caddy 占位域名 atlas.example.com 换真实域名并实启 edge profile；本批是否 push/开 PR；PG 整栈档新端点未重跑（/ready PG 分支、/metrics PG 档仅代码审查＋内存档测试）。

31. ✅ **真实接入交付批（docs/35，T2–T6，2026-09-22 用户「T1到T6开搞」拍板；T1–T6 全部落码收口，仅本地 dev 未 push、合 main 待授权）**：承接 docs/34 §五 P0/P1，工程内可闭环五包，均部分取回缓做项、不解除。形状权威＝docs/35。**九步原子序**：①✅ docs-only 立项（docs/35＋08＋14＋00＋handoff＋CHANGELOG）＋T1 治理（delivery batch push、PR #53 合 main merge `c1fe886`、CI 三道门全绿）→②✅ **T5 deploy 已落码收口（`0842243`）**：deploy/prometheus/prometheus.yml（抓 atlas:8000/metrics 15s）＋deploy/grafana provisioning（datasource uid=atlas-prometheus、folder=Atlas、provider 30s）＋dashboards/atlas-overview.json（7 面板严格对齐 /metrics 的 7 个 gauge：up/tenants/backend/success_rate/runs by status/p50·p95/tool_calls，不用 rate()）＋compose 新 **observability** profile（prom/prometheus:9090 保留15d、grafana/grafana:3000，GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_ADMIN_PASSWORD:-atlasadmin} 生产必改，两命名卷，默认不起、与 edge 正交）＋deploy/README 一键栈小节＋.env.example GRAFANA_ADMIN_PASSWORD；验证：json.tool 过、`docker compose config --services` 默认仅 atlas/db、`--profile observability` 增 grafana/prometheus 且挂载/环境/depends_on 渲染正确；零新 Python 依赖、零后端/前端改动（D11 子集，不解除缓做）→③✅ **T6 审计已落码收口（`f243e04`）**：observability/audit.py（AuditEvent 仅 8 元数据字段、AuditStore ring ATLAS_AUDIT_RING 默认 2000/租户、list 倒序 clamp500/export_jsonl 正序/action 前缀）＋PgAuditStore（迁移 013 audit_events，tenant+id PK、seq 索引、LIKE 转义）＋registry 两档装配（reset 不清）＋get_principal 挂 request.state.principal＋HTTP 中间件（仅 /api 写方法、路由模板 path_format 降级实际 path、异常只 warning、跳过 login）＋登录成功显式记一条（失败不记）＋GET /api/audit/events（administer，limit/action）、/api/audit/export?format=jsonl（administer，StreamingResponse 附件，非 jsonl 422）；tests/test_audit_log.py 16 常跑＋1 PG integration；验证：全量后端 **1089 passed/35 skipped**（基线 1073/34，净增 16 常跑+1 skip 零回归）、PG 直连集成过、迁移 CLI 登记 013、整栈 PG uvicorn:8124 冒烟过（login aud + feedback 中间件 aud 落 PG、倒序/export 正序、viewer 403、无请求体泄露）→④✅ **T3 webhook 已落码收口（`54bf38d`）**：新 message/webhook.py（DefaultWebhookSender：先 EgressGuard.check 再 httpx POST、follow_redirects=False 防重定向绕过 SSRF、10s 超时、Content-Type application/json；非2xx/网络错抛 WebhookDeliveryError、EgressDenied 原样透传；guard/post 可注入；get_webhook_sender 进程单例）＋MessageService 接入（channel=webhook 仅收单个 URL 字符串、数组即使单元素也 INVALID_PARAMETER；payload 5 字段 id/channel/subject/body/sent_at；EGRESS_DENIED/EGRESS_INVALID_URL 透传、其余折 WEBHOOK_SEND_FAILED；失败不写记录、成功 delivered=webhook；未装配回退 in_process）＋registry 两档注入 get_webhook_sender＋adapter channel 描述更新；tests/test_message_webhook.py 22 例全 fake 不触网；验证：消息三件套 56 passed、全量后端 **1110 passed/35 skipped**（T6 后 1089，净增 21 常跑零回归）；不做签名/重试/限流/入站/IM/短信（D24 子集不解除）→⑤⑥✅ **T2 审批通知已落码收口（`5b17784`，前后端一体）**：DSL human_approval.config 新增可选 notifyEmails（string[]≤5；dsl.py 编译期校验非数组/超5/非字符串/静态空或无@、pointer 落 /notifyEmails，含 {{}} 占位符项编译放行）；新 collaboration/notifications.py（ApprovalNotifier Protocol＋EmailApprovalNotifier 复用 MessageService 发纯文本邮件：summary/approver/超时时长/应用入口 URL、**不含一键决策链接**、public_url 构造期注入 rstrip）；loader 全链透传 approval_notifier（_make_executor/_register_approval/_execute_subgraph/compile_graph/run_graph，子图透传），登记 pending 后旁路触发、_resolve_notify_recipients 运行时插值去重并剔除空值/无@（warning）、**fail-safe 吞异常绝不阻断图**、approval 载荷增 notified/notifyError；main.py 同步＋流式两条真实执行路径装配 EmailApprovalNotifier(services.message_service, ATLAS_PUBLIC_URL 默认 http://localhost:5174)，回放/影子/续跑不发；.env.example 加 ATLAS_PUBLIC_URL；前端 human_approval.schema.ts 加 notifyEmails（array items string maxItems5 默认[]，复用通用 ArrayView 字符串列表控件提交 string[]）＋nodeUiSchemas 中文 label/placeholder（数据层文案同全部节点字段、无新 JSX 文案、en-US 随 D12）；tests/test_approval_notifications.py 11 例＋同步 test_graph_loader approval 载荷基线（+notified）；验证：全量后端 **1121 passed/35 skipped**（T3 后 1110，净增 11 零回归）、前端 vitest **555 passed/2 skipped**、oxlint **0/0**、pnpm build 过；不做决策结果邮件/邮件一键决策落地页/持久化中断余部（D20 子集不解除）→⑦✅ **T4 后端已落码收口（`a57164d`）**：新 connections 包（models Connection/public_view 投影永不回传信封或明文、oauth 纯逻辑 HMAC-SHA256 state CSRF＋授权码交换/刷新可注入 post_form 不触网、store 内存 per-tenant、service 状态机＋build_connection_service 进程单例）＋迁移 014 oauth_connections（已 apply atlas-pg）＋storage/base ConnectionRepository Protocol＋PgConnectionStore（20 列、行内 tenant 过滤、reset 不清）＋registry 两档装配（reset 不清连接）＋main.py 9 端点（POST/GET list/GET one/PUT/DELETE administer·read、authorize/exchange/refresh/test operate、GET /connections/callback 无鉴权静态 HTML include_in_schema=False 注册于 StaticFiles 挂载前）＋token/client_secret AES-GCM 信封（复用 security/secrets，缺 master key 退回 Plaintext＋固定 dev state key 并 WARNING）＋tests/test_connections.py 20 常跑＋1 PG integration（PG 往返隔离已单跑过）→⑧✅ **T4 前端已落码收口（同 `a57164d`，前后端一体）**：apiClient 连接类型＋8 端点函数、lib/connections.ts 纯函数（状态标签/颜色、scopes 文本、draft 校验构造、时间格式化）、pages/Connections.tsx 整页（Table＋新建/编辑 Modal〔编辑 secret 留空保留〕＋授权新窗口/完成授权粘贴 code·state/刷新/测试/删除 Popconfirm，admin 建删改、operator 授权四作、viewer 只读）、Dashboard operator+ 入口、App 切页、connections namespace（zh 填实约 60 键/en-US 空{}）＋i18n.test 净增 2 用例、.env.example ATLAS_OAUTH_REDIRECT_URI；验证：全量后端 **1141 passed/36 skipped**（T2 后 1121，净增 20 常跑零回归；+1 PG skip）、前端 vitest **557 passed/2 skipped/44 文件**、oxlint **0/0**、pnpm build 过（仅既有 chunk>500kB 提示）→⑨✅ **总收口已完成（2026-09-22）**：PG 直连集成（ATLAS_RUN_INTEGRATION=1，连接 21＋审计 17）**38 passed**、d26/m11/d28 三内存档 smoke 全过、T4 HTTP 端到端冒烟 ALL_PASS；契约文档全回填——docs/35 顶部落码收口注记（五包 hash＋落码细化）、docs/14 D11/D20/D22/D24 改已落码态、docs/03 加 connection/audit_event 与 human_approval.notifyEmails、docs/09 加 connections/observability 包与 message·smtp·webhook/collaboration·notifications、docs/12 加连接 9 端点+无鉴权回调页+审计 2 端点、docs/13 加 T2–T6 测试行（文件级登记）、docs/00 地图 35 收口态、docs/08 加落码收口条、CHANGELOG 加五包落码段、handoff L3/本条。最终门：后端 **1141 passed/36 skipped**、前端 **557 passed/2 skipped/44 文件**、oxlint **0/0**、pnpm build 过。**五包均部分取回、不解除 D11/D20/D22/D24；本批 12 commit 已 push origin/dev（dev==origin/dev=`ae95968`）、尚未合 main，待开 PR（合 main 须用户授权）。****非目标（仍缓做）**：真实平台业务 API 联调、IM/短信、审批持久化余部/邮件一键决策落地页、OTel SDK/Collector、CD/真实域名、真实种子客户（阻断 #5）。

32. ✅ **审批闭环批（docs/36，2026-09-22 立项同日五原子全部落码收口；四 commit 在 dev、未 push、合 main 待授权）**：承接 docs/34 复审与 T2 余部，接上「通知→决策」断裂段；零外部资源，D20 再次部分取回、不解除。形状权威＝[docs/36](docs/36-审批闭环批契约设计.md)。**五步原子序**：①✅ docs-only 立项（e97ec37）→②✅ `collaboration/email_token.py` TokenIssuer HMAC 签名 capability token（TTL ≤1h+300s、密钥三级解析）＋broker `_Pending.created_at`＋notifier 邮件一键深链（bd6ec7a）→③✅ 公开 GET email-view（只读防预取）/ POST email-decision（`TenantRegistry.peek` 不创建、`_apply_approval_decision` 与登录态共用、409 首决、审计 actor=email-link 不记 token/comment、统一 404）（8923473）→④✅ 前端 `pages/EmailApproval.tsx` 免登录深链页＋`pages/Approvals.tsx` 队列页（10s 可见时轮询、viewer 只读、卡片 Modal）＋approvals i18n namespace＋lib 纯逻辑 14 测（e2771ba）→⑤✅ 总收口（浏览器六场景冒烟过＋契约文档 03/04/06/09/12/13/14/00/08/10 T27/34/36＋CHANGELOG＋handoff 回填）。最终门：后端 **1174 passed/36 skipped**、前端 **571 passed/2 skipped/45 files**。**非目标仍缓做**：KMS/密钥轮换、邮箱绑定、IM/钉钉/决策结果邮件、持久化中断余部/多实例、动态审批人/节点级角色、已决历史页、邮件链接限流、真实投递联调。

33. ✅ **审计可见与审批结果补强批（docs/37，2026-09-22 立项同日落码收口；三 commit ecfa325/bc3f116/033619a 在 dev、未 push、合 main 待授权；零外部资源/零新依赖/无 ADR，不解除 D11/D20/D24）**：承接 docs/34 P1 #8 余部与 docs/36 非目标。形状权威＝[docs/37](docs/37-审计可见与审批结果补强批契约设计.md)。**五步原子序**：①✅docs-only 立项（docs/37＋00＋08＋handoff，223b02a）→②✅feat(collaboration)（ecfa325）：broker `list_decided`（resolved_at 倒序 clamp 1–200、不泄露 card_context）＋`_Pending.notify_recipients`（request/restore）、Notifier `notify_decided`（subject 审批已处理、comment 截断 200、不含 token/链接）、loader 超时/预置路径接线 →③✅feat(api)（bc3f116）：`GET /api/approvals/decided`（read 全角色、租户分区）＋`_apply_approval_decision` 可选 notifier（fail-safe、异常不改 200/409）→④✅feat(frontend)（033619a）：admin `pages/AuditLog.tsx`（复用 events/export 端点、action 前缀过滤、Bearer blob 下载、audit namespace）＋Approvals「已处理」Tab（切 Tab 加载不轮询）→⑤✅docs 收口（浏览器冒烟过＋03/04/06/09/12/13/14/00/08/37＋CHANGELOG＋handoff 回填，收口 commit 待提）。最终门：后端 **1190 passed/36 skipped**（基线 1174，净增 16：collab +5/notifier +4/API +7，零回归）、前端 **571 passed/2 skipped/45 files**（计数不变），oxlint/build 过；浏览器冒烟（审计页过滤/limit/导出/空态、已处理 Tab）过、控制台零错误，2 截图 docs/assets/audit-{log-page,decided-tab}.png。**非目标仍缓做**：已决历史 PG 持久化/多实例（D20）、审计 SIEM/actor·时间过滤/分页（D11）、结果邮件模板化/重试/IM（D24）。

34. ✅ **真实渠道适配批（docs/38，2026-09-22 docs-only 立项、2026-09-23 全部落码收口；ADR T28；`34472ce`→`3d6d28a` 六 commit 在 dev、未 push、合 main 待授权）**：承接 docs/34 P0 #1「打通一个真实电商渠道」——T4 connections 平台无关、不含真实平台业务 API，本批新增 `src/atlas/channels/`：v1 仅 Shopify Admin API（ShopifyChannelClient list_orders/get_order/create_refund、ChannelRegistry/ChannelStore 内存档、ShopifyHarnessAdapter 按绑定动态产出 `channel:shopify:{binding_id}`、工具 shop/list_orders·shop/get_order〔read〕·shop/create_refund〔financial〕），迁移 `015_channel_bindings`＋PgChannelStore（4 集成测试 skipif）、REST `/api/channels` 五端点（read/operate/administer，审计 channel.bind/channel.unbind），绑定 reset 不清；token 每次调用现解密、不缓存明文、不记日志；Connections 页底部「销售渠道绑定」卡（components/ChannelBindingsCard.tsx，绑定弹窗 provider 固定 Shopify＋连接下拉＋shop＋apiVersion 默认 2025-01，删除 admin 门），channels i18n namespace（zh 填实/en-US 空 `{}`）。形状权威 [docs/38](docs/38-真实渠道适配批契约设计.md)。**六步原子序已全部完成**：①docs-only 立项 →②feat(channels)（14 内核测）→③feat(api) REST＋动态适配器接线（10 registry＋6 API 测）→④feat(storage) 迁移 015＋PgChannelStore（4 PG 集成）→⑤feat(frontend) 绑定卡（4 apiClient＋2 i18n 前端测）→⑥docs 收口（浏览器冒烟 3 截图＋CHANGELOG＋handoff/08/38 落码注记）。最终门：后端 **1220 passed/40 skipped**（立项基线 1190 净增 30 零回归，217s）、前端 **577 passed/2 skipped/45 文件**、oxlint 仅既有 warning、pnpm build 过；浏览器冒烟三场景过（空态/绑定弹窗/未授权连接探活→绑定 error 态），控制台仅 antd 既有 deprecation 与一次预期 401，3 截图 docs/smoke-shots/。真实店铺 OAuth 联调/入站 webhook/Amazon/订单同步仍缓做 D22，触发条件不变。
35. ✅ **入站 Webhook 批（docs/39，2026-09-23 收口；ADR T29；AI 判断承接「不用管 git，你推任务」总授权）**：docs/38 落了 Shopify 出向 Admin API，本批补首个公开免登录入站触发口——`src/atlas/channels/webhooks.py`：`verify_shopify_hmac`（原始 body HMAC-SHA256，先于 JSON 解析、compare_digest）、WebhookEnvelope/build_trigger_event（channel=webhook）、WebhookDeliverer（per-tenant 幂等环 ring 200/1h；逐订阅 routing_store.resolve 钉版本、无版本 warning 跳过；后台线程异步触发图）；订阅 WebhookSubscription（topic×graph 唯一、≤10），迁移 016 webhook_subscriptions（reset 不清）；公开 `POST /api/channels/hooks/shopify/{binding_id}`（received/duplicate/ignored；400/401/404/503；验签后绝不非 2xx）＋订阅 GET/PUT（PUT administer，聚合中文 422）；前端绑定卡「Webhook」按钮→订阅 Modal（URL 复制＋topic/graph 行）。六原子交付：①docs（3c17a84）→②feat(channels)（24076c9）→④feat(storage)（8313e7b）→③feat(api)（3b19ac9）→⑤feat(frontend)（695e5f2）→⑥docs 收口（本步）。落账后端 **1252/44**（+32 新测、+4 PG skip）、前端 **579/2/45**；真实 HMAC HTTP 冒烟三态全绿、触发图 completed（截图 docs/smoke-shots/webhooks-smoke-*）。**D22/D24 不解除**：Shopify 侧注册 API（v1 仅人工填 URL）、DLQ/重试调度/投递指标、Amazon/IM/短信入站、幂等环 PG 化仍缓做。
36. ✅ **入站可靠性补强批（docs/40，2026-09-23 docs-only 立项同日全部落码收口；AI 判断承接「不用管 git，你推任务」；四 commit 8cfcf6f/3bc00a5/778acc7/3036acc 在 dev、未 push、合 main 待授权）**：补 docs/39 公开入站口三个可靠性口子——①去重记录 PG 化：`src/atlas/channels/deliveries.py` DeliveryStore 两档＋表 `webhook_deliveries`（迁移 017，PK(tenant,webhook_id)，duplicates 计数，跨重启/多实例；ignored 不落表、reset 不清）；②死信：topic 有订阅但全部订阅未触发（NO_PUBLISHED_VERSION/RESOLVE_FAILED/TRIGGER_FAILED/MISSING_GRAPH_ID）才 mark_dead、payload 仅 dead 行存，一键重放（operate，绕过去重按当前订阅）；③投递指标 GET metrics（byTopic/totals 实时聚合）。REST 四端点（dead-letters 列表/重放/删除/metrics）；前端 Monitoring 加 WebhookReliabilityCard（指标＋按 topic 表＋死信表，viewer 无操作列）。零新依赖/无 ADR；D24 部分取回不解除。收口门后端 **1276 passed/48 skipped**（立项基线 1252/44 净增 24 常跑、+4 PG skip）、前端 **581 passed/2 skipped**；真实 HTTP 签名死信→发布/rollout→一键重放冒烟全绿，3 截图 docs/smoke-shots/docs40-*。语义细化：ignored 投递不留行，重复 ignored 不判 duplicate。
37. ✅ **Shopify 侧 Webhook 注册批（docs/41，2026-09-23 落码收口；AI 判断承接「不用管 git，你推任务」）**：补 docs/39/40 遗留的店铺侧注册口子——`ShopifyChannelClient` 增 list_registered_webhooks/register_webhook/delete_registered_webhook（Admin REST，422→CHANNEL_ALREADY_REGISTERED/409；可选 base_url 测试缝），registry 三方法（地址服务端按 ATLAS_PUBLIC_URL 拼装钉版、非 https 明确 422；取消按 topic+address 双键、UNAUTHORIZED 落 binding error 态），REST 三端点（GET read 折 200+error/POST operate 201·409·422/DELETE 幂等）；前端订阅弹窗加注册区（viewer readonly）。demo mock 缝冒烟全绿（注册→重复 409→取消，3 截图 docs/screenshots/41-shot-*）。交付原子：①docs（92de009）→②feat(channels)（93cd1a0）→③feat(api)（6b86a1d）→④feat(frontend)（cefc4fe）→fix mock 缝（3eccc6d）→⑤docs 收口（本步）。落账后端 **1293 passed/48 skipped**（立项 1276/48，净增 17）、前端 **584/2/45**（净增 3）。零新依赖/零迁移/无 ADR；**D22 不解除**（真实店铺+公网 HTTPS 联调、Amazon/通用渠道、OpenAPI 导入、订单增量同步、双向自动同步仍缓做）。

38. ✅ **OpenAPI 导入与工具自动生成批（docs/42，2026-09-23 docs-only 立项同日全部落码收口；AI 判断承接「不用管 git，你推任务」）**：04 §4.5「方式 1 API 导入」的文档驱动自动形态——新包 `src/atlas/openapi/`：parser 纯函数解析 OpenAPI 3.x JSON（YAML/Swagger2 拒绝），operation → Capability（operationId 清洗/合成、path/query/header＋JSON body 合成 input_schema、$ref 内联深度 8/环守卫、allOf 合并，不支持项 operation 级 skipped 带 reason），GET/HEAD→read 其余 write（永不 financial）；ImportStore 进程内 per-tenant（reset 不清，5 specs/200 ops），`openapi:{spec_id}` 适配器出向过 EgressGuard；REST preview/imports 五端点、`/api/adapters` 合并；前端「API 导入」新页、编辑器零改动（M3 Schema 内核自动渲染参数表单）。六原子：①docs（8c9c512）→②parser（139848a）→③store＋适配器（90b8179）→④API＋发现（712f28a）→⑤前端（3f40b10）→⑥docs 收口（本步）。收口门后端 **1352 passed/48 skipped**（立项 1293，净增 59、零回归）、前端 **591/2/46**（净增 7）。mock 缝浏览器冒烟全绿：预览 skipped 灰行→导入→选择器 `openapi:*`→limit=10 真实运行 SUCCESS（HTTP 200 mock body）→operator 删除 403/admin 删除后适配器摘除、spec 404；4 截图 docs/smoke-shots/openapi-smoke-*。零新依赖/零迁移/无 ADR；**D22 部分取回不解除**（YAML/securitySchemes 接线/PG 持久化/真实外部 API 联调仍缓做）。

39. ✅ **OpenAPI 导入规格 PG 持久化批（docs/43，2026-09-23 落码收口；f39d86b→7bf13f5＋docs）**：docs/42 导入规格 v1 仅进程内、重启即失，本批照 015-017 两档先例落 PG——迁移 018 `openapi_imports`（(tenant_id,spec_id) PK、seq 走 storage_id_seq 排序、operations JSONB），`src/atlas/openapi/pg_store.py` PgImportStore 与 ImportStore 同形（上限 5 specs/200 ops、错误码 OPENAPI_LIMIT_EXCEEDED 不变、reset 不清、跨租户 404），iam/registry PG 档换装配、字段类型放宽；发现接口按请求读 store.list() 动态构建适配器（main.py:345），重启无需预热。**REST/前端/编辑器零改动**。三步原子序全部完成：①docs 立项 f39d86b →②迁移＋PgImportStore＋装配＋PG 集成 8 测 7bf13f5 →③docs 收口（本步；PG 档冒烟 2 截图：重启后仍在→运行 200）。收口门：后端内存档 **1352/56**（PG 直连 8 passed）、前端 **591/2/46** 零改动。零新依赖/无 ADR；**D22 部分取回不解除**（YAML/securitySchemes/原始文档留存/导入去重/软删除/共享仍缓做）。

40. ✅ **OpenAPI securitySchemes 静态密钥批（docs/44，2026-09-23 docs-only 立项同日全部落码收口；AI 判断承接「不用管 git，你推任务」）**：docs/42/43 忽略全部安全声明，真实 API 的 apiKey/Bearer 鉴权只能手填且明文入图。落码范围：①解析 apiKey header/query 与 HTTP Bearer（有效要求 operation 覆盖全局、OR-of-AND，unsupported 方案剔除），OperationDescriptor 增 `security`、ParsedSpec/ImportedSpec 增 `security_schemes`＋`credential_envelopes`；②密钥经 SecretProvider 信封加密（复用 T26，AES-GCM/plain 两档），迁移 019 给 openapi_imports 加两 JSONB 列，内存/PG 两端 store 同形（put_credentials）；③适配器执行逐次解密注入，缺密钥 fail-closed `OPENAPI_CREDENTIAL_MISSING` 不发无鉴权请求；④`PUT .../credentials`（operate；未知 scheme 422 OPENAPI_INVALID_CREDENTIAL），前端 Password 输入/「已配置 n/m」状态进 openapi namespace，响应不回显明文。五原子序全部完成：docs `5a7e580`→parser `ab08c12`→信封/迁移/PUT `2d534a2`→前端 `43d8001`→docs 收口。收口门后端 **1375/59**、PG 直连 **11 passed**、前端 **593/2/45**；浏览器冒烟 3 截图 docs/smoke-shots/openapi44-*，PG 档重启后信封仍在、重跑 200。零新依赖/无 ADR；basic/cookie/oauth2/openIdConnect、密钥取回/轮换、真实外部 API 联调为非目标，**D22 部分取回不解除**。
41. ✅ **循环 foreach 批（docs/45，2026-09-23 docs-only 立项同日全部落码收口；AI 判断承接「不用管 git，你推任务」）**：loop 节点 v1 仅 while，批量逐项只能手工 index 且无聚合。落码范围：`mode=foreach`——itemsExpression 首轮求值一次冻结（≤100）、串行逐项暴露 `{{loop-x.item}}/index/results`、collectTarget 回边按序聚合 results（break 网关补在途结果）；exitReason 增 completed/empty/expression_error/items_too_large；DSL 静态输出键按被引用 loop 的 mode 放宽。前端 mode Select＋schema oneOf/字段显隐＋L1 foreach 手写规则（parseExpression 纯语法）。四原子序全部完成：①docs `efe5de4`→②后端 `1e79121`→③前端 `01296d5`→④浏览器冒烟＋docs 收口（本步）。收口门后端 **1391/59**（净增 16）、前端 **597/2**、oxlint **45/1（46 files）**、build 干净；冒烟 3 截图 docs/smoke-shots/foreach45-*：3 元素 completed/results 长度 3、空数组 empty/循环体不执行，HTTP 与画布一致。零新依赖/零迁移/零新 REST·错误码/无 ADR。嵌套循环/并行 map-reduce/裸 item 短名/skip-current/>100 分批为非目标，**D16 主体取回不解除**。
42. ✅ **OpenAPI HTTP Basic 凭证子集批（docs/46，2026-09-23 落码收口；0252ffd→2f3b12b→685c3a6→11b3cbc→f24ee8a＋docs 收口）**：parser 加 http/basic（kind=basic）；PUT basic 值＝`{username,password}`（信封明文 JSON、密文存储形状不变），缺字段 422 OPENAPI_INVALID_CREDENTIAL，null 删除；适配器渲染 `Authorization: Basic base64(u:p)`，信封损坏 SECRET_DECRYPT_ERROR；前端用户名/密码双输入。冒烟中修复 f24ee8a：`openapi:*` 工具节点接通 params JSON 解析。收口门：后端 1398/59（净 +7）、前端 602/2（净 +5）、lint/build 干净；浏览器冒烟 4 截图（httpbin：fail-closed OPENAPI_CREDENTIAL_MISSING → 已配置 1/1 → SUCCESS 200 authenticated:true）。digest/cookie/oauth2/连接测试/凭证轮换为非目标，**D22 部分取回不解除**。

43. ✅ **wait 事件等待进程内 v1 批（docs/47，2026-09-23 docs-only 立项同日全部落码收口；32abb9f→f647c96→aa82eed→1608002→ce8c4f0＋docs 本步；D19 部分取回、不解除）**：wait 节点 waitType=event——DSL event 四字段（eventKey 支持 {{}} 插值/静态白名单 ≤128、timeoutSeconds 1-3600、onTimeout continue/fail）、per-tenant EventWaitBroker 进程内挂起与信号（广播/直投/预置 waitEvents、超时与协作取消）、三 REST 端点（viewer 可读、operator 信号）；超时 fail=WAIT_TIMEOUT_FAILED，运行时 key 非法=WAIT_EVENT_KEY_INVALID。收口门：后端 **1435 passed / 59 skipped**（净 +37）、前端 **606/2**、lint/build 干净；HTTP 冒烟 19 检查全过（`.smoke/event_wait_smoke.py`），浏览器冒烟 2 截图 docs/smoke-shots/event-wait-*。中断帧/恢复扫描器/多实例、动态时长、竞速取消订阅、公开免登录信号口仍缓做。

72. ✅ **打包 N 落码收口＝定时触发调度器（docs/68，2026-09-26 用户「先开 N4 定时触发」；① 立项 `3bf3824`＋`4de7933` → ②③④ 后端落码 `dae7c77`＋`e1d5869`＋`4e8f2a4`；**⑤⑥ 已补齐 ⇒ 本批收口、N4 闭合**）**：解 docs/63 §0A **N4**——`schedule/cron` 自 W 系列起只有 `graph/dsl.py:1433` 的"填没填"校验，`src/atlas` 内**没有任何调度器**（grep 复核〔跑〕），而 docs/05 三处以"定时触发"为承诺 ⇒ 实况是"能把 trigger 选成定时、能发布、图永远不会自己跑"。**七条决策**（docs/68 §1）：D-1 自研最小 5 字段 cron 子集、**零新依赖**（**ADR T30**，依 T16/T17 先例；不支持 `?`/`L`/`W`/`#`/秒/`@daily`，坏表达式保存期 422 `NODE_TRIGGER_CRON_INVALID`，**不做"看不懂就当 `*`"兜底**）；D-2 **UTC-only**（命名时区留待 docs/08 B 组函数库）；D-3 只跑发布钉版、永不回落草稿；D-4 注册＝发布派生；D-5 **槽位一次性认领**（`schedule_fires` PK `(schedule_id, slot_utc)`＋`ON CONFLICT DO NOTHING`，照 docs/62 L2 纪律 ⇒ **重启不补跑**、双进程也只触发一次，这条是顺带性质不是多副本支持）；D-6 同图 running/suspended ⇒ 跳过并计数且**不消耗认领**；D-7 **不解锁多副本**（三道闸与 D19/D20/D27/D31/D32 一条不动）。形状＝`scheduling/{cron,engine,models}.py`（`engine.tick` 全注入、不持线程不 import 存储）＋迁移 030 两表＋lifespan 装配排在 `recover_pending` 之后＋3 端点（`run-now` 不写认领表）＋前端调度表与 `schedules` i18n 两档。验收 U874–U891 含**三条反向门**（短路认领／短路跳过／打开启动追赶，各自必须实跑转红）与**一次真进程端到端**（`* * * * *`＋短 tick，等真实派发落 `runs`，重启确认不补跑）。六条残余风险照实写（不补跑是选择、UTC 认知负担、无别名会拒外部表达式、内存档认领易失⇒重启可能重跑一次、关调度不取消在途 run、**N3 仍开**）。**下一步＝按 docs/68 §5 的 ②–⑥ 原子序落码（`feat(scheduling)` → `feat(api)` → `test` → `feat(web)` → 真机冒烟＋收口回填）**；本批不解除任何缓做、不改 PROD 判定。
    **②③④ 已落码（2026-09-26 同日）**：`dae7c77`＝`scheduling/{cron,engine,models}.py` ＋ 迁移 030（`schedules`/`schedule_fires`，租户复合键，`slot_utc` 用 **TIMESTAMPTZ**——它是唯一键，文本键的 `Z` 与 `+00:00`、微秒省略会给同一槽两行）＋ DSL `NODE_TRIGGER_CRON_INVALID`（含"5 年内永不触发"）；`e1d5869`＝lifespan 在 `recover_pending()` 之后起 daemon tick、3 端点、两档 store（**store 是全局的不是每租户一份**：`TenantRegistry` 惰性装配会让"重启后还没被请求碰过的租户"永不调度）、发布派生登记与撤销、demo reset 连带清、`ATLAS_SCHEDULE_ENABLED`/`ATLAS_SCHEDULE_TICK_SECONDS`（缺省 30、clamp 1-300）、`_webhook_run_worker` 泛化为 `_background_run_worker`（`runs.mode` 新增 `schedule`）；`4e8f2a4`＝U874–U891 四文件 65 例，**三条反向门各自植缺陷并实跑转红**（认领恒真⇒双发／busy 判假⇒没有跳过／放宽回看窗口⇒开始补跑），PG 直连 5 例含"两档投影逐键全等"与"内存档易失按契约方向断言"。**测试侧自我修正照实记**：`tests/conftest.py` 默认关调度线程——三处 `with TestClient(app)` 会真跑 startup，线程活过整场测试并把别的用例的共享审批 broker 放行掉（实测吹红 `test_subgraph_events` 的审批用例）。门：**1934 passed / 119 skipped / 0 failed**（基线 1874/114）。**八条落码偏差逐条记在 docs/68 顶部进度注记**（含"没有新增 `dispatch_failed` 动作"与"事件载荷沿用 webhook 的双层嵌套、不顺手纠正"两条理由）。同步面本轮已回填 docs/02·03·04（新增 §5.21 运行期语义）·05·09·11·12·13·14（新登 **D41**）·15（新增 §六）·30·63·README·`.env.example`·CHANGELOG·handoff。****⑤⑥ 已补齐（同日）**：`14f7127`＝`POST /api/schedules/cron-preview`（read 档 POST，不落库不占槽；**加它就是为了不在前端复刻第二个 cron 解析器**——两份实现对日/周并集、`7`＝周日这类边角迟早分叉，表现为字段看着合法、保存才 422）＋ DSL 把 `reason` 放进 params 让两语模板能包住后端解释；`4bcc26d`＝U892–U894；`509d6f3`＋`fa2abb3`＝控制台「定时调度」页（图/cron/钉版/下次触发 UTC/最近触发/跳过数/开关/立即运行）＋画布 `cron-input` 业务控件（结果按"属于哪个表达式"存、靠派生显示⇒清空字段不残留；预演失败只显"问不出结果"）＋ `schedules` 两档纳入 `PARITY_PAIRS`；`1f318c4`＝`scripts/dev/schedule_e2e_smoke.py` 真机四场景（**无人点击**下 11:01:02 自动派发并落认领行 → 停服 130 秒 ⇒ 停摆窗那一分钟**既无 run 也无认领行**、当分钟照常派发 → run-now 起得来 → 改手动再发布撤销登记且自收尾）。浏览器实测：入口在、行内"下次 10:55／最近 10:50"由活线程写下、开关往返、`*/5 * * * *` 显"11:00 · 11:05 · 11:10"而 `5/2 * * * *` 显后端原文、清空后无残留。收口门：**后端 1939 passed / 119 skipped / 0 failed**、PG 直连 5 passed、前端 **736/2/55**、oxlint 0/7、build 1730.23 kB（gzip 536.67）。**N4 就此闭合**——但闭合的只是"代码那条阻断"：N3 产品表态、外部凭据、真 prod 演练、种子客户仍开，单副本三道闸一条不撤。
71. ✅ **打包 M＝渠道工具接通通用参数通道（docs/67，2026-09-25 承接用户「你自己能搞定吗」授权自推；`fix(graph)` → `test(graph)` → 收口 docs）**：修 **docs/63 §0A N2**——`channel:shopify/<binding>/create_refund` 在图里必失败：路由只认 `{{http,database,message,memory}}`＋`openapi:`（`loader.py:1701`），渠道 id 落进 demo shop 硬编码装配只拿到 `note` ⇒ 真适配器取 `order_id` KeyError。**修法一行**：`startswith(("openapi:", "channel:"))`，全仓仅此一处路由门（收口 grep 复核）。不另写第二套装配的理由（能力 `input_schema` 本就是对象、`shop` 形状不同、`GENERIC_JSON_ADAPTERS` 不收带后缀 id）记 docs/67 §1。**6 条测试（U868–U873）只假在 HTTP 传输层**：真适配器＋真客户端＋真图执行，断言请求体 `transactions[].amount=="299.00"` 与 `/orders/12345/refunds`；非法 JSON 零次外呼；缺参由适配器报；**U871 完整链形** trigger→ai_decision→human_approval（预置通过）→真渠道退款，补齐 docs/63 §2.2"两段互不覆盖"；U872 静态反证＋**U873 并排反证**（同一真适配器改名成非 `channel:` id 就退回缺参失败）。门：`pytest` **1874 passed / 114 skipped / 0 failed**（净增 6 条常跑）。**口径钉准**：关闭证据是"真适配器＋假 HTTP"，**真 Shopify 200 仍待外部凭据**；`amount` 传字符串与部分退款分摊本批不加校验（避免凭想象收紧，docs/67 §4）。**N3（浏览器自动化算不算 MVP 范围＝产品表态，留给你）与 N4（无调度器）未做 ⇒ PROD 仍不 GO**。
70. ✅ **打包 L＝prod 首任管理员引导落地（docs/66，2026-09-25 承接用户「开搞」；`66e977e` feat → `4f980df` test → 收口 docs 原子）**：修 docs/63 §0A 的 **N1——prod 全新安装谁都进不去**（`iam/deps.py:43` 照播明文种子口令、`deps.py:74-81` 又在 prod 拒这些口令、改密与建号都要求已有身份）。**选定＝第三方案**：prod 必填 `ATLAS_ADMIN_BOOTSTRAP_PASSWORD`（复用 `validate_password`），缺失或弱口令**拒绝启动**；prod 播种只播**各租户一条 ADMIN ＋ 引导口令**，operator/viewer 由首位 admin 经 `POST /api/users` 建。被否的 ①首登豁免＋改密令牌（新造令牌系统且重开弱口令窗口）／②带外 CLI（多一条会被漏的部署步骤、外部持有 DB 连接与哈希实现）记 docs/66 §1。**真机验过**：prod 缺口令连 `import atlas.iam` 都抛；给 `short` 抛"不合口令策略"；给合法引导口令则 `admin-a` 可登、`admin123` 被拒、`operator-a` 不存在。**dev/test 零变化**由 U864 的 `is SEED_USERS` 钉住；**唯一改动的既有测试**＝`test_assert_prod_secrets_passes_with_both_keys`（前提确实变了：补变量＋注记原因，旧语义由 U861b 反向覆盖，不是凑绿）。同批修 **N5**（`.env.example` 的 `ATLAS_ENV=development`→`dev` ＋ U867 守护）、compose 开始注入 `ATLAS_ENV` 与三把密钥（prod 形态首次可表达，`docker compose config` rc=0）、README 过期措辞更正。**N2/N3/N4 未动**，"核心闭环可用"仍不成立；docs/08 A 组 L-1 划销、L-2 仍开。门：`pytest` **1868 passed / 114 skipped / 0 failed**（基线 1856/114，净增本批 12 条常跑；受影响三文件复跑 36 passed；`docker compose config` rc=0）。
69. ✅ **【治理批】两条"纸面约定"改成可机检守护＋handoff 断链修复（2026-09-25 承接用户「按你建议来」，纯 docs＋测试原子）**：① `MIGRATION_CONVENTION.md` §3 重写——原英文模板头自 2026-09-13（`e9c2b73`）起只有 **6/29** 个迁移遵守、运行器从不解析（版本权威＝文件名＋`schema_migrations`，无内容 checksum），故改为两条真规则（文件名 `NNN_verb_snake.sql` 且从 001 严格连续；首个非空行必须是 `--` 注释），旧模板整块移入 §3.1「留档不执行」，§5 示例换成现行实况（照抄 `002` 的 `interruptions` DDL）并明写"后续列变更走新迁移、绝不回改已应用文件"；② 新 `tests/test_migration_convention.py`（命名／连续性／首行注释三条守护 ＋ 一条**反向门**：在 `tmp_path` 里造完整 001–030 再逐项破坏，断言各自变红，且断言第 3 条不回管 029 及以前）；③ docs/29 状态行的「清单权威」就地更正为**已由 docs/63 取代**（正文按留档规则不改）；④ **修 handoff 引用断链**：Active work 里 **打包 G／打包 J／打包 K 三批从来没有条目**，而状态行把 J 指到 `#65`（实为第四次复审）、把 K 与 G 指到不存在的 `#66`——现补 #66（G，注明事后补登记）／#67（J）／#68（K）并改正指针；⑤ 新 `tests/test_handoff_integrity.py`：Active 编号唯一、全文每个 `Active work #N` 引用都能解析、Recently shipped ≤ 5 条。**过程诚实记录**：我一开始把"Active 编号撞号"报成缺陷，实测发现重复的 1–5 属 Active work 与 Recently shipped **两张独立列表**——是我合起来量错了，结论已撤回；真正的缺陷是三条悬空引用。另：反向门暴露并修掉我自己守护里的真 bug（命名不合规的文件会让第 3 条抛 `AttributeError` 而非报告），且本轮**没有**为了凑守护去改 `010_iam_users.sql`（权限门拦下时理由成立：改已应用迁移正是新约定禁止的事），改为把守护限定到 030 起。
68. ✅ **【打包 K】P1-2 运维批收口（docs/65，2026-09-25 用户「继续推进任务」；`c965d22` 立项 → `5aa51ca` retention＋会话清扫 → `2d3ec70` 备份轮转·异地 → `0082b1e` `/metrics` prod Bearer 闸门 → `d171639` 日志 formatter＋request-id → `f0ae1f8` 收口）**：补齐 docs/63 §3 S8 与缓做 D38/D39 的运维下限。**登记说明：本条为 #69 治理批事后补登记**（原收口只写了 Recently shipped，状态行指针 `#66` 悬空）。关键实现口径：`PgBackend.prune_expired` 对 `runs` **只删终止态**、对 `interruptions` **只删已认领帧**（与 docs/62 的 at-most-once 对齐，挂起态永不误删），TEXT 时间列一律 `::timestamptz` 比时刻；备份 `ATLAS_BACKUP_KEEP`（默认 7）＋`ATLAS_BACKUP_REMOTE_DIR`＋`--dry-run`。本轮我逐条回代码复核为真〔码〕，门：后端 **1849/114/0**、前端 728/2/54 沿用（本批未碰前端）。D38/D39 取回、**D11 不解除**（正式 OTel/Grafana 栈仍是另一层）。
67. ✅ **【打包 J】上线最小集工程批收口＝docs/63 的 P0-1 安全闸门＋P0-2 核心完整性＋P1-1 门禁（docs/64，2026-09-25 用户「推进任务」；`7600157` 立项 → 11 功能原子＋2 修正原子 → `7d62f7e` 收口）**：**登记说明：本条为 #69 治理批事后补登记**（原收口只写了 Recently shipped，状态行指针 `#65` 当时被别的条目占用）。落了：`security/bootstrap.py` 真读 `ATLAS_ENV` 且 prod 缺密钥拒启（J-1a）、邮件决策 token 绑收件人 `rcpt`（J-1b）、prod 不播种＋拒种子口令（J-1c，**其"首个管理员从哪来"的死锁由第四次复审 N1 查出、仍未修**）、登录查询 `ORDER BY tenant_id` ＋用户名全局唯一契约（J-1d）、决策器运行模式启动打印（J-2a）、跨审批真实退款全链 e2e 测试（J-2b，打的是 `DemoShopService`）、waits/tasks 运行时操作台（J-2c，D40 取回）、CI 加 postgres 两个 job＋修 audit `seq` 投影红（J-3a）、webhook 线程池＋1MB 上限（J-3d）、demo mock 面 prod 开关（J-3e，**实况只覆盖 3 条路由，见 docs/63 §0A**）。**两处改口径收口**：`limit` `le=` 落码后**还原**（10 处端点本已自带 clamp/reject，加 `le=` 反把 clamp 变 422，docs/64 §10 勘误）；J-3b 以"确认有界"收口。门：后端 1834→（K 后 1849）/108→114、前端 728/2/54、PG 直连全量 1941/1→1962/1（原 seq 红消除）。
66. ✅ **【打包 G】运行时错误码 i18n／OpenAPI 硬删／静默编辑·值班轮换／断点持久化／投递 PG 化收口（docs/60，2026-09-24）**：**登记说明：本条为 #69 治理批事后补登记**——G 当时只写了 Recently shipped 与 Quality gate，Active work 缺条目，导致状态行 `Active work #66` 悬空一整天。内容以 docs/60 与本文件 Quality gate「2026-09-24 打包 G 收口」条为准（含 `ATLAS_MASTER_KEY`／迁移 025 投递 PG 化／静默与值班 PG 化）；细节不在此复制。
65. ✅ **第四次上线复审＝撤回上轮判定（docs/63 §0A，2026-09-25 承接用户第二次「做一次项目级别的…必须达到产品核心完全可用的MVP」评审；纯 docs 原子）**：本轮不复用上轮证据，改做**真路径核验**（上轮只核到"守卫存在"，没核"守完之后合法部署路径还走不走得通"）。**两条新阻断**：**N1 `ATLAS_ENV=prod` 全新安装登录死锁**——`iam/deps.py:43` 应用自己在 import 时播种种子口令（`storage/pg.py:469` 用 `hash_password(user.password)`），而 `deps.py:74-81` 在 prod 拒绝种子口令登录、改密两条路都要先有身份（`main.py:1404-1410` 需 `get_principal`、`:1432/:1479` 需 `administer`）、全仓无带外建号手段（grep `BOOTSTRAP|FIRST_ADMIN|CREATE_ADMIN` 0 命中）⇒ **新 prod 库里不存在能登录的账号：J-1c 把 S2 从"弱口令可登录"修成"谁都进不去"**；**N2 真实店铺退款在图里跑不出来**——`graph/loader.py:1699` 的通用 JSON 通道只放行 `{http,database,message,memory}`＋`openapi:`，`channel:shopify/<binding>`（`channels/adapter.py:63`）落进硬编码装配分支只拿到 `{"note": …}` ⇒ `channels/adapter.py:110-114` 取 `order_id`/`amount` KeyError ⇒ 节点 FAILED；全仓 `channel:shopify` 仅 `tests/test_api_channels.py:148/153` 断言可被发现、**无一条测试把图跑过它**；并更正我上轮引用的 `test_refund_e2e_full_chain.py`——它打的是 `DemoShopService`（`:80`）＝演示店不是真实店铺。另三条落差：**N3** PRD 要求的 `web-playwright` 未接入运行期、且被 `tests/test_graph_loader.py:79` 反向锁死；**N4** `schedule/cron` 只有 `dsl.py:1433` 校验、无调度器；**N5** `.env.example:11` 的 `ATLAS_ENV=development` 非法（实跑 `read_env_profile()` 抛 ValueError）。**同时更正我自己上轮写宽的两处**：S5 实况只守住 3 条 mock 路由（`main.py:3792/3806/3830`），`/api/demo/mock/orders*` 与 `/api/demo/shop/*` 仍无鉴权；「S1–S8 清零 ⇒ 工程侧就绪」不成立。门仍全绿（本轮复跑后端 **1849/114**、前端 **728/2/53**、lint 0 error、build ✓）——**门全绿不等于流程跑得通**。**修订后判定＝「产品核心完全可用的 MVP」未达到；demo／陪同试用可交付、自主上线不可交付。**同步＝docs/63 新增 §0A（撤回注，原文保留）＋docs/00·docs/34 入口判定句更正＋docs/08 A 组重开两行（**L-1 首任管理员引导策略需你拍板**、L-2 真实渠道工具接通）。教训入档：**验证安全收紧的判据必须是"合法部署路径仍走得通"，不是"这段代码有单测"。**
64. ✅ **第三次项目级上线复审（docs/63，2026-09-25 承接用户「做一次项目级别的功能性、完整度、是否可上线的评审，要求必须能达到产品核心完全可用的MVP，判断是否达到了MVP」；纯 docs 原子，零产品代码改动）**：**判定＝技术验证 MVP 达到、生产 MVP 未达到，PROD 不 GO。** 并行三份勘察（前端可达性／里程碑完成度／生产加固面），逐条按〔跑〕〔码〕〔勘〕三级标注证据，**〔勘〕级一律不作上线判定依据**。门实测：后端 **1811/108**、前端 **722 passed / 2 skipped / 53 文件**、`pnpm lint` 0 error——工程纪律是真的强。**但三条打到产品核心**：① `docker-compose.yml:52` 的 `LITELLM_MODEL: ${LITELLM_MODEL:-}` 配 `llm/decision.py:93-97` 的**静默**退规则，一键起来的是一套**没有 AI 决策**的编排壳；② 退款端到端**没有一条测试跑通全链**（`test_refund_e2e.py:34` 无审批节点、`test_api_demo.py:199` 审批后不落 shop 工具），docs/08 §7.3 criterion 4 靠"自动执行退款**或**触发人工审批"的"或"字撑住；③ M7 验收链 `coordination/sandbox.py:29` 从不调 `compile_graph`/`run_graph`。**八条阻断 S1–S8**（最重＝S1 邮件一键决策是不记名 capability token、密钥缺省为仓库写死常量 `email_token.py:26`、compose 两密钥都不注入、缺密钥只 warning ⇒ 看源码者可给任意租户签"退款通过"链接；根因＝S7 `ATLAS_ENV`/`DEBUG` 只在 `.env.example`、`src/atlas` 从不读 ⇒ **没有任何一道能 fail-closed 的闸门**；另 S2 种子口令 `admin123`、S3 登录查询不带 `tenant_id` 且无 `ORDER BY`（PK 却是 `(tenant_id,username)`）、S4 入站 webhook `async def` 里跑同步 SQL 会卡整个事件循环、S5 十三条无鉴权路由里七条 `/api/demo/*` 匿名可写、S6 十处 `limit` 无 `le=` 加三处 litellm 无 timeout、S8 全无 retention）。**结构性缺口单列一节**：CI 无 postgres service，**44 / 1918＝2.3% integration 永不执行**（实跑 `-m integration` 立刻抓出一条既存红：`PgAuditStore.record()` 投影缺 `seq`）⇒ M5b 以来"已 PG 化／跨重启可见"承诺目前**没有任何自动化在守**。产出＝docs/63 新建 ＋ docs/00 地图行 ＋ docs/34 指向注 ＋ docs/08 决策记录条与 A 组 3 批候选（P0-1 安全闸门／P0-2 核心完整性／P1-1 门禁）＋ docs/14 新缓做 **D38/D39/D40**。**单实例结论沿用不变**：docs/62 L2 只拆最贵那颗雷，D19/D20/D27/D31/D32 仍不解除。
63. ✅ **打包 I L2 帧一次性认领落码收口（docs/62 §3；2026-09-25 承接用户「认领 docs/62 L2 帧的落码」；`af38176` feat(storage) → `593bed0` feat(graph) → `df5a18c` test → docs 收口原子）**：上一条留下的"L2 只出契约"已兑现成代码。**形状**＝迁移 029 给 `interruptions` 加 `resumed_at`/`resumed_by`（不建新索引，认领按主键单行命中），`storage/recovery.py:claim_frame_for_resume` 用**一条 `UPDATE ... WHERE resumed_at IS NULL` 的 rowcount 充当互斥本身**（不引 advisory lock／`SELECT FOR UPDATE SKIP LOCKED`／定时器，时钟只取 DB，库不可达保守判负），`graph/loader.py` 三处挂起点醒来先认领、认不到抛控制流 `RunSuperseded`（与 `RunCancelled`/`DebugStopped` 并列进两处穿透豁免，否则被子图 fail-safe 吞成节点失败）；四个入口各有"输家零终态写"分支（sync `/run` 返 suspended 形状、流式发 `event: superseded`、webhook 只记日志、续跑线程不清帧），恢复扫描加第 5 个接线点＝**已认领帧不再重建 pending/起线程**。**为什么门在 loader 不在续跑线程**：inline 续跑与重启续跑两条路径都经过 loader，而续跑线程是重启后唯一的驱动力、删不得（契约 §2 D-2 把"降级恢复扫描"列为已否方案）。**实测验收**：端到端硬门 `scripts/dev/multi_instance_resume_recon.py` 退出码 **1→0**（一次审批下游落库恰 1 次，通过×1／拒绝×0；改造前双写）；**反向门不去拆生产 SQL**，改在测试里并排放两条对照（认领恒真→两人都跑完，证明放行来自 False；去掉 `resumed_at IS NULL` 的同一 UPDATE 两次都 rowcount 1，证明承重的是谓词），另实跑植入缺陷 `_gate_resume` 短路→新文件 12 例红 8 例、还原全绿。**门**：`pytest` **1811 passed / 108 skipped**（基线 1799/105）、`-m integration` 临时库 **43 passed / 1 failed**（唯一红＝既存 `test_pg_audit_store_roundtrip_list_export`，`PgAuditStore.record()` 投影缺 `seq`，被 skip 掩盖，已入 docs/08 B 组候选）、前端零改动未跑前端门。**口径**：**D19/D20/D27/D31/D32 一律不解除、单副本约束与三道闸一条都不撤**（本批只拆最贵那颗雷，节流/`/metrics`/灰度态/跨进程急停未动）；D36 是**有意引入的收紧**（认领后崩溃＝永久 suspended、不自动重放）；新增 **D37**＝前端不认 `superseded` 帧会把接管报成「SSE 流缺少最终运行结果」。**自我订正两处**：docs/62 §1/§8.2 与 docs/15 把「入站 webhook 幂等环」当进程内残余，读 `channels/webhooks.py:171-183` 证伪（PG 档先查 `webhook_deliveries`，仅 store 不可用才退内存环），已就地标注、不改写原句。
62. ✅ **打包 I 立项＋L1 单副本护栏落码（docs/62，2026-09-25 用户「那你搞」；`57a2477` 立项 → `4877677` chore(deploy) → docs(ops) 声明）**：起因＝实测出的缺陷级阻断项——**多副本不是「暂未支持」而是「开了会重复扣款」**：第二个进程启动时 `recover_pending`（`api/main.py:154`）会为 A 仍持有的活帧重建 pending 并起续跑线程，人工在 A 决策 approved 后 A 走通过分支、B 在超时后走 reject，同一条 run 下游落库两次且分支相反；对照组单实例只落 1 次（脚本 `scripts/dev/multi_instance_resume_recon.py`，退出码非 0 即实锤）。**L1 当批落码**：`Dockerfile` 显式 `--workers 1`（此前是没写才恰好为 1 的偶然安全）、`docker-entrypoint.sh` 对 workers>1 与非整数值 fail-closed、compose 钉 `replicas: 1`、docs/15＋README 明文单副本约束。**L2 只出契约**：迁移 029 两列＋原子认领函数＋恢复扫描保持不变、认领落 loader 三处挂起，取向已从「owner＋惰性租约」**自我修正为 at-most-once**（下游副作用不幂等，可回收租约＝崩溃后隔一会儿再跑一遍），代价登记为新缓做 D36；验收含反向门（拆掉认领谓词，脚本必须重新报双跑）。零新依赖/无 ADR/零新端点·错误码；D19/D20/D27/D31/D32 不解除、触发条件重述为「上多副本前必须先完成」；另记 sync `/run` 急停单实例也恒 409（`register` 只在流式路径）为独立小批。 **〔同批追加〕i18n 守护补全已落两原子**：`776b62c`（`dsl.SUB_INPUT_VALUE_REQUIRED` 中英两侧的示例 token 改为非插值写法、顺行使两侧插值集合一致）＋`1a07582`（`validation` 纳入 `i18n.test.ts` 的 `PARITY_PAIRS`，13 个 namespace 全覆盖）；反向门逐一验过——植入「zh 多键／en 混汉字／插值两侧不一致」三种漂移，对应守护逐条变红、还原即绿。CI 门 `pnpm lint` 0 error（6 既有 warning）、`pnpm build` 过、i18n 套 55 passed。**L1 验证（实跑）**：五路拒绝 rc 分别 1/1/1/1/2（`ATLAS_WORKERS=2`、`--workers 4`、`--workers=3`、`-w 2`、非整数 `abc`），均先打印中文原因再退出；`workers=1` 与 `--workers 1` 越过闸门进入迁移步；`docker compose config` 渲染出 `replicas: 1`、`edge` profile 仍三服务合法；**反向门**＝把判据临时改成 `-gt 99` 后同一句 `ATLAS_WORKERS=2` 不再被拦，证明拦截来自阈值而非永真断言。
61. ✅ **上线就绪复审更新＋单实例硬约束实测（2026-09-25，docs-only 口径同步＋两个可重跑勘察脚本，零产品代码改动）**：承接用户「看下里程碑，还有哪些任务要做」→「先做 1 再试 3」。**① 文档收口**：docs/34 追加 2026-09-25 复审更新注记，§五 P0/P1 八项逐条对活代码复验（真实投递 `message/smtp.py`·`message/im.py`·`message/webhook.py`、真实渠道 `channels/shopify.py`·`channels/webhooks.py`、OAuth2 `connections/oauth.py:121/161`＋`storage/pg.py:2155`、审计导出与 H3 过滤均已落），原清单「零真实投递」与 §六「目录下确无 smtp/imap 文件」**被代码证伪**（历史正文按治理规则保留、只在注记里标注）；LICENSE 口径纠正三处（README「未定」→ 已由 `f721c18` 拍板不开放、handoff Active #30 的待拍板划线、docs/14 同期注记补反转说明）。**② 实测**（独立勘察库跑 001–028 全新迁移、测毕 `DROP DATABASE`）：`scripts/dev/w2_split_brain_recon.py` 测出四项跨 worker 不可见——灰度态 40 次 GET＝已存×23／null×17、流式急停＝200×30／409×10（409 文案「运行已结束」而 run 其实在另一进程挂起）、登录节流 12 次＝401×10／429×2（5 次/600s 每进程各一份＝爆破窗口翻倍）、`/metrics` 冷启首轮 1×14／0×26。`scripts/dev/multi_instance_resume_recon.py` 测出**最严重一条**：B 实例启动时 `recover_pending`（`api/main.py:154`）替它重建了 A 仍持有的活帧 pending 并起 `_resume_run` 线程，人工在 A 决策 approved 后 A 走通过分支、B 因收不到跨进程 Event 在 25s 后走 `onTimeout=reject`，**同一条 run 落库两次且分支相反、终态互相覆盖**；对照组 `RECON_SKIP_B=1` 只起一个实例＝只落 1 次，确证跨进程成因。**方法学教训**（已写进脚本头）：第一版用 keep-alive 长连接，整串请求被钉在同一个 worker 上，测出「审批不分裂」的**假绿灯**——改每请求新建连接并加分发自检探针后才拿到真分布。**③ 结论与口径**：多副本风险不是「不可用」而是「重复扣款」，D19/D20/D27/D31/D32 的「随真实部署批」重述为「上多副本前必须先做」，五条均不解除；另记一条与 worker 数无关的既有缺口——sync `/run` 从不 `cancellation_broker.register`（只在 `main.py:2696` 流式路径），单实例下急停也恒 409。**待拍板**：是否先立项做「跨实例续跑认领」最小改造（帧加 owner＋租约，或干脆禁止第二进程认领活帧）。真实客户试用数据（docs/18 §5 仍「假定 Go」）依旧是唯一挂在业务侧的 P0 级缺口。

60. ✅ **【打包 H】编译诊断定位＋审批历史 PG＋审计过滤分页＋影子运行 PG＋口径勘误（docs/61，2026-09-25 docs-only 立项，用户拍板「打包 1–5 先做，出一份 docs-only 契约立项」；**五项已全部落码收口**（下方保留立项原文＝设计口径，落码事实/门结果/七处偏差见本文件 Recently shipped #1 与 docs/61 顶部哈希链＋注记），形状权威＝[docs/61](docs/61-编译诊断定位-审批历史PG-审计过滤分页-影子PG化-口径勘误v1批契约设计.md)，落码按其 §9 七步原子序）**：部分取回 D11/D20/D26 余部并收口 docs/20 M2 的 `locations` 侧车契约遗留；**零新依赖、迁移 027/028、无 ADR、零新 REST 端点、零新错误码、不解除缓做条目**。五项：①**H1 编译 422 诊断逐条定位（后端零改动）**——后端 422 body 已含 `detail/codes/params/locations` 四路（`api/main.py:307-319`、侧车 `graph/dsl.py:155-169`），但前端 `apiClient.ts:253/268` **只读前三路、`locations` 被丢弃**且 `resolveValidationList` 把逐条映射 join 成单串，故 M2 设计的稀疏侧车从未被消费；本批 apiClient 增 `CompileIssue` 与纯函数 `buildCompileIssues`（以 detail 下标为主键、locations 按 index 归位、codes/params 非等长整列忽略）、抛携带 `issues[]` 的 `CompileValidationError` 子类（**`message` 仍为既有 join 串、逐字一致以防 toast/日志/断言回归**），editorStore 增 serverIssues 通道（编译成功/切图/图变更即清），Problems 面板合并本地 `useProblems()` 与后端诊断并**复用既有 `[data-pointer]` 精确·前缀匹配＋FLASH_CLASS 闪烁机制**（`ProblemsPanel.tsx:23-24`），按 `(nodeId, pointer, code)` 三元组去重、本地优先（本地带 quickFix），后端项仅补跨图 `SUBREF_*`/深层子图 `owner` 归并等本地覆盖不到的码，图级错误（无 nodeId 且无 pointer）不可点击；store 只存 code/params/中文兜底、**渲染期** t() 解析故切语言自动重渲染，**不新增 i18n 键**（170 编译码已在 `validation.dsl`）。②**H2 已决审批历史 PG 化（D20 子集，迁移 027）**——新模块 `collaboration/history.py`（`ApprovalHistoryStore` Protocol＋InMemory deque ring 200＋Pg 版），迁移 `027_approval_history`（PK `(tenant_id,token)`、`seq` 取 `storage_id_seq`、索引 `(tenant_id, seq DESC)`、惰性裁 200 照 `message/deliveries.py:110-117` 先例），四类决策来源（人工 REST/邮件链接/超时/run inputs 预置）**经 broker 单一落定点写历史不散写**、写入 fail-safe 只 warning、**绝不因历史落库失败阻断审批放行**（照 G5 `_safe_record` 教训），`ApprovalBroker(history_store=None)` 缺省内存版故既有测试零回归，`registry.py:132` PG 档注入、reset 随 approval_broker 清本租户（与现状一致）、跨重启与跨实例可见，REST `GET /api/approvals/decided`（read）签名与权限零改动；**时间列决策**＝`_Pending.created_at/resolved_at` 现为 `time.time()` float epoch（`approvals.py:28/38`），落库与投影统一转 UTC ISO-8601 TEXT（对齐 002/024 主约定，不采 025 `sent_at TIMESTAMPTZ` 例外）；**投影决策**＝增 `resolvedAt`（超集）＋`createdAt` 由 float 改 ISO 串（**破坏性**，依据＝该端点 2026-09-22 docs/37 才上线、消费点仅 `pages/Approvals.tsx`「已处理」Tab 一处；落码 grep 若超两处则退化为并存 `createdAtIso`，偏差回填 docs/61 §3.3）；`notify_recipients`/`card_context`/`action_id` 不入投影；**pending 挂起审批不动**（已由 `storage/recovery.py` 帧表＋`interruptions` 承担）。③**H3 审计 actor/时间过滤＋游标分页（D11 子集，零迁移）**——`AuditEvent` 增 `seq: int = 0`（带默认值免既有夹具全改；内存档由 store 侧持锁单调计数赋真值、PG 档读回迁移 013 既有 `seq` 列，两档对齐故共用游标），两档 `list` 统一签名 `list(limit=100, *, action, actor, since, until, cursor) -> (items, nextCursor)`：`action` 前缀沿用（PG 侧 LIKE ESCAPE 已有）、`actor` **精确等值**（取值域固定 username/anonymous/email-link/shopify-webhook）、`since`/`until` 为 UTC ISO-8601 字符串**按 TEXT 字典序闭区间比较**（安全前提＝全部 `at` 由同一 `datetime.now(timezone.utc).isoformat()` 生成、同格式同 `+00:00` 时区，**须以不变量测试锁死**）、`cursor` 取 `seq < cursor` 倒序更早一页、`limit` 为页大小 clamp 1-500 默认 100；`export_jsonl` 受同套过滤但**不接受 cursor**（导出全量匹配、正序 `seq ASC` 不变）；两端点（`GET /api/audit/events`、`/api/audit/export`，均 administer 不变）响应超集增 `nextCursor`、items 增 `seq`；**422 口径**＝项目无全局 RequestValidationError 中文处理器，故端点内手工校验抛中文聚合 422（照 G3 静默 PUT 先例）覆盖非法 ISO/`since>until`/`cursor` 非正整数，`limit` 维持静默 clamp；审计中间件（`main.py:358-385`）与登录显式记一条（`:1183-1195`）零改动、GET 读操作仍不记；前端 actor 输入＋**原生 datetime-local 双输入**（本地无时区串→UTC ISO，换算抽 `lib/auditFilters.ts` 纯函数可单测；RangePicker 已回退，见下）＋Table 由 `pagination={false}` 改游标「加载更多」（累积 items、`nextCursor===null` 禁用），audit namespace 补键须过 zh/en 对齐·en 零汉字·插值三守护并进 `PARITY_PAIRS`。④**H4 影子运行 PG 化（D26 子集，迁移 028）**——新模块 `recording/pg_shadow.py PgShadowStore` **照 `recording/pg_reports.py:35 PgReportStore` 全套抄形**（方法签名与 `shadow.py:264 ShadowStore` 一比一：add/get/list clamp 1-200/attach_outcome/reset），id 用 `nextval('storage_id_seq')` 生成 `sr-{n}` 与内存 `shadow.py:287-288` 同形、数字部分另存 seq 列，ring 100 惰性裁剪照 PgDeliveryStore `OFFSET :keep` DELETE 先例（并发下短暂超 100 可接受、不做强一致），迁移 `028_shadow_runs`（PK `(tenant_id,id)`、四子模型 ToolIntent/ShadowDecision/HumanOutcome/ShadowComparison 走 JSONB 用 model_dump/model_validate 读写，照 `openapi_imports.operations` 与 `monitoring_runs.spans` 先例、`created_at` TEXT 承 `shadow.py:301 _now_iso`），`TenantServices.shadow_store` 改联合类型 `ShadowStore | PgShadowStore`（照 `registry.py:74 report_store` 先例）、`:141` PG 档换装配 `:169` 内存档不变、reset 清本租户（`registry.py:204` 已在清单），**REST 四端点（`main.py:1779/1855/1867/1876`）与前端零改动**（照 ReportStore 先例）。⑤**H5 实现/文档口径勘误（零行为变化）**——修三处「实现已 PG 化但注释/docstring 仍写不 PG 化」的矛盾：`monitoring/alerts.py:74/76/78`（rule_name/escalated_at/assignee 实已随迁移 021＋`pg.py:1040-1042/1033-1038/937-941/1082-1088` 落库）、`monitoring/silences.py:5-6`（静默/值班实已随迁移 024 PG 化、内存档仍 OpsStore）、`api/main.py:1704` docstring（报告实已随迁移 022＋`pg_reports.py`＋`registry.py:140` PG 化）；docs/33 批 3/批 4 与 docs/14 D26/D28 行内同源过时表述**按 00 治理规则加勘误注记、不改写历史正文**。**立项动因之一＝勘察发现注释与实现矛盾会误导落码**：立项前候选清单里的「告警 escalated_at/assignee/rule_name PG 化」「发布报告 ReportStore PG 化」「Graph DSL 422 code 化＋前端按 code i18n」**三项均已完成**（分别在打包 B 迁移 021 / 打包 C 迁移 022 / i18n 第二批债），故本批剔出范围、H1 只补其真正缺口＝`locations` 前端接线。测试候选 **U743 起**（H1 U743–U754、H2 U755–U770、H3 U771–U790、H4 U791–U806；H5 无新增测试靠三道门零回归；落码以 docs/13 §9 定稿）。**只许增测不许减**；立项基线（打包 G 收口实跑）：后端内存门 **1758 passed / 86 skipped / 0 failed**、PG 直连集成（migrations/storage/database/openapi/monitoring/message 六文件）**55 passed**、前端 vitest **694 passed / 2 skipped / 52 文件**、tsc 0 error、oxlint **0 error / 6 既有 warning**（cards `react-hooks/set-state-in-effect`，本批不碰）、build 过（1708.29 kB / gzip 529.73 kB）；027/028 由 `storage/migrations.py:35-36` glob+sorted 自动 apply、无清单需改。**进度：①docs-only 立项 ✅；②–⑥ 五项功能全部落码 ✅（H1 `adb9b7c`/`bbb9a2c`/`eea76ad`、H2 `33a5998`/`528f6b1`/`e380f0b`、H3 `8a9f7df`/`36a3cbb`/`a07794c`、H4 `2bb6d3c`/`3eac4af`、H5 `55c1553`）；⑦收口 ✅（三道门实跑全绿、8＋8 截图、七处落码偏差带行号回填 docs/61 顶部注记与 00/03/04/06/08/09/12/13/14/17/20/CHANGELOG）**。**非目标仍缓做**：后端 422 契约变更/后端码 quickFix/i18next、pending 挂起 PG 化与多实例决策竞态/评论流/动态审批人/通知重发、SIEM/保留归档分区/`resource_id` 与角色维度过滤/CSV 导出/给 GET 补记审计/actor 模糊搜索、影子自动旁路/SSE/报表趋势/跨租户共享、D13 元数据多语言。
59. ✅ **【打包 F】告警规则模板市场＋静默/值班/assignee PG 化（docs/59，2026-09-24 docs-only 立项并全部落码收口；D28 部分取回、不解除；零新依赖/迁移 024/无 ADR/无新写端点；dev、未 push）**：F-1 内置只读规则模板市场（`monitoring/rule_templates.py` 4 模板 default-balanced/strict-sre/demo-lenient/custom-quickstart＋两只读 GET /api/alert-rule-templates，一键应用复用 PUT rules 全量替换、Popconfirm 二次确认、无写端点；前端 AlertRuleTemplateMarket Modal＋规则卡 extra＋templates zh/en 11 键）；F-2 PG 档静默/值班/新建告警 assignee 持久化（迁移 024 monitoring_silences/monitoring_oncall，PgMonitoringStore 移除 OpsStore、命中压下计数/取模轮换/cap100/惰性过期/reset 清两表全走 PG，内存档零改动）。原子 F-0 `308335e`/F-1a `39e6daa`/F-1b `ac35b07`/F-2 `88e4be9`/U299 卫生 `ece2645`＋收口 docs。测试 U646–U662（后端 6＋PG 8＋前端 6，落码定稿见 docs/59 §5；U299 转持久化路径）。门：后端全量 **1734/77/0 failed**（8 PG 例无 env 计 skip）、PG 集成 **44 passed**、前端 vitest **675/2/50 文件**、oxlint **0 error/6 既有 warning**、build 过（1697.30 kB/gzip 526.35）、PG 档 live HTTP smoke **20/20**、双语浏览器冒烟过（英文 UI chrome 无汉字；模板 name/description 中文属 D13 豁免）。模板 CRUD/市场上传/分享/按图绑定、模板元数据多语言、静默编辑、定时轮换/排班日历/多值班组、多实例分布式锁、OTel 正式栈、长保留时序仍缓做（docs/14 D28）。
58. ✅ **【i18n 第一批债】后端认证错误码化＋前端 L1 校验诊断 i18n（docs/17 §2.4，2026-09-24 承接「一口气搞了」；前后端、零新依赖/零迁移/无新 REST·ADR，D12 部分取回、不解除）**：①后端 `iam/deps.py` 认证闸门 code 化（auth_error()，detail 为 {code,message}）——login 401 AUTH_INVALID_CREDENTIALS、停用 403 AUTH_ACCOUNT_DISABLED、缺凭证 401 AUTH_UNAUTHENTICATED、角色不足 403 AUTH_FORBIDDEN；跨租户 404 防泄漏不补 code（AUTH_NOT_FOUND 未落码）；②前端认证错误 i18n：common 补 error.auth.accountDisabled/error.requestFailed，apiClient.resolveErrorMessage 按 code 映射 error.auth.*；③L1 校验诊断全量 i18n：新建 validation namespace 82 叶子键 zh/en 对齐、en 零汉字，l1.ts 约 80 条用户可见中文全 t() 化（清零），engine.invalidateForLocale() 清缓存 + useValidationEngine 语言切换全量重算 L1/L2/L3、ToolCallConfig 参数诊断同步；l1.test 加英文态用例。门：后端 1713/69、前端 vitest 663/2、oxlint 0 error/6 warning、build 过。仍显中文：Graph DSL 422 列表（另起小批）、业务数据与元数据 D13。
57. ✅ **【打包 E】消息渠道余部：webhook 出站 HMAC 签名 / IM markdown 富文本与 @人 / 多 URL 群发（docs/58，2026-09-24 docs-only 立项；D24 部分取回、不解除；后端为主、零新依赖/零迁移/无 ADR）**：范围与三家官方消息体契约见 docs/58（形状权威；钉钉 open.dingtalk.com robot-message-type、企微 developer.work.weixin.qq.com 91770、飞书 open.feishu.cn add-custom-bot 已逐字核对）。**进度：①docs-only 立项 ✅（`4f090a9`）；②E-1 webhook HMAC ✅（`83d373a`，固定向量 hex `49b92da7…`，消息相关 129 passed）；③E-2 IM markdown/@人 ✅（`4f2b39a`，三家逐字节向量，全量后端 1709 passed/69 skipped）；④E-3 多 URL 群发＋smoke ✅（另一会话起、本会话接力收口：`_fan_out` 逐 URL 投递、per-URL DeliveryRecord、EGRESS fail-fast、投递错误 best-effort 聚合，webhook 同 id 各签一次）；⑤三道门＋docs 收口 ✅（message 两文件 96 passed、后端全量 **1713 passed/69 skipped** 净增 4、im smoke **27/27 ALL PASS**，前端本批零改动、条件原子未触发）**。**E 批整体关闭**：零新依赖/零迁移/无新 REST·ADR，D24 部分取回、不解除；短信/IM 应用 OAuth·入站/模板系统/真实平台联调仍缓做。
56. ✅ **【打包 D】i18n en-US 全量翻译＋语言切换 UI＋发布协作/审批/属性面板组件抽取（docs/57，2026-09-24 立项并当日落码收口；D12/D13 部分取回、不解除；纯前端/零新依赖/后端零改动/无 ADR）**：范围与决策见 docs/57（形状权威）与 L3 顶部收口时间块。**进度：①docs-only 立项 ✅（`44b6c89`）；②en-US 全量翻译＋三静态守护 ✅（`39328f0`）；③切换运行时（localStorage 读回/UserBadge+Login 入口/antd locale 三处联动）✅（`aca379c`）；④四组件补抽（release/rollout/feedback/card）✅（`2a5fb9f`）；⑤D-4 冒烟扩范围补抽＋浏览器双语冒烟＋docs 收口 ✅（`d05e265`＋收口 docs；审批两页整页接线＋属性面板/表单/画布约 84 处、editor 新增约 120 键；终态 vitest 662 passed/2 skipped、oxlint 0 error/6 既有 warning、build 过、U610–U625 全过、18 张双语冒烟截图）**。
55. ✅ **【打包 A】wait 时长放开/jitter/多事件竞速＋告警 recovery/lifecycle 限流（docs/54，2026-09-23 docs-only 立项；D19/D28 部分取回、不解除）**：五小项见 L3 顶部时间块与 docs/54；落码按 docs/54 §10 六步原子序（docs 立项〔本步〕→ wait 上限+jitter → 多事件竞速+跨重启 → 告警 recovery+限流 → 前端面板 → docs 收口）。零新依赖/零迁移/无 ADR/零新 REST；测试候选 U510 起。**进行中：①立项 `67650ed` ✅、②上限+jitter `76fa61c` ✅、③多事件竞速+跨重启 `dcf4626` ✅、④告警 recovery+lifecycle 限流 `2d53654` ✅（U510–U536，后端相关测试全过零回归）、⑤前端面板 `2e86862` ✅（l1/schema/WaitConfig＋7 新 L1 用例，前端 643/2、lint 0 error、build 过）；⑥docs 收口 ✅（后端 1630/61/0 failed、PG 集成 14〔含新多键 restore〕、前端 643/2/48、HTTP smoke event19/alert21/dynamic21 全绿；另修 WaitNodeFailure 全局结构化 500 handler 防 keep-alive RST、同步 docs/53 suspended 与 B1 lifecycle smoke 语义、event_wait_smoke 补 force-add 入库、新增 d54_smoke_run.sh；03/04/12/13/14/00/08/CHANGELOG 回填）。**
54. ✅ **event wait timeoutSeconds 表达式化 B5（D19 子集，2026-09-23 落码收口；D19 部分取回、不解除）**：event wait config 增 `timeoutMode`（static 默认 / expression）与可选 `timeoutExpression`（≤200）；loader 抽出共用 `_resolve_wait_expression(node_id,expr,context,lo,hi)`——duration（1-600）/event（1-3600）统一经条件引擎 evaluate_expression 求值，非 bool/非有限/越界 run failed WAIT_DURATION_INVALID；旧图缺省 static 零回归，跨重启帧存已求值秒数、resume 不重求值。前端 wait.schema event oneOf 加 timeoutMode radio/timeoutExpression，L1 按 mode 校验（static 缺 timeoutSeconds 报「超时时间为必填（1-3600）」、expression 缺表达式报必填/超长）。tests/test_event_wait_graph.py 净增 3（表达式值传给 broker、越界失败、static 零回归）、test_graph_dsl.py 净增 6（2 valid + 4 reject）；收口全量内存门 **1591 passed / 60 skipped**（净增 9、零失败）、前端 **636/2**（零新增前端测）。零新依赖/零迁移/无新 ADR·REST。**仍缓做**：>3600 长时刻、jitter、多事件竞速取消、多实例跨进程路由，触发条件不变。
53. ✅ **通知模板插值 B4 勘察判空集、不落码（2026-09-23）**：审批/告警/消息正文的 {{...}} 插值接缝已由 `graph/interpolation.interpolate` 全覆盖——所有 tool_call params（message/send 各渠道 to/subject/body）loader 执行前统一插值，human_approval summary/approver/notifyEmails、cards 渲染、event wait eventKey、LLM prompt、subgraph inputs 均已接线；告警正文（monitoring/notify build_*）由系统 alert 字段拼装、无用户可写模板（自定义规则模板随 D28 市场）。故无代码可落、无 CHANGELOG；消息模板系统其余（多语言/模板 CRUD/富文本）仍随 D24/D25 缓做。
52. ✅ **跨版本配置结构 diff B3（D26 子集，2026-09-23 落码收口；D26 部分取回、不解除）**：新纯函数 `graph/diff.py`——`diff_graph(base,candidate)` 输出 nodes/edges/variables 的 added/removed/changed，节点 changed 细分 type/name（{field,from,to}）与 config 键级（新增/删除/值变，{field:"config",configKeys}），**position 等画布布局不参与**；附 `diff_summary(diff)`/`has_changes(diff)`，零依赖。`GET /api/graphs/{id}/diff`（read、纯只读不产版本）：query fromVersion/toVersion（camel alias），toVersion 缺省＝latest 草稿、给定＝该发布快照，fromVersion 缺省＝最近发布版、该图从未发布则基线取空图（首次发布前全为新增），目标/基线快照缺失 404、跨租户不泄漏；返 {graphId,fromVersion,toVersion(null=草稿),summary,diff}。前端 ReleaseModal「与 vN/空图 的配置差异」折叠区（getGraphDiff、与门禁并行、失败 fail-safe 不阻断；DiffView Tag 渲染节点/边/变量增删改）。tests/test_graph_diff.py 净增 6、tests/test_api_graph_diff.py 净增 4（默认草稿 vs 最近发布、首次发布前空基线、两命名版本、图/版本 404）；收口全量内存门 **1582 passed / 60 skipped**（净增 10、零失败，以收口实跑为准）、前端 **636/2**。零新依赖/零迁移/无新 ADR（新增 1 read 端点）。**仍缓做**：影子模式线上自动旁路、Mock 工具响应、用例编辑与入参参数化、用例/报告 PG 持久化与多租户共享，触发条件不变。
51. ✅ **OpenAPI HTTP Digest 认证 B2（docs/46 §8，2026-09-23 落码收口；D22 部分取回、不解除）**：parser 收录 http/scheme=digest（kind=digest/param=Authorization/无 prefix），凭证 {username,password} JSON 信封同 basic；适配器构造 `httpx.DigestAuth` 经 HttpApiClient.request(auth=) 透传（RFC2617 401 挑战-响应，零新依赖 httpx 已有），不静态拼 header。models kind 加 digest、adapter _resolve_credentials 返三元组(headers,query,auth)、service request 加 auth 参数。tests/test_openapi_adapter.py 净增 2（MockTransport 挑战流重算 MD5 response 验证、坏信封 SECRET_DECRYPT_ERROR 零外呼）、parser digest 移入支持集；收口全量内存门 **1572 passed / 60 skipped**（净增 2、零失败）。**cookie 缓做**（OpenAPI 无标准 cookie scheme、属会话管理，随连接批次）；oauth2/openIdConnect、连接测试、凭证轮换、真实 API 联调仍缓做。
50. ✅ **告警 lifecycle 通知 B1（docs/52 §7，2026-09-23 落码收口；D28 部分取回、不解除；991cd19）**：AlertNotifier 除新建外，对三类状态变化经既有 MessageService 投递——merged（合并归并，subject「再次发生已归并」，_raise_or_merge 与 raise_rollout_gate_alert 合并分支）、escalated（读时惰性 warning→critical，「未确认已升级」，升级幂等只触发一次）、resolved（resolve_alert，「告警已解决」）；acknowledge 不通知。过滤/失败口径同新建（enabled/to/minSeverity、fail-safe）。notify.py 增 LIFECYCLE_TITLES/build_lifecycle_subject/body/notify_lifecycle；records.py 六处接线（pending 带 transition、_notify_outside_lock 分流、合并/升级/解决点）。tests/test_monitoring_notify.py 净增 4（merged/escalated/resolved + severity 过滤 + fail-safe，旧 3 测改写为新行为）；收口全量内存门 **1570 passed / 60 skipped**（净增 4、零失败）。零新依赖/零迁移/无新 REST·ADR/前端零改动。**仍缓做**：PG 档 lifecycle（escalated_at/assignee 不持久化、读回重评，随告警 PG 化）、恢复（recovery）通知、lifecycle 限流退避。
49. ✅ **wait 事件跨重启持久化 v1 批（docs/53，2026-09-23 落码收口；D19 部分取回、不解除）**：PG 单实例 event 挂起帧（kind=wait + wait{waitType:event,eventKey,onTimeout,timeoutSeconds}，零 DDL，runs suspended）；`EventWaitBroker.restore` 同 token/event_key、按绝对 deadline 扣剩余超时幂等重建，启动恢复先 restore 后续跑；/api/waits 三端点跨重启可用；缺 wait → WAIT_EVENT_FRAME_INVALID。原子链：docs `53a5cb4` → broker `4450430` → loader `a40873a` → 断环 fix `719b3c9` → api `b88fc41` → seed fix `6ad1a9f` → smoke `ba2028e` → 收口本原子。两个顺带 PG 修复：storage.pg↔iam 循环导入（pg_deliveries 模块级 `_now()`、storage.pg 删顶层 iam import）；PG 首启不播种管理员（两档都幂等 seed）。U400–U402 转正式；HTTP 重启冒烟 `scripts/dev/d53_event_wait_restart_smoke.py` **ALL SCENARIOS PASSED**（直投 completed/payload 透传、广播 released=1、fail 重启后 WAIT_TIMEOUT_FAILED）。收口全量内存门 **1566 passed / 60 skipped**；零新依赖/零迁移/无 ADR/无新 REST/前端零改动；多实例跨进程路由、停摆期信号排队、debug 跨重启（D27）仍缓做。
48. ✅ **监控告警外部通知 v1 批（docs/52，2026-09-23 落码收口；D28/D24 部分取回、不解除）**：`monitoring/notify.py`——`AlertChannel {enabled, channel(dingtalk/wecom/feishu/webhook/email), to, secret, minSeverity, updatedAt}` 租户单例（迁移 020 `alert_notify_settings`，Repository 增 get/update、reset 清空）；`AlertNotifier` 仅**新建告警**三处（内存/PG record_run、rollout_gate）经 MessageService 旁路投固定纯文本（severity 过滤、异常全捕获 fail-safe、最近投递状态可查），setter 回注。REST `GET/PUT /api/monitoring/alert-channel`（read/administer，422 中文聚合）；前端监控页配置卡＋纯逻辑校验。零新依赖/无 ADR；多渠道路由、合并/升级/解决通知、模板富文本、限流退避/投递历史仍缓做。候选 U307；原子序：docs 立项（本步）→ 持久化 → notify 接线 → API → 前端 → 收口。立项基线后端 **1526/59**、前端 **627/2/48 文件**。原子链：docs `92b4621` → persistence `70b4889` → notify `07baa89` → api `6a7d846` → frontend `b352638` → smoke `8ae7d76` → 收口本原子。收口门后端 **1548/59**（净增 22）、前端 **636/2**（净增 9）；两层 HTTP 冒烟 `.smoke/alert_notify_smoke.py` **ALL PASS: 15 checks**，浏览器配置卡/最近投递/中文校验截图 `.smoke/alert-channel-admin.png`（零 JS 错误）。

47. ✅ **IM 群机器人消息投递 v1 批（docs/51，2026-09-23 落码收口；D24 部分取回、不解除）**：message/send channel 增 `dingtalk`/`wecom`/`feishu`，to 为群机器人 Webhook 单 URL（过 EgressGuard），新增可选 `secret`（≤200，仅 dingtalk/feishu；钉钉 URL HMAC 加签、飞书请求体加签）；text=subject+"\n"+body，三家各自 msgtype 包体 POST（10s/不重定向），平台 errcode/code 非 0、非 2xx、超时 → IM_SEND_FAILED 不写记录，EGRESS_DENIED 透传；成功 delivered 标渠道名，未注入投递器回退进程内。零新依赖/零迁移/无新 REST/无 ADR；短信、富文本/@人、多 URL 群发、IM 应用 OAuth/入站、模板系统、限流退避/投递追踪仍缓做。原子链：docs `274d784` → feat(message) `d4823d5`（+36 测）→ feat(frontend) `80b2436`（+3 测）→ 收口本原子。收口门后端 **1526/59**、前端 **627/2/48 文件**；HTTP 冒烟 `.smoke/im_robot_smoke.py` **20/20**、浏览器截图 im-robot-1-secret-field（零 JS 错误）。
46. ✅ **wait 到点时刻等待 v1 批（docs/50，2026-09-23 立项并当日落码收口；D19 部分取回、不解除；e6c9789→12de72d→4f82a1c）**：wait duration config 的 durationMode 增 `absolute`，config 增 `absoluteTime`（≤64；ISO 8601 或 epoch 秒，支持 `{{路径}}` 插值，naive 按 UTC）；loader absolute 先插值再解析（纯数字→fromtimestamp/否则 fromisoformat 兼容 Z），按注入时钟取差值 delta，须有限 1-600（int(round)），秒数同时作帧 timeout_seconds；插值/解析异常、过点(delta<1)、>600 → run failed WAIT_ABSOLUTE_TIME_INVALID 不 sleep。absolute 产出另含 durationMode/absoluteTime（渲染后 ISO，epoch 回写统一 ISO），static/dynamic 旧图零回归。收口门后端 **1490/59**（+18）、前端 **624/2/47 文件**（+3）、lint/build 干净；HTTP 冒烟 21/21＋浏览器 4 截图零 JS 错误（浏览器内运行未做，连线无法合成）。>600 秒长时刻/时区控件/周期时刻/event timeout 时刻化仍缓做。
45. ✅ **wait 定时等待动态时长 v1 批（docs/49，2026-09-23 立项并落码收口；D19 部分取回、不解除；257ba38→8580fcb→4eaaddd）**：wait duration config 增 `durationMode: static|dynamic`（缺省 static）与 `durationExpression`（≤200，dynamic 必填）；loader dynamic 经 D15 条件引擎求值，须非 bool 有限数值 1-600（int(round)），秒数同时作帧 timeout_seconds；求值异常/非数值/越界/除零 → run failed WAIT_DURATION_INVALID 不 sleep。static 旧图零回归、产出形状不变。收口门：后端 **1472/59**（+15）、前端 **621/2**（+5）、lint/build 干净；HTTP 冒烟 21/21＋浏览器 2 截图零 JS 错误（浏览器内运行未做，连线无法合成）。到点时刻/event timeout 表达式化/jitter/跨重启中断仍缓做。

44. ✅ **condition LLM 语义判断分支 v1 批（docs/48，2026-09-23 落码收口；D14 部分取回、不解除）**：condition 节点 conditionMode=llm——分支 `{label,description:≤300,target}`（禁带 expression）、classifierPrompt?≤500；`atlas.llm.condition_classifier` 三层（LiteLLM 单次调用返唯一标签 JSON/Offline 必抛/工厂按 LITELLM_MODEL），上下文 JSON 截断 12000 字符；四类失败全 fail-safe 走 defaultTarget（无新错误码、无新 REST），分类器经 run_graph 注入。原子序已完成：docs 立项 39f7029 → feat(llm) 7f8392d → feat(graph) de3bc83 → feat(frontend) 285cfc0 → fix(frontend) 5a955d2 → docs 收口。收口门：后端 **1457/59**、前端 **616/2**、HTTP 冒烟 11/11、浏览器 3 截图零控制台错误。置信度/多候选、每分支 prompt、按租户模型、字段脱敏、structured outputs、回放断言仍缓做。

## Recently shipped（最近变更；只留最近 5 条）

✅ **打包 N 落码收口＝定时触发调度器（docs/68，2026-09-26；`dae7c77`→`1f318c4` 六代码原子；关闭 docs/63 §0A N4）**：`src/atlas/scheduling/` 五文件（自研最小 5 字段 cron 子集零新依赖＝**ADR T30**、可注入引擎、两档 store）＋迁移 030 两表（`schedules`/`schedule_fires`，`slot_utc` 用 TIMESTAMPTZ 因为它是认领唯一键）＋lifespan 在 `recover_pending` 之后起 tick 线程＋4 端点（含 read 档 `cron-preview`，**前端因此不必复刻第二个解析器**）＋控制台「定时调度」页与画布预演。**三条承重语义各有反向门并实跑转红**（认领恒真⇒双发／busy 判假⇒无跳过／放宽回看窗口⇒开始补跑）。**"重启不补跑"第一次由真实墙钟证明**：`scripts/dev/schedule_e2e_smoke.py` 停服 130 秒后，停摆窗那一分钟**既无 run 也无认领行**，当分钟照常派发；浏览器实测三态（合法显三次 UTC／非法显后端原文／清空不留残留）。门：**1939/119/0**、前端 736/2/55、oxlint 0/7、build +7.2 kB。**没解除**：单副本三道闸一条不撤（"双进程同槽一次"≠支持多副本）、**N3 范围表态仍开**、外部凭据与真 prod 演练仍缺；四条余部新登 **D41**（命名时区／补跑开关／一图多调度／调度级并发）。
✅ **打包 L 落码＝prod 首任管理员引导口令（docs/66，2026-09-25；`66e977e`/`4f980df`＋收口原子）**：关闭 docs/63 §0A N1（新 prod 库登录死锁）——`ATLAS_ADMIN_BOOTSTRAP_PASSWORD` prod 必填、缺失或弱口令即拒绝启动，prod 只播各租户 admin＋引导口令，dev/test 逐键不变；同批修 N5 示例值与 README 过期措辞。**N2 真实渠道工具接通仍未做**。
✅ **打包 M 落码＝渠道工具接通通用 JSON 参数通道（docs/67，2026-09-25；`fix(graph)` 一行前缀＋`test(graph)` U868–U873）**：关闭 docs/63 §0A N2，图里第一次能带正确参数真调 Shopify 退款（测试只假在 HTTP 层）；**N3 范围表态与 N4 调度器未做，PROD 仍不 GO**。
1. ✅ **治理批＝把两条"纸面约定"变成机检守护＋修 handoff 断链（Active #69，2026-09-25，docs＋test 原子）**：迁移约定 §3 重写为两条真规则并新增 `tests/test_migration_convention.py`（含 `tmp_path` 反向门，实测能抓坏命名／跳号／缺首行注释）；docs/29 状态行的「清单权威」就地更正为已被 docs/63 取代；**补登记 Active work 里从不存在条目的打包 G／J／K 三批**并改正两条悬空引用（`#66` 不存在、J 的 `#65` 指向别的条目），新增 `tests/test_handoff_integrity.py`（编号唯一／引用可解析／Recently shipped 上限 5）。守护第一时间抓到我自己的两处错：把 Active 与 Recently shipped 两张表合起来量出的假"撞号"结论，以及 Recently shipped 被逐批追加到 10 行。
2. ✅ **第四次上线复审撤回上轮「工程侧就绪」判定（docs/63 §0A，2026-09-25，纯 docs 原子）**：真路径复核验出新阻断 **N1 prod 全新安装登录死锁**、**N2 真实店铺退款在图里跑不出来**，加 N3/N4/N5 三项 PRD↔运行期落差，并更正我上轮写宽的 S5 与"清零即就绪"。**当前判定＝产品核心完全可用的 MVP 未达到；demo/陪同试用可交付、自主上线不可交付**（docs/08 A 组已重开 L-1/L-2）。
3. ↪ **打包 K 全批收口（docs/65）连同更早的打包 J／打包 I／两次上线复审／打包 H／打包 F／i18n 两批已滚入 [docs/handoff-archive-2026-09-25.md](docs/handoff-archive-2026-09-25.md)**（上限 5 条由 `tests/test_handoff_integrity.py` 守）。

> 更早的完成项见 docs/handoff-archive-*.md（2026-09-18 / 09-19 / 09-24）。

## Quality gate（质量门）

### 2026-09-26 打包 N 收口门（先跑后写，数字取实跑）

- 后端 `.venv/bin/pytest`（照 ci.yml 原文命令）：**1939 passed / 119 skipped / 0 failed**（474.14s；打包 M 基线 1874/114 ⇒ 本批净增 65 常跑＋5 条无库即 skip）。
- PG 直连：`ATLAS_RUN_INTEGRATION=1 DATABASE_URL=… pytest -m integration tests/test_scheduling_pg_integration.py` **5 passed**（两档投影逐键全等／PG 认领跨重启／内存档易失按契约方向断言／引擎驱动 PG 端到端／`reset_tenant` 不越界）；同批迁移用例 U224 证明 001–030 在全新库 apply 且二次零变更。
- 前端（工作目录 `frontend`，照 ci.yml 三步）：`pnpm lint` **0 error / 7 warning**（6 条既有＋1 条与 Approvals/AuditLog/EmailApproval 同形的 `set-state-in-fetch`，为不与三处既有页分叉而保留；cron 控件那条已改派生显示消掉）；`pnpm test` **736 passed / 2 skipped / 55 文件**；`pnpm build` ✓ **1730.23 kB / gzip 536.67 kB**（基线 1723.04/534.50）。
- **真机两条，不是门是流程**：`scripts/dev/schedule_e2e_smoke.py` 四场景全过（自动派发／认领落库／**停服 130 秒后停摆窗那一分钟既无 run 也无认领行**／run-now＋撤销）；浏览器（真后端＋真 vite）看全调度页与画布 cron 预演三态。
- **本轮量到的机器噪声要记账**：同一台机器上有其它会话在跑重型任务（load 一度 272），期间 `tests/test_run_cancel.py`（TIMEOUT=5s）与 `tests/test_api_cards.py` 出现过 **0/3/2 个不一致的时序失败**，隔离跑与低负载全量跑均绿。结论：那是负载伪红，但**"门绿"在这台机器上要连着 load 一起记**，否则下一次会把真问题也归给负载。


### 2026-09-25 打包 J/K 复验（docs/64＋docs/65 收口后，本次为**第三方复核**：不是转抄收口自述）

- 后端 `.venv/bin/pytest`（照 ci.yml 原文命令）：**1849 passed / 114 skipped / 0 failed**（703.37s）——与打包 K 收口自述**逐字一致**〔跑〕。
- 前端（工作目录 `frontend`）：`pnpm lint` **0 error / 6 既有 warning**；`pnpm test` **728 passed / 2 skipped**（53 文件通过、1 跳过）；`pnpm build` ✓ **1723.04 kB / gzip 534.50 kB**〔跑〕。
- 逐条回代码复验 S1–S8 关闭状态（全部命中真实实现，非注释级）：`security/bootstrap.py`（prod 缺密钥拒启）、`email_token.py:102-114`（`rcpt` 绑定）、`docker-entrypoint.sh:60-65`（prod 不播种）、`storage/pg.py:487-495`（登录 `ORDER BY tenant_id`）、`api/main.py:884/1013`（8-worker 池＋1MB 上限）、`main.py:3781-3782`（demo 面 prod fail-closed）、`llm/decision.py:74-75`（timeout/`max_tokens`）、`storage/pg.py:74-100`（`prune_expired`，runs 仅终止态·interruptions 仅已认领）、`observability/logging.py`＋`main.py:307/315`（formatter＋request-id）、`.github/workflows/ci.yml:34/63`（两个 job 都挂 postgres）〔码〕。
- **两处改口径收口要留意**（不是放松，但也不能当"按原方案落地"）：J-3b 的 `limit` `le=` **落地后还原**（10 处端点本已自带 clamp/reject 门禁，加 `le=` 反把 clamp 变 422），以「确认有界」收口，详见 docs/64 §10；docs/63 S6 的更新注也照此措辞。
- **本次未做**：未新建临时库重跑 `-m integration`（权限门拦下建库动作）；PG 全量数字沿用打包 K 收口自述 **1962/1/0**，其**可信度已由 CI 承担**——`backend-integration` job 现在每次 push 都跑，不再依赖手动。

### 2026-09-25 打包 I 收口（docs/62 L1＋L2，dev，未 push；后端 `.venv/bin/pytest` 109.10s）

- 后端内存档 `.venv/bin/pytest`（照 `.github/workflows/ci.yml` 原文命令与工作目录）：**1811 passed / 108 skipped / 0 failed**（打包 H 基线 1799/105；净增 12 常跑＋3 PG skip，全部来自认领门与反向对照）。
- PG 直连 `ATLAS_RUN_INTEGRATION=1 DATABASE_URL=...atlas_l2_test .venv/bin/python -m pytest -m integration`：**43 passed / 1 failed / 1 skipped**。唯一红＝既存缺陷 `tests/test_audit_log.py::test_pg_audit_store_roundtrip_list_export`（`PgAuditStore.record()` 返回投影缺 `seq`，与 `tests/test_audit_log.py:31` 的 `_EVENT_KEYS` 期望集不符），**与打包 I 无关、改造前即红**，因 integration 在默认门里被 skip 而长期无人看见；已入 docs/08 B 组候选与 docs/14 注记。临时库测毕 `DROP DATABASE`（`atlas_l2_test`、`atlas_w2_recon` 各自独立，未碰开发库 `atlas`）。
- 迁移：`scripts/ops/apply_migrations.py` 在勘察库把 001–029 全量跑通（`applied 029_frame_resume_claim.sql`），`\d interruptions` 确认 `resumed_at TIMESTAMPTZ`／`resumed_by TEXT` 到位且**无新索引**（认领按主键单行命中）；`002_storage.sql` 新装库路径同形。
- **端到端硬门（本批唯一能证明修好了的东西）**：`scripts/dev/multi_instance_resume_recon.py` 退出码 **1 → 0**，脚本原文「一次审批、一条 run，下游实际落库＝1 次（通过分支×1／拒绝分支×0）」、run 终态 `completed`、A/B 两侧收尾 pending 均为 0。**诚实边界**：脚本数的是下游落库次数，本次未逐行核 B 侧日志（B 的 `recover_pending` 确为同一 token 建了 pending，即僵尸线程那条路径仍被门挡住）。
- **反向门两条**（不去拆生产 SQL）：`test_reverse_control_always_admitting_claim_lets_both_drivers_through`（认领恒真→两个驱动者都跑完）与 `test_u809c_reverse_control_the_null_predicate_is_what_serializes`（去掉 `resumed_at IS NULL` 的同一 UPDATE 两次都 rowcount 1）。另实跑植入：`_gate_resume` 判据短路 `if False and ...` → `tests/test_frame_resume_claim.py` **12 例转红 8 例**，还原后 12/12 绿。
- 前端零改动（diff 无 `frontend/` 文件），未跑 `pnpm lint/test/build`；L1 的三道闸属 shell/镜像层，其五路拒绝 rc 与 `workers=1` 端到端放行记录在上一条（打包 I 立项批）里，本批未触碰。

### 2026-09-25 打包 H 收口（docs/61，dev；实跑日志见会话，后端 213.73s）

- 后端内存档 `.venv/bin/pytest`：**1799 passed / 105 skipped / 0 failed**（打包 G 基线 1758/86；净增 41 常跑＋19 PG skip；H2/H3/H4 新四文件 60 例）。
- PG 直连（`ATLAS_RUN_INTEGRATION=1` + `DATABASE_URL`，新四文件＋原六文件）：**89 passed**（迁移 027/028 自动 apply；`scripts/ops/apply_migrations.py` 登记 028）。
- 整栈 PG 装配 `ATLAS_STORAGE_BACKEND=pg` 构造 `tenant_registry.get("t1")`：`approval_broker.history_store` 为 `PgApprovalHistoryStore`、`shadow_store` 为 `PgShadowStore`（防再犯 docs/29 那类装配期 AttributeError）。
- 前端：vitest **722 passed / 2 skipped / 53 文件**（基线 694/2/52）、`tsc -b` 0 error、oxlint **0 error / 6 既有 warning**（AuditLog/EmailApproval/WebhookSubscriptionsModal/Approvals×3，**零新增**）、`pnpm build` ✓ `index--AZ2_f07.js` 1712.68 kB / gzip 531.12 kB（chunk>500 kB 为既有提示）。**日期控件回退**：RangePicker 曾落码，因 +112 kB 且 dayjs 在 pnpm 下不可解析（中文面板渲染 `2026年Sep`、本地化需把 dayjs 提为直接依赖＝选型决策）→ **回退原生 `datetime-local`**，缺陷与体积同消、最终 bundle 仅 +4.4 kB（详因见 docs/61 顶部注记）。
- 冒烟：`scripts/dev/h1_diagnostics_smoke.py` **H1_SMOKE_ALL_PASS 12/12**（连跑 13 次、末 9 次全绿；早期一次为语言下拉点击竞态，已改为开合重试）、`scripts/dev/h3_audit_filters_smoke.py` **H3_SMOKE_ALL_PASS 22/22**。
- 待办（收口时未做，非回归）：`/metrics` 与 `/api/ready` 的 PG 整栈档未随本批重跑（本批未触碰）；批 H 已 push（origin/dev=`a53f36f`）；未合 main。

### 2026-09-24 打包 G 收口（docs/60，dev；实跑日志 /tmp/gate_backend_final.log、/tmp/gate_pg.log、/tmp/gate_frontend_build.log）

- 后端全量内存门（`.venv/bin/pytest`）：**1758 passed / 86 skipped / 0 failed**（134.97s；净增 24 常跑 ＋ 9 PG skip＝33 个新测试函数，候选 U663–U742 文件级登记 docs/13 §9）
- PG 直连集成（`ATLAS_RUN_INTEGRATION=1 DATABASE_URL=postgresql+psycopg://atlas:atlas@localhost:5432/atlas`；migrations/storage/database/openapi/monitoring/message 六文件）：**55 passed**（迁移 025/026 被 fixture 按 sorted glob 自动 apply）
- 前端 vitest（`npx vitest run`）：**694 passed / 2 skipped / 52 文件**（净增 19 个 it）；tsc（`npx tsc --noEmit -p tsconfig.app.json`）0；oxlint（`npx oxlint src`）**0 error / 6 既有 warning**（cards 组件 react-hooks set-state-in-effect，非本批）；`npm run build` 过（dist/assets/index-L91FdD1n.js，1708.29 kB / gzip 529.73 kB；chunk>500 kB 为既有提示）

- **i18n 第一批债 / docs/17 §2.4 收口（2026-09-24，Active work 58）**：后端全量内存门 **1713 passed / 69 skipped**（deps code 化、认证两文件 18 passed，零失败）；前端 vitest **663 passed / 2 skipped**（净增 1 英文态 L1 用例，validation 目录 126 passed）、oxlint **0 error / 6 既有 warning**（语言依赖经显式 void 标记、未新增）、pnpm build 过（tsc 0 错）。零新依赖/零迁移/无新 REST·ADR。
- **打包 E / docs/58 收口（2026-09-24，Active work 57）**：后端全量内存门 **1713 passed / 69 skipped**（E-2 1709 净增 4＝多 URL 群发，零失败，以收口实跑为准）；message IM/webhook 两文件 **96 passed**（多 URL 全成/部分失败 best-effort/EGRESS fail-fast）；`.smoke/im_robot_smoke.py` **ALL PASS 27 checks**。前端本批零改动（schema 驱动表单自动呈现 msgFormat/mentions、条件原子未触发），沿用上批前端门。零新依赖/零迁移/无新 REST·ADR。
- **打包 C / docs/56 收口（2026-09-24）**：后端全量 `.venv/bin/pytest -q` **1674 passed / 69 skipped / 0 failed**；PG 直连两文件 **33 passed**（test_storage_pg_integration 19＋test_openapi_imports_pg_integration 14，迁移 022/023）；前端 `pnpm test` **645 passed / 2 skipped（48 文件）**、`pnpm build`（tsc -b && vite build）过（仅既有 chunk>500kB 提示）、`pnpm lint`（oxlint）**0 error / 6 既有 react-hooks warning**（非本批引入，docs/56 无前端改动）。
- **打包 B / docs/55 收口（2026-09-24）**：前端 l1+nodeSchemas **79 passed**、build 过；test_monitoring_notify **38 passed**（U560–U565 内存 flapping）；PG lifecycle/flapping 直连 **18 passed**（U570–U573，迁移 021 自动应用）；test_api_waits signal 响应补 queued 断言（b64084a）。
- **B5（2026-09-23，Active work 54）**：全量内存门 **1591 passed / 60 skipped**（净增 9＝event wait timeout 图级 3＋dsl 6，零失败，以收口实跑为准）；前端 **636 passed / 2 skipped**（schema/L1 接线、零新增前端测），tsc 0 错、oxlint 0 error/6 既有 warning、vite build 过。**B4 勘察判空集**（{{}} 插值接缝 interpolate 全覆盖、不落码）。
- **B3（2026-09-23，Active work 52）**：全量内存门 **1582 passed / 60 skipped**（净增 10＝graph/diff 纯函数 6＋diff 端点 4，零失败，以收口实跑为准）；前端 **636 passed / 2 skipped**（ReleaseModal 接线、未加前端单测），tsc 0 错、oxlint 0 error/6 既有 warning（ReleaseModal 零新增）、vite build 过。

- **最新收口门（OpenAPI HTTP Digest 认证 B2，docs/46 §8，2026-09-23，dev 待 push）**：后端内存档 **1572 passed / 60 skipped**（docs/53 基线 1566，净增 2＝adapter digest 挑战流/坏信封，parser 为改写净 0，零失败）；前端零改动 **636/2**（无前端提交、沿用上批）。digest 经 httpx.DigestAuth RFC2617 挑战-响应（零新依赖 httpx 已有）；cookie 判为缓做（OpenAPI 无标准 cookie scheme、属会话管理）。零迁移/无新 REST·ADR；D22 不解除（oauth2/openIdConnect、连接测试、凭证轮换、真实 API 联调仍缓做）。
- **最新收口门（wait 事件跨重启持久化 v1 批 docs/53，2026-09-23，dev 待 push）**：后端内存档 **1566 passed / 60 skipped**（零失败；含 broker restore / loader 帧 / api 恢复相关测试与两个 PG 整栈阻断修复）；PG 集成文件 13 passed（U402）；前端零改动 **636/2**（无前端提交、沿用上批）。HTTP 重启冒烟 `scripts/dev/d53_event_wait_restart_smoke.py` **ALL SCENARIOS PASSED**（直投 completed/payload 透传、广播 released=1、onTimeout=fail 重启后 WAIT_TIMEOUT_FAILED）。零迁移/零新依赖/无 ADR/无新 REST；D19 不解除（多实例跨进程路由、停摆期信号排队、debug 跨重启仍缓做）。
- **最新收口门（wait 事件等待进程内 v1 批 docs/47，2026-09-23，dev 待 push）**：后端内存档 **1435 passed / 59 skipped**（docs/46 基线 1398，净增 37＝broker/DSL/loader/API，零回归）；前端 **606 passed / 2 skipped**（净增 4，build 过、lint 干净）。HTTP 冒烟 **19 检查全过**（`.smoke/event_wait_smoke.py`：信号放行＋payload 传播、超时 continue/fail、预置、404/409、SSE 取消、坏 key），浏览器冒烟 2 截图 docs/smoke-shots/event-wait-*。无迁移、无新依赖、无 ADR；新错误码 WAIT_TIMEOUT_FAILED/WAIT_EVENT_KEY_INVALID/WAIT_TOKEN_NOT_FOUND/WAIT_ALREADY_SIGNALED。D19 不解除（持久化中断帧/恢复扫描器/多实例）。
- **最新收口门（OpenAPI securitySchemes 静态密钥批 docs/44，2026-09-23，dev 待 push）**：后端内存档 **1375 passed / 59 skipped**（docs/43 基线 1352/56，净增 23 常跑＋3 PG skip：parser 10/adapter 7/API 6，零回归）；PG 直连（atlas-pg，ATLAS_RUN_INTEGRATION=1）**11 passed**（新字段往返/信封跨重启/旧行默认 `{}`）；前端 **593 passed / 2 skipped / 45 files**（净增 2，build 过、oxlint 0 error）。浏览器冒烟 3 截图 docs/smoke-shots/openapi44-{1-preview-credentials,2-missing-credential,3-success}.png：无密钥 fail-closed 中文失败（不发请求）→ PUT 配密钥 → SUCCESS HTTP 200（body Rex）；PG 档重启后信封仍在、重跑仍 200。迁移 019（两 JSONB 列）；新错误码 OPENAPI_INVALID_CREDENTIAL/OPENAPI_CREDENTIAL_MISSING；无新依赖、无 ADR（复用 T26）。收口后恢复普通内存档后端。
- **最新收口门（OpenAPI 导入规格 PG 持久化批 docs/43，2026-09-23，dev 待 push）**：后端内存档 **1352 passed / 56 skipped**（docs/42 收口 48 skip，新增 8 PG 集成默认 skip；数字零回归）；PG 直连（atlas-pg，ATLAS_RUN_INTEGRATION=1）**8 passed**；前端 **591 passed / 2 skipped / 46 files** 零改动（无新前端提交）。PG 档浏览器冒烟 2 截图 docs/smoke-shots/openapi-pg-smoke-{1-survives-restart,2-run-success}.png：导入 openapi-768 → 重启后端 → 列表与 `/api/adapters` 适配器仍在（无需预热）→ 编辑器 `openapi:openapi-768/list_pets` limit=10 运行 SUCCESS HTTP 200，控制台仅一次预期旧 token 401。新表 openapi_imports（迁移 018）；无新错误码、无新依赖、无 ADR；REST/前端/编辑器零改动。收口后恢复普通内存档后端。
- **最新收口门（OpenAPI 导入与工具自动生成批 docs/42，2026-09-23，dev 待 push）**：后端内存档 **1352 passed / 48 skipped**（docs/41 基线 1293，净增 59＝parser/schema 内核＋store/适配器＋API，零回归）；前端 **591 passed / 2 skipped / 46 files**（净增 7＝apiClient 7）、tsc＋vite build 过、oxlint 仅既有提示；浏览器冒烟四场景过（预览 skipped 灰行/导入列表/工具选择器＋M3 参数表单零编辑器改动/真实运行 HTTP 200 SUCCESS），operator DELETE 403、admin 删除后适配器从 `/api/adapters` 摘除 spec GET 404，控制台仅 antd 既有 warning＋冒烟重启的一次预期 401，4 截图 docs/smoke-shots/openapi-smoke-*。新错误码 OPENAPI_INVALID_DOCUMENT/UNSUPPORTED_VERSION/FETCH_FAILED/NO_IMPORTABLE_OPERATION/LIMIT_EXCEEDED/INVALID_PARAMETER；无迁移、无 ADR。
- **最新收口门（真实渠道适配批 docs/38，2026-09-23，dev 待 push）**：后端内存档 **1220 passed / 40 skipped**（docs/37 基线 1190，净增 30＝shopify 内核 14＋registry 10＋API 6，零回归；channel_bindings PG 集成 4 例 skipif 门控）；前端 **577 passed / 2 skipped / 45 files**（净增 6＝apiClient channels 4＋i18n channels 2）、tsc＋vite build 过、oxlint 仅既有 warning；浏览器冒烟三场景过（空态/绑定弹窗/未授权连接探活→error 态），控制台无应用错误，3 截图 docs/smoke-shots/。新表 channel_bindings（迁移 015）；新错误码 CHANNEL_NOT_BOUND/UNAUTHORIZED/UPSTREAM_FAILED/INVALID_RESPONSE/INVALID_PARAMETER/ALREADY_BOUND；ADR T28，三处同步 10→02→09 已完成。
- **最新收口门（docs/34 对外交付层小批，2026-09-22，dev 待 push）**：后端内存档 **1073 passed / 34 skipped**（docs/33 基线 1051＋observability 8＋SMTP 14，零回归，187s）；前端 **555 passed / 2 skipped / 43 文件**、tsc＋vite build 过、oxlint **0 error / 0 warning / 137 files（历史两处 set-state-in-effect 已清零）**；`docker compose config` 默认档与 `--profile edge` 档均合法；gitleaks 本地扫描无泄漏。新端点 `/api/ready`（就绪探针）、`/metrics`（Prometheus 0.0.4 文本，无鉴权靠网络隔离）；新环境变量 ATLAS_SMTP_*；新错误码 SMTP_SEND_FAILED。**未重跑**：PG 整栈档（ATLAS_STORAGE_BACKEND=pg）新端点仅代码审查＋内存档测试覆盖。
- **最新收口门（docs/33，2026-09-21，已随 PR #51 合 main merge `f0999f3`：后端 1051/34、前端 555/2/43；2026-09-22 整栈 PG 装配缺口修复 `b2ac85d` 后端数字不变、整栈 PG uvicorn `/api/health` 200、U269 转正）。P0 批 3 收口门（2026-09-21，已随 PR #48 合 main merge `5a5976c`）**：后端内存档 **1008 passed / 31 skipped**（批2 基线 822，净增 186＝egress 60＋DB guard 58＋resilience 27＋secrets 25＋http/db 接线）；PG database 集成单文件 **3 passed**（`ATLAS_RUN_INTEGRATION=1 DATABASE_URL='postgresql+psycopg://atlas:atlas@localhost:5432/atlas' pytest tests/test_database_integration.py`）；前端零改动 **509 passed / 2 skipped / 40 文件**、tsc＋vite build 过、oxlint **0 error / 2 既有 warning**（ReleaseModal/RollbackModal set-state-in-effect）；离线运行时冒烟四类 fail-closed 错误码（EGRESS_DENIED/DB_WRITE_FORBIDDEN/DB_SQL_NOT_READ_ONLY/SECRET_UNAVAILABLE）零外呼＋公网带秘密 GET 200 全过。

- **文档侧**：00 文档"切分核对报告"已确认 274 章节全映射、逐段覆盖无丢失。改规格文档必须遵循 00 文档治理规则（唯一事实源 / 改内容流程 / Schema 契约防漂移）。
- **代码侧**：最小质量门已建立——GitHub Actions CI（`.github/workflows/ci.yml`，2026-09-14 落地；push main/dev 与 PR 触发）：gitleaks 全历史扫描 + 后端 pytest + 前端 oxlint/vitest/build；**PR #3-#19 与合并后 main 均已实跑全绿**（gitleaks 初版 403 经补 `pull-requests: read` 修复；#12-#16 对应模板库/录制/调试/监控/多租户五项能力，#17 为 `.codegraph/` 忽略规则，#18 为 docs/19 题述提案与 handoff 同步文档，#19 为设计态内核前置项，后两者 2026-09-16 合并）。本地后端 `.venv/bin/pytest`（**M11 收口后 673 passed / 17 skipped（2026-09-19；17＝13 原 PG/Chromium + 4 个 M11 PG 集成在无 DATABASE_URL 时 skip，给 DATABASE_URL+ATLAS_RUN_INTEGRATION 时 PG 套件 9 passed；M11 净增 28 单元〔18 内核+10 适配器/API〕；A+B 打包收口时为 645/13（A3 表达式新文件 + A2 loop break 3 + A1 any_success 3 + B 组引用校验净增 17；上一节点 D26/D30 收尾批为 628（D30-b 数据环 +2），再前 D26 报告 v1 收口为 615（2026-09-18；M9 基线 602，D26 净增 13＝6 存储+7 API）；M9 批 4 后 602、M8 收口基线 518、M9 批 3 后 601，M9 批 4 净增 1（PUT 同图草稿）**；M4 收口时 431，M5–M8 后端净增 87 用例（M5a storage +10、M6 版本化 U46 +4、M5b PG 中断恢复 U43–U45 +5 另 integration 13、M7 任务总线 U47/U48 +13、M10 追踪 U49–U52 +23、M8 卡片 U53/U54 +32）：M5a storage、M6 版本化 U46、M5b PG 中断恢复 U43–U45、M7 任务总线 U47/U48、M10 追踪 U49–U52。M4 收口明细：engine 3 + harness 契约 8 + 三层定位 6 + graph conditions 9 + graph DSL 71（M2 起含 422 locations 侧车 U38 十例） + 编译器/condition/loop/parallel/wait/subgraph/human_approval/http/database/message 通道路由 40 + collaboration 审批 broker 7 + graphs API 4 + 决策 6 + 店铺 8 + NL 15（含 NL 草稿参数尽力校验 U34 七例）+ 退款端到端 8 + Demo API/mock/messages 50（含模板 I15 四例、录制 I16 五例、调试 I17 七例、监控 I18 九例）+ httpapi 通用 HTTP 适配器 24 + database 通用 SQL 适配器 35 + message 进程内消息适配器 21 + 内置模板目录 U27 六例 + 操作录制 U28 二十三例 + 调试会话/暂停状态机 U29 十一例 + 基础监控 U30 二十例 + iam 种子身份/会话/角色矩阵/租户注册表 U31 十七例 + API 鉴权与租户分区 I19 十一例 + L2 模板引用编译期复查 I20 九例 + M4 批 2 L3 跨运行时对拍夹具 `tests/test_l3_fixtures.py` 十一例（参数化，后端重建 raw 图重跑防夹具漂移）；DB/浏览器/店铺 8 个 integration 用例默认跳过；DB/消息/模板/录制/调试/监控/鉴权/作用域新增用例全部 SQLite 内存/进程内/静态常量，零真实外呼），全量 `ATLAS_RUN_INTEGRATION=1`（+`DATABASE_URL`）含真实 Chromium 与店铺平台集成；前端 `cd frontend && pnpm test`（vitest，**M11 收口后 449 passed / 2 skipped（37 测试文件，M11 净增 13＝apiClient 6+memory 纯函数 7；A+B 打包收口时为 436/35 文件（（conditions 同构/scope 汇聚·深层/reverseDeps rename/editorStore renameNode/nodeForms any_success 净增 18）；上一节点 D26/D30 收尾批为 418（l3 +3、scope 类型校验 +8、releaseApi +2），再前 D26 收口为 404/34 文件，另 2 基准 RUN_BENCH 门控跳过；D26 净增 8＝release.test.ts 6+releaseApi 2；M9 批 4 后 396，M9 批 4 新增 release 纯函数 16、releaseApi 端点 10 共 +26；M8 收口为 370/32 文件，M5–M7/M10 前端零改动，M8 新增 cards 7、scope L2 4 共 +11）**；M4 批 2 时基线 336 个单元用例 / 30 个文件；M4 批 1 后 291/27、M3 收口时 243/24、M2 收口时 189/14——扶正迁移删除 dualRun 双跑 339 行等价用例，新增 validation 包 diagnostics/l1/markers/validateGraph 与 scope L2 升级用例，净 −21、文件 +3；M3 增 forms 包、M4 批 1 增 forms 节点表单与 validation/dirty 用例、M4 批 2 增 l3 对拍/engine 调度/validationStore/Problems/嵌套 UiSchema/parallel·subgraph 表单用例）与 `pnpm build`（TS 检查 + Vite 构建）均通过（有 AntD 首包 >500kB 的体积提示，暂不阻塞）。**M9 总收口另过 scripts/dev/m9_smoke.py 32 断言、Router benchmark（10k resolve p50 0.0041ms/p99 0.0450ms）、真实浏览器全链路 0 console error（4 张 docs/assets/m9-*.png），见 Recently shipped·打包 E 收口条目。D26 报告 v1 收口另过 scripts/dev/d26_smoke.py 24 断言、真实浏览器 6 条历史趋势 + 展开懒加载详情冒烟（2 张 docs/assets/d26-report-*.png），见 详见 docs/handoff-archive-2026-09-19.md（原 #5）。**D26/D30 收尾批（2026-09-19）复跑同一 d26_smoke 24 断言全过（含定时 CI workflow 本地端到端起服务实测），后端 628/前端 418，见 Recently shipped·i18n 第一批：认证/L1 英文态条目。A+B 打包（2026-09-19）改动 condition/loop/parallel 运行语义后复跑同一 d26_smoke 24 断言仍全过（发布/回放主链路零回归），后端 645/前端 436，见 Recently shipped·i18n 第二批：Graph DSL 422 码化条目。**M11（2026-09-19）新增 `scripts/dev/m11_smoke.py` 对真实 :8000 全链路（登录→remember/recall 小图运行→列表/搜索→跨租户 404→admin 删除→reset 清空），进程内档与 ATLAS_STORAGE_BACKEND=pg 档均全过（两档 recall score 0.434524 一致）；复跑 d26_smoke 24 断言仍全过（loader 加 memory 通道零回归），后端 673/前端 449，真实浏览器记忆页两角色冒烟零应用错误（3 截图），见 Recently shipped·i18n 第二批：Graph DSL 422 码化条目。**A 包（2026-09-19）子图事件上屏后复跑后端全量 **695 passed/18 skipped**（C 基线 687，新增 test_subgraph_events 8＋改 1 旧子图测试）、前端 **457 passed/2 skipped/38 文件**（新增 subgraphEvents 3＋recordings 1），内存档 d26_smoke 全过、m11_smoke 内存档 ALL_PASS，真实 HTTP SSE 端到端＋浏览器子图审批冒烟过（2 截图），见 Recently shipped·打包 G 五项落码收口条目。**B 包（2026-09-20）协作式急停/hitCount·logpoint/globals 改写后复跑后端全量 **712 passed/18 skipped**（A 基线 695，新增 tests/test_run_cancel.py 17 测）、前端 **472 passed/2 skipped/39 文件**（A 基线 457，新增 15 测），pnpm build 过、oxlint 0 error/2 既有 warning，真实 HTTP SSE 三场景＋真实浏览器三场景冒烟全过（4 截图 docs/assets/b-*.png），见新 Recently shipped·打包 G 五项落码收口条目；总收口（2026-09-20）已全绿：后端 712/前端 472、build 过（修复 B 测试 tsc 类型错误 `b521f5a`）、lint 0 error/2 既有 warning、m11_smoke 内存＋PG 两档 ALL_PASS、d26_smoke 内存档全过、A1·A2·A3 零回归，详见 Active work 21。 **docs/28 四批全部收口后（2026-09-20，#36）最终门：后端 782 passed/24 skipped、PG integration 全量 24 passed、前端 482 passed/2 skipped/39 文件、tsc+vite build 过、oxlint 0 error/2 既有 warning；d26/m11/d28 三 smoke PG 档全过；批 4 新记忆 POST/PUT 与升级体检端点 curl＋权限矩阵实测全过、真实浏览器两截图，详见 Active work 22。**M12 i18n 骨架（2026-09-20，#37）零后端改动：前端 495 passed/2 skipped/40 文件（docs/28 基线 482 净增 13 纯函数 i18n 测）、tsc+vite build 过、oxlint 0 error/2 既有 warning（exhaustive-deps 一处多余 useCallback 依赖已修、不新增）；后端 782 passed/24 skipped 不变；真实浏览器登录页与 UserBadge 中文零视觉差异（2 截图 m12-*.png），详见 Active work 23。****M12 续批 dashboard 接线（2026-09-20，Active 24）零后端改动：前端 498 passed/2 skipped/40 文件（M12 495 净增 3 dashboard 测）、tsc+build 过、oxlint 0 error/2 既有 warning；后端 782 passed/24 skipped 不变；真实浏览器登录后 Dashboard 中文零视觉差异（1 截图 m12-dashboard.png），详见 Active work 24。**2026-09-20 push 前三道门复跑核对（数字与本门记录一致、无需回填）**：后端默认 `.venv/bin/pytest` **782 passed/24 skipped/1 warning**（17.7s）；全量 integration 逐文件复跑 **24 passed**（storage_pg 11 + memory_pg 5 + database 3 + shop 1 + web_browser 4，atlas-pg 在线、Chromium 可用；注意多文件混跑时 memory_pg u94/u95 有已知跨文件时序污染会假失败、单文件复跑全绿，非回归）；前端 vitest **498 passed/2 skipped/40 文件**、`pnpm build`（tsc+vite）过（仅既有 chunk>500kB 提示）、oxlint **0 error/2 既有 warning**。******后续按 13 测试用例清单分层：单元 → 集成 → 回放 → 端到端 → AI 评估，上线前过"四道门"。

## How to run

```bash
python3.11 -m venv .venv
.venv/bin/pip install -e ".[dev]"
.venv/bin/pytest                       # 单元测试（integration 标记默认跳过）
.venv/bin/python -c "from atlas.engine.loop import run_loop; print(run_loop('demo', max_steps=2)['status'])"

# FastAPI graphs 接口（W7-W8；前端 dev 经 /api 代理到此端口）
.venv/bin/uvicorn atlas.api.main:app --reload --port 8000

# PostgreSQL + pgvector（本地开发）
docker run -d --name atlas-pg -e POSTGRES_USER=atlas -e POSTGRES_PASSWORD=atlas \
  -e POSTGRES_DB=atlas -p 5432:5432 pgvector/pgvector:pg16
cp .env.example .env                   # 填 DATABASE_URL（本地开发库：atlas/atlas）
docker exec -i atlas-pg psql -U atlas -d atlas < db/migrations/001_enable_pgvector.sql
DATABASE_URL=postgresql+psycopg://atlas:atlas@localhost:5432/atlas \
  ATLAS_RUN_INTEGRATION=1 .venv/bin/pytest -m integration

# Playwright 浏览器（W3-W4 起，浏览器集成测试需要）
.venv/bin/playwright install chromium
ATLAS_RUN_INTEGRATION=1 .venv/bin/pytest -m integration   # 同时含 DB + Chromium 用例

# 前端
cd frontend
pnpm install
pnpm dev                               # http://localhost:5174
pnpm test                              # vitest 单元测试
pnpm build                             # 类型检查 + 生产构建
```

Demo 里程碑（08 文档 7.1）：
- ✅ W1-W2：LangGraph 最小循环、前端编辑器框架、PostgreSQL+pgvector 连接均已跑通（2026-09-13）。
- ✅ W3-W4：Harness 契约层（注册发现/权限审计）+ Web 适配器（三层定位、navigate/click/type/screenshot，真实 Chromium 验证）已跑通（2026-09-13）；视觉语义层（omni-parser 类解析 + vision LLM）为协议注入桩，待 T1 LiteLLM 接入后替换。
- ✅ W5-W6：编辑器核心（拖拽画布、trigger/ai_decision/tool_call 三类节点类型化配置 + 实时校验、全局变量与 `{{路径}}` 引用、Graph JSON 导出，vitest 20 用例）已跑通（2026-09-13）。
- ✅ W7-W8：Graph DSL 契约 + 静态校验、DSL→LangGraph 编译/运行（`graph/` 包）、FastAPI graphs 保存/读取/编译/运行、编辑器一键编译运行（后端 16 新用例 + 浏览器验证）已跑通（2026-09-13）；节点执行器为确定性占位，待 LiteLLM/业务工具替换。
- ✅ W9-W10：电商退款端到端 Demo（LiteLLM 决策 + 规则兜底、shop 退款业务能力、SSE 实时进度、NL 退款草稿、模拟商家控制台，08 §7.3 七条验收全部达成；后端 58 单元 + 前端 23 用例 + 浏览器实测，2026-09-13）。
- API 已可启动：`uvicorn atlas.api.main:app --reload --port 8000`（graphs/SSE/NL/adapters/Demo 店铺接口；前端 dev 端口 5174，`/api` 代理到 8000；模拟控制台 http://localhost:8000/demo/shop）。

## Known issues / 注意点

- **文档口径**：01-08 为规格唯一事实源；10-13 为工程化推导文档，与正文冲突时以 01-08 为准（00 文档已声明）。
- **技术选型已锁定（2026-09-13 T1-T5 收口）**：`pyproject.toml` 可按 10 文档 §2 清单落码（版本号落码时解析）；Demo 不引入 NATS/vLLM/Go 网关/评估层实现，缓做项见 14 文档触发条件。
- **改 Schema 必须同步所有引用位置**（03 契约索引 + 所在文档正文），防漂移（00 治理规则第 3 条）。
- **新决策必须回写**：08 任务迭代计划记录 + 10 ADR 记录 + 本文档"Active work"同步，三者一致。

## Conventions（约定）

- **commit 消息**：英文、`<type>(<scope>): <subject>` 格式；原子提交；不 push（除非用户明确说）；作者保持用户身份，不加 AI co-author。详见 [git-commit-message.md](git-commit-message.md)。
- **AI 行为规范**：见 [AGENTS.md](AGENTS.md)（文档分层 / 治理规则 / 技术约束，新会话必读）。
- **新增文档后**：在本文档"Project documents"区补一行索引；改规格后同步 00 文档治理规则与 08 计划。
- **handoff 归档惯例（照 agent-world）**：主 handoff 只保留「当前状态 + 活跃任务 + 最近 5 个变更」。① Recently shipped 只留最近 5 条；② 编号待办标 ✅ 后不删除，而是把详细过程（实现步骤/commit 链/踩坑/验证数据）整块滚到 `docs/handoff-archive-YYYY-MM-DD.md`，主文件只留一行结论 + commit hash；③ 正在推进的活跃项详情留在主文件不归档。归档文件保留原条目号，主文件里「Recently shipped N」N>5 的交叉引用按归档文件原号定位——因此归档只动尾部、不重编号。
