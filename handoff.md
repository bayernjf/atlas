# Handoff — Atlas

更新时间：2026-09-18（**M9 入站 Router + 灰度发布 + 指标门控自动回滚已落码收口（批 1-4＋总收口：routing 三段分桶+event/pin+rollout REST、发布流批量回放门禁、monitoring 业务指标+门控越阈自动回滚、前端发布流 UI＋同图草稿 PUT 端点，后端 602 passed/13 skipped、前端 vitest 396 passed/2 skipped/34 文件；m9_smoke 32 断言、Router benchmark、真实浏览器全链路 0 console error、4 截图均已收口，见 Recently shipped 1 与 Active work 15）**；M8 交互模板系统、M10 span 级追踪、M9 灰度回滚全部收口，U49–U59 转正式；**题一 M1–M4、题二 M5a/M6/M5b/M7/M8/M9/M10 全部闭合——M1–M10 主线全闭合**）

## 项目概况

Atlas 是 AI 运营体（Agent）编排平台：以 **Harness（能力接入）/ Graph（可执行因果图）/ Loop（OODA 主循环）** 三位一体为内核，面向企业流程自动化场景（首个目标场景：企业内部 OA 审批），目标是让非技术人员通过自然语言/拖拽定义流程，由运营体自动执行、自愈与进化。

当前阶段：**工程主线已于 2026-09-16 切换到题一/题二落地路线（docs/19/20，M1–M10）；M1（九类内置节点 config → `frontend/src/lib/schemas/` 声明式 Data Schema 提取，零 UI 变化/零新依赖/后端零改动）已于 2026-09-16 落码（十提交 4ade416→51cb57f，当日随 PR #24 合并 main），**ADR T16 亦于同日照 M1 真实复杂度案例收口（用户拍板：手写最小子集、不引 AJV、M3 设重开判据，见 10 §4；package.json 不变，10→02→09 已同步），**M2（L1 字段校验扶正 + 结构化诊断：Diagnostic nodeId+RFC6901 pointer+token range，compile 422 locations 稀疏侧车）已于 2026-09-16 按五段原子序全部落码（四代码提交 234a95f/30156a3/d0b2e33/fbd9f77 + 文档收口 cdc2211，当日随 PR #24 合并 main；后端 420 passed/8 skipped、前端 vitest 168/17 文件，真实浏览器九节点冒烟零应用错误，U37/U38 转正式，见 Recently shipped 15 与 08 落码条）；**M3（WidgetRegistry + FormRenderer 工具 params 表单化首期）已于 2026-09-17 落码收口（五提交 2de5053→6d4a863；ADR T17 同日收口：自研最小表单引擎、既有 AntD 承载、零新依赖，oneOf/无类型降级 JSON 文本框，T16 不重开；契约 08 M3 立项条+落码条/04 §4.10/03 `form_renderer`，U39 已转正式），**M4（UISchema 最小子集 + 手写 Config 迁移二波 + 增量调度 + 前端 L3 预判 + Problems 面板 + reverseDeps/quickFix v1）已于 2026-09-17 立项并完成批 1/批 2/批 3 全部落码收口（立项 docs-only 批 81eb2e3；批 1 九提交 `49cb692→30d5009`：uiSchema groups/hiddenWhen/hideFields+字段文案层、内置控件 8→9〔+radio〕、target-select 节点业务控件层与 NodeConfigForm、HumanApproval/Trigger/Loop 迁移、Wait 判定保留手写、editorStore dirty 失效范围骨架；批 2 六提交 `0dc9b7f`/`1468e18`/`2ad4177`/`aaf50bf`/`06b62fc`/`ac29737`：前端 L3 不可达/环同构 dsl.py+11 夹具跨运行时对拍、L1 同步/L2 防抖/L3 idle 分层调度+结构签名记忆化、Problems 面板点击定位、Parallel/Subgraph 迁移 FormRenderer〔新业务件 saved-graph-select、嵌套通配文案键、数组 min/max 门控〕、后端一处 flaky 测试修复与冒烟修复；T17 复查不重开，前端 vitest 336/30、后端 431/8、真实浏览器冒烟零控制台错误，截图入 docs/assets；批 3 五提交 `4b15d95`/`875922b`/`f496062`/`79317e3`/`471b930`：reverseDeps 双类反向索引（target 8 处/4 类与 schema x-ref 对拍、删节点清 target 索引化、L2 收窄真实引用方）、quickFix v1「删除悬空引用」（Problems 内联按钮，仅挂 REF_NODE_NOT_FOUND）、200/500 节点基准（`frontend/bench/` RUN_BENCH 门控、数字入 BENCHMARK.md：稳态单节点 L2 增量 p95≈10ms@500 低于一帧、冷启动全量 p95≈70ms 一次性，不引 Web Worker）、Condition 迁移 FormRenderer（最后一个手写节点 Config，九节点八迁一留 Wait）+ 冒烟修复空串 label 数组标题；前端 vitest 359/32（另 2 基准场景门控跳过）、后端 431/8 零改动零回归，浏览器冒烟脚本 `scripts/dev/m4_batch3_smoke.py` 实测 quickFix 诊断消失零控制台错误，截图 m4-condition/m4-quickfix-smoke.png；契约 08「M4 立项/批 1/批 2/批 3 落码」条，U40/U41/U42 转正式）；**题一 M1–M4 主线至此闭合；M5 契约设计轮已于 2026-09-17 完成（docs/24，四缺口全收口＋同步面已落），唯 ADR T18 已于 2026-09-17 用户拍板选 B（自研暂停帧+续跑），M5a 已落码**（0358696，441 passed/8 skipped 零行为变化，08 M5a 立项条+落码条；M6 已落码（1f49644，ADR T19 收口），M5b 已落码（三批 bf185ed/b4abb59/280493e/1c1539a/f6a33e7），M7 已落码（三批 0f934d6/ff4acf9/e46fc09，08 M7 立项条+落码条 + ADR T20），门控 M6 落码→M5b→M7→M8/M9，M5 只到单实例重启可恢复，见 Active work 11）；**M10 span 级追踪已于 2026-09-18 立项并落码收口（08 M10 立项条+落码条，ADR T21 不引入 OTel SDK，U49–U52 转正式，立项 `ba9e0d2` + 四批 `1f21b1a`/`89f5645`/`c227f0c`/`2b1dbe9`，后端 486 passed/前端 359 passed，见 Recently shipped 3）；M8 交互模板系统亦于 2026-09-18 立项并三批落码收口（U53/U54 转正式，后端 518 passed/前端 370 passed，见 Recently shipped 2 与 Active work 14），**M9 入站 Router + 灰度发布 + 指标门控自动回滚已于 2026-09-18 立项并落码收口（ADR T22 同日收口；新包 routing/ + recording/gate.py + monitoring/business.py + 前端发布流＋同图草稿 PUT，U55–U59 转正式，后端 602/前端 396，见 Recently shipped 1 与 Active work 15），题二 M1–M10 主线至此全闭合**。原主线 Phase 3 与 docs/18 真实种子试用降为并行/后置，触发条件不变，沙盘假定 Go 口径不变**。此前阶段：Phase 1 沙盘假定通过、Phase 2 全部范围于 2026-09-16 收口并合并 main（2026-09-14 进入 Phase 2 为用户指示的假定，非真实试用结论）。W1-W10 Demo 全部完成（W9-W10 电商退款端到端 Demo 于 2026-09-13 跑通，08 §7.3 七条验收全部达成）。**Phase 2 六项节点「条件分支节点 condition」「循环节点 loop（v1 条件循环 while）」「并行节点 parallel（扇出/汇聚网关，v1 all_success/all_completed）」「等待节点 wait（v1 定时等待 duration 1-600s）」「子图节点 subgraph（v1 引用已保存图，进程内嵌套执行，深度上限 3）」「人机协作节点 human_approval（v1 进程内审批挂起，10-3600s 超时默认 reject）」均已于 2026-09-14 端到端落地**：condition 节点 config 挂 branches/defaultTarget；loop 节点 config 挂 mode/continueExpression/maxIterations/bodyTarget/exitTarget，循环体回边是唯一合法环（U7 调和），达最大次数/表达式异常 fail-safe 退出；parallel 节点 config 挂 joinStrategy/branches/joinTarget，合成 `__join__` 网关节点做汇聚屏障（spike 实测普通多入边不构成 barrier），all_success 分支失败 fail-safe 汇聚不短路（any_success 缓做 14 D18）；wait 节点 config 挂 waitType/durationSeconds，执行器同步 sleep（线程池边界，600s 上限），恰好一条普通出边纯透传，事件等待/动态时长缓做 14 D19；human_approval 节点 config 挂 summary/approver/timeoutSeconds/onTimeout/approvedTarget/rejectedTarget，`collaboration/` 新增 ApprovalBroker（uuid4 token+`threading.Event`，REST 放行；决策三来源：人工/run inputs 预置/超时，首决生效重复 409），固定双出口（通过/拒绝），阻塞不产生超步，持久化 interrupt/checkpointer/多实例/通知/动态审批人缓做 14 D20；subgraph 节点 config 挂 graphId/inputs（graphId 引用 GraphStore 已保存图，inputs 为子图入参→父图 `{{路径}}`/字面量映射），执行器进程内重入 run_graph（复用 registry/decision/approval broker，子图事件 emit 压制），编译期递归校验缺失/自引用/跨图环/超深/子图非法，恰好一条普通出边，子图运行期异常 fail-safe 折叠为 `status:"failed"`，版本钉版/升级回归/子图市场/远程引用缓做 14 D21；六项均 EdgeDSL 与 Graph version 不变。**Phase 2 适配器方向首项「API 适配器（通用 HTTP 请求）」已于 2026-09-15 端到端落地**：新包 `httpapi/`（adapter_id=`http`，09 待定项 7 收口避名 FastAPI `api/`），单工具 `http/request`，节点 params 为 JSON `{method,url,headers,body,timeout}` 支持 `{{路径}}` 插值，env/注册默认 baseUrl+公共 headers 与节点 headers 键级合并；任何 HTTP 响应（含 4xx/5xx）均 SUCCESS 输出 `{status,headers,body}`，仅传输层失败 FAILED（HTTP_TIMEOUT/HTTP_CONNECT_ERROR），v1 无重试/无 SSRF 防护/无凭证加密，OpenAPI 导入/OAuth/秘密变量/录制生成等缓做 14 D22（触发：真实外部系统接入或多租户）；内置 `/api/demo/mock/*` 端点供离线实测，前端工具选择器首次接 `GET /api/adapters`（按适配器分组、权限/幂等标记）。**Phase 2 适配器方向第二、三项「DB 适配器（通用 SQL）」「消息适配器（进程内消息服务）」已于 2026-09-15 端到端落地**：新包 `database/`（adapter_id=`database`，`database/query` read·幂等返结果集、`database/execute` write 返 rowcount，04 §4.7）与 `message/`（adapter_id=`message`，`message/send` write，`{channel,to,subject,body}`、to ≤20、零真实投递，04 §4.8）；适配器专用 `ATLAS_DATABASE_URL`（scheme 白名单 pg+psycopg/sqlite，自建 engine 工厂，与平台 DATABASE_URL 物理隔离；ADR T10，09 待定项 8 收口），未配置时 api/main 与 demo registry 显式注入 StaticPool SQLite 内存库（seed 两笔订单，reset 重建），适配器自身未注入则 fail-closed DB_NOT_CONFIGURED；消息落进程内记录 + `GET /api/demo/messages` 陪同查看；真实数据库驱动/SQL 审查/只读账号缓做 14 D23，SMTP/IM/短信/webhook 真实投递缓做 14 D24；**DB/消息适配器工作经 PR #11 于 2026-09-15 合并 main**。**Phase 2 能力项「流程模板库 v1（5 个内置只读模板）」已于 2026-09-15 端到端落地**：新包 `template/`（`graphs.py` 5 个模板图、`catalog.py` TemplateMeta+TEMPLATES+访问器，ADR T11，04 §5.10），模板 `refund-auto`/`http-orders-branch`/`sql-query-notify`/`sql-approval-write`/`approval-timeout-reject` 全部只用已注册 demo 适配器、零配置可跑；只读 Python 常量随代码发布（无 DB/CRUD、reset 不影响），REST `GET /api/templates`（列表投影无 graph）+ `GET /api/templates/{id}`（含 graph，未知 404）；前端「从模板新建」Modal 整画布替换（复用 loadGraph，不做 id 重映射）+ 未保存警示；退款 golden 图移入 template 包，nl_generate 改 import（单一事实源，U27 固化）；用户自建/市场/远程导入/版本钉版缓做 14 D25。**Phase 2 能力项「操作录制 v1（运行录制 + 回放比对）」已于 2026-09-15 端到端落地**：新包 `recording/`（RecordingCase/RecordingStore 进程内、normalize/preset_approvals/compare 纯函数，04 §5.11，ADR T12），录制=前端在正常 /run/stream 收集 node_end（无专用运行端点），回放=冻结 Graph 快照走标准 run_graph（人工决策预置 inputs.approvals 秒回不挂起），序列+逐节点归一化深等（token/sent_at/消息 uuid/HTTP date/resolvedBy/载荷 approvals 五规则），用例 reset 不清除（测试资产）重启清空；批量回放/跨版本对比/影子模式/Mock 响应缓做 14 D26，浏览器操作录制仍随 D22。**Phase 2 能力项「单步调试与断点 v1（逐步执行/节点断点/条件断点/变量只读查看）」已于 2026-09-15 端到端落地**：新包 `debug/`（DebugSession/DebuggerBroker 进程内会话与 step/continue/stop 状态机、DebugController 统一执行器单点钩子，04 §5.12，ADR T13），断点会话级存前端不进 Graph JSON（EdgeDSL/Graph version 1 不变、node_schema breakpoint 预留位不启用），paused 帧下发 globals/outputs 深拷贝只读快照，条件断点复用 conditions 引擎且表达式异常 fail-safe 不命中，并行暂停会话锁串行、合成 `__join__` 不停、subgraph 整段执行、wait 停在 sleep 前、human_approval 停在审批注册前（Modal 放行后照常），停止仅暂停点生效（运行中急停缓做 14 D27）；REST `POST /api/debug/{token}/resume`+`GET /api/debug`，同步 /run 带 debug 422，reset 按 stop 释放全部调试会话；前端 Header 单个「调试」入口（启动即逐节点）、节点左侧断点圆点（条件白心）、属性面板断点+表达式实时校验、悬浮工具条（下一步/继续/停止+变量过滤只读快照）；持久化中断-恢复/断点持久化进图/变量改写续跑/变化历史/子图内断点/命中计数缓做 14 D27。后端 329 单元全绿（+U29 11/I17 7），前端 vitest 56（8 文件，+4），浏览器六场景实测（逐步序列、条件真假两路、审批暂停先于 Modal、停止无 result、断点标记/刷新清空、reset 释放悬挂会话）零控制台错误。**Phase 2 能力项「基础监控告警 v1（运行/节点指标 + 站内告警中心）」已于 2026-09-15 端到端落地**：新包 `monitoring/`（records.py MonitoringStore ring 200 单锁、alerts.py 四内置规则阈值可调、metrics.py 两形状失败推断与 nearest-rank 分位聚合，04 §5.13，ADR T14），埋点纯在 API 层包装 sync /run 与非 debug /run/stream 两个真实运行入口（零节点类型分支；debug/录制回放/subgraph 重入天然不记），健康=completed 且无失败节点；REST 新增 metrics/runs/rules(GET+PUT)/alerts(列表+acknowledge+resolve)，reset 清空运行/告警/streak 并恢复默认规则；独立前端 /monitoring 页（Dashboard 入口、指标六卡、告警表确认/关闭、规则阈值、最近运行展开、5s 轮询），后端 358 passed/8 skipped（+U30 20/I18 9）、前端 vitest 59（9 文件，+3），浏览器八场景实测；OTel/Prom/Grafana 正式栈随 D11、适配器调用级指标/IM 出口/自定义规则 DSL 等缓做 14 D28。**Phase 2 新增能力收口项「多租户权限 v1（端点级 RBAC + 租户分区）」已于 2026-09-16 端到端落地**：新包 `iam/`（种子两平级租户四账号、三角色 viewer/operator/admin、进程内 `sess-` token 会话、TenantRegistry 按租户惰性持有 stores/brokers/monitoring，04 §5.14，ADR T15），除登录/health/demo 模拟系统外全部端点 Bearer 鉴权（401/403/跨租户 404），用户数据（graphs/recordings/feedback/messages/approvals/debug/monitoring）全分区、全局基础设施（模板/适配器发现/demo shop+mock/demo SQLite/出向 client）共享，admin reset 只清本租户且保留录制/反馈；前端登录页 + localStorage 会话 + 401 自动回登录 + UserBadge + 按角色隐藏写操作 UI；后端 377 passed/8 skipped（+U31 17/I19 11）、前端 vitest 64（10 文件，+5），浏览器八场景实测零控制台错误（登录/坏凭证/刷新保持、租户 B 全空且跨租户 graph 404、viewer 只读 403、operator 告警处置、admin reset 范围、登出、阈值分区）。**至此 08 Phase 2「新增能力」清单全部落地；2026-09-16 新增方案文档 docs/19（Schema 驱动配置内核 + 多智能体端到端，题述设计提案），其「设计态内核前置项」（Capability input/outputSchema + 拓扑作用域索引 + L2 引用校验）已在 08 Phase 2 延伸区立项待落码，提案其余部分登记 14 D29-D34。****连带治理：`/run/stream` 由「先跑完再回放」改为真流式**（run_graph 在 daemon 线程执行、queue.Queue 实时下发 node_start/node_end，终帧 `event: result`，运行期异常 `event: error`）——审批阻塞期间浏览器可实时收到 node_start 并放行，同时清掉 wait 落码时记载的假流式局限。纯 stdlib 安全规则表达式（禁 eval/算术/函数）为 condition/loop 两类节点共用，DSL 图级校验、LangGraph 全 conditional 出边齐备，前端配置面板/多出口 Handle/出边标签齐备，浏览器实测金额分流、循环三场景（条件收敛/撞上限 fail-safe/表达式异常）、并行三场景（不等长双分支汇聚/一支失败 fail-safe/坏配置报错）与审批三路径（人工同意/人工拒绝/10s 超时自动拒绝且弹窗自动关闭）。退款链路：webhook 退款单 → AI 决策或条件分支 → shop 适配器执行退款或转人工；SSE 节点事件实时上屏，自然语言可生成退款流程草稿。Phase 1 种子验证工程侧齐备（Docker 一键交付/reset/反馈入口/docs 18+TRIAL），但**无真实 C1-C5 试用数据**，Go 结论为沙盘假定（18 文档 §8）；真实试用若 No-Go 需回退。

结构：
- `docs/` — 全部规格文档（入口：`00-文档索引与治理.md`）
- `README.md` / `AGENTS.md` / `CLAUDE.md` / `git-commit-message.md` / `CHANGELOG.md` — 根目录元文档
- `src/atlas/` + `frontend/` — OODA 最小循环、Graph DSL 编译运行（FastAPI）、前端编辑器核心（拖拽/配置/变量/一键编译运行）已跑通；规划见 `docs/09-工程骨架与目录结构.md`

## Project documents（文档地图）

**文档地图（按场景怎么读 + 治理规则）**：[docs/00-文档索引与治理.md](docs/00-文档索引与治理.md) 是单一事实源与 AI 工作入口，本区只做直达索引：

* [docs/01-PRD-产品需求规格.md](docs/01-PRD-产品需求规格.md) — 产品概念（Harness/Graph/Loop 定义、全场景图谱、DIY 机制）+ 产品定义 + Demo 范围
* [docs/02-技术架构设计.md](docs/02-技术架构设计.md) — 三位一体架构、系统分层、技术选型、组件全景
* [docs/03-数据模型与Schema-契约索引.md](docs/03-数据模型与Schema-契约索引.md) — 全部 Schema 位置速查与字段概览
* [docs/04-组件设计-编辑后台.md](docs/04-组件设计-编辑后台.md) — 编辑后台组件（节点/工具/适配器/逻辑/变量/上下文）
* [docs/05-组件设计-运营体五项核心.md](docs/05-组件设计-运营体五项核心.md) — Skill / Memory / 智能体协同 / 部署方式 / 自定义前端模板
* [docs/06-运行时与质量保障.md](docs/06-运行时与质量保障.md) — Loop 主循环、决策节点、Harness Gateway、Web 适配器、测试层次、安全清单
* [docs/07-递归自动化引擎设计.md](docs/07-递归自动化引擎设计.md) — 平台探索器 / 行为挖掘器 / 诊断修复器 / 元运营体 / 自我进化
* [docs/08-任务迭代计划.md](docs/08-任务迭代计划.md) — 开发路线图、Demo 计划 W1-W10、Phase 1-4 迭代路线、商业化路径、验收标准
* [docs/09-工程骨架与目录结构.md](docs/09-工程骨架与目录结构.md) — 工程目录树、模块→文档映射、待填项清单 ★落码必读
* [docs/10-技术选型决策记录.md](docs/10-技术选型决策记录.md) — 选型 ADR（✅ 已定 / ⏳ 待决策）、Demo 依赖清单 ★落码必读
* [docs/11-存储与数据持久化设计.md](docs/11-存储与数据持久化设计.md) — 存储分层、数据对象→表映射、读写路径
* [docs/12-API与模块接口清单.md](docs/12-API与模块接口清单.md) — 引擎内部接口、Harness Gateway、后端 REST、协同/评估接口
* [docs/13-测试用例清单.md](docs/13-测试用例清单.md) — 单元/集成/回放/端到端/AI 评估用例 + 上线前四道门
* [docs/14-缓做事项登记表.md](docs/14-缓做事项登记表.md) — 缓做/低优事项登记表（每条带触发条件，单一事实源）
* [docs/15-环境与分支策略.md](docs/15-环境与分支策略.md) — 环境划分（DEV/TEST/PROD）+ 分支→环境映射（main/dev/feature/*）
* [docs/16-反馈工作流.md](docs/16-反馈工作流.md) — 用户与 AI 协作者的反馈方式（截图+标签 / computer use）
* [docs/17-前端国际化与设计Token方案.md](docs/17-前端国际化与设计Token方案.md) — i18n（i18next 触发式引入/错误码边界）+ 设计 Token 三层模型（需求依据 04 §28；Token 已落码，i18n 触发式引入，存量零星硬编码见 17 §3.5）
* [docs/18-种子客户验证计划.md](docs/18-种子客户验证计划.md) — Phase 1 种子客户画像/招募标准、三场景试用脚本、Go/No-Go 指标、反馈机制（客户向操作指南见根目录 TRIAL.md）
* [docs/19-Schema驱动配置内核与多智能体端到端方案.md](docs/19-Schema驱动配置内核与多智能体端到端方案.md) — 题述设计方案（2026-09-16）：Schema 驱动前端配置内核（SchemaRegistry/Widget 注册表/拓扑作用域索引/三级校验/性能）与多智能体协同/交互模板/部署灰度回滚/可观测端到端，逐节标注 Atlas 现状与缺口；**提案未立项**，落码前先走 10 ADR/08/03 流程
* [docs/20-题述方案工程化落地路线.md](docs/20-题述方案工程化落地路线.md) — docs/19 的工程化落地拆解（2026-09-16）：M0 前置项已落 + M1–M10 里程碑（题一 Schema 内核 M1–M4、持久化 M5、题二版本化/任务总线/交互卡片/灰度回滚/span M6–M10），每期含仓内改动面、前置门、不做什么、验收口径，附契约同步矩阵与 D29–D34 触发总表；**M1/M2 均已落码并随 PR #24 合并 main（2026-09-16；M2＝L1 扶正+结构化诊断+422 locations 侧车，U37/U38 转正式），M3 已立项并于 2026-09-17 落码收口（五提交 2de5053→6d4a863，ADR T17 自研最小表单引擎零新依赖/U39 转正式，仅迁 ToolCallConfig；ADR T16 手写最小子集/不引 AJV 继续有效）、**M4 批 1/批 2/批 3 已全部落码收口（2026-09-17，T17 复杂度 dump 复查不重开，U40–U42 转正式，题一主线闭合，见 Recently shipped 11–13 与 Active work 12）**、**M5–M10 已全部落码收口（2026-09-18，题一/题二主线全闭合；ADR T18–T22 均已收口，详见 Recently shipped 1–3 与 Active work 12–15）**
* [docs/21-题述原题与方案作答.md](docs/21-题述原题与方案作答.md) — 两道题的题卷+答卷合订阅读版（2026-09-16）：§1 原题照录，§2/§3 按题面「请给出」条目逐条方案作答（模块/数据流/Schema 形状/伪代码/机制策略，不含实现代码），§4 接缝与落地状态，§5 脱离 Atlas 约束的绿地视角（通用骨架 vs Atlas 塑造的选型分期）；细节以 19 为准、排期以 20 为准
* [docs/22-题述方案绿地独立设计.md](docs/22-题述方案绿地独立设计.md) — 两道题的绿地完整独立方案（2026-09-16）：**显式不受 Atlas 架构/技术栈/单进程 Demo 约束**，按可上线多租户 SaaS 从零设计（rjsf/Formily+AJV+Monaco、Temporal 主协调+Worker Versioning 钉版、独立 Bot 服务、NATS、KMS、OTel/Prometheus、Rollout Controller 灰度），§3 两版选型分叉总对照、§4 七条两版不变通用原则；**非 Atlas 落码依据**，落码以 19/20 为准
* [docs/23-题述两题性质与投入产出分析.md](docs/23-题述两题性质与投入产出分析.md) — 会话问答留档（2026-09-16）：M5 为何是题二硬前置与四契约缺口、题一（重构为主）/题二（增强为主）逐里程碑定性、两题优缺点与一页总结；**讨论整理，非契约非选型决策、不触发立项**，M5 缺口权威登记在 08
* [docs/24-M5持久化与中断恢复契约设计.md](docs/24-M5持久化与中断恢复契约设计.md) — **M5 契约设计轮产物（2026-09-17，M5a/M5b 的形状权威，零代码）**：① Repository＝六资源 Protocol（方法与八 store API 一比一）+ for_tenant/reset 分档统一约定 + `src/atlas/storage/` 包位（不做单一胖接口）；② 中断落库 ADR T18 **✅ 已拍板 B**（自研暂停帧+`loader` 续跑+恢复扫描器，A=LangGraph checkpointer 超出 20 §3 边界；2026-09-17 用户拍板）；③ 04 §5.5/§5.6 v2 语义（绝对 deadline 不重计、决策 Event+轮询双写、SSE 解耦、running 中断标 interrupted、reset PG 档分层）；④ `GET /api/runs` 查询端点（不新增写端点）；§5 表结构、§6 验收对齐；同步面已落 03/12/13（U43–U45）/09/14/11
* [docs/handoff-archive-2026-09-18.md](docs/handoff-archive-2026-09-18.md) — 2026-09-18 自 handoff 归档的 Recently shipped #6–#38 历史变更（原编号保留，主 handoff 交叉引用按号定位）

根目录元文档：[CHANGELOG.md](CHANGELOG.md)（变更日志）/ [CONTRIBUTING.md](CONTRIBUTING.md)（贡献指南）/ [AGENTS.md](AGENTS.md)（AI 行为规范，**新会话必读**）/ [TRIAL.md](TRIAL.md)（种子客户试用一页纸）/ [git-commit-message.md](git-commit-message.md)（commit 详细规范）/ [MIGRATION_CONVENTION.md](MIGRATION_CONVENTION.md)（DB 迁移规范）/ [BENCHMARK.md](BENCHMARK.md)（性能基准记录表）/ [.gitleaks.toml](.gitleaks.toml)（密钥扫描配置）/ [.env.example](.env.example)（环境变量模板）

**代码阶段待补（遵循 agent-world 惯例）**：`.pre-commit-config.yaml`。CI 工作流（`.github/workflows/ci.yml`，gitleaks/后端 pytest/前端 vitest+build 三道门）已于 2026-09-14 落地（14 D10a，ADR T8）；CD 仍缓做（14 D10b）。`Dockerfile` + `docker-compose.yml` + `.dockerignore` 已在 Phase 1 准备期落地（2026-09-13）。`pyproject.toml` 与前端工程已在 W1 创建。

## Current state（当前状态）

- **阶段**：W9-W10 电商退款端到端 Demo 已完成（2026-09-13，08 §7.3 七条验收全达成）。新增 `llm/`（LiteLLM 退款决策 + 规则兜底 + NL 草稿生成）、`shop/`（DemoShopService + ShopHarnessAdapter：login/list_pending_refunds/execute_refund/request_human_approval/process_refund）两个包；`graph/loader.py` 经依赖注入接真实决策/适配器/事件回调；API 新增 SSE 运行流、NL 生成、适配器发现与模拟商家控制台；前端退款单选择、节点实时状态、NL 草稿载入。Phase 2 六类节点 + 适配器三项（通用 HTTP API 适配器 `httpapi/`、通用 SQL 适配器 `database/`、进程内消息适配器 `message/`）+ 能力项「流程模板库 v1」（只读内置目录 `template/`，5 个零配置模板 + 两个 GET 端点 + 前端「从模板新建」整画布替换）+ 能力项「操作录制 v1」（新包 `recording/`：进程内用例存储 reset 不清除 + 冻结 Graph 快照回放 + 序列/逐节点归一化比对，五个 REST 端点 + 编辑器「录制与回放」Modal，04 §5.11）全部落地后，Phase 2 能力项「单步调试与断点 v1」（新包 `debug/`：进程内调试会话与统一执行器暂停钩子、会话级断点不进 Graph JSON、step/continue/stop、条件断点 fail-safe、globals/outputs 只读快照，04 §5.12）亦已落地，能力项「基础监控告警 v1」（新包 `monitoring/`：进程内 ring 200 运行记录、四内置规则阈值可调、站内告警中心，04 §5.13）亦已落地；后端 358 单元全绿、前端 vitest 59（9 文件）全绿（明细见 Quality gate），浏览器实测 12345→refunded / 12346→human_review、子图引用真实保存图的父图运行（入参映射到达子图、父图引用子图产出）、http/request 对内置 mock 端点 GET 200/POST 插值/401 仍 SUCCESS/坏 params FAILED，database/query 打 demo SQLite 返两笔订单、execute INSERT 后 query 可见且 reset 复原、message/send 记录在 `/api/demo/messages` 可见且 reset 清空，模板库五模板加载/编译与 sql-query-notify（min_amount=1000 走有单分支）、approval-timeout-reject（10s 无人操作超时拒绝）、http-orders-branch（200 成功分支）三条实跑链路，以及录制 approval-timeout-reject 一次真实运行→回放秒回三节点全匹配（跨运行 token/消息 uuid/sent_at/resolvedBy/预置 approvals 归一化生效）→reset 后用例保留且回放仍匹配→Popconfirm 删除用例消失；能力项「多租户权限 v1」（新包 `iam/`：种子两租户四账号、三角色端点级 RBAC、进程内 token 会话、TenantRegistry 租户惰性分区，04 §5.14）亦已落地，全部业务端点 Bearer 鉴权、用户数据按租户全分区（跨租户对象 404、角色不足 403、未登录 401），前端登录页/会话/角色门 UI 齐备，后端 377 单元全绿、前端 vitest 64（10 文件）全绿，浏览器八场景实测登录/分区/角色矩阵/reset 范围/登出全通过（明细见 Quality gate）。2026-09-16 当日再落地 **08 Phase 2 延伸立项「设计态内核前置项」①–⑤**（docs/19 提案驱动：Capability input/output_schema JSON Schema 子集白名单与发现投影、前端拓扑作用域 ScopeIndex、L2 模板引用三错误码实时校验、DSL 编译期复查反绕过、NL 草稿参数非阻塞警告，零新依赖，后端 410/前端 87；①–⑤当日经 **PR #19 合并 main**（merge `6b8ce5a`；提案与 handoff 同步随 **PR #18** merge `27e331f`，三道门 CI 实跑全绿，详见 Recently shipped 18）。当日随后完成题述方案文档包：[docs/20](docs/20-题述方案工程化落地路线.md) 工程化落地路线（M1–M10，每个里程碑首行标注对应题面 A1–A6/B1–B6；§1.2 就绪度快照与推荐推进次序——**M1 可直接立项开工、M2 被待决 ADR T16 卡住、M3 依赖 M2、M4 依赖 M3、M5 需先补一轮持久化契约设计并拆 M5a/M5b、M8 是唯一不被 M5 阻塞的题二里程碑（M3 完成后可插队）**）、[docs/21](docs/21-题述原题与方案作答.md) 题卷+答卷合订（§1 原题照录、§2–§4 不含实现代码的方案作答、§5 脱离 Atlas 约束的绿地视角）、[docs/22](docs/22-题述方案绿地独立设计.md) 不受 Atlas 架构/技术栈约束的完整绿地方案（**非 Atlas 落码依据**）；[docs/10](docs/10-技术选型决策记录.md) §4 登记 **ADR T16（前端 L1 JSON Schema 校验器：手写最小子集 / AJV / 后端校验，M2 启动前必须收口；推荐手写起步但未拍板）**。该文档包共 7 个 docs 提交（`b0a1f2a`→`fc67668`）现已在 origin/dev；**M1/M2 均已落码并于当日随 PR #24 合并 main（见 Recently shipped 16/15）；M3 已于 2026-09-17 落码收口（ADR T17/U39 转正式，五提交 2de5053→6d4a863，已推送 origin/dev），**M4 批 1/批 2/批 3 已全部落码收口（T17 复查不重开，U40–U42 转正式，题一主线闭合，见 Active work 12 与 Recently shipped 11–13）；**M5–M10 已全部落码收口（M5a/M6/M5b/M7/M10/M8/M9，ADR T18–T22 均收口，题二主线 2026-09-18 全闭合，见 Recently shipped 1–3）**）。
- **仓库**：git 仓库，**已推送 GitHub private 仓库 `bayernjf/atlas`**（main 为生产分支、dev 为集成分支，分支策略见 15 文档）。Phase 1 工作经 PR #3 于 2026-09-14 合并（merge `f069b53`，19 提交）；Phase 2 工作经 PR #4-#10 陆续合并——#4 condition、#5 loop、#6 parallel、#7 wait、#8 human_approval、#9 subgraph（2026-09-14）、**#10 API 适配器 http/request（2026-09-15，merge `4385a00`）**、**#11 DB/消息适配器 database/query|execute + message/send（2026-09-15 合并）**；PR 与合并后 main 的 GitHub Actions CI 均实跑全绿。流程模板库 v1（2026-09-15，6 提交 8f3238b 起）、操作录制 v1（2026-09-15，5 提交 4f7af04→857924f→7282cb2→19b39ee→42a82f3）、单步调试与断点 v1（2026-09-15，6 提交 2d7fa28→6693023→7e5a23a→5e13cdd→8d5bdd7→ecb9605）与基础监控告警 v1（2026-09-15，5 提交 2362fdf→c474a80→c721f47→91f6bcc→8551f90）、多租户权限 v1（2026-09-16，4 提交 7cbd509→3620545→64d34b3→7a2ed14，外加登录页 token/orientation 与 docs/17 补契约 b0bf5d4/5660718/2759a13）随后经 PR #12-#16 全部合并 main——#12 模板库（merge `9c849ff`）、#13 录制（`1e6475e`）、#14 调试（`ebffa12`）、#15 监控（`96a9d12`）、#16 多租户（`6bbd77c`）；另经 PR #17（merge `34304b5`）将 `.codegraph/` 加入 `.gitignore`；**#18 docs/19 题述提案 + handoff 同步（merge `27e331f`）与 #19 设计态内核前置项（merge `6b8ce5a`）亦于 2026-09-16 合并 main；M1/M2 全部提交（docs/20-23 文档包、T16、十提交 M1、五提交 M2）随 **#24 合并 dev→main（merge `0451d20`，origin/main 当前位置）**，各 PR 三道门 CI 均实跑全绿。**M5a–M10（含 M8 审批卡片、M9 Router 灰度与门控回滚）已全部落码并推送 origin/dev；2026-09-18 HEAD `1d4d2d8` 与 origin/dev 同步，dev 不再领先；主线 M1–M10 全闭合，后续按 docs/14 缓做项与 Phase 3/真实种子试用另行立项，立项前不动代码。**
- **技术选型**：✅ 已全部收口（2026-09-13，T1-T5 见 10 文档 §3）：Python 3.11+ 引擎 / Go Harness 网关为产品化目标、**Demo 用 Python/FastAPI 实现同构接口** / TypeScript + React 19（满足 React 18+）/ LangChain+LangGraph / LiteLLM（Demo 对接主流商业 API，`.env` 切换，不引入 vLLM）/ Playwright / PostgreSQL+pgvector / Redis / **Demo 进程内事件总线替代 NATS** / FastAPI / `@xyflow/react` 12 + Zustand 5 + AntD 6 / 评估优化层仅留接口；剩余 ⏳（vLLM、策略训练、多语言）均为后续阶段范围。
- **Demo 依赖清单已落码**：后端在 `pyproject.toml`，前端在 `frontend/package.json` / `pnpm-lock.yaml`（版本为 2026-09-13 解析的稳定版）。
- **关键文件**：后端 `src/atlas/`（26 个包；engine 最小循环、graph DSL 契约+编译器（W9-W10 注入决策/适配器/事件）、llm 退款决策+NL 生成、shop Demo 店铺服务+适配器、httpapi 通用 HTTP API 适配器（httpx，单工具 http/request）、database 通用 SQL 适配器（database/query|execute，ATLAS_DATABASE_URL 白名单工厂，demo SQLite 回退）、message 进程内消息适配器（message/send 零真实投递）、template 内置只读流程模板目录（graphs.py 5 图 + catalog.py 元数据/访问器，无 DB/CRUD）、recording 运行录制与回放（cases.py pydantic 用例+进程内 store reset 不清除、replay.py 归一化/预置审批抽取/序列与逐节点比对）、debug 进程内调试会话与暂停控制器（sessions.py DebugSession/DebuggerBroker step/continue/stop 状态机、controller.py 统一执行器 before_node 钩子，会话级断点/条件 fail-safe/只读快照）、monitoring 进程内运行指标与告警（records.py ring+单锁 store、alerts.py 四规则 RuleConfig/evaluate_rules、metrics.py 节点失败两形状推断/分位聚合，04 §5.13）、iam 种子身份与租户分区（principals.py 两租户四账号/三角色矩阵、sessions.py 进程内 sess- token、registry.py TenantRegistry 按租户惰性装配服务、deps.py get_principal/require/services_for，04 §5.14）、api FastAPI（auth login/me/logout、graphs/SSE/NL/adapters/templates/recordings 五个录制端点/debug resume+列表/monitoring metrics·runs·rules+alerts 确认关闭/Demo 控制台/mock 端点/messages）、memory 连接层、harness 契约/注册、collaboration 审批 broker、web 三层定位适配器；**M5–M10 新增** `storage/`（七 Repository 协议 + memory 聚合 + PG 六 store/帧序列化/恢复扫描）、`versioning/`（不可变发布 + subgraph `@N` 钉版）、`coordination/`（任务信封 Envelope/TaskStore/退货退款沙盘 run_return_refund）、`skills/`（Skill/Bot/CoordinatorConfig）、`logistics/`（假物流 check/sign_receipt 适配器）、`tracing/`（M10，与 OTel 同形最小 Span/Tracer + contextvars + to_tree 折叠，纯 stdlib 零依赖）、`cards/`（M8，内置只读审批卡片目录 refund-approval + render_card web/im/email 三渠道渲染降级 + map_action_output）、`routing/`（M9，RolloutConfig 判别联合三段分桶 Router 纯函数 + RoutingStore per-tenant 灰度状态机 + gate 门控自动回滚））、`db/migrations/001_enable_pgvector.sql`，前端 `frontend/`（登录页/Dashboard/Editor/监控页 + 画布/节点/属性/变量面板 + `lib/` 纯逻辑（auth 会话与角色矩阵/变量/节点目录/序列化/适配器分组/录制事件转换/监控中文映射/API client 含 Bearer/SSE 与监控告警）+ Zustand store + vitest 单测）；模块填充顺序见 09 文档待填项清单。

## Active work / 待办

按优先级降序：

1. ✅ **W1-W2 基础骨架落地**（08 文档 7.1，2026-09-13 全部完成）：工程骨架/venv、LangGraph 最小 OODA 循环、前端编辑器框架、PostgreSQL+pgvector 连接均已跑通（详见 Recently shipped）。
2. ✅ **W5-W6 编辑器核心功能**（08 文档 7.1，2026-09-13 完成）：拖拽画布、3 种节点类型化配置 + 实时校验、全局变量与 `{{路径}}` 引用、Graph JSON 导出；vitest 20 个单测全绿。
3. ✅ **W7-W8 编译与运行**（08 文档 7.1，2026-09-13 完成）：Graph DSL 契约 + 静态校验、DSL→LangGraph 编译/运行、FastAPI graphs 保存/读取/编译/运行、编辑器一键"编译并运行"；后端新增 16 个用例。
4. ✅ **W9-W10 端到端 Demo**（08 文档 7.1，2026-09-13 完成）：电商退款完整链路跑通（LiteLLM 决策 + 规则兜底、shop 退款业务能力、SSE 实时进度、NL 退款草稿、模拟商家控制台），08 §7.3 七条验收逐条达成（映射见 08 W9-W10 落码记录）。
5. ✅ **Phase 2 全部能力已落地并合并 main**（2026-09-14~16；六类节点 condition/loop/parallel/wait/human_approval/subgraph + 三适配器 httpapi/database/message + 模板库/录制/调试/监控/多租户 v1，PR #4–#16；设计态内核前置项①–⑤ PR #19）。详细过程见 archive #20–#33 与 Current state。
6. 文档缺口登记：`09-工程骨架` 待定项 1/2/3/4/5/6 均已收口（待定项 3：不新增 deployment/ 包，Dockerfile + docker-compose.yml 放仓库根，2026-09-13）。新增测试运行器选型 vitest 已记 10 文档 §4 ADR（W5-W6）。
8. ✅ **Phase 1 种子客户交付准备（工程侧）**：Docker Compose 一键启动 + [docs/18](docs/18-种子客户验证计划.md) + [TRIAL.md](TRIAL.md) + 应用内反馈入口（2026-09-13）；真实招募/回填在沙盘假定下标记完成，真实试用启动按 18 文档执行。
9. ✅ **D10a CI 质量门**（2026-09-14）：`.github/workflows/ci.yml` 三道门 gitleaks + 后端 pytest + 前端 oxlint/vitest/build，main/dev 与 PR 触发；CD 留 14 D10b。
10. ✅ **D9 基准**（2026-09-14）：`scripts/benchmark.py` + [BENCHMARK.md](BENCHMARK.md) 首份基线（commit 407812e，OODA 2.05ms / 编译 1.81ms / 退款端到端 2.28ms）。

11. ✅ **M5 契约设计轮与 M5 子阶段**（2026-09-17 收口＝[docs/24](docs/24-M5持久化与中断恢复契约设计.md)，T18 拍板 B；M5a `0358696` / M6 `1f49644` / M5b `f6a33e7` / M7 `e46fc09` 均已落码）。详见 archive #6–#12。


12. ✅ **工程主线：题一/题二 M1–M10 全闭合**（2026-09-18）：M1/M2 随 PR #24 合并 main、M3/M4 题一闭合（U39–U42 转正式）、M5a/M6/M5b/M7/M10/M8/M9 题二闭合（ADR T18–T22 收口，U43–U59 转正式）。M1–M7 详细过程见 archive #6–#18，M8/M9/M10 见 Recently shipped 1–3。Phase 3 与真实种子试用后置，触发条件见 14 文档。

**Active feedback**：暂无（种子试用开始后，由 docs/18 §6.1 跟进项迁移至此，解决后留痕 Recently shipped；流程见 16 文档）。

13. ✅ **M10 span 级链路追踪**（2026-09-18，立项 `ba9e0d2` + 四批 `1f21b1a`/`89f5645`/`c227f0c`/`2b1dbe9`，ADR T21，U49–U52 转正式）。详见 Recently shipped 3。

14. ✅ **M8 交互模板系统（审批卡片双向 Schema + web/im/email 三渠道渲染降级）**（2026-09-18，立项 `e994ac5` + 三批 `2b35fc9`/`5f4384e`/`4b45571`/`25afa10` + 收口 `94000de`/`486a591`，U53/U54 转正式）。详见 Recently shipped 2。
15. ✅ **M9 入站 Router + 灰度发布 + 指标门控自动回滚**（2026-09-18，立项 `ce57355` + 批 1–4 + 总收口，ADR T22，U55–U59 转正式；m9_smoke 32 断言、Router p50 0.0041ms、浏览器 0 console error）。详见 Recently shipped 1。

7. 📋 **i18n 与设计 Token 方案已定（2026-09-13，docs/17）**：设计 Token 等价替换**已落码**（`frontend/src/theme/tokens.ts` 单一事实源 + `setup.ts` 注入 `--atlas-*` 变量 + AntD theme，三页面主体零视觉差异；2026-09-16 17 §3.5 收尾清单五项已全部清零，`frontend/src` 仅 tokens.ts primitive 定义含 hex，浏览器逐项核对）；i18n 库（i18next+react-i18next）按触发条件引入（14 D12：首个英文使用者/出海需求），组件描述多语言随 Phase 2 模板库（14 D13）。

> 缓做/低优项：统一登记在 [docs/14-缓做事项登记表.md](docs/14-缓做事项登记表.md)（每条带触发条件，条件满足移回本区并标注重启日期）。当前含移动端适配器、策略训练、组件市场、模型路由器、NATS、Go 网关、多租户 v2（真实账号/节点级权限/审计/跨租户共享，v1 端点级 RBAC+分区已落，见 14 D7 边界注记）、运营体市场、CD 自动化部署、可观测性、i18n 落码、组件描述多语言等项（D10a CI、D9 BENCHMARK 均于 2026-09-14 完成移出）。

## Recently shipped（最近变更；只留最近 5 条）

> 完成项详细过程整块归档至 [docs/handoff-archive-2026-09-18.md](docs/handoff-archive-2026-09-18.md)（保留原编号 #6–#38；主文件里「Recently shipped N」N>5 的引用按该文件原号定位）。

1. **feat(routing/release/monitoring/frontend): M9 落码收口——入站 Router 三段分桶 + 灰度发布 + 指标门控自动回滚（题二最后里程碑，2026-09-18；立项 `ce57355`、批 1 `c863f86`/`6e3f74a`/`60d885b`/`57b9966`、批 2 `1dc6314`/`1380cff`、批 3 `dcd40d1`/`7f2b503`、批 4 `d6223ed`/`d53de4b`/`d7f0c77`、收口 `7bdbe72`/`67f82fa`/`e014cdf`/`97c4afb`；08 M9 立项条+落码收口条、ADR T22、U55–U59 转正式，契约 03 rollout_config/route_decision/release_gate/business_metrics、04 §5.11/§5.13/§5.16、06 §6.11/§6.17、09 routing/ 包位、12 §3.12+REST、13 U55–U59、14 D26/D32、20 §4.4/§5/§8）**——**题二 M1–M10 主线至此全闭合**，零新依赖、后端纯 stdlib。四块：① 新包 `routing/`（models RolloutConfig 判别联合规则固定序 internal→lowValueBucket→canary→full + Router 纯函数三段分桶〔租户 allowlist→payload.amount<=200 低金额桶→订单 id 哈希稳定百分比，同订单不跳版本，桶键缺失 fail-safe 落 stable〕+ RoutingStore per-tenant 状态机 idle/canary/full/rolled_back 与 traffic 计数 + gate.evaluate_after_run）；② 发布流批量回放门禁（recording/gate.run_release_gate 复用 replay，POST release-gate 只跑不发布、publish 可选 {gate:true} blocked→409 携 GateReport 不产版本；GateReport case 行无 diff_keys、差异并入 note）；③ monitoring 业务三率（business.py extract_business 以 shop 终态 refunded/human_review 为准、expected_amount 取 trigger/event payload.amount，demo shop 不回金额则全额回退、沙盘金额差异率恒 0）+ Alert.action 与 rollout_gate 规则，门控仅 canary+candidate run+autoRollback 且样本≥minSamples 才求值，任一越阈一次 rollback(actor=auto)+critical 告警带 action{rollback,from,to,reason}，**只自动回滚、放量全手动（promote 仅手动，全仓无自动 promote）**，回滚不改外部已发生事实、无补偿；④ 前端 ReleaseModal（批量回放逐例报告+门禁通过才发布）+ RolloutModal（状态/三动作/五段流量计数/四段规则/门控表单/模拟入站事件看分流落点）+ Editor 草稿/发布版选择与发布灰度按钮 + Monitoring 业务三率卡/per_version 表/告警 action 徽标。**pin-to-version 复用 M6 不可变快照+M5b 中断帧冻结，无新机制**（U56 实测 canary 中挂审批→回滚→resume 续跑仍 candidate、新事件落 stable）。**计划外补件** `PUT /api/graphs/{id}` update_draft（覆盖同图 latest 草稿不新建 id、不动发布版，发布流对同一 graph 发 v2 的闭环前提；d6223ed）。event 路由生命周期实测：仅 publish 未 PUT/start 入站事件 409，start 需≥2 发布版（stable=versions[-2]/candidate=versions[-1]），rolled_back 为终态不能再 start（重配规则需新图），重复回滚幂等 200。收口：m9_smoke.py **32 断言全过**、BENCHMARK Router 10k resolve **p50 0.0041ms/p99 0.0450ms（约 242000 resolves/s）**、真实浏览器全链路（start canary→事件落 v2→manual 100% 越阈自动回滚→新事件回落 v1）**23 条 console 0 error/0 warning**、4 截图入库；e014cdf 修 Editor draftGraphId 集成缺陷（录制用例 graph_id 与门禁筛选错位致门禁恒 skipped）。三道门：后端 **602 passed/13 skipped**、前端 oxlint 0 error（2 既有 warning）/ **vitest 396 passed/2 skipped/34 文件** / build 过。**不解除 D32**（真实生产发布/多实例，随 D5/D6/D10b/S1）、**D26 仅取批量回放+跨版本 diff**（用例集报告/影子/Mock/持久化仍缓做）。**PG 路径本环境未验证**（无 DATABASE_URL：PgGraphStore.update_draft、RoutingStore PG 装配、business JSONB/迁移 002·004·005 仅列对齐审查，在 13 skipped 内；验证 ATLAS_RUN_INTEGRATION=1+DATABASE_URL）。已同步 origin/dev。
2. **feat(cards/approval/approval-ui): M8 落码收口——交互模板系统 审批卡片双向 Schema + web/im/email 三渠道渲染降级（D33 沙盘语义期，2026-09-18；docs-only 立项 `e994ac5`、批 1 `2b35fc9`、批 2 `5f4384e`/`4b45571`、批 3 `25afa10`、收口 `94000de`/`486a591`；08 M8 立项条+落码条、T17 复查不重开/未新增 ADR、U53/U54 转正式，契约 03 `card_template`、04 §5.6、06 §6.16、12 §3.11+REST、14 D33）**——题二 D33 沙盘语义期闭合。批 1 新包 `src/atlas/cards/`（内置只读目录 refund-approval + render_card web/im/email + map_action_output/to_message_params，U53 20 用例）；批 2 human_approval 可选 cardTemplateId（DSL 未命中目录编译 422、loader/broker 透传卡片与上下文快照、M5b 中断帧 card_template_id 恢复重建、`GET /api/cards` + `GET /api/approvals/{token}/card?channel=` + decision actionId/form、GET 只读防邮件预取，U54 12 用例）；批 3 前端 card-select + 卡片 bindings 纳 scope L2 + CardRenderer（FormRenderer 第三类来源 `kind:"card"`）+ 审批 Modal 集成（vitest cards 7/scope 4）。收口 `94000de` 修批 3 审批 Modal footer 空指针（currentApproval=null 进编辑器即 PAGEERROR，补空值守卫）、`486a591` 增正式三渠道冒烟脚本 `scripts/dev/m8_smoke.py` + 证据图 m8-smoke.png/m8-card-config.png。三道门：后端 **518 passed/13 skipped**（+32）、前端 oxlint 0/0 / **vitest 370 passed/2 skipped**（M10 基线 359，+11）/ tsc+vite build 过，零新依赖。三渠道冒烟对真实 :8000+:5174 全绿（API web/im/email 渲染断言 + 浏览器搭 trigger→ai→human[挂卡]→tool_call-1/2 图选大额主观单 12346，CardRenderer 五字段全插值/同意·拒绝按钮/决策续跑 completed/控制台零错误；tool_call 作图合法末端，DSL 不允许 wait 末端）。**不解除 D33**：真实 IM/邮件外发（D24）、自动通知（D20）、卡片 CRUD/市场仍缓做。**题二仅剩 M9（Router 分桶 + 灰度发布 + 指标门控自动回滚，依赖 M5b/M6，含 D26 批量回放，落码前记 ADR 候选 T22；见 Active work 15）。** 已同步 origin/dev。
3. **feat(tracing/graph/coordination/monitoring/api/recording): M10 落码收口——span 级链路追踪（D34 进程内部分，2026-09-18；docs-only 立项 `ba9e0d2` + 四批原子提交 `1f21b1a`/`89f5645`/`c227f0c`/`2b1dbe9`；08 M10 立项条+落码条、ADR T21、U49–U52 转正式，契约 03 `trace_span`/run_event、04 §5.15、06 §6.15、12 SSE/RunRecord、14 D34）**——题二可观测收口，纯 stdlib 零新依赖、**不引 OTel SDK**、前端零改动（SSE 纯超集）。批 1 新包 `src/atlas/tracing/`（Span/Tracer/contextvars 进程内传播/to_tree include_internal 折叠，traceId 32hex/spanId 16hex，9 用例，U49）；批 2 `graph/loader.py` run/节点/tool/parallel/subgraph 全链路埋点 + node_start/node_end/run_end 三帧超集（6 用例，U50），parallel 合成 `__join__` 不外泄（internal `parallel-join` 折叠）、subgraph 建非 internal span 而内部节点 internal 折叠；批 3 coordination 信封贯穿真实 traceId/parentSpanId（取代 M7 runId 占位）+ task_dispatch/task_done/物流 tool span（幂等重放折叠且不重复执行）+ RunRecord.trace_id + api sync/非debug stream 两入口建 tracer 拼 graphVersion（coordination 4 + api 2 用例，U50/U51）；批 4 recording.normalize 任意层级剔除 traceId/spanId/parentSpanId/graphVersion + 折叠投影断言（U52）。**三处落码细化（非选型变更）**：① graphVersion 精确形态发布 `graphId@<releaseVersion:int>`（无 v 前缀，对齐 M6 `f"{sub_id}@{pin}"`）/草稿 `graphId@draft`，立项文案 `@vN` 为记号示意，03/04/06/08/13 已订正；② debug 单步经 `run_graph(tracer=None)` 显式禁用（`_AUTO_TRACER` 哨兵区分未传自建/显式 None 不埋点），SSE 帧保持旧形状；③ 完整 span 树只在进程内返回值 `traceTree`，run_end/result 终帧不下发树（终帧仅 traceId/spanId/graphVersion）。验收三条全达成（退货协同 root→2 dispatch/2 done/1 tool 完整树、include_internal 折叠、normalize 使黄金回放零回归）。三道门：后端 **486 passed/13 skipped**、前端 oxlint 0/0、vitest **359 passed**（2 基准门控跳过）、tsc+vite build 过。**不解除 D34**：跨服务传播（随 D5）、OTel/Prometheus/Tempo 合流（随 D11）、span 持久化导出、前端瀑布 UI 仍缓做。**题二剩 M8（交互卡片，仅依赖 M3）、M9（Router 灰度+门控回滚，依赖 M5b/M6，最大，含 D26 批量回放）。** 已同步 origin/dev。
4. **feat(coordination/skills): M7 落码——多 Bot 任务总线（2026-09-17，三批原子序，`0f934d6`/`ff4acf9`/`e46fc09`；08 M7 立项条+落码条 + ADR T20，契约 docs/19 §2.2.2/§2.3.1/§2.4）**——题二 D31 沙盘语义期闭合。三批：① 批 1 coordination 包（`envelope.py` Envelope + dispatch 状态机 pending→accepted→running→done/failed/timeout + `store.py` TaskStore 进程内按租户 + 幂等键去重返首结果）；② 批 2 skills 包（`skills/skill.py` Skill+Bot+CoordinatorConfig）+ `logistics/adapter.py` 假物流适配器（check_receipt/sign_receipt）+ `shop/service.py` RefundOrder version 字段 + compare_and_set 乐观锁 CAS；③ 批 3 `coordination/sandbox.py` run_return_refund（dispatch 客服+物流两信封→join→未签收禁放款 condition→escalate / 签收后 CAS 放款、幂等重放返首结果）+ `GET /api/tasks`/`/api/tasks/{task_id}` 任务查询端点（state/assignee 过滤、422/404、TaskStore 进 TenantServices）。**两处落码细化**：浏览器实测退货协同沙盘以后端沙盘编排+任务查询 API 测试承载（任务信封是 Graph 之上协调层、无前端 UI，前端展示随 M8/M9）；超时重试 attempt+1 精确状态机未落地（timeout 终态，重试由幂等键不变+重新 dispatch 承载）。**验收全兑现**：后端 **463 passed/13 skipped**（+13 新测试：4 coordination/3 skills+CAS/4 沙盘/2 任务端点）零回归、退货退款沙盘端到端跑通、幂等重放返首结果/CAS 冲突放弃/超时 fail-safe 单测齐备、零新依赖、前端零改动。**沙盘口径兑现：假物流 Bot 不解除 D31**。**下一步＝M8（交互卡片，仅硬依赖 M3）/ M9（灰度回滚，依赖 M5/M6）/ M10（span 追踪，依赖 M7）**。

5. **docs(plan): M7 立项——多 Bot 任务总线（2026-09-17，docs-only 立项批、零代码；08 新增 M7 立项条 + 10 §4 ADR T20，03 `task_envelope`、05 技能注记、06 §6.14 协同运行时、12 任务端点、13 U47/U48、14 D31 注记、09 skills 包位、20 §3/§5、handoff/CHANGELOG 同步）**——题二 D31 沙盘语义期（docs/20 §4.2，契约 docs/19 §2.2.2/§2.3.1/§2.4 提案转权威，前置 M5b+M6 已落码）。范围四块、三批原子序：① 任务信封执行层（`src/atlas/coordination/`：Envelope taskId/runId/idempotencyKey/traceId/graphVersion/type/assignee/payload/refs/deadlineMs/state/result/attempt + TaskStore 进程内首版按租户 + dispatch/join/escalate 三模式）；② Bot 执行体抽象（`skills/` 首次启用＝技能声明+适配器集合+受护栏 Coordinator 只输出意图+置信度；物流 Bot 用进程内假物流适配器沙盘）；③ 冲突三层（L1 幂等键返首结果 + L2 CAS demo shop 进程内 version 模拟 + L3 硬约束 condition+升级 human_approval）；④ 超时三级链（deadlineMs+retry.timeout+timeoutSeconds，重试→升级→fail-safe）。**非目标**：NATS/跨服务（D5）、自由 Agent mesh、Bot 间对话、SLA 看板、技能推荐真实检索。**沙盘口径**：假物流 Bot 不解除 D31。**验收**：退货退款沙盘链路（客服+物流 Bot dispatch→join→未签收禁放款 condition→escalate）+ 幂等/CAS/超时单测与浏览器实测。**下一步＝M7 批 1 落码**。

## Quality gate（质量门）

- **文档侧**：00 文档"切分核对报告"已确认 274 章节全映射、逐段覆盖无丢失。改规格文档必须遵循 00 文档治理规则（唯一事实源 / 改内容流程 / Schema 契约防漂移）。
- **代码侧**：最小质量门已建立——GitHub Actions CI（`.github/workflows/ci.yml`，2026-09-14 落地；push main/dev 与 PR 触发）：gitleaks 全历史扫描 + 后端 pytest + 前端 oxlint/vitest/build；**PR #3-#19 与合并后 main 均已实跑全绿**（gitleaks 初版 403 经补 `pull-requests: read` 修复；#12-#16 对应模板库/录制/调试/监控/多租户五项能力，#17 为 `.codegraph/` 忽略规则，#18 为 docs/19 题述提案与 handoff 同步文档，#19 为设计态内核前置项，后两者 2026-09-16 合并）。本地后端 `.venv/bin/pytest`（**M9 批 4 后 602 passed / 13 skipped（2026-09-18）；M8 收口基线 518、M9 批 3 后 601，批 4 净增 1（PUT 同图草稿）**；M4 收口时 431，M5–M8 后端净增 87 用例（M5a storage +10、M6 版本化 U46 +4、M5b PG 中断恢复 U43–U45 +5 另 integration 13、M7 任务总线 U47/U48 +13、M10 追踪 U49–U52 +23、M8 卡片 U53/U54 +32）：M5a storage、M6 版本化 U46、M5b PG 中断恢复 U43–U45、M7 任务总线 U47/U48、M10 追踪 U49–U52。M4 收口明细：engine 3 + harness 契约 8 + 三层定位 6 + graph conditions 9 + graph DSL 71（M2 起含 422 locations 侧车 U38 十例） + 编译器/condition/loop/parallel/wait/subgraph/human_approval/http/database/message 通道路由 40 + collaboration 审批 broker 7 + graphs API 4 + 决策 6 + 店铺 8 + NL 15（含 NL 草稿参数尽力校验 U34 七例）+ 退款端到端 8 + Demo API/mock/messages 50（含模板 I15 四例、录制 I16 五例、调试 I17 七例、监控 I18 九例）+ httpapi 通用 HTTP 适配器 24 + database 通用 SQL 适配器 35 + message 进程内消息适配器 21 + 内置模板目录 U27 六例 + 操作录制 U28 二十三例 + 调试会话/暂停状态机 U29 十一例 + 基础监控 U30 二十例 + iam 种子身份/会话/角色矩阵/租户注册表 U31 十七例 + API 鉴权与租户分区 I19 十一例 + L2 模板引用编译期复查 I20 九例 + M4 批 2 L3 跨运行时对拍夹具 `tests/test_l3_fixtures.py` 十一例（参数化，后端重建 raw 图重跑防夹具漂移）；DB/浏览器/店铺 8 个 integration 用例默认跳过；DB/消息/模板/录制/调试/监控/鉴权/作用域新增用例全部 SQLite 内存/进程内/静态常量，零真实外呼），全量 `ATLAS_RUN_INTEGRATION=1`（+`DATABASE_URL`）含真实 Chromium 与店铺平台集成；前端 `cd frontend && pnpm test`（vitest，**M9 批 4 后 396 passed / 2 skipped（34 测试文件，另 2 基准 RUN_BENCH 门控跳过；批 4 新增 release 纯函数 16、releaseApi 端点 10 共 +26；M8 收口为 370/32 文件，M5–M7/M10 前端零改动，M8 新增 cards 7、scope L2 4 共 +11）**；M4 批 2 时基线 336 个单元用例 / 30 个文件；M4 批 1 后 291/27、M3 收口时 243/24、M2 收口时 189/14——扶正迁移删除 dualRun 双跑 339 行等价用例，新增 validation 包 diagnostics/l1/markers/validateGraph 与 scope L2 升级用例，净 −21、文件 +3；M3 增 forms 包、M4 批 1 增 forms 节点表单与 validation/dirty 用例、M4 批 2 增 l3 对拍/engine 调度/validationStore/Problems/嵌套 UiSchema/parallel·subgraph 表单用例）与 `pnpm build`（TS 检查 + Vite 构建）均通过（有 AntD 首包 >500kB 的体积提示，暂不阻塞）。**M9 总收口另过 scripts/dev/m9_smoke.py 32 断言、Router benchmark（10k resolve p50 0.0041ms/p99 0.0450ms）、真实浏览器全链路 0 console error（4 张 docs/assets/m9-*.png），见 Recently shipped 1。**后续按 13 测试用例清单分层：单元 → 集成 → 回放 → 端到端 → AI 评估，上线前过"四道门"。

## How to run

```bash
python3.11 -m venv .venv
.venv/bin/pip install -e ".[dev]"
.venv/bin/pytest                       # 单元测试（integration 标记默认跳过）
.venv/bin/python -c "from atlas.engine.loop import run_loop; print(run_loop('demo', max_steps=2)['status'])"

# FastAPI graphs 接口（W7-W8；前端 dev 经 /api 代理到此端口）
.venv/bin/uvicorn atlas.api.main:app --reload --port 8000

# PostgreSQL + pgvector（本地开发）
docker run -d --name atlas-pg -e POSTGRES_USER=atlas -e POSTGRES_PASSWORD=atlas \
  -e POSTGRES_DB=atlas -p 5432:5432 pgvector/pgvector:pg16
cp .env.example .env                   # 填 DATABASE_URL（本地开发库：atlas/atlas）
docker exec -i atlas-pg psql -U atlas -d atlas < db/migrations/001_enable_pgvector.sql
DATABASE_URL=postgresql+psycopg://atlas:atlas@localhost:5432/atlas \
  ATLAS_RUN_INTEGRATION=1 .venv/bin/pytest -m integration

# Playwright 浏览器（W3-W4 起，浏览器集成测试需要）
.venv/bin/playwright install chromium
ATLAS_RUN_INTEGRATION=1 .venv/bin/pytest -m integration   # 同时含 DB + Chromium 用例

# 前端
cd frontend
pnpm install
pnpm dev                               # http://localhost:5174
pnpm test                              # vitest 单元测试
pnpm build                             # 类型检查 + 生产构建
```

Demo 里程碑（08 文档 7.1）：
- ✅ W1-W2：LangGraph 最小循环、前端编辑器框架、PostgreSQL+pgvector 连接均已跑通（2026-09-13）。
- ✅ W3-W4：Harness 契约层（注册发现/权限审计）+ Web 适配器（三层定位、navigate/click/type/screenshot，真实 Chromium 验证）已跑通（2026-09-13）；视觉语义层（omni-parser 类解析 + vision LLM）为协议注入桩，待 T1 LiteLLM 接入后替换。
- ✅ W5-W6：编辑器核心（拖拽画布、trigger/ai_decision/tool_call 三类节点类型化配置 + 实时校验、全局变量与 `{{路径}}` 引用、Graph JSON 导出，vitest 20 用例）已跑通（2026-09-13）。
- ✅ W7-W8：Graph DSL 契约 + 静态校验、DSL→LangGraph 编译/运行（`graph/` 包）、FastAPI graphs 保存/读取/编译/运行、编辑器一键编译运行（后端 16 新用例 + 浏览器验证）已跑通（2026-09-13）；节点执行器为确定性占位，待 LiteLLM/业务工具替换。
- ✅ W9-W10：电商退款端到端 Demo（LiteLLM 决策 + 规则兜底、shop 退款业务能力、SSE 实时进度、NL 退款草稿、模拟商家控制台，08 §7.3 七条验收全部达成；后端 58 单元 + 前端 23 用例 + 浏览器实测，2026-09-13）。
- API 已可启动：`uvicorn atlas.api.main:app --reload --port 8000`（graphs/SSE/NL/adapters/Demo 店铺接口；前端 dev 端口 5174，`/api` 代理到 8000；模拟控制台 http://localhost:8000/demo/shop）。

## Known issues / 注意点

- **文档口径**：01-08 为规格唯一事实源；10-13 为工程化推导文档，与正文冲突时以 01-08 为准（00 文档已声明）。
- **技术选型已锁定（2026-09-13 T1-T5 收口）**：`pyproject.toml` 可按 10 文档 §2 清单落码（版本号落码时解析）；Demo 不引入 NATS/vLLM/Go 网关/评估层实现，缓做项见 14 文档触发条件。
- **改 Schema 必须同步所有引用位置**（03 契约索引 + 所在文档正文），防漂移（00 治理规则第 3 条）。
- **新决策必须回写**：08 任务迭代计划记录 + 10 ADR 记录 + 本文档"Active work"同步，三者一致。

## Conventions（约定）

- **commit 消息**：英文、`<type>(<scope>): <subject>` 格式；原子提交；不 push（除非用户明确说）；作者保持用户身份，不加 AI co-author。详见 [git-commit-message.md](git-commit-message.md)。
- **AI 行为规范**：见 [AGENTS.md](AGENTS.md)（文档分层 / 治理规则 / 技术约束，新会话必读）。
- **新增文档后**：在本文档"Project documents"区补一行索引；改规格后同步 00 文档治理规则与 08 计划。
- **handoff 归档惯例（照 agent-world）**：主 handoff 只保留「当前状态 + 活跃任务 + 最近 5 个变更」。① Recently shipped 只留最近 5 条；② 编号待办标 ✅ 后不删除，而是把详细过程（实现步骤/commit 链/踩坑/验证数据）整块滚到 `docs/handoff-archive-YYYY-MM-DD.md`，主文件只留一行结论 + commit hash；③ 正在推进的活跃项详情留在主文件不归档。归档文件保留原条目号，主文件里「Recently shipped N」N>5 的交叉引用按归档文件原号定位——因此归档只动尾部、不重编号。
